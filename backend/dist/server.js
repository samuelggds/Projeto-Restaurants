var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// src/config/validateEnv.ts
function asNumber(value, fallback) {
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : fallback;
}
function asBoolean(value, fallback) {
  if (value === void 0 || value.trim() === "") return fallback;
  return value.trim().toLowerCase() === "true";
}
function parseHttpUrl(name, value, errors) {
  try {
    const url = new URL(value);
    if (url.protocol !== "http:" && url.protocol !== "https:") {
      errors.push(`${name} deve usar http:// ou https://.`);
      return null;
    }
    return url;
  } catch {
    errors.push(`${name} deve ser uma URL valida.`);
    return null;
  }
}
function isLocalHostname(hostname) {
  const normalized = hostname.toLowerCase();
  return normalized === "localhost" || normalized === "127.0.0.1" || normalized === "::1";
}
function parsePublicUrl(name, value, errors) {
  const url = parseHttpUrl(name, value, errors);
  if (url && url.protocol !== "https:" && !isLocalHostname(url.hostname)) {
    errors.push(`${name} deve usar https:// em producao, exceto no ambiente local.`);
  }
  return url;
}
function validateDatabaseUrl(value, errors) {
  if (!value) return;
  try {
    const url = new URL(value);
    if (url.protocol !== "postgresql:" && url.protocol !== "postgres:") {
      errors.push("DATABASE_URL deve usar o protocolo postgresql:// ou postgres://.");
    }
    if (/substitua|change[_-]?me|replace[_-]?me/i.test(value)) {
      errors.push("DATABASE_URL nao pode conter credenciais placeholder em producao.");
    }
  } catch {
    errors.push("DATABASE_URL deve ser uma URL PostgreSQL valida.");
  }
}
function requireValue(name, errors) {
  const value = String(process.env[name] || "").trim();
  if (!value) errors.push(`${name} e obrigatoria em producao.`);
  return value;
}
function validatePositiveInteger(name, fallback, errors) {
  const value = asNumber(String(process.env[name] || fallback), fallback);
  if (!Number.isInteger(value) || value <= 0) {
    errors.push(`${name} deve ser um numero inteiro maior que zero.`);
  }
}
function isPlaceholder(value) {
  return /^(change[_-]?me|replace[_-]?me|substitua|secret|password|your[_-]?|seu[_-]?|x{6,})/i.test(
    value.trim()
  );
}
function validateCriticalEnv() {
  const isProduction2 = process.env.NODE_ENV === "production";
  if (!isProduction2) {
    return;
  }
  const errors = [];
  const databaseUrl = requireValue("DATABASE_URL", errors);
  validateDatabaseUrl(databaseUrl, errors);
  const frontendUrl = requireValue("FRONTEND_URL", errors);
  if (frontendUrl) parsePublicUrl("FRONTEND_URL", frontendUrl, errors);
  const backendUrl = requireValue("BACKEND_URL", errors);
  if (backendUrl) parsePublicUrl("BACKEND_URL", backendUrl, errors);
  const corsOrigins = requireValue("CORS_ORIGINS", errors);
  if (corsOrigins) {
    corsOrigins.split(",").map((origin) => origin.trim()).filter(Boolean).forEach((origin) => parsePublicUrl("CORS_ORIGINS", origin, errors));
  }
  const socketCorsOrigins = requireValue("SOCKET_CORS_ORIGINS", errors);
  if (socketCorsOrigins) {
    socketCorsOrigins.split(",").map((origin) => origin.trim()).filter(Boolean).forEach((origin) => parsePublicUrl("SOCKET_CORS_ORIGINS", origin, errors));
  }
  const jwtSecret = String(process.env.JWT_SECRET || "").trim();
  if (jwtSecret.length < 32) {
    errors.push("JWT_SECRET deve ter pelo menos 32 caracteres em producao.");
  } else if (isPlaceholder(jwtSecret)) {
    errors.push("JWT_SECRET nao pode usar um valor placeholder em producao.");
  }
  const jwtRefreshSecret = String(process.env.JWT_REFRESH_SECRET || jwtSecret).trim();
  if (jwtRefreshSecret.length < 32) {
    errors.push("JWT_REFRESH_SECRET deve ter pelo menos 32 caracteres em producao.");
  } else if (isPlaceholder(jwtRefreshSecret)) {
    errors.push("JWT_REFRESH_SECRET nao pode usar um valor placeholder em producao.");
  }
  if (jwtRefreshSecret && jwtRefreshSecret === jwtSecret) {
    errors.push("JWT_REFRESH_SECRET deve ser diferente de JWT_SECRET em producao.");
  }
  const rateLimitMax = asNumber(String(process.env.RATE_LIMIT_MAX_REQUESTS || "300"), 300);
  if (rateLimitMax <= 0) {
    errors.push("RATE_LIMIT_MAX_REQUESTS deve ser maior que zero.");
  }
  const authRateLimitMax = asNumber(String(process.env.AUTH_RATE_LIMIT_MAX_REQUESTS || "50"), 50);
  if (authRateLimitMax <= 0) {
    errors.push("AUTH_RATE_LIMIT_MAX_REQUESTS deve ser maior que zero.");
  }
  const loginLockoutAfterFailures = asNumber(
    String(process.env.LOGIN_LOCKOUT_AFTER_FAILURES || "5"),
    5
  );
  if (loginLockoutAfterFailures < 3) {
    errors.push("LOGIN_LOCKOUT_AFTER_FAILURES deve ser >= 3 em producao.");
  }
  const loginLockoutBaseSeconds = asNumber(
    String(process.env.LOGIN_LOCKOUT_BASE_SECONDS || "60"),
    60
  );
  if (loginLockoutBaseSeconds < 30) {
    errors.push("LOGIN_LOCKOUT_BASE_SECONDS deve ser >= 30 em producao.");
  }
  const mfaRoles = String(process.env.MFA_REQUIRED_ROLES || "ADMIN,SUPER_ADMIN").split(",").map((item) => item.trim()).filter(Boolean);
  if (mfaRoles.length > 0) {
    const jwtMfaSecret = String(process.env.JWT_MFA_SECRET || jwtSecret).trim();
    if (jwtMfaSecret.length < 32) {
      errors.push("JWT_MFA_SECRET deve ter pelo menos 32 caracteres em producao.");
    } else if (isPlaceholder(jwtMfaSecret)) {
      errors.push("JWT_MFA_SECRET nao pode usar um valor placeholder em producao.");
    }
    if (jwtMfaSecret === jwtSecret || jwtMfaSecret === jwtRefreshSecret) {
      errors.push("JWT_MFA_SECRET deve ser diferente dos demais segredos JWT em producao.");
    }
  }
  const allowInsecureStripe = String(process.env.ALLOW_INSECURE_STRIPE_WEBHOOK || "false").trim() === "true";
  if (allowInsecureStripe) {
    errors.push("ALLOW_INSECURE_STRIPE_WEBHOOK nao pode ser true em producao.");
  }
  const allowGlobalFallback = String(process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK || "false").trim() === "true";
  if (allowGlobalFallback) {
    errors.push("ALLOW_GLOBAL_PAYMENT_FALLBACK nao pode ser true em producao multi-tenant.");
  }
  const paymentPinSecret = String(
    process.env.PAYMENT_PIN_SECRET || process.env.JWT_MFA_SECRET || jwtSecret
  ).trim();
  if (paymentPinSecret.length < 32) {
    errors.push("PAYMENT_PIN_SECRET deve ter pelo menos 32 caracteres em producao.");
  } else if (isPlaceholder(paymentPinSecret)) {
    errors.push("PAYMENT_PIN_SECRET nao pode usar um valor placeholder em producao.");
  }
  if (paymentPinSecret === jwtSecret || paymentPinSecret === jwtRefreshSecret || paymentPinSecret === String(process.env.JWT_MFA_SECRET || jwtSecret).trim()) {
    errors.push("PAYMENT_PIN_SECRET deve ser independente dos segredos JWT em producao.");
  }
  const enableTestPaymentWebhook = String(process.env.ENABLE_TEST_PAYMENT_WEBHOOK || "false").trim() === "true";
  if (enableTestPaymentWebhook) {
    errors.push("ENABLE_TEST_PAYMENT_WEBHOOK nao pode ser true em producao.");
  }
  const enableDestructiveCleanup = String(process.env.ENABLE_DESTRUCTIVE_CLEANUP || "false").trim() === "true";
  if (enableDestructiveCleanup) {
    errors.push("ENABLE_DESTRUCTIVE_CLEANUP nao pode ser true em producao.");
  }
  const routingRequired = asBoolean(process.env.ROUTING_REQUIRED, true);
  if (routingRequired) {
    const osrmBaseUrl = requireValue("OSRM_BASE_URL", errors);
    const geocoderBaseUrl = requireValue("GEOCODER_BASE_URL", errors);
    const routingUserAgent = requireValue("ROUTING_USER_AGENT", errors);
    const osrmUrl = osrmBaseUrl ? parseHttpUrl("OSRM_BASE_URL", osrmBaseUrl, errors) : null;
    const geocoderUrl = geocoderBaseUrl ? parseHttpUrl("GEOCODER_BASE_URL", geocoderBaseUrl, errors) : null;
    const publicDemoHosts = /* @__PURE__ */ new Set(["router.project-osrm.org", "nominatim.openstreetmap.org"]);
    if (osrmUrl && publicDemoHosts.has(osrmUrl.hostname.toLowerCase())) {
      errors.push("OSRM_BASE_URL nao pode usar o servidor publico de demonstracao em producao.");
    }
    if (geocoderUrl && publicDemoHosts.has(geocoderUrl.hostname.toLowerCase())) {
      errors.push("GEOCODER_BASE_URL nao pode usar o Nominatim publico em producao.");
    }
    if (routingUserAgent && routingUserAgent === "PizzaIADelivery/1.0") {
      errors.push("ROUTING_USER_AGENT deve incluir um contato real em producao.");
    }
    validatePositiveInteger("ROUTING_REQUEST_TIMEOUT_MS", 4e3, errors);
    validatePositiveInteger("GEOCODER_REQUEST_TIMEOUT_MS", 4e3, errors);
    validatePositiveInteger("ROUTING_CACHE_MAX_ENTRIES", 5e3, errors);
  }
  validatePositiveInteger("DELIVERY_LOCATION_RETENTION_DAYS", 30, errors);
  validatePositiveInteger("READINESS_TIMEOUT_MS", 3e3, errors);
  validatePositiveInteger("SHUTDOWN_TIMEOUT_MS", 1e4, errors);
  validatePositiveInteger("PORT", 3e3, errors);
  if (errors.length) {
    throw new Error(`Falha na validacao de ambiente: ${errors.join(" ")}`);
  }
}
var init_validateEnv = __esm({
  "src/config/validateEnv.ts"() {
  }
});

// src/config/validateEnvOnStartup.ts
var init_validateEnvOnStartup = __esm({
  "src/config/validateEnvOnStartup.ts"() {
    init_validateEnv();
    validateCriticalEnv();
  }
});

// src/config/prisma.ts
import { PrismaClient } from "@prisma/client";
var prisma, prisma_default;
var init_prisma = __esm({
  "src/config/prisma.ts"() {
    prisma = new PrismaClient();
    prisma_default = prisma;
  }
});

// src/modules/auth/repositories/UserRepository.ts
var UserRepository, UserRepository_default;
var init_UserRepository = __esm({
  "src/modules/auth/repositories/UserRepository.ts"() {
    init_prisma();
    UserRepository = class {
      async findByEmail(email, db = prisma_default) {
        const normalizedEmail = String(email || "").trim().toLowerCase();
        if (!normalizedEmail) {
          return null;
        }
        return db.user.findFirst({
          where: {
            email: {
              equals: normalizedEmail,
              mode: "insensitive"
            }
          }
        });
      }
      async findByPhone(phone, db = prisma_default) {
        const normalizedPhone = String(phone || "").replace(/\D/g, "");
        if (!normalizedPhone) {
          return null;
        }
        const users = await db.$queryRaw`
      SELECT *
      FROM "User"
      WHERE regexp_replace(COALESCE("phone", ''), '[^0-9]', '', 'g') = ${normalizedPhone}
      LIMIT 1
    `;
        return users[0] || null;
      }
      async create(data, db = prisma_default) {
        return db.user.create({
          data
        });
      }
      async findById(id, db = prisma_default) {
        return db.user.findUnique({
          where: {
            id: Number(id)
          },
          select: {
            id: true,
            name: true,
            email: true,
            role: true,
            active: true,
            mustChangePassword: true,
            mfaEnabled: true,
            phone: true,
            cpf: true,
            address: true,
            number: true,
            district: true,
            city: true,
            state: true,
            zipCode: true,
            complement: true,
            restaurantId: true,
            avatar: true
          }
        });
      }
      async updateProfile(id, data, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data,
          select: {
            id: true,
            name: true,
            email: true,
            role: true,
            active: true,
            mustChangePassword: true,
            mfaEnabled: true,
            phone: true,
            cpf: true,
            address: true,
            number: true,
            district: true,
            city: true,
            state: true,
            zipCode: true,
            complement: true,
            restaurantId: true,
            avatar: true
          }
        });
      }
      async updatePassword(id, password, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            password,
            mustChangePassword: false
          }
        });
      }
      async updateMfaEnabled(id, mfaEnabled, db = prisma_default) {
        return db.user.update({
          where: { id: Number(id) },
          data: { mfaEnabled },
          select: {
            id: true,
            mfaEnabled: true
          }
        });
      }
      async savePasswordResetCode(id, codeHash, expiresAt, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            resetPasswordCodeHash: codeHash,
            resetPasswordCodeExpiresAt: expiresAt
          }
        });
      }
      async clearPasswordResetCode(id, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            resetPasswordCodeHash: null,
            resetPasswordCodeExpiresAt: null
          }
        });
      }
      async updatePasswordAndClearResetCode(id, password, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            password,
            resetPasswordCodeHash: null,
            resetPasswordCodeExpiresAt: null
          }
        });
      }
      async findByIdWithPassword(id, db = prisma_default) {
        return db.user.findUnique({
          where: {
            id: Number(id)
          }
        });
      }
      async deactivate(id, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            active: false
          }
        });
      }
      async reactivate(id, db = prisma_default) {
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            active: true
          }
        });
      }
    };
    UserRepository_default = new UserRepository();
  }
});

// src/validators/RegisterValidator.ts
import { z } from "zod";
var registerSchema;
var init_RegisterValidator = __esm({
  "src/validators/RegisterValidator.ts"() {
    registerSchema = z.object({
      name: z.string().min(1, "Nome obrigat\xF3rio"),
      email: z.string().min(1, "Email obrigat\xF3rio").email("Email inv\xE1lido"),
      password: z.string().min(6, "Senha deve conter no m\xEDnimo 6 caracteres!"),
      confirmPassword: z.string().min(1, "Confirma\xE7\xE3o de senha obrigat\xF3ria")
    }).refine((data) => data.password === data.confirmPassword, {
      message: "As senhas n\xE3o coincidem!",
      path: ["confirmPassword"]
    });
  }
});

// src/modules/auth/services/RegisterService.ts
import bcrypt from "bcrypt";
var RegisterService, RegisterService_default;
var init_RegisterService = __esm({
  "src/modules/auth/services/RegisterService.ts"() {
    init_UserRepository();
    init_RegisterValidator();
    RegisterService = class {
      async execute({ name, email, password, confirmPassword }) {
        registerSchema.parse({ name, email, password, confirmPassword });
        const normalizedEmail = String(email || "").trim().toLowerCase();
        const userExists = await UserRepository_default.findByEmail(normalizedEmail);
        if (userExists) {
          throw new Error("Este e-mail j\xE1 est\xE1 em uso!");
        }
        const passwordhash = await bcrypt.hash(password, 10);
        const user = await UserRepository_default.create({
          name,
          email: normalizedEmail,
          password: passwordhash
        });
        return user;
      }
    };
    RegisterService_default = new RegisterService();
  }
});

// src/modules/auth/controllers/RegisterController.ts
var RegisterController, RegisterController_default;
var init_RegisterController = __esm({
  "src/modules/auth/controllers/RegisterController.ts"() {
    init_RegisterService();
    RegisterController = class {
      async handle(req, res) {
        try {
          const { name, email, password, confirmPassword } = req.body;
          const user = await RegisterService_default.execute({
            name,
            email,
            password,
            confirmPassword
          });
          return res.status(201).json({ user });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao registrar usuario"
          });
        }
      }
    };
    RegisterController_default = new RegisterController();
  }
});

// src/validators/LoginValidator.ts
import { z as z2 } from "zod";
var loginSchema;
var init_LoginValidator = __esm({
  "src/validators/LoginValidator.ts"() {
    loginSchema = z2.object({
      email: z2.string().min(1, "Email obrigat\xF3rio").email("Email inv\xE1lido"),
      password: z2.string().min(1, "Senha obrigat\xF3ria")
    });
  }
});

// src/modules/auth/services/LoginLockoutService.ts
function normalizeKey(email) {
  return String(email || "").trim().toLowerCase().slice(0, 255);
}
async function clearExpiredStates() {
  const cutoff = new Date(Date.now() - STATE_TTL_MS);
  await prisma_default.loginLockout.deleteMany({
    where: {
      updatedAt: {
        lt: cutoff
      }
    }
  });
}
var BASE_LOCK_SECONDS, MAX_LOCK_SECONDS, LOCKOUT_AFTER_FAILURES, STATE_TTL_MS, LoginLockoutService, LoginLockoutService_default;
var init_LoginLockoutService = __esm({
  "src/modules/auth/services/LoginLockoutService.ts"() {
    init_prisma();
    BASE_LOCK_SECONDS = Number(process.env.LOGIN_LOCKOUT_BASE_SECONDS || 60);
    MAX_LOCK_SECONDS = Number(process.env.LOGIN_LOCKOUT_MAX_SECONDS || 3600);
    LOCKOUT_AFTER_FAILURES = Number(process.env.LOGIN_LOCKOUT_AFTER_FAILURES || 5);
    STATE_TTL_MS = Number(process.env.LOGIN_LOCKOUT_STATE_TTL_MS || 864e5);
    LoginLockoutService = class {
      async check(email) {
        await clearExpiredStates();
        const key = normalizeKey(email);
        if (!key) {
          return { locked: false, waitSeconds: 0 };
        }
        const state = await prisma_default.loginLockout.findUnique({
          where: {
            emailNormalized: key
          },
          select: {
            lockUntil: true
          }
        });
        if (!state) {
          return { locked: false, waitSeconds: 0 };
        }
        const now = Date.now();
        const lockUntilMs = state.lockUntil ? new Date(state.lockUntil).getTime() : 0;
        if (lockUntilMs <= now) {
          return { locked: false, waitSeconds: 0 };
        }
        return {
          locked: true,
          waitSeconds: Math.max(1, Math.ceil((lockUntilMs - now) / 1e3))
        };
      }
      async registerFailure(email) {
        await clearExpiredStates();
        const key = normalizeKey(email);
        if (!key) {
          return {
            locked: false,
            waitSeconds: 0,
            failedAttempts: 0
          };
        }
        const now = Date.now();
        const current = await prisma_default.loginLockout.findUnique({
          where: {
            emailNormalized: key
          }
        });
        const nextFailedAttempts = Number(current?.failedAttempts || 0) + 1;
        if (nextFailedAttempts < LOCKOUT_AFTER_FAILURES) {
          await prisma_default.loginLockout.upsert({
            where: {
              emailNormalized: key
            },
            update: {
              failedAttempts: nextFailedAttempts,
              lockUntil: null
            },
            create: {
              emailNormalized: key,
              failedAttempts: nextFailedAttempts,
              lockUntil: null
            }
          });
          return {
            locked: false,
            waitSeconds: 0,
            failedAttempts: nextFailedAttempts
          };
        }
        const exponent = Math.max(0, nextFailedAttempts - LOCKOUT_AFTER_FAILURES);
        const lockSeconds = Math.min(MAX_LOCK_SECONDS, BASE_LOCK_SECONDS * 2 ** exponent);
        await prisma_default.loginLockout.upsert({
          where: {
            emailNormalized: key
          },
          update: {
            failedAttempts: nextFailedAttempts,
            lockUntil: new Date(now + lockSeconds * 1e3)
          },
          create: {
            emailNormalized: key,
            failedAttempts: nextFailedAttempts,
            lockUntil: new Date(now + lockSeconds * 1e3)
          }
        });
        return {
          locked: true,
          waitSeconds: lockSeconds,
          failedAttempts: nextFailedAttempts
        };
      }
      async registerSuccess(email) {
        const key = normalizeKey(email);
        if (!key) {
          return;
        }
        await prisma_default.loginLockout.deleteMany({
          where: {
            emailNormalized: key
          }
        });
      }
    };
    LoginLockoutService_default = new LoginLockoutService();
  }
});

// src/config/auth.ts
function getJwtSecret() {
  return String(process.env.JWT_SECRET || "").trim();
}
function getJwtExpiresIn() {
  const value = String(process.env.JWT_EXPIRES_IN || "").trim();
  return value || DEFAULT_JWT_EXPIRES_IN;
}
function getJwtRefreshSecret() {
  return String(process.env.JWT_REFRESH_SECRET || "").trim();
}
function getJwtRefreshExpiresIn() {
  const value = String(process.env.JWT_REFRESH_EXPIRES_IN || "").trim();
  return value || DEFAULT_JWT_REFRESH_EXPIRES_IN;
}
function getJwtMfaSecret() {
  return String(process.env.JWT_MFA_SECRET || "").trim();
}
function getJwtMfaExpiresIn() {
  const value = String(process.env.JWT_MFA_EXPIRES_IN || "").trim();
  return value || DEFAULT_JWT_MFA_EXPIRES_IN;
}
var DEFAULT_JWT_EXPIRES_IN, DEFAULT_JWT_REFRESH_EXPIRES_IN, DEFAULT_JWT_MFA_EXPIRES_IN;
var init_auth = __esm({
  "src/config/auth.ts"() {
    DEFAULT_JWT_EXPIRES_IN = "12h";
    DEFAULT_JWT_REFRESH_EXPIRES_IN = "14d";
    DEFAULT_JWT_MFA_EXPIRES_IN = "10m";
  }
});

// src/modules/auth/services/AuthTokenService.ts
import crypto2 from "crypto";
import jwt from "jsonwebtoken";
function getSafeRefreshSecret() {
  const refreshSecret = getJwtRefreshSecret();
  return refreshSecret || getJwtSecret();
}
function normalizePayload(payload) {
  return {
    id: Number(payload.id || 0),
    role: String(payload.role || ""),
    subRole: payload.subRole ?? null,
    restaurantId: payload.restaurantId === null || payload.restaurantId === void 0 ? null : Number(payload.restaurantId)
  };
}
var AuthTokenService, AuthTokenService_default;
var init_AuthTokenService = __esm({
  "src/modules/auth/services/AuthTokenService.ts"() {
    init_prisma();
    init_auth();
    AuthTokenService = class {
      createAccessToken(payload) {
        const normalized = normalizePayload(payload);
        return jwt.sign(normalized, getJwtSecret(), {
          expiresIn: getJwtExpiresIn()
        });
      }
      async createRefreshToken(payload) {
        const normalized = normalizePayload(payload);
        const jti = crypto2.randomUUID();
        const refreshPayload = {
          ...normalized,
          type: "refresh",
          jti
        };
        const refreshToken = jwt.sign(refreshPayload, getSafeRefreshSecret(), {
          expiresIn: getJwtRefreshExpiresIn()
        });
        const decoded = jwt.decode(refreshToken);
        const exp = decoded && typeof decoded !== "string" ? Number(decoded.exp || 0) : 0;
        if (!exp) {
          throw new Error("Falha ao gerar refresh token");
        }
        await prisma_default.authRefreshSession.upsert({
          where: {
            userId: normalized.id
          },
          update: {
            jti,
            expiresAt: new Date(exp * 1e3)
          },
          create: {
            userId: normalized.id,
            jti,
            expiresAt: new Date(exp * 1e3)
          }
        });
        return refreshToken;
      }
      async rotateRefreshToken(refreshToken) {
        const decoded = jwt.verify(refreshToken, getSafeRefreshSecret());
        if (!decoded || typeof decoded === "string") {
          throw new Error("Refresh token invalido");
        }
        const userId = Number(decoded.id || 0);
        const role = String(decoded.role || "");
        const subRole = decoded.subRole === null || decoded.subRole === void 0 ? null : String(decoded.subRole);
        const restaurantId = decoded.restaurantId === null || decoded.restaurantId === void 0 ? null : Number(decoded.restaurantId);
        const jti = String(decoded.jti || "").trim();
        const tokenType = String(decoded.type || "").trim();
        if (!Number.isInteger(userId) || userId <= 0 || !role || !jti) {
          throw new Error("Refresh token invalido");
        }
        if (tokenType !== "refresh") {
          throw new Error("Refresh token invalido");
        }
        const session = await prisma_default.authRefreshSession.findUnique({
          where: {
            userId
          },
          select: {
            jti: true,
            expiresAt: true
          }
        });
        const latestJti = String(session?.jti || "");
        if (!latestJti || latestJti !== jti) {
          throw new Error("Refresh token expirado");
        }
        const expiresAt = session?.expiresAt ? new Date(session.expiresAt) : null;
        if (!expiresAt || expiresAt.getTime() <= Date.now()) {
          throw new Error("Refresh token expirado");
        }
        const payload = {
          id: userId,
          role,
          subRole,
          restaurantId
        };
        const accessToken = this.createAccessToken(payload);
        const nextRefreshToken = await this.createRefreshToken(payload);
        return {
          accessToken,
          refreshToken: nextRefreshToken
        };
      }
      async revokeRefreshToken(refreshToken) {
        const decoded = jwt.verify(refreshToken, getSafeRefreshSecret());
        if (!decoded || typeof decoded === "string") {
          throw new Error("Refresh token invalido");
        }
        const userId = Number(decoded.id || 0);
        const jti = String(decoded.jti || "").trim();
        if (!Number.isInteger(userId) || userId <= 0 || !jti) {
          throw new Error("Refresh token invalido");
        }
        await prisma_default.authRefreshSession.deleteMany({
          where: {
            userId,
            jti
          }
        });
      }
    };
    AuthTokenService_default = new AuthTokenService();
  }
});

// src/modules/auth/services/LoginMfaService.ts
import bcrypt2 from "bcrypt";
import crypto3 from "crypto";
import jwt2 from "jsonwebtoken";
import nodemailer from "nodemailer";
import { UserRole } from "@prisma/client";
function createTransporter() {
  const smtpHost2 = String(process.env.SMTP_HOST || "").trim();
  const smtpPort2 = Number(process.env.SMTP_PORT || 587);
  const smtpSecure2 = String(process.env.SMTP_SECURE || "false") === "true";
  const smtpAuthType = String(process.env.SMTP_AUTH_TYPE || "basic").trim().toLowerCase();
  const smtpUser2 = String(process.env.SMTP_USER || "").trim();
  const smtpPass2 = String(process.env.SMTP_PASS || "").trim();
  const smtpClientId = String(process.env.SMTP_CLIENT_ID || "").trim();
  const smtpClientSecret = String(process.env.SMTP_CLIENT_SECRET || "").trim();
  const smtpRefreshToken = String(process.env.SMTP_REFRESH_TOKEN || "").trim();
  const smtpAccessToken = String(process.env.SMTP_ACCESS_TOKEN || "").trim();
  if (!smtpHost2 || !smtpPort2 || !smtpUser2) {
    return null;
  }
  if (smtpAuthType === "oauth2") {
    if (!smtpClientId || !smtpClientSecret || !smtpRefreshToken) {
      return null;
    }
    return nodemailer.createTransport({
      host: smtpHost2,
      port: smtpPort2,
      secure: smtpSecure2,
      requireTLS: true,
      auth: {
        type: "OAuth2",
        user: smtpUser2,
        clientId: smtpClientId,
        clientSecret: smtpClientSecret,
        refreshToken: smtpRefreshToken,
        accessToken: smtpAccessToken || void 0
      }
    });
  }
  if (!smtpPass2) {
    return null;
  }
  return nodemailer.createTransport({
    host: smtpHost2,
    port: smtpPort2,
    secure: smtpSecure2,
    requireTLS: true,
    auth: {
      user: smtpUser2,
      pass: smtpPass2
    }
  });
}
function isBasicAuthDisabledError(error2) {
  const message = error2 instanceof Error ? error2.message : String(error2 || "");
  const normalized = message.toLowerCase();
  return normalized.includes("535") && normalized.includes("basic authentication is disabled");
}
function getMfaSecret() {
  return getJwtMfaSecret() || getJwtSecret();
}
function getRequiredMfaRoles() {
  const rawEnvValue = process.env.MFA_REQUIRED_ROLES;
  const envValue = rawEnvValue === void 0 ? `${UserRole.ADMIN},${UserRole.SUPER_ADMIN}` : String(rawEnvValue);
  return new Set(
    envValue.split(",").map((item) => item.trim().toUpperCase()).filter(Boolean)
  );
}
function requiresMfa(user) {
  return Boolean(user.mfaEnabled) || getRequiredMfaRoles().has(
    String(user.role || "").trim().toUpperCase()
  );
}
function mapUser(user) {
  return {
    id: user.id,
    name: user.name,
    email: user.email,
    role: user.role,
    subRole: user.subRole ?? null,
    active: user.active,
    mustChangePassword: user.mustChangePassword,
    phone: user.phone,
    address: user.address,
    number: user.number,
    district: user.district,
    city: user.city,
    state: user.state,
    zipCode: user.zipCode,
    complement: user.complement,
    avatar: user.avatar,
    restaurantId: user.restaurantId,
    mfaEnabled: Boolean(user.mfaEnabled)
  };
}
var LoginMfaService, LoginMfaService_default;
var init_LoginMfaService = __esm({
  "src/modules/auth/services/LoginMfaService.ts"() {
    init_prisma();
    init_auth();
    init_AuthTokenService();
    init_UserRepository();
    LoginMfaService = class {
      async beginIfRequired(user) {
        if (!requiresMfa(user)) {
          return null;
        }
        await prisma_default.authMfaChallenge.deleteMany({
          where: {
            expiresAt: {
              lt: /* @__PURE__ */ new Date()
            }
          }
        });
        const code = String(crypto3.randomInt(1e5, 1e6));
        const codeHash = await bcrypt2.hash(code, 10);
        const ttlMinutes = Number(process.env.MFA_CODE_TTL_MIN || 10);
        const expiresAt = new Date(Date.now() + ttlMinutes * 60 * 1e3);
        await prisma_default.authMfaChallenge.upsert({
          where: {
            userId: Number(user.id)
          },
          update: {
            codeHash,
            expiresAt
          },
          create: {
            userId: Number(user.id),
            codeHash,
            expiresAt
          }
        });
        const token = jwt2.sign(
          {
            type: "login_mfa",
            userId: Number(user.id)
          },
          getMfaSecret(),
          {
            expiresIn: getJwtMfaExpiresIn()
          }
        );
        const transporter = createTransporter();
        if (transporter) {
          const from = String(process.env.ALERT_EMAIL_FROM || process.env.SMTP_USER || "").trim() || "no-reply@pizzaia.local";
          try {
            await transporter.sendMail({
              from,
              to: user.email,
              subject: "Codigo de verificacao de login - Pizza IA",
              text: `Seu codigo de verificacao e: ${code}. Ele expira em ${ttlMinutes} minutos.`
            });
          } catch (error2) {
            if (process.env.NODE_ENV !== "production") {
              console.warn(
                `[login-2fa] Falha no SMTP em desenvolvimento. Codigo para ${user.email}: ${code}`
              );
              return {
                mfaRequired: true,
                mfaToken: token,
                message: "Codigo de verificacao gerado (SMTP indisponivel em desenvolvimento)."
              };
            }
            if (isBasicAuthDisabledError(error2)) {
              throw new Error(
                "Falha no SMTP: o provedor bloqueou login por usuario/senha (basic auth). Configure SMTP_AUTH_TYPE=oauth2 com credenciais OAuth2 ou use app password."
              );
            }
            throw error2;
          }
        } else {
          if (process.env.NODE_ENV === "production") {
            throw new Error(
              "Falha no SMTP: configure SMTP_HOST, SMTP_PORT, SMTP_USER e credenciais validas para enviar o codigo 2FA por e-mail."
            );
          }
          console.warn(`[login-2fa] SMTP nao configurado. Codigo para ${user.email}: ${code}`);
        }
        return {
          mfaRequired: true,
          mfaToken: token,
          message: "Codigo de verificacao enviado para o e-mail cadastrado."
        };
      }
      async verifyAndIssueTokens({ mfaToken, code }) {
        const rawToken = String(mfaToken || "").trim();
        const rawCode = String(code || "").trim();
        if (!rawToken || !rawCode) {
          throw new Error("Token e codigo de verificacao sao obrigatorios");
        }
        const decoded = jwt2.verify(rawToken, getMfaSecret());
        if (!decoded || typeof decoded === "string") {
          throw new Error("Token de verificacao invalido");
        }
        const tokenType = String(decoded.type || "").trim();
        const userId = Number(decoded.userId || 0);
        if (tokenType !== "login_mfa" || !Number.isInteger(userId) || userId <= 0) {
          throw new Error("Token de verificacao invalido");
        }
        const challenge = await prisma_default.authMfaChallenge.findUnique({
          where: {
            userId
          }
        });
        if (!challenge || new Date(challenge.expiresAt).getTime() <= Date.now()) {
          throw new Error("Codigo de verificacao expirado");
        }
        const validCode = await bcrypt2.compare(rawCode, challenge.codeHash);
        if (!validCode) {
          throw new Error("Codigo de verificacao invalido");
        }
        await prisma_default.authMfaChallenge.deleteMany({
          where: {
            userId
          }
        });
        const user = await UserRepository_default.findByIdWithPassword(userId);
        if (!user || !user.active) {
          throw new Error("Conta desativada. Reative sua conta para continuar.");
        }
        const tokenPayload = {
          id: user.id,
          role: user.role,
          subRole: user.subRole ?? null,
          restaurantId: user.restaurantId
        };
        const token = AuthTokenService_default.createAccessToken(tokenPayload);
        const refreshToken = await AuthTokenService_default.createRefreshToken(tokenPayload);
        return {
          user: mapUser(user),
          token,
          refreshToken
        };
      }
    };
    LoginMfaService_default = new LoginMfaService();
  }
});

// src/modules/auth/services/LoginService.ts
import bcrypt3 from "bcrypt";
var LoginService, LoginService_default;
var init_LoginService = __esm({
  "src/modules/auth/services/LoginService.ts"() {
    init_UserRepository();
    init_LoginValidator();
    init_LoginLockoutService();
    init_AuthTokenService();
    init_LoginMfaService();
    LoginService = class {
      async execute({ email, password }) {
        const normalizedEmail = String(email || "").trim().toLowerCase();
        const lockStatus = await LoginLockoutService_default.check(normalizedEmail);
        if (lockStatus.locked) {
          throw new Error(`Muitas tentativas de login. Tente novamente em ${lockStatus.waitSeconds}s.`);
        }
        try {
          loginSchema.parse({ email, password });
        } catch (_err) {
          throw new Error("Dados inv\xE1lidos");
        }
        const user = await UserRepository_default.findByEmail(normalizedEmail);
        if (!user) {
          await LoginLockoutService_default.registerFailure(normalizedEmail);
          throw new Error("Email ou senha inv\xE1lidos!");
        }
        const passwordMatch = await bcrypt3.compare(password, user.password);
        if (!passwordMatch) {
          const failure = await LoginLockoutService_default.registerFailure(normalizedEmail);
          if (failure.locked) {
            throw new Error(`Muitas tentativas de login. Tente novamente em ${failure.waitSeconds}s.`);
          }
          throw new Error("Senha incorreta!");
        }
        if (!user.active) {
          await LoginLockoutService_default.registerFailure(normalizedEmail);
          throw new Error("Conta desativada. Reative sua conta para continuar.");
        }
        await LoginLockoutService_default.registerSuccess(normalizedEmail);
        const mfaChallenge = await LoginMfaService_default.beginIfRequired(user);
        if (mfaChallenge) {
          return mfaChallenge;
        }
        const tokenPayload = {
          id: user.id,
          role: user.role,
          subRole: user.subRole ?? null,
          restaurantId: user.restaurantId
        };
        const token = AuthTokenService_default.createAccessToken(tokenPayload);
        const refreshToken = await AuthTokenService_default.createRefreshToken(tokenPayload);
        return {
          user: {
            id: user.id,
            name: user.name,
            email: user.email,
            role: user.role,
            subRole: user.subRole ?? null,
            active: user.active,
            mustChangePassword: user.mustChangePassword,
            mfaEnabled: user.mfaEnabled,
            phone: user.phone,
            address: user.address,
            number: user.number,
            district: user.district,
            city: user.city,
            state: user.state,
            zipCode: user.zipCode,
            complement: user.complement,
            restaurantId: user.restaurantId,
            avatar: user.avatar
          },
          token,
          refreshToken
        };
      }
    };
    LoginService_default = new LoginService();
  }
});

// src/modules/auth/controllers/LoginController.ts
var LoginController, LoginController_default;
var init_LoginController = __esm({
  "src/modules/auth/controllers/LoginController.ts"() {
    init_LoginService();
    LoginController = class {
      async handle(req, res) {
        try {
          const { email, password } = req.body;
          const result = await LoginService_default.execute({
            email,
            password
          });
          return res.json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao fazer login"
          });
        }
      }
    };
    LoginController_default = new LoginController();
  }
});

// src/modules/auth/services/GoogleAuthService.ts
import bcrypt4 from "bcrypt";
import { OAuth2Client } from "google-auth-library";
import { UserRole as UserRole2 } from "@prisma/client";
function getSafeNameFromEmail(email) {
  const username = String(email || "").split("@")[0]?.trim();
  return username || "Cliente";
}
function parseGoogleClientIds() {
  const singleClientId = String(process.env.GOOGLE_CLIENT_ID || "").trim();
  const listFromEnv = String(process.env.GOOGLE_CLIENT_IDS || "").split(",").map((item) => item.trim()).filter(Boolean);
  const merged = [singleClientId, ...listFromEnv].filter(Boolean);
  return Array.from(new Set(merged));
}
var GoogleAuthService, GoogleAuthService_default;
var init_GoogleAuthService = __esm({
  "src/modules/auth/services/GoogleAuthService.ts"() {
    init_UserRepository();
    init_AuthTokenService();
    init_LoginMfaService();
    GoogleAuthService = class {
      async execute({ idToken }) {
        if (!idToken) {
          throw new Error("Token do Google n\xE3o informado");
        }
        const googleClientIds = parseGoogleClientIds();
        const googleClientId = googleClientIds[0] || "";
        if (!googleClientId || googleClientIds.length === 0) {
          throw new Error(
            "Login com Google indispon\xEDvel. Configure GOOGLE_CLIENT_ID (ou GOOGLE_CLIENT_IDS) no backend."
          );
        }
        const client = new OAuth2Client(googleClientId);
        const ticket = await client.verifyIdToken({
          idToken,
          audience: googleClientIds
        });
        const payload = ticket.getPayload();
        const email = payload?.email;
        if (!email || payload?.email_verified !== true) {
          throw new Error("N\xE3o foi poss\xEDvel validar sua conta Google");
        }
        let user = await UserRepository_default.findByEmail(email);
        if (!user) {
          const generatedPassword = `google:${payload?.sub || email}:${Date.now()}`;
          const passwordHash = await bcrypt4.hash(generatedPassword, 10);
          user = await UserRepository_default.create({
            name: payload?.name || getSafeNameFromEmail(email),
            email,
            password: passwordHash,
            role: UserRole2.CLIENTE
          });
        }
        if (!user.active) {
          throw new Error("Conta desativada. Reative sua conta para continuar.");
        }
        const mfaChallenge = await LoginMfaService_default.beginIfRequired(user);
        if (mfaChallenge) {
          return mfaChallenge;
        }
        const tokenPayload = {
          id: user.id,
          role: user.role,
          restaurantId: user.restaurantId
        };
        const token = AuthTokenService_default.createAccessToken(tokenPayload);
        const refreshToken = await AuthTokenService_default.createRefreshToken(tokenPayload);
        return {
          user: {
            id: user.id,
            name: user.name,
            email: user.email,
            role: user.role,
            active: user.active,
            mustChangePassword: user.mustChangePassword,
            mfaEnabled: user.mfaEnabled,
            phone: user.phone,
            address: user.address,
            number: user.number,
            district: user.district,
            city: user.city,
            state: user.state,
            zipCode: user.zipCode,
            complement: user.complement,
            restaurantId: user.restaurantId
          },
          token,
          refreshToken
        };
      }
    };
    GoogleAuthService_default = new GoogleAuthService();
  }
});

// src/modules/auth/controllers/GoogleAuthController.ts
var GoogleAuthController, GoogleAuthController_default;
var init_GoogleAuthController = __esm({
  "src/modules/auth/controllers/GoogleAuthController.ts"() {
    init_GoogleAuthService();
    GoogleAuthController = class {
      async handle(req, res) {
        try {
          const { idToken } = req.body;
          const result = await GoogleAuthService_default.execute({ idToken });
          return res.json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro no login Google"
          });
        }
      }
    };
    GoogleAuthController_default = new GoogleAuthController();
  }
});

// src/modules/auth/services/GetProfileService.ts
var GetProfileService, GetProfileService_default;
var init_GetProfileService = __esm({
  "src/modules/auth/services/GetProfileService.ts"() {
    init_UserRepository();
    GetProfileService = class {
      async execute(userId) {
        return UserRepository_default.findById(userId);
      }
    };
    GetProfileService_default = new GetProfileService();
  }
});

// src/modules/auth/controllers/MeController.ts
var MeController, MeController_default;
var init_MeController = __esm({
  "src/modules/auth/controllers/MeController.ts"() {
    init_GetProfileService();
    MeController = class {
      async handle(req, res) {
        try {
          const user = await GetProfileService_default.execute(req.user.id);
          return res.status(200).json(user);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao buscar perfil"
          });
        }
      }
    };
    MeController_default = new MeController();
  }
});

// src/modules/billing/utils/dateUtils.ts
function addDays(date, days) {
  const result = new Date(date);
  result.setDate(result.getDate() + days);
  return result;
}
function addBusinessDays(date, businessDays) {
  const result = new Date(date);
  let added = 0;
  while (added < businessDays) {
    result.setDate(result.getDate() + 1);
    const dayOfWeek = result.getDay();
    const isWeekend = dayOfWeek === 0 || dayOfWeek === 6;
    if (!isWeekend) {
      added += 1;
    }
  }
  return result;
}
var init_dateUtils = __esm({
  "src/modules/billing/utils/dateUtils.ts"() {
  }
});

// src/modules/billing/utils/billingRules.ts
function getGraceLimitDate(dueDate) {
  return addBusinessDays(dueDate, 5);
}
function isInvoiceBlocking(invoice, now = /* @__PURE__ */ new Date()) {
  if (invoice.status === "ATRASADO") {
    return true;
  }
  if (invoice.status !== "PENDENTE") {
    return false;
  }
  const graceLimitDate = getGraceLimitDate(invoice.dueDate);
  return now > graceLimitDate;
}
function hasBlockingInvoices(invoices = [], now = /* @__PURE__ */ new Date()) {
  return invoices.some((invoice) => isInvoiceBlocking(invoice, now));
}
var init_billingRules = __esm({
  "src/modules/billing/utils/billingRules.ts"() {
    init_dateUtils();
  }
});

// src/middlewares/billingMiddleware.ts
import { InvoiceStatus } from "@prisma/client";
async function billingMiddleware(req, res, next) {
  try {
    if (checkedRequests.has(req)) {
      return next();
    }
    checkedRequests.add(req);
    const restaurantId = req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null;
    if (String(req.user?.role || "").toUpperCase() === "SUPER_ADMIN" || !restaurantId) {
      return next();
    }
    const openInvoices = await prisma_default.invoice.findMany({
      where: {
        restaurantId: Number(restaurantId),
        status: {
          in: [InvoiceStatus.PENDENTE, InvoiceStatus.ATRASADO]
        }
      },
      orderBy: {
        dueDate: "asc"
      }
    });
    if (!openInvoices.length) {
      return next();
    }
    const now = /* @__PURE__ */ new Date();
    const shouldBlock = hasBlockingInvoices(openInvoices, now);
    if (shouldBlock) {
      const blockingInvoices = openInvoices.filter((invoice) => isInvoiceBlocking(invoice, now));
      const blockingInvoice = blockingInvoices.find((invoice) => Boolean(invoice.paymentLink)) || blockingInvoices[0] || null;
      const pendingToOverdue = openInvoices.filter((invoice) => invoice.status === InvoiceStatus.PENDENTE).filter((invoice) => isInvoiceBlocking(invoice, now));
      if (pendingToOverdue.length) {
        await prisma_default.invoice.updateMany({
          where: {
            id: {
              in: pendingToOverdue.map((invoice) => invoice.id)
            }
          },
          data: {
            status: InvoiceStatus.ATRASADO
          }
        });
      }
      const subscription = await prisma_default.subscription.findUnique({
        where: {
          restaurantId: Number(restaurantId)
        }
      });
      if (subscription) {
        await prisma_default.subscription.update({
          where: { id: subscription.id },
          data: { status: "EXPIRADA" }
        });
      }
      await prisma_default.restaurant.update({
        where: { id: Number(restaurantId) },
        data: { active: false }
      });
      return res.status(403).json({
        code: "BILLING_BLOCKED",
        blocked: true,
        error: "Restaurante bloqueado por inadimpl\xEAncia",
        invoiceId: blockingInvoice?.id ?? null,
        paymentLink: blockingInvoice?.paymentLink ?? null,
        dueDate: blockingInvoice?.dueDate ?? null
      });
    }
    return next();
  } catch (_error) {
    return res.status(500).json({
      error: "Erro ao validar cobran\xE7a"
    });
  }
}
var checkedRequests;
var init_billingMiddleware = __esm({
  "src/middlewares/billingMiddleware.ts"() {
    init_prisma();
    init_billingRules();
    checkedRequests = /* @__PURE__ */ new WeakSet();
  }
});

// src/middlewares/authMiddleware.ts
import jwt3 from "jsonwebtoken";
function canBypassBillingCheck(req) {
  const role = String(req.user?.role || "").toUpperCase();
  if (role === "SUPER_ADMIN") {
    return true;
  }
  if (req.baseUrl === "/auth" && req.path === "/me") {
    return true;
  }
  return req.baseUrl === "/billing" || req.path.startsWith("/billing/");
}
function authMiddleware(req, res, next) {
  const authHeader = req.headers.authorization;
  if (!authHeader) {
    return res.status(401).json({ error: "Token n\xE3o informado!" });
  }
  const [, token] = authHeader.split(" ");
  try {
    const decoded = jwt3.verify(token, process.env.JWT_SECRET);
    if (typeof decoded === "string") {
      return res.status(401).json({ error: "Token inv\xE1lido!" });
    }
    req.user = {
      id: Number(decoded.id || 0),
      role: String(decoded.role || ""),
      subRole: decoded.subRole === null || decoded.subRole === void 0 ? null : String(decoded.subRole),
      restaurantId: decoded.restaurantId === null || decoded.restaurantId === void 0 ? null : Number(decoded.restaurantId),
      email: decoded.email === null || decoded.email === void 0 ? null : String(decoded.email)
    };
    if (canBypassBillingCheck(req)) {
      return next();
    }
    return billingMiddleware(req, res, next);
  } catch (_error) {
    return res.status(401).json({ error: "Token inv\xE1lido!" });
  }
}
var init_authMiddleware = __esm({
  "src/middlewares/authMiddleware.ts"() {
    init_billingMiddleware();
  }
});

// src/modules/auth/services/UpdatePasswordService.ts
import bcrypt5 from "bcrypt";
var UpdatePasswordService, UpdatePasswordService_default;
var init_UpdatePasswordService = __esm({
  "src/modules/auth/services/UpdatePasswordService.ts"() {
    init_UserRepository();
    UpdatePasswordService = class {
      async execute(userId, oldPassword, newPassword) {
        if (typeof oldPassword !== "string" || typeof newPassword !== "string" || !oldPassword || newPassword.length < 6) {
          throw new Error("Informe a senha atual e uma nova senha com ao menos 6 caracteres");
        }
        const user = await UserRepository_default.findByIdWithPassword(userId);
        if (!user) {
          throw new Error("Usu\xE1rio n\xE3o encontrado!");
        }
        const passwordCompare = await bcrypt5.compare(oldPassword, user.password);
        if (!passwordCompare) {
          throw new Error("Senha atual incorreta!");
        }
        const hashPassword = await bcrypt5.hash(newPassword, 10);
        return UserRepository_default.updatePassword(userId, hashPassword);
      }
    };
    UpdatePasswordService_default = new UpdatePasswordService();
  }
});

// src/modules/auth/controllers/UpdatePasswordController.ts
var UpdatePasswordController, UpdatePasswordController_default;
var init_UpdatePasswordController = __esm({
  "src/modules/auth/controllers/UpdatePasswordController.ts"() {
    init_UpdatePasswordService();
    UpdatePasswordController = class {
      async handle(req, res) {
        try {
          const userId = req.user.id;
          const { oldPassword, newPassword } = req.body;
          await UpdatePasswordService_default.execute(userId, oldPassword, newPassword);
          return res.status(200).json({ message: "Senha atualizada com sucesso!" });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar senha"
          });
        }
      }
    };
    UpdatePasswordController_default = new UpdatePasswordController();
  }
});

// src/modules/auth/services/UpdateProfileService.ts
var UpdateProfileService, UpdateProfileService_default;
var init_UpdateProfileService = __esm({
  "src/modules/auth/services/UpdateProfileService.ts"() {
    init_UserRepository();
    UpdateProfileService = class {
      async execute(userId, profileData) {
        const currentUser = await UserRepository_default.findById(userId);
        if (!currentUser) {
          throw new Error("Usu\xE1rio n\xE3o encontrado!");
        }
        const hasField = (field) => Object.prototype.hasOwnProperty.call(profileData, field);
        const nextEmail = hasField("email") ? String(profileData.email || "").trim().toLowerCase() : currentUser.email;
        if (nextEmail && nextEmail !== currentUser.email) {
          const emailInUse = await UserRepository_default.findByEmail(nextEmail);
          if (emailInUse && Number(emailInUse.id) !== Number(userId)) {
            throw new Error("Este e-mail j\xE1 est\xE1 em uso!");
          }
        }
        const updates = {};
        if (hasField("name")) updates.name = String(profileData.name || "").trim();
        if (hasField("email")) updates.email = nextEmail;
        if (hasField("phone")) updates.phone = String(profileData.phone || "").trim() || null;
        if (hasField("cpf")) updates.cpf = String(profileData.cpf || "").replace(/\D/g, "") || null;
        if (hasField("address")) updates.address = String(profileData.address || "").trim() || null;
        if (hasField("number")) updates.number = String(profileData.number || "").trim() || null;
        if (hasField("district")) updates.district = String(profileData.district || "").trim() || null;
        if (hasField("city")) updates.city = String(profileData.city || "").trim() || null;
        if (hasField("state")) updates.state = String(profileData.state || "").trim() || null;
        if (hasField("zipCode")) updates.zipCode = String(profileData.zipCode || "").trim() || null;
        if (hasField("complement"))
          updates.complement = String(profileData.complement || "").trim() || null;
        if (hasField("avatar")) updates.avatar = String(profileData.avatar || "").trim() || null;
        return UserRepository_default.updateProfile(userId, updates);
      }
    };
    UpdateProfileService_default = new UpdateProfileService();
  }
});

// src/modules/auth/controllers/UpdateProfileController.ts
var UpdateProfileController, UpdateProfileController_default;
var init_UpdateProfileController = __esm({
  "src/modules/auth/controllers/UpdateProfileController.ts"() {
    init_UpdateProfileService();
    UpdateProfileController = class {
      async handle(req, res) {
        try {
          const userId = req.user.id;
          const profileData = req.body;
          const user = await UpdateProfileService_default.execute(userId, profileData);
          return res.status(200).json(user);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar perfil"
          });
        }
      }
    };
    UpdateProfileController_default = new UpdateProfileController();
  }
});

// src/modules/auth/services/DeactivateUserService.ts
var DeactivateUserService, DeactivateUserService_default;
var init_DeactivateUserService = __esm({
  "src/modules/auth/services/DeactivateUserService.ts"() {
    init_UserRepository();
    DeactivateUserService = class {
      async execute(userId) {
        const user = await UserRepository_default.findById(userId);
        if (!user) {
          throw new Error("Usu\xE1rio n\xE3o encontrado!");
        }
        return UserRepository_default.deactivate(userId);
      }
    };
    DeactivateUserService_default = new DeactivateUserService();
  }
});

// src/modules/auth/controllers/DeactivateUserController.ts
var DeactivateUserController, DeactivateUserController_default;
var init_DeactivateUserController = __esm({
  "src/modules/auth/controllers/DeactivateUserController.ts"() {
    init_DeactivateUserService();
    DeactivateUserController = class {
      async handle(req, res) {
        try {
          const userId = req.user.id;
          const user = await DeactivateUserService_default.execute(userId);
          return res.json(user);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao desativar usuario"
          });
        }
      }
    };
    DeactivateUserController_default = new DeactivateUserController();
  }
});

// src/modules/auth/services/ReactivateUserService.ts
var ReactivateUserService, ReactivateUserService_default;
var init_ReactivateUserService = __esm({
  "src/modules/auth/services/ReactivateUserService.ts"() {
    init_UserRepository();
    ReactivateUserService = class {
      async execute(userId) {
        const user = await UserRepository_default.findById(userId);
        if (!user) {
          throw new Error("Usu\xE1rio n\xE3o encontrado!");
        }
        if (user.active) {
          throw new Error("A conta ja est\xE1 ativa!");
        }
        return UserRepository_default.reactivate(userId);
      }
    };
    ReactivateUserService_default = new ReactivateUserService();
  }
});

// src/modules/auth/controllers/ReactivateUserController.ts
var ReactivateUserController, ReactivateUserController_default;
var init_ReactivateUserController = __esm({
  "src/modules/auth/controllers/ReactivateUserController.ts"() {
    init_ReactivateUserService();
    ReactivateUserController = class {
      async handle(req, res) {
        try {
          const userId = req.user.id;
          const user = await ReactivateUserService_default.execute(userId);
          return res.json(user);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao reativar usuario"
          });
        }
      }
    };
    ReactivateUserController_default = new ReactivateUserController();
  }
});

// src/validators/ForgotPasswordValidator.ts
import { z as z3 } from "zod";
var forgotPasswordSchema, resetPasswordSchema;
var init_ForgotPasswordValidator = __esm({
  "src/validators/ForgotPasswordValidator.ts"() {
    forgotPasswordSchema = z3.object({
      email: z3.string().trim().email("Email invalido").optional(),
      phone: z3.string().trim().min(8, "Telefone invalido").optional()
    }).refine((data) => Boolean(data.email || data.phone), {
      message: "Informe e-mail ou telefone",
      path: ["email"]
    });
    resetPasswordSchema = z3.object({
      email: z3.string().trim().email("Email invalido").optional(),
      phone: z3.string().trim().min(8, "Telefone invalido").optional(),
      code: z3.string().trim().regex(/^\d{6}$/, "Codigo invalido"),
      newPassword: z3.string().min(6, "A nova senha deve ter pelo menos 6 caracteres"),
      confirmPassword: z3.string().min(6, "Confirme a nova senha")
    }).refine((data) => Boolean(data.email || data.phone), {
      message: "Informe e-mail ou telefone",
      path: ["email"]
    }).refine((data) => data.newPassword === data.confirmPassword, {
      message: "As senhas nao conferem",
      path: ["confirmPassword"]
    });
  }
});

// src/modules/auth/services/RequestPasswordResetService.ts
import bcrypt6 from "bcrypt";
import crypto4 from "crypto";
import nodemailer2 from "nodemailer";
function createTransporter2() {
  const smtpHost2 = String(process.env.SMTP_HOST || "").trim();
  const smtpPort2 = Number(process.env.SMTP_PORT || 587);
  const smtpSecure2 = String(process.env.SMTP_SECURE || "false") === "true";
  const smtpAuthType = String(process.env.SMTP_AUTH_TYPE || "basic").trim().toLowerCase();
  const smtpUser2 = String(process.env.SMTP_USER || "").trim();
  const smtpPass2 = String(process.env.SMTP_PASS || "").trim();
  const smtpClientId = String(process.env.SMTP_CLIENT_ID || "").trim();
  const smtpClientSecret = String(process.env.SMTP_CLIENT_SECRET || "").trim();
  const smtpRefreshToken = String(process.env.SMTP_REFRESH_TOKEN || "").trim();
  const smtpAccessToken = String(process.env.SMTP_ACCESS_TOKEN || "").trim();
  if (!smtpHost2 || !smtpPort2 || !smtpUser2) {
    return null;
  }
  if (smtpAuthType === "oauth2") {
    if (!smtpClientId || !smtpClientSecret || !smtpRefreshToken) {
      return null;
    }
    return nodemailer2.createTransport({
      host: smtpHost2,
      port: smtpPort2,
      secure: smtpSecure2,
      requireTLS: true,
      auth: {
        type: "OAuth2",
        user: smtpUser2,
        clientId: smtpClientId,
        clientSecret: smtpClientSecret,
        refreshToken: smtpRefreshToken,
        accessToken: smtpAccessToken || void 0
      }
    });
  }
  if (!smtpPass2) {
    return null;
  }
  return nodemailer2.createTransport({
    host: smtpHost2,
    port: smtpPort2,
    secure: smtpSecure2,
    requireTLS: true,
    auth: {
      user: smtpUser2,
      pass: smtpPass2
    }
  });
}
function isBasicAuthDisabledError2(error2) {
  const message = error2 instanceof Error ? error2.message : String(error2 || "");
  const normalized = message.toLowerCase();
  return normalized.includes("535") && normalized.includes("basic authentication is disabled");
}
function canLogPasswordResetCode(nodeEnv = process.env.NODE_ENV) {
  return nodeEnv !== "production";
}
var RequestPasswordResetService, RequestPasswordResetService_default;
var init_RequestPasswordResetService = __esm({
  "src/modules/auth/services/RequestPasswordResetService.ts"() {
    init_UserRepository();
    init_ForgotPasswordValidator();
    RequestPasswordResetService = class {
      async execute({ email, phone }) {
        forgotPasswordSchema.parse({ email, phone });
        const normalizedEmail = String(email || "").trim().toLowerCase();
        const normalizedPhone = String(phone || "").trim();
        const user = normalizedEmail ? await UserRepository_default.findByEmail(normalizedEmail) : await UserRepository_default.findByPhone(normalizedPhone);
        const safeMessage = "Se os dados informados existirem, enviamos um codigo para redefinir a senha.";
        if (!user) {
          return { message: safeMessage };
        }
        const code = String(crypto4.randomInt(1e5, 1e6));
        const codeHash = await bcrypt6.hash(code, 10);
        const expiresAt = new Date(Date.now() + 15 * 60 * 1e3);
        await UserRepository_default.savePasswordResetCode(user.id, codeHash, expiresAt);
        const frontendUrl = String(process.env.FRONTEND_URL || "http://localhost:5173").replace(
          /\/$/,
          ""
        );
        const transporter = createTransporter2();
        if (transporter) {
          const from = String(process.env.ALERT_EMAIL_FROM || process.env.SMTP_USER || "").trim() || "no-reply@pizzaia.local";
          try {
            await transporter.sendMail({
              from,
              to: user.email,
              subject: "Recuperacao de senha - Peca ja food",
              text: `Seu codigo para redefinir a senha e: ${code}. Ele expira em 15 minutos.

Se preferir, abra: ${frontendUrl}/recover-password`
            });
          } catch (error2) {
            if (process.env.NODE_ENV === "production") {
              console.error("[password-reset] Nao foi possivel enviar o e-mail de recuperacao.");
              return { message: safeMessage };
            }
            if (isBasicAuthDisabledError2(error2)) {
              throw new Error(
                "Falha no SMTP: o provedor bloqueou login por usuario/senha (basic auth). Configure SMTP_AUTH_TYPE=oauth2 com credenciais OAuth2 ou use um provedor com app password."
              );
            }
            throw error2;
          }
        } else if (canLogPasswordResetCode()) {
          console.warn(`[password-reset] SMTP nao configurado. Codigo para ${user.email}: ${code}`);
        } else {
          return { message: safeMessage };
        }
        return { message: safeMessage };
      }
    };
    RequestPasswordResetService_default = new RequestPasswordResetService();
  }
});

// src/modules/auth/controllers/RequestPasswordResetController.ts
var RequestPasswordResetController, RequestPasswordResetController_default;
var init_RequestPasswordResetController = __esm({
  "src/modules/auth/controllers/RequestPasswordResetController.ts"() {
    init_RequestPasswordResetService();
    RequestPasswordResetController = class {
      async handle(req, res) {
        try {
          const { email, phone } = req.body;
          const result = await RequestPasswordResetService_default.execute({
            email,
            phone
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao solicitar recuperacao de senha"
          });
        }
      }
    };
    RequestPasswordResetController_default = new RequestPasswordResetController();
  }
});

// src/modules/auth/services/ResetPasswordByCodeService.ts
import bcrypt7 from "bcrypt";
var ResetPasswordByCodeService, ResetPasswordByCodeService_default;
var init_ResetPasswordByCodeService = __esm({
  "src/modules/auth/services/ResetPasswordByCodeService.ts"() {
    init_UserRepository();
    init_ForgotPasswordValidator();
    ResetPasswordByCodeService = class {
      async execute({
        email,
        phone,
        code,
        newPassword,
        confirmPassword
      }) {
        resetPasswordSchema.parse({
          email,
          phone,
          code,
          newPassword,
          confirmPassword
        });
        const normalizedEmail = String(email || "").trim();
        const normalizedPhone = String(phone || "").trim();
        const user = normalizedEmail ? await UserRepository_default.findByEmail(normalizedEmail) : await UserRepository_default.findByPhone(normalizedPhone);
        if (!user?.resetPasswordCodeHash || !user?.resetPasswordCodeExpiresAt || new Date(user.resetPasswordCodeExpiresAt).getTime() < Date.now()) {
          throw new Error("Codigo invalido ou expirado");
        }
        const isCodeValid = await bcrypt7.compare(code, user.resetPasswordCodeHash);
        if (!isCodeValid) {
          throw new Error("Codigo invalido ou expirado");
        }
        const passwordHash = await bcrypt7.hash(newPassword, 10);
        await UserRepository_default.updatePasswordAndClearResetCode(user.id, passwordHash);
        return { message: "Senha redefinida com sucesso" };
      }
    };
    ResetPasswordByCodeService_default = new ResetPasswordByCodeService();
  }
});

// src/modules/auth/controllers/ResetPasswordByCodeController.ts
var ResetPasswordByCodeController, ResetPasswordByCodeController_default;
var init_ResetPasswordByCodeController = __esm({
  "src/modules/auth/controllers/ResetPasswordByCodeController.ts"() {
    init_ResetPasswordByCodeService();
    ResetPasswordByCodeController = class {
      async handle(req, res) {
        try {
          const { email, phone, code, newPassword, confirmPassword } = req.body;
          const result = await ResetPasswordByCodeService_default.execute({
            email,
            phone,
            code,
            newPassword,
            confirmPassword
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao redefinir senha"
          });
        }
      }
    };
    ResetPasswordByCodeController_default = new ResetPasswordByCodeController();
  }
});

// src/middlewares/security/loginRateLimitMiddleware.ts
import rateLimit, { ipKeyGenerator } from "express-rate-limit";
function normalizeEmail(value) {
  return String(value || "").trim().toLowerCase().slice(0, 255);
}
function getClientIp(req) {
  const forwarded = req.headers["x-forwarded-for"];
  if (Array.isArray(forwarded) && forwarded.length) {
    return String(forwarded[0] || "").split(",")[0]?.trim() || req.ip;
  }
  if (typeof forwarded === "string" && forwarded.trim()) {
    return forwarded.split(",")[0]?.trim() || req.ip;
  }
  return req.ip;
}
var windowMs, max, loginRateLimitMiddleware;
var init_loginRateLimitMiddleware = __esm({
  "src/middlewares/security/loginRateLimitMiddleware.ts"() {
    windowMs = Number(process.env.LOGIN_RATE_LIMIT_WINDOW_MS || 15 * 60 * 1e3);
    max = Number(process.env.LOGIN_RATE_LIMIT_MAX_REQUESTS || 8);
    loginRateLimitMiddleware = rateLimit({
      windowMs,
      max,
      standardHeaders: true,
      legacyHeaders: false,
      skipSuccessfulRequests: true,
      keyGenerator: (req) => {
        const email = normalizeEmail(req.body?.email);
        const ip = ipKeyGenerator(String(getClientIp(req) || "unknown").trim());
        return `${ip}:${email || "no-email"}`;
      },
      message: {
        error: "Muitas tentativas de login. Aguarde alguns minutos antes de tentar novamente."
      }
    });
  }
});

// src/modules/auth/services/RefreshTokenService.ts
var RefreshTokenService, RefreshTokenService_default;
var init_RefreshTokenService = __esm({
  "src/modules/auth/services/RefreshTokenService.ts"() {
    init_AuthTokenService();
    RefreshTokenService = class {
      async execute(refreshToken) {
        const token = String(refreshToken || "").trim();
        if (!token) {
          throw new Error("Refresh token nao informado");
        }
        return AuthTokenService_default.rotateRefreshToken(token);
      }
    };
    RefreshTokenService_default = new RefreshTokenService();
  }
});

// src/modules/auth/controllers/RefreshTokenController.ts
var RefreshTokenController, RefreshTokenController_default;
var init_RefreshTokenController = __esm({
  "src/modules/auth/controllers/RefreshTokenController.ts"() {
    init_RefreshTokenService();
    RefreshTokenController = class {
      async handle(req, res) {
        try {
          const refreshToken = String(req.body?.refreshToken || "");
          const result = await RefreshTokenService_default.execute(refreshToken);
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(401).json({
            error: error2 instanceof Error ? error2.message : "Falha ao renovar sessao"
          });
        }
      }
    };
    RefreshTokenController_default = new RefreshTokenController();
  }
});

// src/modules/auth/services/LogoutService.ts
var LogoutService, LogoutService_default;
var init_LogoutService = __esm({
  "src/modules/auth/services/LogoutService.ts"() {
    init_AuthTokenService();
    LogoutService = class {
      async execute(refreshToken) {
        const token = String(refreshToken || "").trim();
        if (!token) {
          throw new Error("Refresh token nao informado");
        }
        await AuthTokenService_default.revokeRefreshToken(token);
        return { ok: true };
      }
    };
    LogoutService_default = new LogoutService();
  }
});

// src/modules/auth/controllers/LogoutController.ts
var LogoutController, LogoutController_default;
var init_LogoutController = __esm({
  "src/modules/auth/controllers/LogoutController.ts"() {
    init_LogoutService();
    LogoutController = class {
      async handle(req, res) {
        try {
          const refreshToken = String(req.body?.refreshToken || "");
          await LogoutService_default.execute(refreshToken);
          return res.status(200).json({ ok: true });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Falha ao fazer logout"
          });
        }
      }
    };
    LogoutController_default = new LogoutController();
  }
});

// src/modules/auth/controllers/VerifyLoginMfaController.ts
var VerifyLoginMfaController, VerifyLoginMfaController_default;
var init_VerifyLoginMfaController = __esm({
  "src/modules/auth/controllers/VerifyLoginMfaController.ts"() {
    init_LoginMfaService();
    VerifyLoginMfaController = class {
      async handle(req, res) {
        try {
          const { mfaToken, code } = req.body;
          const result = await LoginMfaService_default.verifyAndIssueTokens({
            mfaToken,
            code
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(401).json({
            error: error2 instanceof Error ? error2.message : "Falha na verificacao de login"
          });
        }
      }
    };
    VerifyLoginMfaController_default = new VerifyLoginMfaController();
  }
});

// src/modules/auth/services/UpdateMfaPreferenceService.ts
var UpdateMfaPreferenceService, UpdateMfaPreferenceService_default;
var init_UpdateMfaPreferenceService = __esm({
  "src/modules/auth/services/UpdateMfaPreferenceService.ts"() {
    init_UserRepository();
    UpdateMfaPreferenceService = class {
      async execute(userId, enabled) {
        if (typeof enabled !== "boolean") {
          throw new Error("A preferencia de verificacao em duas etapas e obrigatoria");
        }
        const user = await UserRepository_default.findById(userId);
        if (!user) {
          throw new Error("Usuario nao encontrado");
        }
        return UserRepository_default.updateMfaEnabled(userId, enabled);
      }
    };
    UpdateMfaPreferenceService_default = new UpdateMfaPreferenceService();
  }
});

// src/modules/auth/controllers/UpdateMfaPreferenceController.ts
var UpdateMfaPreferenceController, UpdateMfaPreferenceController_default;
var init_UpdateMfaPreferenceController = __esm({
  "src/modules/auth/controllers/UpdateMfaPreferenceController.ts"() {
    init_UpdateMfaPreferenceService();
    UpdateMfaPreferenceController = class {
      async handle(req, res) {
        try {
          const result = await UpdateMfaPreferenceService_default.execute(req.user.id, req.body?.enabled);
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar verificacao em duas etapas"
          });
        }
      }
    };
    UpdateMfaPreferenceController_default = new UpdateMfaPreferenceController();
  }
});

// src/middlewares/security/accountActionRateLimitMiddleware.ts
import rateLimit2, { ipKeyGenerator as ipKeyGenerator2 } from "express-rate-limit";
function getEmailKey(req) {
  const email = String(req.body?.email || "").trim().toLowerCase().slice(0, 255);
  const ip = ipKeyGenerator2(String(req.ip || "unknown").trim());
  return `${ip}:${email || "no-email"}`;
}
function getTablePaymentKey(req) {
  const ip = ipKeyGenerator2(String(req.ip || "unknown").trim());
  const sessionPublicId = String(req.tableSession?.publicId || "no-session").slice(0, 64);
  const participantPublicId = String(req.tableParticipant?.publicId || "no-participant").slice(
    0,
    64
  );
  return `${ip}:${sessionPublicId}:${participantPublicId}`;
}
var passwordResetRateLimitMiddleware, registrationRateLimitMiddleware, tablePaymentActionRateLimitMiddleware;
var init_accountActionRateLimitMiddleware = __esm({
  "src/middlewares/security/accountActionRateLimitMiddleware.ts"() {
    passwordResetRateLimitMiddleware = rateLimit2({
      windowMs: Number(process.env.PASSWORD_RESET_RATE_LIMIT_WINDOW_MS || 60 * 60 * 1e3),
      max: Number(process.env.PASSWORD_RESET_RATE_LIMIT_MAX_REQUESTS || 5),
      standardHeaders: true,
      legacyHeaders: false,
      keyGenerator: getEmailKey,
      message: {
        error: "Muitas solicita\xE7\xF5es de recupera\xE7\xE3o. Aguarde antes de tentar novamente."
      }
    });
    registrationRateLimitMiddleware = rateLimit2({
      windowMs: Number(process.env.REGISTRATION_RATE_LIMIT_WINDOW_MS || 60 * 60 * 1e3),
      max: Number(process.env.REGISTRATION_RATE_LIMIT_MAX_REQUESTS || 10),
      standardHeaders: true,
      legacyHeaders: false,
      keyGenerator: (req) => ipKeyGenerator2(String(req.ip || "unknown").trim()),
      message: {
        error: "Muitos cadastros originados deste endere\xE7o. Tente mais tarde."
      }
    });
    tablePaymentActionRateLimitMiddleware = rateLimit2({
      windowMs: Number(process.env.TABLE_PAYMENT_RATE_LIMIT_WINDOW_MS || 6e4),
      max: Number(process.env.TABLE_PAYMENT_RATE_LIMIT_MAX_REQUESTS || 20),
      standardHeaders: true,
      legacyHeaders: false,
      keyGenerator: getTablePaymentKey,
      message: {
        error: "Muitas tentativas de pagamento nesta mesa. Aguarde alguns instantes."
      }
    });
  }
});

// src/modules/auth/routes/authRoutes.ts
import { Router } from "express";
var router, authRoutes_default;
var init_authRoutes = __esm({
  "src/modules/auth/routes/authRoutes.ts"() {
    init_RegisterController();
    init_LoginController();
    init_GoogleAuthController();
    init_MeController();
    init_authMiddleware();
    init_UpdatePasswordController();
    init_UpdateProfileController();
    init_DeactivateUserController();
    init_ReactivateUserController();
    init_RequestPasswordResetController();
    init_ResetPasswordByCodeController();
    init_loginRateLimitMiddleware();
    init_RefreshTokenController();
    init_LogoutController();
    init_VerifyLoginMfaController();
    init_UpdateMfaPreferenceController();
    init_accountActionRateLimitMiddleware();
    router = Router();
    router.post("/register", registrationRateLimitMiddleware, (req, res) => {
      RegisterController_default.handle(req, res);
    });
    router.post("/login", loginRateLimitMiddleware, (req, res) => {
      LoginController_default.handle(req, res);
    });
    router.post("/forgot-password", passwordResetRateLimitMiddleware, (req, res) => {
      RequestPasswordResetController_default.handle(req, res);
    });
    router.post("/reset-password", passwordResetRateLimitMiddleware, (req, res) => {
      ResetPasswordByCodeController_default.handle(req, res);
    });
    router.post("/google", (req, res) => {
      GoogleAuthController_default.handle(req, res);
    });
    router.post("/refresh", (req, res) => {
      RefreshTokenController_default.handle(req, res);
    });
    router.post("/logout", (req, res) => {
      LogoutController_default.handle(req, res);
    });
    router.post("/login/verify-2fa", loginRateLimitMiddleware, (req, res) => {
      VerifyLoginMfaController_default.handle(req, res);
    });
    router.get("/google/client-id", (req, res) => {
      const singleClientId = String(process.env.GOOGLE_CLIENT_ID || "").trim();
      const listClientIds = String(process.env.GOOGLE_CLIENT_IDS || "").split(",").map((item) => item.trim()).filter(Boolean);
      const clientId = singleClientId || listClientIds[0] || null;
      return res.json({
        clientId
      });
    });
    router.get("/me", authMiddleware, (req, res) => {
      MeController_default.handle(req, res);
    });
    router.put("/password", authMiddleware, (req, res) => {
      UpdatePasswordController_default.handle(req, res);
    });
    router.put("/profile", authMiddleware, (req, res) => {
      UpdateProfileController_default.handle(req, res);
    });
    router.patch("/mfa", authMiddleware, (req, res) => {
      UpdateMfaPreferenceController_default.handle(req, res);
    });
    router.patch("/deactivate", authMiddleware, (req, res) => {
      DeactivateUserController_default.handle(req, res);
    });
    router.patch("/reactivate", authMiddleware, (req, res) => {
      ReactivateUserController_default.handle(req, res);
    });
    authRoutes_default = router;
  }
});

// src/validators/ProductValidator.ts
import { z as z4 } from "zod";
function validateUniqueGroupNames(product, ctx) {
  if (!product.optionGroups) {
    return;
  }
  const normalizedNames = product.optionGroups.map((group) => group.name.trim().toLocaleLowerCase());
  if (new Set(normalizedNames).size !== normalizedNames.length) {
    ctx.addIssue({
      code: z4.ZodIssueCode.custom,
      path: ["optionGroups"],
      message: "Os grupos de op\xE7\xF5es do produto precisam ter nomes diferentes."
    });
  }
}
var ingredientSchema, optionSchema, productOptionGroupSchema, productSchema, createProductSchema, updateProductSchema;
var init_ProductValidator = __esm({
  "src/validators/ProductValidator.ts"() {
    ingredientSchema = z4.object({
      id: z4.number().int().positive().optional(),
      name: z4.string().trim().min(1, "Nome do ingrediente \xE9 obrigat\xF3rio.").max(80),
      price: z4.number().min(0, "O adicional n\xE3o pode ser negativo.").max(9999),
      required: z4.boolean().optional(),
      active: z4.boolean().optional()
    });
    optionSchema = z4.object({
      id: z4.number().int().positive().optional(),
      ingredientId: z4.number().int().positive("Ingrediente inv\xE1lido."),
      active: z4.boolean().optional()
    });
    productOptionGroupSchema = z4.object({
      id: z4.number().int().positive().optional(),
      name: z4.string().trim().min(1, "Nome do grupo \xE9 obrigat\xF3rio.").max(80),
      description: z4.string().trim().max(240).optional(),
      required: z4.boolean().default(false),
      selectionType: z4.enum(["SINGLE", "MULTIPLE"]),
      minSelections: z4.number().int().min(0).max(40),
      maxSelections: z4.number().int().min(1).max(40),
      options: z4.array(optionSchema).min(1, "Adicione ao menos uma op\xE7\xE3o ao grupo.").max(40)
    }).superRefine((group, ctx) => {
      if (group.minSelections > group.maxSelections) {
        ctx.addIssue({
          code: z4.ZodIssueCode.custom,
          path: ["minSelections"],
          message: "O m\xEDnimo de escolhas n\xE3o pode superar o m\xE1ximo."
        });
      }
      if (group.required && group.minSelections < 1) {
        ctx.addIssue({
          code: z4.ZodIssueCode.custom,
          path: ["minSelections"],
          message: "Um grupo obrigat\xF3rio deve exigir ao menos uma escolha."
        });
      }
      if (!group.required && group.minSelections !== 0) {
        ctx.addIssue({
          code: z4.ZodIssueCode.custom,
          path: ["minSelections"],
          message: "Uma categoria opcional deve permitir continuar sem nenhuma escolha."
        });
      }
      if (group.selectionType === "SINGLE" && group.maxSelections !== 1) {
        ctx.addIssue({
          code: z4.ZodIssueCode.custom,
          path: ["maxSelections"],
          message: "Grupos de escolha \xFAnica devem permitir exatamente uma op\xE7\xE3o."
        });
      }
      const uniqueIngredientIds = new Set(group.options.map((option) => option.ingredientId));
      if (uniqueIngredientIds.size !== group.options.length) {
        ctx.addIssue({
          code: z4.ZodIssueCode.custom,
          path: ["options"],
          message: "Um ingrediente n\xE3o pode aparecer duas vezes no mesmo grupo."
        });
      }
      const enabledOptions = group.options.filter((option) => option.active !== false).length;
      if (enabledOptions < group.maxSelections) {
        ctx.addIssue({
          code: z4.ZodIssueCode.custom,
          path: ["maxSelections"],
          message: "O m\xE1ximo de escolhas n\xE3o pode superar as op\xE7\xF5es ativas do grupo."
        });
      }
    });
    productSchema = z4.object({
      name: z4.string().trim().min(1, "Nome obrigat\xF3rio!"),
      description: z4.string().trim().optional(),
      image: z4.string().trim().optional(),
      price: z4.number({
        invalid_type_error: "Pre\xE7o deve ser um n\xFAmero.",
        required_error: "Pre\xE7o deve ser um n\xFAmero."
      }).positive("Pre\xE7o deve ser maior que zero!"),
      active: z4.boolean().optional(),
      featured: z4.boolean().optional(),
      preparationTime: z4.number().int("Tempo deve ser inteiro.").positive("Tempo deve ser maior que zero.").optional(),
      stock: z4.number().int("Estoque deve ser inteiro.").min(0, "Estoque n\xE3o pode ser negativo.").nullable().optional(),
      saleMode: z4.enum(["COMPLETE", "BUILDABLE"]).optional(),
      ingredients: z4.array(ingredientSchema).max(40).optional(),
      optionGroups: z4.array(productOptionGroupSchema).max(20, "Cada produto pode ter no m\xE1ximo 20 grupos de op\xE7\xF5es.").optional(),
      categoryId: z4.number({
        invalid_type_error: "Categoria \xE9 obrigat\xF3ria.",
        required_error: "Categoria \xE9 obrigat\xF3ria."
      }).int()
    });
    createProductSchema = productSchema.superRefine(validateUniqueGroupNames);
    updateProductSchema = productSchema.partial().superRefine(validateUniqueGroupNames);
  }
});

// src/modules/products/utils/productOptionGroups.ts
async function buildProductOptionGroupsCreate(tx, restaurantId, groups) {
  const ingredientIds = [
    ...new Set(groups.flatMap((group) => group.options.map((option) => option.ingredientId)))
  ];
  const ingredients = ingredientIds.length ? await tx.ingredient.findMany({
    where: {
      restaurantId,
      id: { in: ingredientIds }
    },
    select: { id: true }
  }) : [];
  if (ingredients.length !== ingredientIds.length) {
    throw new Error("Um ou mais ingredientes n\xE3o pertencem a este restaurante.");
  }
  return groups.map((group, groupIndex) => ({
    restaurantId,
    name: group.name.trim(),
    description: String(group.description || "").trim() || null,
    required: group.required,
    selectionType: group.selectionType,
    minSelections: group.minSelections,
    maxSelections: group.maxSelections,
    position: groupIndex,
    active: true,
    options: {
      create: group.options.map((option, optionIndex) => ({
        ingredientId: option.ingredientId,
        active: option.active !== false,
        position: optionIndex
      }))
    }
  }));
}
var init_productOptionGroups = __esm({
  "src/modules/products/utils/productOptionGroups.ts"() {
  }
});

// src/modules/products/services/CreateProductService.ts
function requireDefined(value, message) {
  if (value === null || value === void 0) {
    throw new Error(message);
  }
  return value;
}
var CreateProductService, CreateProductService_default;
var init_CreateProductService = __esm({
  "src/modules/products/services/CreateProductService.ts"() {
    init_ProductValidator();
    init_prisma();
    init_productOptionGroups();
    CreateProductService = class {
      async execute(data, restaurantId) {
        if (!restaurantId) {
          throw new Error("Restaurante n\xE3o encontrado");
        }
        const parsedData = createProductSchema.parse(data);
        const normalizedStock = parsedData.stock === null || parsedData.stock === void 0 ? null : Number(parsedData.stock);
        const activeFromStock = normalizedStock === null || normalizedStock > 0;
        const requiredName = requireDefined(parsedData.name, "Nome do produto \xE9 obrigat\xF3rio.");
        const requiredPrice = requireDefined(parsedData.price, "Pre\xE7o do produto \xE9 obrigat\xF3rio.");
        const requiredCategoryId = requireDefined(
          parsedData.categoryId,
          "Categoria do produto \xE9 obrigat\xF3ria."
        );
        const { ingredients: _legacyIngredients, optionGroups = [], saleMode: _saleMode, ...productData } = parsedData;
        if (optionGroups.length === 0) {
          throw new Error("Adicione ao menos um grupo de op\xE7\xF5es para montar o produto.");
        }
        const product = await prisma_default.$transaction(async (tx) => {
          const category = await tx.category.findFirst({
            where: { id: requiredCategoryId, restaurantId },
            select: { id: true }
          });
          if (!category) {
            throw new Error("A categoria informada n\xE3o pertence a este restaurante.");
          }
          const normalizedGroups = await buildProductOptionGroupsCreate(
            tx,
            restaurantId,
            optionGroups
          );
          return tx.product.create({
            data: {
              ...productData,
              name: requiredName,
              price: requiredPrice,
              categoryId: requiredCategoryId,
              restaurantId,
              saleMode: "BUILDABLE",
              active: activeFromStock && parsedData.active !== false,
              optionGroups: { create: normalizedGroups }
            },
            include: {
              category: true,
              optionGroups: {
                orderBy: [{ position: "asc" }, { id: "asc" }],
                include: {
                  options: {
                    orderBy: [{ position: "asc" }, { id: "asc" }],
                    include: { ingredient: true }
                  }
                }
              }
            }
          });
        });
        return {
          product
        };
      }
    };
    CreateProductService_default = new CreateProductService();
  }
});

// src/modules/products/controllers/CreateProductController.ts
var CreateProductController, CreateProductController_default;
var init_CreateProductController = __esm({
  "src/modules/products/controllers/CreateProductController.ts"() {
    init_CreateProductService();
    CreateProductController = class {
      async handle(req, res) {
        try {
          const {
            name,
            description,
            image,
            price,
            categoryId,
            active,
            featured,
            preparationTime,
            stock,
            optionGroups
          } = req.body;
          const product = await CreateProductService_default.execute(
            {
              name,
              description,
              image,
              price,
              categoryId,
              active,
              featured,
              preparationTime,
              stock,
              optionGroups
            },
            req.user.restaurantId
          );
          return res.status(201).json(product);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao criar produto"
          });
        }
      }
    };
    CreateProductController_default = new CreateProductController();
  }
});

// src/modules/products/repositories/ProductRepository.ts
var productConfigurationInclude, ProductRepository, ProductRepository_default;
var init_ProductRepository = __esm({
  "src/modules/products/repositories/ProductRepository.ts"() {
    init_prisma();
    productConfigurationInclude = {
      category: true,
      discount: true,
      ingredients: { orderBy: { id: "asc" } },
      optionGroups: {
        orderBy: [{ position: "asc" }, { id: "asc" }],
        include: {
          options: {
            orderBy: [{ position: "asc" }, { id: "asc" }],
            include: {
              ingredient: true
            }
          }
        }
      }
    };
    ProductRepository = class {
      async create(data, restaurantId, db = prisma_default) {
        return db.product.create({
          data: {
            ...data,
            restaurantId
          }
        });
      }
      async findByName(name, restaurantId, db = prisma_default) {
        return db.product.findFirst({
          where: {
            restaurantId,
            name: {
              equals: String(name || "").trim(),
              mode: "insensitive"
            }
          }
        });
      }
      async findAll(restaurantId, db = prisma_default) {
        return db.product.findMany({
          where: {
            restaurantId
          },
          include: {
            ...productConfigurationInclude
          },
          orderBy: {
            createdAt: "desc"
          }
        });
      }
      async update(id, data, restaurantId, db = prisma_default) {
        return db.product.updateMany({
          where: {
            id: Number(id),
            restaurantId
          },
          data
        });
      }
      async findById(id, restaurantId, db = prisma_default) {
        return db.product.findFirst({
          where: {
            id: Number(id),
            restaurantId
          },
          include: {
            ...productConfigurationInclude
          }
        });
      }
      async delete(id, restaurantId, db = prisma_default) {
        const productId = Number(id);
        const hasOrders = await db.orderItem.findFirst({
          where: {
            productId,
            order: {
              restaurantId
            }
          }
        });
        if (hasOrders) {
          throw new Error("N\xE3o \xE9 poss\xEDvel excluir um produto que j\xE1 possui pedidos.");
        }
        return db.product.deleteMany({
          where: {
            id: productId,
            restaurantId
          }
        });
      }
      async listRatingsByRestaurant(restaurantId, clientKey, db = prisma_default) {
        const summaries = await db.productRating.groupBy({
          by: ["productId"],
          where: {
            restaurantId
          },
          _avg: {
            rating: true
          },
          _count: {
            _all: true
          }
        });
        let userRatingsMap = /* @__PURE__ */ new Map();
        if (clientKey) {
          const userRatings = await db.productRating.findMany({
            where: {
              restaurantId,
              clientKey
            },
            select: {
              productId: true,
              rating: true
            }
          });
          userRatingsMap = new Map(
            userRatings.map((item) => [Number(item.productId), Number(item.rating)])
          );
        }
        return summaries.map((item) => ({
          productId: Number(item.productId),
          average: Number(item._avg.rating || 0),
          count: Number(item._count._all || 0),
          userRating: Number(userRatingsMap.get(Number(item.productId)) || 0)
        }));
      }
      async upsertRating(data, db = prisma_default) {
        const { restaurantId, productId, clientKey, rating } = data;
        return db.productRating.upsert({
          where: {
            restaurantId_productId_clientKey: {
              restaurantId,
              productId,
              clientKey
            }
          },
          update: {
            rating
          },
          create: {
            restaurantId,
            productId,
            clientKey,
            rating
          }
        });
      }
      async getRatingSummary(productId, restaurantId, clientKey, db = prisma_default) {
        const grouped = await db.productRating.groupBy({
          by: ["productId"],
          where: {
            restaurantId,
            productId
          },
          _avg: {
            rating: true
          },
          _count: {
            _all: true
          }
        });
        const summary = grouped[0];
        let userRating = 0;
        if (clientKey) {
          const mine = await db.productRating.findUnique({
            where: {
              restaurantId_productId_clientKey: {
                restaurantId,
                productId,
                clientKey
              }
            },
            select: {
              rating: true
            }
          });
          userRating = Number(mine?.rating || 0);
        }
        return {
          productId: Number(productId),
          average: Number(summary?._avg?.rating || 0),
          count: Number(summary?._count?._all || 0),
          userRating
        };
      }
    };
    ProductRepository_default = new ProductRepository();
  }
});

// src/modules/products/services/UpdateProductService.ts
var UpdateProductService, UpdateProductService_default;
var init_UpdateProductService = __esm({
  "src/modules/products/services/UpdateProductService.ts"() {
    init_ProductValidator();
    init_ProductRepository();
    init_prisma();
    init_productOptionGroups();
    UpdateProductService = class {
      async execute(id, data, restaurantId) {
        const parsedData = updateProductSchema.parse(data);
        const product = await ProductRepository_default.findById(id, restaurantId);
        if (!product) {
          throw new Error("Produto n\xE3o encontrado!");
        }
        const stockWasProvided = Object.prototype.hasOwnProperty.call(data, "stock");
        const normalizedStock = parsedData.stock === null || parsedData.stock === void 0 ? null : Number(parsedData.stock);
        let nextActive = parsedData.active;
        if (stockWasProvided) {
          nextActive = normalizedStock === null || normalizedStock > 0;
        }
        const payload = {
          ...parsedData,
          active: nextActive
        };
        const {
          ingredients: _legacyIngredients,
          optionGroups,
          saleMode: _saleMode,
          ...productData
        } = payload;
        if (optionGroups && optionGroups.length === 0) {
          throw new Error("Adicione ao menos um grupo de op\xE7\xF5es para montar o produto.");
        }
        return prisma_default.$transaction(async (tx) => {
          if (productData.categoryId !== void 0) {
            const category = await tx.category.findFirst({
              where: { id: productData.categoryId, restaurantId },
              select: { id: true }
            });
            if (!category) {
              throw new Error("A categoria informada n\xE3o pertence a este restaurante.");
            }
          }
          const normalizedGroups = optionGroups ? await buildProductOptionGroupsCreate(tx, restaurantId, optionGroups) : null;
          if (normalizedGroups) {
            await tx.productOptionGroup.deleteMany({ where: { productId: product.id, restaurantId } });
          }
          return tx.product.update({
            where: { id: product.id },
            data: {
              ...productData,
              saleMode: "BUILDABLE",
              ...normalizedGroups ? { optionGroups: { create: normalizedGroups } } : {}
            },
            include: {
              category: true,
              optionGroups: {
                orderBy: [{ position: "asc" }, { id: "asc" }],
                include: {
                  options: {
                    orderBy: [{ position: "asc" }, { id: "asc" }],
                    include: { ingredient: true }
                  }
                }
              }
            }
          });
        });
      }
    };
    UpdateProductService_default = new UpdateProductService();
  }
});

// src/modules/products/controllers/UpdateProductController.ts
var UpdateProductController, UpdateProductController_default;
var init_UpdateProductController = __esm({
  "src/modules/products/controllers/UpdateProductController.ts"() {
    init_UpdateProductService();
    UpdateProductController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const data = req.body;
          const updatedProduct = await UpdateProductService_default.execute(id, data, req.user.restaurantId);
          return res.status(200).json(updatedProduct);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao atualizar produto"
          });
        }
      }
    };
    UpdateProductController_default = new UpdateProductController();
  }
});

// src/modules/products/services/DeleteProductService.ts
var DeleteProductService, DeleteProductService_default;
var init_DeleteProductService = __esm({
  "src/modules/products/services/DeleteProductService.ts"() {
    init_ProductRepository();
    DeleteProductService = class {
      async execute(id, restaurantId) {
        const product = await ProductRepository_default.findById(id, restaurantId);
        if (!product) {
          throw new Error("Produto n\xE3o encontrado!");
        }
        await ProductRepository_default.delete(id, restaurantId);
      }
    };
    DeleteProductService_default = new DeleteProductService();
  }
});

// src/modules/products/controllers/DeleteProductController.ts
var DeleteProductController, DeleteProductController_default;
var init_DeleteProductController = __esm({
  "src/modules/products/controllers/DeleteProductController.ts"() {
    init_DeleteProductService();
    DeleteProductController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          await DeleteProductService_default.execute(id, req.user.restaurantId);
          return res.status(200).json({
            message: "Produto deletado com sucesso!"
          });
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao deletar produto"
          });
        }
      }
    };
    DeleteProductController_default = new DeleteProductController();
  }
});

// src/modules/restaurants/repositories/RestaurantRepository.ts
import { UserRole as UserRole3 } from "@prisma/client";
var RestaurantRepository, RestaurantRepository_default;
var init_RestaurantRepository = __esm({
  "src/modules/restaurants/repositories/RestaurantRepository.ts"() {
    init_prisma();
    RestaurantRepository = class {
      async findByEmail(email, db = prisma_default) {
        return db.restaurant.findUnique({
          where: { email }
        });
      }
      async findBySlug(slug, db = prisma_default) {
        return db.restaurant.findUnique({
          where: { slug }
        });
      }
      async create(data, db = prisma_default) {
        return db.restaurant.create({
          data
        });
      }
      async listAll(db = prisma_default) {
        return db.restaurant.findMany({
          include: {
            users: {
              where: { role: UserRole3.ADMIN },
              select: {
                id: true,
                name: true,
                email: true
              },
              take: 1
            },
            subscription: {
              select: {
                id: true,
                plan: true,
                status: true,
                currentPeriodEnd: true
              }
            }
          },
          orderBy: {
            createdAt: "desc"
          }
        });
      }
    };
    RestaurantRepository_default = new RestaurantRepository();
  }
});

// src/modules/products/utils/productDiscount.ts
function roundMoney(value) {
  const numeric = Number(value);
  if (!Number.isFinite(numeric)) {
    throw new Error("Valor monet\xE1rio inv\xE1lido.");
  }
  return Math.round((numeric + Number.EPSILON) * 100) / 100;
}
function validDate(value) {
  if (!value) return null;
  const date = value instanceof Date ? value : new Date(value);
  return Number.isNaN(date.getTime()) ? null : date;
}
function resolveProductBasePricing(product, now = /* @__PURE__ */ new Date()) {
  const originalBasePrice = Math.max(roundMoney(product.price), 0);
  const discount = product.discount;
  const startsAt = validDate(discount?.startsAt);
  const endsAt = validDate(discount?.endsAt);
  const isWithinPeriod = (!startsAt || startsAt <= now) && (!endsAt || endsAt > now);
  const configuredValue = Number(discount?.value || 0);
  const kind = String(discount?.kind || "").toUpperCase();
  const hasValidConfiguration = discount?.active === true && isWithinPeriod && Number.isFinite(configuredValue) && configuredValue > 0 && (kind === "FIXED" || kind === "PERCENTAGE");
  let discountAmount = 0;
  if (hasValidConfiguration) {
    discountAmount = kind === "PERCENTAGE" ? roundMoney(originalBasePrice * (Math.min(configuredValue, 100) / 100)) : roundMoney(Math.min(configuredValue, originalBasePrice));
  }
  const effectiveBasePrice = roundMoney(Math.max(originalBasePrice - discountAmount, 0));
  const discountPercentage = originalBasePrice > 0 ? roundMoney(discountAmount / originalBasePrice * 100) : 0;
  const active = discountAmount > 0;
  const configuredLabel = String(discount?.label || "").trim();
  return {
    originalBasePrice,
    effectiveBasePrice,
    discountAmount,
    discountPercentage,
    badgeLabel: active ? configuredLabel || `${Math.max(Math.round(discountPercentage), 1)}% OFF` : null,
    active,
    endsAt: active && endsAt ? endsAt : null
  };
}
var init_productDiscount = __esm({
  "src/modules/products/utils/productDiscount.ts"() {
  }
});

// src/modules/products/services/ListProductService.ts
var ListProductsService, ListProductService_default;
var init_ListProductService = __esm({
  "src/modules/products/services/ListProductService.ts"() {
    init_ProductRepository();
    init_RestaurantRepository();
    init_productDiscount();
    ListProductsService = class {
      async execute({ restaurantId, slug }) {
        let normalizedRestaurantId = Number(restaurantId);
        if ((!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) && slug) {
          const restaurant = await RestaurantRepository_default.findBySlug(String(slug).trim());
          normalizedRestaurantId = Number(restaurant?.id || 0);
        }
        if (!normalizedRestaurantId) {
          throw new Error("Restaurante n\xE3o encontrado");
        }
        const products = await ProductRepository_default.findAll(normalizedRestaurantId);
        const normalizedProducts = products.map((product) => {
          const stockValue = product?.stock === null || product?.stock === void 0 ? null : Number(product.stock);
          const pricing = resolveProductBasePricing(product);
          if (Number.isFinite(stockValue) && stockValue <= 0) {
            return {
              ...product,
              active: false,
              pricing
            };
          }
          return {
            ...product,
            pricing
          };
        });
        return {
          products: normalizedProducts,
          count: normalizedProducts.length
        };
      }
    };
    ListProductService_default = new ListProductsService();
  }
});

// src/modules/products/controllers/ListProductController.ts
var ListProductsController, ListProductController_default;
var init_ListProductController = __esm({
  "src/modules/products/controllers/ListProductController.ts"() {
    init_ListProductService();
    ListProductsController = class {
      async handle(req, res) {
        try {
          const restaurantId = Number(req.query.restaurantId) || Number(req.user?.restaurantId);
          const slug = typeof req.query.slug === "string" ? req.query.slug : void 0;
          const products = await ListProductService_default.execute({
            restaurantId,
            slug
          });
          return res.status(200).json(products);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao listar produtos"
          });
        }
      }
    };
    ListProductController_default = new ListProductsController();
  }
});

// src/modules/products/services/ListProductRatingsService.ts
var ListProductRatingsService, ListProductRatingsService_default;
var init_ListProductRatingsService = __esm({
  "src/modules/products/services/ListProductRatingsService.ts"() {
    init_ProductRepository();
    ListProductRatingsService = class {
      async execute(restaurantId, clientKey) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!normalizedRestaurantId) {
          throw new Error("Restaurante n\xE3o encontrado");
        }
        const ratings = await ProductRepository_default.listRatingsByRestaurant(
          normalizedRestaurantId,
          String(clientKey || "").trim()
        );
        return {
          ratings,
          count: ratings.length
        };
      }
    };
    ListProductRatingsService_default = new ListProductRatingsService();
  }
});

// src/modules/products/controllers/ListProductRatingsController.ts
var ListProductRatingsController, ListProductRatingsController_default;
var init_ListProductRatingsController = __esm({
  "src/modules/products/controllers/ListProductRatingsController.ts"() {
    init_ListProductRatingsService();
    ListProductRatingsController = class {
      async handle(req, res) {
        try {
          const restaurantId = Number(req.query.restaurantId);
          const clientKey = String(req.query.clientKey || "").trim();
          const result = await ListProductRatingsService_default.execute(restaurantId, clientKey);
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao listar avaliacoes de produtos"
          });
        }
      }
    };
    ListProductRatingsController_default = new ListProductRatingsController();
  }
});

// src/modules/products/services/RateProductService.ts
var RateProductService, RateProductService_default;
var init_RateProductService = __esm({
  "src/modules/products/services/RateProductService.ts"() {
    init_ProductRepository();
    RateProductService = class {
      async execute({ productId, restaurantId, clientKey, rating }) {
        const normalizedProductId = Number(productId);
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedRating = Number(rating);
        const normalizedClientKey = String(clientKey || "").trim();
        if (!normalizedRestaurantId) {
          throw new Error("Restaurante n\xE3o encontrado");
        }
        if (!normalizedProductId) {
          throw new Error("Produto inv\xE1lido");
        }
        if (!normalizedClientKey) {
          throw new Error("Identificador do cliente n\xE3o informado");
        }
        if (!Number.isInteger(normalizedRating) || normalizedRating < 1 || normalizedRating > 5) {
          throw new Error("A avalia\xE7\xE3o deve ser um n\xFAmero inteiro entre 1 e 5");
        }
        const product = await ProductRepository_default.findById(normalizedProductId, normalizedRestaurantId);
        if (!product) {
          throw new Error("Produto n\xE3o encontrado para este restaurante");
        }
        await ProductRepository_default.upsertRating({
          productId: normalizedProductId,
          restaurantId: normalizedRestaurantId,
          clientKey: normalizedClientKey,
          rating: normalizedRating
        });
        const summary = await ProductRepository_default.getRatingSummary(
          normalizedProductId,
          normalizedRestaurantId,
          normalizedClientKey
        );
        return {
          rating: summary
        };
      }
    };
    RateProductService_default = new RateProductService();
  }
});

// src/modules/products/controllers/RateProductController.ts
var RateProductController, RateProductController_default;
var init_RateProductController = __esm({
  "src/modules/products/controllers/RateProductController.ts"() {
    init_RateProductService();
    RateProductController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { restaurantId, clientKey, rating } = req.body;
          const result = await RateProductService_default.execute({
            productId: id,
            restaurantId,
            clientKey,
            rating
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao avaliar produto"
          });
        }
      }
    };
    RateProductController_default = new RateProductController();
  }
});

// src/validators/ProductDiscountValidator.ts
import { z as z5 } from "zod";
var optionalDate, upsertProductDiscountSchema;
var init_ProductDiscountValidator = __esm({
  "src/validators/ProductDiscountValidator.ts"() {
    optionalDate = z5.union([z5.string().datetime({ offset: true }), z5.date(), z5.null()]).optional().transform((value) => {
      if (value === null || value === void 0) return null;
      return value instanceof Date ? value : new Date(value);
    });
    upsertProductDiscountSchema = z5.object({
      kind: z5.enum(["FIXED", "PERCENTAGE"]),
      value: z5.coerce.number().positive("Informe um desconto maior que zero.").max(999999),
      label: z5.string().trim().max(40, "O aviso pode ter no m\xE1ximo 40 caracteres.").optional(),
      active: z5.boolean().optional().default(true),
      startsAt: optionalDate,
      endsAt: optionalDate
    }).superRefine((data, ctx) => {
      if (data.kind === "PERCENTAGE" && data.value >= 100) {
        ctx.addIssue({
          code: z5.ZodIssueCode.custom,
          path: ["value"],
          message: "O desconto percentual deve ser menor que 100%."
        });
      }
      if (data.startsAt && data.endsAt && data.startsAt >= data.endsAt) {
        ctx.addIssue({
          code: z5.ZodIssueCode.custom,
          path: ["endsAt"],
          message: "O t\xE9rmino da oferta deve ser posterior ao in\xEDcio."
        });
      }
    });
  }
});

// src/modules/products/services/UpsertProductDiscountService.ts
var UpsertProductDiscountService, UpsertProductDiscountService_default;
var init_UpsertProductDiscountService = __esm({
  "src/modules/products/services/UpsertProductDiscountService.ts"() {
    init_prisma();
    init_ProductDiscountValidator();
    UpsertProductDiscountService = class {
      async execute({
        productId,
        restaurantId,
        input
      }) {
        const normalizedProductId = Number(productId);
        const normalizedRestaurantId = Number(restaurantId || 0);
        if (!Number.isInteger(normalizedProductId) || normalizedProductId <= 0) {
          throw new Error("Produto inv\xE1lido.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido.");
        }
        const data = upsertProductDiscountSchema.parse(input);
        const product = await prisma_default.product.findFirst({
          where: { id: normalizedProductId, restaurantId: normalizedRestaurantId },
          select: { id: true, price: true }
        });
        if (!product) {
          throw new Error("Produto n\xE3o encontrado neste restaurante.");
        }
        if (data.kind === "FIXED" && data.value >= Number(product.price)) {
          throw new Error("O desconto fixo deve ser menor que o pre\xE7o-base do produto.");
        }
        return prisma_default.productDiscount.upsert({
          where: { productId: normalizedProductId },
          update: {
            kind: data.kind,
            value: data.value,
            label: data.label || null,
            active: data.active,
            startsAt: data.startsAt,
            endsAt: data.endsAt
          },
          create: {
            restaurantId: normalizedRestaurantId,
            productId: normalizedProductId,
            kind: data.kind,
            value: data.value,
            label: data.label || null,
            active: data.active,
            startsAt: data.startsAt,
            endsAt: data.endsAt
          }
        });
      }
    };
    UpsertProductDiscountService_default = new UpsertProductDiscountService();
  }
});

// src/modules/products/controllers/UpsertProductDiscountController.ts
var UpsertProductDiscountController, UpsertProductDiscountController_default;
var init_UpsertProductDiscountController = __esm({
  "src/modules/products/controllers/UpsertProductDiscountController.ts"() {
    init_UpsertProductDiscountService();
    UpsertProductDiscountController = class {
      async handle(req, res) {
        try {
          const discount = await UpsertProductDiscountService_default.execute({
            productId: Array.isArray(req.params.id) ? req.params.id[0] : req.params.id,
            restaurantId: req.user.restaurantId,
            input: req.body
          });
          return res.status(200).json(discount);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel salvar o desconto."
          });
        }
      }
    };
    UpsertProductDiscountController_default = new UpsertProductDiscountController();
  }
});

// src/modules/products/services/DeleteProductDiscountService.ts
var DeleteProductDiscountService, DeleteProductDiscountService_default;
var init_DeleteProductDiscountService = __esm({
  "src/modules/products/services/DeleteProductDiscountService.ts"() {
    init_prisma();
    DeleteProductDiscountService = class {
      async execute(productId, restaurantId) {
        const normalizedProductId = Number(productId);
        const normalizedRestaurantId = Number(restaurantId || 0);
        if (!Number.isInteger(normalizedProductId) || normalizedProductId <= 0) {
          throw new Error("Produto inv\xE1lido.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido.");
        }
        const product = await prisma_default.product.findFirst({
          where: { id: normalizedProductId, restaurantId: normalizedRestaurantId },
          select: { id: true }
        });
        if (!product) {
          throw new Error("Produto n\xE3o encontrado neste restaurante.");
        }
        await prisma_default.productDiscount.deleteMany({
          where: { productId: normalizedProductId, restaurantId: normalizedRestaurantId }
        });
        return { message: "Desconto removido com sucesso." };
      }
    };
    DeleteProductDiscountService_default = new DeleteProductDiscountService();
  }
});

// src/modules/products/controllers/DeleteProductDiscountController.ts
var DeleteProductDiscountController, DeleteProductDiscountController_default;
var init_DeleteProductDiscountController = __esm({
  "src/modules/products/controllers/DeleteProductDiscountController.ts"() {
    init_DeleteProductDiscountService();
    DeleteProductDiscountController = class {
      async handle(req, res) {
        try {
          const result = await DeleteProductDiscountService_default.execute(
            Array.isArray(req.params.id) ? req.params.id[0] : req.params.id,
            req.user.restaurantId
          );
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel remover o desconto."
          });
        }
      }
    };
    DeleteProductDiscountController_default = new DeleteProductDiscountController();
  }
});

// src/middlewares/adminMiddleware.ts
import { UserRole as UserRole4 } from "@prisma/client";
function adminMiddleware(req, res, next) {
  if (req.user.role !== UserRole4.ADMIN) {
    return res.status(403).json({ error: "Acesso negado!" });
  }
  return next();
}
var init_adminMiddleware = __esm({
  "src/middlewares/adminMiddleware.ts"() {
  }
});

// src/middlewares/publicRestaurantBillingMiddleware.ts
import { InvoiceStatus as InvoiceStatus2 } from "@prisma/client";
async function resolveRestaurantId(req) {
  const directId = Number(
    req.params.restaurantId || req.query.restaurantId || req.body?.restaurantId || 0
  );
  if (Number.isInteger(directId) && directId > 0) {
    return directId;
  }
  const slug = String(req.params.slug || req.query.slug || "").trim();
  if (slug) {
    const restaurant = await prisma_default.restaurant.findUnique({
      where: { slug },
      select: { id: true }
    });
    return restaurant?.id || null;
  }
  if (req.path.endsWith("/default")) {
    const restaurant = await prisma_default.restaurant.findFirst({
      select: { id: true },
      orderBy: { id: "asc" }
    });
    return restaurant?.id || null;
  }
  return null;
}
async function publicRestaurantBillingMiddleware(req, res, next) {
  try {
    const restaurantId = await resolveRestaurantId(req);
    if (!restaurantId) {
      return next();
    }
    const openInvoices = await prisma_default.invoice.findMany({
      where: {
        restaurantId,
        status: { in: [InvoiceStatus2.PENDENTE, InvoiceStatus2.ATRASADO] }
      },
      select: { status: true, dueDate: true }
    });
    if (!hasBlockingInvoices(openInvoices, /* @__PURE__ */ new Date())) {
      return next();
    }
    return res.status(403).json({
      code: "BILLING_BLOCKED",
      blocked: true,
      error: "Restaurante temporariamente indispon\xEDvel"
    });
  } catch {
    return res.status(500).json({
      error: "Erro ao validar disponibilidade do restaurante"
    });
  }
}
var init_publicRestaurantBillingMiddleware = __esm({
  "src/middlewares/publicRestaurantBillingMiddleware.ts"() {
    init_prisma();
    init_billingRules();
  }
});

// src/modules/products/routes/productsRoutes.ts
import { Router as Router2 } from "express";
var router2, productsRoutes_default;
var init_productsRoutes = __esm({
  "src/modules/products/routes/productsRoutes.ts"() {
    init_CreateProductController();
    init_UpdateProductController();
    init_DeleteProductController();
    init_ListProductController();
    init_ListProductRatingsController();
    init_RateProductController();
    init_UpsertProductDiscountController();
    init_DeleteProductDiscountController();
    init_authMiddleware();
    init_adminMiddleware();
    init_publicRestaurantBillingMiddleware();
    router2 = Router2();
    router2.post("/", authMiddleware, adminMiddleware, CreateProductController_default.handle);
    router2.get("/", publicRestaurantBillingMiddleware, ListProductController_default.handle);
    router2.get("/ratings", ListProductRatingsController_default.handle);
    router2.post("/:id/rating", RateProductController_default.handle);
    router2.put("/:id", authMiddleware, adminMiddleware, UpdateProductController_default.handle);
    router2.put(
      "/:id/discount",
      authMiddleware,
      adminMiddleware,
      (req, res) => UpsertProductDiscountController_default.handle(req, res)
    );
    router2.delete(
      "/:id/discount",
      authMiddleware,
      adminMiddleware,
      (req, res) => DeleteProductDiscountController_default.handle(req, res)
    );
    router2.delete("/:id", authMiddleware, adminMiddleware, DeleteProductController_default.handle);
    productsRoutes_default = router2;
  }
});

// src/modules/orders/repositories/OrderRepository.ts
import {
  OrderRefundStatus,
  OrderStatus,
  PaymentMethod,
  OrderType,
  TableBillItemFinancialStatus,
  TableOrderFinancialStatus
} from "@prisma/client";
function isUniqueConstraintError(error2) {
  return Boolean(error2 && typeof error2 === "object" && "code" in error2 && error2.code === "P2002");
}
var operationalTableSelect, tableParticipantSelect, waiterReadyOrderInclude, OrderRepository, OrderRepository_default;
var init_OrderRepository = __esm({
  "src/modules/orders/repositories/OrderRepository.ts"() {
    init_prisma();
    operationalTableSelect = {
      id: true,
      number: true,
      active: true,
      restaurantId: true
    };
    tableParticipantSelect = {
      id: true,
      publicId: true,
      displayName: true
    };
    waiterReadyOrderInclude = {
      user: { select: { id: true, name: true } },
      table: { select: { id: true, number: true } },
      items: {
        select: {
          id: true,
          quantity: true,
          price: true,
          observation: true,
          ingredients: true,
          customizations: true,
          product: { select: { id: true, name: true } }
        }
      }
    };
    OrderRepository = class {
      async create(data, db = prisma_default) {
        return db.order.create({
          data,
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            participant: { select: tableParticipantSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async findAll(restaurantId, status, db = prisma_default) {
        return db.order.findMany({
          where: {
            restaurantId,
            NOT: {
              paid: false,
              paymentMethod: {
                in: [PaymentMethod.PIX, PaymentMethod.CARTAO]
              },
              payOnDelivery: false
            },
            ...status && { status }
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            participant: { select: tableParticipantSelect },
            items: {
              include: {
                product: true
              }
            },
            issueThread: {
              select: {
                orderId: true,
                isResolved: true,
                messages: {
                  orderBy: {
                    sentAt: "desc"
                  },
                  take: 40,
                  select: {
                    senderType: true,
                    message: true
                  }
                }
              }
            }
          },
          orderBy: {
            createdAt: "desc"
          }
        });
      }
      async countActiveOperationalOrders(restaurantId, db = prisma_default) {
        return db.order.count({
          where: {
            restaurantId,
            status: { in: [OrderStatus.PENDENTE, OrderStatus.PREPARANDO, OrderStatus.PRONTO] },
            NOT: {
              paid: false,
              paymentMethod: { in: [PaymentMethod.PIX, PaymentMethod.CARTAO] },
              payOnDelivery: false
            }
          }
        });
      }
      async findCourierOrders(restaurantId, courierId, status, db = prisma_default) {
        const allowedStatuses = [
          OrderStatus.PRONTO,
          OrderStatus.SAIU_PARA_ENTREGA,
          OrderStatus.ENTREGUE
        ];
        if (status && !allowedStatuses.includes(status)) {
          return [];
        }
        return db.order.findMany({
          where: {
            restaurantId,
            type: OrderType.DELIVERY,
            status: status || { in: allowedStatuses },
            OR: [
              { status: OrderStatus.PRONTO, assignedCourierId: null },
              { assignedCourierId: courierId }
            ],
            NOT: {
              paid: false,
              paymentMethod: { in: [PaymentMethod.PIX, PaymentMethod.CARTAO] },
              payOnDelivery: false
            }
          },
          include: {
            user: {
              select: { id: true, name: true, phone: true }
            },
            restaurant: {
              select: { id: true, name: true, whatsapp: true }
            },
            items: { include: { product: true } }
          },
          orderBy: { createdAt: "asc" }
        });
      }
      async findCourierOrderById(id, restaurantId, courierId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            id: Number(id),
            restaurantId,
            type: OrderType.DELIVERY,
            OR: [
              { status: OrderStatus.PRONTO, assignedCourierId: null },
              {
                status: { in: [OrderStatus.SAIU_PARA_ENTREGA, OrderStatus.ENTREGUE] },
                assignedCourierId: courierId
              }
            ],
            NOT: {
              paid: false,
              paymentMethod: { in: [PaymentMethod.PIX, PaymentMethod.CARTAO] },
              payOnDelivery: false
            }
          },
          include: {
            user: { select: { id: true, name: true, phone: true } },
            restaurant: { select: { id: true, name: true, whatsapp: true } },
            items: { include: { product: true } }
          }
        });
      }
      async updateStatus(id, status, restaurantId, db = prisma_default) {
        await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId
          },
          data: {
            status,
            ...status === OrderStatus.PREPARANDO ? { preparationStartedAt: /* @__PURE__ */ new Date(), readyAt: null } : {},
            ...status === OrderStatus.PRONTO ? { readyAt: /* @__PURE__ */ new Date() } : {}
          }
        });
        return this.findById(id, restaurantId, db);
      }
      async findReadyTableOrders(restaurantId, db = prisma_default) {
        return db.order.findMany({
          where: {
            restaurantId,
            type: OrderType.MESA,
            status: OrderStatus.PRONTO,
            NOT: {
              paid: false,
              paymentMethod: { in: [PaymentMethod.PIX, PaymentMethod.CARTAO] },
              payOnDelivery: false
            }
          },
          include: waiterReadyOrderInclude,
          orderBy: [{ readyAt: "asc" }, { createdAt: "asc" }]
        });
      }
      async findReadyTableOrderById(id, restaurantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            id: Number(id),
            restaurantId,
            type: OrderType.MESA,
            status: OrderStatus.PRONTO,
            NOT: {
              paid: false,
              paymentMethod: { in: [PaymentMethod.PIX, PaymentMethod.CARTAO] },
              payOnDelivery: false
            }
          },
          include: waiterReadyOrderInclude
        });
      }
      async updateStatusIfCurrent(id, status, restaurantId, expected, db = prisma_default) {
        const result = await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId,
            status: expected.status,
            refundStatus: {
              notIn: [OrderRefundStatus.PROCESSING, OrderRefundStatus.SUCCEEDED]
            },
            ...typeof expected.paid === "boolean" ? { paid: expected.paid } : {}
          },
          data: {
            status,
            ...status === OrderStatus.PREPARANDO ? { preparationStartedAt: /* @__PURE__ */ new Date(), readyAt: null } : {},
            ...status === OrderStatus.PRONTO ? { readyAt: /* @__PURE__ */ new Date() } : {}
          }
        });
        if (result.count !== 1) {
          const current = await this.findById(id, restaurantId, db);
          if (!current) {
            throw new Error("Pedido n\xE3o encontrado!");
          }
          throw new Error(
            "O pedido foi atualizado por outro processo. Atualize a tela e tente novamente."
          );
        }
        const updated = await this.findById(id, restaurantId, db);
        if (!updated) {
          throw new Error("Pedido n\xE3o encontrado ap\xF3s a atualiza\xE7\xE3o.");
        }
        return updated;
      }
      async confirmDeliveryReceived(id, restaurantId, db = prisma_default) {
        await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId,
            status: OrderStatus.ENTREGUE
          },
          data: {
            deliveryConfirmedAt: /* @__PURE__ */ new Date()
          }
        });
        return this.findById(id, restaurantId, db);
      }
      async confirmPayment(id, restaurantId, db = prisma_default) {
        const paidAt = /* @__PURE__ */ new Date();
        const result = await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId,
            paid: false,
            status: { not: OrderStatus.CANCELADO }
          },
          data: {
            paid: true,
            paidAt,
            paymentConfirmationPin: null,
            paymentConfirmationPinExpiresAt: null
          }
        });
        if (result.count === 1) {
          await db.order.updateMany({
            where: { id: Number(id), restaurantId, tableSessionId: { not: null } },
            data: { tableFinancialStatus: TableOrderFinancialStatus.PAID }
          });
          await db.tableBillItem.updateMany({
            where: { orderId: Number(id), restaurantId, canceledAt: null },
            data: { financialStatus: TableBillItemFinancialStatus.PAID, paidAt }
          });
        }
        const current = await this.findById(id, restaurantId, db);
        if (!current) {
          throw new Error("Pedido n\xE3o encontrado para confirmar o pagamento.");
        }
        if (result.count === 1 || current.paid === true) {
          return current;
        }
        if (current.status === OrderStatus.CANCELADO) {
          throw new Error("Pagamento recebido para um pedido cancelado; confirma\xE7\xE3o bloqueada.");
        }
        throw new Error("O pagamento n\xE3o p\xF4de ser confirmado no estado atual do pedido.");
      }
      async confirmPixPayment(id, restaurantId, {
        paymentProof,
        paymentProofImage
      } = {}, db = prisma_default) {
        const paidAt = /* @__PURE__ */ new Date();
        const result = await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId,
            paid: false,
            status: { not: OrderStatus.CANCELADO }
          },
          data: {
            paid: true,
            paidAt,
            paymentProof: String(paymentProof || "").trim() || null,
            paymentProofImage: String(paymentProofImage || "").trim() || null,
            paymentConfirmationPin: null,
            paymentConfirmationPinExpiresAt: null
          }
        });
        if (result.count === 1) {
          await db.order.updateMany({
            where: { id: Number(id), restaurantId, tableSessionId: { not: null } },
            data: { tableFinancialStatus: TableOrderFinancialStatus.PAID }
          });
          await db.tableBillItem.updateMany({
            where: { orderId: Number(id), restaurantId, canceledAt: null },
            data: { financialStatus: TableBillItemFinancialStatus.PAID, paidAt }
          });
        }
        const current = await this.findById(id, restaurantId, db);
        if (!current) {
          throw new Error("Pedido n\xE3o encontrado para confirmar o pagamento PIX.");
        }
        if (result.count === 1 || current.paid === true) {
          return current;
        }
        if (current.status === OrderStatus.CANCELADO) {
          throw new Error("Pagamento PIX recebido para um pedido cancelado; confirma\xE7\xE3o bloqueada.");
        }
        throw new Error("O pagamento PIX n\xE3o p\xF4de ser confirmado no estado atual do pedido.");
      }
      async setCardCheckoutSessionId(id, restaurantId, cardCheckoutSessionId, db = prisma_default) {
        await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId
          },
          data: {
            cardCheckoutSessionId
          }
        });
        return this.findById(id, restaurantId, db);
      }
      async deleteById(id, restaurantId, db = prisma_default) {
        await db.order.deleteMany({
          where: {
            id: Number(id),
            restaurantId
          }
        });
      }
      async deleteAllByRestaurant(restaurantId, db = prisma_default) {
        return db.order.deleteMany({
          where: {
            restaurantId
          }
        });
      }
      async setPaymentConfirmationPin(id, restaurantId, paymentConfirmationPin, paymentConfirmationPinExpiresAt, db = prisma_default) {
        await db.order.updateMany({
          where: {
            id: Number(id),
            restaurantId
          },
          data: {
            paymentConfirmationPin,
            paymentConfirmationPinExpiresAt
          }
        });
        return this.findById(id, restaurantId, db);
      }
      async findById(id, restaurantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            id: Number(id),
            restaurantId
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            participant: { select: tableParticipantSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async findByPublicIdForTableParticipant(publicId, tableSessionId, restaurantId, participantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            publicId,
            tableSessionId,
            restaurantId,
            participantId,
            type: OrderType.MESA
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            participant: { select: tableParticipantSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async findOperationalById(id, restaurantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            id: Number(id),
            restaurantId,
            NOT: {
              paid: false,
              paymentMethod: {
                in: [PaymentMethod.PIX, PaymentMethod.CARTAO]
              },
              payOnDelivery: false
            }
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            participant: { select: tableParticipantSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async findByIdForCustomer(id, customerId2, restaurantId, db = prisma_default) {
        const normalizedRestaurantId = Number(restaurantId || 0);
        return db.order.findFirst({
          where: {
            id: Number(id),
            userId: customerId2,
            ...Number.isInteger(normalizedRestaurantId) && normalizedRestaurantId > 0 ? { restaurantId: normalizedRestaurantId } : {}
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            participant: { select: tableParticipantSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async findByPixPaymentId(pixPaymentId, restaurantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            pixPaymentId,
            ...restaurantId ? {
              restaurantId
            } : {}
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async claimPixPaymentId(id, restaurantId, pixPaymentId, db = prisma_default) {
        const normalizedPaymentId = String(pixPaymentId || "").trim();
        if (!normalizedPaymentId) {
          throw new Error("Pagamento PIX inv\xE1lido para vincular ao pedido.");
        }
        const existingOwner = await db.order.findFirst({
          where: { pixPaymentId: normalizedPaymentId },
          select: { id: true }
        });
        if (existingOwner && existingOwner.id !== Number(id)) {
          throw new Error("Este pagamento PIX j\xE1 foi utilizado em outro pedido.");
        }
        let result;
        try {
          result = await db.order.updateMany({
            where: {
              id: Number(id),
              restaurantId,
              paid: false,
              status: { not: OrderStatus.CANCELADO },
              OR: [{ pixPaymentId: null }, { pixPaymentId: normalizedPaymentId }]
            },
            data: { pixPaymentId: normalizedPaymentId }
          });
        } catch (error2) {
          if (isUniqueConstraintError(error2)) {
            throw new Error("Este pagamento PIX j\xE1 foi utilizado em outro pedido.");
          }
          throw error2;
        }
        const current = await this.findById(id, restaurantId, db);
        if (!current) {
          throw new Error("Pedido n\xE3o encontrado para vincular o pagamento PIX.");
        }
        if (result.count === 1 || current.pixPaymentId === normalizedPaymentId) {
          return current;
        }
        if (current.status === OrderStatus.CANCELADO) {
          throw new Error("Pagamento PIX n\xE3o pode ser vinculado a um pedido cancelado.");
        }
        if (String(current.pixPaymentId || "").trim()) {
          throw new Error("O pedido j\xE1 est\xE1 vinculado a outro pagamento PIX.");
        }
        throw new Error("N\xE3o foi poss\xEDvel vincular o pagamento PIX ao pedido.");
      }
      async findByCardCheckoutSessionId(cardCheckoutSessionId, restaurantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            cardCheckoutSessionId,
            ...restaurantId ? {
              restaurantId
            } : {}
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            items: {
              include: {
                product: true
              }
            }
          }
        });
      }
      async findLatestByTable(tableId, restaurantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            tableId: Number(tableId),
            restaurantId: Number(restaurantId)
          },
          include: {
            user: {
              select: {
                id: true,
                name: true,
                email: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                id: true,
                name: true,
                whatsapp: true
              }
            },
            table: { select: operationalTableSelect },
            items: {
              include: {
                product: true
              }
            }
          },
          orderBy: {
            createdAt: "desc"
          }
        });
      }
      async findLatestByTableParticipant(tableSessionId, restaurantId, participantId, db = prisma_default) {
        return db.order.findFirst({
          where: {
            tableSessionId: Number(tableSessionId),
            restaurantId: Number(restaurantId),
            participantId: Number(participantId),
            type: OrderType.MESA
          },
          select: {
            publicId: true,
            total: true,
            status: true,
            type: true,
            paid: true,
            observation: true,
            createdAt: true,
            updatedAt: true,
            items: {
              select: {
                id: true,
                quantity: true,
                price: true,
                observation: true,
                ingredients: true,
                customizations: true,
                product: {
                  select: {
                    id: true,
                    name: true,
                    image: true
                  }
                }
              }
            }
          },
          orderBy: [{ createdAt: "desc" }, { id: "desc" }]
        });
      }
      async findByUserId(userId, restaurantId, db = prisma_default) {
        const normalizedRestaurantId = Number(restaurantId);
        const where = {
          userId: Number(userId)
        };
        if (Number.isFinite(normalizedRestaurantId) && normalizedRestaurantId > 0) {
          where.restaurantId = normalizedRestaurantId;
        }
        return db.order.findMany({
          where: {
            ...where,
            NOT: [
              {
                paymentMethod: PaymentMethod.PIX,
                paid: false,
                pixPaymentId: {
                  not: null
                }
              },
              {
                paymentMethod: PaymentMethod.CARTAO,
                paid: false,
                cardCheckoutSessionId: {
                  not: null
                }
              }
            ]
          },
          include: {
            items: {
              include: {
                product: true
              }
            },
            table: { select: operationalTableSelect },
            issueThread: {
              select: {
                orderId: true,
                isResolved: true,
                resolvedAt: true,
                resolvedByName: true
              }
            }
          },
          orderBy: {
            createdAt: "desc"
          }
        });
      }
    };
    OrderRepository_default = new OrderRepository();
  }
});

// src/validators/OrderValidator.ts
import { z as z6 } from "zod";
import { OrderType as OrderType2, PaymentMethod as PaymentMethod2 } from "@prisma/client";
var optionalCustomerPhoneSchema, createOrderSchema;
var init_OrderValidator = __esm({
  "src/validators/OrderValidator.ts"() {
    optionalCustomerPhoneSchema = z6.preprocess(
      (value) => value === null || typeof value === "string" && value.trim() === "" ? void 0 : value,
      z6.string().trim().refine((value) => {
        const digits = value.replace(/\D/g, "");
        return digits.length >= 10 && digits.length <= 13;
      }, "Informe um celular/WhatsApp v\xE1lido com DDD.").optional()
    );
    createOrderSchema = z6.object({
      restaurantId: z6.number().int().positive().optional(),
      customerName: z6.string().trim().min(2).optional(),
      customerCpf: z6.string().trim().min(11).optional(),
      customerPhone: optionalCustomerPhoneSchema,
      type: z6.nativeEnum(OrderType2),
      paymentMethod: z6.nativeEnum(PaymentMethod2).optional(),
      payOnDelivery: z6.boolean().optional(),
      payOnDeliveryMethod: z6.nativeEnum(PaymentMethod2).optional(),
      paid: z6.boolean().optional(),
      pixPaymentId: z6.string().trim().min(3).optional(),
      observation: z6.string().trim().optional(),
      tableId: z6.number().int().positive().optional(),
      address: z6.string().trim().optional(),
      number: z6.string().trim().optional(),
      district: z6.string().trim().optional(),
      city: z6.string().trim().optional(),
      state: z6.string().trim().optional(),
      zipCode: z6.string().trim().optional(),
      complement: z6.string().trim().optional(),
      couponRedemptionId: z6.number().int().positive().nullable().optional(),
      items: z6.array(
        z6.object({
          productId: z6.number().int().positive(),
          quantity: z6.number().int().positive().max(100, "A quantidade m\xE1xima por item \xE9 100."),
          observation: z6.string().trim().max(500, "A observa\xE7\xE3o do item deve ter no m\xE1ximo 500 caracteres.").optional(),
          ingredientIds: z6.array(z6.number().int().positive()).max(40).optional(),
          optionIds: z6.array(z6.number().int().positive()).max(100).optional(),
          selectedOptions: z6.array(
            z6.object({
              groupId: z6.number().int().positive(),
              optionIds: z6.array(z6.number().int().positive()).max(40)
            })
          ).max(20).optional()
        })
      ).min(1, "O pedido deve conter pelo menos um item.").max(50, "O pedido deve conter no m\xE1ximo 50 itens diferentes.")
    }).superRefine((data, ctx) => {
      const totalQuantity = data.items.reduce((total, item) => total + item.quantity, 0);
      if (totalQuantity > 200) {
        ctx.addIssue({
          code: z6.ZodIssueCode.custom,
          path: ["items"],
          message: "O pedido deve conter no m\xE1ximo 200 unidades no total."
        });
      }
      if (data.type !== OrderType2.DELIVERY) {
        return;
      }
      const phoneDigits = String(data.customerPhone || "").replace(/\D/g, "");
      if (phoneDigits.length < 10 || phoneDigits.length > 13) {
        ctx.addIssue({
          code: z6.ZodIssueCode.custom,
          path: ["customerPhone"],
          message: "Informe um celular/WhatsApp v\xE1lido para pedidos de delivery."
        });
      }
      const requiredAddressFields = [
        ["address", data.address],
        ["number", data.number],
        ["district", data.district],
        ["city", data.city]
      ];
      requiredAddressFields.forEach(([field, value]) => {
        const minimumLength = field === "number" ? 1 : 2;
        if (String(value || "").trim().length < minimumLength) {
          ctx.addIssue({
            code: z6.ZodIssueCode.custom,
            path: [field],
            message: `Informe ${field === "address" ? "a rua" : field === "number" ? "o n\xFAmero" : field === "district" ? "o bairro" : "a cidade"}.`
          });
        }
      });
      if (!/^\d{8}$/.test(String(data.zipCode || "").replace(/\D/g, ""))) {
        ctx.addIssue({
          code: z6.ZodIssueCode.custom,
          path: ["zipCode"],
          message: "Informe um CEP v\xE1lido com 8 n\xFAmeros."
        });
      }
      if (!/^[A-Za-z]{2}$/.test(String(data.state || "").trim())) {
        ctx.addIssue({
          code: z6.ZodIssueCode.custom,
          path: ["state"],
          message: "Informe uma UF v\xE1lida com duas letras."
        });
      }
      if (data.payOnDelivery === true) {
        if (data.type !== OrderType2.DELIVERY) {
          ctx.addIssue({
            code: z6.ZodIssueCode.custom,
            path: ["type"],
            message: "Pagar na entrega s\xF3 \xE9 permitido para pedidos de delivery."
          });
        }
        if (!data.payOnDeliveryMethod) {
          ctx.addIssue({
            code: z6.ZodIssueCode.custom,
            path: ["payOnDeliveryMethod"],
            message: "Informe o m\xE9todo de pagamento para pagar na entrega."
          });
        }
      }
    });
  }
});

// src/modules/tableSession/repositories/TableSessionRepository.ts
import { OrderStatus as OrderStatus2, TableSessionStatus } from "@prisma/client";
var operationalTableSelect2, TableSessionRepository, TableSessionRepository_default;
var init_TableSessionRepository = __esm({
  "src/modules/tableSession/repositories/TableSessionRepository.ts"() {
    init_prisma();
    operationalTableSelect2 = {
      id: true,
      number: true,
      active: true,
      restaurantId: true
    };
    TableSessionRepository = class {
      async create(data, db = prisma_default) {
        return db.tableSession.create({
          data,
          include: {
            table: { select: operationalTableSelect2 },
            openedBy: {
              select: {
                id: true,
                name: true
              }
            }
          }
        });
      }
      async findOpenedByTable(tableId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            tableId: Number(tableId),
            status: TableSessionStatus.OPEN,
            OR: [{ expiresAt: null }, { expiresAt: { gt: /* @__PURE__ */ new Date() } }]
          },
          include: {
            table: { select: operationalTableSelect2 }
          },
          orderBy: { openedAt: "desc" }
        });
      }
      async findActiveByTable(tableId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            tableId: Number(tableId),
            status: { in: [TableSessionStatus.OPEN, TableSessionStatus.CLOSING_REQUESTED] },
            OR: [{ expiresAt: null }, { expiresAt: { gt: /* @__PURE__ */ new Date() } }]
          },
          include: {
            table: { select: operationalTableSelect2 }
          },
          orderBy: { openedAt: "desc" }
        });
      }
      async findLatestOpenByTable(tableId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            tableId: Number(tableId),
            status: TableSessionStatus.OPEN
          },
          include: { table: { select: operationalTableSelect2 } },
          orderBy: { openedAt: "desc" }
        });
      }
      async listExpiredOpenByTable(tableId, db = prisma_default) {
        return db.tableSession.findMany({
          where: {
            tableId: Number(tableId),
            status: { in: [TableSessionStatus.OPEN, TableSessionStatus.CLOSING_REQUESTED] },
            expiresAt: { lte: /* @__PURE__ */ new Date() }
          },
          include: { table: { select: operationalTableSelect2 } },
          orderBy: [{ openedAt: "desc" }, { id: "desc" }]
        });
      }
      async findById(id, db = prisma_default) {
        return db.tableSession.findUnique({
          where: {
            id: Number(id)
          },
          include: {
            table: { select: operationalTableSelect2 }
          }
        });
      }
      async findBySessionToken(sessionToken, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            sessionToken,
            status: TableSessionStatus.OPEN,
            OR: [{ expiresAt: null }, { expiresAt: { gt: /* @__PURE__ */ new Date() } }]
          },
          include: {
            table: { select: operationalTableSelect2 }
          }
        });
      }
      async close(id, closedById, db = prisma_default) {
        return db.tableSession.update({
          where: {
            id: Number(id)
          },
          data: {
            status: TableSessionStatus.CLOSED,
            closedById,
            closedAt: /* @__PURE__ */ new Date()
          }
        });
      }
      async forceClose(id, closedById, reason, db = prisma_default) {
        return db.tableSession.update({
          where: { id: Number(id) },
          data: {
            status: TableSessionStatus.CLOSED,
            closedById,
            forceClosedById: closedById,
            forcedClosed: true,
            forceCloseReason: reason,
            closedAt: /* @__PURE__ */ new Date()
          }
        });
      }
      async listOpenByRestaurant(restaurantId, db = prisma_default) {
        return db.tableSession.findMany({
          where: {
            status: { in: [TableSessionStatus.OPEN, TableSessionStatus.CLOSING_REQUESTED] },
            OR: [{ expiresAt: null }, { expiresAt: { gt: /* @__PURE__ */ new Date() } }],
            table: {
              restaurantId
            }
          },
          select: {
            id: true,
            publicId: true,
            restaurantId: true,
            tableId: true,
            status: true,
            openedAt: true,
            expiresAt: true,
            table: { select: { id: true, number: true, active: true, restaurantId: true } },
            openedBy: { select: { id: true, name: true } }
          },
          orderBy: {
            openedAt: "desc"
          }
        });
      }
      async findBlockingOrdersForSession(tableId, restaurantId, openedAt, db = prisma_default) {
        return db.order.findMany({
          where: {
            tableId,
            restaurantId,
            createdAt: { gte: openedAt },
            status: { not: OrderStatus2.CANCELADO },
            OR: [{ status: { not: OrderStatus2.ENTREGUE } }, { paid: false }]
          },
          select: {
            id: true,
            status: true,
            paid: true,
            total: true,
            createdAt: true
          },
          orderBy: { createdAt: "asc" }
        });
      }
      async findOperationalBlockingOrdersForSession(tableId, restaurantId, openedAt, db = prisma_default) {
        return db.order.findMany({
          where: {
            tableId,
            restaurantId,
            createdAt: { gte: openedAt },
            status: { notIn: [OrderStatus2.CANCELADO, OrderStatus2.ENTREGUE] }
          },
          select: {
            id: true,
            status: true,
            paid: true,
            total: true,
            createdAt: true
          },
          orderBy: { createdAt: "asc" }
        });
      }
    };
    TableSessionRepository_default = new TableSessionRepository();
  }
});

// src/modules/payments/providers/providerCatalog.ts
function normalizePixProvider(value) {
  const provider = String(value || PIX_PROVIDERS.MERCADO_PAGO).trim().toUpperCase();
  if (Object.values(PIX_PROVIDERS).includes(provider)) {
    return provider;
  }
  return PIX_PROVIDERS.MERCADO_PAGO;
}
function normalizeCardProvider(value) {
  const provider = String(value || CARD_PROVIDERS.MERCADO_PAGO).trim().toUpperCase();
  if (Object.values(CARD_PROVIDERS).includes(provider)) {
    return provider;
  }
  return CARD_PROVIDERS.MERCADO_PAGO;
}
var PIX_PROVIDERS, CARD_PROVIDERS;
var init_providerCatalog = __esm({
  "src/modules/payments/providers/providerCatalog.ts"() {
    PIX_PROVIDERS = {
      MERCADO_PAGO: "MERCADO_PAGO",
      ASAAS: "ASAAS",
      PAGBANK: "PAGBANK",
      NUBANK: "NUBANK",
      PICPAY: "PICPAY"
    };
    CARD_PROVIDERS = {
      STRIPE: "STRIPE",
      MERCADO_PAGO: "MERCADO_PAGO",
      ASAAS: "ASAAS",
      PAGARME: "PAGARME",
      PAGBANK: "PAGBANK",
      STONE: "STONE",
      ZOOP: "ZOOP"
    };
  }
});

// src/modules/orders/services/pixPayload.ts
function extractErrorText(error2) {
  if (typeof error2 === "string") {
    return error2.trim().toLowerCase();
  }
  const asRecord = typeof error2 === "object" && error2 !== null ? error2 : null;
  const message = String(
    asRecord?.message || asRecord?.cause?.message || ""
  );
  const causeText = String(asRecord?.cause || "");
  return `${message} ${causeText}`.trim().toLowerCase();
}
function isMarketplaceSplitConfigurationError(error2) {
  const text = extractErrorText(error2);
  if (!text) {
    return false;
  }
  return text.includes("application_fee") || text.includes("marketplace") || text.includes("split") || text.includes("collector") || text.includes("platform") || text.includes("not allowed") || text.includes("unauthorized") || text.includes("invalid");
}
function parseProviderPaymentId(paymentId) {
  const normalizedPaymentId = String(paymentId || "").trim();
  if (normalizedPaymentId.toLowerCase().startsWith("asaas:")) {
    return {
      provider: PIX_PROVIDERS.ASAAS,
      rawPaymentId: normalizedPaymentId.slice("asaas:".length).trim()
    };
  }
  if (normalizedPaymentId.toLowerCase().startsWith("pagbank:")) {
    return {
      provider: PIX_PROVIDERS.PAGBANK,
      rawPaymentId: normalizedPaymentId.slice("pagbank:".length).trim()
    };
  }
  return {
    provider: PIX_PROVIDERS.MERCADO_PAGO,
    rawPaymentId: normalizedPaymentId
  };
}
function normalizeTxid(value) {
  const normalized = String(value || "").replace(/[^A-Za-z0-9]/g, "").slice(0, 25);
  return normalized || "***";
}
var init_pixPayload = __esm({
  "src/modules/orders/services/pixPayload.ts"() {
    init_providerCatalog();
  }
});

// src/modules/restaurantSettings/repositories/RestaurantSettingsRepository.ts
var RestaurantSettingsRepository, RestaurantSettingsRepository_default;
var init_RestaurantSettingsRepository = __esm({
  "src/modules/restaurantSettings/repositories/RestaurantSettingsRepository.ts"() {
    init_prisma();
    RestaurantSettingsRepository = class {
      async findByRestaurantId(restaurantId) {
        return prisma_default.restaurantSettings.findUnique({
          where: {
            restaurantId: Number(restaurantId)
          },
          include: {
            restaurant: {
              select: {
                name: true,
                email: true,
                phone: true,
                cnpj: true,
                slug: true,
                logo: true,
                coverImage: true,
                description: true,
                whatsapp: true,
                address: true,
                addressNumber: true,
                addressComplement: true,
                addressDistrict: true,
                city: true,
                state: true,
                zipCode: true
              }
            }
          }
        });
      }
      async findRestaurantById(restaurantId) {
        return prisma_default.restaurant.findUnique({
          where: {
            id: Number(restaurantId)
          },
          select: {
            id: true,
            active: true,
            name: true,
            email: true,
            phone: true,
            cnpj: true,
            slug: true,
            logo: true,
            coverImage: true,
            description: true,
            whatsapp: true,
            address: true,
            addressNumber: true,
            addressComplement: true,
            addressDistrict: true,
            city: true,
            state: true,
            zipCode: true,
            banners: {
              where: { active: true },
              select: { id: true, title: true, image: true },
              orderBy: { id: "asc" }
            }
          }
        });
      }
      async findDefaultActiveRestaurant() {
        return prisma_default.restaurant.findFirst({
          where: { active: true },
          select: { id: true },
          orderBy: { id: "asc" }
        });
      }
      async findPublicByRestaurantId(restaurantId) {
        return prisma_default.restaurantSettings.findUnique({
          where: {
            restaurantId: Number(restaurantId)
          },
          select: {
            restaurantId: true,
            primaryColor: true,
            deliveryFee: true,
            minimumOrder: true,
            freeShippingMinimum: true,
            acceptsDelivery: true,
            acceptsPickup: true,
            acceptsPix: true,
            acceptsCard: true,
            tableOrderingEnabled: true,
            waiterCallEnabled: true,
            billRequestEnabled: true,
            pixProvider: true,
            pixKey: true,
            whatsappEnabled: true,
            whatsappDisplayName: true,
            whatsappDefaultMessage: true,
            receiveOrdersOnWhatsapp: true,
            receiveStatusNotifications: true,
            instagram: true,
            facebook: true,
            tiktok: true,
            youtube: true,
            fontFamily: true,
            seoTitle: true,
            seoDescription: true,
            companyLegalName: true,
            ownerEmail: true,
            ownerPhone: true,
            businessHours: true,
            isOpenForOrders: true,
            averageDeliveryTime: true,
            autoAcceptOrders: true,
            trackingRequiresLogin: true,
            soundNotifications: true,
            maxConcurrentOrders: true,
            restaurant: {
              select: {
                name: true,
                active: true,
                slug: true,
                logo: true,
                coverImage: true,
                description: true,
                whatsapp: true,
                address: true,
                addressNumber: true,
                addressComplement: true,
                addressDistrict: true,
                city: true,
                state: true,
                zipCode: true,
                banners: {
                  where: { active: true },
                  select: { id: true, title: true, image: true },
                  orderBy: { id: "asc" }
                }
              }
            }
          }
        });
      }
      async create(data) {
        return prisma_default.restaurantSettings.create({
          data
        });
      }
      async update(restaurantId, data) {
        return prisma_default.restaurantSettings.update({
          where: {
            restaurantId: Number(restaurantId)
          },
          data
        });
      }
    };
    RestaurantSettingsRepository_default = new RestaurantSettingsRepository();
  }
});

// src/modules/restaurantSettings/utils/businessHours.ts
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function normalizeBusinessHours(value) {
  if (value === void 0) return void 0;
  if (value === null || Array.isArray(value) && value.length === 0) return [];
  if (!Array.isArray(value)) {
    throw new Error("Os hor\xE1rios de funcionamento devem ser uma lista com os 7 dias da semana.");
  }
  if (value.length !== BUSINESS_DAY_IDS.length) {
    throw new Error("Informe os hor\xE1rios dos 7 dias da semana.");
  }
  const days = /* @__PURE__ */ new Map();
  value.forEach((rawDay) => {
    if (!isRecord(rawDay)) {
      throw new Error("Dia inv\xE1lido nos hor\xE1rios de funcionamento.");
    }
    const id = String(rawDay.id || "").trim();
    if (!BUSINESS_DAY_IDS.includes(id)) {
      throw new Error("Dia inv\xE1lido nos hor\xE1rios de funcionamento.");
    }
    if (days.has(id)) {
      throw new Error(`O dia ${DAY_LABELS[id]} foi informado mais de uma vez.`);
    }
    days.set(id, rawDay);
  });
  return BUSINESS_DAY_IDS.map((id) => {
    const rawDay = days.get(id);
    if (!rawDay) {
      throw new Error(`Informe o hor\xE1rio de ${DAY_LABELS[id]}.`);
    }
    if (typeof rawDay.enabled !== "boolean") {
      throw new Error(`Informe se ${DAY_LABELS[id]} estar\xE1 aberto ou fechado.`);
    }
    const openingTime = String(rawDay.openingTime || "").trim();
    const closingTime = String(rawDay.closingTime || "").trim();
    if (!TIME_PATTERN.test(openingTime) || !TIME_PATTERN.test(closingTime)) {
      throw new Error(`Informe hor\xE1rios v\xE1lidos para ${DAY_LABELS[id]} no formato HH:mm.`);
    }
    if (rawDay.enabled && openingTime === closingTime) {
      throw new Error(
        `A abertura e o fechamento de ${DAY_LABELS[id]} n\xE3o podem ter o mesmo hor\xE1rio.`
      );
    }
    return {
      id,
      label: DAY_LABELS[id],
      enabled: rawDay.enabled,
      openingTime,
      closingTime
    };
  });
}
function tryNormalizeBusinessHours(value) {
  try {
    return normalizeBusinessHours(value) || [];
  } catch {
    return [];
  }
}
var BUSINESS_DAY_IDS, DAY_LABELS, TIME_PATTERN;
var init_businessHours = __esm({
  "src/modules/restaurantSettings/utils/businessHours.ts"() {
    BUSINESS_DAY_IDS = [
      "monday",
      "tuesday",
      "wednesday",
      "thursday",
      "friday",
      "saturday",
      "sunday"
    ];
    DAY_LABELS = {
      monday: "Segunda-feira",
      tuesday: "Ter\xE7a-feira",
      wednesday: "Quarta-feira",
      thursday: "Quinta-feira",
      friday: "Sexta-feira",
      saturday: "S\xE1bado",
      sunday: "Domingo"
    };
    TIME_PATTERN = /^(?:[01]\d|2[0-3]):[0-5]\d$/;
  }
});

// src/modules/orders/utils/restaurantAvailability.ts
function timeToMinutes(value) {
  const [hours, minutes] = value.split(":").map(Number);
  return hours * 60 + minutes;
}
function isValidTimeZone(value) {
  try {
    new Intl.DateTimeFormat("en-US", { timeZone: value }).format();
    return true;
  } catch {
    return false;
  }
}
function resolveRestaurantTimeZone(timeZone) {
  const requested = typeof timeZone === "string" ? timeZone.trim() : "";
  return requested && isValidTimeZone(requested) ? requested : DEFAULT_RESTAURANT_TIME_ZONE;
}
function getZonedClock(now, timeZone) {
  const resolvedTimeZone = resolveRestaurantTimeZone(timeZone);
  let formatter = zonedClockFormatters.get(resolvedTimeZone);
  if (!formatter) {
    formatter = new Intl.DateTimeFormat("en-US", {
      timeZone: resolvedTimeZone,
      weekday: "short",
      hour: "2-digit",
      minute: "2-digit",
      hourCycle: "h23"
    });
    zonedClockFormatters.set(resolvedTimeZone, formatter);
  }
  const parts = formatter.formatToParts(now);
  const weekday = parts.find((part) => part.type === "weekday")?.value;
  const hours = Number(parts.find((part) => part.type === "hour")?.value);
  const minutes = Number(parts.find((part) => part.type === "minute")?.value);
  const dayId = weekday ? DAY_IDS_BY_WEEKDAY[weekday] : void 0;
  if (!dayId || !Number.isInteger(hours) || !Number.isInteger(minutes)) {
    throw new Error("N\xE3o foi poss\xEDvel calcular o hor\xE1rio local do restaurante.");
  }
  return { dayId, minuteOfDay: hours * 60 + minutes };
}
function isRestaurantOpenForOrders(isOpenForOrders, businessHours, now = /* @__PURE__ */ new Date(), timeZone) {
  if (isOpenForOrders === false) return false;
  const schedule2 = tryNormalizeBusinessHours(businessHours);
  if (schedule2.length === 0) return true;
  const { dayId, minuteOfDay } = getZonedClock(now, timeZone);
  const currentDay = schedule2.find((day) => day.id === dayId);
  if (currentDay?.enabled) {
    const opening = timeToMinutes(currentDay.openingTime);
    const closing = timeToMinutes(currentDay.closingTime);
    if (opening < closing && minuteOfDay >= opening && minuteOfDay < closing) return true;
    if (opening > closing && minuteOfDay >= opening) return true;
  }
  const previousDay = schedule2.find((day) => day.id === PREVIOUS_DAY[dayId]);
  if (previousDay?.enabled) {
    const previousOpening = timeToMinutes(previousDay.openingTime);
    const previousClosing = timeToMinutes(previousDay.closingTime);
    if (previousOpening > previousClosing && minuteOfDay < previousClosing) return true;
  }
  return false;
}
function assertRestaurantIsOpenForOrders(isOpenForOrders, businessHours, now = /* @__PURE__ */ new Date(), timeZone) {
  if (!isRestaurantOpenForOrders(isOpenForOrders, businessHours, now, timeZone)) {
    throw new Error(RESTAURANT_CLOSED_MESSAGE);
  }
}
var RESTAURANT_CLOSED_MESSAGE, DEFAULT_RESTAURANT_TIME_ZONE, DAY_IDS_BY_WEEKDAY, PREVIOUS_DAY, zonedClockFormatters;
var init_restaurantAvailability = __esm({
  "src/modules/orders/utils/restaurantAvailability.ts"() {
    init_businessHours();
    RESTAURANT_CLOSED_MESSAGE = "O restaurante est\xE1 fechado no momento e n\xE3o est\xE1 recebendo pedidos.";
    DEFAULT_RESTAURANT_TIME_ZONE = "America/Sao_Paulo";
    DAY_IDS_BY_WEEKDAY = {
      Sun: "sunday",
      Mon: "monday",
      Tue: "tuesday",
      Wed: "wednesday",
      Thu: "thursday",
      Fri: "friday",
      Sat: "saturday"
    };
    PREVIOUS_DAY = {
      monday: "sunday",
      tuesday: "monday",
      wednesday: "tuesday",
      thursday: "wednesday",
      friday: "thursday",
      saturday: "friday",
      sunday: "saturday"
    };
    zonedClockFormatters = /* @__PURE__ */ new Map();
  }
});

// src/modules/billing/repositories/BillingRepository.ts
import { UserRole as UserRole5 } from "@prisma/client";
var BillingRepository, BillingRepository_default;
var init_BillingRepository = __esm({
  "src/modules/billing/repositories/BillingRepository.ts"() {
    init_prisma();
    BillingRepository = class {
      async findSubscriptionByRestaurantId(restaurantId, db = prisma_default) {
        return db.subscription.findUnique({
          where: {
            restaurantId
          },
          include: {
            restaurant: {
              select: {
                name: true,
                email: true,
                createdAt: true,
                users: {
                  where: { role: UserRole5.ADMIN },
                  orderBy: { createdAt: "asc" },
                  take: 1,
                  select: { id: true, name: true, email: true, createdAt: true }
                }
              }
            }
          }
        });
      }
      async updateSubscription(id, data, db = prisma_default) {
        return db.subscription.update({
          where: {
            id: Number(id)
          },
          data
        });
      }
      async createInvoice(data) {
        return prisma_default.invoice.create({
          data
        });
      }
      async findInvoiceByMonth(restaurantId, month, year) {
        return prisma_default.invoice.findFirst({
          where: {
            restaurantId,
            month,
            year
          }
        });
      }
      async findPendingInvoices() {
        return prisma_default.invoice.findMany({
          where: {
            status: {
              in: ["PENDENTE", "ATRASADO"]
            }
          },
          include: {
            restaurant: true
          }
        });
      }
      async updateInvoice(id, data, db = prisma_default) {
        return db.invoice.update({
          where: {
            id: Number(id)
          },
          data
        });
      }
      async deactivateRestaurant(id, db = prisma_default) {
        return db.restaurant.update({
          where: {
            id: Number(id)
          },
          data: {
            active: false
          }
        });
      }
      async activateRestaurant(id, db = prisma_default) {
        return db.restaurant.update({
          where: {
            id: Number(id)
          },
          data: {
            active: true
          }
        });
      }
      async findPaidOrdersByPeriod(restaurantId, startDate, endDate) {
        return prisma_default.order.findMany({
          where: {
            restaurantId,
            paid: true,
            createdAt: {
              gte: startDate,
              lte: endDate
            }
          }
        });
      }
      async findExpiredTrials() {
        return prisma_default.subscription.findMany({
          where: {
            status: "TESTE",
            trialEndsAt: {
              lte: /* @__PURE__ */ new Date()
            }
          },
          include: {
            restaurant: true
          }
        });
      }
      async findExpiredInvoices() {
        return prisma_default.invoice.findMany({
          where: {
            status: "PENDENTE",
            dueDate: {
              lt: /* @__PURE__ */ new Date()
            }
          },
          include: {
            restaurant: true
          }
        });
      }
      async findInvoiceById(id, db = prisma_default) {
        return db.invoice.findUnique({
          where: { id: Number(id) }
        });
      }
      async findInvoiceByIdAndRestaurantId(id, restaurantId) {
        return prisma_default.invoice.findFirst({
          where: {
            id: Number(id),
            restaurantId
          },
          include: {
            restaurant: { select: { name: true, email: true } }
          }
        });
      }
      async findAllSubscriptions() {
        return prisma_default.subscription.findMany();
      }
      async findInvoicesByRestaurantId(restaurantId) {
        return prisma_default.invoice.findMany({
          where: {
            restaurantId
          },
          orderBy: {
            createdAt: "desc"
          }
        });
      }
    };
    BillingRepository_default = new BillingRepository();
  }
});

// src/modules/billing/config/planConfig.ts
import { PlanType } from "@prisma/client";
function isAvailablePlan(plan) {
  return AVAILABLE_PLAN_TYPES.some((availablePlan) => availablePlan === plan);
}
var PLAN_CONFIG, AVAILABLE_PLAN_TYPES;
var init_planConfig = __esm({
  "src/modules/billing/config/planConfig.ts"() {
    PLAN_CONFIG = {
      [PlanType.BASICO]: {
        name: "B\xE1sico",
        monthlyFee: 149.9,
        trialDays: 30,
        availableForSale: true,
        features: ["Sistema de delivery", "Suporte padr\xE3o"]
      },
      [PlanType.PREMIUM]: {
        name: "Premium",
        monthlyFee: 249.9,
        trialDays: 30,
        availableForSale: true,
        features: [
          "Sistema de delivery",
          "Card\xE1pio digital com QR Code de mesa",
          "Suporte priorit\xE1rio"
        ]
      }
    };
    AVAILABLE_PLAN_TYPES = [PlanType.BASICO, PlanType.PREMIUM];
  }
});

// src/modules/billing/services/SplitService.ts
var SplitService, SplitService_default;
var init_SplitService = __esm({
  "src/modules/billing/services/SplitService.ts"() {
    init_BillingRepository();
    init_planConfig();
    SplitService = class {
      async execute({ restaurantId, orderTotal }) {
        const subscription = await BillingRepository_default.findSubscriptionByRestaurantId(restaurantId);
        if (!subscription) {
          return 0;
        }
        const plan = PLAN_CONFIG[subscription.plan];
        if (!plan) {
          throw new Error("Plano inv\xE1lido.");
        }
        return 0;
      }
    };
    SplitService_default = new SplitService();
  }
});

// src/modules/payments/providers/mercadoPagoOrderNotification.ts
function appendRestaurantId(baseUrl, restaurantId) {
  try {
    const notificationUrl = new URL(baseUrl);
    notificationUrl.searchParams.set("restaurantId", String(restaurantId));
    return notificationUrl.toString();
  } catch {
    const separator = baseUrl.includes("?") ? "&" : "?";
    return `${baseUrl}${separator}restaurantId=${encodeURIComponent(String(restaurantId))}`;
  }
}
function resolveMercadoPagoOrderNotificationUrl(restaurantId) {
  const normalizedRestaurantId = Number(restaurantId || 0);
  if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
    return "";
  }
  const explicitOrderNotificationUrl = String(process.env.MP_ORDER_NOTIFICATION_URL || "").trim();
  const backendUrl = String(process.env.BACKEND_URL || "").trim().replace(/\/+$/, "");
  const baseUrl = explicitOrderNotificationUrl || (backendUrl ? `${backendUrl}/orders/webhook/mercadopago` : "");
  if (!baseUrl) {
    return "";
  }
  return appendRestaurantId(baseUrl, normalizedRestaurantId);
}
function mercadoPagoOrderNotificationFields(restaurantId) {
  const notificationUrl = resolveMercadoPagoOrderNotificationUrl(restaurantId);
  return notificationUrl ? { notification_url: notificationUrl } : {};
}
var init_mercadoPagoOrderNotification = __esm({
  "src/modules/payments/providers/mercadoPagoOrderNotification.ts"() {
  }
});

// src/modules/orders/utils/productIngredients.ts
function money(value) {
  const normalized = Number(value);
  if (!Number.isFinite(normalized) || normalized < 0) {
    throw new Error("O produto possui um valor de op\xE7\xE3o inv\xE1lido.");
  }
  return Math.round((normalized + Number.EPSILON) * 100) / 100;
}
function uniquePositiveIds(values, field) {
  const ids = (values || []).map(Number);
  if (ids.some((id) => !Number.isInteger(id) || id <= 0)) {
    throw new Error(`${field} cont\xE9m uma op\xE7\xE3o inv\xE1lida.`);
  }
  if (new Set(ids).size !== ids.length) {
    throw new Error(`${field} cont\xE9m op\xE7\xF5es repetidas.`);
  }
  return ids;
}
function resolveExplicitOptionIds(product, selection) {
  const flatIds = uniquePositiveIds(selection.optionIds, "A montagem");
  const structured = selection.selectedOptions;
  if (!structured?.length) {
    return flatIds;
  }
  const seenGroups = /* @__PURE__ */ new Set();
  const structuredIds = [];
  structured.forEach((selectedGroup) => {
    const groupId = Number(selectedGroup.groupId);
    if (!Number.isInteger(groupId) || groupId <= 0) {
      throw new Error("A montagem cont\xE9m um grupo inv\xE1lido.");
    }
    if (seenGroups.has(groupId)) {
      throw new Error("A montagem cont\xE9m o mesmo grupo mais de uma vez.");
    }
    seenGroups.add(groupId);
    const group = (product.optionGroups || []).find((candidate) => candidate.id === groupId);
    if (!group || !group.active || group.restaurantId !== product.restaurantId) {
      throw new Error(`Grupo de op\xE7\xF5es inv\xE1lido para ${product.name}.`);
    }
    const ids = uniquePositiveIds(selectedGroup.optionIds, `O grupo ${group.name}`);
    if (ids.some((id) => !group.options.some((option) => option.id === id))) {
      throw new Error(`Uma op\xE7\xE3o n\xE3o pertence ao grupo ${group.name}.`);
    }
    structuredIds.push(...ids);
  });
  if (flatIds.length) {
    const flatSignature = [...flatIds].sort((a, b) => a - b).join(",");
    const structuredSignature = [...structuredIds].sort((a, b) => a - b).join(",");
    if (flatSignature !== structuredSignature) {
      throw new Error("Os campos optionIds e selectedOptions informam montagens diferentes.");
    }
  }
  return uniquePositiveIds(structuredIds, "A montagem");
}
function resolveLegacyIds(product, ingredientIds) {
  return ingredientIds.map((ingredientId) => {
    const matches = (product.optionGroups || []).flatMap(
      (group) => group.options.filter((option) => option.ingredientId === ingredientId)
    );
    if (matches.length !== 1) {
      throw new Error(`Ingrediente legado inv\xE1lido ou amb\xEDguo para ${product.name}.`);
    }
    return matches[0].id;
  });
}
function resolveOrderItemCustomizations(product, selection = {}) {
  const activeGroups = (product.optionGroups || []).filter((group) => group.active);
  if (!activeGroups.length) {
    return resolveLegacyProductIngredients(
      product,
      selection.ingredientIds?.length ? selection.ingredientIds : selection.optionIds
    );
  }
  activeGroups.forEach((group) => {
    if (group.restaurantId !== product.restaurantId) {
      throw new Error(`A configura\xE7\xE3o de ${product.name} pertence a outro restaurante.`);
    }
  });
  const legacyIds = uniquePositiveIds(selection.ingredientIds, "A montagem antiga");
  let selectedIds = resolveExplicitOptionIds(product, selection);
  if (!selectedIds.length && legacyIds.length) {
    selectedIds = resolveLegacyIds(product, legacyIds);
  }
  const allActiveOptions = activeGroups.flatMap(
    (group) => group.options.filter(
      (option) => option.active && option.ingredient.active && option.ingredient.restaurantId === product.restaurantId
    )
  );
  const allActiveOptionIds = new Set(allActiveOptions.map((option) => option.id));
  if (selectedIds.some((id) => !allActiveOptionIds.has(id))) {
    throw new Error(`Uma op\xE7\xE3o selecionada est\xE1 indispon\xEDvel para ${product.name}.`);
  }
  const customizations = activeGroups.map((group) => {
    const availableOptions = group.options.filter(
      (option) => option.active && option.ingredient.active && option.ingredient.restaurantId === product.restaurantId
    );
    const selected = availableOptions.filter((option) => selectedIds.includes(option.id));
    const minimum = group.required ? Math.max(1, group.minSelections) : group.minSelections;
    const maximum = group.selectionType === "SINGLE" ? 1 : group.maxSelections;
    if (availableOptions.length < minimum) {
      throw new Error(`O grupo ${group.name} est\xE1 sem op\xE7\xF5es suficientes. Avise o restaurante.`);
    }
    if (selected.length < minimum) {
      throw new Error(
        `Escolha pelo menos ${minimum} ${minimum === 1 ? "op\xE7\xE3o" : "op\xE7\xF5es"} em ${group.name}.`
      );
    }
    if (selected.length > maximum) {
      throw new Error(
        `Escolha no m\xE1ximo ${maximum} ${maximum === 1 ? "op\xE7\xE3o" : "op\xE7\xF5es"} em ${group.name}.`
      );
    }
    return {
      groupId: group.id,
      groupName: group.name,
      selectionType: group.selectionType,
      minSelections: minimum,
      maxSelections: maximum,
      options: selected.map((option) => ({
        optionId: option.id,
        ingredientId: option.ingredient.id,
        name: option.ingredient.name,
        price: money(option.ingredient.price)
      }))
    };
  });
  const selectedOptions = customizations.flatMap((group) => group.options);
  const additionalPrice = selectedOptions.reduce((total, option) => total + option.price, 0);
  const price = money(money(product.price) + additionalPrice);
  return {
    price,
    ingredients: selectedOptions.map((option) => ({
      id: option.ingredientId,
      name: option.name,
      price: option.price
    })),
    customizations
  };
}
function buildOrderItemCustomizationSnapshot(product, item) {
  const resolved = resolveOrderItemCustomizations(product, item);
  const basePricing = resolveProductBasePricing(product);
  const originalUnitPrice = roundMoney(resolved.price);
  const unitDiscount = roundMoney(basePricing.discountAmount);
  const effectiveUnitPrice = roundMoney(Math.max(originalUnitPrice - unitDiscount, 0));
  const quantity = Number(item.quantity);
  if (!Number.isInteger(quantity) || quantity <= 0) {
    throw new Error(`Quantidade inv\xE1lida para ${product.name}.`);
  }
  const observation = String(item.observation || "").trim();
  return {
    productId: product.id,
    quantity,
    price: effectiveUnitPrice,
    originalUnitPrice,
    unitDiscount,
    observation: observation || null,
    ingredients: resolved.ingredients,
    customizations: resolved.customizations
  };
}
function resolveLegacyProductIngredients(product, ingredientIds = []) {
  const selectedIds = uniquePositiveIds(ingredientIds, "A montagem");
  const available = product.ingredients.filter((ingredient) => ingredient.active);
  if (!available.length) {
    throw new Error(`${product.name} ainda n\xE3o possui op\xE7\xF5es de montagem configuradas.`);
  }
  const selected = selectedIds.map((id) => available.find((ingredient) => ingredient.id === id));
  if (selected.some((ingredient) => !ingredient)) {
    throw new Error(`Ingrediente inv\xE1lido para ${product.name}.`);
  }
  const requiredIds = available.filter((ingredient) => ingredient.required).map((ingredient) => ingredient.id);
  if (requiredIds.some((id) => !selectedIds.includes(id))) {
    throw new Error(`Selecione os ingredientes obrigat\xF3rios de ${product.name}.`);
  }
  const ingredients = selected.map((ingredient) => ({
    id: ingredient.id,
    name: ingredient.name,
    price: money(ingredient.price)
  }));
  return {
    price: money(money(product.price) + ingredients.reduce((sum, item) => sum + item.price, 0)),
    ingredients,
    customizations: []
  };
}
var init_productIngredients = __esm({
  "src/modules/orders/utils/productIngredients.ts"() {
    init_productDiscount();
  }
});

// src/modules/orders/services/couponRedemptionLifecycle.ts
import { CouponRedemptionStatus } from "@prisma/client";
async function reserveCouponRedemption({
  redemptionId,
  restaurantId,
  userId,
  db,
  now = /* @__PURE__ */ new Date()
}) {
  if (!redemptionId) return;
  const result = await db.couponRedemption.updateMany({
    where: {
      id: redemptionId,
      restaurantId,
      userId,
      status: CouponRedemptionStatus.CLAIMED,
      expiresAt: { gt: now }
    },
    data: {
      status: CouponRedemptionStatus.RESERVED,
      reservedAt: /* @__PURE__ */ new Date(),
      usedAt: null
    }
  });
  if (result.count !== 1) {
    const expired = await db.couponRedemption.updateMany({
      where: {
        id: redemptionId,
        restaurantId,
        userId,
        status: CouponRedemptionStatus.CLAIMED,
        expiresAt: { lte: now }
      },
      data: { status: CouponRedemptionStatus.EXPIRED }
    });
    if (expired.count === 1) {
      throw new Error("Este cupom expirou e n\xE3o pode mais ser utilizado.");
    }
    throw new Error("Este cupom j\xE1 foi reservado ou utilizado em outro pedido.");
  }
}
async function markCouponRedemptionUsedForOrder(orderId, restaurantId, db = prisma_default) {
  const order = await db.order.findFirst({
    where: { id: Number(orderId), restaurantId },
    select: { couponRedemptionId: true }
  });
  if (!order?.couponRedemptionId) return;
  const result = await db.couponRedemption.updateMany({
    where: {
      id: order.couponRedemptionId,
      restaurantId,
      status: CouponRedemptionStatus.RESERVED
    },
    data: { status: CouponRedemptionStatus.USED, usedAt: /* @__PURE__ */ new Date() }
  });
  if (result.count === 1) return;
  const current = await db.couponRedemption.findFirst({
    where: { id: order.couponRedemptionId, restaurantId },
    select: { status: true }
  });
  if (current?.status !== CouponRedemptionStatus.USED) {
    throw new Error("N\xE3o foi poss\xEDvel registrar o uso da recompensa deste pedido.");
  }
}
async function releaseCouponRedemptionForOrder(orderId, restaurantId, db = prisma_default, { now = /* @__PURE__ */ new Date() } = {}) {
  const order = await db.order.findFirst({
    where: { id: Number(orderId), restaurantId },
    select: { couponRedemptionId: true }
  });
  if (!order?.couponRedemptionId) return;
  const result = await db.couponRedemption.updateMany({
    where: {
      id: order.couponRedemptionId,
      restaurantId,
      status: CouponRedemptionStatus.RESERVED,
      expiresAt: { gt: now }
    },
    data: {
      status: CouponRedemptionStatus.CLAIMED,
      reservedAt: null,
      usedAt: null
    }
  });
  if (result.count !== 1) {
    const expired = await db.couponRedemption.updateMany({
      where: {
        id: order.couponRedemptionId,
        restaurantId,
        status: CouponRedemptionStatus.RESERVED,
        expiresAt: { lte: now }
      },
      data: {
        status: CouponRedemptionStatus.EXPIRED,
        reservedAt: null,
        usedAt: null
      }
    });
    if (expired.count === 1) {
      await db.order.updateMany({
        where: { id: Number(orderId), restaurantId },
        data: { couponRedemptionId: null }
      });
      return;
    }
    const current = await db.couponRedemption.findFirst({
      where: { id: order.couponRedemptionId, restaurantId },
      select: { status: true }
    });
    if (current?.status === CouponRedemptionStatus.USED) {
      return;
    }
    if (current?.status !== CouponRedemptionStatus.CLAIMED && current?.status !== CouponRedemptionStatus.EXPIRED) {
      throw new Error("N\xE3o foi poss\xEDvel liberar a recompensa deste pedido.");
    }
  }
  await db.order.updateMany({
    where: { id: Number(orderId), restaurantId },
    data: { couponRedemptionId: null }
  });
}
var init_couponRedemptionLifecycle = __esm({
  "src/modules/orders/services/couponRedemptionLifecycle.ts"() {
    init_prisma();
  }
});

// src/modules/orders/services/restoreOrderItemsStock.ts
async function restoreOrderItemsStock(tx, order) {
  const items = Array.isArray(order?.items) ? order.items : [];
  const quantityByProduct = /* @__PURE__ */ new Map();
  for (const item of items) {
    const productId = Number(item?.productId || 0);
    const quantity = Number(item?.quantity || 0);
    if (!Number.isInteger(productId) || productId <= 0) {
      continue;
    }
    if (!Number.isInteger(quantity) || quantity <= 0) {
      continue;
    }
    quantityByProduct.set(productId, (quantityByProduct.get(productId) || 0) + quantity);
  }
  for (const [productId, quantity] of quantityByProduct) {
    await tx.product.updateMany({
      where: {
        id: productId,
        restaurantId: Number(order.restaurantId),
        stock: { gte: 0 }
      },
      data: {
        stock: { increment: quantity },
        active: true
      }
    });
  }
}
var init_restoreOrderItemsStock = __esm({
  "src/modules/orders/services/restoreOrderItemsStock.ts"() {
  }
});

// src/modules/orders/services/OrderPixPaymentService.ts
import { MercadoPagoConfig, Payment } from "mercadopago";
function normalizeReferenceToken(value) {
  return String(value || "").toLowerCase().replace(/[^a-z0-9]/g, "").trim();
}
function doesProofContainTransactionId(paymentProof, transactionId) {
  const normalizedProof = normalizeReferenceToken(paymentProof);
  const normalizedTransactionId = normalizeReferenceToken(transactionId);
  if (!normalizedProof || !normalizedTransactionId) {
    return false;
  }
  return normalizedProof.includes(normalizedTransactionId);
}
function toCurrencyCents(value) {
  const amount = Number(value);
  if (!Number.isFinite(amount) || amount <= 0) {
    return null;
  }
  return Math.round((amount + Number.EPSILON) * 100);
}
var APPROVED_PAYMENT_STATUSES, APPROVED_ASAAS_PAYMENT_STATUSES, OrderPixPaymentService, OrderPixPaymentService_default;
var init_OrderPixPaymentService = __esm({
  "src/modules/orders/services/OrderPixPaymentService.ts"() {
    init_pixPayload();
    init_ProductRepository();
    init_RestaurantSettingsRepository();
    init_restaurantAvailability();
    init_SplitService();
    init_providerCatalog();
    init_mercadoPagoOrderNotification();
    init_productIngredients();
    init_prisma();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    init_restoreOrderItemsStock();
    APPROVED_PAYMENT_STATUSES = /* @__PURE__ */ new Set(["approved", "accredited", "paid"]);
    APPROVED_ASAAS_PAYMENT_STATUSES = /* @__PURE__ */ new Set(["received", "confirmed", "received_in_cash"]);
    OrderPixPaymentService = class {
      getPagBankBaseUrl() {
        return String(process.env.PAGBANK_API_BASE_URL || "https://api.pagseguro.com").trim().replace(/\/+$/, "");
      }
      async getPagBankToken(restaurantId) {
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        const token = String(settings?.pagbankToken || "").trim();
        if (!token) {
          throw new Error(
            "Pagamento PIX PagBank indispon\xEDvel. Configure o token PagBank nas configura\xE7\xF5es do restaurante."
          );
        }
        return token;
      }
      async fetchPagBankJson(url, token, init2 = {}) {
        const response = await fetch(url, {
          ...init2,
          headers: {
            Authorization: `Bearer ${token}`,
            Accept: "application/json",
            "Content-Type": "application/json",
            ...init2.headers || {}
          }
        });
        const body = await response.json();
        return { ok: response.ok, body };
      }
      getAsaasBaseUrl() {
        return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
      }
      async getAsaasAccessToken(restaurantId) {
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        const settingsToken = String(settings?.asaasAccessToken || "").trim();
        const globalToken = String(process.env.ASAAS_API_KEY || "").trim();
        const accessToken = settingsToken || (allowGlobalFallback ? globalToken : "");
        if (!accessToken) {
          throw new Error(
            "Pagamento PIX Asaas indisponivel. Configure token Asaas nas configuracoes do restaurante."
          );
        }
        return accessToken;
      }
      getAsaasError(payload, fallbackMessage) {
        if (!Array.isArray(payload?.errors) || payload.errors.length === 0) {
          return fallbackMessage;
        }
        const message = String(payload.errors[0]?.description || "").trim();
        return message || fallbackMessage;
      }
      normalizeAsaasStatus(value) {
        return String(value || "").trim().toLowerCase();
      }
      normalizeAsaasPaymentId(paymentId) {
        const normalized = String(paymentId || "").trim();
        if (!normalized) {
          return "";
        }
        if (normalized.toLowerCase().startsWith("asaas:")) {
          return normalized;
        }
        return `asaas:${normalized}`;
      }
      async fetchAsaasJson(url, accessToken, {
        method = "GET",
        body
      } = {}) {
        const response = await fetch(url, {
          method,
          headers: {
            "Content-Type": "application/json",
            access_token: accessToken
          },
          ...body !== void 0 ? { body: JSON.stringify(body) } : {}
        });
        const responseBody = await response.json();
        return {
          ok: response.ok,
          responseBody
        };
      }
      parseManualPaymentId(paymentId) {
        const normalizedPaymentId = String(paymentId || "").trim();
        const [
          prefix = "",
          provider = "",
          restaurant = "",
          createdAtMs = "",
          transactionIdFromId = ""
        ] = normalizedPaymentId.split(":");
        if (prefix !== "manual") {
          throw new Error("Pagamento PIX manual inv\xE1lido.");
        }
        const restaurantId = Number(restaurant || 0);
        const createdAtTimestamp = Number(createdAtMs || 0);
        const createdAt = new Date(createdAtTimestamp);
        const fallbackTransactionId = normalizeTxid(`${provider}${restaurantId}${createdAtTimestamp}`);
        const transactionId = normalizeTxid(transactionIdFromId || fallbackTransactionId);
        if (!Number.isInteger(restaurantId) || restaurantId <= 0 || !Number.isFinite(createdAtTimestamp) || Number.isNaN(createdAt.getTime()) || !transactionId) {
          throw new Error("Pagamento PIX manual inv\xE1lido.");
        }
        return {
          provider: this.normalizePixProvider(provider),
          restaurantId,
          createdAt,
          transactionId
        };
      }
      ensureManualPaymentConfirmationAllowed({
        paymentId,
        paymentProof
      }) {
        const parsed = this.parseManualPaymentId(paymentId);
        const normalizedProof = String(paymentProof || "").trim();
        if (normalizedProof.length < 6) {
          throw new Error(
            "Informe no comprovante o c\xF3digo/ID da transa\xE7\xE3o PIX para confirmar este pagamento."
          );
        }
        if (!doesProofContainTransactionId(normalizedProof, parsed.transactionId)) {
          throw new Error(
            "Comprovante PIX inv\xE1lido: o ID da transa\xE7\xE3o n\xE3o corresponde ao pagamento deste pedido."
          );
        }
      }
      async getMercadoPagoPaymentApi(restaurantId) {
        const normalizedRestaurantId = Number(restaurantId || 0);
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const settings = Number.isInteger(normalizedRestaurantId) && normalizedRestaurantId > 0 ? await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId) : null;
        const settingsToken = String(settings?.mercadoPagoAccessToken || "").trim();
        const globalToken = String(process.env.MP_ACCESS_TOKEN || "").trim();
        const accessToken = settingsToken || (allowGlobalFallback ? globalToken : "");
        if (!accessToken) {
          throw new Error(
            "Pagamento PIX indisponivel no momento. Configure access token Mercado Pago nas configuracoes do restaurante."
          );
        }
        const client = new MercadoPagoConfig({ accessToken });
        return new Payment(client);
      }
      normalizeCpf(value) {
        const digits = String(value || "").replace(/\D/g, "");
        return digits.length === 11 ? digits : null;
      }
      normalizeEmail(email, restaurantId) {
        const trimmed = String(email || "").trim();
        if (trimmed && trimmed.includes("@")) {
          return trimmed;
        }
        return `guest.pix.${restaurantId}.${Date.now()}@pecaja.local`;
      }
      normalizePaymentStatus(status) {
        return String(status || "").trim().toLowerCase();
      }
      normalizePixProvider(value) {
        return normalizePixProvider(value);
      }
      async calculateOrderSubtotal({
        restaurantId,
        items
      }) {
        const products = await Promise.all(
          items.map((item) => ProductRepository_default.findById(item.productId, restaurantId))
        );
        products.forEach((product, index) => {
          if (!product) {
            throw new Error(`Produto n\xE3o encontrado: ${items[index].productId}`);
          }
        });
        return items.reduce((acc, item, index) => {
          const product = products[index];
          const quantity = Number(item.quantity);
          if (!Number.isInteger(quantity) || quantity <= 0) {
            throw new Error(`Quantidade inv\xE1lida para ${product.name}.`);
          }
          const snapshot = buildOrderItemCustomizationSnapshot(product, item);
          return acc + Number(snapshot.price) * quantity;
        }, 0);
      }
      async createPixPayment({
        restaurantId,
        type,
        paymentMethod,
        pixProvider,
        items,
        address,
        number,
        district,
        city,
        state,
        customerName,
        customerCpf,
        customerPhone,
        userEmail,
        orderId: sourceOrderId,
        orderTotal,
        orderSubtotal,
        orderDeliveryFee
      }) {
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedType = String(type || "").toUpperCase();
        const normalizedPaymentMethod = String(paymentMethod || "").toUpperCase();
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para gerar PIX.");
        }
        const allowsPixType = normalizedType === "DELIVERY" || normalizedType === "MESA" || normalizedType === "RETIRADA";
        if (!allowsPixType || normalizedPaymentMethod !== "PIX") {
          throw new Error(
            "A geracao de PIX e permitida para pedidos DELIVERY, MESA ou RETIRADA com pagamento PIX."
          );
        }
        if (normalizedType === "DELIVERY") {
          const requiredAddressFields = [address, number, district, city, state].map((value) => String(value || "").trim()).filter(Boolean);
          if (requiredAddressFields.length < 5) {
            throw new Error("Informe o endereco completo para pedidos de delivery.");
          }
        }
        const settings = await RestaurantSettingsRepository_default.findPublicByRestaurantId(normalizedRestaurantId);
        assertRestaurantIsOpenForOrders(settings?.isOpenForOrders, settings?.businessHours);
        if (settings?.acceptsPix === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pagamentos por PIX no momento.");
        }
        if (normalizedType === "DELIVERY" && settings?.acceptsDelivery === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pedidos para delivery no momento.");
        }
        if (normalizedType === "RETIRADA" && settings?.acceptsPickup === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pedidos para retirada no momento.");
        }
        if (normalizedType === "MESA" && settings?.tableOrderingEnabled === false) {
          throw new Error("Os pedidos pelo card\xE1pio de mesa est\xE3o desativados no momento.");
        }
        void pixProvider;
        const resolvedPixProvider = this.normalizePixProvider(settings?.pixProvider);
        const pixKey = String(settings?.pixKey || "").trim();
        if (!pixKey) {
          throw new Error("Chave PIX n\xE3o configurada para este restaurante.");
        }
        const minimumOrder = Number(settings?.minimumOrder || 0);
        const deliveryFee = Number(settings?.deliveryFee || 0);
        const freeShippingMinimum = Number(settings?.freeShippingMinimum || 0);
        const persistedTotal = Number(orderTotal);
        const hasPersistedTotal = Number.isFinite(persistedTotal) && persistedTotal >= 0;
        const persistedSubtotal = Number(orderSubtotal);
        const persistedDeliveryFee = Number(orderDeliveryFee);
        const subtotal = hasPersistedTotal ? Number.isFinite(persistedSubtotal) && persistedSubtotal >= 0 ? persistedSubtotal : Math.max(
          persistedTotal - (Number.isFinite(persistedDeliveryFee) ? persistedDeliveryFee : normalizedType === "DELIVERY" ? Math.max(deliveryFee, 0) : 0),
          0
        ) : await this.calculateOrderSubtotal({
          restaurantId: normalizedRestaurantId,
          items
        });
        if (!hasPersistedTotal && normalizedType === "DELIVERY" && minimumOrder > 0 && subtotal < minimumOrder) {
          throw new Error(
            `Pedido m\xEDnimo sobre o subtotal para delivery: R$ ${minimumOrder.toFixed(2)}. A taxa de entrega \xE9 cobrada \xE0 parte.`
          );
        }
        const additionalFee = hasPersistedTotal ? Math.max(Number.isFinite(persistedDeliveryFee) ? persistedDeliveryFee : 0, 0) : normalizedType === "DELIVERY" ? freeShippingMinimum > 0 && subtotal >= freeShippingMinimum ? 0 : Math.max(deliveryFee, 0) : 0;
        const systemFee = await SplitService_default.execute({
          restaurantId: normalizedRestaurantId,
          orderTotal: subtotal
        });
        const totalAmount = Number(
          (hasPersistedTotal ? persistedTotal : subtotal + additionalFee).toFixed(2)
        );
        if (totalAmount <= 0) {
          throw new Error("Total do pedido inv\xE1lido para gerar cobran\xE7a PIX.");
        }
        const payerEmail = this.normalizeEmail(userEmail, normalizedRestaurantId);
        const payerName = String(customerName || "Cliente").trim();
        const cpf = this.normalizeCpf(customerCpf);
        const normalizedSystemFee = Number(systemFee || 0);
        if (resolvedPixProvider === PIX_PROVIDERS.PAGBANK) {
          const token = await this.getPagBankToken(normalizedRestaurantId);
          const backendUrl = String(process.env.BACKEND_URL || "").trim().replace(/\/+$/, "");
          const notificationUrl = backendUrl ? `${backendUrl}/orders/webhook/pagbank?restaurantId=${normalizedRestaurantId}` : "";
          const result = await this.fetchPagBankJson(
            `${this.getPagBankBaseUrl()}/orders`,
            token,
            {
              method: "POST",
              headers: { "x-idempotency-key": crypto.randomUUID() },
              body: JSON.stringify({
                reference_id: sourceOrderId ? `orderpix:${normalizedRestaurantId}:${sourceOrderId}` : `orderpix:${normalizedRestaurantId}:${Date.now()}`,
                customer: {
                  name: payerName || "Cliente",
                  email: payerEmail,
                  ...cpf ? { tax_id: cpf } : {}
                },
                items: [
                  {
                    reference_id: `restaurant-${normalizedRestaurantId}`,
                    name: `Pedido restaurante ${normalizedRestaurantId}`,
                    quantity: 1,
                    unit_amount: Math.round(totalAmount * 100)
                  }
                ],
                qr_codes: [{ amount: { value: Math.round(totalAmount * 100) } }],
                ...notificationUrl ? { notification_urls: [notificationUrl] } : {}
              })
            }
          );
          const providerError = String(result.body?.error_messages?.[0]?.description || "").trim();
          const orderId = String(result.body?.id || "").trim();
          const qrCode2 = String(result.body?.qr_codes?.[0]?.text || "").trim();
          if (!result.ok || !orderId || !qrCode2) {
            throw new Error(providerError || "N\xE3o foi poss\xEDvel gerar o Pix no PagBank.");
          }
          const base64Url = String(
            result.body?.qr_codes?.[0]?.links?.find((link) => link.rel === "QRCODE.BASE64")?.href || ""
          ).trim();
          let qrCodeBase642 = null;
          if (base64Url) {
            const imageResponse = await fetch(base64Url, {
              headers: { Authorization: `Bearer ${token}` }
            });
            if (imageResponse.ok) {
              qrCodeBase642 = (await imageResponse.text()).trim() || null;
            }
          }
          return {
            paymentId: `pagbank:${orderId}`,
            status: "WAITING",
            provider: resolvedPixProvider,
            totalAmount,
            qrCode: qrCode2,
            qrCodeBase64: qrCodeBase642,
            requiresStatusCheck: true
          };
        }
        if (resolvedPixProvider === PIX_PROVIDERS.ASAAS) {
          const accessToken = await this.getAsaasAccessToken(normalizedRestaurantId);
          const asaasBaseUrl = this.getAsaasBaseUrl();
          const privateSettings = await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId);
          const customerResult = await this.fetchAsaasJson(
            `${asaasBaseUrl}/v3/customers`,
            accessToken,
            {
              method: "POST",
              body: {
                name: payerName || "Cliente",
                email: payerEmail,
                ...cpf ? { cpfCnpj: cpf } : {},
                ...customerPhone ? { mobilePhone: String(customerPhone).replace(/\D/g, "") } : {}
              }
            }
          );
          if (!customerResult.ok || !String(customerResult.responseBody?.id || "").trim()) {
            throw new Error(
              this.getAsaasError(
                customerResult.responseBody,
                "Nao foi possivel criar/identificar cliente para pagamento PIX no Asaas."
              )
            );
          }
          const customerId2 = String(customerResult.responseBody.id || "").trim();
          const walletId = String(privateSettings?.gatewayMerchantId || "").trim();
          const platformWalletId = String(process.env.ASAAS_PLATFORM_WALLET_ID || "").trim();
          const buildAsaasPaymentBody = (includeSplit) => ({
            customer: customerId2,
            billingType: "PIX",
            value: totalAmount,
            dueDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
            description: `Pedido delivery restaurante ${normalizedRestaurantId}`,
            externalReference: sourceOrderId ? `orderpix:${normalizedRestaurantId}:${sourceOrderId}` : `orderpix:${normalizedRestaurantId}:${Date.now()}`,
            ...includeSplit && normalizedSystemFee > 0 && platformWalletId ? {
              split: [
                {
                  walletId: platformWalletId,
                  fixedValue: normalizedSystemFee
                },
                ...walletId ? [
                  {
                    walletId,
                    remainingValue: true
                  }
                ] : []
              ]
            } : {}
          });
          let paymentResult = await this.fetchAsaasJson(
            `${asaasBaseUrl}/v3/payments`,
            accessToken,
            {
              method: "POST",
              body: buildAsaasPaymentBody(normalizedSystemFee > 0)
            }
          );
          const shouldRetryWithoutSplit = normalizedSystemFee > 0 && !paymentResult.ok && isMarketplaceSplitConfigurationError(
            this.getAsaasError(paymentResult.responseBody, "Erro ao criar pagamento PIX no Asaas.")
          );
          if (shouldRetryWithoutSplit) {
            console.warn(
              "[ASAAS_PIX_SPLIT_FALLBACK] Asaas rejeitou split. Recriando pagamento sem split.",
              {
                restaurantId: normalizedRestaurantId,
                systemFee: normalizedSystemFee
              }
            );
            paymentResult = await this.fetchAsaasJson(
              `${asaasBaseUrl}/v3/payments`,
              accessToken,
              {
                method: "POST",
                body: buildAsaasPaymentBody(false)
              }
            );
          }
          if (!paymentResult.ok) {
            throw new Error(
              this.getAsaasError(
                paymentResult.responseBody,
                "Nao foi possivel gerar cobranca PIX no Asaas."
              )
            );
          }
          const asaasPaymentId = String(paymentResult.responseBody?.id || "").trim();
          if (!asaasPaymentId) {
            throw new Error("Asaas nao retornou id do pagamento PIX.");
          }
          const qrResult = await this.fetchAsaasJson(
            `${asaasBaseUrl}/v3/payments/${encodeURIComponent(asaasPaymentId)}/pixQrCode`,
            accessToken
          );
          if (!qrResult.ok) {
            throw new Error(
              this.getAsaasError(qrResult.responseBody, "Nao foi possivel gerar QR Code PIX no Asaas.")
            );
          }
          const qrCode2 = String(qrResult.responseBody?.payload || "").trim();
          const qrCodeBase642 = String(qrResult.responseBody?.encodedImage || "").trim();
          if (!qrCode2) {
            throw new Error("Asaas nao retornou payload PIX para pagamento.");
          }
          return {
            paymentId: this.normalizeAsaasPaymentId(asaasPaymentId),
            status: String(paymentResult.responseBody?.status || "PENDING"),
            provider: resolvedPixProvider,
            totalAmount,
            qrCode: qrCode2,
            qrCodeBase64: qrCodeBase642 || null,
            requiresStatusCheck: true
          };
        }
        if (resolvedPixProvider !== PIX_PROVIDERS.MERCADO_PAGO) {
          throw new Error(
            "Este provedor PIX ainda nao possui confirmacao automatica. Selecione Mercado Pago ou Asaas."
          );
        }
        const paymentApi = await this.getMercadoPagoPaymentApi(normalizedRestaurantId);
        const baseBody = {
          transaction_amount: totalAmount,
          description: `Pedido delivery restaurante ${normalizedRestaurantId}`,
          payment_method_id: "pix",
          payer: {
            email: payerEmail,
            first_name: payerName || "Cliente",
            ...cpf ? {
              identification: {
                type: "CPF",
                number: cpf
              }
            } : {}
          },
          metadata: {
            restaurant_id: String(normalizedRestaurantId),
            source: "order_checkout",
            provider: resolvedPixProvider
          },
          ...mercadoPagoOrderNotificationFields(normalizedRestaurantId),
          external_reference: sourceOrderId ? `orderpix:${normalizedRestaurantId}:${sourceOrderId}` : `orderpix:${normalizedRestaurantId}:${Date.now()}`
        };
        let response;
        if (normalizedSystemFee > 0) {
          try {
            response = await paymentApi.create({
              body: {
                ...baseBody,
                application_fee: normalizedSystemFee
              }
            });
          } catch (error2) {
            if (!isMarketplaceSplitConfigurationError(error2)) {
              throw error2;
            }
            console.warn(
              "[PIX_SPLIT_FALLBACK] Mercado Pago rejeitou application_fee. Recriando pagamento sem split.",
              {
                restaurantId: normalizedRestaurantId,
                systemFee: normalizedSystemFee
              }
            );
            response = await paymentApi.create({
              body: baseBody
            });
          }
        } else {
          response = await paymentApi.create({
            body: baseBody
          });
        }
        const payment = typeof response === "object" && response !== null ? response.body ?? response : {};
        const paymentData = payment;
        const transactionData = paymentData?.point_of_interaction?.transaction_data || {};
        const qrCode = String(transactionData?.qr_code || "").trim();
        const qrCodeBase64 = String(transactionData?.qr_code_base64 || "").trim();
        if (!paymentData?.id || !qrCode) {
          throw new Error("N\xE3o foi poss\xEDvel gerar o QR Code PIX no momento.");
        }
        return {
          paymentId: String(paymentData.id),
          status: String(paymentData.status || "pending"),
          provider: resolvedPixProvider,
          totalAmount,
          qrCode,
          qrCodeBase64: qrCodeBase64 || null,
          requiresStatusCheck: true
        };
      }
      async getPaymentStatus({ paymentId, restaurantId }) {
        const normalizedPaymentId = String(paymentId || "").trim();
        if (!normalizedPaymentId) {
          throw new Error("Pagamento PIX inv\xE1lido.");
        }
        if (normalizedPaymentId.startsWith("manual:")) {
          throw new Error("Pagamento PIX manual nao e permitido.");
        }
        const parsedPaymentId = parseProviderPaymentId(normalizedPaymentId);
        const normalizedRestaurantIdNumber = Number(restaurantId || 0);
        if (parsedPaymentId.provider === PIX_PROVIDERS.ASAAS) {
          const effectiveRestaurantId = Number.isInteger(normalizedRestaurantIdNumber) && normalizedRestaurantIdNumber > 0 ? normalizedRestaurantIdNumber : 0;
          if (!effectiveRestaurantId) {
            throw new Error("Restaurante inv\xE1lido para consulta de pagamento PIX Asaas.");
          }
          const accessToken = await this.getAsaasAccessToken(effectiveRestaurantId);
          const asaasBaseUrl = this.getAsaasBaseUrl();
          const statusResult = await this.fetchAsaasJson(
            `${asaasBaseUrl}/v3/payments/${encodeURIComponent(parsedPaymentId.rawPaymentId)}`,
            accessToken
          );
          if (!statusResult.ok) {
            throw new Error(
              this.getAsaasError(
                statusResult.responseBody,
                "Nao foi possivel consultar pagamento PIX Asaas."
              )
            );
          }
          const status2 = this.normalizeAsaasStatus(statusResult.responseBody?.status);
          const amount = Number(statusResult.responseBody?.value);
          return {
            paymentId: normalizedPaymentId,
            status: status2,
            provider: PIX_PROVIDERS.ASAAS,
            isApproved: APPROVED_ASAAS_PAYMENT_STATUSES.has(status2),
            sameRestaurant: true,
            externalReference: String(statusResult.responseBody?.externalReference || "").trim(),
            amount: Number.isFinite(amount) ? amount : null,
            currency: String(statusResult.responseBody?.currency || "BRL").trim().toUpperCase(),
            requiresStatusCheck: true
          };
        }
        if (parsedPaymentId.provider === PIX_PROVIDERS.PAGBANK) {
          if (!normalizedRestaurantIdNumber) {
            throw new Error("Restaurante inv\xE1lido para consultar Pix PagBank.");
          }
          const token = await this.getPagBankToken(normalizedRestaurantIdNumber);
          const result = await this.fetchPagBankJson(
            `${this.getPagBankBaseUrl()}/orders/${encodeURIComponent(parsedPaymentId.rawPaymentId)}`,
            token
          );
          if (!result.ok) {
            throw new Error("N\xE3o foi poss\xEDvel consultar o Pix no PagBank.");
          }
          const statuses = (result.body?.charges || []).map(
            (charge) => String(charge.status || "").toUpperCase()
          );
          const isApproved = statuses.includes("PAID");
          return {
            paymentId: normalizedPaymentId,
            status: isApproved ? "paid" : statuses[0] || "waiting",
            provider: PIX_PROVIDERS.PAGBANK,
            isApproved,
            sameRestaurant: true,
            requiresStatusCheck: true
          };
        }
        const paymentApi = await this.getMercadoPagoPaymentApi(
          Number.isInteger(normalizedRestaurantIdNumber) && normalizedRestaurantIdNumber > 0 ? normalizedRestaurantIdNumber : void 0
        );
        const response = await paymentApi.get({
          id: parsedPaymentId.rawPaymentId
        });
        const payment = typeof response === "object" && response !== null ? response.body ?? response : {};
        const paymentData = payment;
        const status = this.normalizePaymentStatus(paymentData?.status);
        const metadataRestaurantId = String(paymentData?.metadata?.restaurant_id || "").trim();
        const normalizedRestaurantId = String(restaurantId || "").trim();
        const sameRestaurant = !normalizedRestaurantId || !metadataRestaurantId || metadataRestaurantId === normalizedRestaurantId;
        return {
          paymentId: normalizedPaymentId,
          status,
          provider: PIX_PROVIDERS.MERCADO_PAGO,
          isApproved: APPROVED_PAYMENT_STATUSES.has(status),
          sameRestaurant,
          requiresStatusCheck: true
        };
      }
      async ensurePaymentApproved({
        paymentId,
        restaurantId,
        expectedOrderId,
        expectedAmount,
        expectedCurrency = "BRL"
      }) {
        const statusResult = await this.getPaymentStatus({
          paymentId,
          restaurantId
        });
        if (!statusResult.sameRestaurant) {
          throw new Error("Este pagamento PIX n\xE3o pertence ao restaurante do pedido.");
        }
        if (!statusResult.isApproved) {
          throw new Error("Pagamento PIX ainda n\xE3o foi aprovado.");
        }
        if (statusResult.provider === PIX_PROVIDERS.ASAAS) {
          const normalizedRestaurantId = Number(restaurantId || 0);
          const normalizedOrderId = Number(expectedOrderId || 0);
          const expectedAmountInCents = toCurrencyCents(expectedAmount);
          const actualAmountInCents = toCurrencyCents(statusResult.amount);
          const normalizedExpectedCurrency = String(expectedCurrency || "").trim().toUpperCase();
          if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0 || !Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0 || expectedAmountInCents === null || !normalizedExpectedCurrency) {
            throw new Error("N\xE3o foi poss\xEDvel validar o v\xEDnculo do pagamento PIX com o pedido.");
          }
          const expectedReference = `orderpix:${normalizedRestaurantId}:${normalizedOrderId}`;
          if (statusResult.externalReference !== expectedReference) {
            throw new Error("Pagamento PIX n\xE3o corresponde ao pedido informado.");
          }
          if (actualAmountInCents === null || actualAmountInCents !== expectedAmountInCents) {
            throw new Error("O valor do pagamento PIX n\xE3o corresponde ao total do pedido.");
          }
          if (statusResult.currency !== normalizedExpectedCurrency) {
            throw new Error("A moeda do pagamento PIX n\xE3o corresponde \xE0 moeda do pedido.");
          }
        }
        return statusResult;
      }
      async attachPaymentToOrder({
        orderId,
        restaurantId,
        paymentId
      }) {
        const normalizedPaymentId = String(paymentId || "").trim();
        if (!normalizedPaymentId) {
          throw new Error("O provedor n\xE3o retornou um identificador de pagamento PIX.");
        }
        await OrderRepository_default.claimPixPaymentId(orderId, restaurantId, normalizedPaymentId);
      }
      async removePendingOrderAfterPaymentFailure({
        orderId,
        restaurantId
      }) {
        await prisma_default.$transaction(async (tx) => {
          const pendingOrder = await OrderRepository_default.findById(orderId, restaurantId, tx);
          if (pendingOrder) {
            await restoreOrderItemsStock(tx, pendingOrder);
          }
          await releaseCouponRedemptionForOrder(orderId, restaurantId, tx);
          await OrderRepository_default.deleteById(orderId, restaurantId, tx);
        });
      }
    };
    OrderPixPaymentService_default = new OrderPixPaymentService();
  }
});

// src/services/customerNotifier.ts
function getErrorMessage(error2) {
  return error2 instanceof Error ? error2.message : "unknown";
}
async function resolveCustomerWhatsappPreference(restaurantId) {
  if (restaurantId === void 0 || restaurantId === null || restaurantId === "") {
    return { enabled: true };
  }
  const normalizedRestaurantId = Number(restaurantId);
  if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
    console.error("[CUSTOMER_NOTIFICATION_SETTINGS_ERROR]", {
      restaurantId,
      message: "restaurantId inv\xE1lido"
    });
    return {
      enabled: false,
      reason: "notification_preference_lookup_failed"
    };
  }
  try {
    const settings = await prisma_default.restaurantSettings.findUnique({
      where: { restaurantId: normalizedRestaurantId },
      select: {
        whatsappEnabled: true,
        receiveStatusNotifications: true
      }
    });
    if (!settings) {
      return {
        enabled: false,
        reason: "restaurant_settings_not_found"
      };
    }
    if ("whatsappEnabled" in settings && settings.whatsappEnabled === false) {
      return {
        enabled: false,
        reason: "whatsapp_disabled"
      };
    }
    if ("receiveStatusNotifications" in settings && settings.receiveStatusNotifications === false) {
      return {
        enabled: false,
        reason: "status_notifications_disabled"
      };
    }
    return { enabled: true };
  } catch (error2) {
    console.error("[CUSTOMER_NOTIFICATION_SETTINGS_ERROR]", {
      restaurantId: normalizedRestaurantId,
      message: getErrorMessage(error2)
    });
    return {
      enabled: false,
      reason: "notification_preference_lookup_failed"
    };
  }
}
function resolveProvider() {
  if (configuredProvider && configuredProvider !== "none") {
    return configuredProvider;
  }
  if (whatsappWebhookUrl) {
    return "whatsapp_webhook";
  }
  return "none";
}
function normalizeToE164Br(phone) {
  const digits = String(phone || "").replace(/\D/g, "");
  if (!digits) {
    return "";
  }
  if (/^55\d{10,11}$/.test(digits)) {
    return `+${digits}`;
  }
  if (/^\d{10}$/.test(digits)) {
    return `+55${digits}`;
  }
  if (/^\d{11}$/.test(digits)) {
    return `+55${digits}`;
  }
  return "";
}
function formatCurrencyBrl(value) {
  const amount = Number(value || 0);
  return new Intl.NumberFormat("pt-BR", {
    style: "currency",
    currency: "BRL"
  }).format(Number.isFinite(amount) ? amount : 0);
}
function buildCustomerMessage({
  customerName,
  restaurantName,
  orderId,
  total,
  paymentMethod
}) {
  const resolvedCustomerName = String(customerName || "Cliente").trim();
  const resolvedRestaurantName = String(restaurantName || "restaurante").trim();
  const resolvedPaymentMethod = String(paymentMethod || "PIX").toUpperCase();
  return [
    `Oi, ${resolvedCustomerName}!`,
    `Seu pagamento via ${resolvedPaymentMethod} foi confirmado com sucesso.`,
    `Pedido #${orderId} confirmado no ${resolvedRestaurantName}.`,
    `Total: ${formatCurrencyBrl(total)}.`,
    "Agora e so aguardar, seu pedido entrou em preparo."
  ].join("\n");
}
function buildOrderStatusChangedMessage({
  customerName,
  restaurantName,
  orderId,
  status
}) {
  const resolvedCustomerName = String(customerName || "Cliente").trim();
  const resolvedRestaurantName = String(restaurantName || "restaurante").trim();
  const resolvedStatus = String(status || "EM ANDAMENTO").replace(/_/g, " ").toUpperCase();
  return [
    `Oi, ${resolvedCustomerName}!`,
    `Atualizacao do seu pedido #${orderId} no ${resolvedRestaurantName}.`,
    `Novo status: ${resolvedStatus}.`
  ].join("\n");
}
function buildRestaurantPinRequestMessage({
  restaurantName,
  orderId,
  requestedByRole
}) {
  const resolvedRestaurantName = String(restaurantName || "restaurante").trim();
  const normalizedRole = String(requestedByRole || "MOTOQUEIRO").toUpperCase();
  const requesterLabel = normalizedRole === "ADMIN" ? "Admin" : "Motoqueiro";
  return [
    `Notificacao - ${resolvedRestaurantName}`,
    `${requesterLabel} solicitou PIN de confirmacao de pagamento.`,
    `Pedido #${orderId}.`
  ].join("\n");
}
function buildRestaurantOrderIssueReportedMessage({
  restaurantName,
  orderId,
  customerName,
  customerPhone,
  issueMessage,
  orderStatus,
  orderType,
  paymentMethod,
  total,
  addressLabel,
  itemsSummary,
  createdAt
}) {
  const resolvedRestaurantName = String(restaurantName || "restaurante").trim();
  const resolvedCustomerName = String(customerName || "Cliente").trim();
  const resolvedCustomerPhone = String(customerPhone || "").trim();
  const resolvedIssueMessage = String(issueMessage || "").trim().slice(0, 600);
  const resolvedOrderStatus = String(orderStatus || "N/A").trim().replace(/_/g, " ").toUpperCase();
  const resolvedOrderType = String(orderType || "N/A").trim().replace(/_/g, " ").toUpperCase();
  const resolvedPaymentMethod = String(paymentMethod || "N/A").trim().replace(/_/g, " ").toUpperCase();
  const resolvedAddressLabel = String(addressLabel || "").trim();
  const resolvedItemsSummary = Array.isArray(itemsSummary) ? itemsSummary.map((item) => String(item || "").trim()).filter(Boolean).slice(0, 8) : [];
  const resolvedCreatedAt = String(createdAt || "").trim();
  const resolvedCreatedAtText = resolvedCreatedAt ? new Date(resolvedCreatedAt).toLocaleString("pt-BR") : "N/A";
  return [
    `Notificacao - ${resolvedRestaurantName}`,
    `Cliente ${resolvedCustomerName} relatou problema no pedido #${orderId}.`,
    resolvedCustomerPhone ? `Telefone do cliente: ${resolvedCustomerPhone}.` : null,
    `Status: ${resolvedOrderStatus} | Tipo: ${resolvedOrderType} | Pagamento: ${resolvedPaymentMethod}.`,
    `Total: ${formatCurrencyBrl(total)}.`,
    resolvedAddressLabel ? `Endereco: ${resolvedAddressLabel}.` : null,
    resolvedItemsSummary.length > 0 ? `Itens: ${resolvedItemsSummary.join("; ")}.` : null,
    `Criado em: ${resolvedCreatedAtText}.`,
    `Mensagem: ${resolvedIssueMessage || "(sem detalhes)"}`
  ].filter(Boolean).join("\n");
}
async function notifyViaWhatsappWebhook({
  restaurantWhatsapp,
  customerPhone,
  customerName,
  restaurantName,
  orderId,
  total,
  paymentMethod
}) {
  if (!whatsappWebhookUrl) {
    return {
      sent: false,
      reason: "webhook_not_configured"
    };
  }
  const from = normalizeToE164Br(restaurantWhatsapp);
  if (!from) {
    return {
      sent: false,
      reason: "restaurant_whatsapp_not_configured"
    };
  }
  const to = normalizeToE164Br(customerPhone);
  if (!to) {
    return {
      sent: false,
      reason: "invalid_or_missing_phone"
    };
  }
  const message = buildCustomerMessage({
    customerName,
    restaurantName,
    orderId,
    total,
    paymentMethod
  });
  const headers = {
    "Content-Type": "application/json"
  };
  if (whatsappWebhookToken) {
    headers.Authorization = `Bearer ${whatsappWebhookToken}`;
  }
  const response = await fetch(whatsappWebhookUrl, {
    method: "POST",
    headers,
    body: JSON.stringify({
      channel: "whatsapp",
      from,
      to,
      message,
      metadata: {
        orderId,
        restaurantWhatsapp: from
      }
    })
  });
  if (!response.ok) {
    const body = await response.text().catch(() => "");
    throw new Error(`Webhook respondeu ${response.status}: ${body}`.trim());
  }
  return {
    sent: true,
    provider: "whatsapp_webhook",
    from,
    to
  };
}
async function notifyOrderStatusChangedViaWhatsappWebhook({
  restaurantWhatsapp,
  customerPhone,
  customerName,
  restaurantName,
  orderId,
  status
}) {
  if (!whatsappWebhookUrl) {
    return {
      sent: false,
      reason: "webhook_not_configured"
    };
  }
  const from = normalizeToE164Br(restaurantWhatsapp);
  if (!from) {
    return {
      sent: false,
      reason: "restaurant_whatsapp_not_configured"
    };
  }
  const to = normalizeToE164Br(customerPhone);
  if (!to) {
    return {
      sent: false,
      reason: "invalid_or_missing_phone"
    };
  }
  const message = buildOrderStatusChangedMessage({
    customerName,
    restaurantName,
    orderId,
    status
  });
  const headers = {
    "Content-Type": "application/json"
  };
  if (whatsappWebhookToken) {
    headers.Authorization = `Bearer ${whatsappWebhookToken}`;
  }
  const response = await fetch(whatsappWebhookUrl, {
    method: "POST",
    headers,
    body: JSON.stringify({
      channel: "whatsapp",
      from,
      to,
      message,
      metadata: {
        orderId,
        status,
        restaurantWhatsapp: from
      }
    })
  });
  if (!response.ok) {
    const body = await response.text().catch(() => "");
    throw new Error(`Webhook respondeu ${response.status}: ${body}`.trim());
  }
  return {
    sent: true,
    provider: "whatsapp_webhook",
    from,
    to
  };
}
async function notifyRestaurantPinRequestedViaWhatsappWebhook({
  restaurantWhatsapp,
  restaurantName,
  orderId,
  requestedByRole
}) {
  if (!whatsappWebhookUrl) {
    return {
      sent: false,
      reason: "webhook_not_configured"
    };
  }
  const from = normalizeToE164Br(restaurantWhatsapp);
  if (!from) {
    return {
      sent: false,
      reason: "restaurant_whatsapp_not_configured"
    };
  }
  const to = from;
  const message = buildRestaurantPinRequestMessage({
    restaurantName,
    orderId,
    requestedByRole
  });
  const headers = {
    "Content-Type": "application/json"
  };
  if (whatsappWebhookToken) {
    headers.Authorization = `Bearer ${whatsappWebhookToken}`;
  }
  const response = await fetch(whatsappWebhookUrl, {
    method: "POST",
    headers,
    body: JSON.stringify({
      channel: "whatsapp",
      from,
      to,
      message,
      metadata: {
        orderId,
        requestedByRole,
        restaurantWhatsapp: from
      }
    })
  });
  if (!response.ok) {
    const body = await response.text().catch(() => "");
    throw new Error(`Webhook respondeu ${response.status}: ${body}`.trim());
  }
  return {
    sent: true,
    provider: "whatsapp_webhook",
    from,
    to
  };
}
async function notifyRestaurantOrderIssueReportedViaWhatsappWebhook({
  restaurantWhatsapp,
  restaurantName,
  orderId,
  customerName,
  issueMessage,
  orderStatus,
  orderType,
  paymentMethod,
  total,
  addressLabel,
  itemsSummary,
  createdAt
}) {
  if (!whatsappWebhookUrl) {
    return {
      sent: false,
      reason: "webhook_not_configured"
    };
  }
  const from = normalizeToE164Br(restaurantWhatsapp);
  if (!from) {
    return {
      sent: false,
      reason: "restaurant_whatsapp_not_configured"
    };
  }
  const to = from;
  const message = buildRestaurantOrderIssueReportedMessage({
    restaurantName,
    orderId,
    customerName,
    issueMessage,
    orderStatus,
    orderType,
    paymentMethod,
    total,
    addressLabel,
    itemsSummary,
    createdAt
  });
  const headers = {
    "Content-Type": "application/json"
  };
  if (whatsappWebhookToken) {
    headers.Authorization = `Bearer ${whatsappWebhookToken}`;
  }
  const response = await fetch(whatsappWebhookUrl, {
    method: "POST",
    headers,
    body: JSON.stringify({
      channel: "whatsapp",
      from,
      to,
      message,
      metadata: {
        orderId,
        customerName,
        issueMessage,
        orderStatus,
        orderType,
        paymentMethod,
        total,
        addressLabel,
        itemsSummary,
        createdAt,
        restaurantWhatsapp: from
      }
    })
  });
  if (!response.ok) {
    const body = await response.text().catch(() => "");
    throw new Error(`Webhook respondeu ${response.status}: ${body}`.trim());
  }
  return {
    sent: true,
    provider: "whatsapp_webhook",
    from,
    to
  };
}
async function notifyCustomerPaymentConfirmed({
  restaurantId,
  restaurantWhatsapp,
  customerPhone,
  customerName,
  restaurantName,
  orderId,
  total,
  paymentMethod
}) {
  const preference = await resolveCustomerWhatsappPreference(restaurantId);
  if (!preference.enabled) {
    return {
      sent: false,
      reason: preference.reason
    };
  }
  const provider = resolveProvider();
  if (provider === "none") {
    return {
      sent: false,
      reason: "provider_not_configured"
    };
  }
  try {
    if (provider === "whatsapp_webhook") {
      return await notifyViaWhatsappWebhook({
        restaurantWhatsapp,
        customerPhone,
        customerName,
        restaurantName,
        orderId,
        total,
        paymentMethod
      });
    }
    return {
      sent: false,
      reason: "provider_not_supported",
      provider
    };
  } catch (error2) {
    console.error("[CUSTOMER_NOTIFICATION_ERROR]", getErrorMessage(error2));
    return {
      sent: false,
      reason: "send_failed",
      provider
    };
  }
}
async function notifyCustomerOrderStatusChanged({
  restaurantId,
  restaurantWhatsapp,
  customerPhone,
  customerName,
  restaurantName,
  orderId,
  status
}) {
  const preference = await resolveCustomerWhatsappPreference(restaurantId);
  if (!preference.enabled) {
    return {
      sent: false,
      reason: preference.reason
    };
  }
  const provider = resolveProvider();
  if (provider === "none") {
    return {
      sent: false,
      reason: "provider_not_configured"
    };
  }
  try {
    if (provider === "whatsapp_webhook") {
      return await notifyOrderStatusChangedViaWhatsappWebhook({
        restaurantWhatsapp,
        customerPhone,
        customerName,
        restaurantName,
        orderId,
        status
      });
    }
    return {
      sent: false,
      reason: "provider_not_supported",
      provider
    };
  } catch (error2) {
    console.error("[CUSTOMER_STATUS_NOTIFICATION_ERROR]", getErrorMessage(error2));
    return {
      sent: false,
      reason: "send_failed",
      provider
    };
  }
}
async function notifyRestaurantPaymentPinRequested({
  restaurantWhatsapp,
  restaurantName,
  orderId,
  requestedByRole
}) {
  const provider = resolveProvider();
  if (provider === "none") {
    return {
      sent: false,
      reason: "provider_not_configured"
    };
  }
  try {
    if (provider === "whatsapp_webhook") {
      return await notifyRestaurantPinRequestedViaWhatsappWebhook({
        restaurantWhatsapp,
        restaurantName,
        orderId,
        requestedByRole
      });
    }
    return {
      sent: false,
      reason: "provider_not_supported",
      provider
    };
  } catch (error2) {
    console.error("[RESTAURANT_PIN_NOTIFICATION_ERROR]", getErrorMessage(error2));
    return {
      sent: false,
      reason: "send_failed",
      provider,
      error: getErrorMessage(error2)
    };
  }
}
async function notifyRestaurantOrderIssueReported({
  restaurantWhatsapp,
  restaurantName,
  orderId,
  customerName,
  issueMessage,
  orderStatus,
  orderType,
  paymentMethod,
  total,
  addressLabel,
  itemsSummary,
  createdAt
}) {
  const provider = resolveProvider();
  if (provider === "none") {
    return {
      sent: false,
      reason: "provider_not_configured"
    };
  }
  try {
    if (provider === "whatsapp_webhook") {
      return await notifyRestaurantOrderIssueReportedViaWhatsappWebhook({
        restaurantWhatsapp,
        restaurantName,
        orderId,
        customerName,
        issueMessage,
        orderStatus,
        orderType,
        paymentMethod,
        total,
        addressLabel,
        itemsSummary,
        createdAt
      });
    }
    return {
      sent: false,
      reason: "provider_not_supported",
      provider
    };
  } catch (error2) {
    console.error("[RESTAURANT_ORDER_ISSUE_NOTIFICATION_ERROR]", getErrorMessage(error2));
    return {
      sent: false,
      reason: "send_failed",
      provider,
      error: getErrorMessage(error2)
    };
  }
}
var configuredProvider, whatsappWebhookUrl, whatsappWebhookToken;
var init_customerNotifier = __esm({
  "src/services/customerNotifier.ts"() {
    init_prisma();
    configuredProvider = String(process.env.CUSTOMER_NOTIFICATION_PROVIDER || "none").trim().toLowerCase();
    whatsappWebhookUrl = String(process.env.WHATSAPP_WEBHOOK_URL || "").trim();
    whatsappWebhookToken = String(process.env.WHATSAPP_WEBHOOK_TOKEN || "").trim();
  }
});

// src/modules/orders/utils/orderCapacity.ts
function assertOrderCapacity(activeOrders, configuredLimit) {
  const limit = Math.min(500, Math.max(1, Number(configuredLimit) || 20));
  if (activeOrders >= limit) {
    throw new Error(
      "O restaurante atingiu o limite de pedidos em andamento. Tente novamente em alguns minutos."
    );
  }
  return limit;
}
var init_orderCapacity = __esm({
  "src/modules/orders/utils/orderCapacity.ts"() {
  }
});

// src/modules/orders/utils/orderTenant.ts
function positiveInteger(value) {
  const normalized = Number(value);
  return Number.isInteger(normalized) && normalized > 0 ? normalized : null;
}
function resolveOrderRestaurantId({
  requestedRestaurantId,
  contextRestaurantId
}) {
  const requested = positiveInteger(requestedRestaurantId);
  const context = positiveInteger(contextRestaurantId);
  if (context) {
    if (requested && requested !== context) {
      throw new Error("O restaurante informado n\xE3o corresponde \xE0 sess\xE3o atual.");
    }
    return context;
  }
  if (requested) {
    return requested;
  }
  throw new Error("Restaurante n\xE3o informado para o pedido.");
}
var init_orderTenant = __esm({
  "src/modules/orders/utils/orderTenant.ts"() {
  }
});

// src/modules/orders/services/OrderPricingService.ts
import { CouponRedemptionStatus as CouponRedemptionStatus2, OrderType as OrderType3 } from "@prisma/client";
var OrderPricingService, OrderPricingService_default;
var init_OrderPricingService = __esm({
  "src/modules/orders/services/OrderPricingService.ts"() {
    init_prisma();
    init_ProductRepository();
    init_productDiscount();
    init_productIngredients();
    OrderPricingService = class {
      async quote({
        restaurantId,
        userId,
        type,
        items,
        couponRedemptionId,
        now = /* @__PURE__ */ new Date(),
        db = prisma_default
      }) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para calcular o pedido.");
        }
        if (!Array.isArray(items) || items.length === 0) {
          throw new Error("O pedido deve conter pelo menos um item.");
        }
        const products = await Promise.all(
          items.map(
            (item) => ProductRepository_default.findById(Number(item.productId), normalizedRestaurantId, db)
          )
        );
        const requestedQuantityByProduct = /* @__PURE__ */ new Map();
        products.forEach((product, index) => {
          const item = items[index];
          if (!product) {
            throw new Error(`Produto n\xE3o encontrado: ${Number(item.productId || 0)}`);
          }
          if (product.active === false) {
            throw new Error(`Produto indispon\xEDvel: ${product.name}`);
          }
          const quantity = Number(item.quantity || 0);
          if (!Number.isInteger(quantity) || quantity <= 0) {
            throw new Error(`Quantidade inv\xE1lida para ${product.name}.`);
          }
          requestedQuantityByProduct.set(
            product.id,
            (requestedQuantityByProduct.get(product.id) || 0) + quantity
          );
        });
        requestedQuantityByProduct.forEach((requestedQuantity, productId) => {
          const product = products.find((candidate) => candidate?.id === productId);
          const stock = product.stock === null || product.stock === void 0 ? null : Number(product.stock);
          if (Number.isInteger(stock) && stock >= 0 && requestedQuantity > stock) {
            throw new Error(`Estoque insuficiente para ${product.name}. Dispon\xEDvel: ${stock}.`);
          }
        });
        const orderItems = items.map(
          (item, index) => buildOrderItemCustomizationSnapshot(products[index], item)
        );
        const itemsSubtotal = roundMoney(
          orderItems.reduce((sum, item) => sum + Number(item.price) * item.quantity, 0)
        );
        const productDiscountTotal = roundMoney(
          orderItems.reduce((sum, item) => sum + Number(item.unitDiscount) * item.quantity, 0)
        );
        const settings = await db.restaurantSettings.findUnique({
          where: { restaurantId: normalizedRestaurantId },
          select: {
            deliveryFee: true,
            minimumOrder: true,
            freeShippingMinimum: true,
            acceptsDelivery: true,
            acceptsPickup: true,
            tableOrderingEnabled: true
          }
        });
        const normalizedType = String(type || "").toUpperCase();
        if (normalizedType === OrderType3.DELIVERY && settings?.acceptsDelivery === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pedidos para delivery no momento.");
        }
        if (normalizedType === OrderType3.RETIRADA && settings?.acceptsPickup === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pedidos para retirada no momento.");
        }
        if (normalizedType === OrderType3.MESA && settings?.tableOrderingEnabled === false) {
          throw new Error("Os pedidos pelo card\xE1pio de mesa est\xE3o desativados no momento.");
        }
        const freeShippingMinimum = Math.max(Number(settings?.freeShippingMinimum || 0), 0);
        const hasFreeShipping = normalizedType === OrderType3.DELIVERY && freeShippingMinimum > 0 && itemsSubtotal >= freeShippingMinimum;
        const deliveryFeeAmount = normalizedType === OrderType3.DELIVERY ? hasFreeShipping ? 0 : roundMoney(Math.max(Number(settings?.deliveryFee || 0), 0)) : 0;
        const minimumOrder = Math.max(Number(settings?.minimumOrder || 0), 0);
        if (normalizedType === OrderType3.DELIVERY && minimumOrder > 0 && itemsSubtotal < minimumOrder) {
          throw new Error(
            `Pedido m\xEDnimo ap\xF3s as ofertas: R$ ${minimumOrder.toFixed(2)}. A taxa de entrega \xE9 cobrada \xE0 parte.`
          );
        }
        let couponDiscount = 0;
        let couponCode = null;
        let couponId = null;
        let redemptionId = null;
        const requestedRedemptionId = Number(couponRedemptionId || 0);
        if (requestedRedemptionId > 0) {
          const normalizedUserId = Number(userId || 0);
          if (!Number.isInteger(normalizedUserId) || normalizedUserId <= 0) {
            throw new Error("Entre na sua conta para usar uma recompensa de fidelidade.");
          }
          const redemption = await db.couponRedemption.findFirst({
            where: {
              id: requestedRedemptionId,
              restaurantId: normalizedRestaurantId,
              userId: normalizedUserId,
              status: CouponRedemptionStatus2.CLAIMED,
              expiresAt: { gt: now }
            },
            include: { coupon: true }
          });
          if (!redemption || redemption.coupon.restaurantId !== normalizedRestaurantId) {
            throw new Error("Cupom resgatado inv\xE1lido ou indispon\xEDvel.");
          }
          const coupon = redemption.coupon;
          if (itemsSubtotal < Number(coupon.minimumSubtotal || 0)) {
            throw new Error(
              `Este cupom exige subtotal m\xEDnimo de R$ ${Number(coupon.minimumSubtotal).toFixed(2)}.`
            );
          }
          const configuredDiscount = Number(coupon.discount || 0);
          const rawDiscount = coupon.discountType === "PERCENTAGE" ? itemsSubtotal * (Math.min(configuredDiscount, 100) / 100) : configuredDiscount;
          const limitedDiscount = coupon.maxDiscount ? Math.min(rawDiscount, Number(coupon.maxDiscount)) : rawDiscount;
          const maximumCouponDiscount = Math.max(itemsSubtotal - 0.01, 0);
          couponDiscount = roundMoney(
            Math.min(Math.max(limitedDiscount, 0), maximumCouponDiscount)
          );
          if (couponDiscount <= 0) {
            throw new Error(
              "Este cupom n\xE3o gera desconto neste pedido. Escolha outro benef\xEDcio ou aumente o subtotal."
            );
          }
          couponCode = coupon.code;
          couponId = coupon.id;
          redemptionId = redemption.id;
        } else if (couponRedemptionId !== null && couponRedemptionId !== void 0 && couponRedemptionId !== "") {
          throw new Error("Cupom resgatado inv\xE1lido.");
        }
        const total = roundMoney(Math.max(itemsSubtotal - couponDiscount + deliveryFeeAmount, 0));
        return {
          itemsSubtotal,
          productDiscountTotal,
          couponDiscount,
          deliveryFeeAmount,
          total,
          couponCode,
          couponId,
          couponRedemptionId: redemptionId,
          orderItems,
          products
        };
      }
    };
    OrderPricingService_default = new OrderPricingService();
  }
});

// src/modules/orders/utils/waiterOrderRealtime.ts
import { OrderType as OrderType4 } from "@prisma/client";
function buildWaiterOrderRealtimePayload(order) {
  if (!order || order.type !== OrderType4.MESA || !order.table) {
    return null;
  }
  return {
    id: order.id,
    restaurantId: order.restaurantId,
    type: order.type,
    status: order.status,
    total: order.total,
    observation: order.observation || null,
    createdAt: order.createdAt,
    readyAt: order.readyAt || null,
    table: {
      id: order.table.id,
      number: order.table.number
    },
    customer: order.user?.name ? { id: order.user.id, name: order.user.name } : null,
    items: Array.isArray(order.items) ? order.items.map((item) => ({
      id: item.id,
      quantity: item.quantity,
      observation: item.observation || null,
      ingredients: item.ingredients || null,
      customizations: item.customizations || null,
      product: item.product ? { id: item.product.id, name: item.product.name } : null
    })) : []
  };
}
function emitWaiterTableOrderEvent(io2, event, order) {
  const payload = buildWaiterOrderRealtimePayload(order);
  if (!payload) return false;
  io2.to(`restaurant:${payload.restaurantId}:waiter`).emit(event, payload);
  return true;
}
function emitTableSessionOrderEvent(io2, event, order) {
  const payload = buildWaiterOrderRealtimePayload(order);
  if (!payload) return false;
  io2.to(`table:${payload.table.id}`).emit(event, {
    id: payload.id,
    restaurantId: payload.restaurantId,
    type: payload.type,
    status: payload.status,
    createdAt: payload.createdAt,
    readyAt: payload.readyAt,
    table: payload.table
  });
  return true;
}
var init_waiterOrderRealtime = __esm({
  "src/modules/orders/utils/waiterOrderRealtime.ts"() {
  }
});

// src/modules/tableAccount/domain/tableAccountContracts.ts
var TABLE_ACCOUNT_CONTRACT_VERSION, TABLE_ORDER_SETTLEMENT_MODES, TABLE_PAYMENT_SELECTION_MODES, TABLE_PAYMENT_METHODS, TABLE_SERVICE_FEE_MODES, DEFAULT_TABLE_ACCOUNT_TIME_ZONE;
var init_tableAccountContracts = __esm({
  "src/modules/tableAccount/domain/tableAccountContracts.ts"() {
    TABLE_ACCOUNT_CONTRACT_VERSION = 1;
    TABLE_ORDER_SETTLEMENT_MODES = ["TABLE_ACCOUNT", "PAY_NOW"];
    TABLE_PAYMENT_SELECTION_MODES = [
      "MY_ITEMS",
      "SELECTED_ITEMS",
      "EQUAL_SPLIT",
      "FULL_ACCOUNT",
      "WAITER"
    ];
    TABLE_PAYMENT_METHODS = ["PIX", "CARD", "CASH", "CARD_MACHINE"];
    TABLE_SERVICE_FEE_MODES = ["DISABLED", "OPTIONAL", "MANDATORY"];
    DEFAULT_TABLE_ACCOUNT_TIME_ZONE = "America/Sao_Paulo";
  }
});

// src/modules/tableAccount/domain/tableAccountSchemas.ts
import { z as z7 } from "zod";
function isValidTimeZone2(value) {
  try {
    new Intl.DateTimeFormat("pt-BR", { timeZone: value }).format();
    return true;
  } catch {
    return false;
  }
}
var moneyCentsSchema, publicIdSchema, reasonSchema, tableParticipantIdentityInputSchema, tableOrderContinuationInputSchema, idempotencyKeySchema, createTablePaymentIntentInputSchema, forceCloseTableAccountInputSchema, cancelTableBillItemInputSchema, refundTablePaymentInputSchema, tablePrepaymentWindowSchema, tableAccountSettingsFields, tableAccountSettingsSchema, tableAccountSettingsPatchSchema;
var init_tableAccountSchemas = __esm({
  "src/modules/tableAccount/domain/tableAccountSchemas.ts"() {
    init_tableAccountContracts();
    moneyCentsSchema = z7.number({ invalid_type_error: "O valor deve ser informado em centavos inteiros." }).int("O valor deve ser informado em centavos inteiros.").min(0, "O valor n\xE3o pode ser negativo.").max(Number.MAX_SAFE_INTEGER, "O valor ultrapassa o limite monet\xE1rio seguro.");
    publicIdSchema = z7.string().uuid("Identificador p\xFAblico inv\xE1lido.");
    reasonSchema = z7.string().trim().min(5, "Informe um motivo com pelo menos 5 caracteres.").max(500, "O motivo deve ter no m\xE1ximo 500 caracteres.");
    tableParticipantIdentityInputSchema = z7.object({
      displayName: z7.string().trim().min(2, "Informe um nome com pelo menos 2 caracteres.").max(100, "O nome deve ter no m\xE1ximo 100 caracteres.").transform((value) => value.replace(/\s+/g, " ")).nullable().optional(),
      phone: z7.string().trim().regex(/^\+?[0-9()\s-]+$/, "Informe um telefone v\xE1lido.").transform((value) => value.replace(/\D/g, "")).refine((value) => value.length >= 10 && value.length <= 15, "Informe um telefone v\xE1lido.").optional()
    }).strict();
    tableOrderContinuationInputSchema = z7.object({
      settlementMode: z7.enum(TABLE_ORDER_SETTLEMENT_MODES),
      paymentMethod: z7.enum(["PIX", "CARD"]).optional()
    }).strict().superRefine((input, context) => {
      if (input.settlementMode === "PAY_NOW" && !input.paymentMethod) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["paymentMethod"],
          message: "Escolha PIX ou cart\xE3o para pagar este pedido agora."
        });
      }
      if (input.settlementMode === "TABLE_ACCOUNT" && input.paymentMethod) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["paymentMethod"],
          message: "A forma de pagamento s\xF3 deve ser informada ao pagar agora."
        });
      }
    });
    idempotencyKeySchema = z7.string().trim().min(16, "A chave de idempot\xEAncia deve ter pelo menos 16 caracteres.").max(128, "A chave de idempot\xEAncia deve ter no m\xE1ximo 128 caracteres.").regex(/^[A-Za-z0-9._:-]+$/, "A chave de idempot\xEAncia cont\xE9m caracteres inv\xE1lidos.");
    createTablePaymentIntentInputSchema = z7.object({
      selectionMode: z7.enum(TABLE_PAYMENT_SELECTION_MODES),
      method: z7.enum(TABLE_PAYMENT_METHODS),
      billItemPublicIds: z7.array(publicIdSchema).min(1).max(100).optional(),
      splitCount: z7.number().int().min(2).max(100).optional(),
      includeOptionalServiceFee: z7.boolean().optional().default(false),
      idempotencyKey: idempotencyKeySchema
    }).strict().superRefine((input, context) => {
      if (input.billItemPublicIds && new Set(input.billItemPublicIds).size !== input.billItemPublicIds.length) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["billItemPublicIds"],
          message: "Um mesmo item n\xE3o pode ser selecionado mais de uma vez."
        });
      }
      if (input.selectionMode === "SELECTED_ITEMS" && !input.billItemPublicIds) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["billItemPublicIds"],
          message: "Selecione ao menos um item para pagar."
        });
      }
      if (input.selectionMode !== "SELECTED_ITEMS" && input.billItemPublicIds) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["billItemPublicIds"],
          message: "A lista de itens s\xF3 pode ser enviada na op\xE7\xE3o de itens selecionados."
        });
      }
      if (input.selectionMode === "EQUAL_SPLIT" && !input.splitCount) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["splitCount"],
          message: "Informe entre quantas pessoas o saldo ser\xE1 dividido."
        });
      }
      if (input.selectionMode !== "EQUAL_SPLIT" && input.splitCount) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["splitCount"],
          message: "A quantidade de pessoas s\xF3 pode ser enviada na divis\xE3o igual."
        });
      }
      const isWaiterPayment = input.selectionMode === "WAITER";
      const isManualMethod = input.method === "CASH" || input.method === "CARD_MACHINE";
      if (isWaiterPayment && !isManualMethod) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["method"],
          message: "O pagamento com o gar\xE7om deve ser em dinheiro ou maquininha."
        });
      }
      if (!isWaiterPayment && isManualMethod) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["method"],
          message: "Dinheiro e maquininha s\xF3 podem ser escolhidos no pagamento com o gar\xE7om."
        });
      }
    });
    forceCloseTableAccountInputSchema = z7.object({ reason: reasonSchema }).strict();
    cancelTableBillItemInputSchema = z7.object({ reason: reasonSchema }).strict();
    refundTablePaymentInputSchema = z7.object({ reason: reasonSchema }).strict();
    tablePrepaymentWindowSchema = z7.object({
      weekdays: z7.array(z7.number().int().min(0).max(6)).min(1, "Escolha ao menos um dia da semana.").max(7),
      startsAtMinute: z7.number().int().min(0).max(1439),
      endsAtMinute: z7.number().int().min(0).max(1439)
    }).strict().superRefine((window, context) => {
      if (new Set(window.weekdays).size !== window.weekdays.length) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["weekdays"],
          message: "Um dia da semana n\xE3o pode ser repetido."
        });
      }
      if (window.startsAtMinute === window.endsAtMinute) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["endsAtMinute"],
          message: "O in\xEDcio e o fim do per\xEDodo devem ser diferentes."
        });
      }
    });
    tableAccountSettingsFields = {
      enabled: z7.boolean(),
      requirePrepaymentAboveCents: moneyCentsSchema.nullable(),
      prepaymentWindows: z7.array(tablePrepaymentWindowSchema).max(50),
      allowCash: z7.boolean(),
      allowCardMachine: z7.boolean(),
      allowOnlinePayment: z7.boolean(),
      allowSplit: z7.boolean(),
      serviceFeeMode: z7.enum(TABLE_SERVICE_FEE_MODES),
      serviceFeeBasisPoints: z7.number().int().min(0).max(1e4),
      preventCloseWithOutstandingBalance: z7.boolean(),
      requireEmployeeApprovalForPreparedItemCancellation: z7.boolean(),
      blockNewOrdersOnClosingRequest: z7.boolean(),
      reservationTimeoutMinutes: z7.number().int().min(1).max(60),
      timeZone: z7.string().trim().min(1).max(100).refine(isValidTimeZone2, "Informe um fuso hor\xE1rio IANA v\xE1lido.")
    };
    tableAccountSettingsSchema = z7.object({
      enabled: tableAccountSettingsFields.enabled.default(false),
      requirePrepaymentAboveCents: tableAccountSettingsFields.requirePrepaymentAboveCents.default(null),
      prepaymentWindows: tableAccountSettingsFields.prepaymentWindows.default([]),
      allowCash: tableAccountSettingsFields.allowCash.default(false),
      allowCardMachine: tableAccountSettingsFields.allowCardMachine.default(false),
      allowOnlinePayment: tableAccountSettingsFields.allowOnlinePayment.default(true),
      allowSplit: tableAccountSettingsFields.allowSplit.default(true),
      serviceFeeMode: tableAccountSettingsFields.serviceFeeMode.default("DISABLED"),
      serviceFeeBasisPoints: tableAccountSettingsFields.serviceFeeBasisPoints.default(0),
      preventCloseWithOutstandingBalance: tableAccountSettingsFields.preventCloseWithOutstandingBalance.default(true),
      requireEmployeeApprovalForPreparedItemCancellation: tableAccountSettingsFields.requireEmployeeApprovalForPreparedItemCancellation.default(true),
      blockNewOrdersOnClosingRequest: tableAccountSettingsFields.blockNewOrdersOnClosingRequest.default(true),
      reservationTimeoutMinutes: tableAccountSettingsFields.reservationTimeoutMinutes.default(10),
      timeZone: tableAccountSettingsFields.timeZone.default(DEFAULT_TABLE_ACCOUNT_TIME_ZONE)
    }).strict().superRefine((settings, context) => {
      if (settings.serviceFeeMode === "DISABLED" && settings.serviceFeeBasisPoints !== 0) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["serviceFeeBasisPoints"],
          message: "A taxa deve ser zero quando a cobran\xE7a estiver desativada."
        });
      }
      if (settings.serviceFeeMode !== "DISABLED" && settings.serviceFeeBasisPoints === 0) {
        context.addIssue({
          code: z7.ZodIssueCode.custom,
          path: ["serviceFeeBasisPoints"],
          message: "Informe o percentual da taxa de servi\xE7o."
        });
      }
    });
    tableAccountSettingsPatchSchema = z7.object(tableAccountSettingsFields).partial().strict();
  }
});

// src/modules/tableAccount/domain/tableAccountRules.ts
function assertMoneyCents(value, fieldName = "valor") {
  if (!Number.isSafeInteger(value) || value < 0) {
    throw new RangeError(`${fieldName} deve ser um inteiro n\xE3o negativo em centavos.`);
  }
  return value;
}
function sumMoneyCents(values) {
  return values.reduce((total, value, index) => {
    const safeValue = assertMoneyCents(value, `valor[${index}]`);
    const nextTotal = total + safeValue;
    if (!Number.isSafeInteger(nextTotal)) {
      throw new RangeError("A soma ultrapassa o limite monet\xE1rio seguro.");
    }
    return nextTotal;
  }, 0);
}
function splitCentsEqually(totalCents, parts) {
  const safeTotal = assertMoneyCents(totalCents, "total");
  if (!Number.isSafeInteger(parts) || parts < 2 || parts > 100 || safeTotal === 0 || parts > safeTotal) {
    throw new RangeError("A quantidade de partes deve ser um inteiro entre 2 e 100.");
  }
  const base = Math.floor(safeTotal / parts);
  const remainder = safeTotal % parts;
  return Array.from({ length: parts }, (_, index) => base + (index < remainder ? 1 : 0));
}
function calculateServiceFeeCents(subtotalCents, serviceFeeBasisPoints) {
  const safeSubtotal = assertMoneyCents(subtotalCents, "subtotal");
  if (!Number.isSafeInteger(serviceFeeBasisPoints) || serviceFeeBasisPoints < 0 || serviceFeeBasisPoints > 1e4) {
    throw new RangeError("A taxa de servi\xE7o deve estar entre 0 e 10.000 basis points.");
  }
  const rounded = (BigInt(safeSubtotal) * BigInt(serviceFeeBasisPoints) + 5000n) / 10000n;
  const result = Number(rounded);
  return assertMoneyCents(result, "taxa de servi\xE7o");
}
function calculateTableAccountBalance({
  consumedCents,
  serviceFeeCents,
  grossPaidCents,
  refundedCents
}) {
  const safeConsumed = assertMoneyCents(consumedCents, "total consumido");
  const safeFee = assertMoneyCents(serviceFeeCents, "taxa de servi\xE7o");
  const safePaid = assertMoneyCents(grossPaidCents, "total pago bruto");
  const safeRefunded = assertMoneyCents(refundedCents, "total estornado");
  if (safeRefunded > safePaid) {
    throw new RangeError("O total estornado n\xE3o pode ser maior que o total pago.");
  }
  const owedCents = sumMoneyCents([safeConsumed, safeFee]);
  const netPaidCents = safePaid - safeRefunded;
  return {
    owedCents,
    netPaidCents,
    remainingCents: Math.max(0, owedCents - netPaidCents),
    overpaidCents: Math.max(0, netPaidCents - owedCents)
  };
}
function shouldIncludeServiceFee(serviceFeeMode, includeOptionalServiceFee) {
  if (serviceFeeMode === "MANDATORY") {
    return true;
  }
  if (serviceFeeMode === "OPTIONAL") {
    return includeOptionalServiceFee;
  }
  return false;
}
function isPrepaymentWindowActive(window, currentWeekday, currentMinute) {
  if (window.startsAtMinute < window.endsAtMinute) {
    return window.weekdays.includes(currentWeekday) && currentMinute >= window.startsAtMinute && currentMinute < window.endsAtMinute;
  }
  if (window.weekdays.includes(currentWeekday) && currentMinute >= window.startsAtMinute) {
    return true;
  }
  const previousWeekday = (currentWeekday + 6) % 7;
  return window.weekdays.includes(previousWeekday) && currentMinute < window.endsAtMinute;
}
function requiresPrepayment({
  currentOutstandingCents,
  incomingOrderCents,
  thresholdCents,
  windows,
  currentWeekday,
  currentMinute
}) {
  const projectedOutstandingCents = sumMoneyCents([currentOutstandingCents, incomingOrderCents]);
  if (!Number.isInteger(currentWeekday) || currentWeekday < 0 || currentWeekday > 6) {
    throw new RangeError("O dia da semana deve estar entre 0 e 6.");
  }
  if (!Number.isInteger(currentMinute) || currentMinute < 0 || currentMinute > 1439) {
    throw new RangeError("O minuto do dia deve estar entre 0 e 1439.");
  }
  const thresholdRequiresPayment = thresholdCents !== null && projectedOutstandingCents > assertMoneyCents(thresholdCents, "limite de antecipa\xE7\xE3o");
  const scheduleRequiresPayment = windows.some(
    (window) => isPrepaymentWindowActive(window, currentWeekday, currentMinute)
  );
  return {
    required: thresholdRequiresPayment || scheduleRequiresPayment,
    reason: scheduleRequiresPayment ? "SCHEDULE" : thresholdRequiresPayment ? "THRESHOLD" : null,
    projectedOutstandingCents
  };
}
function getWeekdayAndMinuteInTimeZone(date, timeZone) {
  const formatter = new Intl.DateTimeFormat("en-US", {
    timeZone,
    weekday: "short",
    hour: "2-digit",
    minute: "2-digit",
    hourCycle: "h23"
  });
  const parts = Object.fromEntries(
    formatter.formatToParts(date).filter((part) => part.type !== "literal").map((part) => [part.type, part.value])
  );
  const weekday = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].indexOf(parts.weekday);
  const hour = Number(parts.hour);
  const minute = Number(parts.minute);
  if (weekday < 0 || !Number.isInteger(hour) || !Number.isInteger(minute)) {
    throw new RangeError("N\xE3o foi poss\xEDvel calcular o hor\xE1rio local do restaurante.");
  }
  return { weekday, minuteOfDay: hour * 60 + minute };
}
function isActorFromRestaurant(actor, restaurantId) {
  return Number.isSafeInteger(restaurantId) && restaurantId > 0 && actor.restaurantId === restaurantId;
}
function canConfirmManualTablePayment(actor, restaurantId) {
  return isActorFromRestaurant(actor, restaurantId) && (actor.role === "ADMIN" || actor.role === "FUNCIONARIO" && actor.subRole === "GARCOM");
}
function canViewTableAccountFinancialHistory(actor, restaurantId) {
  return canConfirmManualTablePayment(actor, restaurantId);
}
function canRefundTablePayment(actor, restaurantId) {
  return isActorFromRestaurant(actor, restaurantId) && actor.role === "ADMIN";
}
var init_tableAccountRules = __esm({
  "src/modules/tableAccount/domain/tableAccountRules.ts"() {
  }
});

// src/modules/tableAccount/domain/tableBillItemPricing.ts
function decimalMoneyToCents(value, fieldName = "valor") {
  const normalized = String(value).trim();
  const match = /^([+-]?)(\d+)(?:\.(\d+))?$/.exec(normalized);
  if (!match) {
    throw new RangeError(`${fieldName} possui formato monet\xE1rio inv\xE1lido.`);
  }
  const [, sign, whole, fraction = ""] = match;
  if (sign === "-") {
    throw new RangeError(`${fieldName} n\xE3o pode ser negativo.`);
  }
  if (fraction.length > 2 && /[1-9]/.test(fraction.slice(2))) {
    throw new RangeError(`${fieldName} deve possuir no m\xE1ximo duas casas decimais.`);
  }
  const cents = BigInt(whole) * 100n + BigInt((fraction + "00").slice(0, 2));
  if (cents > BigInt(Number.MAX_SAFE_INTEGER)) {
    throw new RangeError(`${fieldName} ultrapassa o limite monet\xE1rio seguro.`);
  }
  return assertMoneyCents(Number(cents), fieldName);
}
function allocateDiscountAcrossUnitPrices(unitPricesCents, discountCents) {
  const prices = unitPricesCents.map(
    (value, index) => assertMoneyCents(value, `pre\xE7o da unidade ${index + 1}`)
  );
  const safeDiscount = assertMoneyCents(discountCents, "desconto");
  const subtotal = sumMoneyCents(prices);
  if (safeDiscount > subtotal) {
    throw new RangeError("O desconto n\xE3o pode ser maior que o subtotal das unidades.");
  }
  if (safeDiscount === 0) {
    return [...prices];
  }
  if (subtotal === 0) {
    throw new RangeError("N\xE3o \xE9 poss\xEDvel aplicar desconto a um subtotal zerado.");
  }
  const subtotalBigInt = BigInt(subtotal);
  const discountBigInt = BigInt(safeDiscount);
  const shares = prices.map(
    (price) => Number(BigInt(price) * discountBigInt / subtotalBigInt)
  );
  let remaining = safeDiscount - sumMoneyCents(shares);
  for (let index = 0; index < shares.length && remaining > 0; index += 1) {
    if (shares[index] < prices[index]) {
      shares[index] += 1;
      remaining -= 1;
    }
  }
  if (remaining !== 0) {
    throw new RangeError("N\xE3o foi poss\xEDvel distribuir todos os centavos do desconto.");
  }
  return prices.map((price, index) => price - shares[index]);
}
function buildTableBillUnitSeeds(orderItems, couponDiscount) {
  const rawUnits = orderItems.flatMap((item, orderItemIndex) => {
    const quantity = Number(item.quantity);
    if (!Number.isSafeInteger(quantity) || quantity <= 0) {
      throw new RangeError(`Quantidade inv\xE1lida no item ${orderItemIndex + 1}.`);
    }
    const unitPriceCents = decimalMoneyToCents(
      item.price,
      `pre\xE7o do item ${orderItemIndex + 1}`
    );
    return Array.from({ length: quantity }, (_, index) => ({
      orderItemIndex,
      unitIndex: index + 1,
      unitPriceCents
    }));
  });
  const discountedPrices = allocateDiscountAcrossUnitPrices(
    rawUnits.map((unit) => unit.unitPriceCents),
    decimalMoneyToCents(couponDiscount, "desconto do cupom")
  );
  return rawUnits.map((unit, index) => ({
    ...unit,
    unitPriceCents: discountedPrices[index]
  }));
}
var init_tableBillItemPricing = __esm({
  "src/modules/tableAccount/domain/tableBillItemPricing.ts"() {
    init_tableAccountRules();
  }
});

// src/modules/tableAccount/repositories/TableAccountSettingsRepository.ts
function toSafeCents(value) {
  if (value === null) return null;
  const cents = Number(value);
  if (!Number.isSafeInteger(cents) || cents < 0) {
    throw new RangeError("A configura\xE7\xE3o monet\xE1ria da conta da mesa ultrapassa o limite seguro.");
  }
  return cents;
}
function serializeTableAccountSettings(record) {
  if (!record) return tableAccountSettingsSchema.parse({});
  return tableAccountSettingsSchema.parse({
    ...record,
    requirePrepaymentAboveCents: toSafeCents(record.requirePrepaymentAboveCents),
    prepaymentWindows: record.prepaymentWindows
  });
}
function toPersistenceData(settings) {
  return {
    enabled: settings.enabled,
    requirePrepaymentAboveCents: settings.requirePrepaymentAboveCents === null ? null : BigInt(settings.requirePrepaymentAboveCents),
    prepaymentWindows: settings.prepaymentWindows,
    allowCash: settings.allowCash,
    allowCardMachine: settings.allowCardMachine,
    allowOnlinePayment: settings.allowOnlinePayment,
    allowSplit: settings.allowSplit,
    serviceFeeMode: settings.serviceFeeMode,
    serviceFeeBasisPoints: settings.serviceFeeBasisPoints,
    preventCloseWithOutstandingBalance: settings.preventCloseWithOutstandingBalance,
    requireEmployeeApprovalForPreparedItemCancellation: settings.requireEmployeeApprovalForPreparedItemCancellation,
    blockNewOrdersOnClosingRequest: settings.blockNewOrdersOnClosingRequest,
    reservationTimeoutMinutes: settings.reservationTimeoutMinutes,
    timeZone: settings.timeZone
  };
}
var tableAccountSettingsSelect, TableAccountSettingsRepository, TableAccountSettingsRepository_default;
var init_TableAccountSettingsRepository = __esm({
  "src/modules/tableAccount/repositories/TableAccountSettingsRepository.ts"() {
    init_prisma();
    init_tableAccountSchemas();
    tableAccountSettingsSelect = {
      enabled: true,
      requirePrepaymentAboveCents: true,
      prepaymentWindows: true,
      allowCash: true,
      allowCardMachine: true,
      allowOnlinePayment: true,
      allowSplit: true,
      serviceFeeMode: true,
      serviceFeeBasisPoints: true,
      preventCloseWithOutstandingBalance: true,
      requireEmployeeApprovalForPreparedItemCancellation: true,
      blockNewOrdersOnClosingRequest: true,
      reservationTimeoutMinutes: true,
      timeZone: true
    };
    TableAccountSettingsRepository = class {
      async findByRestaurantId(restaurantId, db = prisma_default) {
        const record = await db.tableAccountSettings.findUnique({
          where: { restaurantId },
          select: tableAccountSettingsSelect
        });
        return serializeTableAccountSettings(record);
      }
      async upsert(restaurantId, settings, db = prisma_default) {
        const data = toPersistenceData(settings);
        const record = await db.tableAccountSettings.upsert({
          where: { restaurantId },
          create: { restaurantId, ...data },
          update: data,
          select: tableAccountSettingsSelect
        });
        return serializeTableAccountSettings(record);
      }
    };
    TableAccountSettingsRepository_default = new TableAccountSettingsRepository();
  }
});

// src/modules/tableAccount/domain/tablePaymentAllocation.ts
function isReservationCurrent(expiresAt, now) {
  if (!expiresAt) {
    return true;
  }
  const parsed = expiresAt instanceof Date ? expiresAt : new Date(expiresAt);
  return Number.isFinite(parsed.getTime()) && parsed.getTime() > now.getTime();
}
function calculateTableBillItemLedger({
  unitPriceCents,
  projectedStatus,
  allocations,
  canceled = false,
  now = /* @__PURE__ */ new Date()
}) {
  const safeUnitPrice = assertMoneyCents(unitPriceCents, "pre\xE7o unit\xE1rio");
  if (canceled || projectedStatus === "REFUNDED") {
    return {
      unitPriceCents: safeUnitPrice,
      paidCents: 0,
      reservedCents: 0,
      processingCents: 0,
      availableCents: 0,
      projectedStatus: projectedStatus === "REFUNDED" ? "REFUNDED" : "UNPAID"
    };
  }
  if (allocations.length === 0) {
    const paidCents2 = projectedStatus === "PAID" ? safeUnitPrice : 0;
    const reservedCents2 = projectedStatus === "RESERVED" ? safeUnitPrice : 0;
    const processingCents2 = projectedStatus === "PROCESSING" ? safeUnitPrice : 0;
    return {
      unitPriceCents: safeUnitPrice,
      paidCents: paidCents2,
      reservedCents: reservedCents2,
      processingCents: processingCents2,
      availableCents: Math.max(
        0,
        safeUnitPrice - paidCents2 - reservedCents2 - processingCents2
      ),
      projectedStatus
    };
  }
  let paidCents = 0;
  let reservedCents = 0;
  let processingCents = 0;
  allocations.forEach((allocation, index) => {
    const amountCents = assertMoneyCents(allocation.amountCents, `aloca\xE7\xE3o[${index}]`);
    if (allocation.intentStatus === "PAID") {
      paidCents = sumMoneyCents([paidCents, amountCents]);
      return;
    }
    if (allocation.intentStatus === "RESERVED" && isReservationCurrent(allocation.expiresAt, now)) {
      reservedCents = sumMoneyCents([reservedCents, amountCents]);
      return;
    }
    if (allocation.intentStatus === "PROCESSING" && isReservationCurrent(allocation.expiresAt, now)) {
      processingCents = sumMoneyCents([processingCents, amountCents]);
    }
  });
  const committedCents = sumMoneyCents([paidCents, reservedCents, processingCents]);
  if (committedCents > safeUnitPrice) {
    throw new RangeError("As aloca\xE7\xF5es financeiras ultrapassam o valor do item.");
  }
  const availableCents = safeUnitPrice - committedCents;
  const nextProjectedStatus = paidCents === safeUnitPrice ? "PAID" : processingCents > 0 ? "PROCESSING" : reservedCents > 0 ? "RESERVED" : "UNPAID";
  return {
    unitPriceCents: safeUnitPrice,
    paidCents,
    reservedCents,
    processingCents,
    availableCents,
    projectedStatus: nextProjectedStatus
  };
}
function allocateCentsAcrossTableBillItems(items, requestedCents) {
  const safeRequested = assertMoneyCents(requestedCents, "valor solicitado");
  if (safeRequested === 0) {
    throw new RangeError("O pagamento deve possuir valor maior que zero.");
  }
  let remainingCents = safeRequested;
  const result = [];
  for (const item of items) {
    if (remainingCents === 0) {
      break;
    }
    const availableCents = assertMoneyCents(item.availableCents, "saldo dispon\xEDvel do item");
    const amountCents = Math.min(availableCents, remainingCents);
    if (amountCents <= 0) {
      continue;
    }
    result.push({
      tableBillItemId: item.id,
      tableBillItemPublicId: item.publicId,
      amountCents
    });
    remainingCents -= amountCents;
  }
  if (remainingCents !== 0) {
    throw new RangeError("O saldo dispon\xEDvel n\xE3o cobre o valor solicitado.");
  }
  return result;
}
function sumAvailableTableBillItemCents(items) {
  return sumMoneyCents(items.map((item) => item.availableCents));
}
var init_tablePaymentAllocation = __esm({
  "src/modules/tableAccount/domain/tablePaymentAllocation.ts"() {
    init_tableAccountRules();
  }
});

// src/modules/tableAccount/services/tablePaymentLedger.ts
import {
  OrderStatus as OrderStatus3,
  Prisma,
  TableOrderFinancialStatus as TableOrderFinancialStatus2,
  TablePaymentEventType,
  TablePaymentIntentStatus
} from "@prisma/client";
function bigintToMoneyCents(value, fieldName) {
  return assertMoneyCents(Number(value), fieldName);
}
async function lockTablePaymentSession(tx, restaurantId, tableSessionId) {
  await tx.$queryRaw(
    Prisma.sql`SELECT 1::int AS "lockAcquired"
               FROM pg_advisory_xact_lock(${restaurantId}::int, ${tableSessionId}::int)`
  );
}
async function loadTablePaymentLedgerItems(db, restaurantId, tableSessionId, now = /* @__PURE__ */ new Date()) {
  const items = await db.tableBillItem.findMany({
    where: {
      restaurantId,
      tableSessionId
    },
    select: {
      id: true,
      publicId: true,
      participantId: true,
      orderId: true,
      unitPriceCents: true,
      financialStatus: true,
      canceledAt: true,
      createdAt: true,
      order: {
        select: {
          status: true
        }
      },
      paymentAllocations: {
        select: {
          amountCents: true,
          paymentIntent: {
            select: {
              status: true,
              expiresAt: true
            }
          }
        }
      }
    },
    orderBy: [{ createdAt: "asc" }, { id: "asc" }]
  });
  return items.map((item) => {
    const canceled = Boolean(item.canceledAt) || item.order.status === OrderStatus3.CANCELADO;
    const ledger = calculateTableBillItemLedger({
      unitPriceCents: bigintToMoneyCents(
        item.unitPriceCents,
        `valor do item financeiro ${item.publicId}`
      ),
      projectedStatus: item.financialStatus,
      canceled,
      now,
      allocations: item.paymentAllocations.map((allocation) => ({
        amountCents: bigintToMoneyCents(
          allocation.amountCents,
          `aloca\xE7\xE3o do item ${item.publicId}`
        ),
        intentStatus: allocation.paymentIntent.status,
        expiresAt: allocation.paymentIntent.expiresAt
      }))
    });
    return {
      id: item.id,
      publicId: item.publicId,
      participantId: item.participantId,
      orderId: item.orderId,
      createdAt: item.createdAt,
      canceled,
      currentProjectedStatus: item.financialStatus,
      ...ledger
    };
  });
}
async function projectTableSessionFinancialState(tx, restaurantId, tableSessionId, now = /* @__PURE__ */ new Date()) {
  const items = await loadTablePaymentLedgerItems(tx, restaurantId, tableSessionId, now);
  for (const item of items) {
    if (item.currentProjectedStatus !== item.projectedStatus) {
      await tx.tableBillItem.update({
        where: { id: item.id },
        data: {
          financialStatus: item.projectedStatus,
          paidAt: item.projectedStatus === "PAID" ? now : void 0
        }
      });
    }
  }
  const orders = await tx.order.findMany({
    where: {
      restaurantId,
      tableSessionId
    },
    select: {
      id: true,
      status: true,
      paid: true,
      paidAt: true,
      tableFinancialStatus: true
    }
  });
  for (const order of orders) {
    if (order.status === OrderStatus3.CANCELADO) {
      continue;
    }
    const activeItems = items.filter(
      (item) => item.orderId === order.id && !item.canceled && item.projectedStatus !== "REFUNDED"
    );
    if (activeItems.length === 0) {
      continue;
    }
    const allPaid = activeItems.every(
      (item) => item.paidCents === item.unitPriceCents && item.availableCents === 0
    );
    const hasProcessing = activeItems.some((item) => item.processingCents > 0);
    const hasReservation = activeItems.some((item) => item.reservedCents > 0);
    const financialStatus = allPaid ? TableOrderFinancialStatus2.PAID : hasProcessing ? TableOrderFinancialStatus2.PROCESSING : hasReservation ? TableOrderFinancialStatus2.RESERVED : TableOrderFinancialStatus2.UNPAID;
    if (order.paid !== allPaid || order.tableFinancialStatus !== financialStatus || allPaid && !order.paidAt) {
      await tx.order.update({
        where: { id: order.id },
        data: {
          paid: allPaid,
          tableFinancialStatus: financialStatus,
          paidAt: allPaid ? order.paidAt || now : order.paidAt
        }
      });
    }
  }
  return items;
}
async function expireTablePaymentReservations(tx, restaurantId, tableSessionId, now = /* @__PURE__ */ new Date()) {
  const expired = await tx.tablePaymentIntent.findMany({
    where: {
      restaurantId,
      tableSessionId,
      status: {
        in: [TablePaymentIntentStatus.RESERVED, TablePaymentIntentStatus.PROCESSING]
      },
      expiresAt: { lte: now }
    },
    select: {
      id: true,
      publicId: true,
      status: true,
      totalCents: true
    }
  });
  for (const intent of expired) {
    const updated = await tx.tablePaymentIntent.updateMany({
      where: {
        id: intent.id,
        restaurantId,
        tableSessionId,
        status: intent.status
      },
      data: {
        status: TablePaymentIntentStatus.EXPIRED,
        failedAt: now,
        failureCode: "RESERVATION_EXPIRED"
      }
    });
    if (updated.count === 1) {
      await tx.tablePaymentEvent.create({
        data: {
          restaurantId,
          tableSessionId,
          paymentIntentId: intent.id,
          deduplicationKey: `table-payment:${intent.publicId}:expired`,
          type: TablePaymentEventType.EXPIRED,
          fromStatus: intent.status,
          toStatus: TablePaymentIntentStatus.EXPIRED,
          amountCents: intent.totalCents,
          occurredAt: now
        }
      });
    }
  }
  if (expired.length > 0) {
    await projectTableSessionFinancialState(tx, restaurantId, tableSessionId, now);
  }
  return expired.length;
}
var init_tablePaymentLedger = __esm({
  "src/modules/tableAccount/services/tablePaymentLedger.ts"() {
    init_tableAccountRules();
    init_tablePaymentAllocation();
  }
});

// src/modules/orders/services/CreateOrderService.ts
import {
  PaymentMethod as PaymentMethod3,
  Prisma as Prisma2,
  TableBillItemFinancialStatus as TableBillItemFinancialStatus2,
  TableOrderFinancialStatus as TableOrderFinancialStatus3,
  TableOrderSettlementMode,
  TableParticipantStatus,
  TableSessionStatus as TableSessionStatus2,
  OrderType as OrderType5,
  OrderStatus as OrderStatus4
} from "@prisma/client";
var CreateOrderService, CreateOrderService_default;
var init_CreateOrderService = __esm({
  "src/modules/orders/services/CreateOrderService.ts"() {
    init_prisma();
    init_OrderRepository();
    init_server();
    init_OrderValidator();
    init_TableSessionRepository();
    init_OrderPixPaymentService();
    init_customerNotifier();
    init_RestaurantSettingsRepository();
    init_restaurantAvailability();
    init_orderCapacity();
    init_orderTenant();
    init_OrderPricingService();
    init_couponRedemptionLifecycle();
    init_waiterOrderRealtime();
    init_tableAccountSchemas();
    init_tableBillItemPricing();
    init_TableAccountSettingsRepository();
    init_tableAccountRules();
    init_tablePaymentLedger();
    CreateOrderService = class {
      formatCpf(value) {
        const digits = String(value || "").replace(/\D/g, "");
        if (digits.length !== 11) {
          return null;
        }
        return `${digits.slice(0, 3)}.${digits.slice(3, 6)}.${digits.slice(6, 9)}-${digits.slice(9)}`;
      }
      normalizePhone(value) {
        const digits = String(value || "").replace(/\D/g, "");
        if (!digits) {
          return null;
        }
        if (/^55\d{10,11}$/.test(digits)) {
          return `+${digits}`;
        }
        if (/^\d{10,11}$/.test(digits)) {
          return `+55${digits}`;
        }
        return null;
      }
      async resolveOrderUser({
        tx,
        userId,
        restaurantId,
        customerName,
        customerCpf,
        customerPhone
      }) {
        const normalizedPhone = this.normalizePhone(customerPhone);
        if (userId) {
          if (normalizedPhone) {
            await tx.user.update({
              where: {
                id: Number(userId)
              },
              data: {
                phone: normalizedPhone
              }
            });
          }
          return Number(userId);
        }
        const normalizedName = String(customerName || "").trim();
        const cpfDigits = String(customerCpf || "").replace(/\D/g, "");
        if (normalizedName.length < 2) {
          throw new Error("Informe o nome para finalizar o pedido.");
        }
        if (cpfDigits.length !== 11) {
          throw new Error("Informe um CPF v\xE1lido com 11 d\xEDgitos.");
        }
        const guestEmail = `guest.${restaurantId}.${cpfDigits}@pecaja.local`;
        const guestPassword = `guest-${restaurantId}-${cpfDigits}`;
        const guestUser = await tx.user.upsert({
          where: {
            email: guestEmail
          },
          update: {
            name: normalizedName,
            active: true,
            ...normalizedPhone ? {
              phone: normalizedPhone
            } : {}
          },
          create: {
            name: normalizedName,
            email: guestEmail,
            password: guestPassword,
            role: "CLIENTE",
            active: true,
            phone: normalizedPhone,
            restaurantId
          },
          select: {
            id: true
          }
        });
        return Number(guestUser.id);
      }
      async resolvePaymentState({
        paymentMethod,
        paid,
        pixPaymentId,
        restaurantId
      }) {
        const normalizedPaymentMethod = String(paymentMethod || "").toUpperCase();
        const normalizedPixPaymentId = String(pixPaymentId || "").trim();
        const requestedAsPaid = paid === true;
        if (!requestedAsPaid) {
          return {
            normalizedPaymentMethod,
            normalizedPixPaymentId,
            shouldMarkAsPaid: false,
            paidAt: null
          };
        }
        if (normalizedPaymentMethod === PaymentMethod3.PIX) {
          if (normalizedPixPaymentId && !normalizedPixPaymentId.startsWith("manual:")) {
            const paymentStatus = await OrderPixPaymentService_default.ensurePaymentApproved({
              paymentId: normalizedPixPaymentId,
              restaurantId
            });
            if (!paymentStatus.sameRestaurant) {
              throw new Error("O pagamento PIX informado nao pertence a este restaurante.");
            }
            return {
              normalizedPaymentMethod,
              normalizedPixPaymentId,
              shouldMarkAsPaid: true,
              paidAt: /* @__PURE__ */ new Date()
            };
          }
          throw new Error("Pagamento PIX ainda nao foi confirmado pelo provedor.");
        }
        if (normalizedPaymentMethod === PaymentMethod3.CARTAO) {
          return {
            normalizedPaymentMethod,
            normalizedPixPaymentId,
            shouldMarkAsPaid: true,
            paidAt: /* @__PURE__ */ new Date()
          };
        }
        return {
          normalizedPaymentMethod,
          normalizedPixPaymentId,
          shouldMarkAsPaid: false,
          paidAt: null
        };
      }
      async execute({
        userId,
        restaurantId,
        userRestaurantId,
        tableSessionId,
        tableSessionTableId,
        participantId,
        settlementMode,
        deferRealtimeUntilPaid,
        type,
        paymentMethod,
        payOnDelivery,
        payOnDeliveryMethod,
        paid,
        pixPaymentId,
        paymentProof,
        observation,
        customerName,
        customerCpf,
        customerPhone,
        tableId,
        couponRedemptionId,
        items,
        address,
        number,
        district,
        city,
        state,
        zipCode,
        paymentProofImage,
        complement
      }) {
        const resolvedRestaurantId = resolveOrderRestaurantId({
          requestedRestaurantId: restaurantId,
          contextRestaurantId: userRestaurantId
        });
        if (paid === true) {
          throw new Error(
            "O pagamento s\xF3 pode ser confirmado pelo provedor ou pelo fluxo administrativo seguro."
          );
        }
        if (String(pixPaymentId || "").trim()) {
          throw new Error("O identificador PIX s\xF3 pode ser vinculado pelo provedor de pagamento.");
        }
        const restaurantSettings = await RestaurantSettingsRepository_default.findByRestaurantId(resolvedRestaurantId);
        const preliminaryTableAccountSettings = type === OrderType5.MESA ? await TableAccountSettingsRepository_default.findByRestaurantId(resolvedRestaurantId) : null;
        assertRestaurantIsOpenForOrders(
          restaurantSettings?.isOpenForOrders,
          restaurantSettings?.businessHours
        );
        const shouldPayOnDelivery = payOnDelivery === true;
        const effectivePaymentMethod = shouldPayOnDelivery ? payOnDeliveryMethod || paymentMethod : paymentMethod;
        const normalizedRequestedPaymentMethod = String(effectivePaymentMethod || "").toUpperCase();
        const tablePaymentMethod = normalizedRequestedPaymentMethod === PaymentMethod3.PIX ? "PIX" : normalizedRequestedPaymentMethod === PaymentMethod3.CARTAO ? "CARD" : void 0;
        const tableContinuation = type === OrderType5.MESA ? tableOrderContinuationInputSchema.parse({
          settlementMode: String(settlementMode || "").trim().toUpperCase() || (tablePaymentMethod ? TableOrderSettlementMode.PAY_NOW : TableOrderSettlementMode.TABLE_ACCOUNT),
          ...tablePaymentMethod ? { paymentMethod: tablePaymentMethod } : {}
        }) : null;
        if (type === OrderType5.MESA && normalizedRequestedPaymentMethod && !tablePaymentMethod) {
          throw new Error(
            "O pedido da mesa s\xF3 aceita PIX ou cart\xE3o no pagamento imediato. Dinheiro e maquininha s\xE3o registrados na conta pelo gar\xE7om."
          );
        }
        if (type === OrderType5.MESA && tableContinuation?.settlementMode === TableOrderSettlementMode.PAY_NOW && preliminaryTableAccountSettings?.enabled && !preliminaryTableAccountSettings.allowOnlinePayment) {
          throw new Error("O pagamento online de pedidos da mesa est\xE1 desativado neste restaurante.");
        }
        if (type === OrderType5.MESA && !Number(participantId || 0)) {
          throw new Error("Participante da mesa n\xE3o identificado. Leia o QR Code novamente.");
        }
        if (normalizedRequestedPaymentMethod === PaymentMethod3.PIX && restaurantSettings?.acceptsPix === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pagamentos por PIX no momento.");
        }
        if (normalizedRequestedPaymentMethod === PaymentMethod3.CARTAO && restaurantSettings?.acceptsCard === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pagamentos com cart\xE3o no momento.");
        }
        if (shouldPayOnDelivery && type !== OrderType5.DELIVERY) {
          throw new Error("Pagar na entrega s\xF3 \xE9 permitido para pedidos de delivery.");
        }
        if (shouldPayOnDelivery && !effectivePaymentMethod) {
          throw new Error("Informe o m\xE9todo de pagamento para pedidos com pagar na entrega.");
        }
        if (normalizedRequestedPaymentMethod === PaymentMethod3.PIX && (String(paymentProof || "").trim() || String(paymentProofImage || "").trim())) {
          throw new Error(
            "Nao e permitido enviar comprovante manual para PIX. O pedido sera confirmado automaticamente pelo provedor."
          );
        }
        createOrderSchema.parse({
          restaurantId: resolvedRestaurantId,
          customerName,
          customerCpf,
          customerPhone,
          type,
          paymentMethod: effectivePaymentMethod,
          payOnDelivery: shouldPayOnDelivery,
          payOnDeliveryMethod: shouldPayOnDelivery ? effectivePaymentMethod : void 0,
          paid,
          pixPaymentId,
          paymentProof,
          observation,
          tableId,
          couponRedemptionId,
          items,
          address,
          number,
          district,
          city,
          state,
          zipCode,
          complement,
          paymentProofImage
        });
        const { normalizedPaymentMethod, normalizedPixPaymentId, shouldMarkAsPaid, paidAt } = await this.resolvePaymentState({
          paymentMethod: effectivePaymentMethod,
          paid: shouldPayOnDelivery ? false : paid,
          pixPaymentId,
          restaurantId: resolvedRestaurantId
        });
        const initialStatus = restaurantSettings?.autoAcceptOrders ? OrderStatus4.PREPARANDO : OrderStatus4.PENDENTE;
        if (type === "MESA") {
          if (!tableSessionId) {
            throw new Error("Sess\xE3o da mesa n\xE3o informada. Acesse novamente pelo QR Code oficial.");
          }
          const session = await TableSessionRepository_default.findById(tableSessionId);
          if (!session || session.status !== TableSessionStatus2.OPEN && !(preliminaryTableAccountSettings?.enabled && !preliminaryTableAccountSettings.blockNewOrdersOnClosingRequest && session.status === TableSessionStatus2.CLOSING_REQUESTED) || session.expiresAt && session.expiresAt.getTime() <= Date.now()) {
            throw new Error("Essa mesa est\xE1 fechada. Pe\xE7a ao gar\xE7om para abrir o atendimento.");
          }
          if (session.table.restaurantId !== resolvedRestaurantId) {
            throw new Error("A sess\xE3o da mesa n\xE3o pertence a este restaurante.");
          }
          if (Number(tableId || 0) && Number(tableId) !== Number(session.tableId)) {
            throw new Error("Mesa do pedido n\xE3o confere com a sess\xE3o validada.");
          }
          if (Number(tableSessionTableId || 0) > 0 && Number(tableSessionTableId) !== Number(session.tableId)) {
            throw new Error("Sess\xE3o da mesa inv\xE1lida para este pedido.");
          }
          tableId = Number(session.tableId);
        }
        if (type === "DELIVERY") {
          const requiredAddressFields = [address, number, district, city, state].map((value) => String(value || "").trim()).filter(Boolean);
          if (requiredAddressFields.length < 5) {
            throw new Error("Informe o endere\xE7o completo para pedidos de delivery.");
          }
          const normalizedCustomerPhone = this.normalizePhone(customerPhone);
          if (!normalizedCustomerPhone && userId) {
            const existingUser = await prisma_default.user.findUnique({
              where: {
                id: Number(userId)
              },
              select: {
                phone: true
              }
            });
            const normalizedExistingPhone = this.normalizePhone(existingUser?.phone);
            if (!normalizedExistingPhone) {
              throw new Error("Informe um celular/WhatsApp v\xE1lido para pedidos de delivery.");
            }
          }
          if (!normalizedCustomerPhone && !userId) {
            throw new Error("Informe um celular/WhatsApp v\xE1lido para pedidos de delivery.");
          }
        }
        const createdOrder = await prisma_default.$transaction(
          async (tx) => {
            let tableParticipant = null;
            if (type === OrderType5.MESA) {
              await lockTablePaymentSession(
                tx,
                resolvedRestaurantId,
                Number(tableSessionId)
              );
              const tableAccountSettings = await TableAccountSettingsRepository_default.findByRestaurantId(resolvedRestaurantId, tx);
              const session = await TableSessionRepository_default.findById(Number(tableSessionId), tx);
              if (!session || session.status !== TableSessionStatus2.OPEN && !(tableAccountSettings.enabled && !tableAccountSettings.blockNewOrdersOnClosingRequest && session.status === TableSessionStatus2.CLOSING_REQUESTED) || session.expiresAt && session.expiresAt.getTime() <= Date.now() || session.table.restaurantId !== resolvedRestaurantId || session.tableId !== Number(tableId)) {
                throw new Error(
                  "A mesa foi fechada durante o pedido. Pe\xE7a ao gar\xE7om para abrir o atendimento novamente."
                );
              }
              tableParticipant = await tx.tableParticipant.findFirst({
                where: {
                  id: Number(participantId),
                  tableSessionId: session.id,
                  restaurantId: resolvedRestaurantId,
                  status: TableParticipantStatus.ACTIVE,
                  revokedAt: null,
                  OR: [{ userId: { not: null } }, { tokenExpiresAt: { gt: /* @__PURE__ */ new Date() } }]
                },
                select: {
                  id: true,
                  userId: true,
                  publicId: true,
                  displayName: true
                }
              });
              if (!tableParticipant) {
                throw new Error(
                  "Sua identifica\xE7\xE3o nesta mesa expirou. Leia o QR Code novamente antes de pedir."
                );
              }
            }
            const activeOrders = await OrderRepository_default.countActiveOperationalOrders(
              resolvedRestaurantId,
              tx
            );
            assertOrderCapacity(activeOrders, restaurantSettings?.maxConcurrentOrders);
            const resolvedUserId = type === OrderType5.MESA ? tableParticipant?.userId ?? null : await this.resolveOrderUser({
              tx,
              userId,
              restaurantId: resolvedRestaurantId,
              customerName,
              customerCpf,
              customerPhone
            });
            const pricing = await OrderPricingService_default.quote({
              restaurantId: resolvedRestaurantId,
              userId: resolvedUserId,
              type,
              items,
              couponRedemptionId,
              db: tx
            });
            const { products, orderItems } = pricing;
            if (type === OrderType5.MESA) {
              const tableAccountSettings = await TableAccountSettingsRepository_default.findByRestaurantId(resolvedRestaurantId, tx);
              if (!tableAccountSettings.enabled) {
                throw new Error(
                  "A conta por mesa est\xE1 desativada. Pe\xE7a ao administrador para revisar as configura\xE7\xF5es."
                );
              }
              if (tableContinuation?.settlementMode === TableOrderSettlementMode.PAY_NOW && !tableAccountSettings.allowOnlinePayment) {
                throw new Error(
                  "O pagamento online de pedidos da mesa est\xE1 desativado neste restaurante."
                );
              }
              if (tableContinuation?.settlementMode === TableOrderSettlementMode.TABLE_ACCOUNT) {
                const ledgerItems = await loadTablePaymentLedgerItems(
                  tx,
                  resolvedRestaurantId,
                  Number(tableSessionId)
                );
                const currentOutstandingCents = ledgerItems.filter((item) => !item.canceled && item.projectedStatus !== "REFUNDED").reduce(
                  (total, item) => total + Math.max(0, item.unitPriceCents - item.paidCents),
                  0
                );
                const incomingOrderCents = decimalMoneyToCents(
                  pricing.total,
                  "total do novo pedido da mesa"
                );
                const localTime = getWeekdayAndMinuteInTimeZone(
                  /* @__PURE__ */ new Date(),
                  tableAccountSettings.timeZone
                );
                const prepayment = requiresPrepayment({
                  currentOutstandingCents,
                  incomingOrderCents,
                  thresholdCents: tableAccountSettings.requirePrepaymentAboveCents,
                  windows: tableAccountSettings.prepaymentWindows,
                  currentWeekday: localTime.weekday,
                  currentMinute: localTime.minuteOfDay
                });
                if (prepayment.required) {
                  throw new Error(
                    prepayment.reason === "SCHEDULE" ? "Neste hor\xE1rio, o pedido da mesa precisa ser pago antes de ser enviado." : "Este pedido ultrapassa o limite da conta da mesa. Escolha pagar agora."
                  );
                }
              }
            }
            const formattedCpf = this.formatCpf(customerCpf);
            const guestSummary = type !== OrderType5.MESA && !userId && customerName ? `Cliente: ${String(customerName).trim()}${formattedCpf ? ` | CPF: ${formattedCpf}` : ""}` : "";
            const mergedObservation = [guestSummary, observation].map((item) => String(item || "").trim()).filter(Boolean).join(" | ");
            const normalizedTableId = tableId === null || tableId === void 0 || tableId === "" ? null : Number(tableId);
            const order = await OrderRepository_default.create(
              {
                total: pricing.total,
                itemsSubtotal: pricing.itemsSubtotal,
                productDiscountTotal: pricing.productDiscountTotal,
                couponDiscount: pricing.couponDiscount,
                deliveryFeeAmount: pricing.deliveryFeeAmount,
                couponId: pricing.couponId,
                couponRedemptionId: pricing.couponRedemptionId,
                couponCode: pricing.couponCode,
                systemFee: 0,
                type,
                paymentMethod: tableContinuation?.settlementMode === TableOrderSettlementMode.TABLE_ACCOUNT ? null : effectivePaymentMethod,
                payOnDelivery: shouldPayOnDelivery,
                payOnDeliveryMethod: shouldPayOnDelivery ? effectivePaymentMethod : null,
                paid: shouldMarkAsPaid,
                pixPaymentId: normalizedPixPaymentId || null,
                paidAt,
                paymentProof: null,
                paymentProofImage: null,
                observation: mergedObservation || null,
                userId: resolvedUserId,
                restaurantId: resolvedRestaurantId,
                tableId: normalizedTableId,
                ...type === OrderType5.MESA ? {
                  tableSessionId: Number(tableSessionId),
                  participantId: Number(tableParticipant?.id),
                  settlementMode: tableContinuation?.settlementMode,
                  tableFinancialStatus: shouldMarkAsPaid ? TableOrderFinancialStatus3.PAID : tableContinuation?.settlementMode === TableOrderSettlementMode.PAY_NOW ? TableOrderFinancialStatus3.PROCESSING : TableOrderFinancialStatus3.UNPAID
                } : {},
                address,
                number,
                district,
                city,
                state,
                zipCode,
                complement,
                status: initialStatus,
                preparationStartedAt: initialStatus === OrderStatus4.PREPARANDO ? /* @__PURE__ */ new Date() : null
              },
              tx
            );
            await reserveCouponRedemption({
              redemptionId: pricing.couponRedemptionId,
              restaurantId: resolvedRestaurantId,
              userId: resolvedUserId,
              db: tx
            });
            if (shouldMarkAsPaid) {
              await markCouponRedemptionUsedForOrder(order.id, resolvedRestaurantId, tx);
            }
            const persistedOrderItems = [];
            for (const item of orderItems) {
              const persistedItem = await tx.orderItem.create({
                data: {
                  ...item,
                  orderId: order.id,
                  ...type === OrderType5.MESA ? {
                    restaurantId: resolvedRestaurantId,
                    tableSessionId: Number(tableSessionId),
                    participantId: Number(tableParticipant?.id)
                  } : {}
                },
                select: {
                  id: true,
                  orderId: true,
                  productId: true,
                  quantity: true,
                  price: true
                }
              });
              persistedOrderItems.push(persistedItem);
            }
            if (type === OrderType5.MESA) {
              const billUnitSeeds = buildTableBillUnitSeeds(orderItems, pricing.couponDiscount);
              const expectedOrderTotalCents = decimalMoneyToCents(
                pricing.total,
                "total do pedido da mesa"
              );
              const billTotalCents = billUnitSeeds.reduce(
                (total, unit) => total + unit.unitPriceCents,
                0
              );
              if (billTotalCents !== expectedOrderTotalCents) {
                throw new Error(
                  "N\xE3o foi poss\xEDvel fechar os centavos dos itens com o total do pedido da mesa."
                );
              }
              const financialStatus = shouldMarkAsPaid ? TableBillItemFinancialStatus2.PAID : tableContinuation?.settlementMode === TableOrderSettlementMode.PAY_NOW ? TableBillItemFinancialStatus2.PROCESSING : TableBillItemFinancialStatus2.UNPAID;
              await tx.tableBillItem.createMany({
                data: billUnitSeeds.map((unit) => ({
                  restaurantId: resolvedRestaurantId,
                  tableSessionId: Number(tableSessionId),
                  participantId: Number(tableParticipant?.id),
                  orderId: order.id,
                  orderItemId: persistedOrderItems[unit.orderItemIndex].id,
                  unitIndex: unit.unitIndex,
                  productName: products[unit.orderItemIndex].name,
                  unitPriceCents: BigInt(unit.unitPriceCents),
                  financialStatus,
                  paidAt: shouldMarkAsPaid ? paidAt : null
                }))
              });
            }
            const requestedQuantityByProduct = /* @__PURE__ */ new Map();
            orderItems.forEach((item) => {
              requestedQuantityByProduct.set(
                item.productId,
                (requestedQuantityByProduct.get(item.productId) || 0) + Number(item.quantity)
              );
            });
            for (const [productId, requestedQuantity] of requestedQuantityByProduct) {
              const product = products.find((candidate) => candidate.id === productId);
              const stockValue = product.stock === null || product.stock === void 0 ? null : Number(product.stock);
              if (!Number.isInteger(stockValue) || stockValue < 0) {
                continue;
              }
              const decremented = await tx.product.updateMany({
                where: {
                  id: productId,
                  restaurantId: resolvedRestaurantId,
                  stock: { gte: requestedQuantity }
                },
                data: {
                  stock: { decrement: requestedQuantity }
                }
              });
              if (decremented.count !== 1) {
                throw new Error(
                  `Estoque de ${product.name} mudou. Confira a quantidade e tente novamente.`
                );
              }
              await tx.product.updateMany({
                where: { id: productId, restaurantId: resolvedRestaurantId, stock: 0 },
                data: { active: false }
              });
            }
            return OrderRepository_default.findById(order.id, resolvedRestaurantId, tx);
          },
          { isolationLevel: Prisma2.TransactionIsolationLevel.Serializable }
        );
        const isUnpaidDelivery = type === OrderType5.DELIVERY && shouldPayOnDelivery !== true && shouldMarkAsPaid !== true;
        const isUnpaidDigitalPayment = shouldMarkAsPaid !== true && shouldPayOnDelivery !== true && (normalizedPaymentMethod === PaymentMethod3.PIX || normalizedPaymentMethod === PaymentMethod3.CARTAO);
        const shouldDeferRealtimeUntilPaid = deferRealtimeUntilPaid === true || isUnpaidDelivery || isUnpaidDigitalPayment;
        if (!shouldDeferRealtimeUntilPaid) {
          io.to(`restaurant:${createdOrder.restaurantId}`).emit("new-order", createdOrder);
          if (createdOrder.userId) {
            io.to(`user:${createdOrder.userId}`).emit("new-order", createdOrder);
          }
          emitWaiterTableOrderEvent(io, "new-order", createdOrder);
          emitTableSessionOrderEvent(io, "new-order", createdOrder);
        }
        if (shouldMarkAsPaid) {
          io.to(`restaurant:${createdOrder.restaurantId}`).emit("order:payment-confirmed", {
            orderId: createdOrder.id,
            paymentMethod: normalizedPaymentMethod,
            paid: true,
            status: createdOrder.status
          });
          if (createdOrder.userId) {
            io.to(`user:${createdOrder.userId}`).emit("payment-confirmed", {
              orderId: createdOrder.id,
              paymentMethod: normalizedPaymentMethod,
              paid: true,
              status: createdOrder.status
            });
          }
          notifyCustomerPaymentConfirmed({
            restaurantId: createdOrder.restaurantId,
            customerPhone: createdOrder?.user?.phone || customerPhone,
            customerName: createdOrder?.user?.name || createdOrder?.participant?.displayName || customerName,
            restaurantName: createdOrder?.restaurant?.name,
            restaurantWhatsapp: createdOrder?.restaurant?.whatsapp,
            orderId: createdOrder?.id,
            total: createdOrder?.total,
            paymentMethod: normalizedPaymentMethod
          }).catch((error2) => {
            console.error(
              "[CUSTOMER_NOTIFICATION_UNHANDLED]",
              error2 instanceof Error ? error2.message : String(error2)
            );
          });
        }
        return createdOrder;
      }
    };
    CreateOrderService_default = new CreateOrderService();
  }
});

// src/modules/orders/controllers/CreateOrderController.ts
var CreateOrderController, CreateOrderController_default;
var init_CreateOrderController = __esm({
  "src/modules/orders/controllers/CreateOrderController.ts"() {
    init_CreateOrderService();
    CreateOrderController = class {
      async handle(req, res) {
        try {
          const {
            restaurantId,
            type,
            paymentMethod,
            payOnDelivery,
            payOnDeliveryMethod,
            observation,
            customerName,
            customerCpf,
            customerPhone,
            tableId,
            settlementMode,
            couponRedemptionId,
            items,
            address,
            number,
            district,
            city,
            state,
            zipCode,
            complement
          } = req.body;
          const userId = req.user?.id ?? null;
          const userRestaurantId = req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null;
          if (payOnDelivery === true && String(payOnDeliveryMethod || paymentMethod || "").toUpperCase() === "DINHEIRO" && String(req.user?.role || "").toUpperCase() !== "ADMIN") {
            throw new Error("Pagamento em dinheiro \xE9 registrado somente pelo administrador.");
          }
          const order = await CreateOrderService_default.execute({
            userId,
            restaurantId,
            userRestaurantId,
            tableSessionId: req.tableSession?.id ?? null,
            tableSessionTableId: req.tableSession?.tableId ?? null,
            participantId: req.tableParticipant?.id ?? null,
            settlementMode,
            type,
            paymentMethod,
            payOnDelivery,
            payOnDeliveryMethod,
            observation,
            customerName,
            customerCpf,
            customerPhone,
            tableId,
            couponRedemptionId,
            items,
            address,
            number,
            district,
            city,
            state,
            zipCode,
            complement
          });
          return res.status(201).json(order);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar pedido"
          });
        }
      }
    };
    CreateOrderController_default = new CreateOrderController();
  }
});

// src/modules/orders/state/orderStateMachine.ts
import { OrderStatus as OrderStatus5 } from "@prisma/client";
function canTransition(from, to) {
  const allowed = transitions[from] || [];
  return allowed.includes(to);
}
var transitions, OrderStateMachine;
var init_orderStateMachine = __esm({
  "src/modules/orders/state/orderStateMachine.ts"() {
    transitions = {
      [OrderStatus5.PENDENTE]: [OrderStatus5.PREPARANDO, OrderStatus5.CANCELADO],
      [OrderStatus5.PREPARANDO]: [OrderStatus5.PRONTO],
      [OrderStatus5.PRONTO]: [OrderStatus5.SAIU_PARA_ENTREGA, OrderStatus5.ENTREGUE],
      [OrderStatus5.SAIU_PARA_ENTREGA]: [OrderStatus5.ENTREGUE],
      [OrderStatus5.ENTREGUE]: [],
      [OrderStatus5.CANCELADO]: []
    };
    OrderStateMachine = {
      transitions,
      canTransition
    };
  }
});

// src/modules/orders/permissions/orderPermissions.ts
import { FuncionarioSubRole, OrderStatus as OrderStatus6, UserRole as UserRole6 } from "@prisma/client";
function canUserChangeStatus(role, status, subRole) {
  if (role === UserRole6.FUNCIONARIO) {
    if (String(subRole || "").toUpperCase() !== FuncionarioSubRole.COZINHA) {
      return false;
    }
    const kitchenStatuses = [OrderStatus6.PREPARANDO, OrderStatus6.PRONTO];
    return kitchenStatuses.includes(status);
  }
  const allowed = permissions[role] || [];
  return allowed.includes(status);
}
var permissions, OrderPermissions;
var init_orderPermissions = __esm({
  "src/modules/orders/permissions/orderPermissions.ts"() {
    permissions = {
      [UserRole6.ADMIN]: [
        OrderStatus6.PENDENTE,
        OrderStatus6.PREPARANDO,
        OrderStatus6.PRONTO,
        OrderStatus6.SAIU_PARA_ENTREGA,
        OrderStatus6.ENTREGUE,
        OrderStatus6.CANCELADO
      ],
      [UserRole6.FUNCIONARIO]: [
        OrderStatus6.PREPARANDO,
        OrderStatus6.PRONTO,
        OrderStatus6.SAIU_PARA_ENTREGA,
        OrderStatus6.ENTREGUE
      ],
      [UserRole6.MOTOQUEIRO]: [OrderStatus6.ENTREGUE],
      [UserRole6.CLIENTE]: [OrderStatus6.CANCELADO]
    };
    OrderPermissions = {
      permissions,
      canUserChangeStatus
    };
  }
});

// src/modules/orders/services/CourierAccessService.ts
import { UserRole as UserRole7 } from "@prisma/client";
var CourierAccessService, CourierAccessService_default;
var init_CourierAccessService = __esm({
  "src/modules/orders/services/CourierAccessService.ts"() {
    init_prisma();
    CourierAccessService = class {
      async assertActiveCourier(courierId, restaurantId, db = prisma_default) {
        if (!Number.isInteger(courierId) || courierId <= 0 || !Number.isInteger(restaurantId) || restaurantId <= 0) {
          throw new Error("Conta de motoqueiro inv\xE1lida para este restaurante.");
        }
        const courier = await db.user.findFirst({
          where: {
            id: courierId,
            restaurantId,
            role: UserRole7.MOTOQUEIRO,
            active: true
          },
          select: { id: true, restaurantId: true }
        });
        if (!courier) {
          throw new Error(
            "Sua conta de motoqueiro n\xE3o est\xE1 ativa neste restaurante. Entre novamente ou fale com o administrador."
          );
        }
        return courier;
      }
    };
    CourierAccessService_default = new CourierAccessService();
  }
});

// src/modules/orders/services/UpdateOrderStatusService.ts
import { OrderStatus as OrderStatus7, OrderType as OrderType6, PaymentMethod as PaymentMethod4, UserRole as UserRole8 } from "@prisma/client";
var UpdateOrderStatusService, UpdateOrderStatusService_default;
var init_UpdateOrderStatusService = __esm({
  "src/modules/orders/services/UpdateOrderStatusService.ts"() {
    init_OrderRepository();
    init_server();
    init_orderStateMachine();
    init_orderPermissions();
    init_customerNotifier();
    init_prisma();
    init_restoreOrderItemsStock();
    init_couponRedemptionLifecycle();
    init_waiterOrderRealtime();
    init_CourierAccessService();
    UpdateOrderStatusService = class {
      PAY_ON_DELIVERY_MARKER = "PAY_ON_DELIVERY:";
      hasLegacyPayOnDeliveryMarker(observation) {
        return String(observation || "").toUpperCase().includes(this.PAY_ON_DELIVERY_MARKER);
      }
      async execute(orderId, restaurantId, status, role, deliveryConfirmationCode, actorUserId, actorSubRole) {
        const order = await OrderRepository_default.findById(orderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado!");
        }
        const currentStatus = order.status;
        const canChange = OrderStateMachine.canTransition(currentStatus, status);
        if (!canChange) {
          throw new Error(`Transi\xE7\xE3o inv\xE1lida: ${currentStatus} \u2192 ${status} `);
        }
        const normalizedRole = String(role || "").toUpperCase();
        const canUserChange = OrderPermissions.canUserChangeStatus(
          normalizedRole,
          status,
          actorSubRole
        );
        if (!canUserChange) {
          throw new Error("Usu\xE1rio n\xE3o tem permiss\xE3o para isso!");
        }
        if (normalizedRole === UserRole8.MOTOQUEIRO) {
          await CourierAccessService_default.assertActiveCourier(
            Number(actorUserId || 0),
            Number(restaurantId)
          );
          if (order.type !== OrderType6.DELIVERY) {
            throw new Error("Motoqueiros s\xF3 podem atualizar pedidos de entrega.");
          }
          if (order.assignedCourierId !== Number(actorUserId || 0)) {
            throw new Error("Esta entrega n\xE3o est\xE1 atribu\xEDda a voc\xEA.");
          }
        }
        if (status === OrderStatus7.ENTREGUE && normalizedRole === UserRole8.MOTOQUEIRO && order.type === OrderType6.DELIVERY) {
          const customerPhoneDigits = String(order?.user?.phone || "").replace(/\D/g, "");
          const expectedCode = customerPhoneDigits.slice(-4);
          const providedCode = String(deliveryConfirmationCode || "").replace(/\D/g, "");
          if (!customerPhoneDigits || customerPhoneDigits.length < 4) {
            throw new Error(
              "N\xE3o \xE9 poss\xEDvel confirmar a entrega: cliente sem telefone v\xE1lido cadastrado."
            );
          }
          if (!/^\d{4}$/.test(providedCode)) {
            throw new Error(
              "Informe os 4 \xFAltimos d\xEDgitos do celular do cliente para concluir a entrega."
            );
          }
          if (providedCode !== expectedCode) {
            throw new Error("C\xF3digo de confirma\xE7\xE3o inv\xE1lido para esta entrega.");
          }
        }
        const digitalMethods = [PaymentMethod4.PIX, PaymentMethod4.CARTAO];
        const isPayOnDelivery = order.payOnDelivery === true || this.hasLegacyPayOnDeliveryMarker(order?.observation);
        const isDigitalPayment = !!order.paymentMethod && digitalMethods.includes(order.paymentMethod);
        const isUnpaidDigitalOrderBlocked = isDigitalPayment && !isPayOnDelivery && order.paid !== true;
        if (status === OrderStatus7.CANCELADO && isDigitalPayment && !isPayOnDelivery && order.paid === true) {
          throw new Error(
            "Pedido pago online deve ser cancelado pelo fluxo de estorno para devolver o valor ao cliente."
          );
        }
        if (isUnpaidDigitalOrderBlocked && status !== OrderStatus7.PENDENTE && status !== OrderStatus7.CANCELADO) {
          throw new Error(
            "Pedido com pagamento digital pendente deve permanecer em PENDENTE at\xE9 a confirma\xE7\xE3o do pagamento."
          );
        }
        let updatedOrder;
        let paymentConfirmedOnDelivery = false;
        if (status === OrderStatus7.CANCELADO) {
          updatedOrder = await prisma_default.$transaction(async (tx) => {
            const cancelledOrder = await OrderRepository_default.updateStatusIfCurrent(
              orderId,
              status,
              restaurantId,
              { status: currentStatus, paid: order.paid },
              tx
            );
            await restoreOrderItemsStock(tx, order);
            await releaseCouponRedemptionForOrder(orderId, restaurantId, tx);
            return cancelledOrder;
          });
        } else if (status === OrderStatus7.ENTREGUE) {
          updatedOrder = await prisma_default.$transaction(async (tx) => {
            let deliveredOrder = await OrderRepository_default.updateStatusIfCurrent(
              orderId,
              status,
              restaurantId,
              { status: currentStatus, paid: order.paid },
              tx
            );
            if (!deliveredOrder) {
              throw new Error("Pedido n\xE3o encontrado para atualizar.");
            }
            deliveredOrder = await tx.order.update({
              where: { id: deliveredOrder.id },
              data: { deliveredAt: /* @__PURE__ */ new Date() },
              include: {
                user: { select: { id: true, name: true, email: true, phone: true } },
                restaurant: {
                  select: { id: true, name: true, whatsapp: true }
                },
                table: {
                  select: { id: true, number: true, active: true, restaurantId: true }
                },
                participant: {
                  select: { id: true, publicId: true, displayName: true }
                },
                items: { include: { product: true } }
              }
            });
            if ((order.paymentMethod === PaymentMethod4.DINHEIRO || isPayOnDelivery) && deliveredOrder.paid !== true) {
              paymentConfirmedOnDelivery = true;
              deliveredOrder = await OrderRepository_default.confirmPayment(orderId, restaurantId, tx);
            }
            if (deliveredOrder?.paid === true) {
              await markCouponRedemptionUsedForOrder(orderId, restaurantId, tx);
            }
            return deliveredOrder;
          });
        } else {
          updatedOrder = await OrderRepository_default.updateStatusIfCurrent(orderId, status, restaurantId, {
            status: currentStatus,
            paid: order.paid
          });
        }
        if (paymentConfirmedOnDelivery && updatedOrder) {
          io.to(`restaurant:${restaurantId}`).emit("order:payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod
          });
          if (updatedOrder.userId) {
            io.to(`user:${updatedOrder.userId}`).emit("order:payment-confirmed", {
              orderId: updatedOrder.id,
              paid: true,
              paymentMethod: updatedOrder.paymentMethod
            });
          }
          emitTableSessionOrderEvent(io, "order:payment-confirmed", updatedOrder);
        }
        notifyCustomerOrderStatusChanged({
          restaurantId,
          customerPhone: order?.user?.phone,
          customerName: order?.user?.name,
          restaurantName: order?.restaurant?.name,
          restaurantWhatsapp: order?.restaurant?.whatsapp,
          orderId: updatedOrder?.id,
          status: updatedOrder?.status
        }).catch((error2) => {
          console.error(
            "[CUSTOMER_STATUS_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        io.to(`restaurant:${restaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        emitWaiterTableOrderEvent(io, "waiter:order-updated", updatedOrder);
        emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
        return updatedOrder;
      }
    };
    UpdateOrderStatusService_default = new UpdateOrderStatusService();
  }
});

// src/modules/orders/controllers/UpdateOrderStatusController.ts
var UpdateOrderStatusController, UpdateOrderStatusController_default;
var init_UpdateOrderStatusController = __esm({
  "src/modules/orders/controllers/UpdateOrderStatusController.ts"() {
    init_UpdateOrderStatusService();
    UpdateOrderStatusController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { status, deliveryConfirmationCode } = req.body;
          const normalizedStatus = String(status || "").toUpperCase();
          const { restaurantId, role } = req.user;
          const updatedOrder = await UpdateOrderStatusService_default.execute(
            id,
            restaurantId,
            normalizedStatus,
            role,
            deliveryConfirmationCode,
            req.user.id,
            req.user.subRole
          );
          return res.status(200).json(updatedOrder);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar status do pedido"
          });
        }
      }
    };
    UpdateOrderStatusController_default = new UpdateOrderStatusController();
  }
});

// src/socket/deliveryLocationPayload.ts
function optionalFiniteNumber(value) {
  if (value === null || value === void 0 || value === "") return null;
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}
function validateDeliveryLocationPayload(rawPayload, now = Date.now()) {
  const orderId = Number(rawPayload?.orderId || 0);
  if (!Number.isInteger(orderId) || orderId <= 0) {
    return { ok: false, error: "Pedido inv\xE1lido para rastreio." };
  }
  const latitude = Number(rawPayload?.latitude);
  const longitude = Number(rawPayload?.longitude);
  const validCoordinates = Number.isFinite(latitude) && Number.isFinite(longitude) && latitude >= -90 && latitude <= 90 && longitude >= -180 && longitude <= 180;
  if (!validCoordinates) {
    return { ok: false, error: "Coordenadas inv\xE1lidas." };
  }
  const heading = optionalFiniteNumber(rawPayload?.heading);
  if (heading !== null && (heading < 0 || heading > 360)) {
    return { ok: false, error: "Dire\xE7\xE3o da localiza\xE7\xE3o inv\xE1lida." };
  }
  const speed = optionalFiniteNumber(rawPayload?.speed);
  if (speed !== null && (speed < 0 || speed > 100)) {
    return { ok: false, error: "Velocidade da localiza\xE7\xE3o inv\xE1lida." };
  }
  const accuracy = optionalFiniteNumber(rawPayload?.accuracy);
  if (accuracy !== null && (accuracy < 0 || accuracy > 1e4)) {
    return { ok: false, error: "Precis\xE3o da localiza\xE7\xE3o inv\xE1lida." };
  }
  const sentAtValue = typeof rawPayload?.sentAt === "string" && rawPayload.sentAt.trim() ? rawPayload.sentAt.trim() : new Date(now).toISOString();
  const sentAtMs = Date.parse(sentAtValue);
  if (!Number.isFinite(sentAtMs)) {
    return { ok: false, error: "Hor\xE1rio da localiza\xE7\xE3o inv\xE1lido." };
  }
  if (sentAtMs < now - 5 * 6e4 || sentAtMs > now + 6e4) {
    return { ok: false, error: "Localiza\xE7\xE3o fora da janela de tempo permitida." };
  }
  const recordedAt = new Date(sentAtMs);
  return {
    ok: true,
    value: {
      orderId,
      latitude,
      longitude,
      heading,
      speed,
      accuracy: accuracy === null ? null : Math.round(accuracy),
      sentAt: recordedAt.toISOString(),
      recordedAt
    }
  };
}
var init_deliveryLocationPayload = __esm({
  "src/socket/deliveryLocationPayload.ts"() {
  }
});

// src/modules/orders/services/ClaimOrderForDeliveryService.ts
import { OrderStatus as OrderStatus8, OrderType as OrderType7, UserRole as UserRole9 } from "@prisma/client";
var ClaimOrderForDeliveryService, ClaimOrderForDeliveryService_default;
var init_ClaimOrderForDeliveryService = __esm({
  "src/modules/orders/services/ClaimOrderForDeliveryService.ts"() {
    init_prisma();
    init_server();
    init_customerNotifier();
    init_OrderRepository();
    init_CourierAccessService();
    init_deliveryLocationPayload();
    ClaimOrderForDeliveryService = class {
      async execute({
        orderId,
        restaurantId,
        courierId,
        role,
        initialLocation
      }) {
        const normalizedOrderId = Number(orderId);
        if (String(role || "").toUpperCase() !== UserRole9.MOTOQUEIRO) {
          throw new Error("Somente motoqueiros podem retirar pedidos para entrega.");
        }
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new Error("Pedido inv\xE1lido.");
        }
        if (!initialLocation || typeof initialLocation !== "object" || Array.isArray(initialLocation)) {
          throw new Error("A localiza\xE7\xE3o atual \xE9 obrigat\xF3ria para retirar o pedido.");
        }
        const initialLocationValidation = validateDeliveryLocationPayload({
          ...initialLocation,
          orderId: normalizedOrderId
        });
        if ("error" in initialLocationValidation) {
          throw new Error(initialLocationValidation.error);
        }
        const result = await prisma_default.$transaction(async (tx) => {
          await CourierAccessService_default.assertActiveCourier(courierId, restaurantId, tx);
          const settings = await tx.restaurantSettings.findUnique({
            where: { restaurantId },
            select: { courierFeePerDelivery: true }
          });
          const courierEarning = settings?.courierFeePerDelivery || 0;
          const claimed = await tx.order.updateMany({
            where: {
              id: normalizedOrderId,
              restaurantId,
              type: OrderType7.DELIVERY,
              status: OrderStatus8.PRONTO,
              assignedCourierId: null,
              NOT: {
                paid: false,
                paymentMethod: { in: ["PIX", "CARTAO"] },
                payOnDelivery: false
              }
            },
            data: {
              assignedCourierId: courierId,
              deliveryStartedAt: /* @__PURE__ */ new Date(),
              courierEarning,
              status: OrderStatus8.SAIU_PARA_ENTREGA
            }
          });
          if (claimed.count !== 1) {
            const current = await tx.order.findFirst({
              where: { id: normalizedOrderId, restaurantId },
              select: { type: true, status: true, assignedCourierId: true }
            });
            if (!current) throw new Error("Pedido n\xE3o encontrado.");
            if (current.type !== OrderType7.DELIVERY) throw new Error("Este pedido n\xE3o \xE9 uma entrega.");
            if (current.assignedCourierId)
              throw new Error("Este pedido j\xE1 foi retirado por outro motoqueiro.");
            throw new Error("O pedido n\xE3o est\xE1 dispon\xEDvel para retirada.");
          }
          const updatedOrder2 = await OrderRepository_default.findById(normalizedOrderId, restaurantId, tx);
          if (!updatedOrder2) throw new Error("N\xE3o foi poss\xEDvel carregar o pedido.");
          const location2 = initialLocationValidation.value;
          const savedLocation2 = await tx.deliveryLocation.create({
            data: {
              orderId: normalizedOrderId,
              courierId,
              latitude: location2.latitude,
              longitude: location2.longitude,
              heading: location2.heading,
              speed: location2.speed,
              accuracy: location2.accuracy,
              recordedAt: location2.recordedAt
            },
            select: { recordedAt: true }
          });
          return { updatedOrder: updatedOrder2, location: location2, savedLocation: savedLocation2 };
        });
        const { updatedOrder, location, savedLocation } = result;
        notifyCustomerOrderStatusChanged({
          restaurantId,
          customerPhone: updatedOrder.user?.phone,
          customerName: updatedOrder.user?.name,
          restaurantName: updatedOrder.restaurant?.name,
          restaurantWhatsapp: updatedOrder.restaurant?.whatsapp,
          orderId: updatedOrder.id,
          status: updatedOrder.status
        }).catch((error2) => {
          console.error(
            "[CUSTOMER_STATUS_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        io.to(`restaurant:${restaurantId}`).emit("order:status-changed", updatedOrder);
        io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        const payload = {
          orderId: updatedOrder.id,
          restaurantId,
          latitude: location.latitude,
          longitude: location.longitude,
          heading: location.heading,
          speed: location.speed,
          accuracy: location.accuracy,
          sentAt: location.sentAt,
          recordedAt: savedLocation.recordedAt.toISOString(),
          updatedAt: (/* @__PURE__ */ new Date()).toISOString()
        };
        io.to(`user:${updatedOrder.userId}`).emit("order:delivery-location", payload);
        io.to(`restaurant:${restaurantId}:admin`).emit("order:delivery-location", payload);
        return updatedOrder;
      }
    };
    ClaimOrderForDeliveryService_default = new ClaimOrderForDeliveryService();
  }
});

// src/modules/orders/controllers/ClaimOrderForDeliveryController.ts
var ClaimOrderForDeliveryController, ClaimOrderForDeliveryController_default;
var init_ClaimOrderForDeliveryController = __esm({
  "src/modules/orders/controllers/ClaimOrderForDeliveryController.ts"() {
    init_ClaimOrderForDeliveryService();
    ClaimOrderForDeliveryController = class {
      async handle(req, res) {
        try {
          const order = await ClaimOrderForDeliveryService_default.execute({
            orderId: Array.isArray(req.params.id) ? req.params.id[0] : req.params.id,
            restaurantId: Number(req.user.restaurantId || 0),
            courierId: Number(req.user.id || 0),
            role: req.user.role,
            initialLocation: req.body?.initialLocation && typeof req.body.initialLocation === "object" ? req.body.initialLocation : null
          });
          return res.status(200).json(order);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao retirar pedido"
          });
        }
      }
    };
    ClaimOrderForDeliveryController_default = new ClaimOrderForDeliveryController();
  }
});

// src/modules/orders/services/GetCourierFinanceService.ts
import { OrderStatus as OrderStatus9, UserRole as UserRole10 } from "@prisma/client";
function startOfDay(date) {
  return new Date(date.getFullYear(), date.getMonth(), date.getDate());
}
var GetCourierFinanceService, GetCourierFinanceService_default;
var init_GetCourierFinanceService = __esm({
  "src/modules/orders/services/GetCourierFinanceService.ts"() {
    init_prisma();
    init_CourierAccessService();
    GetCourierFinanceService = class {
      async execute({
        courierId,
        restaurantId,
        role
      }) {
        if (String(role || "").toUpperCase() !== UserRole10.MOTOQUEIRO) {
          throw new Error("Financeiro dispon\xEDvel somente para motoqueiros.");
        }
        await CourierAccessService_default.assertActiveCourier(courierId, restaurantId);
        const now = /* @__PURE__ */ new Date();
        const today = startOfDay(now);
        const week = new Date(today);
        week.setDate(today.getDate() - (today.getDay() + 6) % 7);
        const month = new Date(today.getFullYear(), today.getMonth(), 1);
        const baseWhere = {
          assignedCourierId: courierId,
          restaurantId,
          status: OrderStatus9.ENTREGUE
        };
        const [todayData, weekData, monthData, pendingData, deliveries] = await Promise.all([
          prisma_default.order.aggregate({
            where: { ...baseWhere, deliveredAt: { gte: today } },
            _sum: { courierEarning: true },
            _count: true
          }),
          prisma_default.order.aggregate({
            where: { ...baseWhere, deliveredAt: { gte: week } },
            _sum: { courierEarning: true },
            _count: true
          }),
          prisma_default.order.aggregate({
            where: { ...baseWhere, deliveredAt: { gte: month } },
            _sum: { courierEarning: true },
            _count: true
          }),
          prisma_default.order.aggregate({
            where: { ...baseWhere, courierPaidAt: null },
            _sum: { courierEarning: true },
            _count: true
          }),
          prisma_default.order.findMany({
            where: baseWhere,
            select: {
              id: true,
              courierEarning: true,
              courierPaidAt: true,
              deliveredAt: true,
              deliveryStartedAt: true,
              city: true,
              district: true
            },
            orderBy: { deliveredAt: "desc" },
            take: 100
          })
        ]);
        const format = (entry) => ({
          amount: Number(entry._sum.courierEarning || 0),
          deliveries: entry._count
        });
        return {
          today: format(todayData),
          week: format(weekData),
          month: format(monthData),
          pending: format(pendingData),
          deliveries: deliveries.map((order) => ({
            ...order,
            courierEarning: Number(order.courierEarning || 0)
          }))
        };
      }
    };
    GetCourierFinanceService_default = new GetCourierFinanceService();
  }
});

// src/modules/orders/controllers/GetCourierFinanceController.ts
var GetCourierFinanceController, GetCourierFinanceController_default;
var init_GetCourierFinanceController = __esm({
  "src/modules/orders/controllers/GetCourierFinanceController.ts"() {
    init_GetCourierFinanceService();
    GetCourierFinanceController = class {
      async handle(req, res) {
        try {
          const result = await GetCourierFinanceService_default.execute({
            courierId: Number(req.user.id || 0),
            restaurantId: Number(req.user.restaurantId || 0),
            role: req.user.role
          });
          return res.json(result);
        } catch (error2) {
          return res.status(403).json({ error: error2 instanceof Error ? error2.message : "Erro ao consultar financeiro" });
        }
      }
    };
    GetCourierFinanceController_default = new GetCourierFinanceController();
  }
});

// src/modules/orders/utils/deliveryRouteEstimate.ts
function buildDeliveryDestination(address) {
  return [address.address, address.number, address.district, address.city, address.state, "Brasil"].map((part) => String(part || "").trim()).filter(Boolean).join(", ");
}
function parseOsrmRouteEstimate(response) {
  const route = response.routes?.[0];
  const durationSeconds = Number(route?.duration);
  if (!Number.isFinite(durationSeconds) || durationSeconds <= 0) return null;
  const rawCoordinates = Array.isArray(route?.geometry?.coordinates) ? route.geometry.coordinates : [];
  const routeCoordinates = rawCoordinates.map((coordinate) => {
    if (!Array.isArray(coordinate) || coordinate.length < 2) return null;
    const longitude = Number(coordinate[0]);
    const latitude = Number(coordinate[1]);
    return hasValidCoordinates({ latitude, longitude }) ? { latitude, longitude } : null;
  }).filter((coordinate) => Boolean(coordinate));
  return {
    durationSeconds: Math.round(durationSeconds),
    distanceMeters: typeof route?.distance === "number" && route.distance >= 0 ? Math.round(route.distance) : null,
    provider: "OSRM",
    ...routeCoordinates.length ? { routeCoordinates } : {}
  };
}
function limitRouteCoordinates(coordinates, maximumPoints = 250) {
  if (coordinates.length <= maximumPoints) return coordinates;
  const lastIndex = coordinates.length - 1;
  return Array.from({ length: maximumPoints }, (_, index) => {
    const sourceIndex = Math.round(index * lastIndex / (maximumPoints - 1));
    return coordinates[sourceIndex];
  });
}
function hasValidCoordinates(value) {
  return Number.isFinite(value?.latitude) && Number.isFinite(value?.longitude) && Number(value?.latitude) >= -90 && Number(value?.latitude) <= 90 && Number(value?.longitude) >= -180 && Number(value?.longitude) <= 180;
}
var init_deliveryRouteEstimate = __esm({
  "src/modules/orders/utils/deliveryRouteEstimate.ts"() {
  }
});

// src/modules/orders/services/GetOsrmDeliveryRouteService.ts
function normalizedBaseUrl(value) {
  return value.replace(/\/$/, "");
}
function positiveInteger2(value, fallback) {
  const parsed = Number(value);
  return Number.isInteger(parsed) && parsed > 0 ? parsed : fallback;
}
function getFreshCachedValue(cache, key) {
  const cached = cache.get(key);
  if (!cached) return void 0;
  if (cached.expiresAt <= Date.now()) {
    cache.delete(key);
    return void 0;
  }
  cache.delete(key);
  cache.set(key, cached);
  return cached.value;
}
function setBoundedCacheValue(cache, key, value, ttlMs, maximumEntries) {
  const now = Date.now();
  for (const [cachedKey, cached] of cache) {
    if (cached.expiresAt <= now) cache.delete(cachedKey);
  }
  cache.delete(key);
  while (cache.size >= maximumEntries) {
    const oldestKey = cache.keys().next().value;
    if (oldestKey === void 0) break;
    cache.delete(oldestKey);
  }
  cache.set(key, { value, expiresAt: now + ttlMs });
}
function attachCurrentDestination(route, coordinates, label) {
  if (!route) return null;
  return {
    ...route,
    destination: {
      ...coordinates,
      label
    }
  };
}
var ROUTE_CACHE_TTL_MS, GEOCODE_CACHE_TTL_MS, GetOsrmDeliveryRouteService, GetOsrmDeliveryRouteService_default;
var init_GetOsrmDeliveryRouteService = __esm({
  "src/modules/orders/services/GetOsrmDeliveryRouteService.ts"() {
    init_deliveryRouteEstimate();
    ROUTE_CACHE_TTL_MS = 2e4;
    GEOCODE_CACHE_TTL_MS = 24 * 60 * 60 * 1e3;
    GetOsrmDeliveryRouteService = class {
      // O endereço textual pertence ao pedido atual e nunca pode entrar neste cache global.
      routeCache = /* @__PURE__ */ new Map();
      geocodeCache = /* @__PURE__ */ new Map();
      get osrmBaseUrl() {
        return normalizedBaseUrl(String(process.env.OSRM_BASE_URL || "").trim());
      }
      get geocoderBaseUrl() {
        return normalizedBaseUrl(String(process.env.GEOCODER_BASE_URL || "").trim());
      }
      get maximumCacheEntries() {
        return positiveInteger2(process.env.ROUTING_CACHE_MAX_ENTRIES, 5e3);
      }
      get routeTimeoutMs() {
        return positiveInteger2(process.env.ROUTING_REQUEST_TIMEOUT_MS, 4e3);
      }
      get geocoderTimeoutMs() {
        return positiveInteger2(process.env.GEOCODER_REQUEST_TIMEOUT_MS, 4e3);
      }
      async geocode(destination) {
        const cached = getFreshCachedValue(this.geocodeCache, destination);
        if (cached !== void 0) return cached;
        if (!this.geocoderBaseUrl) return null;
        try {
          const url = new URL(`${this.geocoderBaseUrl}/search`);
          url.searchParams.set("q", destination);
          url.searchParams.set("format", "jsonv2");
          url.searchParams.set("limit", "1");
          url.searchParams.set("countrycodes", "br");
          const response = await fetch(url, {
            headers: {
              Accept: "application/json",
              "User-Agent": String(process.env.ROUTING_USER_AGENT || "PizzaIADelivery/1.0")
            },
            signal: AbortSignal.timeout(this.geocoderTimeoutMs)
          });
          const result = response.ok ? (await response.json())?.[0] : null;
          const coordinates = result ? { latitude: Number(result.lat), longitude: Number(result.lon) } : null;
          const value = hasValidCoordinates(coordinates) ? coordinates : null;
          setBoundedCacheValue(
            this.geocodeCache,
            destination,
            value,
            GEOCODE_CACHE_TTL_MS,
            this.maximumCacheEntries
          );
          return value;
        } catch (error2) {
          console.warn(
            "[delivery-route] Nao foi possivel localizar o endereco do pedido",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return null;
        }
      }
      async execute(input) {
        const destination = buildDeliveryDestination(input.destination);
        if (!this.osrmBaseUrl || !destination || !hasValidCoordinates(input)) return null;
        const destinationCoordinates = await this.geocode(destination);
        if (!destinationCoordinates) return null;
        const cacheKey = `${input.latitude.toFixed(4)}:${input.longitude.toFixed(4)}:${destinationCoordinates.latitude.toFixed(4)}:${destinationCoordinates.longitude.toFixed(4)}`;
        const cached = getFreshCachedValue(this.routeCache, cacheKey);
        if (cached !== void 0) {
          return attachCurrentDestination(cached, destinationCoordinates, destination);
        }
        try {
          const coordinates = `${input.longitude},${input.latitude};${destinationCoordinates.longitude},${destinationCoordinates.latitude}`;
          const response = await fetch(
            `${this.osrmBaseUrl}/route/v1/driving/${coordinates}?overview=full&geometries=geojson`,
            {
              headers: { Accept: "application/json" },
              signal: AbortSignal.timeout(this.routeTimeoutMs)
            }
          );
          const route = response.ok ? parseOsrmRouteEstimate(await response.json()) : null;
          const estimate = route ? {
            ...route,
            routeCoordinates: limitRouteCoordinates(
              route.routeCoordinates?.length ? route.routeCoordinates : [
                { latitude: input.latitude, longitude: input.longitude },
                destinationCoordinates
              ]
            )
          } : null;
          setBoundedCacheValue(
            this.routeCache,
            cacheKey,
            estimate,
            ROUTE_CACHE_TTL_MS,
            this.maximumCacheEntries
          );
          return attachCurrentDestination(estimate, destinationCoordinates, destination);
        } catch (error2) {
          console.warn(
            "[delivery-route] Nao foi possivel calcular a rota do pedido",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return null;
        }
      }
    };
    GetOsrmDeliveryRouteService_default = new GetOsrmDeliveryRouteService();
  }
});

// src/modules/orders/services/GetDeliveryTrackingService.ts
import { UserRole as UserRole11 } from "@prisma/client";
var GetDeliveryTrackingService, GetDeliveryTrackingService_default;
var init_GetDeliveryTrackingService = __esm({
  "src/modules/orders/services/GetDeliveryTrackingService.ts"() {
    init_prisma();
    init_GetOsrmDeliveryRouteService();
    init_CourierAccessService();
    GetDeliveryTrackingService = class {
      async execute({
        orderId,
        userId,
        restaurantId,
        role
      }) {
        const id = Number(orderId);
        if (!Number.isInteger(id) || id <= 0) throw new Error("Pedido inv\xE1lido.");
        const order = await prisma_default.order.findUnique({
          where: { id },
          select: {
            id: true,
            userId: true,
            restaurantId: true,
            assignedCourierId: true,
            status: true,
            type: true,
            address: true,
            number: true,
            district: true,
            city: true,
            state: true,
            deliveryStartedAt: true,
            deliveredAt: true,
            assignedCourier: { select: { id: true, name: true, phone: true, avatar: true } }
          }
        });
        if (!order) throw new Error("Pedido n\xE3o encontrado.");
        if (String(order.type || "").toUpperCase() !== "DELIVERY") {
          throw new Error("Rastreamento dispon\xEDvel apenas para pedidos de delivery.");
        }
        const normalizedRole = String(role || "").toUpperCase();
        const allowed = order.userId === userId || normalizedRole === UserRole11.MOTOQUEIRO && order.assignedCourierId === userId || normalizedRole === UserRole11.ADMIN && order.restaurantId === restaurantId;
        if (!allowed) throw new Error("Voc\xEA n\xE3o pode acompanhar esta entrega.");
        if (normalizedRole === UserRole11.MOTOQUEIRO) {
          await CourierAccessService_default.assertActiveCourier(userId, Number(restaurantId || 0));
        }
        if (normalizedRole === UserRole11.ADMIN) {
          const activeAdmin = await prisma_default.user.findFirst({
            where: {
              id: userId,
              restaurantId: Number(restaurantId || 0),
              role: UserRole11.ADMIN,
              active: true
            },
            select: { id: true }
          });
          if (!activeAdmin) {
            throw new Error("Sua conta de administrador n\xE3o est\xE1 ativa neste restaurante.");
          }
        }
        const locations = await prisma_default.deliveryLocation.findMany({
          where: { orderId: id },
          orderBy: { recordedAt: "desc" },
          take: 1e3,
          select: {
            latitude: true,
            longitude: true,
            heading: true,
            speed: true,
            accuracy: true,
            recordedAt: true
          }
        });
        locations.reverse();
        const latestLocation = locations.length ? locations[locations.length - 1] : null;
        const routeEstimate = order.status === "SAIU_PARA_ENTREGA" && latestLocation ? await GetOsrmDeliveryRouteService_default.execute({
          latitude: Number(latestLocation.latitude),
          longitude: Number(latestLocation.longitude),
          destination: order
        }) : null;
        const estimatedArrival = routeEstimate ? new Date(Date.now() + routeEstimate.durationSeconds * 1e3).toISOString() : null;
        return {
          order: { ...order, estimatedArrival, routeEstimate },
          locations: locations.map((point) => ({
            ...point,
            latitude: Number(point.latitude),
            longitude: Number(point.longitude)
          })),
          latestLocation: latestLocation ? {
            ...latestLocation,
            latitude: Number(latestLocation.latitude),
            longitude: Number(latestLocation.longitude)
          } : null
        };
      }
    };
    GetDeliveryTrackingService_default = new GetDeliveryTrackingService();
  }
});

// src/modules/orders/controllers/GetDeliveryTrackingController.ts
var GetDeliveryTrackingController, GetDeliveryTrackingController_default;
var init_GetDeliveryTrackingController = __esm({
  "src/modules/orders/controllers/GetDeliveryTrackingController.ts"() {
    init_GetDeliveryTrackingService();
    GetDeliveryTrackingController = class {
      async handle(req, res, next) {
        try {
          const result = await GetDeliveryTrackingService_default.execute({
            orderId: Array.isArray(req.params.id) ? req.params.id[0] : req.params.id,
            userId: Number(req.user.id || 0),
            restaurantId: req.user.restaurantId,
            role: req.user.role
          });
          return res.json(result);
        } catch (error2) {
          const message = error2 instanceof Error ? error2.message : "";
          if (/Pedido inválido|apenas para pedidos de delivery/i.test(message)) {
            return res.status(400).json({ error: message });
          }
          if (/Pedido não encontrado/i.test(message)) {
            return res.status(404).json({ error: message });
          }
          if (/não pode acompanhar|conta de .*não está ativa/i.test(message)) {
            return res.status(403).json({ error: message });
          }
          return next(error2);
        }
      }
    };
    GetDeliveryTrackingController_default = new GetDeliveryTrackingController();
  }
});

// src/modules/orders/services/ListOrdersService.ts
import { FuncionarioSubRole as FuncionarioSubRole2, OrderStatus as OrderStatus10, UserRole as UserRole12 } from "@prisma/client";
var ListOrdersService, ListOrdersService_default;
var init_ListOrdersService = __esm({
  "src/modules/orders/services/ListOrdersService.ts"() {
    init_OrderRepository();
    init_CourierAccessService();
    ListOrdersService = class {
      async execute(restaurantId, status, role, userId, subRole) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para listar pedidos.");
        }
        if (String(role || "").toUpperCase() === UserRole12.MOTOQUEIRO) {
          const courierId = Number(userId || 0);
          if (!Number.isInteger(courierId) || courierId <= 0) {
            throw new Error("Motoqueiro inv\xE1lido.");
          }
          await CourierAccessService_default.assertActiveCourier(courierId, normalizedRestaurantId);
          return OrderRepository_default.findCourierOrders(normalizedRestaurantId, courierId, status);
        }
        if (String(role || "").toUpperCase() === UserRole12.FUNCIONARIO) {
          const normalizedSubRole = String(subRole || "").toUpperCase();
          if (normalizedSubRole === FuncionarioSubRole2.GARCOM) {
            if (status && status !== OrderStatus10.PRONTO) {
              return [];
            }
            return OrderRepository_default.findReadyTableOrders(normalizedRestaurantId);
          }
          if (normalizedSubRole !== FuncionarioSubRole2.COZINHA) {
            throw new Error("Funcion\xE1rio sem perfil operacional v\xE1lido.");
          }
        }
        return OrderRepository_default.findAll(normalizedRestaurantId, status);
      }
    };
    ListOrdersService_default = new ListOrdersService();
  }
});

// src/modules/orders/controllers/ListOrdersController.ts
import { OrderStatus as OrderStatus11 } from "@prisma/client";
var ListOrdersController, ListOrdersController_default;
var init_ListOrdersController = __esm({
  "src/modules/orders/controllers/ListOrdersController.ts"() {
    init_ListOrdersService();
    ListOrdersController = class {
      async handle(req, res) {
        try {
          const status = Array.isArray(req.query.status) ? req.query.status[0] : req.query.status;
          const normalizedStatus = status ? String(status).toUpperCase() : void 0;
          if (normalizedStatus && !Object.values(OrderStatus11).includes(normalizedStatus)) {
            return res.status(400).json({ error: "Status de pedido inv\xE1lido." });
          }
          const restaurantId = req.user.restaurantId;
          const orders = await ListOrdersService_default.execute(
            restaurantId,
            normalizedStatus,
            req.user.role,
            req.user.id,
            req.user.subRole
          );
          return res.json(orders);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar pedidos"
          });
        }
      }
    };
    ListOrdersController_default = new ListOrdersController();
  }
});

// src/modules/orders/services/GetOrderByIdService.ts
import { FuncionarioSubRole as FuncionarioSubRole3, UserRole as UserRole13 } from "@prisma/client";
var GetOrderByIdService, GetOrderByIdService_default;
var init_GetOrderByIdService = __esm({
  "src/modules/orders/services/GetOrderByIdService.ts"() {
    init_OrderRepository();
    init_CourierAccessService();
    GetOrderByIdService = class {
      async execute(orderId, restaurantId, role, subRole, actorUserId) {
        const normalizedRole = String(role || "").toUpperCase();
        const normalizedSubRole = String(subRole || "").toUpperCase();
        if (normalizedRole === UserRole13.MOTOQUEIRO) {
          const courierId = Number(actorUserId || 0);
          await CourierAccessService_default.assertActiveCourier(courierId, Number(restaurantId));
          const courierOrder = await OrderRepository_default.findCourierOrderById(
            orderId,
            Number(restaurantId),
            courierId
          );
          if (!courierOrder) {
            throw new Error("Pedido n\xE3o encontrado nas suas entregas.");
          }
          return courierOrder;
        }
        if (normalizedRole === UserRole13.FUNCIONARIO) {
          if (normalizedSubRole === FuncionarioSubRole3.GARCOM) {
            const readyTableOrder = await OrderRepository_default.findReadyTableOrderById(
              orderId,
              restaurantId
            );
            if (!readyTableOrder) {
              throw new Error("Pedido n\xE3o encontrado na fila do gar\xE7om.");
            }
            return readyTableOrder;
          }
          if (normalizedSubRole === FuncionarioSubRole3.COZINHA) {
            const operationalOrder = await OrderRepository_default.findOperationalById(orderId, restaurantId);
            if (!operationalOrder) {
              throw new Error("Pedido n\xE3o encontrado na fila da cozinha.");
            }
            return operationalOrder;
          }
          throw new Error("Funcion\xE1rio sem perfil operacional v\xE1lido.");
        }
        const order = await OrderRepository_default.findById(orderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado!");
        }
        return order;
      }
    };
    GetOrderByIdService_default = new GetOrderByIdService();
  }
});

// src/modules/orders/controllers/GetOrderByIdController.ts
var GetOrderByIdController, GetOrderByIdController_default;
var init_GetOrderByIdController = __esm({
  "src/modules/orders/controllers/GetOrderByIdController.ts"() {
    init_GetOrderByIdService();
    GetOrderByIdController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const restaurantId = req.user.restaurantId;
          const order = await GetOrderByIdService_default.execute(
            id,
            restaurantId,
            req.user.role,
            req.user.subRole,
            req.user.id
          );
          return res.json(order);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao buscar pedido"
          });
        }
      }
    };
    GetOrderByIdController_default = new GetOrderByIdController();
  }
});

// src/modules/orders/services/ListMyOrdersService.ts
var ListMyOrdersService, ListMyOrdersService_default;
var init_ListMyOrdersService = __esm({
  "src/modules/orders/services/ListMyOrdersService.ts"() {
    init_OrderRepository();
    ListMyOrdersService = class {
      async execute(userId, restaurantId) {
        return OrderRepository_default.findByUserId(userId, restaurantId);
      }
    };
    ListMyOrdersService_default = new ListMyOrdersService();
  }
});

// src/modules/orders/controllers/ListMyOrdersController.ts
var ListMyOrdersController, ListMyOrdersController_default;
var init_ListMyOrdersController = __esm({
  "src/modules/orders/controllers/ListMyOrdersController.ts"() {
    init_ListMyOrdersService();
    ListMyOrdersController = class {
      async handle(req, res) {
        try {
          const { id: userId, restaurantId } = req.user;
          const orders = await ListMyOrdersService_default.execute(userId, restaurantId);
          return res.json(orders);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar pedidos do usuario"
          });
        }
      }
    };
    ListMyOrdersController_default = new ListMyOrdersController();
  }
});

// src/modules/payments/providers/mercadoPagoClient.ts
import { MercadoPagoConfig as MercadoPagoConfig2, Payment as Payment2, PaymentRefund, Preference } from "mercadopago";
async function getAccessToken(restaurantId) {
  const normalizedRestaurantId = Number(restaurantId || 0);
  const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
  const settings = Number.isInteger(normalizedRestaurantId) && normalizedRestaurantId > 0 ? await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId) : null;
  const settingsToken = String(settings?.mercadoPagoAccessToken || "").trim();
  const globalToken = String(process.env.MP_ACCESS_TOKEN || "").trim();
  const token = settingsToken || (allowGlobalFallback ? globalToken : "");
  if (!token) {
    throw new Error(
      "Pagamento Mercado Pago indisponivel. Configure access token do Mercado Pago nas configuracoes do restaurante."
    );
  }
  return token;
}
async function getMercadoPagoClient(restaurantId) {
  return new MercadoPagoConfig2({
    accessToken: await getAccessToken(restaurantId)
  });
}
async function getMercadoPagoPaymentApi(restaurantId) {
  return new Payment2(await getMercadoPagoClient(restaurantId));
}
async function getMercadoPagoPaymentRefundApi(restaurantId) {
  return new PaymentRefund(await getMercadoPagoClient(restaurantId));
}
async function getMercadoPagoPreferenceApi(restaurantId) {
  return new Preference(await getMercadoPagoClient(restaurantId));
}
var init_mercadoPagoClient = __esm({
  "src/modules/payments/providers/mercadoPagoClient.ts"() {
    init_RestaurantSettingsRepository();
  }
});

// src/modules/orders/services/RefundOrderPaymentService.ts
import Stripe from "stripe";
import { PaymentMethod as PaymentMethod5 } from "@prisma/client";
var AutomaticRefundError, RefundOrderPaymentService, RefundOrderPaymentService_default;
var init_RefundOrderPaymentService = __esm({
  "src/modules/orders/services/RefundOrderPaymentService.ts"() {
    init_mercadoPagoClient();
    init_RestaurantSettingsRepository();
    AutomaticRefundError = class extends Error {
      constructor(message, code = "PROVIDER_FAILURE") {
        super(message);
        this.code = code;
        this.name = "AutomaticRefundError";
      }
      code;
    };
    RefundOrderPaymentService = class {
      // Seam pequeno e substituível nos testes; em produção sempre cria o SDK oficial.
      createStripeClient(secretKey) {
        return new Stripe(secretKey);
      }
      resolveAsaasApiBaseUrl() {
        return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
      }
      async getAsaasAccessToken(restaurantId) {
        if (!restaurantId || !Number.isInteger(restaurantId) || restaurantId <= 0) {
          throw new AutomaticRefundError(
            "N\xE3o foi poss\xEDvel identificar o restaurante para realizar o estorno. O pedido n\xE3o foi cancelado.",
            "MISSING_CREDENTIALS"
          );
        }
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        const restaurantToken = String(settings?.asaasAccessToken || "").trim();
        const globalToken = String(process.env.ASAAS_API_KEY || "").trim();
        const accessToken = restaurantToken || (allowGlobalFallback ? globalToken : "");
        if (!accessToken) {
          throw new AutomaticRefundError(
            "O estorno autom\xE1tico est\xE1 indispon\xEDvel porque a credencial Asaas n\xE3o est\xE1 configurada para este restaurante. O pedido n\xE3o foi cancelado.",
            "MISSING_CREDENTIALS"
          );
        }
        return accessToken;
      }
      extractAsaasError(payload) {
        const firstError = Array.isArray(payload?.errors) ? payload.errors[0] : void 0;
        return {
          code: String(firstError?.code || "").trim(),
          description: String(firstError?.description || "").trim()
        };
      }
      async executeAsaasRefund(paymentId, order, options) {
        const normalizedPaymentId = String(paymentId || "").trim();
        const restaurantId = Number(order.restaurantId || 0);
        if (!normalizedPaymentId) {
          throw new AutomaticRefundError(
            "Este pagamento Asaas n\xE3o possui uma refer\xEAncia v\xE1lida para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        const accessToken = await this.getAsaasAccessToken(restaurantId);
        const amount = this.parseAmount(order.total);
        const paymentUrl = `${this.resolveAsaasApiBaseUrl()}/v3/payments/${encodeURIComponent(normalizedPaymentId)}`;
        if (options.verifyExistingRefund) {
          const currentPaymentResponse = await fetch(paymentUrl, {
            headers: {
              Accept: "application/json",
              access_token: accessToken
            }
          });
          const currentPayment = await currentPaymentResponse.json().catch(() => ({}));
          if (currentPaymentResponse.ok && String(currentPayment.status || "").toUpperCase() === "REFUNDED") {
            return {
              provider: "ASAAS",
              externalId: String(currentPayment.id || normalizedPaymentId).trim()
            };
          }
        }
        const response = await fetch(`${paymentUrl}/refund`, {
          method: "POST",
          headers: {
            Accept: "application/json",
            "Content-Type": "application/json",
            access_token: accessToken
          },
          body: JSON.stringify({
            ...amount ? { value: amount } : {},
            description: `Estorno do pedido #${String(order.id)}`
          })
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          const providerError = this.extractAsaasError(payload);
          console.error("[ASAAS_REFUND_ERROR]", {
            orderId: order.id,
            restaurantId,
            paymentId: normalizedPaymentId,
            status: response.status,
            code: providerError.code || void 0,
            description: providerError.description || void 0
          });
          throw new AutomaticRefundError(
            "O Asaas n\xE3o confirmou o estorno. O pedido n\xE3o foi cancelado e pode ser tentado novamente.",
            "PROVIDER_FAILURE"
          );
        }
        return {
          provider: "ASAAS",
          externalId: String(payload.id || normalizedPaymentId).trim() || normalizedPaymentId
        };
      }
      resolvePagBankEnvironment() {
        return "production";
      }
      resolvePagBankApiBaseUrl(environment2) {
        void environment2;
        return "https://ws.pagseguro.uol.com.br";
      }
      extractXmlTagValue(xml, tag) {
        const regex = new RegExp(`<${tag}>([^<]+)</${tag}>`, "i");
        const match = regex.exec(String(xml || ""));
        return String(match?.[1] || "").trim();
      }
      parsePagBankErrorDetails(xml) {
        const normalizedXml = String(xml || "").trim();
        if (!normalizedXml) {
          return {
            code: "",
            message: ""
          };
        }
        const errorBlockMatch = /<error>([\s\S]*?)<\/error>/i.exec(normalizedXml);
        const errorScope = errorBlockMatch?.[1] || normalizedXml;
        const code = this.extractXmlTagValue(errorScope, "code") || this.extractXmlTagValue(normalizedXml, "code");
        const message = this.extractXmlTagValue(errorScope, "message") || this.extractXmlTagValue(normalizedXml, "message") || this.extractXmlTagValue(errorScope, "error") || this.extractXmlTagValue(normalizedXml, "error");
        return {
          code,
          message
        };
      }
      async getPagBankCredentials(restaurantId) {
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const settings = restaurantId ? await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId) : null;
        const email = String(
          settings?.pagbankEmail || (allowGlobalFallback ? process.env.PAGBANK_EMAIL || process.env.PAGSEGURO_EMAIL : "") || ""
        ).trim();
        const token = String(
          settings?.pagbankToken || (allowGlobalFallback ? process.env.PAGBANK_TOKEN || process.env.PAGSEGURO_TOKEN : "") || ""
        ).trim();
        if (!email || !token) {
          throw new AutomaticRefundError(
            "O estorno autom\xE1tico est\xE1 indispon\xEDvel porque as credenciais PagBank n\xE3o est\xE3o configuradas para este restaurante. O pedido n\xE3o foi cancelado.",
            "MISSING_CREDENTIALS"
          );
        }
        return {
          email,
          token,
          environment: this.resolvePagBankEnvironment()
        };
      }
      parseAmount(value) {
        const amount = Number(value || 0);
        return Number.isFinite(amount) && amount > 0 ? Number(amount.toFixed(2)) : void 0;
      }
      async getMercadoPagoAccessTokenByRestaurant(restaurantId) {
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const normalizedRestaurantId = Number(restaurantId || 0);
        const settings = Number.isInteger(normalizedRestaurantId) && normalizedRestaurantId > 0 ? await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId) : null;
        const token = String(
          settings?.mercadoPagoAccessToken || (allowGlobalFallback ? process.env.MP_ACCESS_TOKEN : "") || ""
        ).trim();
        if (!token) {
          throw new AutomaticRefundError(
            "O estorno autom\xE1tico est\xE1 indispon\xEDvel porque a credencial Mercado Pago n\xE3o est\xE1 configurada para este restaurante. O pedido n\xE3o foi cancelado.",
            "MISSING_CREDENTIALS"
          );
        }
        return token;
      }
      async executeMercadoPagoRefund(paymentId, restaurantId, idempotencyKey) {
        const normalizedPaymentId = String(paymentId || "").trim();
        if (!normalizedPaymentId) {
          throw new AutomaticRefundError(
            "Este pagamento Mercado Pago n\xE3o possui uma refer\xEAncia v\xE1lida para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        const refundApi = await getMercadoPagoPaymentRefundApi(Number(restaurantId || 0) || void 0);
        const response = await refundApi.total({
          payment_id: normalizedPaymentId,
          ...idempotencyKey ? {
            requestOptions: {
              idempotencyKey
            }
          } : {}
        });
        const refund = typeof response === "object" && response !== null ? response.body ?? response : {};
        return {
          provider: "MERCADO_PAGO",
          externalId: String(refund.id || normalizedPaymentId).trim()
        };
      }
      resolvePagBankOrderApiBaseUrl() {
        return String(process.env.PAGBANK_API_BASE_URL || "https://api.pagseguro.com").trim().replace(/\/+$/, "");
      }
      async getPagBankBearerToken(restaurantId) {
        const normalizedRestaurantId = Number(restaurantId || 0);
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const settings = Number.isInteger(normalizedRestaurantId) && normalizedRestaurantId > 0 ? await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId) : null;
        const token = String(
          settings?.pagbankToken || (allowGlobalFallback ? process.env.PAGBANK_TOKEN : "") || ""
        ).trim();
        if (!token) {
          throw new AutomaticRefundError(
            "O estorno autom\xE1tico est\xE1 indispon\xEDvel porque a credencial PagBank n\xE3o est\xE1 configurada para este restaurante. O pedido n\xE3o foi cancelado.",
            "MISSING_CREDENTIALS"
          );
        }
        return token;
      }
      async refundPagBankPixOrder(pagBankOrderId, order, idempotencyKey) {
        const normalizedOrderId = String(pagBankOrderId || "").trim();
        const restaurantId = Number(order.restaurantId || 0);
        if (!normalizedOrderId) {
          throw new AutomaticRefundError(
            "Este Pix PagBank n\xE3o possui uma refer\xEAncia v\xE1lida para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        const token = await this.getPagBankBearerToken(restaurantId);
        const apiBaseUrl = this.resolvePagBankOrderApiBaseUrl();
        const orderResponse = await fetch(
          `${apiBaseUrl}/orders/${encodeURIComponent(normalizedOrderId)}`,
          {
            headers: {
              Authorization: `Bearer ${token}`,
              Accept: "application/json"
            }
          }
        );
        const orderPayload = await orderResponse.json().catch(() => ({}));
        if (!orderResponse.ok) {
          console.error("[PAGBANK_PIX_REFUND_LOOKUP_ERROR]", {
            orderId: order.id,
            restaurantId,
            pagBankOrderId: normalizedOrderId,
            status: orderResponse.status
          });
          throw new AutomaticRefundError(
            "O PagBank n\xE3o permitiu localizar a cobran\xE7a Pix para estorno. O pedido n\xE3o foi cancelado.",
            "PROVIDER_FAILURE"
          );
        }
        const expectedAmountInCents = Math.round(Number(order.total || 0) * 100);
        const charges = Array.isArray(orderPayload.charges) ? orderPayload.charges : [];
        const paidCharge = charges.find(
          (charge) => String(charge.status || "").toUpperCase() === "PAID" && charge.id
        );
        const alreadyRefundedCharge = charges.find((charge) => {
          const status = String(charge.status || "").toUpperCase();
          const refundedAmount = Number(charge.summary?.refunded || 0);
          return status === "CANCELED" && Boolean(charge.id) && expectedAmountInCents > 0 && refundedAmount >= expectedAmountInCents;
        });
        if (alreadyRefundedCharge?.id) {
          return {
            provider: "PAGBANK",
            externalId: String(alreadyRefundedCharge.id)
          };
        }
        if (!paidCharge?.id || expectedAmountInCents <= 0) {
          throw new AutomaticRefundError(
            "O PagBank n\xE3o retornou uma cobran\xE7a Pix paga e eleg\xEDvel para estorno. O pedido n\xE3o foi cancelado.",
            "NOT_SUPPORTED"
          );
        }
        const cancelResponse = await fetch(
          `${apiBaseUrl}/charges/${encodeURIComponent(String(paidCharge.id))}/cancel`,
          {
            method: "POST",
            headers: {
              Authorization: `Bearer ${token}`,
              Accept: "application/json",
              "Content-Type": "application/json",
              ...idempotencyKey ? { "x-idempotency-key": idempotencyKey } : {}
            },
            body: JSON.stringify({
              amount: {
                value: expectedAmountInCents
              }
            })
          }
        );
        const cancelPayload = await cancelResponse.json().catch(() => ({}));
        if (!cancelResponse.ok) {
          console.error("[PAGBANK_PIX_REFUND_ERROR]", {
            orderId: order.id,
            restaurantId,
            chargeId: paidCharge.id,
            status: cancelResponse.status,
            providerCode: cancelPayload.error_messages?.[0]?.code || void 0
          });
          throw new AutomaticRefundError(
            "O PagBank n\xE3o confirmou o estorno do Pix. O pedido n\xE3o foi cancelado e pode ser tentado novamente.",
            "PROVIDER_FAILURE"
          );
        }
        return {
          provider: "PAGBANK",
          externalId: String(cancelPayload.id || paidCharge.id).trim()
        };
      }
      async refundPix(order, options) {
        const paymentId = String(order.pixPaymentId || "").trim();
        const normalizedPaymentId = paymentId.toLowerCase();
        if (!paymentId) {
          throw new AutomaticRefundError(
            "Este pedido Pix n\xE3o possui uma refer\xEAncia de pagamento para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        if (normalizedPaymentId.startsWith("manual:")) {
          throw new AutomaticRefundError(
            "Este Pix foi confirmado manualmente e exige devolu\xE7\xE3o manual. O pedido n\xE3o foi cancelado.",
            "NOT_SUPPORTED"
          );
        }
        if (normalizedPaymentId.startsWith("asaas:")) {
          const asaasPaymentId = paymentId.slice("asaas:".length).trim();
          return this.executeAsaasRefund(asaasPaymentId, order, options);
        }
        if (normalizedPaymentId.startsWith("pagbank:")) {
          const pagBankOrderId = paymentId.slice("pagbank:".length).trim();
          return this.refundPagBankPixOrder(pagBankOrderId, order, options.idempotencyKey);
        }
        return this.executeMercadoPagoRefund(paymentId, order.restaurantId, options.idempotencyKey);
      }
      async refundStripeCard(order, options) {
        const rawSessionId = String(order.cardCheckoutSessionId || "").trim();
        const stripeSessionId = rawSessionId;
        if (!stripeSessionId || !stripeSessionId.startsWith("cs_")) {
          throw new AutomaticRefundError(
            "Este pagamento com cart\xE3o n\xE3o possui uma sess\xE3o Stripe v\xE1lida para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        const restaurantId = Number(order.restaurantId || 0) || void 0;
        const settings = restaurantId ? await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId) : null;
        const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
        const secretKey = String(
          settings?.stripeSecretKey || (allowGlobalFallback ? process.env.STRIPE_SECRET_KEY : "") || ""
        ).trim();
        if (!secretKey) {
          throw new AutomaticRefundError(
            "O estorno autom\xE1tico est\xE1 indispon\xEDvel porque a chave Stripe n\xE3o est\xE1 configurada para este restaurante. O pedido n\xE3o foi cancelado.",
            "MISSING_CREDENTIALS"
          );
        }
        const stripe = this.createStripeClient(secretKey);
        const session = await stripe.checkout.sessions.retrieve(stripeSessionId, {
          expand: ["payment_intent"]
        });
        const paymentIntentId = typeof session.payment_intent === "string" ? session.payment_intent : session.payment_intent?.id;
        if (!paymentIntentId) {
          throw new AutomaticRefundError(
            "A Stripe n\xE3o retornou a refer\xEAncia financeira necess\xE1ria para o estorno. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        const refund = await stripe.refunds.create(
          {
            payment_intent: paymentIntentId
          },
          options.idempotencyKey ? { idempotencyKey: options.idempotencyKey } : void 0
        );
        return {
          provider: "STRIPE",
          externalId: String(refund.id || paymentIntentId).trim()
        };
      }
      async refundPagBankByTransaction(transactionCode, order, options) {
        const normalizedTransactionCode = String(transactionCode || "").trim();
        if (!normalizedTransactionCode) {
          throw new AutomaticRefundError(
            "Este pagamento PagBank n\xE3o possui uma transa\xE7\xE3o v\xE1lida para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        const restaurantId = Number(order.restaurantId || 0) || void 0;
        const { email, token, environment: environment2 } = await this.getPagBankCredentials(restaurantId);
        const amount = this.parseAmount(order.total);
        if (options.verifyExistingRefund) {
          const queryUrl = `${this.resolvePagBankApiBaseUrl(environment2)}/v3/transactions/${encodeURIComponent(normalizedTransactionCode)}?email=${encodeURIComponent(email)}&token=${encodeURIComponent(token)}`;
          const queryResponse = await fetch(queryUrl, { method: "GET" });
          const queryText = await queryResponse.text().catch(() => "");
          const currentStatus = this.extractXmlTagValue(queryText, "status");
          if (queryResponse.ok && currentStatus === "6") {
            return {
              provider: "PAGBANK",
              externalId: normalizedTransactionCode
            };
          }
        }
        const params = new URLSearchParams();
        params.set("transactionCode", normalizedTransactionCode);
        if (amount) {
          params.set("refundValue", amount.toFixed(2));
        }
        const credentials = new URLSearchParams({ email, token });
        const url = `${this.resolvePagBankApiBaseUrl(environment2)}/v2/transactions/refunds?${credentials.toString()}`;
        const response = await fetch(url, {
          method: "POST",
          headers: {
            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8"
          },
          body: params.toString()
        });
        const responseText = await response.text().catch(() => "");
        const parsedError = this.parsePagBankErrorDetails(responseText);
        if (!response.ok) {
          const fallbackSnippet = String(responseText || "").replace(/\s+/g, " ").trim().slice(0, 220);
          const providerMessage = parsedError.message || (fallbackSnippet ? `Falha ao estornar no PagBank. Resposta: ${fallbackSnippet}` : "Falha ao estornar no PagBank.");
          const detailsSuffix = parsedError.code ? ` (code ${parsedError.code})` : "";
          console.error("[PAGBANK_CARD_REFUND_ERROR]", {
            orderId: order.id,
            restaurantId,
            transactionCode: normalizedTransactionCode,
            status: response.status,
            code: parsedError.code || void 0,
            providerMessage,
            detailsSuffix
          });
          throw new AutomaticRefundError(
            "O PagBank n\xE3o confirmou o estorno do cart\xE3o. O pedido n\xE3o foi cancelado e pode ser tentado novamente.",
            "PROVIDER_FAILURE"
          );
        }
        if (parsedError.code && parsedError.message) {
          console.error("[PAGBANK_CARD_REFUND_XML_ERROR]", {
            orderId: order.id,
            restaurantId,
            transactionCode: normalizedTransactionCode,
            status: response.status,
            code: parsedError.code,
            providerMessage: parsedError.message
          });
          throw new AutomaticRefundError(
            "O PagBank n\xE3o confirmou o estorno do cart\xE3o. O pedido n\xE3o foi cancelado e pode ser tentado novamente.",
            "PROVIDER_FAILURE"
          );
        }
        return {
          provider: "PAGBANK",
          externalId: normalizedTransactionCode
        };
      }
      async refundCard(order, options) {
        const checkoutSessionId = String(order.cardCheckoutSessionId || "").trim();
        const normalizedCheckoutSessionId = checkoutSessionId.toLowerCase();
        if (!checkoutSessionId) {
          throw new AutomaticRefundError(
            "Este pedido com cart\xE3o n\xE3o possui uma refer\xEAncia de checkout para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        if (normalizedCheckoutSessionId.startsWith("asaas_pay:")) {
          const asaasPaymentId = checkoutSessionId.slice("asaas_pay:".length).trim();
          return this.executeAsaasRefund(asaasPaymentId, order, options);
        }
        if (normalizedCheckoutSessionId.startsWith("mp_pay:")) {
          const paymentId = checkoutSessionId.replace(/^mp_pay:/i, "").trim();
          if (!paymentId) {
            throw new AutomaticRefundError(
              "Este pagamento Mercado Pago n\xE3o possui uma refer\xEAncia v\xE1lida para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
              "MISSING_REFERENCE"
            );
          }
          return this.executeMercadoPagoRefund(paymentId, order.restaurantId, options.idempotencyKey);
        }
        if (normalizedCheckoutSessionId.startsWith("mp_pref:")) {
          const preferenceId = checkoutSessionId.replace(/^mp_pref:/i, "").trim();
          const orderId = Number(order.id || 0);
          const restaurantId = Number(order.restaurantId || 0);
          if (!preferenceId || !Number.isInteger(orderId) || orderId <= 0) {
            throw new AutomaticRefundError(
              "Este pagamento Mercado Pago n\xE3o possui dados suficientes para localizar a cobran\xE7a. O pedido n\xE3o foi cancelado.",
              "MISSING_REFERENCE"
            );
          }
          const externalReference = Number.isInteger(restaurantId) && restaurantId > 0 ? `ordercard:${orderId}:${restaurantId}` : `ordercard:${orderId}`;
          const searchUrl = new URL("https://api.mercadopago.com/v1/payments/search");
          searchUrl.searchParams.set("external_reference", externalReference);
          searchUrl.searchParams.set("sort", "date_created");
          searchUrl.searchParams.set("criteria", "desc");
          searchUrl.searchParams.set("limit", "1");
          const response = await fetch(searchUrl.toString(), {
            method: "GET",
            headers: {
              Authorization: `Bearer ${await this.getMercadoPagoAccessTokenByRestaurant(order.restaurantId)}`,
              "Content-Type": "application/json"
            }
          });
          const payload = await response.json().catch(() => ({}));
          if (!response.ok) {
            throw new AutomaticRefundError(
              "O Mercado Pago n\xE3o permitiu localizar o pagamento para estorno. O pedido n\xE3o foi cancelado.",
              "PROVIDER_FAILURE"
            );
          }
          const resolvedPaymentId = String(payload?.results?.[0]?.id || "").trim();
          if (!resolvedPaymentId) {
            throw new AutomaticRefundError(
              "N\xE3o foi poss\xEDvel localizar o pagamento com cart\xE3o no Mercado Pago. O pedido n\xE3o foi cancelado.",
              "MISSING_REFERENCE"
            );
          }
          return this.executeMercadoPagoRefund(
            resolvedPaymentId,
            order.restaurantId,
            options.idempotencyKey
          );
        }
        if (normalizedCheckoutSessionId.startsWith("pagbank_chk:")) {
          throw new AutomaticRefundError(
            "O pagamento PagBank ainda n\xE3o possui o c\xF3digo da transa\xE7\xE3o necess\xE1rio para estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "MISSING_REFERENCE"
          );
        }
        if (normalizedCheckoutSessionId.startsWith("pagbank_tx:")) {
          const transactionCode = checkoutSessionId.replace(/^pagbank_tx:/i, "").trim();
          return this.refundPagBankByTransaction(transactionCode, order, options);
        }
        if (normalizedCheckoutSessionId.startsWith("pagbank:")) {
          throw new AutomaticRefundError(
            "Este identificador PagBank n\xE3o oferece estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
            "NOT_SUPPORTED"
          );
        }
        return this.refundStripeCard(order, options);
      }
      async execute(order, options = {}) {
        const paymentMethod = String(order.paymentMethod || "").toUpperCase();
        if (order.paid !== true) {
          throw new AutomaticRefundError(
            "Este pedido n\xE3o possui pagamento confirmado para estorno autom\xE1tico.",
            "NOT_SUPPORTED"
          );
        }
        try {
          if (paymentMethod === PaymentMethod5.PIX) {
            return await this.refundPix(order, options);
          }
          if (paymentMethod === PaymentMethod5.CARTAO) {
            return await this.refundCard(order, options);
          }
        } catch (error2) {
          if (error2 instanceof AutomaticRefundError) {
            throw error2;
          }
          console.error("[ORDER_REFUND_PROVIDER_UNEXPECTED_ERROR]", {
            orderId: order.id,
            restaurantId: order.restaurantId,
            paymentMethod,
            error: error2 instanceof Error ? error2.message : String(error2)
          });
          throw new AutomaticRefundError(
            "O provedor de pagamento n\xE3o confirmou o estorno. O pedido n\xE3o foi cancelado e pode ser tentado novamente.",
            "PROVIDER_FAILURE"
          );
        }
        throw new AutomaticRefundError(
          "Este m\xE9todo de pagamento n\xE3o oferece estorno autom\xE1tico. O pedido n\xE3o foi cancelado.",
          "NOT_SUPPORTED"
        );
      }
    };
    RefundOrderPaymentService_default = new RefundOrderPaymentService();
  }
});

// src/modules/orders/services/CancelOrderWorkflowService.ts
import {
  OrderRefundStatus as OrderRefundStatus2,
  OrderStatus as OrderStatus12,
  PaymentMethod as PaymentMethod6,
  TableBillItemFinancialStatus as TableBillItemFinancialStatus3,
  TableOrderFinancialStatus as TableOrderFinancialStatus4
} from "@prisma/client";
function getPublicOrderCancellationErrorMessage(error2, fallback) {
  if (error2 instanceof OrderCancellationError || error2 instanceof AutomaticRefundError) {
    return error2.message;
  }
  return fallback;
}
function isOrderPaidOnDelivery(order) {
  return order.payOnDelivery === true || String(order.observation || "").toUpperCase().includes(PAY_ON_DELIVERY_MARKER);
}
function requiresAutomaticOrderRefund(order) {
  return order.paid === true && !isOrderPaidOnDelivery(order) && DIGITAL_PAYMENT_METHODS.has(String(order.paymentMethod || "").toUpperCase());
}
var DIGITAL_PAYMENT_METHODS, PAY_ON_DELIVERY_MARKER, OrderCancellationError, CancelOrderWorkflowService, CancelOrderWorkflowService_default;
var init_CancelOrderWorkflowService = __esm({
  "src/modules/orders/services/CancelOrderWorkflowService.ts"() {
    init_prisma();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    init_RefundOrderPaymentService();
    init_restoreOrderItemsStock();
    DIGITAL_PAYMENT_METHODS = /* @__PURE__ */ new Set([PaymentMethod6.PIX, PaymentMethod6.CARTAO]);
    PAY_ON_DELIVERY_MARKER = "PAY_ON_DELIVERY:";
    OrderCancellationError = class extends Error {
      constructor(message) {
        super(message);
        this.name = "OrderCancellationError";
      }
    };
    CancelOrderWorkflowService = class {
      buildIdempotencyKey(order) {
        return `order-refund-${order.restaurantId}-${order.id}`;
      }
      async cancelWithoutRefund(order) {
        if (order.status === OrderStatus12.CANCELADO) {
          return { order, refunded: false };
        }
        try {
          const cancelledOrder = await prisma_default.$transaction(async (tx) => {
            const canceledAt = /* @__PURE__ */ new Date();
            const updated = await OrderRepository_default.updateStatusIfCurrent(
              order.id,
              OrderStatus12.CANCELADO,
              order.restaurantId,
              { status: order.status, paid: order.paid },
              tx
            );
            await restoreOrderItemsStock(tx, order);
            await releaseCouponRedemptionForOrder(order.id, order.restaurantId, tx);
            if (order.tableSessionId) {
              await tx.tableBillItem.updateMany({
                where: {
                  orderId: order.id,
                  restaurantId: order.restaurantId,
                  canceledAt: null
                },
                data: {
                  canceledAt,
                  cancellationReason: "Pedido cancelado"
                }
              });
            }
            return updated;
          });
          return { order: cancelledOrder, refunded: false };
        } catch (error2) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          if (latest?.status === OrderStatus12.CANCELADO) {
            return { order: latest, refunded: false };
          }
          throw error2;
        }
      }
      async finalizeSucceededRefund(order) {
        if (order.status === OrderStatus12.CANCELADO) {
          return { order, refunded: true };
        }
        if (order.status === OrderStatus12.ENTREGUE) {
          throw new OrderCancellationError(
            "O pagamento j\xE1 foi estornado, mas o pedido entregue exige concilia\xE7\xE3o administrativa. Nenhuma movimenta\xE7\xE3o de estoque foi realizada."
          );
        }
        try {
          const cancelledOrder = await prisma_default.$transaction(async (tx) => {
            const reconciledAt = order.refundedAt || /* @__PURE__ */ new Date();
            const result = await tx.order.updateMany({
              where: {
                id: order.id,
                restaurantId: order.restaurantId,
                refundStatus: OrderRefundStatus2.SUCCEEDED,
                status: { notIn: [OrderStatus12.CANCELADO, OrderStatus12.ENTREGUE] }
              },
              data: {
                status: OrderStatus12.CANCELADO,
                ...order.tableSessionId ? { tableFinancialStatus: TableOrderFinancialStatus4.REFUNDED } : {}
              }
            });
            if (result.count !== 1) {
              throw new OrderCancellationError("O cancelamento do pedido precisa ser conciliado.");
            }
            await restoreOrderItemsStock(tx, order);
            await releaseCouponRedemptionForOrder(order.id, order.restaurantId, tx);
            if (order.tableSessionId) {
              await tx.tableBillItem.updateMany({
                where: {
                  orderId: order.id,
                  restaurantId: order.restaurantId,
                  canceledAt: null
                },
                data: {
                  financialStatus: TableBillItemFinancialStatus3.REFUNDED,
                  refundedAt: reconciledAt,
                  canceledAt: reconciledAt,
                  cancellationReason: "Pedido cancelado ap\xF3s estorno"
                }
              });
            }
            const updated = await OrderRepository_default.findById(order.id, order.restaurantId, tx);
            if (!updated) {
              throw new OrderCancellationError("Pedido n\xE3o encontrado ap\xF3s o cancelamento.");
            }
            return updated;
          });
          return { order: cancelledOrder, refunded: true };
        } catch (error2) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          if (latest?.status === OrderStatus12.CANCELADO && latest.refundStatus === OrderRefundStatus2.SUCCEEDED) {
            return { order: latest, refunded: true };
          }
          throw error2;
        }
      }
      async persistRefundSucceeded(order, idempotencyKey, receipt) {
        const result = await prisma_default.order.updateMany({
          where: {
            id: order.id,
            restaurantId: order.restaurantId,
            paid: true,
            refundStatus: OrderRefundStatus2.PROCESSING,
            refundIdempotencyKey: idempotencyKey
          },
          data: {
            refundStatus: OrderRefundStatus2.SUCCEEDED,
            refundedAt: /* @__PURE__ */ new Date(),
            refundFailureReason: null,
            refundProvider: receipt.provider,
            refundExternalId: receipt.externalId
          }
        });
        if (result.count === 1) {
          return;
        }
        const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
        if (latest?.refundStatus === OrderRefundStatus2.SUCCEEDED) {
          return;
        }
        console.error("[ORDER_REFUND_RECONCILIATION_REQUIRED]", {
          orderId: order.id,
          restaurantId: order.restaurantId,
          provider: receipt.provider,
          externalId: receipt.externalId
        });
        throw new OrderCancellationError(
          "O provedor confirmou o estorno, mas a atualiza\xE7\xE3o do pedido ficou em concilia\xE7\xE3o. N\xE3o repita o estorno; contate o suporte."
        );
      }
      async markRefundFailed(order, idempotencyKey, error2) {
        const result = await prisma_default.order.updateMany({
          where: {
            id: order.id,
            restaurantId: order.restaurantId,
            refundStatus: OrderRefundStatus2.PROCESSING,
            refundIdempotencyKey: idempotencyKey
          },
          data: {
            refundStatus: OrderRefundStatus2.FAILED,
            refundFailureReason: error2.message,
            refundProvider: null,
            refundExternalId: null
          }
        });
        if (result.count !== 1) {
          console.error("[ORDER_REFUND_FAILURE_STATE_NOT_PERSISTED]", {
            orderId: order.id,
            restaurantId: order.restaurantId
          });
        }
      }
      async refundAndCancel(order) {
        if (order.refundStatus === OrderRefundStatus2.SUCCEEDED) {
          return this.finalizeSucceededRefund(order);
        }
        if (order.refundStatus === OrderRefundStatus2.PROCESSING) {
          throw new OrderCancellationError(
            "O estorno deste pedido j\xE1 est\xE1 em processamento ou aguardando concilia\xE7\xE3o. Aguarde a atualiza\xE7\xE3o antes de tentar novamente."
          );
        }
        const previousRefundStatus = order.refundStatus;
        const idempotencyKey = String(order.refundIdempotencyKey || "").trim() || this.buildIdempotencyKey(order);
        const claim = await prisma_default.order.updateMany({
          where: {
            id: order.id,
            restaurantId: order.restaurantId,
            status: order.status,
            paid: true,
            refundStatus: previousRefundStatus
          },
          data: {
            refundStatus: OrderRefundStatus2.PROCESSING,
            refundRequestedAt: /* @__PURE__ */ new Date(),
            refundFailureReason: null,
            refundIdempotencyKey: idempotencyKey,
            refundProvider: null,
            refundExternalId: null
          }
        });
        if (claim.count !== 1) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          if (latest?.refundStatus === OrderRefundStatus2.SUCCEEDED) {
            return this.finalizeSucceededRefund(latest);
          }
          if (latest?.refundStatus === OrderRefundStatus2.PROCESSING) {
            throw new OrderCancellationError(
              "O estorno deste pedido j\xE1 est\xE1 em processamento. Aguarde a atualiza\xE7\xE3o antes de tentar novamente."
            );
          }
          if (latest?.status === OrderStatus12.CANCELADO) {
            throw new OrderCancellationError(
              "O pedido foi cancelado por outro processo, mas o estorno autom\xE1tico n\xE3o est\xE1 confirmado. Contate o suporte."
            );
          }
          throw new OrderCancellationError(
            "O pedido foi atualizado por outro processo. Atualize a tela e tente novamente."
          );
        }
        let receipt;
        try {
          receipt = await RefundOrderPaymentService_default.execute(order, {
            idempotencyKey,
            verifyExistingRefund: previousRefundStatus === OrderRefundStatus2.FAILED
          });
        } catch (error2) {
          const safeError = error2 instanceof AutomaticRefundError ? error2 : new AutomaticRefundError(
            "O provedor de pagamento n\xE3o confirmou o estorno. O pedido n\xE3o foi cancelado e pode ser tentado novamente."
          );
          await this.markRefundFailed(order, idempotencyKey, safeError);
          throw safeError;
        }
        await this.persistRefundSucceeded(order, idempotencyKey, receipt);
        const refundedOrder = await OrderRepository_default.findById(order.id, order.restaurantId);
        if (!refundedOrder) {
          throw new OrderCancellationError(
            "O estorno foi confirmado, mas o pedido precisa de concilia\xE7\xE3o. N\xE3o repita o estorno; contate o suporte."
          );
        }
        return this.finalizeSucceededRefund(refundedOrder);
      }
      async execute(order) {
        if (!requiresAutomaticOrderRefund(order)) {
          return this.cancelWithoutRefund(order);
        }
        return this.refundAndCancel(order);
      }
    };
    CancelOrderWorkflowService_default = new CancelOrderWorkflowService();
  }
});

// src/modules/orders/services/CancelOrderService.ts
import { OrderRefundStatus as OrderRefundStatus3, OrderStatus as OrderStatus13 } from "@prisma/client";
var CancelOrderService, CancelOrderService_default;
var init_CancelOrderService = __esm({
  "src/modules/orders/services/CancelOrderService.ts"() {
    init_OrderRepository();
    init_orderStateMachine();
    init_server();
    init_customerNotifier();
    init_CancelOrderWorkflowService();
    init_waiterOrderRealtime();
    CancelOrderService = class {
      async execute(orderId, userId, restaurantId) {
        const normalizedOrderId = Array.isArray(orderId) ? orderId[0] : orderId;
        const normalizedRestaurantId = Number(restaurantId || 0);
        const order = await OrderRepository_default.findByIdForCustomer(
          normalizedOrderId,
          userId,
          normalizedRestaurantId
        );
        if (!order) {
          throw new OrderCancellationError("Pedido n\xE3o encontrado!");
        }
        if (order.userId !== userId) {
          throw new OrderCancellationError("Sem permiss\xE3o!");
        }
        const orderRestaurantId = order.restaurantId;
        if (order.status === OrderStatus13.CANCELADO) {
          if (!requiresAutomaticOrderRefund(order) || order.refundStatus === OrderRefundStatus3.SUCCEEDED) {
            return order;
          }
          throw new OrderCancellationError(
            "Este pedido j\xE1 est\xE1 cancelado, mas n\xE3o h\xE1 confirma\xE7\xE3o persistida do estorno autom\xE1tico. Contate o restaurante."
          );
        }
        const canCancel = OrderStateMachine.canTransition(order.status, OrderStatus13.CANCELADO);
        if (!canCancel) {
          throw new OrderCancellationError("Pedido n\xE3o pode ser cancelado!");
        }
        const { order: updatedOrder } = await CancelOrderWorkflowService_default.execute(order);
        notifyCustomerOrderStatusChanged({
          restaurantId: orderRestaurantId,
          customerPhone: order?.user?.phone,
          customerName: order?.user?.name,
          restaurantName: order?.restaurant?.name,
          restaurantWhatsapp: order?.restaurant?.whatsapp,
          orderId: updatedOrder?.id,
          status: updatedOrder?.status
        }).catch((error2) => {
          console.error("[CUSTOMER_STATUS_NOTIFICATION_UNHANDLED]", error2?.message || error2);
        });
        io.to(`restaurant:${orderRestaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        emitWaiterTableOrderEvent(io, "order:status-changed", updatedOrder);
        emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
        return updatedOrder;
      }
    };
    CancelOrderService_default = new CancelOrderService();
  }
});

// src/modules/orders/controllers/CancelOrderController.ts
var CancelOrderController, CancelOrderController_default;
var init_CancelOrderController = __esm({
  "src/modules/orders/controllers/CancelOrderController.ts"() {
    init_CancelOrderService();
    init_CancelOrderWorkflowService();
    CancelOrderController = class {
      async handle(req, res) {
        try {
          const { id } = req.params;
          const { id: userId, restaurantId } = req.user;
          const order = await CancelOrderService_default.execute(id, userId, restaurantId);
          return res.json(order);
        } catch (error2) {
          const publicMessage = getPublicOrderCancellationErrorMessage(
            error2,
            "N\xE3o foi poss\xEDvel cancelar o pedido agora. Tente novamente ou contate o restaurante."
          );
          if (publicMessage !== (error2 instanceof Error ? error2.message : "")) {
            console.error("[CANCEL_ORDER_UNEXPECTED_ERROR]", {
              error: error2 instanceof Error ? error2.message : String(error2)
            });
          }
          return res.status(400).json({
            error: publicMessage
          });
        }
      }
    };
    CancelOrderController_default = new CancelOrderController();
  }
});

// src/modules/orders/services/CancelTableParticipantOrderService.ts
import { OrderRefundStatus as OrderRefundStatus4, OrderStatus as OrderStatus14 } from "@prisma/client";
import { z as z8 } from "zod";
var publicOrderIdSchema, CancelTableParticipantOrderService, CancelTableParticipantOrderService_default;
var init_CancelTableParticipantOrderService = __esm({
  "src/modules/orders/services/CancelTableParticipantOrderService.ts"() {
    init_server();
    init_customerNotifier();
    init_OrderRepository();
    init_orderStateMachine();
    init_waiterOrderRealtime();
    init_CancelOrderWorkflowService();
    init_TableAccountSettingsRepository();
    publicOrderIdSchema = z8.string().uuid();
    CancelTableParticipantOrderService = class {
      async execute(input) {
        const parsedPublicOrderId = publicOrderIdSchema.safeParse(input.publicOrderId);
        const tableSessionId = Number(input.tableSessionId);
        const restaurantId = Number(input.restaurantId);
        const participantId = Number(input.participantId);
        if (!parsedPublicOrderId.success || !Number.isSafeInteger(tableSessionId) || tableSessionId <= 0 || !Number.isSafeInteger(restaurantId) || restaurantId <= 0 || !Number.isSafeInteger(participantId) || participantId <= 0) {
          throw new OrderCancellationError("Pedido n\xE3o encontrado!");
        }
        const order = await OrderRepository_default.findByPublicIdForTableParticipant(
          parsedPublicOrderId.data,
          tableSessionId,
          restaurantId,
          participantId
        );
        if (!order) {
          throw new OrderCancellationError("Pedido n\xE3o encontrado!");
        }
        if (order.status === OrderStatus14.CANCELADO) {
          if (!requiresAutomaticOrderRefund(order) || order.refundStatus === OrderRefundStatus4.SUCCEEDED) {
            return order;
          }
          throw new OrderCancellationError(
            "Este pedido j\xE1 est\xE1 cancelado, mas o estorno autom\xE1tico ainda n\xE3o foi confirmado."
          );
        }
        const isPreparing = order.status === OrderStatus14.PREPARANDO;
        if (isPreparing) {
          const settings = await TableAccountSettingsRepository_default.findByRestaurantId(restaurantId);
          if (settings.requireEmployeeApprovalForPreparedItemCancellation) {
            throw new OrderCancellationError(
              "Este pedido j\xE1 est\xE1 em preparo e precisa da autoriza\xE7\xE3o de um funcion\xE1rio para ser cancelado."
            );
          }
        } else if (!OrderStateMachine.canTransition(order.status, OrderStatus14.CANCELADO)) {
          throw new OrderCancellationError("Pedido n\xE3o pode ser cancelado!");
        }
        const { order: updatedOrder } = await CancelOrderWorkflowService_default.execute(order);
        notifyCustomerOrderStatusChanged({
          restaurantId,
          customerPhone: order.user?.phone,
          customerName: order.user?.name || order.participant?.displayName,
          restaurantName: order.restaurant?.name,
          restaurantWhatsapp: order.restaurant?.whatsapp,
          orderId: updatedOrder.id,
          status: updatedOrder.status
        }).catch((error2) => {
          console.error(
            "[TABLE_ORDER_STATUS_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        io.to(`restaurant:${restaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        emitWaiterTableOrderEvent(io, "order:status-changed", updatedOrder);
        emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
        return updatedOrder;
      }
    };
    CancelTableParticipantOrderService_default = new CancelTableParticipantOrderService();
  }
});

// src/modules/orders/controllers/CancelTableParticipantOrderController.ts
var CancelTableParticipantOrderController, CancelTableParticipantOrderController_default;
var init_CancelTableParticipantOrderController = __esm({
  "src/modules/orders/controllers/CancelTableParticipantOrderController.ts"() {
    init_CancelTableParticipantOrderService();
    init_CancelOrderWorkflowService();
    CancelTableParticipantOrderController = class {
      async handle(req, res) {
        try {
          const order = await CancelTableParticipantOrderService_default.execute({
            publicOrderId: req.params.publicOrderId,
            tableSessionId: req.tableSession.id,
            restaurantId: req.tableSession.restaurantId,
            participantId: req.tableParticipant.id
          });
          return res.status(200).json({
            order: {
              publicId: order.publicId,
              status: order.status,
              paid: order.paid,
              refundStatus: order.refundStatus,
              updatedAt: order.updatedAt
            }
          });
        } catch (error2) {
          return res.status(400).json({
            error: getPublicOrderCancellationErrorMessage(
              error2,
              "N\xE3o foi poss\xEDvel cancelar o pedido agora. Tente novamente ou chame o gar\xE7om."
            )
          });
        }
      }
    };
    CancelTableParticipantOrderController_default = new CancelTableParticipantOrderController();
  }
});

// src/modules/orders/services/ConfirmOrderPaymentService.ts
var ConfirmOrderPaymentService, ConfirmOrderPaymentService_default;
var init_ConfirmOrderPaymentService = __esm({
  "src/modules/orders/services/ConfirmOrderPaymentService.ts"() {
    init_server();
    init_prisma();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    ConfirmOrderPaymentService = class {
      async execute(orderId, restaurantId, role) {
        const normalizedOrderId = Array.isArray(orderId) ? orderId[0] : orderId;
        if (String(role || "").toUpperCase() !== "ADMIN") {
          throw new Error("Somente o administrador pode confirmar pagamento diretamente.");
        }
        const order = await OrderRepository_default.findById(normalizedOrderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado!");
        }
        if (order.payOnDelivery !== true || !order.paymentMethod) {
          throw new Error(
            "A confirma\xE7\xE3o manual est\xE1 dispon\xEDvel apenas para pedidos com pagamento na entrega."
          );
        }
        if (order.paid === true) {
          return order;
        }
        const updatedOrder = await prisma_default.$transaction(async (tx) => {
          const confirmedOrder = await OrderRepository_default.confirmPayment(
            normalizedOrderId,
            restaurantId,
            tx
          );
          await markCouponRedemptionUsedForOrder(normalizedOrderId, restaurantId, tx);
          return confirmedOrder;
        });
        io.to(`restaurant:${restaurantId}`).emit("order:payment-confirmed", {
          orderId: updatedOrder.id,
          paid: true,
          paymentMethod: updatedOrder.paymentMethod
        });
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod
          });
          io.to(`user:${updatedOrder.userId}`).emit("payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod
          });
        }
        io.to(`restaurant:${restaurantId}`).emit("new-order", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("new-order", updatedOrder);
        }
        io.to(`restaurant:${restaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        return updatedOrder;
      }
    };
    ConfirmOrderPaymentService_default = new ConfirmOrderPaymentService();
  }
});

// src/modules/orders/controllers/ConfirmOrderPaymentController.ts
var ConfirmOrderPaymentController, ConfirmOrderPaymentController_default;
var init_ConfirmOrderPaymentController = __esm({
  "src/modules/orders/controllers/ConfirmOrderPaymentController.ts"() {
    init_ConfirmOrderPaymentService();
    ConfirmOrderPaymentController = class {
      async handle(req, res) {
        try {
          const { id } = req.params;
          const { restaurantId, role } = req.user;
          const updatedOrder = await ConfirmOrderPaymentService_default.execute(id, restaurantId, role);
          return res.status(200).json(updatedOrder);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao confirmar pagamento do pedido"
          });
        }
      }
    };
    ConfirmOrderPaymentController_default = new ConfirmOrderPaymentController();
  }
});

// src/modules/orders/utils/paymentConfirmationPin.ts
import crypto5 from "crypto";
function getSecret() {
  const secret = String(
    process.env.PAYMENT_PIN_SECRET || process.env.JWT_MFA_SECRET || process.env.JWT_SECRET || "development-payment-pin-secret"
  ).trim();
  if (process.env.NODE_ENV === "production" && secret.length < 32) {
    throw new Error("PAYMENT_PIN_SECRET deve ter pelo menos 32 caracteres em produ\xE7\xE3o.");
  }
  return secret;
}
function hashPaymentConfirmationPin(pin) {
  const digest = crypto5.createHmac("sha256", getSecret()).update(String(pin)).digest("hex");
  return `${HASH_PREFIX}${digest}`;
}
function verifyPaymentConfirmationPin(pin, stored) {
  const normalizedStored = String(stored || "");
  if (!normalizedStored.startsWith(HASH_PREFIX)) {
    const providedBuffer = Buffer.from(String(pin));
    const storedBuffer2 = Buffer.from(normalizedStored);
    return providedBuffer.length === storedBuffer2.length && crypto5.timingSafeEqual(providedBuffer, storedBuffer2);
  }
  const expected = hashPaymentConfirmationPin(pin);
  const expectedBuffer = Buffer.from(expected);
  const storedBuffer = Buffer.from(normalizedStored);
  return expectedBuffer.length === storedBuffer.length && crypto5.timingSafeEqual(expectedBuffer, storedBuffer);
}
var HASH_PREFIX;
var init_paymentConfirmationPin = __esm({
  "src/modules/orders/utils/paymentConfirmationPin.ts"() {
    HASH_PREFIX = "hmac:v1:";
  }
});

// src/modules/orders/services/ConfirmOrderPaymentWithPinService.ts
var ConfirmOrderPaymentWithPinService, ConfirmOrderPaymentWithPinService_default;
var init_ConfirmOrderPaymentWithPinService = __esm({
  "src/modules/orders/services/ConfirmOrderPaymentWithPinService.ts"() {
    init_server();
    init_prisma();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    init_paymentConfirmationPin();
    init_CourierAccessService();
    ConfirmOrderPaymentWithPinService = class {
      async execute(orderId, restaurantId, role, pin, actorUserId) {
        const normalizedOrderId = Array.isArray(orderId) ? orderId[0] : orderId;
        const normalizedRole = String(role || "").toUpperCase();
        const allowedRoles2 = ["MOTOQUEIRO", "ADMIN"];
        if (!allowedRoles2.includes(normalizedRole)) {
          throw new Error(
            "A confirma\xE7\xE3o por PIN \xE9 permitida apenas para admin ou motoqueiro na entrega."
          );
        }
        const order = await OrderRepository_default.findById(normalizedOrderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado!");
        }
        if (String(order.type || "").toUpperCase() !== "DELIVERY") {
          throw new Error("Confirma\xE7\xE3o por PIN dispon\xEDvel apenas para pedidos DELIVERY.");
        }
        if (normalizedRole === "MOTOQUEIRO") {
          const courierId = Number(actorUserId || 0);
          await CourierAccessService_default.assertActiveCourier(courierId, restaurantId);
          if (String(order.status || "").toUpperCase() !== "SAIU_PARA_ENTREGA" || Number(order.assignedCourierId || 0) !== courierId) {
            throw new Error("Esta entrega em andamento n\xE3o est\xE1 atribu\xEDda a voc\xEA.");
          }
        }
        if (order.payOnDelivery !== true || !order.paymentMethod) {
          throw new Error("Confirma\xE7\xE3o por PIN dispon\xEDvel apenas para pagamento na entrega.");
        }
        if (order.paid === true) {
          return order;
        }
        const normalizedPin = String(pin || "").trim();
        if (!/^\d{4}$/.test(normalizedPin)) {
          throw new Error("PIN inv\xE1lido. Informe os 4 d\xEDgitos enviados por um usu\xE1rio autorizado.");
        }
        if (!order.paymentConfirmationPin || !order.paymentConfirmationPinExpiresAt) {
          throw new Error("Este pedido n\xE3o possui PIN ativo. Solicite um novo PIN ao dono/admin.");
        }
        if (new Date(order.paymentConfirmationPinExpiresAt).getTime() < Date.now()) {
          throw new Error("PIN expirado. Solicite um novo PIN ao dono/admin.");
        }
        if (!verifyPaymentConfirmationPin(normalizedPin, String(order.paymentConfirmationPin))) {
          throw new Error("PIN incorreto. Confira com o dono/admin.");
        }
        const updatedOrder = await prisma_default.$transaction(async (tx) => {
          const confirmedOrder = await OrderRepository_default.confirmPayment(
            normalizedOrderId,
            restaurantId,
            tx
          );
          await markCouponRedemptionUsedForOrder(normalizedOrderId, restaurantId, tx);
          return confirmedOrder;
        });
        io.to(`restaurant:${restaurantId}`).emit("order:payment-confirmed", {
          orderId: updatedOrder.id,
          paid: true,
          paymentMethod: updatedOrder.paymentMethod,
          confirmedWithPin: true
        });
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod,
            confirmedWithPin: true
          });
          io.to(`user:${updatedOrder.userId}`).emit("payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod,
            confirmedWithPin: true
          });
        }
        io.to(`restaurant:${restaurantId}`).emit("new-order", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("new-order", updatedOrder);
        }
        io.to(`restaurant:${restaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        return updatedOrder;
      }
    };
    ConfirmOrderPaymentWithPinService_default = new ConfirmOrderPaymentWithPinService();
  }
});

// src/modules/orders/controllers/ConfirmOrderPaymentWithPinController.ts
var ConfirmOrderPaymentWithPinController, ConfirmOrderPaymentWithPinController_default;
var init_ConfirmOrderPaymentWithPinController = __esm({
  "src/modules/orders/controllers/ConfirmOrderPaymentWithPinController.ts"() {
    init_ConfirmOrderPaymentWithPinService();
    ConfirmOrderPaymentWithPinController = class {
      async handle(req, res) {
        try {
          const { id } = req.params;
          const { pin } = req.body;
          const { restaurantId, role } = req.user;
          const updatedOrder = await ConfirmOrderPaymentWithPinService_default.execute(
            id,
            restaurantId,
            role,
            pin,
            req.user.id
          );
          return res.status(200).json(updatedOrder);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao confirmar pagamento com PIN"
          });
        }
      }
    };
    ConfirmOrderPaymentWithPinController_default = new ConfirmOrderPaymentWithPinController();
  }
});

// src/modules/orders/services/GenerateOrderPaymentConfirmationPinService.ts
import crypto6 from "crypto";
function generateFourDigitPin() {
  return String(crypto6.randomInt(1e3, 1e4));
}
var GenerateOrderPaymentConfirmationPinService, GenerateOrderPaymentConfirmationPinService_default;
var init_GenerateOrderPaymentConfirmationPinService = __esm({
  "src/modules/orders/services/GenerateOrderPaymentConfirmationPinService.ts"() {
    init_server();
    init_OrderRepository();
    init_paymentConfirmationPin();
    GenerateOrderPaymentConfirmationPinService = class {
      async execute(orderId, restaurantId) {
        const order = await OrderRepository_default.findById(orderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado!");
        }
        if (String(order.type || "").toUpperCase() !== "DELIVERY") {
          throw new Error("PIN de confirma\xE7\xE3o dispon\xEDvel apenas para pedidos DELIVERY.");
        }
        if (String(order.status || "").toUpperCase() !== "SAIU_PARA_ENTREGA") {
          throw new Error(
            "PIN de confirma\xE7\xE3o dispon\xEDvel apenas quando o pedido estiver em SAIU_PARA_ENTREGA."
          );
        }
        if (order.paid === true) {
          throw new Error("Pagamento deste pedido j\xE1 est\xE1 confirmado.");
        }
        if (order.payOnDelivery !== true || !order.paymentMethod) {
          throw new Error("PIN de confirma\xE7\xE3o dispon\xEDvel apenas para pagamento na entrega.");
        }
        const pin = generateFourDigitPin();
        const pinHash = hashPaymentConfirmationPin(pin);
        const expiresAt = new Date(Date.now() + 10 * 60 * 1e3);
        const updatedOrder = await OrderRepository_default.setPaymentConfirmationPin(
          orderId,
          restaurantId,
          pinHash,
          expiresAt
        );
        io.to(`restaurant:${restaurantId}`).emit("order:payment-pin-generated", {
          orderId: updatedOrder.id,
          expiresAt
        });
        io.to(`restaurant:${restaurantId}:admin`).emit("order:payment-pin-generated", {
          orderId: updatedOrder.id,
          expiresAt,
          pin
        });
        return {
          orderId: updatedOrder.id,
          pin,
          expiresAt
        };
      }
    };
    GenerateOrderPaymentConfirmationPinService_default = new GenerateOrderPaymentConfirmationPinService();
  }
});

// src/modules/orders/controllers/GenerateOrderPaymentConfirmationPinController.ts
var GenerateOrderPaymentConfirmationPinController, GenerateOrderPaymentConfirmationPinController_default;
var init_GenerateOrderPaymentConfirmationPinController = __esm({
  "src/modules/orders/controllers/GenerateOrderPaymentConfirmationPinController.ts"() {
    init_GenerateOrderPaymentConfirmationPinService();
    GenerateOrderPaymentConfirmationPinController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { restaurantId } = req.user;
          const result = await GenerateOrderPaymentConfirmationPinService_default.execute(id, restaurantId);
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao gerar PIN de confirmacao de pagamento"
          });
        }
      }
    };
    GenerateOrderPaymentConfirmationPinController_default = new GenerateOrderPaymentConfirmationPinController();
  }
});

// src/modules/orders/services/RequestOrderPaymentConfirmationPinService.ts
import { UserRole as UserRole14 } from "@prisma/client";
var RequestOrderPaymentConfirmationPinService, RequestOrderPaymentConfirmationPinService_default;
var init_RequestOrderPaymentConfirmationPinService = __esm({
  "src/modules/orders/services/RequestOrderPaymentConfirmationPinService.ts"() {
    init_server();
    init_OrderRepository();
    init_customerNotifier();
    init_CourierAccessService();
    RequestOrderPaymentConfirmationPinService = class {
      async execute(orderId, restaurantId, role, actorUserId) {
        const normalizedRole = String(role || "").toUpperCase();
        const allowedRoles2 = [UserRole14.MOTOQUEIRO, UserRole14.ADMIN];
        if (!allowedRoles2.includes(normalizedRole)) {
          throw new Error(
            "Somente admin ou motoqueiro podem solicitar PIN de confirma\xE7\xE3o de pagamento."
          );
        }
        const order = await OrderRepository_default.findById(orderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado!");
        }
        if (String(order.type || "").toUpperCase() !== "DELIVERY") {
          throw new Error("Solicita\xE7\xE3o de PIN dispon\xEDvel apenas para pedidos DELIVERY.");
        }
        if (String(order.status || "").toUpperCase() !== "SAIU_PARA_ENTREGA") {
          throw new Error("Solicita\xE7\xE3o de PIN dispon\xEDvel apenas durante a entrega.");
        }
        if (normalizedRole === UserRole14.MOTOQUEIRO) {
          const courierId = Number(actorUserId || 0);
          await CourierAccessService_default.assertActiveCourier(courierId, restaurantId);
          if (Number(order.assignedCourierId || 0) !== courierId) {
            throw new Error("Esta entrega n\xE3o est\xE1 atribu\xEDda a voc\xEA.");
          }
        }
        if (order.paid === true) {
          throw new Error("Pagamento deste pedido j\xE1 est\xE1 confirmado.");
        }
        if (order.payOnDelivery !== true || !order.paymentMethod) {
          throw new Error("Solicita\xE7\xE3o de PIN dispon\xEDvel apenas para pagamento na entrega.");
        }
        const requestedAt = (/* @__PURE__ */ new Date()).toISOString();
        notifyRestaurantPaymentPinRequested({
          restaurantWhatsapp: order?.restaurant?.whatsapp,
          restaurantName: order?.restaurant?.name,
          orderId: order?.id,
          requestedByRole: normalizedRole
        }).catch((error2) => {
          console.error(
            "[RESTAURANT_PIN_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        io.to(`restaurant:${restaurantId}`).emit("order:payment-pin-requested", {
          orderId: order.id,
          requestedAt,
          requestedByRole: normalizedRole
        });
        return {
          orderId: order.id,
          requestedAt,
          message: "Solicita\xE7\xE3o de PIN enviada para o dono/admin."
        };
      }
    };
    RequestOrderPaymentConfirmationPinService_default = new RequestOrderPaymentConfirmationPinService();
  }
});

// src/modules/orders/controllers/RequestOrderPaymentConfirmationPinController.ts
var RequestOrderPaymentConfirmationPinController, RequestOrderPaymentConfirmationPinController_default;
var init_RequestOrderPaymentConfirmationPinController = __esm({
  "src/modules/orders/controllers/RequestOrderPaymentConfirmationPinController.ts"() {
    init_RequestOrderPaymentConfirmationPinService();
    RequestOrderPaymentConfirmationPinController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { restaurantId, role } = req.user;
          const result = await RequestOrderPaymentConfirmationPinService_default.execute(
            id,
            restaurantId,
            role,
            req.user.id
          );
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao solicitar PIN de confirmacao de pagamento"
          });
        }
      }
    };
    RequestOrderPaymentConfirmationPinController_default = new RequestOrderPaymentConfirmationPinController();
  }
});

// src/modules/orders/controllers/CreateOrderPixPaymentController.ts
var CreateOrderPixPaymentController, CreateOrderPixPaymentController_default;
var init_CreateOrderPixPaymentController = __esm({
  "src/modules/orders/controllers/CreateOrderPixPaymentController.ts"() {
    init_OrderPixPaymentService();
    init_CreateOrderService();
    init_orderTenant();
    CreateOrderPixPaymentController = class {
      async handle(req, res) {
        try {
          const {
            restaurantId,
            type,
            paymentMethod,
            pixProvider,
            observation,
            tableId,
            settlementMode,
            items,
            address,
            number,
            district,
            city,
            state,
            zipCode,
            complement,
            customerName,
            customerCpf,
            customerPhone,
            couponRedemptionId
          } = req.body;
          const userId = req.user?.id ?? null;
          const userRestaurantId = req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null;
          const resolvedRestaurantId = resolveOrderRestaurantId({
            requestedRestaurantId: restaurantId,
            contextRestaurantId: userRestaurantId
          });
          const order = await CreateOrderService_default.execute({
            userId,
            restaurantId: resolvedRestaurantId,
            userRestaurantId,
            tableSessionId: req.tableSession?.id ?? null,
            tableSessionTableId: req.tableSession?.tableId ?? null,
            participantId: req.tableParticipant?.id ?? null,
            settlementMode,
            deferRealtimeUntilPaid: true,
            type,
            paymentMethod,
            paid: false,
            observation,
            tableId,
            customerName,
            customerCpf,
            customerPhone,
            couponRedemptionId,
            items,
            address,
            number,
            district,
            city,
            state,
            zipCode,
            complement
          });
          let result;
          try {
            result = await OrderPixPaymentService_default.createPixPayment({
              restaurantId: resolvedRestaurantId,
              type,
              paymentMethod,
              pixProvider,
              items,
              address,
              number,
              district,
              city,
              state,
              customerName,
              customerCpf,
              customerPhone,
              userEmail: req.user?.email || null,
              orderId: order.id,
              orderTotal: Number(order.total),
              orderSubtotal: Number(order.itemsSubtotal) - Number(order.couponDiscount),
              orderDeliveryFee: Number(order.deliveryFeeAmount)
            });
          } catch (error2) {
            await OrderPixPaymentService_default.removePendingOrderAfterPaymentFailure({
              orderId: order.id,
              restaurantId: resolvedRestaurantId
            });
            throw error2;
          }
          try {
            await OrderPixPaymentService_default.attachPaymentToOrder({
              orderId: order.id,
              restaurantId: resolvedRestaurantId,
              paymentId: String(result.paymentId || "")
            });
          } catch (error2) {
            console.error(
              "[PIX_ORDER_PAYMENT_LINK_ERROR]",
              error2 instanceof Error ? error2.message : String(error2),
              { orderId: order.id, restaurantId: resolvedRestaurantId }
            );
          }
          return res.status(201).json({
            ...result,
            orderId: order.id
          });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao gerar pagamento PIX"
          });
        }
      }
    };
    CreateOrderPixPaymentController_default = new CreateOrderPixPaymentController();
  }
});

// src/modules/orders/services/cardCheckoutProviders.ts
import Stripe2 from "stripe";
function withQueryParam(baseUrl, params) {
  try {
    const nextUrl = new URL(baseUrl);
    Object.entries(params).forEach(([key, value]) => {
      nextUrl.searchParams.set(key, value);
    });
    return nextUrl.toString();
  } catch {
    return baseUrl;
  }
}
async function getStripeClient(restaurantId) {
  const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
  const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
  const settingsSecretKey = String(settings?.stripeSecretKey || "").trim();
  const globalSecretKey = String(process.env.STRIPE_SECRET_KEY || "").trim();
  const secretKey = settingsSecretKey || (allowGlobalFallback ? globalSecretKey : "");
  if (!secretKey) {
    throw new Error(
      "Pagamento com cartao indisponivel. Configure chave secreta Stripe nas configuracoes do restaurante."
    );
  }
  return new Stripe2(secretKey);
}
function resolvePagBankEnvironment() {
  return "production";
}
async function getPagBankCredentials(restaurantId) {
  const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
  const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
  const settingsEmail = String(settings?.pagbankEmail || "").trim();
  const settingsToken = String(settings?.pagbankToken || "").trim();
  const globalEmail = String(process.env.PAGBANK_EMAIL || process.env.PAGSEGURO_EMAIL || "").trim();
  const globalToken = String(process.env.PAGBANK_TOKEN || process.env.PAGSEGURO_TOKEN || "").trim();
  const email = settingsEmail || (allowGlobalFallback ? globalEmail : "");
  const token = settingsToken || (allowGlobalFallback ? globalToken : "");
  const environment2 = resolvePagBankEnvironment();
  if (!email || !token) {
    throw new Error(
      "Pagamento com cartao PagBank indisponivel. Configure email/token PagBank nas configuracoes do restaurante."
    );
  }
  return { email, token, environment: environment2 };
}
function resolvePagBankCheckoutApiUrl(environment2) {
  void environment2;
  return "https://ws.pagseguro.uol.com.br/v2/checkout";
}
function resolvePagBankCheckoutPageBaseUrl(environment2) {
  void environment2;
  return "https://pagseguro.uol.com.br/v2/checkout/payment.html";
}
function resolveAsaasBaseUrl() {
  return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
}
async function getAsaasAccessToken(restaurantId) {
  const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
  const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
  const settingsToken = String(settings?.asaasAccessToken || "").trim();
  const globalToken = String(process.env.ASAAS_API_KEY || "").trim();
  const accessToken = settingsToken || (allowGlobalFallback ? globalToken : "");
  if (!accessToken) {
    throw new Error(
      "Pagamento com cartao Asaas indisponivel. Configure token Asaas nas configuracoes do restaurante."
    );
  }
  return accessToken;
}
function getAsaasError(payload, fallback) {
  if (!Array.isArray(payload?.errors) || payload.errors.length === 0) {
    return fallback;
  }
  const message = String(payload.errors[0]?.description || "").trim();
  return message || fallback;
}
async function fetchAsaasJson(url, accessToken, {
  method = "GET",
  body
} = {}) {
  const response = await fetch(url, {
    method,
    headers: {
      "Content-Type": "application/json",
      access_token: accessToken
    },
    ...body !== void 0 ? { body: JSON.stringify(body) } : {}
  });
  const responseBody = await response.json();
  return {
    ok: response.ok,
    responseBody
  };
}
function resolvePagBankNotificationUrl(restaurantId) {
  const explicitNotificationUrl = String(process.env.PAGBANK_NOTIFICATION_URL || "").trim();
  const backendUrl = String(process.env.BACKEND_URL || "").trim().replace(/\/+$/, "");
  const baseNotificationUrl = explicitNotificationUrl || (backendUrl ? `${backendUrl}/orders/webhook/pagbank` : "");
  if (!baseNotificationUrl || !restaurantId) {
    return baseNotificationUrl;
  }
  return withQueryParam(baseNotificationUrl, {
    restaurantId: String(restaurantId)
  });
}
function extractXmlTagValue(xml, tag) {
  const regex = new RegExp(`<${tag}>([^<]+)</${tag}>`, "i");
  const match = regex.exec(String(xml || ""));
  return String(match?.[1] || "").trim();
}
function extractProviderErrorText(error2) {
  if (typeof error2 === "string") {
    return error2.trim().toLowerCase();
  }
  const asRecord = typeof error2 === "object" && error2 !== null ? error2 : null;
  const message = String(
    asRecord?.message || asRecord?.cause?.message || ""
  );
  const causeText = String(asRecord?.cause || "");
  return `${message} ${causeText}`.trim().toLowerCase();
}
function isMarketplaceSplitConfigurationError2(error2) {
  const text = extractProviderErrorText(error2);
  if (!text) {
    return false;
  }
  return text.includes("marketplace_fee") || text.includes("application_fee") || text.includes("marketplace") || text.includes("split") || text.includes("collector") || text.includes("platform") || text.includes("not allowed") || text.includes("unauthorized") || text.includes("invalid");
}
function getCardCheckoutProviderHandler(provider) {
  const handler = CARD_CHECKOUT_PROVIDER_HANDLERS[provider];
  if (!handler) {
    throw new Error(
      `Gateway de cartao ${provider} ainda nao integrado. Configure STRIPE, MERCADO_PAGO, PAGBANK ou ASAAS para processar checkout com cartao no momento.`
    );
  }
  return handler;
}
var stripeCardCheckoutProvider, mercadoPagoCardCheckoutProvider, pagBankCardCheckoutProvider, asaasCardCheckoutProvider, CARD_CHECKOUT_PROVIDER_HANDLERS;
var init_cardCheckoutProviders = __esm({
  "src/modules/orders/services/cardCheckoutProviders.ts"() {
    init_providerCatalog();
    init_mercadoPagoClient();
    init_mercadoPagoOrderNotification();
    init_RestaurantSettingsRepository();
    stripeCardCheckoutProvider = {
      async createCheckout({ order, successUrlBase, cancelUrlBase }) {
        const stripe = await getStripeClient(order.restaurantId);
        const session = await stripe.checkout.sessions.create({
          mode: "payment",
          payment_method_types: ["card"],
          line_items: [
            {
              quantity: 1,
              price_data: {
                currency: "brl",
                unit_amount: Math.round(Number(order.total || 0) * 100),
                product_data: {
                  name: `Pedido #${order.id}`,
                  description: order.restaurant?.name || "Pedido online"
                }
              }
            }
          ],
          metadata: {
            orderId: String(order.id),
            restaurantId: String(order.restaurantId)
          },
          success_url: withQueryParam(successUrlBase, {
            cardCheckoutStatus: "success",
            orderId: String(order.id)
          }),
          cancel_url: withQueryParam(cancelUrlBase, {
            cardCheckoutStatus: "cancel",
            orderId: String(order.id)
          })
        });
        return {
          provider: CARD_PROVIDERS.STRIPE,
          sessionId: String(session.id),
          checkoutUrl: String(session.url || "")
        };
      }
    };
    mercadoPagoCardCheckoutProvider = {
      async createCheckout({ order, successUrlBase, cancelUrlBase }) {
        const preferenceApi = await getMercadoPagoPreferenceApi(order.restaurantId);
        const marketplaceFee = Number(order.systemFee || 0);
        const buildPreferenceBody = (includeMarketplaceFee) => ({
          items: [
            {
              id: String(order.id),
              title: `Pedido #${order.id}`,
              description: order.restaurant?.name || "Pedido online",
              quantity: 1,
              currency_id: "BRL",
              unit_price: Number(order.total || 0)
            }
          ],
          external_reference: `ordercard:${order.id}:${order.restaurantId}`,
          metadata: {
            order_id: String(order.id),
            restaurant_id: String(order.restaurantId),
            source: "order_card_checkout"
          },
          ...includeMarketplaceFee && marketplaceFee > 0 ? { marketplace_fee: marketplaceFee } : {},
          ...mercadoPagoOrderNotificationFields(order.restaurantId),
          back_urls: {
            success: withQueryParam(successUrlBase, {
              cardCheckoutStatus: "success",
              orderId: String(order.id)
            }),
            failure: withQueryParam(cancelUrlBase, {
              cardCheckoutStatus: "cancel",
              orderId: String(order.id)
            }),
            pending: withQueryParam(successUrlBase, {
              cardCheckoutStatus: "pending",
              orderId: String(order.id)
            })
          }
        });
        let response;
        if (marketplaceFee > 0) {
          try {
            response = await preferenceApi.create({
              body: buildPreferenceBody(true)
            });
          } catch (error2) {
            if (!isMarketplaceSplitConfigurationError2(error2)) {
              throw error2;
            }
            console.warn(
              "[CARD_SPLIT_FALLBACK] Mercado Pago rejeitou marketplace_fee. Recriando checkout sem split.",
              {
                orderId: order.id,
                restaurantId: order.restaurantId,
                marketplaceFee
              }
            );
            response = await preferenceApi.create({
              body: buildPreferenceBody(false)
            });
          }
        } else {
          response = await preferenceApi.create({
            body: buildPreferenceBody(false)
          });
        }
        const preference = typeof response === "object" && response !== null ? response.body ?? response : {};
        const preferenceId = String(preference.id || "").trim();
        const checkoutUrl = String(preference.init_point || "").trim();
        if (!preferenceId || !checkoutUrl) {
          throw new Error("Nao foi possivel criar checkout de cartao no Mercado Pago.");
        }
        return {
          provider: CARD_PROVIDERS.MERCADO_PAGO,
          sessionId: preferenceId,
          persistenceSessionId: `mp_pref:${preferenceId}`,
          checkoutUrl
        };
      }
    };
    pagBankCardCheckoutProvider = {
      async createCheckout({ order, successUrlBase }) {
        const { email, token, environment: environment2 } = await getPagBankCredentials(order.restaurantId);
        const params = new URLSearchParams();
        params.set("email", email);
        params.set("token", token);
        params.set("currency", "BRL");
        params.set("itemId1", String(order.id));
        params.set("itemDescription1", `Pedido #${order.id}`);
        params.set("itemAmount1", Number(order.total || 0).toFixed(2));
        params.set("itemQuantity1", "1");
        params.set("reference", `ordercard:${order.id}:${order.restaurantId}`);
        params.set(
          "redirectURL",
          withQueryParam(successUrlBase, {
            cardCheckoutStatus: "success",
            orderId: String(order.id)
          })
        );
        const notificationUrl = resolvePagBankNotificationUrl(order.restaurantId);
        if (notificationUrl) {
          params.set("notificationURL", notificationUrl);
        }
        const response = await fetch(resolvePagBankCheckoutApiUrl(environment2), {
          method: "POST",
          headers: {
            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8"
          },
          body: params.toString()
        });
        const responseText = await response.text();
        if (!response.ok) {
          const providerMessage = extractXmlTagValue(responseText, "message") || extractXmlTagValue(responseText, "error") || "Falha ao criar checkout no PagBank.";
          throw new Error(`PagBank: ${providerMessage}`);
        }
        const checkoutCode = extractXmlTagValue(responseText, "code");
        if (!checkoutCode) {
          throw new Error("PagBank nao retornou codigo de checkout.");
        }
        const checkoutUrl = `${resolvePagBankCheckoutPageBaseUrl(environment2)}?code=${encodeURIComponent(checkoutCode)}`;
        return {
          provider: CARD_PROVIDERS.PAGBANK,
          sessionId: checkoutCode,
          persistenceSessionId: `pagbank_chk:${checkoutCode}`,
          checkoutUrl
        };
      }
    };
    asaasCardCheckoutProvider = {
      async createCheckout({ payload, order }) {
        const asaasBaseUrl = resolveAsaasBaseUrl();
        const accessToken = await getAsaasAccessToken(order.restaurantId);
        const payerEmail = String(payload.userId ? "" : "").trim();
        const customerName = String(payload.customerName || "Cliente").trim();
        const cpf = String(payload.customerCpf || "").replace(/\D/g, "");
        const normalizedEmail = String(payload.customerName || "").trim() && payload.customerCpf ? `guest.card.${order.restaurantId}.${Date.now()}@pecaja.local` : `guest.card.${order.restaurantId}.${Date.now()}@pecaja.local`;
        const customerResult = await fetchAsaasJson(
          `${asaasBaseUrl}/v3/customers`,
          accessToken,
          {
            method: "POST",
            body: {
              name: customerName || "Cliente",
              email: payerEmail || normalizedEmail,
              ...cpf.length === 11 ? { cpfCnpj: cpf } : {},
              ...payload.customerPhone ? {
                mobilePhone: String(payload.customerPhone).replace(/\D/g, "")
              } : {}
            }
          }
        );
        if (!customerResult.ok || !String(customerResult.responseBody?.id || "").trim()) {
          throw new Error(
            getAsaasError(
              customerResult.responseBody,
              "Nao foi possivel criar/identificar cliente para checkout de cartao no Asaas."
            )
          );
        }
        const customerId2 = String(customerResult.responseBody.id || "").trim();
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(order.restaurantId);
        const walletId = String(settings?.gatewayMerchantId || "").trim();
        const platformWalletId = String(process.env.ASAAS_PLATFORM_WALLET_ID || "").trim();
        const systemFee = Number(order.systemFee || 0);
        const buildPaymentBody = (includeSplit) => ({
          customer: customerId2,
          billingType: "UNDEFINED",
          value: Number(order.total || 0),
          dueDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
          description: `Pedido #${order.id}`,
          externalReference: String(order.id),
          ...includeSplit && systemFee > 0 && platformWalletId ? {
            split: [
              {
                walletId: platformWalletId,
                fixedValue: systemFee
              },
              ...walletId ? [
                {
                  walletId,
                  remainingValue: true
                }
              ] : []
            ]
          } : {}
        });
        let paymentResult = await fetchAsaasJson(
          `${asaasBaseUrl}/v3/payments`,
          accessToken,
          {
            method: "POST",
            body: buildPaymentBody(systemFee > 0)
          }
        );
        const shouldRetryWithoutSplit = systemFee > 0 && !paymentResult.ok && isMarketplaceSplitConfigurationError2(
          getAsaasError(paymentResult.responseBody, "Erro ao criar checkout de cartao no Asaas.")
        );
        if (shouldRetryWithoutSplit) {
          console.warn(
            "[ASAAS_CARD_SPLIT_FALLBACK] Asaas rejeitou split. Recriando checkout sem split.",
            {
              orderId: order.id,
              restaurantId: order.restaurantId,
              systemFee
            }
          );
          paymentResult = await fetchAsaasJson(
            `${asaasBaseUrl}/v3/payments`,
            accessToken,
            {
              method: "POST",
              body: buildPaymentBody(false)
            }
          );
        }
        if (!paymentResult.ok) {
          throw new Error(
            getAsaasError(
              paymentResult.responseBody,
              "Nao foi possivel criar checkout de cartao no Asaas."
            )
          );
        }
        const sessionId = String(paymentResult.responseBody?.id || "").trim();
        const checkoutUrl = String(paymentResult.responseBody?.invoiceUrl || "").trim();
        if (!sessionId || !checkoutUrl) {
          throw new Error("Asaas nao retornou link de checkout para pagamento com cartao.");
        }
        return {
          provider: CARD_PROVIDERS.ASAAS,
          sessionId,
          persistenceSessionId: `asaas_pay:${sessionId}`,
          checkoutUrl
        };
      }
    };
    CARD_CHECKOUT_PROVIDER_HANDLERS = {
      [CARD_PROVIDERS.STRIPE]: stripeCardCheckoutProvider,
      [CARD_PROVIDERS.MERCADO_PAGO]: mercadoPagoCardCheckoutProvider,
      [CARD_PROVIDERS.PAGBANK]: pagBankCardCheckoutProvider,
      [CARD_PROVIDERS.ASAAS]: asaasCardCheckoutProvider
    };
  }
});

// src/modules/orders/services/CreateOrderCardCheckoutService.ts
var CreateOrderCardCheckoutService, CreateOrderCardCheckoutService_default;
var init_CreateOrderCardCheckoutService = __esm({
  "src/modules/orders/services/CreateOrderCardCheckoutService.ts"() {
    init_CreateOrderService();
    init_OrderRepository();
    init_RestaurantSettingsRepository();
    init_restaurantAvailability();
    init_providerCatalog();
    init_cardCheckoutProviders();
    init_orderTenant();
    init_prisma();
    init_couponRedemptionLifecycle();
    init_restoreOrderItemsStock();
    CreateOrderCardCheckoutService = class {
      async resolveCardProvider(payload) {
        const resolvedRestaurantId = resolveOrderRestaurantId({
          requestedRestaurantId: payload.restaurantId,
          contextRestaurantId: payload.userRestaurantId
        });
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(resolvedRestaurantId);
        assertRestaurantIsOpenForOrders(settings?.isOpenForOrders, settings?.businessHours);
        if (settings?.acceptsCard === false) {
          throw new Error("O restaurante n\xE3o est\xE1 aceitando pagamentos com cart\xE3o no momento.");
        }
        const configuredProvider3 = String(settings?.cardGateway || "").trim();
        if (!configuredProvider3) {
          throw new Error(
            "Pagamento com cart\xE3o indispon\xEDvel. Configure o gateway nas configura\xE7\xF5es do restaurante."
          );
        }
        if (!["MERCADO_PAGO", "ASAAS", "PAGBANK"].includes(configuredProvider3.toUpperCase())) {
          throw new Error("Gateway inv\xE1lido. Escolha Mercado Pago, Asaas ou PagBank.");
        }
        return normalizeCardProvider(configuredProvider3);
      }
      ensureCardProviderSupported(provider) {
        getCardCheckoutProviderHandler(provider);
      }
      async execute(payload) {
        const resolvedCardProvider = await this.resolveCardProvider(payload);
        this.ensureCardProviderSupported(resolvedCardProvider);
        const createdOrder = await CreateOrderService_default.execute({
          ...payload,
          deferRealtimeUntilPaid: true,
          paid: false
        });
        const successUrlBase = String(
          payload.successUrl || process.env.FRONTEND_URL || "http://localhost:5173/cart"
        ).trim();
        const cancelUrlBase = String(payload.cancelUrl || successUrlBase).trim();
        let checkout;
        try {
          const providerHandler = getCardCheckoutProviderHandler(resolvedCardProvider);
          checkout = await providerHandler.createCheckout({
            payload,
            order: {
              id: createdOrder.id,
              restaurantId: createdOrder.restaurantId,
              total: createdOrder.total,
              systemFee: createdOrder.systemFee,
              restaurant: createdOrder.restaurant
            },
            successUrlBase,
            cancelUrlBase
          });
        } catch (error2) {
          await prisma_default.$transaction(async (tx) => {
            const pendingOrder = await OrderRepository_default.findById(
              createdOrder.id,
              createdOrder.restaurantId,
              tx
            );
            if (pendingOrder) {
              await restoreOrderItemsStock(tx, pendingOrder);
            }
            await releaseCouponRedemptionForOrder(createdOrder.id, createdOrder.restaurantId, tx);
            await OrderRepository_default.deleteById(createdOrder.id, createdOrder.restaurantId, tx);
          });
          throw error2;
        }
        try {
          await OrderRepository_default.setCardCheckoutSessionId(
            createdOrder.id,
            createdOrder.restaurantId,
            String(checkout.persistenceSessionId || checkout.sessionId)
          );
        } catch (error2) {
          console.error(
            "[CARD_ORDER_PAYMENT_LINK_ERROR]",
            error2 instanceof Error ? error2.message : String(error2),
            { orderId: createdOrder.id, restaurantId: createdOrder.restaurantId }
          );
        }
        return {
          orderId: createdOrder.id,
          provider: checkout.provider,
          sessionId: checkout.sessionId,
          checkoutUrl: checkout.checkoutUrl
        };
      }
    };
    CreateOrderCardCheckoutService_default = new CreateOrderCardCheckoutService();
  }
});

// src/modules/orders/controllers/CreateOrderCardCheckoutController.ts
var CreateOrderCardCheckoutController, CreateOrderCardCheckoutController_default;
var init_CreateOrderCardCheckoutController = __esm({
  "src/modules/orders/controllers/CreateOrderCardCheckoutController.ts"() {
    init_CreateOrderCardCheckoutService();
    CreateOrderCardCheckoutController = class {
      async handle(req, res) {
        try {
          const {
            restaurantId,
            type,
            paymentMethod,
            items,
            address,
            number,
            district,
            city,
            state,
            zipCode,
            complement,
            customerName,
            customerCpf,
            customerPhone,
            observation,
            tableId,
            settlementMode,
            cardProvider,
            successUrl,
            cancelUrl,
            couponRedemptionId
          } = req.body;
          const userId = req.user?.id ?? null;
          const userRestaurantId = req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null;
          const result = await CreateOrderCardCheckoutService_default.execute({
            userId,
            restaurantId,
            userRestaurantId,
            tableSessionId: req.tableSession?.id ?? null,
            tableSessionTableId: req.tableSession?.tableId ?? null,
            participantId: req.tableParticipant?.id ?? null,
            settlementMode,
            type,
            paymentMethod,
            items,
            address,
            number,
            district,
            city,
            state,
            zipCode,
            complement,
            customerName,
            customerCpf,
            customerPhone,
            observation,
            tableId,
            cardProvider,
            successUrl,
            cancelUrl,
            couponRedemptionId
          });
          return res.status(201).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao iniciar pagamento com cartao"
          });
        }
      }
    };
    CreateOrderCardCheckoutController_default = new CreateOrderCardCheckoutController();
  }
});

// src/modules/orders/controllers/GetOrderPixPaymentStatusController.ts
var GetOrderPixPaymentStatusController, GetOrderPixPaymentStatusController_default;
var init_GetOrderPixPaymentStatusController = __esm({
  "src/modules/orders/controllers/GetOrderPixPaymentStatusController.ts"() {
    init_OrderPixPaymentService();
    init_orderTenant();
    GetOrderPixPaymentStatusController = class {
      async handle(req, res) {
        try {
          const { paymentId, restaurantId } = req.body;
          const resolvedRestaurantId = resolveOrderRestaurantId({
            requestedRestaurantId: restaurantId,
            contextRestaurantId: req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null
          });
          const result = await OrderPixPaymentService_default.getPaymentStatus({
            paymentId,
            restaurantId: resolvedRestaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao consultar status do pagamento PIX"
          });
        }
      }
    };
    GetOrderPixPaymentStatusController_default = new GetOrderPixPaymentStatusController();
  }
});

// src/modules/orders/services/ReconcileLateCancelledPaymentService.ts
import { OrderRefundStatus as OrderRefundStatus5, OrderStatus as OrderStatus15, PaymentMethod as PaymentMethod7 } from "@prisma/client";
var ReconcileLateCancelledPaymentService, ReconcileLateCancelledPaymentService_default;
var init_ReconcileLateCancelledPaymentService = __esm({
  "src/modules/orders/services/ReconcileLateCancelledPaymentService.ts"() {
    init_prisma();
    init_OrderRepository();
    init_RefundOrderPaymentService();
    ReconcileLateCancelledPaymentService = class {
      async execute({
        orderId,
        restaurantId,
        paymentMethod,
        paymentReference
      }) {
        const normalizedRestaurantId = Number(restaurantId || 0);
        const normalizedMethod = String(paymentMethod || "").toUpperCase();
        const normalizedReference = String(paymentReference || "").trim();
        if (!normalizedReference) {
          throw new Error("Pagamento tardio sem refer\xEAncia para estorno.");
        }
        const order = await OrderRepository_default.findById(orderId, normalizedRestaurantId);
        if (!order || order.status !== OrderStatus15.CANCELADO || order.paid === true) {
          return false;
        }
        if (order.refundStatus === OrderRefundStatus5.SUCCEEDED) {
          return true;
        }
        if (order.refundStatus === OrderRefundStatus5.PROCESSING) {
          throw new Error("Estorno de pagamento tardio j\xE1 est\xE1 em processamento.");
        }
        const isPix = normalizedMethod === PaymentMethod7.PIX;
        const isCard = normalizedMethod === PaymentMethod7.CARTAO;
        if (!isPix && !isCard) {
          return false;
        }
        const pendingMarker = `late_refund_pending:${normalizedReference}`;
        const completedMarker = `late_refunded:${normalizedReference}`;
        const idempotencyKey = `late-order-refund-${order.restaurantId}-${order.id}`;
        const currentReference = String(
          isPix ? order.pixPaymentId || "" : order.cardCheckoutSessionId || ""
        ).trim();
        if (currentReference === completedMarker) {
          return true;
        }
        if (currentReference.startsWith("late_refund_pending:")) {
          throw new Error("Estorno de pagamento tardio j\xE1 est\xE1 em processamento.");
        }
        const claim = await prisma_default.order.updateMany({
          where: {
            id: order.id,
            restaurantId: order.restaurantId,
            status: OrderStatus15.CANCELADO,
            paid: false,
            ...isPix ? { pixPaymentId: order.pixPaymentId } : { cardCheckoutSessionId: order.cardCheckoutSessionId }
          },
          data: isPix ? {
            pixPaymentId: pendingMarker,
            refundStatus: OrderRefundStatus5.PROCESSING,
            refundRequestedAt: /* @__PURE__ */ new Date(),
            refundFailureReason: null,
            refundIdempotencyKey: idempotencyKey
          } : {
            cardCheckoutSessionId: pendingMarker,
            refundStatus: OrderRefundStatus5.PROCESSING,
            refundRequestedAt: /* @__PURE__ */ new Date(),
            refundFailureReason: null,
            refundIdempotencyKey: idempotencyKey
          }
        });
        if (claim.count !== 1) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          const latestReference = String(
            isPix ? latest?.pixPaymentId || "" : latest?.cardCheckoutSessionId || ""
          ).trim();
          if (latestReference === completedMarker) {
            return true;
          }
          throw new Error("N\xE3o foi poss\xEDvel iniciar o estorno do pagamento tardio.");
        }
        let receipt;
        try {
          receipt = await RefundOrderPaymentService_default.execute(
            {
              ...order,
              paid: true,
              ...isPix ? { pixPaymentId: normalizedReference } : { cardCheckoutSessionId: normalizedReference }
            },
            {
              idempotencyKey,
              verifyExistingRefund: order.refundStatus === OrderRefundStatus5.FAILED
            }
          );
        } catch (error2) {
          await prisma_default.order.updateMany({
            where: {
              id: order.id,
              restaurantId: order.restaurantId,
              refundStatus: OrderRefundStatus5.PROCESSING,
              ...isPix ? { pixPaymentId: pendingMarker } : { cardCheckoutSessionId: pendingMarker }
            },
            data: isPix ? {
              pixPaymentId: order.pixPaymentId,
              refundStatus: OrderRefundStatus5.FAILED,
              refundFailureReason: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel confirmar o estorno do pagamento tardio."
            } : {
              cardCheckoutSessionId: order.cardCheckoutSessionId,
              refundStatus: OrderRefundStatus5.FAILED,
              refundFailureReason: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel confirmar o estorno do pagamento tardio."
            }
          });
          throw error2;
        }
        const persisted = await prisma_default.order.updateMany({
          where: {
            id: order.id,
            restaurantId: order.restaurantId,
            refundStatus: OrderRefundStatus5.PROCESSING,
            ...isPix ? { pixPaymentId: pendingMarker } : { cardCheckoutSessionId: pendingMarker }
          },
          data: isPix ? {
            pixPaymentId: completedMarker,
            refundStatus: OrderRefundStatus5.SUCCEEDED,
            refundedAt: /* @__PURE__ */ new Date(),
            refundFailureReason: null,
            refundProvider: receipt.provider,
            refundExternalId: receipt.externalId
          } : {
            cardCheckoutSessionId: completedMarker,
            refundStatus: OrderRefundStatus5.SUCCEEDED,
            refundedAt: /* @__PURE__ */ new Date(),
            refundFailureReason: null,
            refundProvider: receipt.provider,
            refundExternalId: receipt.externalId
          }
        });
        if (persisted.count !== 1) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          if (latest?.refundStatus !== OrderRefundStatus5.SUCCEEDED) {
            console.error("[LATE_ORDER_REFUND_RECONCILIATION_REQUIRED]", {
              orderId: order.id,
              restaurantId: order.restaurantId,
              paymentMethod: normalizedMethod,
              provider: receipt.provider,
              externalId: receipt.externalId
            });
            throw new Error(
              "O estorno tardio foi confirmado pelo provedor e aguarda concilia\xE7\xE3o. N\xE3o repita a opera\xE7\xE3o."
            );
          }
        }
        console.warn("[LATE_CANCELLED_PAYMENT_REFUNDED]", {
          orderId: order.id,
          restaurantId: order.restaurantId,
          paymentMethod: normalizedMethod
        });
        return true;
      }
    };
    ReconcileLateCancelledPaymentService_default = new ReconcileLateCancelledPaymentService();
  }
});

// src/modules/orders/services/FinalizeOrderPixPaymentService.ts
var FinalizeOrderPixPaymentService, FinalizeOrderPixPaymentService_default;
var init_FinalizeOrderPixPaymentService = __esm({
  "src/modules/orders/services/FinalizeOrderPixPaymentService.ts"() {
    init_server();
    init_prisma();
    init_customerNotifier();
    init_OrderRepository();
    init_OrderPixPaymentService();
    init_couponRedemptionLifecycle();
    init_ReconcileLateCancelledPaymentService();
    init_waiterOrderRealtime();
    FinalizeOrderPixPaymentService = class {
      async execute({
        orderId,
        paymentId,
        restaurantId,
        allowMissingOrder = false
      }) {
        const normalizedPaymentId = String(paymentId || "").trim();
        if (!normalizedPaymentId) {
          throw new Error("Pagamento PIX invalido.");
        }
        if (normalizedPaymentId.startsWith("manual:")) {
          throw new Error("Pagamento PIX manual nao e permitido.");
        }
        const normalizedRestaurantId = Number(restaurantId || 0) || void 0;
        const existingPaymentOrder = await OrderRepository_default.findByPixPaymentId(normalizedPaymentId);
        if (existingPaymentOrder && orderId && existingPaymentOrder.id !== Number(orderId)) {
          throw new Error("Este pagamento PIX j\xE1 foi utilizado em outro pedido.");
        }
        const order = orderId ? await OrderRepository_default.findById(orderId, Number(normalizedRestaurantId || 0)) : existingPaymentOrder;
        if (!order) {
          if (allowMissingOrder) {
            return null;
          }
          throw new Error("Pedido PIX nao encontrado para este pagamento.");
        }
        if (normalizedRestaurantId && order.restaurantId !== normalizedRestaurantId) {
          throw new Error("Este pagamento PIX n\xE3o pertence ao restaurante do pedido.");
        }
        await OrderPixPaymentService_default.ensurePaymentApproved({
          paymentId: normalizedPaymentId,
          restaurantId: order.restaurantId,
          expectedOrderId: order.id,
          expectedAmount: Number(order.total),
          expectedCurrency: "BRL"
        });
        const storedPaymentId = String(order.pixPaymentId || "").trim();
        if (storedPaymentId !== normalizedPaymentId) {
          if (storedPaymentId && String(order.status) !== "CANCELADO") {
            throw new Error("Pagamento PIX nao corresponde ao pedido informado.");
          }
        }
        if (String(order.status) === "CANCELADO" && order.paid !== true) {
          await ReconcileLateCancelledPaymentService_default.execute({
            orderId: order.id,
            restaurantId: order.restaurantId,
            paymentMethod: "PIX",
            paymentReference: normalizedPaymentId
          });
          return OrderRepository_default.findById(order.id, order.restaurantId);
        }
        if (order.paid === true) {
          return order;
        }
        let updatedOrder;
        try {
          updatedOrder = await prisma_default.$transaction(async (tx) => {
            await OrderRepository_default.claimPixPaymentId(
              order.id,
              order.restaurantId,
              normalizedPaymentId,
              tx
            );
            const confirmedOrder = await OrderRepository_default.confirmPayment(
              order.id,
              order.restaurantId,
              tx
            );
            await markCouponRedemptionUsedForOrder(order.id, order.restaurantId, tx);
            return confirmedOrder;
          });
        } catch (error2) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          if (latest?.status === "CANCELADO" && latest.paid !== true) {
            await ReconcileLateCancelledPaymentService_default.execute({
              orderId: latest.id,
              restaurantId: latest.restaurantId,
              paymentMethod: "PIX",
              paymentReference: normalizedPaymentId
            });
            return OrderRepository_default.findById(latest.id, latest.restaurantId);
          }
          throw error2;
        }
        io.to(`restaurant:${updatedOrder.restaurantId}`).emit("order:payment-confirmed", {
          orderId: updatedOrder.id,
          paid: true,
          paymentMethod: updatedOrder.paymentMethod
        });
        emitTableSessionOrderEvent(io, "order:payment-confirmed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod,
            status: updatedOrder.status
          });
        }
        io.to(`restaurant:${updatedOrder.restaurantId}`).emit("new-order", updatedOrder);
        io.to(`restaurant:${updatedOrder.restaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        emitWaiterTableOrderEvent(io, "new-order", updatedOrder);
        emitTableSessionOrderEvent(io, "new-order", updatedOrder);
        emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
        notifyCustomerPaymentConfirmed({
          restaurantId: updatedOrder.restaurantId,
          customerPhone: updatedOrder?.user?.phone,
          customerName: updatedOrder?.user?.name,
          restaurantName: updatedOrder?.restaurant?.name,
          restaurantWhatsapp: updatedOrder?.restaurant?.whatsapp,
          orderId: updatedOrder?.id,
          total: updatedOrder?.total,
          paymentMethod: updatedOrder?.paymentMethod
        }).catch((error2) => {
          console.error(
            "[CUSTOMER_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        return updatedOrder;
      }
    };
    FinalizeOrderPixPaymentService_default = new FinalizeOrderPixPaymentService();
  }
});

// src/modules/orders/controllers/ConfirmOrderPixPaymentController.ts
var ConfirmOrderPixPaymentController, ConfirmOrderPixPaymentController_default;
var init_ConfirmOrderPixPaymentController = __esm({
  "src/modules/orders/controllers/ConfirmOrderPixPaymentController.ts"() {
    init_FinalizeOrderPixPaymentService();
    init_orderTenant();
    ConfirmOrderPixPaymentController = class {
      async handle(req, res) {
        try {
          const { orderId, paymentId, restaurantId } = req.body;
          const resolvedRestaurantId = resolveOrderRestaurantId({
            requestedRestaurantId: restaurantId,
            contextRestaurantId: req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null
          });
          const order = await FinalizeOrderPixPaymentService_default.execute({
            orderId,
            paymentId,
            restaurantId: resolvedRestaurantId
          });
          return res.status(200).json(order);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao confirmar pagamento PIX do pedido"
          });
        }
      }
    };
    ConfirmOrderPixPaymentController_default = new ConfirmOrderPixPaymentController();
  }
});

// src/modules/orders/services/orderIssueChatStore.ts
async function loadThread(orderId) {
  return prisma_default.orderIssueThread.findUnique({
    where: {
      orderId
    },
    include: {
      messages: {
        orderBy: {
          sentAt: "asc"
        }
      }
    }
  });
}
async function ensureOrderIssueThread(input) {
  await prisma_default.orderIssueThread.upsert({
    where: {
      orderId: input.orderId
    },
    create: {
      orderId: input.orderId,
      userId: input.userId,
      restaurantId: input.restaurantId,
      customerName: input.customerName,
      customerPhone: input.customerPhone || null,
      orderStatus: input.orderStatus,
      orderType: input.orderType,
      paymentMethod: input.paymentMethod || null,
      total: input.total,
      orderCreatedAt: new Date(input.createdAt),
      addressLabel: input.addressLabel || null,
      itemsSummary: input.itemsSummary
    },
    update: {
      customerName: input.customerName,
      customerPhone: input.customerPhone || null,
      orderStatus: input.orderStatus,
      orderType: input.orderType,
      paymentMethod: input.paymentMethod || null,
      total: input.total,
      orderCreatedAt: new Date(input.createdAt),
      addressLabel: input.addressLabel || null,
      itemsSummary: input.itemsSummary
    }
  });
  const thread = await loadThread(input.orderId);
  if (!thread) {
    throw new Error("N\xE3o foi poss\xEDvel preparar o chat do pedido.");
  }
  return thread;
}
async function getOrderIssueThread(orderId) {
  return loadThread(orderId);
}
async function addOrderIssueMessage({
  orderId,
  senderType,
  senderName,
  message
}) {
  const thread = await prisma_default.orderIssueThread.findUnique({
    where: {
      orderId
    },
    select: {
      id: true,
      isResolved: true
    }
  });
  if (!thread) {
    throw new Error("Conversa n\xE3o encontrada para este pedido.");
  }
  if (thread.isResolved) {
    throw new Error("Este problema j\xE1 foi resolvido e o chat foi encerrado.");
  }
  const chatMessage = await prisma_default.orderIssueMessage.create({
    data: {
      threadId: thread.id,
      senderType,
      senderName: String(senderName || senderType).trim() || senderType,
      message
    }
  });
  const fullThread = await loadThread(orderId);
  if (!fullThread) {
    throw new Error("N\xE3o foi poss\xEDvel atualizar a conversa do pedido.");
  }
  return {
    thread: fullThread,
    chatMessage
  };
}
async function resolveOrderIssueThread({
  orderId,
  resolvedByName
}) {
  const thread = await prisma_default.orderIssueThread.findUnique({
    where: {
      orderId
    },
    select: {
      id: true,
      isResolved: true
    }
  });
  if (!thread) {
    throw new Error("Conversa n\xE3o encontrada para este pedido.");
  }
  if (thread.isResolved) {
    const existing = await loadThread(orderId);
    if (!existing) {
      throw new Error("Conversa n\xE3o encontrada para este pedido.");
    }
    return existing;
  }
  await prisma_default.orderIssueThread.update({
    where: {
      id: thread.id
    },
    data: {
      isResolved: true,
      resolvedAt: /* @__PURE__ */ new Date(),
      resolvedByName: String(resolvedByName || "Admin").trim() || "Admin"
    }
  });
  const resolved = await loadThread(orderId);
  if (!resolved) {
    throw new Error("Conversa n\xE3o encontrada para este pedido.");
  }
  return resolved;
}
function toOrderIssueThreadPayload(thread) {
  if (!thread) {
    return null;
  }
  return {
    orderId: thread.orderId,
    userId: thread.userId,
    restaurantId: thread.restaurantId,
    customerName: thread.customerName,
    customerPhone: thread.customerPhone,
    status: thread.orderStatus,
    type: thread.orderType,
    paymentMethod: thread.paymentMethod,
    total: Number(thread.total || 0),
    createdAt: thread.orderCreatedAt?.toISOString?.() || null,
    addressLabel: thread.addressLabel,
    itemsSummary: thread.itemsSummary,
    isResolved: thread.isResolved,
    resolvedAt: thread.resolvedAt?.toISOString?.() || null,
    resolvedByName: thread.resolvedByName,
    messages: thread.messages.map((message) => ({
      id: String(message.id),
      senderType: message.senderType,
      senderName: message.senderName,
      message: message.message,
      sentAt: message.sentAt?.toISOString?.() || null
    })),
    updatedAt: thread.updatedAt?.toISOString?.() || null
  };
}
var init_orderIssueChatStore = __esm({
  "src/modules/orders/services/orderIssueChatStore.ts"() {
    init_prisma();
  }
});

// src/modules/orders/services/ReportOrderIssueService.ts
function buildOrderAddressLabel(order) {
  const parts = [
    String(order?.address || "").trim(),
    String(order?.number || "").trim(),
    String(order?.district || "").trim(),
    String(order?.city || "").trim(),
    String(order?.state || "").trim(),
    String(order?.zipCode || "").trim()
  ].filter(Boolean);
  return parts.join(", ");
}
var ReportOrderIssueService, ReportOrderIssueService_default;
var init_ReportOrderIssueService = __esm({
  "src/modules/orders/services/ReportOrderIssueService.ts"() {
    init_prisma();
    init_server();
    init_customerNotifier();
    init_orderIssueChatStore();
    ReportOrderIssueService = class {
      async execute(orderId, userId, restaurantId, issueMessage) {
        const normalizedOrderId = Number(orderId);
        const normalizedUserId = Number(userId);
        const normalizedRestaurantId = Number(restaurantId || 0);
        const normalizedIssueMessage = String(issueMessage || "").replace(/\s+/g, " ").trim();
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new Error("Pedido inv\xE1lido para relatar problema.");
        }
        if (!Number.isInteger(normalizedUserId) || normalizedUserId <= 0) {
          throw new Error("Usu\xE1rio inv\xE1lido para relatar problema.");
        }
        if (normalizedIssueMessage.length > 600) {
          throw new Error("Mensagem muito longa. Use no m\xE1ximo 600 caracteres.");
        }
        const order = await prisma_default.order.findFirst({
          where: {
            id: normalizedOrderId,
            userId: normalizedUserId,
            ...Number.isFinite(normalizedRestaurantId) && normalizedRestaurantId > 0 ? {
              restaurantId: normalizedRestaurantId
            } : {}
          },
          select: {
            id: true,
            userId: true,
            status: true,
            type: true,
            paymentMethod: true,
            total: true,
            createdAt: true,
            restaurantId: true,
            address: true,
            number: true,
            district: true,
            city: true,
            state: true,
            zipCode: true,
            items: {
              select: {
                quantity: true,
                product: {
                  select: {
                    name: true
                  }
                }
              }
            },
            user: {
              select: {
                name: true,
                phone: true
              }
            },
            restaurant: {
              select: {
                name: true,
                whatsapp: true
              }
            }
          }
        });
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado para este usu\xE1rio.");
        }
        const orderAddressLabel = buildOrderAddressLabel(order);
        const orderItemsSummary = Array.isArray(order?.items) ? order.items.map((item) => {
          const quantity = Number(item?.quantity || 0);
          const productName = String(item?.product?.name || "Item").trim();
          if (!productName) {
            return "";
          }
          return quantity > 0 ? `${quantity}x ${productName}` : productName;
        }).filter(Boolean) : [];
        const existingThread = await getOrderIssueThread(order.id);
        if (!existingThread && normalizedIssueMessage.length < 10) {
          throw new Error("Descreva o problema com pelo menos 10 caracteres.");
        }
        if (existingThread && normalizedIssueMessage.length < 2) {
          throw new Error("Digite uma mensagem para continuar o chat.");
        }
        if (existingThread?.isResolved) {
          throw new Error("Este problema j\xE1 foi resolvido e o chat foi encerrado.");
        }
        await ensureOrderIssueThread({
          orderId: order.id,
          userId: order.userId,
          restaurantId: order.restaurantId,
          customerName: String(order?.user?.name || "Cliente").trim(),
          customerPhone: String(order?.user?.phone || "").trim(),
          orderStatus: String(order.status || ""),
          orderType: String(order.type || ""),
          paymentMethod: String(order.paymentMethod || ""),
          total: Number(order.total || 0),
          createdAt: order.createdAt.toISOString(),
          addressLabel: orderAddressLabel,
          itemsSummary: orderItemsSummary
        });
        const { thread, chatMessage } = await addOrderIssueMessage({
          orderId: order.id,
          senderType: "CLIENT",
          senderName: String(order?.user?.name || "Cliente"),
          message: normalizedIssueMessage
        });
        const threadPayload = toOrderIssueThreadPayload(thread);
        if (!threadPayload) {
          throw new Error("N\xE3o foi poss\xEDvel atualizar a conversa do pedido.");
        }
        const payload = {
          orderId: order.id,
          userId: order.userId,
          status: order.status,
          type: order.type,
          paymentMethod: order.paymentMethod,
          total: Number(order.total || 0),
          createdAt: order.createdAt,
          restaurantId: order.restaurantId,
          addressLabel: orderAddressLabel,
          itemsSummary: orderItemsSummary,
          customerName: String(order?.user?.name || "Cliente").trim(),
          customerPhone: String(order?.user?.phone || "").trim(),
          issueMessage: normalizedIssueMessage,
          reportedAt: chatMessage.sentAt,
          isResolved: threadPayload.isResolved,
          messages: threadPayload.messages
        };
        notifyRestaurantOrderIssueReported({
          restaurantWhatsapp: order?.restaurant?.whatsapp,
          restaurantName: order?.restaurant?.name,
          orderId: order.id,
          customerName: payload.customerName,
          customerPhone: payload.customerPhone,
          issueMessage: payload.issueMessage,
          orderStatus: payload.status,
          orderType: payload.type,
          paymentMethod: payload.paymentMethod,
          total: payload.total,
          addressLabel: payload.addressLabel,
          itemsSummary: payload.itemsSummary,
          createdAt: payload.createdAt?.toISOString?.() || null
        }).catch((error2) => {
          console.error(
            "[RESTAURANT_ORDER_ISSUE_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        io.to(`restaurant:${order.restaurantId}:admin`).emit("order:issue-reported", payload);
        io.to(`restaurant:${order.restaurantId}:admin`).emit("order:issue-message", {
          ...threadPayload,
          message: chatMessage
        });
        io.to(`user:${order.userId}`).emit("order:issue-message", {
          ...threadPayload,
          message: chatMessage
        });
        return {
          ...threadPayload,
          lastMessage: chatMessage,
          info: "Problema relatado para o admin com sucesso."
        };
      }
    };
    ReportOrderIssueService_default = new ReportOrderIssueService();
  }
});

// src/modules/orders/controllers/ReportOrderIssueController.ts
var ReportOrderIssueController, ReportOrderIssueController_default;
var init_ReportOrderIssueController = __esm({
  "src/modules/orders/controllers/ReportOrderIssueController.ts"() {
    init_ReportOrderIssueService();
    ReportOrderIssueController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { id: userId, restaurantId } = req.user;
          const { message } = req.body || {};
          const result = await ReportOrderIssueService_default.execute(id, userId, restaurantId, message);
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao relatar problema no pedido"
          });
        }
      }
    };
    ReportOrderIssueController_default = new ReportOrderIssueController();
  }
});

// src/modules/orders/services/ReplyOrderIssueService.ts
var ReplyOrderIssueService, ReplyOrderIssueService_default;
var init_ReplyOrderIssueService = __esm({
  "src/modules/orders/services/ReplyOrderIssueService.ts"() {
    init_prisma();
    init_server();
    init_orderIssueChatStore();
    ReplyOrderIssueService = class {
      async execute({
        orderId,
        restaurantId,
        adminUserId,
        replyMessage
      }) {
        const normalizedOrderId = Number(orderId);
        const normalizedRestaurantId = Number(restaurantId || 0);
        const normalizedAdminUserId = Number(adminUserId);
        const normalizedReplyMessage = String(replyMessage || "").replace(/\s+/g, " ").trim();
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new Error("Pedido inv\xE1lido para responder.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para responder.");
        }
        if (!Number.isInteger(normalizedAdminUserId) || normalizedAdminUserId <= 0) {
          throw new Error("Admin inv\xE1lido para responder.");
        }
        if (normalizedReplyMessage.length < 2) {
          throw new Error("Digite uma resposta para o cliente.");
        }
        if (normalizedReplyMessage.length > 600) {
          throw new Error("Resposta muito longa. Use no m\xE1ximo 600 caracteres.");
        }
        const [order, adminUser] = await Promise.all([
          prisma_default.order.findFirst({
            where: {
              id: normalizedOrderId,
              restaurantId: normalizedRestaurantId
            },
            select: {
              id: true,
              restaurantId: true,
              userId: true,
              user: {
                select: {
                  name: true
                }
              }
            }
          }),
          prisma_default.user.findUnique({
            where: {
              id: normalizedAdminUserId
            },
            select: {
              name: true
            }
          })
        ]);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado para este restaurante.");
        }
        const existingThread = await getOrderIssueThread(order.id);
        if (!existingThread) {
          throw new Error("Cliente ainda n\xE3o iniciou conversa neste pedido.");
        }
        if (existingThread.isResolved) {
          throw new Error("Este problema j\xE1 foi resolvido e o chat foi encerrado.");
        }
        await ensureOrderIssueThread({
          orderId: order.id,
          userId: order.userId,
          restaurantId: order.restaurantId,
          customerName: String(order?.user?.name || "Cliente").trim(),
          customerPhone: existingThread.customerPhone || "",
          orderStatus: String(existingThread.orderStatus || ""),
          orderType: String(existingThread.orderType || ""),
          paymentMethod: String(existingThread.paymentMethod || ""),
          total: Number(existingThread.total || 0),
          createdAt: existingThread.orderCreatedAt.toISOString(),
          addressLabel: existingThread.addressLabel || "",
          itemsSummary: Array.isArray(existingThread.itemsSummary) ? existingThread.itemsSummary : []
        });
        const adminName = String(adminUser?.name || "Admin").trim() || "Admin";
        const { thread, chatMessage } = await addOrderIssueMessage({
          orderId: order.id,
          senderType: "ADMIN",
          senderName: adminName,
          message: normalizedReplyMessage
        });
        const threadPayload = toOrderIssueThreadPayload(thread);
        if (!threadPayload) {
          throw new Error("N\xE3o foi poss\xEDvel atualizar a conversa do pedido.");
        }
        io.to(`restaurant:${order.restaurantId}:admin`).emit("order:issue-message", {
          ...threadPayload,
          message: chatMessage
        });
        io.to(`user:${order.userId}`).emit("order:issue-message", {
          ...threadPayload,
          message: chatMessage
        });
        return {
          ...threadPayload,
          lastMessage: chatMessage,
          info: "Resposta enviada para o cliente com sucesso."
        };
      }
    };
    ReplyOrderIssueService_default = new ReplyOrderIssueService();
  }
});

// src/modules/orders/controllers/ReplyOrderIssueController.ts
var ReplyOrderIssueController, ReplyOrderIssueController_default;
var init_ReplyOrderIssueController = __esm({
  "src/modules/orders/controllers/ReplyOrderIssueController.ts"() {
    init_ReplyOrderIssueService();
    ReplyOrderIssueController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { restaurantId, id: adminUserId } = req.user;
          const { message } = req.body || {};
          const result = await ReplyOrderIssueService_default.execute({
            orderId: id,
            restaurantId,
            adminUserId,
            replyMessage: message
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao responder problema do pedido"
          });
        }
      }
    };
    ReplyOrderIssueController_default = new ReplyOrderIssueController();
  }
});

// src/modules/orders/services/GetOrderIssueThreadService.ts
var GetOrderIssueThreadService, GetOrderIssueThreadService_default;
var init_GetOrderIssueThreadService = __esm({
  "src/modules/orders/services/GetOrderIssueThreadService.ts"() {
    init_prisma();
    init_orderIssueChatStore();
    GetOrderIssueThreadService = class {
      async execute({
        orderId,
        requesterUserId,
        requesterRole,
        requesterRestaurantId
      }) {
        const normalizedOrderId = Number(orderId);
        const normalizedUserId = Number(requesterUserId);
        const normalizedRestaurantId = Number(requesterRestaurantId || 0);
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new Error("Pedido inv\xE1lido para carregar conversa.");
        }
        if (!Number.isInteger(normalizedUserId) || normalizedUserId <= 0) {
          throw new Error("Usu\xE1rio inv\xE1lido para carregar conversa.");
        }
        const role = String(requesterRole || "").toUpperCase();
        const isAdmin = role === "ADMIN" || role === "SUPER_ADMIN";
        if (isAdmin) {
          if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
            throw new Error("Restaurante inv\xE1lido para carregar conversa.");
          }
          const order2 = await prisma_default.order.findFirst({
            where: {
              id: normalizedOrderId,
              restaurantId: normalizedRestaurantId
            },
            select: {
              id: true,
              userId: true,
              status: true,
              type: true,
              paymentMethod: true,
              total: true,
              createdAt: true,
              restaurantId: true,
              address: true,
              number: true,
              district: true,
              city: true,
              state: true,
              zipCode: true,
              user: {
                select: {
                  name: true,
                  phone: true
                }
              },
              items: {
                select: {
                  quantity: true,
                  product: {
                    select: {
                      name: true
                    }
                  }
                }
              }
            }
          });
          if (!order2) {
            throw new Error("Pedido n\xE3o encontrado para este restaurante.");
          }
          const thread2 = await getOrderIssueThread(order2.id);
          if (!thread2) {
            return {
              orderId: order2.id,
              isResolved: false,
              messages: []
            };
          }
          const payload2 = toOrderIssueThreadPayload(thread2);
          if (!payload2) {
            return {
              orderId: order2.id,
              isResolved: false,
              messages: []
            };
          }
          return payload2;
        }
        const order = await prisma_default.order.findFirst({
          where: {
            id: normalizedOrderId,
            userId: normalizedUserId
          },
          select: {
            id: true
          }
        });
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado para este usu\xE1rio.");
        }
        const thread = await getOrderIssueThread(order.id);
        if (!thread) {
          return {
            orderId: order.id,
            isResolved: false,
            messages: []
          };
        }
        const payload = toOrderIssueThreadPayload(thread);
        if (!payload) {
          return {
            orderId: order.id,
            isResolved: false,
            messages: []
          };
        }
        return payload;
      }
    };
    GetOrderIssueThreadService_default = new GetOrderIssueThreadService();
  }
});

// src/modules/orders/controllers/GetOrderIssueThreadController.ts
var GetOrderIssueThreadController, GetOrderIssueThreadController_default;
var init_GetOrderIssueThreadController = __esm({
  "src/modules/orders/controllers/GetOrderIssueThreadController.ts"() {
    init_GetOrderIssueThreadService();
    GetOrderIssueThreadController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { id: requesterUserId, role, restaurantId } = req.user;
          const result = await GetOrderIssueThreadService_default.execute({
            orderId: id,
            requesterUserId,
            requesterRole: role,
            requesterRestaurantId: restaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao carregar conversa do problema do pedido"
          });
        }
      }
    };
    GetOrderIssueThreadController_default = new GetOrderIssueThreadController();
  }
});

// src/modules/orders/services/ResolveOrderIssueService.ts
var ResolveOrderIssueService, ResolveOrderIssueService_default;
var init_ResolveOrderIssueService = __esm({
  "src/modules/orders/services/ResolveOrderIssueService.ts"() {
    init_prisma();
    init_server();
    init_orderIssueChatStore();
    ResolveOrderIssueService = class {
      async execute({
        orderId,
        adminUserId,
        restaurantId
      }) {
        const normalizedOrderId = Number(orderId);
        const normalizedAdminUserId = Number(adminUserId);
        const normalizedRestaurantId = Number(restaurantId || 0);
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new Error("Pedido inv\xE1lido para resolver conversa.");
        }
        if (!Number.isInteger(normalizedAdminUserId) || normalizedAdminUserId <= 0) {
          throw new Error("Admin inv\xE1lido para resolver conversa.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para resolver conversa.");
        }
        const [order, adminUser] = await Promise.all([
          prisma_default.order.findFirst({
            where: {
              id: normalizedOrderId,
              restaurantId: normalizedRestaurantId
            },
            select: {
              id: true,
              restaurantId: true,
              userId: true
            }
          }),
          prisma_default.user.findUnique({
            where: {
              id: normalizedAdminUserId
            },
            select: {
              name: true
            }
          })
        ]);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado para este restaurante.");
        }
        const resolvedByName = String(adminUser?.name || "Admin").trim() || "Admin";
        const thread = await resolveOrderIssueThread({
          orderId: order.id,
          resolvedByName
        });
        const threadPayload = toOrderIssueThreadPayload(thread);
        if (!threadPayload) {
          throw new Error("N\xE3o foi poss\xEDvel resolver a conversa do pedido.");
        }
        const resolvedPayload = {
          orderId: order.id,
          isResolved: true,
          resolvedAt: threadPayload.resolvedAt,
          resolvedByName
        };
        io.to(`restaurant:${order.restaurantId}:admin`).emit("order:issue-resolved", resolvedPayload);
        io.to(`user:${order.userId}`).emit("order:issue-resolved", resolvedPayload);
        return {
          ...threadPayload,
          info: "Problema marcado como resolvido e chat encerrado."
        };
      }
    };
    ResolveOrderIssueService_default = new ResolveOrderIssueService();
  }
});

// src/modules/orders/controllers/ResolveOrderIssueController.ts
var ResolveOrderIssueController, ResolveOrderIssueController_default;
var init_ResolveOrderIssueController = __esm({
  "src/modules/orders/controllers/ResolveOrderIssueController.ts"() {
    init_ResolveOrderIssueService();
    ResolveOrderIssueController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { restaurantId, id: adminUserId } = req.user;
          const result = await ResolveOrderIssueService_default.execute({
            orderId: id,
            restaurantId,
            adminUserId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao resolver problema do pedido"
          });
        }
      }
    };
    ResolveOrderIssueController_default = new ResolveOrderIssueController();
  }
});

// src/modules/orders/services/RefundOrderByAdminService.ts
import { OrderRefundStatus as OrderRefundStatus6, OrderStatus as OrderStatus16, UserRole as UserRole15 } from "@prisma/client";
var RefundOrderByAdminService, RefundOrderByAdminService_default;
var init_RefundOrderByAdminService = __esm({
  "src/modules/orders/services/RefundOrderByAdminService.ts"() {
    init_prisma();
    init_server();
    init_customerNotifier();
    init_OrderRepository();
    init_CancelOrderWorkflowService();
    init_waiterOrderRealtime();
    init_orderIssueChatStore();
    RefundOrderByAdminService = class {
      async execute({
        orderId,
        restaurantId,
        adminUserId
      }) {
        const normalizedOrderId = Number(orderId);
        const normalizedRestaurantId = Number(restaurantId || 0);
        const normalizedAdminUserId = Number(adminUserId);
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new OrderCancellationError("Pedido inv\xE1lido para estorno.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new OrderCancellationError("Restaurante inv\xE1lido para estorno.");
        }
        if (!Number.isInteger(normalizedAdminUserId) || normalizedAdminUserId <= 0) {
          throw new OrderCancellationError("Admin inv\xE1lido para estorno.");
        }
        const [order, adminUser, issueThread] = await Promise.all([
          OrderRepository_default.findById(normalizedOrderId, normalizedRestaurantId),
          prisma_default.user.findFirst({
            where: {
              id: normalizedAdminUserId,
              restaurantId: normalizedRestaurantId,
              role: UserRole15.ADMIN,
              active: true
            },
            select: {
              name: true
            }
          }),
          getOrderIssueThread(normalizedOrderId)
        ]);
        if (!order) {
          throw new OrderCancellationError("Pedido n\xE3o encontrado para este restaurante.");
        }
        if (!adminUser) {
          throw new OrderCancellationError("Admin sem permiss\xE3o para este restaurante.");
        }
        if (order.status === OrderStatus16.CANCELADO) {
          const alreadyRefunded = order.refundStatus === OrderRefundStatus6.SUCCEEDED;
          return {
            order,
            refunded: alreadyRefunded,
            info: alreadyRefunded ? "Este pedido j\xE1 est\xE1 cancelado e o estorno j\xE1 foi confirmado." : "Este pedido j\xE1 est\xE1 cancelado. Nenhum novo estorno foi solicitado.",
            issueThread: toOrderIssueThreadPayload(issueThread)
          };
        }
        if (order.status === OrderStatus16.ENTREGUE) {
          throw new OrderCancellationError(
            "Pedidos j\xE1 entregues n\xE3o podem ser cancelados ou estornados por este fluxo. Abra uma concilia\xE7\xE3o financeira."
          );
        }
        const hasOnlinePaymentToRefund = requiresAutomaticOrderRefund(order);
        const { order: updatedOrder, refunded } = await CancelOrderWorkflowService_default.execute(order);
        const resolvedByName = String(adminUser?.name || "Admin").trim() || "Admin";
        const resolvedThread = await resolveOrderIssueThread({
          orderId: order.id,
          resolvedByName
        }).catch(() => null);
        const threadPayload = toOrderIssueThreadPayload(resolvedThread);
        const resolvedPayload = threadPayload ? {
          orderId: order.id,
          isResolved: true,
          resolvedAt: threadPayload.resolvedAt,
          resolvedByName
        } : null;
        notifyCustomerOrderStatusChanged({
          restaurantId: normalizedRestaurantId,
          customerPhone: order?.user?.phone,
          customerName: order?.user?.name,
          restaurantName: order?.restaurant?.name,
          restaurantWhatsapp: order?.restaurant?.whatsapp,
          orderId: updatedOrder?.id,
          status: updatedOrder?.status
        }).catch((error2) => {
          console.error(
            "[CUSTOMER_STATUS_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        io.to(`restaurant:${normalizedRestaurantId}`).emit("order:status-changed", updatedOrder);
        if (order.userId) {
          io.to(`user:${order.userId}`).emit("order:status-changed", updatedOrder);
        }
        emitWaiterTableOrderEvent(io, "waiter:order-updated", updatedOrder);
        emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
        if (resolvedPayload) {
          io.to(`restaurant:${normalizedRestaurantId}:admin`).emit(
            "order:issue-resolved",
            resolvedPayload
          );
          if (order.userId) {
            io.to(`user:${order.userId}`).emit("order:issue-resolved", resolvedPayload);
          }
        }
        return {
          order: updatedOrder,
          refunded,
          info: refunded ? "Pagamento estornado e pedido cancelado com sucesso." : hasOnlinePaymentToRefund ? "Pedido cancelado, mas nenhum estorno online foi necess\xE1rio." : "Pedido cancelado com sucesso. Nenhum estorno online foi realizado.",
          issueThread: threadPayload
        };
      }
    };
    RefundOrderByAdminService_default = new RefundOrderByAdminService();
  }
});

// src/modules/orders/controllers/RefundOrderByAdminController.ts
var RefundOrderByAdminController, RefundOrderByAdminController_default;
var init_RefundOrderByAdminController = __esm({
  "src/modules/orders/controllers/RefundOrderByAdminController.ts"() {
    init_RefundOrderByAdminService();
    init_CancelOrderWorkflowService();
    RefundOrderByAdminController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { restaurantId, id: adminUserId } = req.user;
          const result = await RefundOrderByAdminService_default.execute({
            orderId: id,
            restaurantId,
            adminUserId
          });
          return res.status(200).json(result);
        } catch (error2) {
          const publicMessage = getPublicOrderCancellationErrorMessage(
            error2,
            "N\xE3o foi poss\xEDvel cancelar e estornar este pedido agora. Tente novamente ou contate o suporte."
          );
          if (publicMessage !== (error2 instanceof Error ? error2.message : "")) {
            console.error("[REFUND_ORDER_ADMIN_UNEXPECTED_ERROR]", {
              error: error2 instanceof Error ? error2.message : String(error2)
            });
          }
          return res.status(400).json({
            error: publicMessage
          });
        }
      }
    };
    RefundOrderByAdminController_default = new RefundOrderByAdminController();
  }
});

// src/modules/categories/repositories/CategoryRepository.ts
var CategoryRepository, CategoryRepository_default;
var init_CategoryRepository = __esm({
  "src/modules/categories/repositories/CategoryRepository.ts"() {
    init_prisma();
    CategoryRepository = class {
      async create(data, restaurantId, db = prisma_default) {
        return db.category.create({
          data: {
            ...data,
            restaurantId
          }
        });
      }
      async findAll(restaurantId, db = prisma_default) {
        return db.category.findMany({
          where: {
            restaurantId
          },
          orderBy: {
            name: "asc"
          }
        });
      }
      async findById(id, restaurantId, db = prisma_default) {
        return db.category.findFirst({
          where: {
            id: Number(id),
            restaurantId
          }
        });
      }
      async findByName(name, restaurantId, db = prisma_default) {
        return db.category.findFirst({
          where: {
            restaurantId,
            name: {
              equals: String(name || "").trim(),
              mode: "insensitive"
            }
          }
        });
      }
      async update(id, data, restaurantId, db = prisma_default) {
        return db.category.updateMany({
          where: {
            id: Number(id),
            restaurantId
          },
          data
        });
      }
      async delete(id, restaurantId, db = prisma_default) {
        const categoryId = Number(id);
        const hasProducts = await db.product.findFirst({
          where: {
            categoryId,
            restaurantId
          }
        });
        if (hasProducts) {
          throw new Error("N\xE3o \xE9 possivel excluir uma categoria que possui produtos!");
        }
        return db.category.deleteMany({
          where: {
            id: categoryId,
            restaurantId
          }
        });
      }
      async deleteAllByRestaurant(restaurantId, db = prisma_default) {
        return db.category.deleteMany({
          where: {
            restaurantId
          }
        });
      }
    };
    CategoryRepository_default = new CategoryRepository();
  }
});

// src/modules/orders/services/ClearOrdersAndCategoriesService.ts
var ClearOrdersAndCategoriesService, ClearOrdersAndCategoriesService_default;
var init_ClearOrdersAndCategoriesService = __esm({
  "src/modules/orders/services/ClearOrdersAndCategoriesService.ts"() {
    init_prisma();
    init_CategoryRepository();
    init_OrderRepository();
    ClearOrdersAndCategoriesService = class {
      async execute(restaurantId, now = /* @__PURE__ */ new Date()) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isFinite(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurant inv\xE1lido!");
        }
        await prisma_default.$transaction(async (db) => {
          const attachedRedemptions = await db.order.findMany({
            where: {
              restaurantId: normalizedRestaurantId,
              couponRedemptionId: { not: null }
            },
            select: { couponRedemptionId: true }
          });
          const redemptionIds = attachedRedemptions.map((order) => Number(order.couponRedemptionId || 0)).filter((id) => id > 0);
          if (redemptionIds.length > 0) {
            await db.couponRedemption.updateMany({
              where: {
                id: { in: redemptionIds },
                restaurantId: normalizedRestaurantId,
                status: "RESERVED",
                expiresAt: { gt: now }
              },
              data: {
                status: "CLAIMED",
                reservedAt: null,
                usedAt: null
              }
            });
            await db.couponRedemption.updateMany({
              where: {
                id: { in: redemptionIds },
                restaurantId: normalizedRestaurantId,
                status: "RESERVED",
                expiresAt: { lte: now }
              },
              data: {
                status: "EXPIRED",
                reservedAt: null,
                usedAt: null
              }
            });
          }
          await OrderRepository_default.deleteAllByRestaurant(normalizedRestaurantId, db);
          await CategoryRepository_default.deleteAllByRestaurant(normalizedRestaurantId, db);
        });
      }
    };
    ClearOrdersAndCategoriesService_default = new ClearOrdersAndCategoriesService();
  }
});

// src/modules/orders/controllers/ClearOrdersAndCategoriesController.ts
var ClearOrdersAndCategoriesController, ClearOrdersAndCategoriesController_default;
var init_ClearOrdersAndCategoriesController = __esm({
  "src/modules/orders/controllers/ClearOrdersAndCategoriesController.ts"() {
    init_ClearOrdersAndCategoriesService();
    ClearOrdersAndCategoriesController = class {
      async handle(req, res) {
        try {
          const isEnabled = process.env.NODE_ENV !== "production" && String(process.env.ENABLE_DESTRUCTIVE_CLEANUP || "false") === "true";
          const confirmation = String(req.body?.confirmation || "").trim();
          if (!isEnabled) {
            return res.status(404).json({ error: "Opera\xE7\xE3o n\xE3o dispon\xEDvel." });
          }
          if (confirmation !== "EXCLUIR TODOS OS PEDIDOS") {
            return res.status(400).json({
              error: 'Confirma\xE7\xE3o inv\xE1lida. Informe "EXCLUIR TODOS OS PEDIDOS".'
            });
          }
          await ClearOrdersAndCategoriesService_default.execute(req.user.restaurantId);
          return res.status(200).json({
            message: "Pedidos e categorias exclu\xEDdos com sucesso!"
          });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao excluir pedidos e categorias"
          });
        }
      }
    };
    ClearOrdersAndCategoriesController_default = new ClearOrdersAndCategoriesController();
  }
});

// src/modules/orders/services/FinalizeOrderCardPaymentService.ts
var FinalizeOrderCardPaymentService, FinalizeOrderCardPaymentService_default;
var init_FinalizeOrderCardPaymentService = __esm({
  "src/modules/orders/services/FinalizeOrderCardPaymentService.ts"() {
    init_server();
    init_prisma();
    init_customerNotifier();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    init_ReconcileLateCancelledPaymentService();
    init_waiterOrderRealtime();
    FinalizeOrderCardPaymentService = class {
      async execute({
        orderId,
        checkoutSessionId,
        restaurantId,
        allowMissingOrder = false
      }) {
        const normalizedCheckoutSessionId = String(checkoutSessionId || "").trim();
        const normalizedRestaurantId = Number(restaurantId || 0) || void 0;
        const order = orderId ? await OrderRepository_default.findById(orderId, Number(normalizedRestaurantId || 0)) : normalizedCheckoutSessionId ? await OrderRepository_default.findByCardCheckoutSessionId(
          normalizedCheckoutSessionId,
          normalizedRestaurantId
        ) : null;
        if (!order) {
          if (allowMissingOrder) {
            return null;
          }
          throw new Error("Pedido do cartao nao encontrado para esta sessao.");
        }
        if (String(order.status) === "CANCELADO" && order.paid !== true) {
          const paymentReference = normalizedCheckoutSessionId || String(order.cardCheckoutSessionId || "").trim();
          await ReconcileLateCancelledPaymentService_default.execute({
            orderId: order.id,
            restaurantId: order.restaurantId,
            paymentMethod: "CARTAO",
            paymentReference
          });
          return OrderRepository_default.findById(order.id, order.restaurantId);
        }
        if (order.paid === true) {
          return order;
        }
        let updatedOrder;
        try {
          updatedOrder = await prisma_default.$transaction(async (tx) => {
            const confirmedOrder = await OrderRepository_default.confirmPayment(
              order.id,
              order.restaurantId,
              tx
            );
            await markCouponRedemptionUsedForOrder(order.id, order.restaurantId, tx);
            return confirmedOrder;
          });
        } catch (error2) {
          const latest = await OrderRepository_default.findById(order.id, order.restaurantId);
          if (latest?.status === "CANCELADO" && latest.paid !== true) {
            await ReconcileLateCancelledPaymentService_default.execute({
              orderId: latest.id,
              restaurantId: latest.restaurantId,
              paymentMethod: "CARTAO",
              paymentReference: normalizedCheckoutSessionId || String(latest.cardCheckoutSessionId || "").trim()
            });
            return OrderRepository_default.findById(latest.id, latest.restaurantId);
          }
          throw error2;
        }
        io.to(`restaurant:${updatedOrder.restaurantId}`).emit("order:payment-confirmed", {
          orderId: updatedOrder.id,
          paid: true,
          paymentMethod: updatedOrder.paymentMethod
        });
        emitTableSessionOrderEvent(io, "order:payment-confirmed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("payment-confirmed", {
            orderId: updatedOrder.id,
            paid: true,
            paymentMethod: updatedOrder.paymentMethod,
            status: updatedOrder.status
          });
        }
        io.to(`restaurant:${updatedOrder.restaurantId}`).emit("new-order", updatedOrder);
        io.to(`restaurant:${updatedOrder.restaurantId}`).emit("order:status-changed", updatedOrder);
        if (updatedOrder.userId) {
          io.to(`user:${updatedOrder.userId}`).emit("order:status-changed", updatedOrder);
        }
        emitWaiterTableOrderEvent(io, "new-order", updatedOrder);
        emitTableSessionOrderEvent(io, "new-order", updatedOrder);
        emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
        notifyCustomerPaymentConfirmed({
          restaurantId: updatedOrder.restaurantId,
          customerPhone: updatedOrder?.user?.phone,
          customerName: updatedOrder?.user?.name,
          restaurantName: updatedOrder?.restaurant?.name,
          restaurantWhatsapp: updatedOrder?.restaurant?.whatsapp,
          orderId: updatedOrder?.id,
          total: updatedOrder?.total,
          paymentMethod: updatedOrder?.paymentMethod
        }).catch((error2) => {
          console.error(
            "[CUSTOMER_NOTIFICATION_UNHANDLED]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        return updatedOrder;
      }
    };
    FinalizeOrderCardPaymentService_default = new FinalizeOrderCardPaymentService();
  }
});

// src/modules/orders/services/FailPendingOrderPaymentService.ts
import { OrderStatus as OrderStatus17 } from "@prisma/client";
var FailPendingOrderPaymentService, FailPendingOrderPaymentService_default;
var init_FailPendingOrderPaymentService = __esm({
  "src/modules/orders/services/FailPendingOrderPaymentService.ts"() {
    init_prisma();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    init_restoreOrderItemsStock();
    FailPendingOrderPaymentService = class {
      async execute({
        orderId,
        restaurantId,
        pixPaymentId,
        cardCheckoutSessionId
      }) {
        const normalizedRestaurantId = Number(restaurantId || 0) || void 0;
        const normalizedPixPaymentId = String(pixPaymentId || "").trim();
        const normalizedCardSessionId = String(cardCheckoutSessionId || "").trim();
        if (!normalizedRestaurantId) {
          throw new Error("Restaurante obrigat\xF3rio para reconciliar falha de pagamento.");
        }
        const order = orderId ? await OrderRepository_default.findById(orderId, normalizedRestaurantId) : normalizedPixPaymentId ? await OrderRepository_default.findByPixPaymentId(
          normalizedPixPaymentId,
          normalizedRestaurantId
        ) : normalizedCardSessionId ? await OrderRepository_default.findByCardCheckoutSessionId(
          normalizedCardSessionId,
          normalizedRestaurantId
        ) : null;
        if (!order || order.paid === true || order.status === OrderStatus17.CANCELADO) {
          return order;
        }
        if (order.status !== OrderStatus17.PENDENTE) {
          throw new Error(
            "Falha de pagamento recebida para um pedido que j\xE1 avan\xE7ou na opera\xE7\xE3o."
          );
        }
        const cancelledOrder = await prisma_default.$transaction(async (tx) => {
          const updated = await OrderRepository_default.updateStatusIfCurrent(
            order.id,
            OrderStatus17.CANCELADO,
            order.restaurantId,
            { status: OrderStatus17.PENDENTE, paid: false },
            tx
          );
          await restoreOrderItemsStock(tx, order);
          await releaseCouponRedemptionForOrder(order.id, order.restaurantId, tx);
          return updated;
        });
        return cancelledOrder;
      }
    };
    FailPendingOrderPaymentService_default = new FailPendingOrderPaymentService();
  }
});

// src/modules/orders/controllers/MercadoPagoOrderWebhookController.ts
function parseMercadoPagoOrderReference(externalReference) {
  const match = /^order(pix|card):(\d+):(\d+)$/i.exec(String(externalReference || "").trim());
  if (!match) {
    return null;
  }
  const type = String(match[1] || "").toLowerCase();
  const firstId = Number(match[2] || 0);
  const secondId = Number(match[3] || 0);
  return {
    type,
    restaurantId: type === "pix" ? firstId : secondId,
    orderId: type === "pix" ? secondId : firstId
  };
}
var APPROVED_STATUSES, TERMINAL_UNPAID_STATUSES, MercadoPagoOrderWebhookController, MercadoPagoOrderWebhookController_default;
var init_MercadoPagoOrderWebhookController = __esm({
  "src/modules/orders/controllers/MercadoPagoOrderWebhookController.ts"() {
    init_FinalizeOrderPixPaymentService();
    init_FinalizeOrderCardPaymentService();
    init_mercadoPagoClient();
    init_OrderRepository();
    init_FailPendingOrderPaymentService();
    APPROVED_STATUSES = /* @__PURE__ */ new Set(["approved", "accredited", "paid"]);
    TERMINAL_UNPAID_STATUSES = /* @__PURE__ */ new Set(["cancelled", "rejected", "refunded", "charged_back"]);
    MercadoPagoOrderWebhookController = class {
      async handle(req, res) {
        try {
          const paymentId = req.body?.data?.id || req.body?.id || req.query?.id;
          const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
          const hintedRestaurantId = Number(req.query?.restaurantId || req.body?.restaurantId || 0);
          if (!paymentId) {
            return res.sendStatus(200);
          }
          if ((!Number.isInteger(hintedRestaurantId) || hintedRestaurantId <= 0) && !allowGlobalFallback) {
            return res.status(400).json({
              error: "restaurantId obrigatorio no webhook Mercado Pago para ambiente multi-tenant."
            });
          }
          const paymentApi = await getMercadoPagoPaymentApi(
            Number.isInteger(hintedRestaurantId) && hintedRestaurantId > 0 ? hintedRestaurantId : void 0
          );
          const response = await paymentApi.get({
            id: String(paymentId)
          });
          const payment = typeof response === "object" && response !== null ? response.body ?? response : {};
          const status = String(payment.status || "").toLowerCase();
          const externalReference = String(
            payment.external_reference || ""
          ).trim();
          const metadataRestaurantId = Number(
            payment.metadata?.restaurant_id || 0
          );
          const resolvedRestaurantId = Number.isInteger(hintedRestaurantId) && hintedRestaurantId > 0 ? hintedRestaurantId : Number.isInteger(metadataRestaurantId) && metadataRestaurantId > 0 ? metadataRestaurantId : void 0;
          const parsedReference = parseMercadoPagoOrderReference(externalReference);
          const referenceType = parsedReference?.type || "";
          const referenceRestaurantId = parsedReference?.restaurantId || 0;
          const referenceOrderId = parsedReference?.orderId || 0;
          if (parsedReference && (hintedRestaurantId > 0 && referenceRestaurantId !== hintedRestaurantId || metadataRestaurantId > 0 && referenceRestaurantId !== metadataRestaurantId)) {
            return res.status(400).json({
              error: "Webhook Mercado Pago rejeitado: restaurante da transa\xE7\xE3o n\xE3o confere."
            });
          }
          if (TERMINAL_UNPAID_STATUSES.has(status)) {
            if (parsedReference) {
              await FailPendingOrderPaymentService_default.execute({
                orderId: referenceOrderId,
                restaurantId: referenceRestaurantId
              });
            } else {
              await FailPendingOrderPaymentService_default.execute({
                restaurantId: resolvedRestaurantId,
                pixPaymentId: String(paymentId)
              });
            }
            return res.sendStatus(200);
          }
          if (!APPROVED_STATUSES.has(status)) {
            return res.sendStatus(200);
          }
          if (referenceType === "card") {
            const orderId = referenceOrderId;
            const normalizedPaymentId = String(paymentId || "").trim();
            if (!Number.isInteger(orderId) || orderId <= 0 || !Number.isInteger(referenceRestaurantId) || referenceRestaurantId <= 0) {
              return res.status(400).json({
                error: "Webhook Mercado Pago rejeitado: restaurante da transa\xE7\xE3o n\xE3o confere."
              });
            }
            if (normalizedPaymentId) {
              await OrderRepository_default.setCardCheckoutSessionId(
                orderId,
                referenceRestaurantId,
                `mp_pay:${normalizedPaymentId}`
              );
            }
            await FinalizeOrderCardPaymentService_default.execute({
              orderId,
              restaurantId: referenceRestaurantId,
              allowMissingOrder: true
            });
            return res.sendStatus(200);
          }
          await FinalizeOrderPixPaymentService_default.execute({
            orderId: referenceType === "pix" ? referenceOrderId : void 0,
            paymentId: String(paymentId),
            restaurantId: referenceType === "pix" ? referenceRestaurantId : resolvedRestaurantId,
            allowMissingOrder: true
          });
          return res.sendStatus(200);
        } catch (error2) {
          console.error(
            "[ORDER_PIX_WEBHOOK_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.sendStatus(500);
        }
      }
    };
    MercadoPagoOrderWebhookController_default = new MercadoPagoOrderWebhookController();
  }
});

// src/modules/orders/controllers/StripeOrderWebhookController.ts
import Stripe3 from "stripe";
var StripeOrderWebhookController, StripeOrderWebhookController_default;
var init_StripeOrderWebhookController = __esm({
  "src/modules/orders/controllers/StripeOrderWebhookController.ts"() {
    init_FinalizeOrderCardPaymentService();
    init_RestaurantSettingsRepository();
    init_FailPendingOrderPaymentService();
    StripeOrderWebhookController = class {
      async handle(req, res) {
        try {
          const allowInsecureWebhookInDev = process.env.ALLOW_INSECURE_STRIPE_WEBHOOK === "true";
          const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
          const stripeSignature = String(req.headers["stripe-signature"] || "").trim();
          const rawBody = Buffer.isBuffer(req.body) ? req.body : Buffer.from(JSON.stringify(req.body || {}), "utf-8");
          const untrustedPayload = JSON.parse(rawBody.toString("utf-8") || "{}");
          const untrustedRestaurantId = Number(
            untrustedPayload?.data?.object?.metadata?.restaurantId || 0
          );
          const settings = Number.isInteger(untrustedRestaurantId) && untrustedRestaurantId > 0 ? await RestaurantSettingsRepository_default.findByRestaurantId(untrustedRestaurantId) : null;
          const tenantWebhookSecret = String(settings?.stripeWebhookSecret || "").trim();
          const globalWebhookSecret = String(process.env.STRIPE_WEBHOOK_SECRET || "").trim();
          const stripeWebhookSecret = tenantWebhookSecret || (allowGlobalFallback || process.env.NODE_ENV !== "production" ? globalWebhookSecret : "");
          let eventPayload = {};
          if (stripeWebhookSecret) {
            if (!stripeSignature) {
              return res.status(400).json({
                error: "Assinatura Stripe ausente no webhook."
              });
            }
            const stripe = new Stripe3(process.env.STRIPE_SECRET_KEY || "");
            const event = stripe.webhooks.constructEvent(rawBody, stripeSignature, stripeWebhookSecret);
            eventPayload = event;
          } else {
            if (process.env.NODE_ENV === "production" && !allowInsecureWebhookInDev) {
              return res.status(503).json({
                error: "Webhook Stripe indisponivel sem STRIPE_WEBHOOK_SECRET em producao."
              });
            }
            eventPayload = untrustedPayload;
          }
          const eventType = String(eventPayload?.type || "").trim();
          const session = eventPayload?.data?.object || {};
          const sessionId = String(session?.id || "").trim();
          const paymentStatus = String(session?.payment_status || "").trim();
          const metadataOrderId = session?.metadata?.orderId || null;
          const metadataRestaurantId = Number(session?.metadata?.restaurantId || 0);
          if (!sessionId) {
            return res.sendStatus(200);
          }
          const allowedEventTypes = /* @__PURE__ */ new Set([
            "checkout.session.completed",
            "checkout.session.async_payment_succeeded"
          ]);
          const terminalFailureEventTypes = /* @__PURE__ */ new Set([
            "checkout.session.expired",
            "checkout.session.async_payment_failed"
          ]);
          if (!allowedEventTypes.has(eventType) && !terminalFailureEventTypes.has(eventType)) {
            return res.sendStatus(200);
          }
          if (!metadataOrderId || !Number.isInteger(metadataRestaurantId) || metadataRestaurantId <= 0) {
            return res.status(400).json({
              error: "Webhook Stripe invalido: metadata orderId/restaurantId obrigatoria."
            });
          }
          if (terminalFailureEventTypes.has(eventType)) {
            await FailPendingOrderPaymentService_default.execute({
              orderId: metadataOrderId,
              restaurantId: metadataRestaurantId
            });
            return res.sendStatus(200);
          }
          if (paymentStatus !== "paid") {
            return res.sendStatus(200);
          }
          await FinalizeOrderCardPaymentService_default.execute({
            orderId: metadataOrderId,
            checkoutSessionId: sessionId,
            restaurantId: metadataRestaurantId,
            allowMissingOrder: true
          });
          return res.sendStatus(200);
        } catch (error2) {
          console.error(
            "[ORDER_CARD_WEBHOOK_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.sendStatus(500);
        }
      }
    };
    StripeOrderWebhookController_default = new StripeOrderWebhookController();
  }
});

// src/modules/orders/controllers/PagBankOrderWebhookController.ts
function normalizeEnvironment() {
  return "production";
}
async function getPagBankCredentials2(restaurantId) {
  const allowGlobalFallback = process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK === "true";
  if (!restaurantId && !allowGlobalFallback) {
    throw new PagBankWebhookError(
      "Webhook PagBank sem restaurantId. Configure notificationURL com restaurantId.",
      400
    );
  }
  const settings = restaurantId ? await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId) : null;
  const settingsEmail = String(settings?.pagbankEmail || "").trim();
  const settingsToken = String(settings?.pagbankToken || "").trim();
  const globalEmail = String(process.env.PAGBANK_EMAIL || process.env.PAGSEGURO_EMAIL || "").trim();
  const globalToken = String(process.env.PAGBANK_TOKEN || process.env.PAGSEGURO_TOKEN || "").trim();
  const email = settingsEmail || (allowGlobalFallback ? globalEmail : "");
  const token = settingsToken || (allowGlobalFallback ? globalToken : "");
  const environment2 = normalizeEnvironment();
  if (!email || !token) {
    throw new PagBankWebhookError(
      "Webhook PagBank indisponivel. Configure email/token PagBank nas configuracoes do restaurante.",
      503
    );
  }
  return { email, token, environment: environment2 };
}
function resolvePagBankApiBaseUrl(environment2) {
  void environment2;
  return "https://ws.pagseguro.uol.com.br";
}
function extractXmlTagValue2(xml, tag) {
  const regex = new RegExp(`<${tag}>([^<]+)</${tag}>`, "i");
  const match = regex.exec(String(xml || ""));
  return String(match?.[1] || "").trim();
}
async function fetchPagBankTransactionByNotificationCode(notificationCode, restaurantId) {
  const { email, token, environment: environment2 } = await getPagBankCredentials2(restaurantId);
  const url = `${resolvePagBankApiBaseUrl(environment2)}/v3/transactions/notifications/${encodeURIComponent(notificationCode)}?email=${encodeURIComponent(email)}&token=${encodeURIComponent(token)}`;
  const response = await fetch(url, {
    method: "GET"
  });
  const responseText = await response.text();
  if (!response.ok) {
    const providerMessage = extractXmlTagValue2(responseText, "message") || extractXmlTagValue2(responseText, "error") || "Falha ao consultar notificacao no PagBank.";
    throw new PagBankWebhookError(`PagBank webhook: ${providerMessage}`, 502);
  }
  return {
    code: extractXmlTagValue2(responseText, "code"),
    status: extractXmlTagValue2(responseText, "status"),
    reference: extractXmlTagValue2(responseText, "reference")
  };
}
async function fetchPagBankTransactionByCode(transactionCode, restaurantId) {
  const { email, token, environment: environment2 } = await getPagBankCredentials2(restaurantId);
  const url = `${resolvePagBankApiBaseUrl(environment2)}/v3/transactions/${encodeURIComponent(transactionCode)}?email=${encodeURIComponent(email)}&token=${encodeURIComponent(token)}`;
  const response = await fetch(url, {
    method: "GET"
  });
  const responseText = await response.text();
  if (!response.ok) {
    const providerMessage = extractXmlTagValue2(responseText, "message") || extractXmlTagValue2(responseText, "error") || "Falha ao consultar transacao no PagBank.";
    throw new PagBankWebhookError(`PagBank webhook: ${providerMessage}`, 502);
  }
  return {
    code: extractXmlTagValue2(responseText, "code"),
    status: extractXmlTagValue2(responseText, "status"),
    reference: extractXmlTagValue2(responseText, "reference")
  };
}
var APPROVED_TRANSACTION_STATUSES, TERMINAL_TRANSACTION_STATUSES, TERMINAL_ORDER_STATUSES, PagBankWebhookError, PagBankOrderWebhookController, PagBankOrderWebhookController_default;
var init_PagBankOrderWebhookController = __esm({
  "src/modules/orders/controllers/PagBankOrderWebhookController.ts"() {
    init_FinalizeOrderCardPaymentService();
    init_FinalizeOrderPixPaymentService();
    init_RestaurantSettingsRepository();
    init_OrderRepository();
    init_OrderPixPaymentService();
    init_FailPendingOrderPaymentService();
    APPROVED_TRANSACTION_STATUSES = /* @__PURE__ */ new Set(["3", "4"]);
    TERMINAL_TRANSACTION_STATUSES = /* @__PURE__ */ new Set(["6", "7", "8"]);
    TERMINAL_ORDER_STATUSES = /* @__PURE__ */ new Set(["CANCELED", "CANCELLED", "DECLINED", "EXPIRED"]);
    PagBankWebhookError = class extends Error {
      statusCode;
      constructor(message, statusCode = 500) {
        super(message);
        this.statusCode = statusCode;
      }
    };
    PagBankOrderWebhookController = class {
      async handle(req, res) {
        try {
          const notificationCode = String(
            req.body?.notificationCode || req.query?.notificationCode || ""
          ).trim();
          const transactionCode = String(
            req.body?.transactionCode || req.body?.code || req.query?.transactionCode || req.query?.code || ""
          ).trim();
          const restaurantIdHint = Number(req.body?.restaurantId || req.query?.restaurantId || 0) || void 0;
          const pagBankOrderId = String(req.body?.id || req.body?.order?.id || "").trim();
          const referenceId = String(
            req.body?.reference_id || req.body?.order?.reference_id || ""
          ).trim();
          const chargeStatuses = [
            ...Array.isArray(req.body?.charges) ? req.body.charges : [],
            ...Array.isArray(req.body?.order?.charges) ? req.body.order.charges : []
          ].map((charge) => String(charge?.status || "").toUpperCase());
          const pixReference = /^orderpix:(\d+):(\d+)$/i.exec(referenceId);
          if (pagBankOrderId && pixReference) {
            const referenceRestaurantId = Number(pixReference[1]);
            const referenceOrderId = Number(pixReference[2]);
            if (restaurantIdHint && restaurantIdHint !== referenceRestaurantId) {
              return res.status(400).json({
                error: "Webhook PagBank rejeitado: restaurante da transa\xE7\xE3o n\xE3o confere."
              });
            }
            const paymentId = `pagbank:${pagBankOrderId}`;
            const providerStatus = await OrderPixPaymentService_default.getPaymentStatus({
              paymentId,
              restaurantId: referenceRestaurantId
            });
            const normalizedProviderStatus = String(providerStatus.status || "").toUpperCase();
            if (providerStatus.isApproved || chargeStatuses.includes("PAID")) {
              await FinalizeOrderPixPaymentService_default.execute({
                orderId: referenceOrderId,
                paymentId,
                restaurantId: referenceRestaurantId,
                allowMissingOrder: true
              });
            } else if (TERMINAL_ORDER_STATUSES.has(normalizedProviderStatus)) {
              await FailPendingOrderPaymentService_default.execute({
                orderId: referenceOrderId,
                restaurantId: referenceRestaurantId
              });
            }
            return res.sendStatus(200);
          }
          if (!restaurantIdHint && process.env.ALLOW_GLOBAL_PAYMENT_FALLBACK !== "true") {
            return res.status(400).json({
              error: "restaurantId obrigatorio no webhook PagBank para ambiente multi-tenant."
            });
          }
          if (!notificationCode && !transactionCode) {
            return res.sendStatus(200);
          }
          const details = notificationCode ? await fetchPagBankTransactionByNotificationCode(notificationCode, restaurantIdHint) : await fetchPagBankTransactionByCode(transactionCode, restaurantIdHint);
          const externalReference = String(details.reference || "").trim();
          const cardReference = /^ordercard:(\d+):(\d+)$/i.exec(externalReference);
          if (cardReference && restaurantIdHint && Number(cardReference[2]) !== restaurantIdHint) {
            return res.status(400).json({
              error: "Webhook PagBank rejeitado: restaurante da transa\xE7\xE3o n\xE3o confere."
            });
          }
          if (TERMINAL_TRANSACTION_STATUSES.has(String(details.status || "")) && cardReference) {
            await FailPendingOrderPaymentService_default.execute({
              orderId: Number(cardReference[1]),
              restaurantId: Number(cardReference[2])
            });
            return res.sendStatus(200);
          }
          if (!APPROVED_TRANSACTION_STATUSES.has(String(details.status || ""))) {
            return res.sendStatus(200);
          }
          if (externalReference.startsWith("ordercard:")) {
            const [, orderId = "", restaurantId = ""] = externalReference.split(":");
            const referenceRestaurantId = Number(restaurantId || 0);
            if (!Number.isInteger(referenceRestaurantId) || referenceRestaurantId <= 0 || restaurantIdHint && referenceRestaurantId !== restaurantIdHint) {
              return res.status(400).json({
                error: "Webhook PagBank rejeitado: restaurante da transa\xE7\xE3o n\xE3o confere."
              });
            }
            if (orderId) {
              if (details.code) {
                await OrderRepository_default.setCardCheckoutSessionId(
                  orderId,
                  referenceRestaurantId,
                  `pagbank_tx:${details.code}`
                );
              }
              await FinalizeOrderCardPaymentService_default.execute({
                orderId,
                restaurantId: referenceRestaurantId,
                allowMissingOrder: true
              });
            }
          }
          return res.sendStatus(200);
        } catch (error2) {
          const statusCode = error2 instanceof PagBankWebhookError ? error2.statusCode : 500;
          const message = error2 instanceof Error ? error2.message : "Erro interno no webhook PagBank.";
          console.error("[ORDER_CARD_PAGBANK_WEBHOOK_ERROR]", message);
          return res.status(statusCode).json({ error: message });
        }
      }
    };
    PagBankOrderWebhookController_default = new PagBankOrderWebhookController();
  }
});

// src/modules/orders/services/GetCurrentTableOrderService.ts
var GetCurrentTableOrderService, GetCurrentTableOrderService_default;
var init_GetCurrentTableOrderService = __esm({
  "src/modules/orders/services/GetCurrentTableOrderService.ts"() {
    init_OrderRepository();
    GetCurrentTableOrderService = class {
      async execute(input) {
        const tableSessionId = Number(input.tableSessionId || 0);
        const restaurantId = Number(input.restaurantId || 0);
        const participantId = Number(input.participantId || 0);
        if (!Number.isSafeInteger(tableSessionId) || tableSessionId <= 0 || !Number.isSafeInteger(restaurantId) || restaurantId <= 0 || !Number.isSafeInteger(participantId) || participantId <= 0) {
          return null;
        }
        return OrderRepository_default.findLatestByTableParticipant(
          tableSessionId,
          restaurantId,
          participantId
        );
      }
    };
    GetCurrentTableOrderService_default = new GetCurrentTableOrderService();
  }
});

// src/modules/orders/controllers/GetCurrentTableOrderController.ts
var GetCurrentTableOrderController, GetCurrentTableOrderController_default;
var init_GetCurrentTableOrderController = __esm({
  "src/modules/orders/controllers/GetCurrentTableOrderController.ts"() {
    init_GetCurrentTableOrderService();
    GetCurrentTableOrderController = class {
      async handle(req, res) {
        try {
          const order = await GetCurrentTableOrderService_default.execute({
            tableSessionId: req.tableSession?.id,
            restaurantId: req.tableSession?.restaurantId,
            participantId: req.tableParticipant?.id
          });
          res.setHeader("Cache-Control", "no-store");
          return res.status(200).json({ order });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao obter pedido atual da mesa"
          });
        }
      }
    };
    GetCurrentTableOrderController_default = new GetCurrentTableOrderController();
  }
});

// src/modules/orders/utils/deliveryReceiptConfirmation.ts
import { OrderStatus as OrderStatus18, OrderType as OrderType8, UserRole as UserRole16 } from "@prisma/client";
function canConfirmDeliveryReceipt(order, customerId2, role) {
  if (String(role).toUpperCase() !== UserRole16.CLIENTE) {
    throw new Error("Somente o cliente do pedido pode confirmar o recebimento.");
  }
  if (order.userId !== customerId2) {
    throw new Error("Pedido n\xE3o encontrado.");
  }
  if (order.type !== OrderType8.DELIVERY) {
    throw new Error("A confirma\xE7\xE3o de recebimento \xE9 exclusiva para pedidos de entrega.");
  }
  if (order.status !== OrderStatus18.ENTREGUE) {
    throw new Error("O pedido ainda n\xE3o foi marcado como entregue.");
  }
  return !order.deliveryConfirmedAt;
}
var init_deliveryReceiptConfirmation = __esm({
  "src/modules/orders/utils/deliveryReceiptConfirmation.ts"() {
  }
});

// src/modules/orders/services/ConfirmOrderDeliveryReceivedService.ts
import { UserRole as UserRole17 } from "@prisma/client";
var ConfirmOrderDeliveryReceivedService, ConfirmOrderDeliveryReceivedService_default;
var init_ConfirmOrderDeliveryReceivedService = __esm({
  "src/modules/orders/services/ConfirmOrderDeliveryReceivedService.ts"() {
    init_server();
    init_OrderRepository();
    init_deliveryReceiptConfirmation();
    ConfirmOrderDeliveryReceivedService = class {
      async execute({
        orderId,
        restaurantId,
        customerId: customerId2,
        role
      }) {
        const normalizedOrderId = Number(orderId);
        if (!Number.isInteger(normalizedOrderId) || normalizedOrderId <= 0) {
          throw new Error("Pedido inv\xE1lido.");
        }
        const isCustomer = String(role).toUpperCase() === UserRole17.CLIENTE;
        const order = isCustomer ? await OrderRepository_default.findByIdForCustomer(normalizedOrderId, customerId2) : await OrderRepository_default.findById(normalizedOrderId, restaurantId);
        if (!order) {
          throw new Error("Pedido n\xE3o encontrado.");
        }
        const shouldNotify = canConfirmDeliveryReceipt(order, customerId2, role);
        const updatedOrder = shouldNotify ? await OrderRepository_default.confirmDeliveryReceived(normalizedOrderId, order.restaurantId) : order;
        if (!updatedOrder) {
          throw new Error("N\xE3o foi poss\xEDvel confirmar o recebimento do pedido.");
        }
        if (shouldNotify) {
          const payload = {
            ...updatedOrder,
            deliveryConfirmedByCustomer: true
          };
          io.to(`restaurant:${restaurantId}`).emit("order:delivery-confirmed", payload);
          io.to(`restaurant:${restaurantId}`).emit("order:status-changed", payload);
          io.to(`user:${updatedOrder.userId}`).emit("order:delivery-confirmed", payload);
        }
        return updatedOrder;
      }
    };
    ConfirmOrderDeliveryReceivedService_default = new ConfirmOrderDeliveryReceivedService();
  }
});

// src/modules/orders/controllers/ConfirmOrderDeliveryReceivedController.ts
var ConfirmOrderDeliveryReceivedController, ConfirmOrderDeliveryReceivedController_default;
var init_ConfirmOrderDeliveryReceivedController = __esm({
  "src/modules/orders/controllers/ConfirmOrderDeliveryReceivedController.ts"() {
    init_ConfirmOrderDeliveryReceivedService();
    ConfirmOrderDeliveryReceivedController = class {
      async handle(req, res) {
        try {
          const order = await ConfirmOrderDeliveryReceivedService_default.execute({
            orderId: Array.isArray(req.params.id) ? req.params.id[0] : req.params.id,
            restaurantId: Number(req.user.restaurantId || 0),
            customerId: Number(req.user.id || 0),
            role: req.user.role
          });
          return res.status(200).json(order);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel confirmar o recebimento do pedido."
          });
        }
      }
    };
    ConfirmOrderDeliveryReceivedController_default = new ConfirmOrderDeliveryReceivedController();
  }
});

// src/modules/orders/controllers/QuoteOrderController.ts
import { OrderType as OrderType9 } from "@prisma/client";
import { z as z9 } from "zod";
var QuoteOrderController, QuoteOrderController_default;
var init_QuoteOrderController = __esm({
  "src/modules/orders/controllers/QuoteOrderController.ts"() {
    init_OrderPricingService();
    init_orderTenant();
    QuoteOrderController = class {
      async handle(req, res) {
        try {
          const { restaurantId, type, items, couponRedemptionId } = req.body;
          const resolvedRestaurantId = resolveOrderRestaurantId({
            requestedRestaurantId: restaurantId,
            contextRestaurantId: req.user?.restaurantId ?? req.tableSession?.restaurantId ?? null
          });
          const parsed = z9.object({
            type: z9.nativeEnum(OrderType9),
            couponRedemptionId: z9.number().int().positive().nullable().optional(),
            items: z9.array(
              z9.object({
                productId: z9.number().int().positive(),
                quantity: z9.number().int().positive(),
                observation: z9.string().trim().max(500).optional(),
                ingredientIds: z9.array(z9.number().int().positive()).max(40).optional(),
                optionIds: z9.array(z9.number().int().positive()).max(100).optional(),
                selectedOptions: z9.array(
                  z9.object({
                    groupId: z9.number().int().positive(),
                    optionIds: z9.array(z9.number().int().positive()).max(40)
                  })
                ).max(20).optional()
              })
            ).min(1)
          }).parse({ type, items, couponRedemptionId });
          const quote = await OrderPricingService_default.quote({
            restaurantId: resolvedRestaurantId,
            userId: req.user?.id,
            type: parsed.type,
            items: parsed.items,
            couponRedemptionId: parsed.couponRedemptionId
          });
          return res.status(200).json({
            itemsSubtotal: quote.itemsSubtotal,
            productDiscountTotal: quote.productDiscountTotal,
            couponDiscount: quote.couponDiscount,
            deliveryFeeAmount: quote.deliveryFeeAmount,
            total: quote.total,
            couponCode: quote.couponCode
          });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel calcular o pedido."
          });
        }
      }
    };
    QuoteOrderController_default = new QuoteOrderController();
  }
});

// src/middlewares/staffMiddleware.ts
import { UserRole as UserRole18 } from "@prisma/client";
function staffMiddleware(req, res, next) {
  if (!req.user) {
    return res.status(401).json({
      error: "N\xE3o autenticado"
    });
  }
  const allowedRoles2 = [UserRole18.ADMIN, UserRole18.FUNCIONARIO, UserRole18.MOTOQUEIRO];
  if (!allowedRoles2.includes(String(req.user.role))) {
    return res.status(403).json({
      error: "Acesso negado"
    });
  }
  return next();
}
var init_staffMiddleware = __esm({
  "src/middlewares/staffMiddleware.ts"() {
  }
});

// src/middlewares/optionalAuthMiddleware.ts
import jwt4 from "jsonwebtoken";
function optionalAuthMiddleware(req, res, next) {
  const authHeader = req.headers.authorization;
  if (!authHeader) {
    return next();
  }
  const [scheme, token] = authHeader.split(" ");
  if (scheme !== "Bearer" || !token) {
    return res.status(401).json({ error: "Token inv\xE1lido!" });
  }
  try {
    const decoded = jwt4.verify(token, process.env.JWT_SECRET);
    if (typeof decoded === "string") {
      return res.status(401).json({ error: "Token inv\xE1lido!" });
    }
    const userId = Number(decoded.id || 0);
    if (!Number.isInteger(userId) || userId <= 0) {
      return res.status(401).json({ error: "Token inv\xE1lido!" });
    }
    req.user = {
      id: userId,
      role: String(decoded.role || ""),
      subRole: decoded.subRole === null || decoded.subRole === void 0 ? null : String(decoded.subRole),
      restaurantId: decoded.restaurantId === null || decoded.restaurantId === void 0 ? null : Number(decoded.restaurantId),
      email: decoded.email === null || decoded.email === void 0 ? null : String(decoded.email)
    };
    return next();
  } catch {
    return res.status(401).json({ error: "Token inv\xE1lido!" });
  }
}
var init_optionalAuthMiddleware = __esm({
  "src/middlewares/optionalAuthMiddleware.ts"() {
  }
});

// src/middlewares/sessionMiddleware.ts
import { TableSessionStatus as TableSessionStatus3 } from "@prisma/client";
async function sessionMiddleware(req, res, next) {
  try {
    const rawSessionToken = req.headers["x-session-token"] || req.headers.authorization?.replace("Bearer ", "");
    const sessionToken = Array.isArray(rawSessionToken) ? rawSessionToken[0] : rawSessionToken;
    if (!sessionToken) {
      return res.status(401).json({
        error: "SessionToken n\xE3o informado"
      });
    }
    const session = await TableSessionRepository_default.findBySessionToken(sessionToken);
    if (!session) {
      return res.status(404).json({
        error: "Sess\xE3o n\xE3o encontrada"
      });
    }
    if (session.status !== TableSessionStatus3.OPEN) {
      return res.status(403).json({
        error: "Sess\xE3o encerrada"
      });
    }
    if (session.expiresAt && session.expiresAt.getTime() <= Date.now()) {
      return res.status(403).json({
        error: "Sess\xE3o da mesa expirada. Pe\xE7a ao gar\xE7om para abrir a mesa novamente."
      });
    }
    req.tableSession = {
      id: session.id,
      publicId: session.publicId,
      tableId: session.tableId,
      restaurantId: session.restaurantId,
      status: session.status
    };
    return next();
  } catch (error2) {
    return res.status(500).json({
      error: error2 instanceof Error ? error2.message : "Erro ao validar sessao"
    });
  }
}
var init_sessionMiddleware = __esm({
  "src/middlewares/sessionMiddleware.ts"() {
    init_TableSessionRepository();
  }
});

// src/modules/tableSession/repositories/TableParticipantRepository.ts
import { TableParticipantStatus as TableParticipantStatus2 } from "@prisma/client";
var participantSelect, TableParticipantRepository, TableParticipantRepository_default;
var init_TableParticipantRepository = __esm({
  "src/modules/tableSession/repositories/TableParticipantRepository.ts"() {
    init_prisma();
    participantSelect = {
      id: true,
      publicId: true,
      restaurantId: true,
      tableSessionId: true,
      userId: true,
      displayName: true,
      tokenExpiresAt: true,
      status: true,
      joinedAt: true,
      leftAt: true,
      revokedAt: true,
      user: {
        select: {
          id: true,
          name: true
        }
      }
    };
    TableParticipantRepository = class {
      async findGuestByTokenHash(guestTokenHash, tableSessionId, restaurantId, db = prisma_default) {
        return db.tableParticipant.findFirst({
          where: {
            guestTokenHash,
            tableSessionId,
            restaurantId,
            userId: null,
            status: TableParticipantStatus2.ACTIVE,
            revokedAt: null,
            tokenExpiresAt: { gt: /* @__PURE__ */ new Date() }
          },
          select: participantSelect
        });
      }
      async findByUser(userId, tableSessionId, restaurantId, db = prisma_default) {
        return db.tableParticipant.findFirst({
          where: {
            userId,
            tableSessionId,
            restaurantId,
            status: TableParticipantStatus2.ACTIVE,
            revokedAt: null
          },
          select: participantSelect
        });
      }
      async createGuest(data, db = prisma_default) {
        return db.tableParticipant.create({
          data,
          select: participantSelect
        });
      }
      async upsertAuthenticated(data, db = prisma_default) {
        return db.tableParticipant.upsert({
          where: {
            tableSessionId_userId: {
              tableSessionId: data.tableSessionId,
              userId: data.userId
            }
          },
          create: data,
          update: {
            displayName: data.displayName,
            status: TableParticipantStatus2.ACTIVE,
            leftAt: null,
            revokedAt: null
          },
          select: participantSelect
        });
      }
      async linkGuestToUser(guestParticipantId, userId, displayName, tableSessionId, restaurantId, db = prisma_default) {
        return db.tableParticipant.update({
          where: {
            id: guestParticipantId,
            tableSessionId,
            restaurantId,
            status: TableParticipantStatus2.ACTIVE,
            revokedAt: null
          },
          data: {
            userId,
            displayName,
            guestTokenHash: null,
            tokenExpiresAt: null
          },
          select: participantSelect
        });
      }
      async revoke(participantId, tableSessionId, restaurantId, db = prisma_default) {
        const now = /* @__PURE__ */ new Date();
        return db.tableParticipant.updateMany({
          where: {
            id: participantId,
            tableSessionId,
            restaurantId,
            status: TableParticipantStatus2.ACTIVE
          },
          data: {
            status: TableParticipantStatus2.LEFT,
            leftAt: now,
            revokedAt: now,
            tokenExpiresAt: now
          }
        });
      }
      async revokeActiveBySession(tableSessionId, restaurantId, db = prisma_default) {
        const now = /* @__PURE__ */ new Date();
        return db.tableParticipant.updateMany({
          where: {
            tableSessionId,
            restaurantId,
            status: TableParticipantStatus2.ACTIVE
          },
          data: {
            status: TableParticipantStatus2.LEFT,
            leftAt: now,
            revokedAt: now,
            tokenExpiresAt: now
          }
        });
      }
      async updateDisplayName(participantId, tableSessionId, restaurantId, displayName, db = prisma_default) {
        return db.tableParticipant.update({
          where: {
            id: participantId,
            tableSessionId,
            restaurantId,
            status: TableParticipantStatus2.ACTIVE,
            revokedAt: null
          },
          data: { displayName },
          select: participantSelect
        });
      }
      async attachUserToOwnedOrders(participantId, userId, tableSessionId, restaurantId, db = prisma_default) {
        return db.order.updateMany({
          where: {
            participantId,
            tableSessionId,
            restaurantId
          },
          data: { userId }
        });
      }
      async transferOwnedTableData(sourceParticipantId, targetParticipantId, targetUserId, tableSessionId, restaurantId, db = prisma_default) {
        const orders = await db.order.updateMany({
          where: {
            participantId: sourceParticipantId,
            tableSessionId,
            restaurantId
          },
          data: {
            participantId: targetParticipantId,
            userId: targetUserId
          }
        });
        const orderItems = await db.orderItem.updateMany({
          where: {
            participantId: sourceParticipantId,
            tableSessionId,
            restaurantId
          },
          data: { participantId: targetParticipantId }
        });
        const paymentIntents = await db.tablePaymentIntent.updateMany({
          where: {
            payerParticipantId: sourceParticipantId,
            tableSessionId,
            restaurantId
          },
          data: { payerParticipantId: targetParticipantId }
        });
        return {
          orders: orders.count,
          orderItems: orderItems.count,
          paymentIntents: paymentIntents.count
        };
      }
    };
    TableParticipantRepository_default = new TableParticipantRepository();
  }
});

// src/modules/tableSession/security/participantToken.ts
import crypto7 from "crypto";
function createParticipantToken() {
  return crypto7.randomBytes(32).toString("base64url");
}
function hashParticipantToken(token) {
  return crypto7.createHash("sha256").update(token, "utf8").digest("hex");
}
function isParticipantTokenShape(token) {
  return typeof token === "string" && /^[a-zA-Z0-9_-]{43}$/.test(token);
}
function getParticipantCookieName(sessionPublicId) {
  const safeSessionId = String(sessionPublicId || "").replace(/[^a-zA-Z0-9_-]/g, "");
  if (!safeSessionId) {
    throw new Error("Sess\xE3o p\xFAblica inv\xE1lida para identificar o participante.");
  }
  return `${PARTICIPANT_COOKIE_PREFIX}${safeSessionId}`;
}
function parseCookieHeader(cookieHeader) {
  if (!cookieHeader) return {};
  return cookieHeader.split(";").reduce((cookies, part) => {
    const separatorIndex = part.indexOf("=");
    if (separatorIndex <= 0) return cookies;
    const name = part.slice(0, separatorIndex).trim();
    const value = part.slice(separatorIndex + 1).trim();
    if (!name || !value) return cookies;
    try {
      cookies[name] = decodeURIComponent(value);
    } catch {
    }
    return cookies;
  }, {});
}
function resolveParticipantTokenExpiration(sessionExpiresAt, now = /* @__PURE__ */ new Date()) {
  const configuredHours = Number(
    process.env.TABLE_PARTICIPANT_TOKEN_HOURS || DEFAULT_PARTICIPANT_TOKEN_HOURS
  );
  const hours = Number.isFinite(configuredHours) ? Math.min(Math.max(configuredHours, 1), 24) : DEFAULT_PARTICIPANT_TOKEN_HOURS;
  const configuredExpiration = now.getTime() + hours * 60 * 60 * 1e3;
  const sessionExpiration = sessionExpiresAt?.getTime();
  return new Date(
    Number.isFinite(sessionExpiration) ? Math.min(configuredExpiration, Number(sessionExpiration)) : configuredExpiration
  );
}
function getParticipantCookieOptions(expires) {
  return {
    httpOnly: true,
    secure: process.env.NODE_ENV === "production",
    sameSite: "lax",
    path: "/",
    expires
  };
}
var PARTICIPANT_COOKIE_PREFIX, DEFAULT_PARTICIPANT_TOKEN_HOURS;
var init_participantToken = __esm({
  "src/modules/tableSession/security/participantToken.ts"() {
    PARTICIPANT_COOKIE_PREFIX = "table_participant_";
    DEFAULT_PARTICIPANT_TOKEN_HOURS = 12;
  }
});

// src/middlewares/tableParticipantMiddleware.ts
import { UserRole as UserRole19 } from "@prisma/client";
async function tableParticipantMiddleware(req, res, next) {
  try {
    const session = req.tableSession;
    if (!session) {
      return res.status(401).json({
        error: "Entre novamente na mesa pelo QR Code.",
        code: "TABLE_SESSION_REQUIRED"
      });
    }
    const authenticatedCustomerId = req.user?.role === UserRole19.CLIENTE && Number(req.user.id) > 0 ? Number(req.user.id) : null;
    let participant = authenticatedCustomerId ? await TableParticipantRepository_default.findByUser(
      authenticatedCustomerId,
      session.id,
      session.restaurantId
    ) : null;
    if (!participant) {
      const cookieName = getParticipantCookieName(session.publicId);
      const rawToken = parseCookieHeader(req.headers.cookie)[cookieName];
      if (isParticipantTokenShape(rawToken)) {
        participant = await TableParticipantRepository_default.findGuestByTokenHash(
          hashParticipantToken(rawToken),
          session.id,
          session.restaurantId
        );
      }
    }
    if (!participant) {
      return res.status(401).json({
        error: "Sua identifica\xE7\xE3o nesta mesa expirou. Leia o QR Code novamente.",
        code: "TABLE_PARTICIPANT_REQUIRED"
      });
    }
    req.tableParticipant = {
      id: participant.id,
      publicId: participant.publicId,
      tableSessionId: participant.tableSessionId,
      restaurantId: participant.restaurantId,
      userId: participant.userId,
      displayName: participant.displayName || participant.user?.name || null,
      authenticated: Boolean(participant.userId)
    };
    return next();
  } catch (error2) {
    return res.status(500).json({
      error: "N\xE3o foi poss\xEDvel validar sua identifica\xE7\xE3o nesta mesa.",
      ...process.env.NODE_ENV === "development" && error2 instanceof Error ? { detail: error2.message } : {}
    });
  }
}
var init_tableParticipantMiddleware = __esm({
  "src/middlewares/tableParticipantMiddleware.ts"() {
    init_TableParticipantRepository();
    init_participantToken();
  }
});

// src/middlewares/orderAccessMiddleware.ts
async function orderAccessMiddleware(req, res, next) {
  const authHeader = req.headers.authorization;
  const sessionToken = req.headers["x-session-token"];
  const bodyRestaurantId = Number(req.body?.restaurantId || 0);
  const orderType = String(req.body?.type || "").toUpperCase();
  if (orderType === "MESA" && !sessionToken) {
    return res.status(401).json({
      error: "Sess\xE3o da mesa n\xE3o informada. Acesse novamente pelo QR Code oficial."
    });
  }
  if (orderType === "MESA" && sessionToken) {
    return optionalAuthMiddleware(
      req,
      res,
      () => sessionMiddleware(req, res, () => {
        const requestedTableId = Number(req.body?.tableId || 0);
        if (requestedTableId > 0 && requestedTableId !== Number(req.tableSession.tableId)) {
          return res.status(403).json({
            error: "Sess\xE3o da mesa inv\xE1lida para este pedido."
          });
        }
        req.body.tableId = Number(req.tableSession.tableId);
        return tableParticipantMiddleware(req, res, next);
      })
    );
  }
  if (authHeader) {
    return authMiddleware(req, res, next);
  }
  if (sessionToken) {
    return sessionMiddleware(req, res, () => {
      if (orderType === "MESA") {
        const requestedTableId = Number(req.body?.tableId || 0);
        if (requestedTableId > 0 && requestedTableId !== Number(req.tableSession.tableId)) {
          return res.status(403).json({
            error: "Sess\xE3o da mesa inv\xE1lida para este pedido."
          });
        }
        req.body.tableId = Number(req.tableSession.tableId);
      }
      req.user = {
        id: null,
        restaurantId: req.tableSession.restaurantId,
        role: "CLIENTE"
      };
      return next();
    });
  }
  if (Number.isInteger(bodyRestaurantId) && bodyRestaurantId > 0) {
    req.user = {
      id: null,
      restaurantId: bodyRestaurantId,
      role: "CLIENTE",
      isGuest: true
    };
    return next();
  }
  return res.status(401).json({ error: "Token n\xE3o informado!" });
}
var init_orderAccessMiddleware = __esm({
  "src/middlewares/orderAccessMiddleware.ts"() {
    init_authMiddleware();
    init_optionalAuthMiddleware();
    init_sessionMiddleware();
    init_tableParticipantMiddleware();
  }
});

// src/middlewares/security/orderPaymentRateLimitMiddleware.ts
import rateLimit3, { ipKeyGenerator as ipKeyGenerator3 } from "express-rate-limit";
function getOrderActorKey(req) {
  const orderId = String(req.params.id || "").trim().slice(0, 32);
  const userId = String(req.user?.id || "anonymous").slice(0, 32);
  const ip = ipKeyGenerator3(String(req.ip || "unknown").trim());
  return `${ip}:${userId}:${orderId || "no-order"}`;
}
var paymentPinAttemptRateLimitMiddleware, paymentPinRequestRateLimitMiddleware;
var init_orderPaymentRateLimitMiddleware = __esm({
  "src/middlewares/security/orderPaymentRateLimitMiddleware.ts"() {
    paymentPinAttemptRateLimitMiddleware = rateLimit3({
      windowMs: Number(process.env.PAYMENT_PIN_RATE_LIMIT_WINDOW_MS || 10 * 60 * 1e3),
      max: Number(process.env.PAYMENT_PIN_RATE_LIMIT_MAX_REQUESTS || 8),
      standardHeaders: true,
      legacyHeaders: false,
      skipSuccessfulRequests: true,
      keyGenerator: getOrderActorKey,
      message: {
        error: "Muitas tentativas de PIN para este pedido. Aguarde alguns minutos."
      }
    });
    paymentPinRequestRateLimitMiddleware = rateLimit3({
      windowMs: Number(process.env.PAYMENT_PIN_REQUEST_RATE_LIMIT_WINDOW_MS || 60 * 1e3),
      max: Number(process.env.PAYMENT_PIN_REQUEST_RATE_LIMIT_MAX_REQUESTS || 3),
      standardHeaders: true,
      legacyHeaders: false,
      keyGenerator: getOrderActorKey,
      message: {
        error: "Muitas solicita\xE7\xF5es de PIN para este pedido. Aguarde um instante."
      }
    });
  }
});

// src/modules/orders/routes/orderRoutes.ts
import { Router as Router3 } from "express";
var router3, orderRoutes_default;
var init_orderRoutes = __esm({
  "src/modules/orders/routes/orderRoutes.ts"() {
    init_CreateOrderController();
    init_adminMiddleware();
    init_UpdateOrderStatusController();
    init_ClaimOrderForDeliveryController();
    init_GetCourierFinanceController();
    init_GetDeliveryTrackingController();
    init_ListOrdersController();
    init_GetOrderByIdController();
    init_ListMyOrdersController();
    init_CancelOrderController();
    init_CancelTableParticipantOrderController();
    init_ConfirmOrderPaymentController();
    init_ConfirmOrderPaymentWithPinController();
    init_GenerateOrderPaymentConfirmationPinController();
    init_RequestOrderPaymentConfirmationPinController();
    init_CreateOrderPixPaymentController();
    init_CreateOrderCardCheckoutController();
    init_GetOrderPixPaymentStatusController();
    init_ConfirmOrderPixPaymentController();
    init_ReportOrderIssueController();
    init_ReplyOrderIssueController();
    init_GetOrderIssueThreadController();
    init_ResolveOrderIssueController();
    init_RefundOrderByAdminController();
    init_ClearOrdersAndCategoriesController();
    init_MercadoPagoOrderWebhookController();
    init_StripeOrderWebhookController();
    init_PagBankOrderWebhookController();
    init_GetCurrentTableOrderController();
    init_ConfirmOrderDeliveryReceivedController();
    init_QuoteOrderController();
    init_staffMiddleware();
    init_billingMiddleware();
    init_orderAccessMiddleware();
    init_authMiddleware();
    init_optionalAuthMiddleware();
    init_sessionMiddleware();
    init_tableParticipantMiddleware();
    init_orderPaymentRateLimitMiddleware();
    router3 = Router3();
    router3.post("/webhook/mercadopago", MercadoPagoOrderWebhookController_default.handle);
    router3.post("/webhook/stripe", StripeOrderWebhookController_default.handle);
    router3.post("/webhook/pagbank", PagBankOrderWebhookController_default.handle);
    router3.post("/", orderAccessMiddleware, billingMiddleware, (req, res) => {
      CreateOrderController_default.handle(req, res);
    });
    router3.post("/quote", orderAccessMiddleware, billingMiddleware, (req, res) => {
      QuoteOrderController_default.handle(req, res);
    });
    router3.post("/pix/payment", orderAccessMiddleware, billingMiddleware, (req, res) => {
      CreateOrderPixPaymentController_default.handle(req, res);
    });
    router3.post("/card/checkout", orderAccessMiddleware, billingMiddleware, (req, res) => {
      CreateOrderCardCheckoutController_default.handle(req, res);
    });
    router3.post("/pix/payment/status", orderAccessMiddleware, billingMiddleware, (req, res) => {
      GetOrderPixPaymentStatusController_default.handle(req, res);
    });
    router3.post("/pix/payment/confirm", orderAccessMiddleware, billingMiddleware, (req, res) => {
      ConfirmOrderPixPaymentController_default.handle(req, res);
    });
    router3.put("/:id/status", authMiddleware, staffMiddleware, (req, res) => {
      UpdateOrderStatusController_default.handle(req, res);
    });
    router3.patch("/:id/claim-delivery", authMiddleware, (req, res) => {
      ClaimOrderForDeliveryController_default.handle(req, res);
    });
    router3.patch("/:id/confirm-delivery-received", authMiddleware, (req, res) => {
      ConfirmOrderDeliveryReceivedController_default.handle(req, res);
    });
    router3.patch("/:id/confirm-payment", authMiddleware, adminMiddleware, (req, res) => {
      ConfirmOrderPaymentController_default.handle(req, res);
    });
    router3.post("/:id/payment-confirmation-pin", authMiddleware, adminMiddleware, (req, res) => {
      GenerateOrderPaymentConfirmationPinController_default.handle(req, res);
    });
    router3.post(
      "/:id/request-payment-confirmation-pin",
      authMiddleware,
      staffMiddleware,
      paymentPinRequestRateLimitMiddleware,
      (req, res) => {
        RequestOrderPaymentConfirmationPinController_default.handle(req, res);
      }
    );
    router3.patch(
      "/:id/confirm-payment-with-pin",
      authMiddleware,
      staffMiddleware,
      paymentPinAttemptRateLimitMiddleware,
      (req, res) => {
        ConfirmOrderPaymentWithPinController_default.handle(req, res);
      }
    );
    router3.get("/", authMiddleware, staffMiddleware, (req, res) => {
      ListOrdersController_default.handle(req, res);
    });
    router3.get("/courier/finance", authMiddleware, (req, res) => {
      GetCourierFinanceController_default.handle(req, res);
    });
    router3.delete("/cleanup/orders-categories", authMiddleware, adminMiddleware, (req, res) => {
      ClearOrdersAndCategoriesController_default.handle(req, res);
    });
    router3.get("/my-orders", authMiddleware, (req, res) => {
      ListMyOrdersController_default.handle(req, res);
    });
    router3.get(
      "/table/current",
      optionalAuthMiddleware,
      sessionMiddleware,
      tableParticipantMiddleware,
      (req, res) => {
        GetCurrentTableOrderController_default.handle(req, res);
      }
    );
    router3.patch(
      "/table/:publicOrderId/cancel",
      optionalAuthMiddleware,
      sessionMiddleware,
      tableParticipantMiddleware,
      (req, res) => {
        CancelTableParticipantOrderController_default.handle(req, res);
      }
    );
    router3.get("/:id/tracking", authMiddleware, (req, res, next) => {
      GetDeliveryTrackingController_default.handle(req, res, next);
    });
    router3.get("/:id", authMiddleware, staffMiddleware, (req, res) => {
      GetOrderByIdController_default.handle(req, res);
    });
    router3.patch("/:id/cancel", authMiddleware, (req, res) => {
      CancelOrderController_default.handle(req, res);
    });
    router3.post("/:id/report-issue", authMiddleware, (req, res) => {
      ReportOrderIssueController_default.handle(req, res);
    });
    router3.get("/:id/issue-thread", authMiddleware, (req, res) => {
      GetOrderIssueThreadController_default.handle(req, res);
    });
    router3.post("/:id/reply-issue", authMiddleware, adminMiddleware, (req, res) => {
      ReplyOrderIssueController_default.handle(req, res);
    });
    router3.patch("/:id/resolve-issue", authMiddleware, adminMiddleware, (req, res) => {
      ResolveOrderIssueController_default.handle(req, res);
    });
    router3.patch("/:id/refund", authMiddleware, adminMiddleware, (req, res) => {
      RefundOrderByAdminController_default.handle(req, res);
    });
    orderRoutes_default = router3;
  }
});

// src/middlewares/superAdminMiddleware.ts
import { UserRole as UserRole20 } from "@prisma/client";
function superAdminMiddleware(req, res, next) {
  if (!req.user) {
    return res.status(401).json({ message: "N\xE3o autenticado" });
  }
  if (req.user.role !== UserRole20.SUPER_ADMIN) {
    return res.status(403).json({ message: "Acesso negado!" });
  }
  return next();
}
var init_superAdminMiddleware = __esm({
  "src/middlewares/superAdminMiddleware.ts"() {
  }
});

// src/modules/subscription/repositories/SubscriptionRepository.ts
var SubscriptionRepository, SubscriptionRepository_default;
var init_SubscriptionRepository = __esm({
  "src/modules/subscription/repositories/SubscriptionRepository.ts"() {
    init_prisma();
    SubscriptionRepository = class {
      async create(data, tx = prisma_default) {
        return tx.subscription.create({
          data
        });
      }
      async findByRestaurantId(restaurantId) {
        return prisma_default.subscription.findUnique({
          where: {
            restaurantId: Number(restaurantId)
          }
        });
      }
      async update(restaurantId, data, tx = prisma_default) {
        return tx.subscription.update({
          where: {
            restaurantId: Number(restaurantId)
          },
          data
        });
      }
    };
    SubscriptionRepository_default = new SubscriptionRepository();
  }
});

// src/validators/RestaurantValidator.ts
import { z as z10 } from "zod";
function normalizePhone(value) {
  return String(value || "").replace(/\D/g, "").trim();
}
function validateEmailDomainTypos(value) {
  const domain = String(value || "").split("@")[1]?.trim().toLowerCase();
  if (!domain) {
    return { valid: true };
  }
  const suggestion = COMMON_EMAIL_DOMAIN_TYPOS[domain];
  if (!suggestion) {
    return { valid: true };
  }
  return {
    valid: false,
    message: `Dom\xEDnio de email inv\xE1lido (${domain}). Voc\xEA quis dizer ${suggestion}?`
  };
}
var slugPattern, COMMON_EMAIL_DOMAIN_TYPOS, createRestaurantSchema;
var init_RestaurantValidator = __esm({
  "src/validators/RestaurantValidator.ts"() {
    slugPattern = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
    COMMON_EMAIL_DOMAIN_TYPOS = {
      "hotmali.com": "hotmail.com",
      "hotmai.com": "hotmail.com",
      "hotmal.com": "hotmail.com",
      "gmil.com": "gmail.com",
      "gmai.com": "gmail.com",
      "yahho.com": "yahoo.com",
      "outlok.com": "outlook.com"
    };
    createRestaurantSchema = z10.object({
      plan: z10.enum(["BASICO", "PREMIUM"], {
        errorMap: () => ({ message: "Escolha o plano B\xE1sico ou Premium." })
      }),
      restaurant: z10.object({
        name: z10.string().trim().min(2, "Nome do restaurante deve ter no m\xEDnimo 2 caracteres!").max(120, "Nome do restaurante muito longo!"),
        slug: z10.string().trim().toLowerCase().min(3, "Slug deve ter no m\xEDnimo 3 caracteres!").max(60, "Slug muito longo!").regex(slugPattern, "Slug inv\xE1lido! Use apenas letras min\xFAsculas, n\xFAmeros e h\xEDfen."),
        email: z10.string().trim().toLowerCase().email("Email do restaurante inv\xE1lido!").superRefine((value, context) => {
          const result = validateEmailDomainTypos(value);
          if (!result.valid) {
            context.addIssue({
              code: z10.ZodIssueCode.custom,
              message: result.message
            });
          }
        }),
        phone: z10.string().optional().transform((value) => normalizePhone(value || "")).refine((value) => !value || /^\d{10,11}$/.test(value), "Telefone do restaurante inv\xE1lido!"),
        whatsapp: z10.string().optional(),
        cnpj: z10.string().optional(),
        logo: z10.string().optional(),
        coverImage: z10.string().optional(),
        description: z10.string().optional(),
        address: z10.string().trim().optional(),
        city: z10.string().trim().optional().refine((value) => !value || value.length >= 2, "Cidade deve ter no m\xEDnimo 2 caracteres!"),
        state: z10.string().trim().toUpperCase().optional().refine(
          (value) => !value || /^[A-Z]{2}$/.test(value),
          "Estado deve conter exatamente 2 letras."
        ),
        zipCode: z10.string().optional(),
        openingHours: z10.string().optional()
      }),
      admin: z10.object({
        name: z10.string().trim().min(2, "Nome do admin deve ter no m\xEDnimo 2 caracteres!").max(120, "Nome do admin muito longo!"),
        email: z10.string().trim().toLowerCase().email("Email do admin inv\xE1lido!").superRefine((value, context) => {
          const result = validateEmailDomainTypos(value);
          if (!result.valid) {
            context.addIssue({
              code: z10.ZodIssueCode.custom,
              message: result.message
            });
          }
        }),
        password: z10.string().min(6, "Senha deve ter no m\xEDnimo 6 caracteres!").max(72, "Senha muito longa!")
      })
    });
  }
});

// src/modules/restaurants/services/CreateRestaurantService.ts
import bcrypt8 from "bcrypt";
import { SubscriptionStatus, UserRole as UserRole21 } from "@prisma/client";
function requireDefined2(value, message) {
  if (value === null || value === void 0) {
    throw new Error(message);
  }
  return value;
}
var CreateRestaurantService, CreateRestaurantService_default;
var init_CreateRestaurantService = __esm({
  "src/modules/restaurants/services/CreateRestaurantService.ts"() {
    init_RestaurantRepository();
    init_UserRepository();
    init_SubscriptionRepository();
    init_prisma();
    init_RestaurantValidator();
    CreateRestaurantService = class {
      async execute({ restaurant, admin, plan }) {
        const parsedPayloadResult = createRestaurantSchema.safeParse({
          restaurant,
          admin,
          plan
        });
        if (!parsedPayloadResult.success) {
          const firstIssue = parsedPayloadResult.error.issues[0];
          throw new Error(firstIssue?.message || "Dados inv\xE1lidos para cadastro.");
        }
        const parsedPayload = parsedPayloadResult.data;
        const parsedRestaurant = parsedPayload.restaurant;
        const parsedAdmin = parsedPayload.admin;
        const restaurantExists = await RestaurantRepository_default.findByEmail(parsedRestaurant.email);
        if (restaurantExists) {
          throw new Error("J\xE1 existe um restaurante com esse e-mail.");
        }
        const slugExists = await RestaurantRepository_default.findBySlug(parsedRestaurant.slug);
        if (slugExists) {
          throw new Error("Esse slug j\xE1 existe. Escolha outro.");
        }
        const userExists = await UserRepository_default.findByEmail(parsedAdmin.email);
        if (userExists) {
          throw new Error("J\xE1 existe um admin com esse e-mail.");
        }
        return prisma_default.$transaction(async (tx) => {
          const requiredName = requireDefined2(
            parsedRestaurant.name,
            "Nome do restaurante \xE9 obrigat\xF3rio."
          );
          const requiredSlug = requireDefined2(
            parsedRestaurant.slug,
            "Slug do restaurante \xE9 obrigat\xF3rio."
          );
          const requiredEmail = requireDefined2(
            parsedRestaurant.email,
            "Email do restaurante \xE9 obrigat\xF3rio."
          );
          const restaurantCreateData = {
            ...parsedRestaurant,
            name: requiredName,
            slug: requiredSlug,
            email: requiredEmail
          };
          const createdRestaurant = await RestaurantRepository_default.create(restaurantCreateData, tx);
          const passwordHash = await bcrypt8.hash(parsedAdmin.password, 10);
          const createdAdmin = await UserRepository_default.create(
            {
              name: parsedAdmin.name,
              email: parsedAdmin.email,
              password: passwordHash,
              role: UserRole21.ADMIN,
              active: true,
              mustChangePassword: true,
              restaurantId: createdRestaurant.id
            },
            tx
          );
          const TRIAL_DAYS = 30;
          const today = /* @__PURE__ */ new Date();
          const trialEndsAt = new Date(today);
          trialEndsAt.setDate(trialEndsAt.getDate() + TRIAL_DAYS);
          await SubscriptionRepository_default.create(
            {
              restaurantId: createdRestaurant.id,
              plan: parsedPayload.plan,
              status: SubscriptionStatus.TESTE,
              trialEndsAt,
              currentPeriodStart: today,
              currentPeriodEnd: trialEndsAt
            },
            tx
          );
          return {
            restaurant: createdRestaurant,
            admin: {
              id: createdAdmin.id,
              name: createdAdmin.name,
              email: createdAdmin.email
            }
          };
        });
      }
    };
    CreateRestaurantService_default = new CreateRestaurantService();
  }
});

// src/modules/restaurants/controllers/CreateRestaurantController.ts
var CreateRestaurantController, CreateRestaurantController_default;
var init_CreateRestaurantController = __esm({
  "src/modules/restaurants/controllers/CreateRestaurantController.ts"() {
    init_CreateRestaurantService();
    CreateRestaurantController = class {
      async handle(req, res) {
        try {
          const { restaurant, admin, plan } = req.body;
          const result = await CreateRestaurantService_default.execute({
            restaurant,
            admin,
            plan
          });
          return res.status(201).json(result);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao criar restaurante"
          });
        }
      }
    };
    CreateRestaurantController_default = new CreateRestaurantController();
  }
});

// src/modules/restaurants/services/ListRestaurantsService.ts
function getRestaurantStatus(restaurant) {
  if (!restaurant.active) {
    return "Bloqueado";
  }
  if (restaurant.subscription?.status === "EXPIRADA" || restaurant.subscription?.status === "CANCELADA") {
    return "Expirado";
  }
  if (!restaurant.subscription || restaurant.subscription?.status === "TESTE") {
    return "Aviso";
  }
  return "Ativo";
}
var PLAN_PRICES, ListRestaurantsService, ListRestaurantsService_default;
var init_ListRestaurantsService = __esm({
  "src/modules/restaurants/services/ListRestaurantsService.ts"() {
    init_prisma();
    init_RestaurantRepository();
    init_planConfig();
    PLAN_PRICES = {
      BASICO: PLAN_CONFIG.BASICO.monthlyFee,
      PREMIUM: PLAN_CONFIG.PREMIUM.monthlyFee
    };
    ListRestaurantsService = class {
      async execute() {
        const restaurants = await RestaurantRepository_default.listAll();
        const now = /* @__PURE__ */ new Date();
        const periodStart = new Date(now.getFullYear(), now.getMonth(), 1);
        const periodEnd = new Date(now.getFullYear(), now.getMonth() + 1, 1);
        const revenueByRestaurant = await prisma_default.order.groupBy({
          by: ["restaurantId"],
          where: {
            paid: true,
            status: { not: "CANCELADO" },
            createdAt: {
              gte: periodStart,
              lt: periodEnd
            }
          },
          _sum: {
            total: true
          }
        });
        const revenueMap = new Map(
          revenueByRestaurant.map((item) => [item.restaurantId, Number(item._sum.total || 0)])
        );
        return restaurants.map((restaurant) => ({
          id: restaurant.id,
          name: restaurant.name,
          slug: restaurant.slug,
          email: restaurant.email,
          city: restaurant.city,
          state: restaurant.state,
          cnpj: restaurant.cnpj,
          active: restaurant.active,
          createdAt: restaurant.createdAt,
          owner: restaurant.users?.[0] || null,
          subscription: restaurant.subscription || null,
          status: getRestaurantStatus(restaurant),
          uptime: restaurant.active ? 100 : 0,
          price: restaurant.subscription?.plan ? PLAN_PRICES[restaurant.subscription.plan] : 0,
          revenue: revenueMap.get(restaurant.id) || 0,
          nextBillingAt: restaurant.subscription?.currentPeriodEnd ?? null
        }));
      }
    };
    ListRestaurantsService_default = new ListRestaurantsService();
  }
});

// src/modules/restaurants/controllers/ListRestaurantsController.ts
var ListRestaurantsController, ListRestaurantsController_default;
var init_ListRestaurantsController = __esm({
  "src/modules/restaurants/controllers/ListRestaurantsController.ts"() {
    init_ListRestaurantsService();
    ListRestaurantsController = class {
      async handle(req, res) {
        try {
          const restaurants = await ListRestaurantsService_default.execute();
          return res.status(200).json(restaurants);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao listar restaurantes"
          });
        }
      }
    };
    ListRestaurantsController_default = new ListRestaurantsController();
  }
});

// src/modules/restaurants/services/GetRestaurantsMetricsService.ts
function last6Months() {
  return Array.from({ length: 6 }, (_, i) => {
    const d = /* @__PURE__ */ new Date();
    d.setDate(1);
    d.setMonth(d.getMonth() - (5 - i));
    const start = new Date(d.getFullYear(), d.getMonth(), 1);
    const end = new Date(d.getFullYear(), d.getMonth() + 1, 0, 23, 59, 59);
    const label = d.toLocaleString("pt-BR", { month: "short" });
    return { start, end, label };
  });
}
var GetRestaurantsMetricsService, GetRestaurantsMetricsService_default;
var init_GetRestaurantsMetricsService = __esm({
  "src/modules/restaurants/services/GetRestaurantsMetricsService.ts"() {
    init_prisma();
    GetRestaurantsMetricsService = class {
      async execute() {
        const months = last6Months();
        const [restaurants, paidOrders, invoices] = await Promise.all([
          prisma_default.restaurant.findMany({
            select: { id: true, active: true }
          }),
          prisma_default.order.findMany({
            where: { paid: true, status: { not: "CANCELADO" } },
            select: { total: true, systemFee: true }
          }),
          prisma_default.invoice.findMany({
            where: { status: { in: ["PENDENTE", "ATRASADO"] } },
            select: { id: true, total: true }
          })
        ]);
        const [monthlyGrowth, monthlyRevenue] = await Promise.all([
          Promise.all(
            months.map(async ({ end, label }) => {
              const count = await prisma_default.restaurant.count({
                where: { active: true, createdAt: { lte: end } }
              });
              return { label, count };
            })
          ),
          Promise.all(
            months.map(async ({ start, end, label }) => {
              const result = await prisma_default.order.aggregate({
                where: {
                  paid: true,
                  status: { not: "CANCELADO" },
                  createdAt: { gte: start, lte: end }
                },
                _sum: { systemFee: true }
              });
              return { label, value: Number(result._sum.systemFee || 0) };
            })
          )
        ]);
        const totalGenerated = paidOrders.reduce((acc, o) => acc + Number(o.total || 0), 0);
        const totalReceivable = paidOrders.reduce((acc, o) => acc + Number(o.systemFee || 0), 0);
        const pendingInvoiceTotal = invoices.reduce((acc, i) => acc + Number(i.total || 0), 0);
        const activeRestaurants = restaurants.filter((r) => r.active).length;
        return {
          restaurantsTotal: restaurants.length,
          restaurantsActive: activeRestaurants,
          restaurantsInactive: restaurants.length - activeRestaurants,
          totalGenerated,
          totalReceivable,
          pendingInvoicesCount: invoices.length,
          pendingInvoicesTotal: pendingInvoiceTotal,
          monthlyGrowth,
          monthlyRevenue
        };
      }
    };
    GetRestaurantsMetricsService_default = new GetRestaurantsMetricsService();
  }
});

// src/modules/restaurants/controllers/GetRestaurantsMetricsController.ts
var GetRestaurantsMetricsController, GetRestaurantsMetricsController_default;
var init_GetRestaurantsMetricsController = __esm({
  "src/modules/restaurants/controllers/GetRestaurantsMetricsController.ts"() {
    init_GetRestaurantsMetricsService();
    GetRestaurantsMetricsController = class {
      async handle(req, res) {
        try {
          const metrics = await GetRestaurantsMetricsService_default.execute();
          return res.status(200).json(metrics);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao buscar metricas de restaurantes"
          });
        }
      }
    };
    GetRestaurantsMetricsController_default = new GetRestaurantsMetricsController();
  }
});

// src/modules/restaurants/routes/restaurantRoutes.ts
import { Router as Router4 } from "express";
var router4, restaurantRoutes_default;
var init_restaurantRoutes = __esm({
  "src/modules/restaurants/routes/restaurantRoutes.ts"() {
    init_authMiddleware();
    init_superAdminMiddleware();
    init_CreateRestaurantController();
    init_ListRestaurantsController();
    init_GetRestaurantsMetricsController();
    router4 = Router4();
    router4.get("/", authMiddleware, superAdminMiddleware, (req, res) => {
      ListRestaurantsController_default.handle(req, res);
    });
    router4.get("/metrics", authMiddleware, superAdminMiddleware, (req, res) => {
      GetRestaurantsMetricsController_default.handle(req, res);
    });
    router4.post("/", authMiddleware, superAdminMiddleware, (req, res) => {
      CreateRestaurantController_default.handle(req, res);
    });
    restaurantRoutes_default = router4;
  }
});

// src/validators/CategoryValidator.ts
import { z as z11 } from "zod";
var createCategorySchema;
var init_CategoryValidator = __esm({
  "src/validators/CategoryValidator.ts"() {
    createCategorySchema = z11.object({
      name: z11.string().trim().min(1, "Nome \xE9 obrigat\xF3rio!").max(50, "Nome deve ter no m\xE1ximo 50 caracteres."),
      description: z11.string().trim().max(255, "Descri\xE7\xE3o deve ter no m\xE1ximo 255 caracteres.").optional(),
      image: z11.string().trim().optional(),
      active: z11.boolean().optional()
    });
  }
});

// src/modules/categories/services/CreateCategoryService.ts
var CreateCategoryService, CreateCategoryService_default;
var init_CreateCategoryService = __esm({
  "src/modules/categories/services/CreateCategoryService.ts"() {
    init_CategoryRepository();
    init_CategoryValidator();
    CreateCategoryService = class {
      async execute(data, restaurantId) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!normalizedRestaurantId) {
          throw new Error("Restaurante n\xE3o encontrado");
        }
        const parsed = createCategorySchema.parse(data);
        const normalizedName = String(parsed.name || "").trim();
        const existingCategory = await CategoryRepository_default.findByName(
          normalizedName,
          normalizedRestaurantId
        );
        if (existingCategory) {
          throw new Error("J\xE1 existe uma categoria com esse nome.");
        }
        const category = await CategoryRepository_default.create(
          {
            ...parsed,
            name: normalizedName
          },
          normalizedRestaurantId
        );
        return {
          category
        };
      }
    };
    CreateCategoryService_default = new CreateCategoryService();
  }
});

// src/modules/categories/controllers/CreateCategoryController.ts
var CreateCategoryController, CreateCategoryController_default;
var init_CreateCategoryController = __esm({
  "src/modules/categories/controllers/CreateCategoryController.ts"() {
    init_CreateCategoryService();
    CreateCategoryController = class {
      async handle(req, res) {
        try {
          const { name, description, image, active } = req.body;
          const category = await CreateCategoryService_default.execute(
            {
              name,
              description,
              image,
              active
            },
            req.user.restaurantId
          );
          return res.status(201).json(category);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar categoria"
          });
        }
      }
    };
    CreateCategoryController_default = new CreateCategoryController();
  }
});

// src/modules/categories/services/DeleteCategoryService.ts
var DeleteCategoryService, DeleteCategoryService_default;
var init_DeleteCategoryService = __esm({
  "src/modules/categories/services/DeleteCategoryService.ts"() {
    init_CategoryRepository();
    DeleteCategoryService = class {
      async execute(id, restaurantId) {
        const normalizedRestaurantId = Number(restaurantId);
        const category = await CategoryRepository_default.findById(id, normalizedRestaurantId);
        if (!category) {
          throw new Error("Categoria n\xE3o encontrada!");
        }
        await CategoryRepository_default.delete(id, normalizedRestaurantId);
      }
    };
    DeleteCategoryService_default = new DeleteCategoryService();
  }
});

// src/modules/categories/controllers/DeleteCategoryController.ts
var DeleteCategoryController, DeleteCategoryController_default;
var init_DeleteCategoryController = __esm({
  "src/modules/categories/controllers/DeleteCategoryController.ts"() {
    init_DeleteCategoryService();
    DeleteCategoryController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          await DeleteCategoryService_default.execute(id, req.user.restaurantId);
          return res.status(200).json({ message: "Categoria deletada com sucesso!" });
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao deletar categoria"
          });
        }
      }
    };
    DeleteCategoryController_default = new DeleteCategoryController();
  }
});

// src/modules/categories/services/UpdateCategoryService.ts
var UpdateCategoryService, UpdateCategoryService_default;
var init_UpdateCategoryService = __esm({
  "src/modules/categories/services/UpdateCategoryService.ts"() {
    init_CategoryRepository();
    init_CategoryValidator();
    UpdateCategoryService = class {
      async execute(id, data, restaurantId) {
        const normalizedRestaurantId = Number(restaurantId);
        const parsedData = createCategorySchema.partial().parse(data);
        const category = await CategoryRepository_default.findById(id, normalizedRestaurantId);
        if (!category) {
          throw new Error("Categoria n\xE3o encontrada!");
        }
        const hasNameUpdate = Object.prototype.hasOwnProperty.call(parsedData, "name");
        if (hasNameUpdate) {
          const normalizedName = String(parsedData.name || "").trim();
          if (!normalizedName) {
            throw new Error("Nome da categoria inv\xE1lido.");
          }
          const existingCategory = await CategoryRepository_default.findByName(
            normalizedName,
            normalizedRestaurantId
          );
          if (existingCategory && Number(existingCategory.id) !== Number(id)) {
            throw new Error("J\xE1 existe uma categoria com esse nome.");
          }
          parsedData.name = normalizedName;
        }
        return CategoryRepository_default.update(id, parsedData, normalizedRestaurantId);
      }
    };
    UpdateCategoryService_default = new UpdateCategoryService();
  }
});

// src/modules/categories/controllers/UpdateCategoryController.ts
var UpdateCategoryController, UpdateCategoryController_default;
var init_UpdateCategoryController = __esm({
  "src/modules/categories/controllers/UpdateCategoryController.ts"() {
    init_UpdateCategoryService();
    UpdateCategoryController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const data = req.body;
          const category = await UpdateCategoryService_default.execute(id, data, req.user.restaurantId);
          return res.status(200).json(category);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar categoria"
          });
        }
      }
    };
    UpdateCategoryController_default = new UpdateCategoryController();
  }
});

// src/modules/categories/services/ListCategoryService.ts
var ListCategoryService, ListCategoryService_default;
var init_ListCategoryService = __esm({
  "src/modules/categories/services/ListCategoryService.ts"() {
    init_CategoryRepository();
    ListCategoryService = class {
      async execute(restaurantId) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!normalizedRestaurantId) {
          throw new Error("Restaurante n\xE3o encontrado!");
        }
        const categories = await CategoryRepository_default.findAll(normalizedRestaurantId);
        return {
          categories
        };
      }
    };
    ListCategoryService_default = new ListCategoryService();
  }
});

// src/modules/categories/controllers/ListCategoryController.ts
var ListCategoryController, ListCategoryController_default;
var init_ListCategoryController = __esm({
  "src/modules/categories/controllers/ListCategoryController.ts"() {
    init_ListCategoryService();
    ListCategoryController = class {
      async handle(req, res) {
        try {
          const categories = await ListCategoryService_default.execute(req.user.restaurantId);
          return res.status(200).json(categories);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar categorias"
          });
        }
      }
    };
    ListCategoryController_default = new ListCategoryController();
  }
});

// src/modules/categories/routes/CategoryRoutes.ts
import { Router as Router5 } from "express";
var router5, CategoryRoutes_default;
var init_CategoryRoutes = __esm({
  "src/modules/categories/routes/CategoryRoutes.ts"() {
    init_authMiddleware();
    init_adminMiddleware();
    init_CreateCategoryController();
    init_DeleteCategoryController();
    init_UpdateCategoryController();
    init_ListCategoryController();
    router5 = Router5();
    router5.post("/", authMiddleware, adminMiddleware, (req, res) => {
      CreateCategoryController_default.handle(req, res);
    });
    router5.delete("/:id", authMiddleware, adminMiddleware, (req, res) => {
      DeleteCategoryController_default.handle(req, res);
    });
    router5.put("/:id", authMiddleware, adminMiddleware, (req, res) => {
      UpdateCategoryController_default.handle(req, res);
    });
    router5.get("/", authMiddleware, adminMiddleware, (req, res) => {
      ListCategoryController_default.handle(req, res);
    });
    CategoryRoutes_default = router5;
  }
});

// src/modules/employee/repositories/EmployeeRepository.ts
import { UserRole as UserRole22 } from "@prisma/client";
var employeePublicSelect, EmployeeRepository, EmployeeRepository_default;
var init_EmployeeRepository = __esm({
  "src/modules/employee/repositories/EmployeeRepository.ts"() {
    init_prisma();
    employeePublicSelect = {
      id: true,
      name: true,
      email: true,
      phone: true,
      restaurantId: true,
      role: true,
      subRole: true,
      active: true,
      createdAt: true,
      updatedAt: true
    };
    EmployeeRepository = class {
      async findByEmail(email, db = prisma_default) {
        return db.user.findFirst({
          where: { email },
          select: employeePublicSelect
        });
      }
      async create(data, db = prisma_default) {
        return db.user.create({
          data,
          select: employeePublicSelect
        });
      }
      async findAllByRestaurant(restaurantId, db = prisma_default) {
        return db.user.findMany({
          where: {
            restaurantId,
            role: {
              in: [UserRole22.FUNCIONARIO, UserRole22.MOTOQUEIRO]
            }
          },
          select: employeePublicSelect
        });
      }
      async findById(id, restaurantId, db = prisma_default) {
        return db.user.findFirst({
          where: {
            id: Number(id),
            restaurantId,
            role: {
              in: [UserRole22.FUNCIONARIO, UserRole22.MOTOQUEIRO]
            }
          },
          select: employeePublicSelect
        });
      }
      async update(id, data, restaurantId, db = prisma_default) {
        const employee = await this.findById(id, restaurantId, db);
        if (!employee) {
          throw new Error("Funcion\xE1rio n\xE3o encontrado!");
        }
        return db.user.update({
          where: {
            id: Number(id)
          },
          data,
          select: employeePublicSelect
        });
      }
      async deactivate(id, restaurantId, db = prisma_default) {
        const employee = await this.findById(id, restaurantId, db);
        if (!employee) {
          throw new Error("Funcion\xE1rio n\xE3o encontrado!");
        }
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            active: false
          },
          select: employeePublicSelect
        });
      }
      async reactivate(id, restaurantId, db = prisma_default) {
        const employee = await this.findById(id, restaurantId, db);
        if (!employee) {
          throw new Error("Funcion\xE1rio n\xE3o encontrado!");
        }
        return db.user.update({
          where: {
            id: Number(id)
          },
          data: {
            active: true
          },
          select: employeePublicSelect
        });
      }
    };
    EmployeeRepository_default = new EmployeeRepository();
  }
});

// src/modules/employee/services/CreateEmployeeService.ts
import { UserRole as UserRole23 } from "@prisma/client";
import bcrypt9 from "bcrypt";
var CreateEmployeeService, CreateEmployeeService_default;
var init_CreateEmployeeService = __esm({
  "src/modules/employee/services/CreateEmployeeService.ts"() {
    init_EmployeeRepository();
    CreateEmployeeService = class {
      async execute({
        name,
        email,
        password,
        phone,
        restaurantId,
        role,
        subRole,
        cpf
      }) {
        const exists = await EmployeeRepository_default.findByEmail(email);
        if (exists) {
          throw new Error("Email j\xE1 est\xE1 em uso!");
        }
        const passwordHash = await bcrypt9.hash(password, 10);
        const employee = await EmployeeRepository_default.create({
          name,
          email,
          password: passwordHash,
          phone,
          cpf: cpf ? String(cpf).replace(/\D/g, "") : void 0,
          restaurantId,
          role: role || UserRole23.FUNCIONARIO,
          subRole: subRole ?? null
        });
        return employee;
      }
    };
    CreateEmployeeService_default = new CreateEmployeeService();
  }
});

// src/validators/EmployeeSchema.ts
import { FuncionarioSubRole as FuncionarioSubRole5, UserRole as UserRole24 } from "@prisma/client";
import { z as z12 } from "zod";
var phoneRegex, employeeNameSchema, employeeEmailSchema, employeePhoneSchema, EmployeeUserSchema, UpdateEmployeeSchema, loginSchema2;
var init_EmployeeSchema = __esm({
  "src/validators/EmployeeSchema.ts"() {
    phoneRegex = /^(?:\+?55\s?)?(?:\(?([1-9][0-9])\)?\s?)?(?:((?:9\d|[2-9])\d{3})\s?-?\s?(\d{4}))$/;
    employeeNameSchema = z12.string().trim().min(2, "Nome deve conter pelo menos 2 caracteres").max(120, "Nome deve conter no m\xE1ximo 120 caracteres");
    employeeEmailSchema = z12.string().trim().toLowerCase().email("Email inv\xE1lido").max(180, "Email deve conter no m\xE1ximo 180 caracteres");
    employeePhoneSchema = z12.string({
      required_error: "Telefone obrigat\xF3rio",
      invalid_type_error: "Telefone inv\xE1lido"
    }).trim().min(1, "Telefone obrigat\xF3rio").regex(phoneRegex, "N\xFAmero de telefone inv\xE1lido!");
    EmployeeUserSchema = z12.object({
      name: employeeNameSchema,
      email: employeeEmailSchema,
      password: z12.string().min(6, "Senha deve conter no m\xEDnimo 6 caracteres!"),
      confirmPassword: z12.string().min(6, "Confirma\xE7\xE3o de senha obrigat\xF3ria"),
      role: z12.nativeEnum(UserRole24).optional().refine(
        (value) => !value || value === UserRole24.FUNCIONARIO || value === UserRole24.MOTOQUEIRO,
        {
          message: "Cargo inv\xE1lido"
        }
      ),
      phone: employeePhoneSchema,
      subRole: z12.nativeEnum(FuncionarioSubRole5).optional().nullable(),
      cpf: z12.string().optional().refine(
        (value) => !value || /^\d{3}\.?\d{3}\.?\d{3}-?\d{2}$/.test(
          value.replace(/\D/g, "").replace(/^(\d{3})(\d{3})(\d{3})(\d{2})$/, "$1.$2.$3-$4")
        ),
        { message: "CPF inv\xE1lido" }
      )
    }).refine((data) => data.password === data.confirmPassword, {
      message: "As senhas n\xE3o conferem!",
      path: ["confirmPassword"]
    });
    UpdateEmployeeSchema = z12.object({
      name: employeeNameSchema.optional(),
      email: employeeEmailSchema.optional(),
      phone: z12.union([employeePhoneSchema, z12.literal(""), z12.null()]).optional(),
      role: z12.nativeEnum(UserRole24).optional().refine(
        (value) => !value || value === UserRole24.FUNCIONARIO || value === UserRole24.MOTOQUEIRO,
        { message: "Cargo inv\xE1lido" }
      ),
      subRole: z12.nativeEnum(FuncionarioSubRole5).optional().nullable()
    }).refine((data) => Object.keys(data).length > 0, {
      message: "Informe ao menos um dado para atualizar"
    });
    loginSchema2 = z12.object({
      email: z12.string().email("Email inv\xE1lido"),
      password: z12.string().min(1, "Senha obrigat\xF3ria")
    });
  }
});

// src/modules/employee/Controllers/CreateEmployeeController.ts
var CreateEmployeeController, CreateEmployeeController_default;
var init_CreateEmployeeController = __esm({
  "src/modules/employee/Controllers/CreateEmployeeController.ts"() {
    init_CreateEmployeeService();
    init_EmployeeSchema();
    CreateEmployeeController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const parsed = EmployeeUserSchema.parse({
            ...req.body
          });
          const { name, email, password, phone, role, cpf, subRole } = parsed;
          const employee = await CreateEmployeeService_default.execute({
            name,
            email,
            password,
            phone,
            role,
            subRole: subRole ?? null,
            cpf,
            restaurantId
          });
          return res.status(201).json(employee);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar funcionario"
          });
        }
      }
    };
    CreateEmployeeController_default = new CreateEmployeeController();
  }
});

// src/modules/employee/services/ListEmployeeService.ts
var ListEmployeeService, ListEmployeeService_default;
var init_ListEmployeeService = __esm({
  "src/modules/employee/services/ListEmployeeService.ts"() {
    init_EmployeeRepository();
    ListEmployeeService = class {
      async execute(restaurantId) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!normalizedRestaurantId) {
          throw new Error("RestaurantId obrigat\xF3rio");
        }
        return EmployeeRepository_default.findAllByRestaurant(normalizedRestaurantId);
      }
    };
    ListEmployeeService_default = new ListEmployeeService();
  }
});

// src/modules/employee/Controllers/ListEmployeeController.ts
var ListEmployeeController, ListEmployeeController_default;
var init_ListEmployeeController = __esm({
  "src/modules/employee/Controllers/ListEmployeeController.ts"() {
    init_ListEmployeeService();
    ListEmployeeController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const employess = await ListEmployeeService_default.execute(restaurantId);
          return res.status(200).json(employess);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar funcionarios"
          });
        }
      }
    };
    ListEmployeeController_default = new ListEmployeeController();
  }
});

// src/modules/employee/services/UpdateEmployeeService.ts
import { UserRole as UserRole25 } from "@prisma/client";
var UpdateEmployeeService, UpdateEmployeeService_default;
var init_UpdateEmployeeService = __esm({
  "src/modules/employee/services/UpdateEmployeeService.ts"() {
    init_EmployeeRepository();
    UpdateEmployeeService = class {
      async execute({ id, restaurantId, name, phone, email, role, subRole }) {
        const employee = await EmployeeRepository_default.findById(id, restaurantId);
        if (!employee) {
          throw new Error("Funcion\xE1rio n\xE3o encontrado!");
        }
        const emailExists = email ? await EmployeeRepository_default.findByEmail(email) : null;
        if (emailExists && emailExists.id !== employee.id) {
          throw new Error("Email j\xE1 est\xE1 em uso!");
        }
        return EmployeeRepository_default.update(
          id,
          {
            ...name !== void 0 ? { name } : {},
            ...phone !== void 0 ? { phone: phone || null } : {},
            ...email !== void 0 ? { email } : {},
            ...role !== void 0 ? { role } : {},
            ...role === UserRole25.MOTOQUEIRO ? { subRole: null } : subRole !== void 0 ? { subRole } : {}
          },
          restaurantId
        );
      }
    };
    UpdateEmployeeService_default = new UpdateEmployeeService();
  }
});

// src/modules/employee/Controllers/UpdateEmployeeController.ts
var UpdateEmployeeController, UpdateEmployeeController_default;
var init_UpdateEmployeeController = __esm({
  "src/modules/employee/Controllers/UpdateEmployeeController.ts"() {
    init_UpdateEmployeeService();
    init_EmployeeSchema();
    UpdateEmployeeController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { name, email, phone, role, subRole } = UpdateEmployeeSchema.parse(req.body);
          const employee = await UpdateEmployeeService_default.execute({
            id,
            restaurantId,
            name,
            email,
            phone,
            role,
            subRole
          });
          return res.status(200).json(employee);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar funcionario"
          });
        }
      }
    };
    UpdateEmployeeController_default = new UpdateEmployeeController();
  }
});

// src/modules/employee/services/DeactivateEmployeeService.ts
var DeactivateEmployeeService, DeactivateEmployeeService_default;
var init_DeactivateEmployeeService = __esm({
  "src/modules/employee/services/DeactivateEmployeeService.ts"() {
    init_prisma();
    init_EmployeeRepository();
    DeactivateEmployeeService = class {
      async execute(id, restaurantId) {
        const normalizedUserId = Number(id);
        if (!Number.isInteger(normalizedUserId) || normalizedUserId <= 0) {
          throw new Error("Funcion\xE1rio inv\xE1lido!");
        }
        return prisma_default.$transaction(async (transaction) => {
          const employee = await EmployeeRepository_default.findById(
            normalizedUserId,
            restaurantId,
            transaction
          );
          if (!employee) {
            throw new Error("Funcion\xE1rio n\xE3o encontrado!");
          }
          const deactivated = await EmployeeRepository_default.deactivate(
            normalizedUserId,
            restaurantId,
            transaction
          );
          await transaction.authRefreshSession.deleteMany({
            where: { userId: normalizedUserId }
          });
          return deactivated;
        });
      }
    };
    DeactivateEmployeeService_default = new DeactivateEmployeeService();
  }
});

// src/modules/employee/Controllers/DeactivateEmployeeController.ts
var DeactivateEmployeeController, DeactivateEmployeeController_default;
var init_DeactivateEmployeeController = __esm({
  "src/modules/employee/Controllers/DeactivateEmployeeController.ts"() {
    init_DeactivateEmployeeService();
    DeactivateEmployeeController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const employee = await DeactivateEmployeeService_default.execute(id, restaurantId);
          return res.status(200).json(employee);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao desativar funcionario"
          });
        }
      }
    };
    DeactivateEmployeeController_default = new DeactivateEmployeeController();
  }
});

// src/modules/employee/services/ReactivateEmployeeService.ts
var ReactivateEmployeeService, ReactivateEmployeeService_default;
var init_ReactivateEmployeeService = __esm({
  "src/modules/employee/services/ReactivateEmployeeService.ts"() {
    init_EmployeeRepository();
    ReactivateEmployeeService = class {
      async execute(id, restaurantId) {
        const employee = await EmployeeRepository_default.findById(id, restaurantId);
        if (!employee) {
          throw new Error("Funcion\xE1rio n\xE3o encontrado!");
        }
        return EmployeeRepository_default.reactivate(id, restaurantId);
      }
    };
    ReactivateEmployeeService_default = new ReactivateEmployeeService();
  }
});

// src/modules/employee/Controllers/ReactivateEmployeeController.ts
var ReactivateEmployeeController, ReactivateEmployeeController_default;
var init_ReactivateEmployeeController = __esm({
  "src/modules/employee/Controllers/ReactivateEmployeeController.ts"() {
    init_ReactivateEmployeeService();
    ReactivateEmployeeController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const employee = await ReactivateEmployeeService_default.execute(id, restaurantId);
          return res.status(200).json(employee);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao reativar funcion\xE1rio"
          });
        }
      }
    };
    ReactivateEmployeeController_default = new ReactivateEmployeeController();
  }
});

// src/modules/employee/routes/EmployeeRoutes.ts
import { Router as Router6 } from "express";
var router6, EmployeeRoutes_default;
var init_EmployeeRoutes = __esm({
  "src/modules/employee/routes/EmployeeRoutes.ts"() {
    init_authMiddleware();
    init_adminMiddleware();
    init_CreateEmployeeController();
    init_ListEmployeeController();
    init_UpdateEmployeeController();
    init_DeactivateEmployeeController();
    init_ReactivateEmployeeController();
    router6 = Router6();
    router6.post("/", authMiddleware, adminMiddleware, (req, res) => {
      CreateEmployeeController_default.handle(req, res);
    });
    router6.get("/", authMiddleware, adminMiddleware, (req, res) => {
      ListEmployeeController_default.handle(req, res);
    });
    router6.put("/:id", authMiddleware, adminMiddleware, (req, res) => {
      UpdateEmployeeController_default.handle(req, res);
    });
    router6.patch("/:id", authMiddleware, adminMiddleware, (req, res) => {
      DeactivateEmployeeController_default.handle(req, res);
    });
    router6.patch("/:id/reactivate", authMiddleware, adminMiddleware, (req, res) => {
      ReactivateEmployeeController_default.handle(req, res);
    });
    EmployeeRoutes_default = router6;
  }
});

// src/middlewares/waiterMiddleware.ts
import { FuncionarioSubRole as FuncionarioSubRole7, UserRole as UserRole26 } from "@prisma/client";
function waiterMiddleware(req, res, next) {
  if (!req.user) {
    return res.status(401).json({ error: "N\xE3o autenticado" });
  }
  const role = String(req.user.role || "").toUpperCase();
  const subRole = String(req.user.subRole || "").toUpperCase();
  const isAdmin = role === UserRole26.ADMIN;
  const isWaiter = role === UserRole26.FUNCIONARIO && subRole === FuncionarioSubRole7.GARCOM;
  if (!isAdmin && !isWaiter) {
    return res.status(403).json({
      error: "Esta a\xE7\xE3o est\xE1 dispon\xEDvel apenas para o administrador ou gar\xE7ons cadastrados."
    });
  }
  const restaurantId = Number(req.user.restaurantId || 0);
  if (!Number.isInteger(restaurantId) || restaurantId <= 0) {
    return res.status(403).json({ error: "Restaurante n\xE3o identificado para este usu\xE1rio." });
  }
  return next();
}
var init_waiterMiddleware = __esm({
  "src/middlewares/waiterMiddleware.ts"() {
  }
});

// src/modules/table/repositories/TableRepository.ts
import { OrderStatus as OrderStatus19, PaymentMethod as PaymentMethod8, TableSessionStatus as TableSessionStatus4 } from "@prisma/client";
var TableRepository, TableRepository_default;
var init_TableRepository = __esm({
  "src/modules/table/repositories/TableRepository.ts"() {
    init_prisma();
    TableRepository = class {
      async create(data, db = prisma_default) {
        return db.table.create({
          data
        });
      }
      async findById(id, db = prisma_default) {
        return db.table.findUnique({
          where: {
            id: Number(id)
          }
        });
      }
      async findByIdForRestaurant(id, restaurantId, db = prisma_default) {
        return db.table.findFirst({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          }
        });
      }
      async findByNumber(number, restaurantId, db = prisma_default) {
        return db.table.findFirst({
          where: {
            number: Number(number),
            restaurantId
          }
        });
      }
      async findPublicByReference({
        number,
        tableToken,
        restaurantId,
        restaurantSlug
      }, db = prisma_default) {
        return db.table.findFirst({
          where: {
            number,
            token: tableToken,
            active: true,
            restaurant: {
              active: true,
              ...restaurantId ? { id: restaurantId } : {},
              ...restaurantSlug ? { slug: restaurantSlug } : {}
            }
          },
          select: {
            id: true,
            number: true,
            restaurantId: true,
            restaurant: {
              select: {
                slug: true,
                settings: {
                  select: {
                    tableOrderingEnabled: true,
                    waiterCallEnabled: true,
                    billRequestEnabled: true
                  }
                },
                subscription: {
                  select: {
                    plan: true,
                    status: true
                  }
                }
              }
            }
          }
        });
      }
      async findAllByRestaurant(restaurantId, db = prisma_default) {
        return db.table.findMany({
          where: {
            restaurantId
          },
          include: {
            tableSessions: {
              where: {
                status: {
                  in: [TableSessionStatus4.OPEN, TableSessionStatus4.CLOSING_REQUESTED]
                },
                OR: [{ expiresAt: null }, { expiresAt: { gt: /* @__PURE__ */ new Date() } }]
              },
              orderBy: { openedAt: "desc" },
              take: 1,
              select: {
                id: true,
                status: true,
                openedAt: true,
                expiresAt: true,
                openedBy: { select: { id: true, name: true } }
              }
            },
            orders: {
              where: {
                status: { in: [OrderStatus19.PENDENTE, OrderStatus19.PREPARANDO, OrderStatus19.PRONTO] },
                NOT: {
                  paid: false,
                  paymentMethod: { in: [PaymentMethod8.PIX, PaymentMethod8.CARTAO] },
                  payOnDelivery: false
                }
              },
              select: {
                id: true,
                userId: true,
                total: true,
                status: true,
                createdAt: true
              }
            },
            _count: {
              select: {
                orders: true,
                tableSessions: true
              }
            }
          },
          orderBy: {
            number: "asc"
          }
        });
      }
      async update(id, data) {
        return prisma_default.table.update({
          where: {
            id: Number(id)
          },
          data
        });
      }
      async deactivate(id, db = prisma_default) {
        return db.table.update({
          where: {
            id: Number(id)
          },
          data: {
            active: false
          }
        });
      }
      async deleteIfUnused(id, restaurantId, db = prisma_default) {
        const result = await db.table.deleteMany({
          where: {
            id: Number(id),
            restaurantId,
            orders: { none: {} },
            tableSessions: {
              none: {
                status: {
                  in: [TableSessionStatus4.OPEN, TableSessionStatus4.CLOSING_REQUESTED]
                }
              }
            }
          }
        });
        return result.count;
      }
    };
    TableRepository_default = new TableRepository();
  }
});

// src/modules/table/services/ResolvePublicTableService.ts
var PublicTableResolutionError, positiveInteger3, ResolvePublicTableService, ResolvePublicTableService_default;
var init_ResolvePublicTableService = __esm({
  "src/modules/table/services/ResolvePublicTableService.ts"() {
    init_TableRepository();
    PublicTableResolutionError = class extends Error {
      constructor(message, statusCode = 400, code = "INVALID_TABLE_REFERENCE") {
        super(message);
        this.statusCode = statusCode;
        this.code = code;
      }
      statusCode;
      code;
    };
    positiveInteger3 = (value) => {
      const parsed = Number(value);
      return Number.isInteger(parsed) && parsed > 0 ? parsed : null;
    };
    ResolvePublicTableService = class {
      async execute({
        tableNumber,
        tableToken,
        tableId,
        restaurantId,
        restaurantSlug
      }) {
        const normalizedTableNumber = positiveInteger3(tableNumber);
        const normalizedTableId = tableId === void 0 || tableId === null || String(tableId).trim() === "" ? null : positiveInteger3(tableId);
        const normalizedRestaurantId = restaurantId === void 0 || restaurantId === null || String(restaurantId).trim() === "" ? null : positiveInteger3(restaurantId);
        const normalizedSlug = String(restaurantSlug || "").trim().toLowerCase();
        const normalizedTableToken = String(tableToken || "").trim().toLowerCase();
        if (!normalizedTableNumber) {
          throw new PublicTableResolutionError("N\xFAmero da mesa inv\xE1lido.");
        }
        if (!/^[a-f0-9]{32}$/.test(normalizedTableToken)) {
          throw new PublicTableResolutionError(
            "O QR Code da mesa \xE9 inv\xE1lido. Escaneie o c\xF3digo oficial novamente.",
            400,
            "INVALID_TABLE_TOKEN"
          );
        }
        if (tableId !== void 0 && tableId !== null && String(tableId).trim() && !normalizedTableId) {
          throw new PublicTableResolutionError("Identificador da mesa inv\xE1lido.");
        }
        if (restaurantId !== void 0 && restaurantId !== null && String(restaurantId).trim() && !normalizedRestaurantId) {
          throw new PublicTableResolutionError("Restaurante inv\xE1lido.");
        }
        if (!normalizedRestaurantId && !normalizedSlug) {
          throw new PublicTableResolutionError(
            "O QR Code n\xE3o identifica o restaurante. Escaneie o c\xF3digo oficial novamente."
          );
        }
        if (normalizedSlug && !/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(normalizedSlug)) {
          throw new PublicTableResolutionError("Identificador do restaurante inv\xE1lido.");
        }
        const table = await TableRepository_default.findPublicByReference({
          number: normalizedTableNumber,
          tableToken: normalizedTableToken,
          ...normalizedRestaurantId ? { restaurantId: normalizedRestaurantId } : {},
          ...normalizedSlug ? { restaurantSlug: normalizedSlug } : {}
        });
        if (!table) {
          throw new PublicTableResolutionError(
            "Mesa n\xE3o encontrada neste restaurante.",
            404,
            "TABLE_NOT_FOUND"
          );
        }
        const subscription = table.restaurant.subscription;
        const hasPremiumTableAccess = subscription?.plan === "PREMIUM" && (subscription.status === "ATIVA" || subscription.status === "TESTE");
        if (!hasPremiumTableAccess) {
          throw new PublicTableResolutionError(
            "O card\xE1pio de mesa n\xE3o est\xE1 dispon\xEDvel para este restaurante.",
            403,
            "TABLE_MENU_UNAVAILABLE"
          );
        }
        const settings = table.restaurant.settings;
        return {
          id: table.id,
          number: table.number,
          restaurantId: table.restaurantId,
          restaurantSlug: table.restaurant.slug,
          tableOrderingEnabled: settings?.tableOrderingEnabled !== false,
          waiterCallEnabled: settings?.waiterCallEnabled !== false,
          billRequestEnabled: settings?.billRequestEnabled !== false
        };
      }
    };
    ResolvePublicTableService_default = new ResolvePublicTableService();
  }
});

// src/modules/tableSession/realtime/tableSessionEvents.ts
var tableSessionEvents;
var init_tableSessionEvents = __esm({
  "src/modules/tableSession/realtime/tableSessionEvents.ts"() {
    tableSessionEvents = {
      async opened(payload) {
        const { io: io2 } = await Promise.resolve().then(() => (init_server(), server_exports));
        io2.to(`restaurant:${payload.restaurantId}:waiter`).emit("table:session-opened", payload);
        io2.to(`restaurant:${payload.restaurantId}:admin`).emit("table:session-opened", payload);
        io2.to(`table-waiting:${payload.tableId}`).emit("table:session-opened", payload);
      },
      async closed(payload) {
        const { io: io2 } = await Promise.resolve().then(() => (init_server(), server_exports));
        io2.to(`restaurant:${payload.restaurantId}:waiter`).emit("table:session-closed", payload);
        io2.to(`restaurant:${payload.restaurantId}:admin`).emit("table:session-closed", payload);
        io2.to(`table-session:${payload.sessionId}`).emit("table:session-closed", {
          ...payload,
          reason: payload.reason || "closed-by-staff"
        });
      }
    };
  }
});

// src/modules/waiterCalls/repositories/TableServiceCallRepository.ts
import {
  TableServiceCallStatus,
  TableSessionStatus as TableSessionStatus5
} from "@prisma/client";
var callInclude, TableServiceCallRepository, TableServiceCallRepository_default;
var init_TableServiceCallRepository = __esm({
  "src/modules/waiterCalls/repositories/TableServiceCallRepository.ts"() {
    init_prisma();
    callInclude = {
      table: {
        select: {
          id: true,
          number: true,
          active: true
        }
      },
      assignedTo: {
        select: {
          id: true,
          name: true
        }
      },
      resolvedBy: {
        select: {
          id: true,
          name: true
        }
      }
    };
    TableServiceCallRepository = class {
      async findOpenSessionContext(sessionId, tableId, restaurantId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            id: sessionId,
            tableId,
            status: { in: [TableSessionStatus5.OPEN, TableSessionStatus5.CLOSING_REQUESTED] },
            OR: [{ expiresAt: null }, { expiresAt: { gt: /* @__PURE__ */ new Date() } }],
            table: {
              restaurantId,
              active: true,
              restaurant: { active: true }
            }
          },
          select: {
            id: true,
            tableId: true,
            table: {
              select: {
                id: true,
                number: true,
                restaurantId: true,
                restaurant: {
                  select: {
                    settings: {
                      select: {
                        tableOrderingEnabled: true,
                        waiterCallEnabled: true,
                        billRequestEnabled: true
                      }
                    },
                    subscription: {
                      select: {
                        plan: true,
                        status: true
                      }
                    }
                  }
                }
              }
            }
          }
        });
      }
      async findActiveByTableAndType(restaurantId, tableId, type, db = prisma_default) {
        return db.tableServiceCall.findFirst({
          where: {
            restaurantId,
            tableId,
            type,
            status: { in: [TableServiceCallStatus.WAITING, TableServiceCallStatus.IN_PROGRESS] }
          },
          include: callInclude,
          orderBy: { requestedAt: "asc" }
        });
      }
      async create(data, db = prisma_default) {
        return db.tableServiceCall.create({ data, include: callInclude });
      }
      async findByIdForRestaurant(id, restaurantId, db = prisma_default) {
        return db.tableServiceCall.findFirst({
          where: { id, restaurantId },
          include: callInclude
        });
      }
      async listByRestaurant(restaurantId, filters = {}, db = prisma_default) {
        const statusFilter = filters.status ? { status: filters.status } : {
          OR: [
            {
              status: { in: [TableServiceCallStatus.WAITING, TableServiceCallStatus.IN_PROGRESS] }
            },
            {
              status: TableServiceCallStatus.RESOLVED,
              resolvedAt: { gte: filters.resolvedSince || /* @__PURE__ */ new Date(0) }
            }
          ]
        };
        return db.tableServiceCall.findMany({
          where: {
            restaurantId,
            ...statusFilter,
            ...filters.type ? { type: filters.type } : {},
            ...filters.tableNumber ? { table: { number: filters.tableNumber } } : {}
          },
          include: callInclude,
          orderBy: [{ status: "asc" }, { requestedAt: "asc" }],
          take: filters.take || 200
        });
      }
      async assignIfWaiting(id, restaurantId, assignedToId, db = prisma_default) {
        const result = await db.tableServiceCall.updateMany({
          where: { id, restaurantId, status: TableServiceCallStatus.WAITING },
          data: {
            status: TableServiceCallStatus.IN_PROGRESS,
            assignedToId,
            assignedAt: /* @__PURE__ */ new Date()
          }
        });
        return result.count;
      }
      async resolveIfInProgress(id, restaurantId, resolvedById, db = prisma_default) {
        const result = await db.tableServiceCall.updateMany({
          where: { id, restaurantId, status: TableServiceCallStatus.IN_PROGRESS },
          data: {
            status: TableServiceCallStatus.RESOLVED,
            resolvedById,
            resolvedAt: /* @__PURE__ */ new Date()
          }
        });
        return result.count;
      }
      async deleteResolved(id, restaurantId, db = prisma_default) {
        const result = await db.tableServiceCall.deleteMany({
          where: { id, restaurantId, status: TableServiceCallStatus.RESOLVED }
        });
        return result.count;
      }
      async listActiveBySession(tableSessionId, restaurantId, db = prisma_default) {
        return db.tableServiceCall.findMany({
          where: {
            tableSessionId,
            restaurantId,
            status: { in: [TableServiceCallStatus.WAITING, TableServiceCallStatus.IN_PROGRESS] }
          },
          include: callInclude
        });
      }
      async resolveActiveBySession(tableSessionId, restaurantId, resolvedById, db = prisma_default) {
        return db.tableServiceCall.updateMany({
          where: {
            tableSessionId,
            restaurantId,
            status: { in: [TableServiceCallStatus.WAITING, TableServiceCallStatus.IN_PROGRESS] }
          },
          data: {
            status: TableServiceCallStatus.RESOLVED,
            resolvedById,
            resolvedAt: /* @__PURE__ */ new Date()
          }
        });
      }
      async requestSessionClosing(tableSessionId, restaurantId, db = prisma_default) {
        return db.tableSession.updateMany({
          where: {
            id: tableSessionId,
            restaurantId,
            status: TableSessionStatus5.OPEN
          },
          data: {
            status: TableSessionStatus5.CLOSING_REQUESTED,
            closingRequestedAt: /* @__PURE__ */ new Date()
          }
        });
      }
    };
    TableServiceCallRepository_default = new TableServiceCallRepository();
  }
});

// src/modules/waiterCalls/realtime/tableServiceCallEvents.ts
var tableServiceCallEvents;
var init_tableServiceCallEvents = __esm({
  "src/modules/waiterCalls/realtime/tableServiceCallEvents.ts"() {
    tableServiceCallEvents = {
      async created(payload) {
        const { io: io2 } = await Promise.resolve().then(() => (init_server(), server_exports));
        io2.to(`restaurant:${payload.restaurantId}:waiter`).emit("waiter-call:created", payload);
        io2.to(`restaurant:${payload.restaurantId}:admin`).emit("waiter-call:created", payload);
      },
      async updated(payload) {
        const { io: io2 } = await Promise.resolve().then(() => (init_server(), server_exports));
        io2.to(`restaurant:${payload.restaurantId}:waiter`).emit("waiter-call:updated", payload);
        io2.to(`restaurant:${payload.restaurantId}:admin`).emit("waiter-call:updated", payload);
        io2.to(`table:${payload.tableId}`).emit("waiter-call:updated", payload);
      }
    };
  }
});

// src/modules/tableSession/services/OpenTableSessionService.ts
import crypto8 from "crypto";
function isUniqueConflict(error2) {
  return Boolean(error2 && typeof error2 === "object" && "code" in error2 && error2.code === "P2002");
}
function logRealtimeError(error2) {
  console.error(
    "[TABLE_SESSION_REALTIME_ERROR]",
    error2 instanceof Error ? error2.message : String(error2)
  );
}
var OpenTableSessionService, OpenTableSessionService_default;
var init_OpenTableSessionService = __esm({
  "src/modules/tableSession/services/OpenTableSessionService.ts"() {
    init_TableSessionRepository();
    init_TableRepository();
    init_ResolvePublicTableService();
    init_tableSessionEvents();
    init_TableServiceCallRepository();
    init_tableServiceCallEvents();
    init_TableParticipantRepository();
    OpenTableSessionService = class {
      async execute({ tableId, restaurantId, openedById }) {
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedTableId = Number(tableId);
        const normalizedOpenedById = Number(openedById);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para abrir a mesa.");
        }
        if (!Number.isInteger(normalizedTableId) || normalizedTableId <= 0) {
          throw new Error("Mesa inv\xE1lida para abrir sess\xE3o.");
        }
        if (!Number.isInteger(normalizedOpenedById) || normalizedOpenedById <= 0) {
          throw new Error("Usu\xE1rio inv\xE1lido para abrir sess\xE3o.");
        }
        const table = await TableRepository_default.findByIdForRestaurant(
          normalizedTableId,
          normalizedRestaurantId
        );
        if (!table || !table.active) {
          throw new Error("Mesa n\xE3o encontrada!");
        }
        const publicTable = await ResolvePublicTableService_default.execute({
          tableId: table.id,
          tableNumber: table.number,
          tableToken: table.token,
          restaurantId: normalizedRestaurantId
        });
        if (!publicTable.tableOrderingEnabled) {
          throw new Error("Os pedidos pelo card\xE1pio de mesa est\xE3o desativados neste restaurante.");
        }
        const activeSession = await TableSessionRepository_default.findActiveByTable(normalizedTableId);
        if (activeSession) {
          throw new Error("Essa mesa j\xE1 est\xE1 aberta!");
        }
        const expiredSessions = await TableSessionRepository_default.listExpiredOpenByTable(normalizedTableId);
        for (const expiredSession of expiredSessions) {
          const activeCalls = await TableServiceCallRepository_default.listActiveBySession(
            expiredSession.id,
            normalizedRestaurantId
          );
          const closedSession = await TableSessionRepository_default.close(
            expiredSession.id,
            normalizedOpenedById
          );
          await TableParticipantRepository_default.revokeActiveBySession(
            expiredSession.id,
            normalizedRestaurantId
          );
          await TableServiceCallRepository_default.resolveActiveBySession(
            expiredSession.id,
            normalizedRestaurantId,
            normalizedOpenedById
          );
          try {
            await tableSessionEvents.closed({
              sessionId: expiredSession.id,
              tableId: expiredSession.tableId,
              tableNumber: expiredSession.table?.number ?? table.number,
              restaurantId: normalizedRestaurantId,
              status: "CLOSED",
              closedAt: closedSession.closedAt,
              reason: "expired"
            });
          } catch (error2) {
            logRealtimeError(error2);
          }
          for (const activeCall of activeCalls) {
            const resolvedCall = await TableServiceCallRepository_default.findByIdForRestaurant(
              activeCall.id,
              normalizedRestaurantId
            );
            if (!resolvedCall) continue;
            tableServiceCallEvents.updated(resolvedCall).catch((error2) => {
              console.error(
                "[WAITER_CALL_REALTIME_ERROR]",
                error2 instanceof Error ? error2.message : String(error2)
              );
            });
          }
        }
        const sessionToken = crypto8.randomBytes(32).toString("hex");
        const configuredHours = Number(process.env.TABLE_SESSION_MAX_HOURS || 12);
        const maxHours = Number.isFinite(configuredHours) ? Math.min(Math.max(configuredHours, 1), 24) : 12;
        const expiresAt = new Date(Date.now() + maxHours * 60 * 60 * 1e3);
        let session;
        try {
          session = await TableSessionRepository_default.create({
            restaurantId: normalizedRestaurantId,
            tableId: normalizedTableId,
            // Kept only for backwards-compatible persistence. No PIN is generated or exposed.
            pinHash: "PIN_FLOW_DISABLED",
            sessionToken,
            openedById: normalizedOpenedById,
            expiresAt
          });
        } catch (error2) {
          if (isUniqueConflict(error2)) {
            throw new Error("Essa mesa j\xE1 foi aberta por outra pessoa. Atualize a tela.");
          }
          throw error2;
        }
        tableSessionEvents.opened({
          sessionId: session.id,
          tableId: session.tableId,
          tableNumber: session.table?.number ?? table.number,
          restaurantId: normalizedRestaurantId,
          status: "OPEN",
          openedAt: session.openedAt
        }).catch(logRealtimeError);
        return {
          sessionId: session.id,
          session: {
            id: session.id,
            tableId: session.tableId,
            status: session.status,
            openedAt: session.openedAt,
            expiresAt: session.expiresAt,
            table: {
              id: session.table.id,
              number: session.table.number,
              active: session.table.active,
              restaurantId: session.table.restaurantId
            },
            openedBy: {
              id: session.openedBy.id,
              name: session.openedBy.name
            }
          }
        };
      }
    };
    OpenTableSessionService_default = new OpenTableSessionService();
  }
});

// src/modules/tableSession/controllers/OpenTableSessionController.ts
var OpenTableSessionController, OpenTableSessionController_default;
var init_OpenTableSessionController = __esm({
  "src/modules/tableSession/controllers/OpenTableSessionController.ts"() {
    init_OpenTableSessionService();
    OpenTableSessionController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const openedById = req.user.id;
          const { tableId } = req.body;
          const result = await OpenTableSessionService_default.execute({
            tableId,
            restaurantId,
            openedById
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao abrir sessao de mesa"
          });
        }
      }
    };
    OpenTableSessionController_default = new OpenTableSessionController();
  }
});

// src/modules/tableSession/services/ValidatePinService.ts
var ValidatePinService, ValidatePinService_default;
var init_ValidatePinService = __esm({
  "src/modules/tableSession/services/ValidatePinService.ts"() {
    ValidatePinService = class {
      async execute(_legacyPayload) {
        throw new Error(
          "A valida\xE7\xE3o por PIN foi desativada. Acesse a mesa pelo QR Code oficial depois que o gar\xE7om abrir o atendimento."
        );
      }
    };
    ValidatePinService_default = new ValidatePinService();
  }
});

// src/modules/tableSession/controllers/ValidatePinController.ts
var ValidatePinController, ValidatePinController_default;
var init_ValidatePinController = __esm({
  "src/modules/tableSession/controllers/ValidatePinController.ts"() {
    init_ValidatePinService();
    ValidatePinController = class {
      async handle(req, res) {
        try {
          const { tableId, pin, tableNumber, restaurantId, restaurantSlug } = req.body;
          const result = await ValidatePinService_default.execute({
            tableId,
            pin,
            tableNumber,
            restaurantId,
            restaurantSlug
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(410).json({
            error: error2 instanceof Error ? error2.message : "Erro ao validar PIN",
            code: "PIN_FLOW_DISABLED"
          });
        }
      }
    };
    ValidatePinController_default = new ValidatePinController();
  }
});

// src/modules/tableSession/services/CloseTableSessionService.ts
import { Prisma as Prisma5, TableSessionStatus as TableSessionStatus6 } from "@prisma/client";
var CloseTableSessionService, CloseTableSessionService_default;
var init_CloseTableSessionService = __esm({
  "src/modules/tableSession/services/CloseTableSessionService.ts"() {
    init_TableSessionRepository();
    init_prisma();
    init_TableServiceCallRepository();
    init_tableServiceCallEvents();
    init_tableSessionEvents();
    init_TableParticipantRepository();
    init_TableAccountSettingsRepository();
    init_tablePaymentLedger();
    CloseTableSessionService = class {
      async execute({ sessionId, closedById, restaurantId }) {
        const normalizedSessionId = Number(sessionId);
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedClosedById = Number(closedById || 0);
        if (!Number.isInteger(normalizedSessionId) || normalizedSessionId <= 0) {
          throw new Error("Sess\xE3o inv\xE1lida para encerrar a mesa.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para encerrar a mesa.");
        }
        if (!Number.isInteger(normalizedClosedById) || normalizedClosedById <= 0) {
          throw new Error("Funcion\xE1rio inv\xE1lido para encerrar a mesa.");
        }
        const result = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, normalizedRestaurantId, normalizedSessionId);
            const session = await TableSessionRepository_default.findById(normalizedSessionId, tx);
            if (!session || session.table.restaurantId !== normalizedRestaurantId) {
              throw new Error("Sess\xE3o n\xE3o encontrada neste restaurante!");
            }
            if (session.status === TableSessionStatus6.CLOSED) {
              throw new Error("Essa mesa j\xE1 est\xE1 fechada!");
            }
            const settings = await TableAccountSettingsRepository_default.findByRestaurantId(
              normalizedRestaurantId,
              tx
            );
            const blockingOrders = settings.preventCloseWithOutstandingBalance ? await TableSessionRepository_default.findBlockingOrdersForSession(
              session.tableId,
              normalizedRestaurantId,
              session.openedAt,
              tx
            ) : await TableSessionRepository_default.findOperationalBlockingOrdersForSession(
              session.tableId,
              normalizedRestaurantId,
              session.openedAt,
              tx
            );
            if (blockingOrders.length) {
              const orderReferences = blockingOrders.slice(0, 5).map((order) => `#${order.id}`).join(", ");
              throw new Error(
                settings.preventCloseWithOutstandingBalance ? `N\xE3o \xE9 poss\xEDvel fechar a mesa: existem pedidos ou pagamentos pendentes (${orderReferences}).` : `N\xE3o \xE9 poss\xEDvel fechar a mesa: existem pedidos em andamento (${orderReferences}).`
              );
            }
            const activeCalls = await TableServiceCallRepository_default.listActiveBySession(
              session.id,
              normalizedRestaurantId,
              tx
            );
            const closedSession = await TableSessionRepository_default.close(
              normalizedSessionId,
              normalizedClosedById,
              tx
            );
            await TableParticipantRepository_default.revokeActiveBySession(
              session.id,
              normalizedRestaurantId,
              tx
            );
            if (activeCalls.length) {
              await TableServiceCallRepository_default.resolveActiveBySession(
                session.id,
                normalizedRestaurantId,
                normalizedClosedById,
                tx
              );
            }
            return { session, closedSession, activeCalls };
          },
          { isolationLevel: Prisma5.TransactionIsolationLevel.Serializable }
        );
        if (result.activeCalls.length) {
          for (const activeCall of result.activeCalls) {
            const resolvedCall = await TableServiceCallRepository_default.findByIdForRestaurant(
              activeCall.id,
              normalizedRestaurantId
            );
            if (resolvedCall) {
              tableServiceCallEvents.updated(
                resolvedCall
              ).catch((error2) => {
                console.error(
                  "[WAITER_CALL_REALTIME_ERROR]",
                  error2 instanceof Error ? error2.message : String(error2)
                );
              });
            }
          }
        }
        tableSessionEvents.closed({
          sessionId: result.session.id,
          tableId: result.session.tableId,
          tableNumber: result.session?.table?.number ?? null,
          restaurantId: normalizedRestaurantId,
          status: "CLOSED",
          closedAt: result.closedSession.closedAt
        }).catch((error2) => {
          console.error(
            "[TABLE_SESSION_REALTIME_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        });
        return {
          id: result.closedSession.id,
          tableId: result.closedSession.tableId,
          status: result.closedSession.status,
          openedAt: result.closedSession.openedAt,
          closedAt: result.closedSession.closedAt,
          closedById: result.closedSession.closedById
        };
      }
    };
    CloseTableSessionService_default = new CloseTableSessionService();
  }
});

// src/modules/tableSession/controllers/CloseTableSessionController.ts
var CloseTableSessionController, CloseTableSessionController_default;
var init_CloseTableSessionController = __esm({
  "src/modules/tableSession/controllers/CloseTableSessionController.ts"() {
    init_CloseTableSessionService();
    CloseTableSessionController = class {
      async handle(req, res) {
        try {
          const closedById = req.user.id;
          const restaurantId = req.user.restaurantId;
          const sessionId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const session = await CloseTableSessionService_default.execute({
            sessionId,
            closedById,
            restaurantId
          });
          return res.status(200).json(session);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao fechar sessao de mesa"
          });
        }
      }
    };
    CloseTableSessionController_default = new CloseTableSessionController();
  }
});

// src/modules/tableSession/services/ListOpenSessionService.ts
var ListOpenSessionService, ListOpenSessionService_default;
var init_ListOpenSessionService = __esm({
  "src/modules/tableSession/services/ListOpenSessionService.ts"() {
    init_TableSessionRepository();
    ListOpenSessionService = class {
      async execute({ restaurantId }) {
        const sessions = await TableSessionRepository_default.listOpenByRestaurant(restaurantId);
        return sessions;
      }
    };
    ListOpenSessionService_default = new ListOpenSessionService();
  }
});

// src/modules/tableSession/controllers/ListOpenSessionsController.ts
var ListOpenSessionsController, ListOpenSessionsController_default;
var init_ListOpenSessionsController = __esm({
  "src/modules/tableSession/controllers/ListOpenSessionsController.ts"() {
    init_ListOpenSessionService();
    ListOpenSessionsController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const sessions = await ListOpenSessionService_default.execute({
            restaurantId
          });
          return res.status(200).json(sessions);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar sessoes abertas"
          });
        }
      }
    };
    ListOpenSessionsController_default = new ListOpenSessionsController();
  }
});

// src/modules/tableSession/services/RequestPinAssistanceService.ts
var RequestPinAssistanceService, RequestPinAssistanceService_default;
var init_RequestPinAssistanceService = __esm({
  "src/modules/tableSession/services/RequestPinAssistanceService.ts"() {
    RequestPinAssistanceService = class {
      async execute(_legacyPayload) {
        throw new Error(
          "Solicita\xE7\xF5es de PIN foram desativadas. O gar\xE7om deve abrir a mesa e o cliente acessar pelo QR Code oficial."
        );
      }
    };
    RequestPinAssistanceService_default = new RequestPinAssistanceService();
  }
});

// src/modules/tableSession/controllers/RequestPinAssistanceController.ts
var RequestPinAssistanceController, RequestPinAssistanceController_default;
var init_RequestPinAssistanceController = __esm({
  "src/modules/tableSession/controllers/RequestPinAssistanceController.ts"() {
    init_RequestPinAssistanceService();
    RequestPinAssistanceController = class {
      async handle(req, res) {
        try {
          const { tableId, tableNumber, restaurantId, restaurantSlug } = req.body;
          const result = await RequestPinAssistanceService_default.execute({
            tableId,
            tableNumber,
            restaurantId,
            restaurantSlug
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(410).json({
            error: error2 instanceof Error ? error2.message : "Erro ao solicitar apoio de PIN",
            code: "PIN_FLOW_DISABLED"
          });
        }
      }
    };
    RequestPinAssistanceController_default = new RequestPinAssistanceController();
  }
});

// src/modules/tableSession/controllers/GetCurrentSessionController.ts
var GetCurrentSessionController, GetCurrentSessionController_default;
var init_GetCurrentSessionController = __esm({
  "src/modules/tableSession/controllers/GetCurrentSessionController.ts"() {
    GetCurrentSessionController = class {
      async handle(req, res) {
        try {
          return res.status(200).json({
            sessionId: req.tableSession.id,
            sessionPublicId: req.tableSession.publicId,
            tableId: req.tableSession.tableId,
            restaurantId: req.tableSession.restaurantId,
            participant: req.tableParticipant ? {
              publicId: req.tableParticipant.publicId,
              displayName: req.tableParticipant.displayName,
              authenticated: req.tableParticipant.authenticated
            } : null
          });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao obter sessao atual"
          });
        }
      }
    };
    GetCurrentSessionController_default = new GetCurrentSessionController();
  }
});

// src/modules/tableSession/services/JoinTableParticipantService.ts
import crypto9 from "crypto";
import { Prisma as Prisma6, UserRole as UserRole27 } from "@prisma/client";
function isUniqueConflict2(error2) {
  return Boolean(error2 && typeof error2 === "object" && "code" in error2 && error2.code === "P2002");
}
function toPublicParticipant(participant) {
  return {
    publicId: participant.publicId,
    displayName: participant.displayName || participant.user?.name || null,
    authenticated: Boolean(participant.userId),
    status: participant.status,
    joinedAt: participant.joinedAt,
    leftAt: participant.leftAt
  };
}
var JoinTableParticipantService, JoinTableParticipantService_default;
var init_JoinTableParticipantService = __esm({
  "src/modules/tableSession/services/JoinTableParticipantService.ts"() {
    init_prisma();
    init_tableAccountSchemas();
    init_TableParticipantRepository();
    init_participantToken();
    JoinTableParticipantService = class {
      async execute({ session, authenticatedUser, cookies = {}, displayName }) {
        const identity = tableParticipantIdentityInputSchema.parse(
          displayName === void 0 ? {} : { displayName }
        );
        const cookieName = getParticipantCookieName(session.publicId);
        const existingRawToken = cookies[cookieName];
        const existingTokenHash = isParticipantTokenShape(existingRawToken) ? hashParticipantToken(existingRawToken) : null;
        const authenticatedCustomerId = authenticatedUser?.role === UserRole27.CLIENTE && Number(authenticatedUser.id) > 0 ? Number(authenticatedUser.id) : null;
        if (authenticatedCustomerId) {
          const participant2 = await prisma_default.$transaction(
            async (tx) => {
              const activeCustomer = await tx.user.findFirst({
                where: {
                  id: authenticatedCustomerId,
                  role: UserRole27.CLIENTE,
                  active: true
                },
                select: { id: true }
              });
              if (!activeCustomer) {
                throw new Error("A conta autenticada n\xE3o est\xE1 dispon\xEDvel para entrar nesta mesa.");
              }
              const existingAuthenticated = await TableParticipantRepository_default.findByUser(
                authenticatedCustomerId,
                session.id,
                session.restaurantId,
                tx
              );
              const guest = existingTokenHash ? await TableParticipantRepository_default.findGuestByTokenHash(
                existingTokenHash,
                session.id,
                session.restaurantId,
                tx
              ) : null;
              if (guest && existingAuthenticated && guest.id !== existingAuthenticated.id) {
                await TableParticipantRepository_default.transferOwnedTableData(
                  guest.id,
                  existingAuthenticated.id,
                  authenticatedCustomerId,
                  session.id,
                  session.restaurantId,
                  tx
                );
                await TableParticipantRepository_default.revoke(guest.id, session.id, session.restaurantId, tx);
                return existingAuthenticated;
              }
              if (guest) {
                try {
                  const linkedParticipant = await TableParticipantRepository_default.linkGuestToUser(
                    guest.id,
                    authenticatedCustomerId,
                    identity.displayName ?? guest.displayName,
                    session.id,
                    session.restaurantId,
                    tx
                  );
                  await TableParticipantRepository_default.attachUserToOwnedOrders(
                    linkedParticipant.id,
                    authenticatedCustomerId,
                    session.id,
                    session.restaurantId,
                    tx
                  );
                  return linkedParticipant;
                } catch (error2) {
                  if (!isUniqueConflict2(error2)) throw error2;
                  const concurrentParticipant = await TableParticipantRepository_default.findByUser(
                    authenticatedCustomerId,
                    session.id,
                    session.restaurantId,
                    tx
                  );
                  if (!concurrentParticipant) throw error2;
                  await TableParticipantRepository_default.transferOwnedTableData(
                    guest.id,
                    concurrentParticipant.id,
                    authenticatedCustomerId,
                    session.id,
                    session.restaurantId,
                    tx
                  );
                  await TableParticipantRepository_default.revoke(
                    guest.id,
                    session.id,
                    session.restaurantId,
                    tx
                  );
                  return concurrentParticipant;
                }
              }
              return TableParticipantRepository_default.upsertAuthenticated(
                {
                  publicId: crypto9.randomUUID(),
                  restaurantId: session.restaurantId,
                  tableSessionId: session.id,
                  userId: authenticatedCustomerId,
                  displayName: identity.displayName
                },
                tx
              );
            },
            { isolationLevel: Prisma6.TransactionIsolationLevel.Serializable }
          );
          return {
            participant: toPublicParticipant(participant2),
            participantToken: null,
            participantCookieName: cookieName,
            participantCookieExpiresAt: null,
            clearParticipantCookie: Boolean(existingRawToken)
          };
        }
        if (existingTokenHash) {
          const existingGuest = await TableParticipantRepository_default.findGuestByTokenHash(
            existingTokenHash,
            session.id,
            session.restaurantId
          );
          if (existingGuest) {
            const participant2 = identity.displayName !== void 0 && identity.displayName !== existingGuest.displayName ? await TableParticipantRepository_default.updateDisplayName(
              existingGuest.id,
              session.id,
              session.restaurantId,
              identity.displayName
            ) : existingGuest;
            return {
              participant: toPublicParticipant(participant2),
              participantToken: existingRawToken,
              participantCookieName: cookieName,
              participantCookieExpiresAt: participant2.tokenExpiresAt,
              clearParticipantCookie: false
            };
          }
        }
        const participantToken = createParticipantToken();
        const participantCookieExpiresAt = resolveParticipantTokenExpiration(session.expiresAt);
        const participant = await TableParticipantRepository_default.createGuest({
          publicId: crypto9.randomUUID(),
          restaurantId: session.restaurantId,
          tableSessionId: session.id,
          displayName: identity.displayName,
          guestTokenHash: hashParticipantToken(participantToken),
          tokenExpiresAt: participantCookieExpiresAt
        });
        return {
          participant: toPublicParticipant(participant),
          participantToken,
          participantCookieName: cookieName,
          participantCookieExpiresAt,
          clearParticipantCookie: false
        };
      }
    };
    JoinTableParticipantService_default = new JoinTableParticipantService();
  }
});

// src/modules/tableSession/services/JoinTableSessionService.ts
import { TableSessionStatus as TableSessionStatus7 } from "@prisma/client";
var TableSessionJoinError, JoinTableSessionService, JoinTableSessionService_default;
var init_JoinTableSessionService = __esm({
  "src/modules/tableSession/services/JoinTableSessionService.ts"() {
    init_TableSessionRepository();
    init_ResolvePublicTableService();
    init_JoinTableParticipantService();
    TableSessionJoinError = class extends Error {
      constructor(message, statusCode, code) {
        super(message);
        this.statusCode = statusCode;
        this.code = code;
        this.name = "TableSessionJoinError";
      }
      statusCode;
      code;
    };
    JoinTableSessionService = class {
      async execute({
        tableId,
        tableNumber,
        tableToken,
        restaurantId,
        restaurantSlug,
        authenticatedUser,
        cookies,
        displayName
      }) {
        const resolvedTable = await ResolvePublicTableService_default.execute({
          tableId,
          tableNumber,
          tableToken,
          restaurantId,
          restaurantSlug
        });
        if (!resolvedTable.tableOrderingEnabled) {
          throw new Error("Os pedidos pelo card\xE1pio de mesa est\xE3o desativados neste restaurante.");
        }
        const session = await TableSessionRepository_default.findActiveByTable(resolvedTable.id);
        if (!session) {
          throw new TableSessionJoinError(
            "Esta mesa ainda n\xE3o foi aberta pelo gar\xE7om. Aguarde o atendimento e tente novamente.",
            409,
            "TABLE_NOT_OPEN"
          );
        }
        const closingRequested = session.status === TableSessionStatus7.CLOSING_REQUESTED;
        const sessionRestaurantId = Number(
          session.restaurantId ?? session.table?.restaurantId ?? resolvedTable.restaurantId
        );
        if (sessionRestaurantId !== Number(resolvedTable.restaurantId)) {
          throw new Error("A sess\xE3o aberta n\xE3o pertence ao restaurante informado pelo QR Code.");
        }
        const participantResult = await JoinTableParticipantService_default.execute({
          session: {
            id: session.id,
            publicId: session.publicId || `legacy-session-${session.id}`,
            restaurantId: sessionRestaurantId,
            expiresAt: session.expiresAt
          },
          authenticatedUser,
          cookies,
          displayName
        });
        return {
          sessionToken: session.sessionToken,
          sessionId: session.id,
          sessionPublicId: session.publicId || null,
          tableId: session.tableId,
          tableNumber: session.table?.number ?? resolvedTable.number,
          restaurantId: sessionRestaurantId,
          expiresAt: session.expiresAt,
          sessionStatus: session.status,
          // A sessão continua acessível para consultar e quitar a conta, mas o
          // cardápio não deve oferecer novos pedidos após a solicitação de fechamento.
          tableOrderingEnabled: resolvedTable.tableOrderingEnabled && !closingRequested,
          waiterCallEnabled: resolvedTable.waiterCallEnabled,
          billRequestEnabled: resolvedTable.billRequestEnabled,
          ...participantResult
        };
      }
    };
    JoinTableSessionService_default = new JoinTableSessionService();
  }
});

// src/modules/tableSession/controllers/JoinTableSessionController.ts
var JoinTableSessionController, JoinTableSessionController_default;
var init_JoinTableSessionController = __esm({
  "src/modules/tableSession/controllers/JoinTableSessionController.ts"() {
    init_JoinTableSessionService();
    init_ResolvePublicTableService();
    init_participantToken();
    JoinTableSessionController = class {
      async handle(req, res) {
        try {
          const result = await JoinTableSessionService_default.execute({
            tableId: req.body?.tableId,
            tableNumber: req.body?.tableNumber,
            tableToken: req.body?.tableToken || req.body?.token,
            restaurantId: req.body?.restaurantId,
            restaurantSlug: req.body?.restaurantSlug || req.body?.slug,
            authenticatedUser: req.user ? { id: req.user.id, role: req.user.role } : null,
            cookies: parseCookieHeader(req.headers.cookie),
            displayName: req.body?.displayName ?? req.body?.guestName
          });
          const {
            participantToken,
            participantCookieName,
            participantCookieExpiresAt,
            clearParticipantCookie,
            ...publicResult
          } = result;
          if (clearParticipantCookie) {
            res.clearCookie(participantCookieName, getParticipantCookieOptions(/* @__PURE__ */ new Date(0)));
          } else if (participantToken && participantCookieExpiresAt) {
            res.cookie(
              participantCookieName,
              participantToken,
              getParticipantCookieOptions(participantCookieExpiresAt)
            );
          }
          return res.status(200).json(publicResult);
        } catch (error2) {
          const isTypedError = error2 instanceof PublicTableResolutionError || error2 instanceof TableSessionJoinError;
          const statusCode = isTypedError ? error2.statusCode : 400;
          return res.status(statusCode).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel acessar a mesa.",
            ...isTypedError ? { code: error2.code } : {}
          });
        }
      }
    };
    JoinTableSessionController_default = new JoinTableSessionController();
  }
});

// src/middlewares/security/tableSessionRateLimitMiddleware.ts
import rateLimit4, { ipKeyGenerator as ipKeyGenerator4 } from "express-rate-limit";
function getTableKey(req) {
  const tableId = String(req.body?.tableId || "").trim().slice(0, 32);
  const ip = ipKeyGenerator4(String(req.ip || "unknown").trim());
  return `${ip}:${tableId || "no-table"}`;
}
var tablePinRateLimitMiddleware, tablePinAssistanceRateLimitMiddleware, tableServiceCallRateLimitMiddleware, tableJoinRateLimitMiddleware;
var init_tableSessionRateLimitMiddleware = __esm({
  "src/middlewares/security/tableSessionRateLimitMiddleware.ts"() {
    tablePinRateLimitMiddleware = rateLimit4({
      windowMs: Number(process.env.TABLE_PIN_RATE_LIMIT_WINDOW_MS || 5 * 60 * 1e3),
      max: Number(process.env.TABLE_PIN_RATE_LIMIT_MAX_REQUESTS || 10),
      standardHeaders: true,
      legacyHeaders: false,
      skipSuccessfulRequests: true,
      keyGenerator: getTableKey,
      message: {
        error: "Muitas tentativas de PIN. Aguarde alguns minutos."
      }
    });
    tablePinAssistanceRateLimitMiddleware = rateLimit4({
      windowMs: Number(process.env.TABLE_PIN_ASSISTANCE_RATE_LIMIT_WINDOW_MS || 60 * 1e3),
      max: Number(process.env.TABLE_PIN_ASSISTANCE_RATE_LIMIT_MAX_REQUESTS || 3),
      standardHeaders: true,
      legacyHeaders: false,
      keyGenerator: getTableKey,
      message: {
        error: "Muitas solicita\xE7\xF5es de ajuda. Aguarde um instante."
      }
    });
    tableServiceCallRateLimitMiddleware = rateLimit4({
      windowMs: Number(process.env.TABLE_SERVICE_CALL_RATE_LIMIT_WINDOW_MS || 60 * 1e3),
      max: Number(process.env.TABLE_SERVICE_CALL_RATE_LIMIT_MAX_REQUESTS || 6),
      standardHeaders: true,
      legacyHeaders: false,
      keyGenerator: (req) => {
        const rawSessionToken = req.headers["x-session-token"];
        const sessionToken = Array.isArray(rawSessionToken) ? rawSessionToken[0] : rawSessionToken;
        const safeSessionSuffix = String(sessionToken || "no-session").slice(-16);
        const ip = ipKeyGenerator4(String(req.ip || "unknown").trim());
        return `${ip}:${safeSessionSuffix}`;
      },
      message: {
        error: "Muitos chamados em pouco tempo. Aguarde um instante."
      }
    });
    tableJoinRateLimitMiddleware = rateLimit4({
      windowMs: Number(process.env.TABLE_JOIN_RATE_LIMIT_WINDOW_MS || 60 * 1e3),
      max: Number(process.env.TABLE_JOIN_RATE_LIMIT_MAX_REQUESTS || 12),
      standardHeaders: true,
      legacyHeaders: false,
      skipSuccessfulRequests: true,
      keyGenerator: getTableKey,
      message: {
        error: "Muitas tentativas de acesso \xE0 mesa. Aguarde um instante."
      }
    });
  }
});

// src/middlewares/premiumTablePlanMiddleware.ts
async function premiumTablePlanMiddleware(req, res, next) {
  try {
    const restaurantId = Number(req.user?.restaurantId || 0);
    if (!restaurantId) {
      return res.status(400).json({ error: "Restaurante n\xE3o identificado." });
    }
    const subscription = await prisma_default.subscription.findUnique({
      where: { restaurantId },
      select: { plan: true, status: true }
    });
    const isActive = subscription?.status === "ATIVA" || subscription?.status === "TESTE";
    if (!isActive || subscription?.plan !== "PREMIUM") {
      return res.status(403).json({
        error: "O card\xE1pio digital com QR Code de mesa est\xE1 dispon\xEDvel no plano Premium.",
        code: "PREMIUM_TABLE_PLAN_REQUIRED"
      });
    }
    return next();
  } catch {
    return res.status(500).json({
      error: "N\xE3o foi poss\xEDvel validar o plano do restaurante."
    });
  }
}
var init_premiumTablePlanMiddleware = __esm({
  "src/middlewares/premiumTablePlanMiddleware.ts"() {
    init_prisma();
  }
});

// src/modules/tableSession/services/UpdateTableParticipantService.ts
var UpdateTableParticipantService, UpdateTableParticipantService_default;
var init_UpdateTableParticipantService = __esm({
  "src/modules/tableSession/services/UpdateTableParticipantService.ts"() {
    init_tableAccountSchemas();
    init_TableParticipantRepository();
    UpdateTableParticipantService = class {
      async execute({ participantId, tableSessionId, restaurantId, displayName }) {
        const identity = tableParticipantIdentityInputSchema.parse({ displayName });
        if (identity.displayName === void 0) {
          throw new Error("Informe o nome que deseja usar nesta mesa.");
        }
        const participant = await TableParticipantRepository_default.updateDisplayName(
          participantId,
          tableSessionId,
          restaurantId,
          identity.displayName
        );
        return {
          publicId: participant.publicId,
          displayName: participant.displayName || participant.user?.name || null,
          authenticated: Boolean(participant.userId),
          status: participant.status,
          joinedAt: participant.joinedAt,
          leftAt: participant.leftAt
        };
      }
    };
    UpdateTableParticipantService_default = new UpdateTableParticipantService();
  }
});

// src/modules/tableSession/controllers/UpdateTableParticipantController.ts
import { ZodError } from "zod";
var UpdateTableParticipantController, UpdateTableParticipantController_default;
var init_UpdateTableParticipantController = __esm({
  "src/modules/tableSession/controllers/UpdateTableParticipantController.ts"() {
    init_UpdateTableParticipantService();
    UpdateTableParticipantController = class {
      async handle(req, res) {
        try {
          const participant = await UpdateTableParticipantService_default.execute({
            participantId: req.tableParticipant.id,
            tableSessionId: req.tableSession.id,
            restaurantId: req.tableSession.restaurantId,
            displayName: req.body?.displayName
          });
          return res.status(200).json(participant);
        } catch (error2) {
          return res.status(error2 instanceof ZodError ? 422 : 400).json({
            error: error2 instanceof ZodError ? error2.issues[0]?.message || "Nome inv\xE1lido." : error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel atualizar seu nome nesta mesa."
          });
        }
      }
    };
    UpdateTableParticipantController_default = new UpdateTableParticipantController();
  }
});

// src/modules/tableSession/services/ForceCloseTableSessionService.ts
import {
  Prisma as Prisma7,
  TablePaymentEventType as TablePaymentEventType2,
  TablePaymentIntentStatus as TablePaymentIntentStatus2,
  TableSessionStatus as TableSessionStatus8
} from "@prisma/client";
var ForceCloseTableSessionService, ForceCloseTableSessionService_default;
var init_ForceCloseTableSessionService = __esm({
  "src/modules/tableSession/services/ForceCloseTableSessionService.ts"() {
    init_prisma();
    init_tableAccountSchemas();
    init_tablePaymentLedger();
    init_TableServiceCallRepository();
    init_tableServiceCallEvents();
    init_TableParticipantRepository();
    init_TableSessionRepository();
    init_tableSessionEvents();
    ForceCloseTableSessionService = class {
      async execute(input) {
        const sessionId = Number(input.sessionId);
        const restaurantId = Number(input.restaurantId);
        const actorUserId = Number(input.actorUserId);
        const { reason } = forceCloseTableAccountInputSchema.parse({ reason: input.reason });
        if (![sessionId, restaurantId, actorUserId].every((value) => Number.isInteger(value) && value > 0)) {
          throw new Error("Dados inv\xE1lidos para o fechamento administrativo da mesa.");
        }
        const result = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, restaurantId, sessionId);
            const session = await TableSessionRepository_default.findById(sessionId, tx);
            if (!session || session.table.restaurantId !== restaurantId) {
              throw new Error("Sess\xE3o n\xE3o encontrada neste restaurante.");
            }
            if (session.status === TableSessionStatus8.CLOSED) {
              throw new Error("Essa mesa j\xE1 est\xE1 fechada.");
            }
            const activePayments = await tx.tablePaymentIntent.findMany({
              where: {
                restaurantId,
                tableSessionId: sessionId,
                status: {
                  in: [TablePaymentIntentStatus2.RESERVED, TablePaymentIntentStatus2.PROCESSING]
                }
              },
              select: { id: true, publicId: true, status: true, totalCents: true }
            });
            const now = /* @__PURE__ */ new Date();
            for (const payment of activePayments) {
              const changed = await tx.tablePaymentIntent.updateMany({
                where: {
                  id: payment.id,
                  restaurantId,
                  tableSessionId: sessionId,
                  status: payment.status
                },
                data: {
                  status: TablePaymentIntentStatus2.CANCELED,
                  canceledAt: now,
                  failureCode: "ADMIN_FORCE_CLOSE"
                }
              });
              if (changed.count === 1) {
                await tx.tablePaymentEvent.create({
                  data: {
                    restaurantId,
                    tableSessionId: sessionId,
                    paymentIntentId: payment.id,
                    deduplicationKey: `table-payment:${payment.publicId}:force-close`,
                    type: TablePaymentEventType2.CANCELED,
                    fromStatus: payment.status,
                    toStatus: TablePaymentIntentStatus2.CANCELED,
                    amountCents: payment.totalCents,
                    actorUserId,
                    metadata: { reason, action: "FORCE_CLOSE" },
                    occurredAt: now
                  }
                });
              }
            }
            if (activePayments.length > 0) {
              await projectTableSessionFinancialState(tx, restaurantId, sessionId, now);
            }
            const activeCalls = await TableServiceCallRepository_default.listActiveBySession(
              sessionId,
              restaurantId,
              tx
            );
            const closedSession = await TableSessionRepository_default.forceClose(
              sessionId,
              actorUserId,
              reason,
              tx
            );
            await TableParticipantRepository_default.revokeActiveBySession(sessionId, restaurantId, tx);
            if (activeCalls.length) {
              await TableServiceCallRepository_default.resolveActiveBySession(
                sessionId,
                restaurantId,
                actorUserId,
                tx
              );
            }
            return { session, closedSession, activeCalls };
          },
          { isolationLevel: Prisma7.TransactionIsolationLevel.Serializable }
        );
        for (const activeCall of result.activeCalls) {
          const resolvedCall = await TableServiceCallRepository_default.findByIdForRestaurant(
            activeCall.id,
            restaurantId
          );
          if (resolvedCall) {
            void tableServiceCallEvents.updated(
              resolvedCall
            );
          }
        }
        void tableSessionEvents.closed({
          sessionId: result.session.id,
          tableId: result.session.tableId,
          tableNumber: result.session.table.number,
          restaurantId,
          status: "CLOSED",
          closedAt: result.closedSession.closedAt
        });
        return {
          id: result.closedSession.id,
          tableId: result.closedSession.tableId,
          status: result.closedSession.status,
          openedAt: result.closedSession.openedAt,
          closedAt: result.closedSession.closedAt,
          closedById: result.closedSession.closedById,
          forcedClosed: result.closedSession.forcedClosed,
          forceCloseReason: result.closedSession.forceCloseReason
        };
      }
    };
    ForceCloseTableSessionService_default = new ForceCloseTableSessionService();
  }
});

// src/modules/tableSession/controllers/ForceCloseTableSessionController.ts
var ForceCloseTableSessionController, ForceCloseTableSessionController_default;
var init_ForceCloseTableSessionController = __esm({
  "src/modules/tableSession/controllers/ForceCloseTableSessionController.ts"() {
    init_ForceCloseTableSessionService();
    ForceCloseTableSessionController = class {
      async handle(req, res) {
        try {
          const sessionId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const result = await ForceCloseTableSessionService_default.execute({
            sessionId,
            actorUserId: req.user.id,
            restaurantId: req.user.restaurantId,
            reason: req.body?.reason
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao for\xE7ar o fechamento da mesa."
          });
        }
      }
    };
    ForceCloseTableSessionController_default = new ForceCloseTableSessionController();
  }
});

// src/modules/tableSession/routes/SessionsTablesRoutes.ts
import { Router as Router7 } from "express";
var router7, SessionsTablesRoutes_default;
var init_SessionsTablesRoutes = __esm({
  "src/modules/tableSession/routes/SessionsTablesRoutes.ts"() {
    init_authMiddleware();
    init_waiterMiddleware();
    init_sessionMiddleware();
    init_OpenTableSessionController();
    init_ValidatePinController();
    init_CloseTableSessionController();
    init_ListOpenSessionsController();
    init_RequestPinAssistanceController();
    init_GetCurrentSessionController();
    init_JoinTableSessionController();
    init_tableSessionRateLimitMiddleware();
    init_premiumTablePlanMiddleware();
    init_optionalAuthMiddleware();
    init_tableParticipantMiddleware();
    init_UpdateTableParticipantController();
    init_ForceCloseTableSessionController();
    init_adminMiddleware();
    router7 = Router7();
    router7.post(
      "/join",
      tableJoinRateLimitMiddleware,
      optionalAuthMiddleware,
      (req, res) => JoinTableSessionController_default.handle(req, res)
    );
    router7.post(
      "/validate",
      tablePinRateLimitMiddleware,
      (req, res) => ValidatePinController_default.handle(req, res)
    );
    router7.post(
      "/request-pin",
      tablePinAssistanceRateLimitMiddleware,
      (req, res) => RequestPinAssistanceController_default.handle(req, res)
    );
    router7.get(
      "/current",
      optionalAuthMiddleware,
      sessionMiddleware,
      tableParticipantMiddleware,
      (req, res) => GetCurrentSessionController_default.handle(req, res)
    );
    router7.patch(
      "/participant",
      optionalAuthMiddleware,
      sessionMiddleware,
      tableParticipantMiddleware,
      (req, res) => UpdateTableParticipantController_default.handle(req, res)
    );
    router7.post(
      "/open",
      authMiddleware,
      waiterMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => OpenTableSessionController_default.handle(req, res)
    );
    router7.patch(
      "/:id/close",
      authMiddleware,
      waiterMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => CloseTableSessionController_default.handle(req, res)
    );
    router7.patch(
      "/:id/force-close",
      authMiddleware,
      adminMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => ForceCloseTableSessionController_default.handle(req, res)
    );
    router7.get(
      "/open",
      authMiddleware,
      waiterMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => ListOpenSessionsController_default.handle(req, res)
    );
    SessionsTablesRoutes_default = router7;
  }
});

// src/modules/table/services/CreateTableService.ts
import crypto10 from "crypto";
var CreateTableService, CreateTableService_default;
var init_CreateTableService = __esm({
  "src/modules/table/services/CreateTableService.ts"() {
    init_TableRepository();
    CreateTableService = class {
      async execute({ number, restaurantId }) {
        const normalizedNumber = Number(number);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para cadastrar mesa.");
        }
        if (!Number.isInteger(normalizedNumber) || normalizedNumber <= 0 || normalizedNumber > 9999) {
          throw new Error("Informe um n\xFAmero de mesa inteiro entre 1 e 9999.");
        }
        const tableExists = await TableRepository_default.findByNumber(
          normalizedNumber,
          normalizedRestaurantId
        );
        if (tableExists) {
          throw new Error("J\xE1 existe uma mesa com esse n\xFAmero!");
        }
        const token = crypto10.randomBytes(16).toString("hex");
        return TableRepository_default.create({
          number: normalizedNumber,
          restaurantId: normalizedRestaurantId,
          token
        });
      }
    };
    CreateTableService_default = new CreateTableService();
  }
});

// src/modules/table/controllers/CreateTableController.ts
var CreateTableController, CreateTableController_default;
var init_CreateTableController = __esm({
  "src/modules/table/controllers/CreateTableController.ts"() {
    init_CreateTableService();
    CreateTableController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const { number } = req.body;
          const table = await CreateTableService_default.execute({
            number,
            restaurantId
          });
          return res.status(201).json(table);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar mesa"
          });
        }
      }
    };
    CreateTableController_default = new CreateTableController();
  }
});

// src/modules/table/services/ListTableService.ts
var ListTableService, ListTableService_default;
var init_ListTableService = __esm({
  "src/modules/table/services/ListTableService.ts"() {
    init_TableRepository();
    ListTableService = class {
      async execute({ restaurantId, includeQrToken = false }) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante n\xE3o identificado para listar mesas.");
        }
        const tables = await TableRepository_default.findAllByRestaurant(normalizedRestaurantId);
        return tables.map((table) => {
          const { orders, token, ...safeTableData } = table;
          const openSession = table.tableSessions[0] || null;
          const activeTotal = orders.reduce((total, order) => total + Number(order.total || 0), 0);
          const guests = new Set(orders.map((order) => order.userId)).size;
          const operationalStatus = openSession ? "OCCUPIED" : "FREE";
          return {
            ...safeTableData,
            ...includeQrToken ? { token } : {},
            status: operationalStatus,
            sessionId: openSession?.id ?? null,
            openedAt: openSession?.openedAt ?? null,
            guests,
            total: Number(activeTotal.toFixed(2)),
            operational: {
              status: operationalStatus,
              openSession,
              activeOrdersCount: orders.length,
              guests,
              total: Number(activeTotal.toFixed(2))
            }
          };
        });
      }
    };
    ListTableService_default = new ListTableService();
  }
});

// src/modules/table/controllers/ListTableController.ts
import { UserRole as UserRole28 } from "@prisma/client";
var ListTableController, ListTableController_default;
var init_ListTableController = __esm({
  "src/modules/table/controllers/ListTableController.ts"() {
    init_ListTableService();
    ListTableController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const tables = await ListTableService_default.execute({
            restaurantId,
            includeQrToken: req.user.role === UserRole28.ADMIN
          });
          return res.status(200).json(tables);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar mesas"
          });
        }
      }
    };
    ListTableController_default = new ListTableController();
  }
});

// src/modules/table/services/GetTableByIdService.ts
var GetTableByIdService, GetTableByIdService_default;
var init_GetTableByIdService = __esm({
  "src/modules/table/services/GetTableByIdService.ts"() {
    init_TableRepository();
    GetTableByIdService = class {
      async execute({ id, restaurantId, includeQrToken = false }) {
        const normalizedId = Number(id);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedId) || normalizedId <= 0) {
          throw new Error("Mesa inv\xE1lida para consulta.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para consultar mesa.");
        }
        const table = await TableRepository_default.findByIdForRestaurant(
          normalizedId,
          normalizedRestaurantId
        );
        if (!table) {
          throw new Error("Mesa n\xE3o encontrada!");
        }
        if (includeQrToken) {
          return table;
        }
        const { token: _token, ...safeTable } = table;
        return safeTable;
      }
    };
    GetTableByIdService_default = new GetTableByIdService();
  }
});

// src/modules/table/controllers/GetTableByIdController.ts
import { UserRole as UserRole29 } from "@prisma/client";
var GetTableByIdController, GetTableByIdController_default;
var init_GetTableByIdController = __esm({
  "src/modules/table/controllers/GetTableByIdController.ts"() {
    init_GetTableByIdService();
    GetTableByIdController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const parsedId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const table = await GetTableByIdService_default.execute({
            id: parsedId,
            restaurantId,
            includeQrToken: req.user.role === UserRole29.ADMIN
          });
          return res.status(200).json(table);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao buscar mesa"
          });
        }
      }
    };
    GetTableByIdController_default = new GetTableByIdController();
  }
});

// src/modules/table/services/UpdateTableService.ts
var UpdateTableService, UpdateTableService_default;
var init_UpdateTableService = __esm({
  "src/modules/table/services/UpdateTableService.ts"() {
    init_TableRepository();
    UpdateTableService = class {
      async execute({ id, restaurantId, number, active }) {
        const normalizedId = Number(id);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedId) || normalizedId <= 0) {
          throw new Error("Mesa inv\xE1lida para atualiza\xE7\xE3o.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para atualizar mesa.");
        }
        const table = await TableRepository_default.findByIdForRestaurant(
          normalizedId,
          normalizedRestaurantId
        );
        if (!table) {
          throw new Error("Mesa n\xE3o encontrada!");
        }
        if (number !== void 0 && number !== null && String(number).trim()) {
          const normalizedNumber = Number(number);
          if (!Number.isInteger(normalizedNumber) || normalizedNumber <= 0 || normalizedNumber > 9999) {
            throw new Error("Informe um n\xFAmero de mesa inteiro entre 1 e 9999.");
          }
          const tableExists = await TableRepository_default.findByNumber(
            normalizedNumber,
            normalizedRestaurantId
          );
          if (tableExists && tableExists.id !== table.id) {
            throw new Error("J\xE1 existe uma mesa com esse n\xFAmero!");
          }
        }
        const hasNumber = number !== void 0 && number !== null && String(number).trim() !== "";
        const hasActive = typeof active === "boolean";
        if (!hasNumber && !hasActive) {
          throw new Error("Informe n\xFAmero e/ou status ativo da mesa.");
        }
        const updateData = {};
        if (hasNumber) {
          updateData.number = Number(number);
        }
        if (hasActive) {
          updateData.active = Boolean(active);
        }
        return await TableRepository_default.update(normalizedId, {
          ...updateData
        });
      }
    };
    UpdateTableService_default = new UpdateTableService();
  }
});

// src/modules/table/controllers/UpdateTableController.ts
var UpdateTableController, UpdateTableController_default;
var init_UpdateTableController = __esm({
  "src/modules/table/controllers/UpdateTableController.ts"() {
    init_UpdateTableService();
    UpdateTableController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const parsedId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { number, active } = req.body;
          const table = await UpdateTableService_default.execute({
            id: parsedId,
            restaurantId,
            number,
            active
          });
          return res.status(200).json(table);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar mesa"
          });
        }
      }
    };
    UpdateTableController_default = new UpdateTableController();
  }
});

// src/modules/table/services/DeactivateTableService.ts
var DeactivateTableService, DeactivateTableService_default;
var init_DeactivateTableService = __esm({
  "src/modules/table/services/DeactivateTableService.ts"() {
    init_TableRepository();
    init_TableSessionRepository();
    DeactivateTableService = class {
      async execute({ id, restaurantId }) {
        const normalizedId = Number(id);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedId) || normalizedId <= 0) {
          throw new Error("Mesa inv\xE1lida para desativa\xE7\xE3o.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para desativar mesa.");
        }
        const table = await TableRepository_default.findByIdForRestaurant(normalizedId, normalizedRestaurantId);
        if (!table) {
          throw new Error("Mesa n\xE3o encontrada!");
        }
        const openSession = await TableSessionRepository_default.findActiveByTable(normalizedId);
        if (openSession) {
          throw new Error("Feche o atendimento da mesa antes de desativ\xE1-la.");
        }
        return await TableRepository_default.deactivate(normalizedId);
      }
    };
    DeactivateTableService_default = new DeactivateTableService();
  }
});

// src/modules/table/controllers/DeactivateTableController.ts
var DeactivateTableController, DeactivateTableController_default;
var init_DeactivateTableController = __esm({
  "src/modules/table/controllers/DeactivateTableController.ts"() {
    init_DeactivateTableService();
    DeactivateTableController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const parsedId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const table = await DeactivateTableService_default.execute({
            id: parsedId,
            restaurantId
          });
          return res.status(200).json(table);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao desativar mesa"
          });
        }
      }
    };
    DeactivateTableController_default = new DeactivateTableController();
  }
});

// src/modules/table/services/DeleteTableService.ts
var DeleteTableService, DeleteTableService_default;
var init_DeleteTableService = __esm({
  "src/modules/table/services/DeleteTableService.ts"() {
    init_TableRepository();
    init_TableSessionRepository();
    DeleteTableService = class {
      async execute({ id, restaurantId }) {
        const normalizedId = Number(id);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedId) || normalizedId <= 0) {
          throw new Error("Mesa inv\xE1lida para exclus\xE3o.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para excluir mesa.");
        }
        const table = await TableRepository_default.findByIdForRestaurant(normalizedId, normalizedRestaurantId);
        if (!table) throw new Error("Mesa n\xE3o encontrada!");
        const openSession = await TableSessionRepository_default.findActiveByTable(normalizedId);
        if (openSession) throw new Error("Feche o atendimento da mesa antes de exclu\xED-la.");
        const deleted = await TableRepository_default.deleteIfUnused(normalizedId, normalizedRestaurantId);
        if (deleted !== 1) {
          throw new Error("N\xE3o \xE9 poss\xEDvel excluir uma mesa que possui pedidos no hist\xF3rico.");
        }
        return { id: normalizedId };
      }
    };
    DeleteTableService_default = new DeleteTableService();
  }
});

// src/modules/table/controllers/DeleteTableController.ts
var DeleteTableController, DeleteTableController_default;
var init_DeleteTableController = __esm({
  "src/modules/table/controllers/DeleteTableController.ts"() {
    init_DeleteTableService();
    DeleteTableController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const table = await DeleteTableService_default.execute({
            id,
            restaurantId: req.user.restaurantId
          });
          return res.status(200).json(table);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel excluir a mesa."
          });
        }
      }
    };
    DeleteTableController_default = new DeleteTableController();
  }
});

// src/modules/table/controllers/ResolvePublicTableController.ts
var ResolvePublicTableController, ResolvePublicTableController_default;
var init_ResolvePublicTableController = __esm({
  "src/modules/table/controllers/ResolvePublicTableController.ts"() {
    init_ResolvePublicTableService();
    ResolvePublicTableController = class {
      async handle(req, res) {
        try {
          const table = await ResolvePublicTableService_default.execute({
            tableNumber: String(req.query.tableNumber || ""),
            tableToken: String(req.query.tableToken || req.query.token || ""),
            tableId: req.query.tableId ? String(req.query.tableId) : null,
            restaurantId: req.query.restaurantId ? String(req.query.restaurantId) : null,
            restaurantSlug: req.query.slug ? String(req.query.slug) : null
          });
          return res.status(200).json(table);
        } catch (error2) {
          const statusCode = error2 instanceof PublicTableResolutionError ? error2.statusCode : 500;
          const code = error2 instanceof PublicTableResolutionError ? error2.code : "PUBLIC_TABLE_RESOLUTION_FAILED";
          return res.status(statusCode).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel identificar a mesa.",
            code
          });
        }
      }
    };
    ResolvePublicTableController_default = new ResolvePublicTableController();
  }
});

// src/modules/table/routes/TablesRoutes.ts
import { Router as Router8 } from "express";
var router8, TablesRoutes_default;
var init_TablesRoutes = __esm({
  "src/modules/table/routes/TablesRoutes.ts"() {
    init_authMiddleware();
    init_waiterMiddleware();
    init_adminMiddleware();
    init_CreateTableController();
    init_ListTableController();
    init_GetTableByIdController();
    init_UpdateTableController();
    init_DeactivateTableController();
    init_DeleteTableController();
    init_ResolvePublicTableController();
    init_billingMiddleware();
    init_premiumTablePlanMiddleware();
    router8 = Router8();
    router8.get("/public/resolve", (req, res) => ResolvePublicTableController_default.handle(req, res));
    router8.post(
      "/",
      authMiddleware,
      adminMiddleware,
      billingMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => CreateTableController_default.handle(req, res)
    );
    router8.get(
      "/",
      authMiddleware,
      waiterMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => ListTableController_default.handle(req, res)
    );
    router8.get(
      "/:id",
      authMiddleware,
      waiterMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => GetTableByIdController_default.handle(req, res)
    );
    router8.put(
      "/:id",
      authMiddleware,
      adminMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => UpdateTableController_default.handle(req, res)
    );
    router8.patch(
      "/:id",
      authMiddleware,
      adminMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => DeactivateTableController_default.handle(req, res)
    );
    router8.delete(
      "/:id",
      authMiddleware,
      adminMiddleware,
      premiumTablePlanMiddleware,
      (req, res) => DeleteTableController_default.handle(req, res)
    );
    TablesRoutes_default = router8;
  }
});

// src/modules/restaurantSettings/utils/normalizeRestaurantImage.ts
function normalizeRestaurantImage(value) {
  const normalized = String(value || "").trim();
  if (!normalized) return null;
  if (normalized.startsWith("blob:")) {
    throw new Error("A imagem enviada \xE9 tempor\xE1ria. Selecione o arquivo novamente.");
  }
  const isPersistentUrl = /^https?:\/\//i.test(normalized);
  const isPersistentImageData = /^data:image\/(jpeg|png|webp);base64,/i.test(normalized);
  if (!isPersistentUrl && !isPersistentImageData) {
    throw new Error("Formato de imagem inv\xE1lido.");
  }
  if (normalized.length > MAX_PERSISTENT_IMAGE_LENGTH) {
    throw new Error("A imagem ultrapassa o tamanho permitido.");
  }
  return normalized;
}
var MAX_PERSISTENT_IMAGE_LENGTH;
var init_normalizeRestaurantImage = __esm({
  "src/modules/restaurantSettings/utils/normalizeRestaurantImage.ts"() {
    MAX_PERSISTENT_IMAGE_LENGTH = 75e4;
  }
});

// src/modules/restaurantSettings/utils/establishmentAddress.ts
function normalizeEstablishmentAddress(input) {
  return {
    address: String(input.address || "").trim(),
    number: String(input.number || "").trim(),
    complement: String(input.complement || "").trim(),
    district: String(input.district || "").trim(),
    city: String(input.city || "").trim(),
    state: String(input.state || "").trim().toUpperCase(),
    zipCode: String(input.zipCode || "").replace(/\D/g, "")
  };
}
function hasEstablishmentAddress(address) {
  return Object.values(address).some(Boolean);
}
function validateEstablishmentAddress(address) {
  if (!hasEstablishmentAddress(address)) return null;
  if (address.zipCode.length !== 8) return "CEP do estabelecimento inv\xE1lido.";
  if (address.address.length < 3) return "Rua do estabelecimento inv\xE1lida.";
  if (!address.number) return "N\xFAmero do estabelecimento \xE9 obrigat\xF3rio.";
  if (address.district.length < 2) return "Bairro do estabelecimento inv\xE1lido.";
  if (address.city.length < 2) return "Cidade do estabelecimento inv\xE1lida.";
  if (!/^[A-Z]{2}$/.test(address.state)) return "UF do estabelecimento inv\xE1lida.";
  if (address.complement.length > 160) return "Complemento do estabelecimento muito longo.";
  return null;
}
var init_establishmentAddress = __esm({
  "src/modules/restaurantSettings/utils/establishmentAddress.ts"() {
  }
});

// src/modules/restaurantSettings/utils/adminSettingsValidation.ts
function normalizeNonNegativeMoney(value, label, fallback = 0) {
  const normalized = value === void 0 || value === null || value === "" ? fallback : Number(value);
  if (!Number.isFinite(normalized) || normalized < 0) {
    throw new Error(`${label} deve ser um valor v\xE1lido e n\xE3o negativo.`);
  }
  return Math.round(normalized * 100) / 100;
}
function normalizeOptionalNonNegativeMoney(value, label) {
  if (value === void 0 || value === null || value === "") return null;
  const normalized = normalizeNonNegativeMoney(value, label);
  return normalized > 0 ? normalized : null;
}
function normalizeStrictBoolean(value, label, fallback) {
  if (value === void 0) return fallback;
  if (typeof value !== "boolean") throw new Error(`${label} deve ser verdadeiro ou falso.`);
  return value;
}
function normalizeIntegerInRange(value, label, minimum, maximum, fallback) {
  const candidate = value === void 0 || value === null || value === "" ? fallback : value;
  const normalized = Number(candidate);
  if (!Number.isInteger(normalized) || normalized < minimum || normalized > maximum) {
    throw new Error(`${label} deve ser um n\xFAmero inteiro entre ${minimum} e ${maximum}.`);
  }
  return normalized;
}
function normalizePrimaryColor(value, fallback = "#c95d3d") {
  const normalized = String(value ?? fallback).trim();
  if (!/^#[0-9a-f]{6}$/i.test(normalized)) {
    throw new Error("Cor principal inv\xE1lida. Use o formato hexadecimal #RRGGBB.");
  }
  return normalized.toLowerCase();
}
function normalizeFontFamily(value, fallback = "Inter") {
  const normalized = String(value ?? fallback).trim() || fallback;
  if (!ALLOWED_FONT_FAMILIES.has(normalized)) {
    throw new Error("Fonte inv\xE1lida. Escolha Inter, Manrope ou DM Sans.");
  }
  return normalized;
}
function normalizeOptionalText(value, label, maxLength) {
  const normalized = String(value ?? "").trim();
  if (normalized.length > maxLength) {
    throw new Error(`${label} pode ter no m\xE1ximo ${maxLength} caracteres.`);
  }
  return normalized || null;
}
function normalizeSocialReference(value, label) {
  const normalized = normalizeOptionalText(value, label, 2048);
  if (!normalized) return null;
  if (/^javascript:/i.test(normalized)) throw new Error(`${label} inv\xE1lido.`);
  if (/^https?:\/\//i.test(normalized)) {
    try {
      const url = new URL(normalized);
      if (!["http:", "https:"].includes(url.protocol)) throw new Error("protocol");
    } catch {
      throw new Error(`${label} inv\xE1lido.`);
    }
    return normalized;
  }
  if (!/^[A-Za-z0-9@._/-]+$/.test(normalized)) {
    throw new Error(`${label} inv\xE1lido. Use o nome de usu\xE1rio ou a URL completa do perfil.`);
  }
  return normalized;
}
function normalizeWhatsappNumber(value) {
  const normalized = String(value ?? "").replace(/\D/g, "");
  if (normalized && !/^\d{10,15}$/.test(normalized)) {
    throw new Error("N\xFAmero do WhatsApp inv\xE1lido. Informe DDI, DDD e n\xFAmero.");
  }
  return normalized || null;
}
function isValidCpf(value) {
  const cpf = String(value ?? "").replace(/\D/g, "");
  if (!/^\d{11}$/.test(cpf) || /^(\d)\1+$/.test(cpf)) return false;
  const digit = (base, factor) => {
    const sum = base.split("").reduce((total, number, index) => total + Number(number) * (factor - index), 0);
    const result = sum * 10 % 11;
    return result === 10 ? 0 : result;
  };
  return cpf.endsWith(`${digit(cpf.slice(0, 9), 10)}${digit(cpf.slice(0, 10), 11)}`);
}
function isValidCnpj(value) {
  const cnpj = String(value ?? "").replace(/\D/g, "");
  if (!/^\d{14}$/.test(cnpj) || /^(\d)\1+$/.test(cnpj)) return false;
  const digit = (base, weights) => {
    const sum = base.split("").reduce((total, number, index) => total + Number(number) * weights[index], 0);
    const remainder = sum % 11;
    return remainder < 2 ? 0 : 11 - remainder;
  };
  const first2 = digit(cnpj.slice(0, 12), [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]);
  const second = digit(cnpj.slice(0, 12) + first2, [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]);
  return cnpj.endsWith(`${first2}${second}`);
}
var ALLOWED_FONT_FAMILIES;
var init_adminSettingsValidation = __esm({
  "src/modules/restaurantSettings/utils/adminSettingsValidation.ts"() {
    ALLOWED_FONT_FAMILIES = /* @__PURE__ */ new Set(["Inter", "Manrope", "DM Sans"]);
  }
});

// src/modules/restaurantSettings/services/CreateRestaurantSettingsService.ts
var CreateRestaurantSettingsService, CreateRestaurantSettingsService_default;
var init_CreateRestaurantSettingsService = __esm({
  "src/modules/restaurantSettings/services/CreateRestaurantSettingsService.ts"() {
    init_RestaurantSettingsRepository();
    init_prisma();
    init_normalizeRestaurantImage();
    init_establishmentAddress();
    init_businessHours();
    init_adminSettingsValidation();
    CreateRestaurantSettingsService = class {
      async execute({
        restaurantId,
        deliveryFee,
        courierFeePerDelivery,
        minimumOrder,
        freeShippingMinimum,
        acceptsDelivery,
        acceptsPickup,
        acceptsPix,
        acceptsCard,
        tableOrderingEnabled,
        waiterCallEnabled,
        billRequestEnabled,
        pixProvider,
        pixKey,
        legalDocumentType,
        companyDocument,
        companyLegalName,
        companyTradeName,
        companyAddress,
        companyCnae,
        monthlyRevenue,
        ownerFullName,
        ownerCpf,
        ownerBirthDate,
        ownerEmail,
        ownerPhone,
        ownerAddress,
        bankName,
        bankCode,
        bankAccountType,
        bankBranch,
        bankAccount,
        bankHolderDocument,
        cardGateway,
        gatewayMerchantId,
        stripeSecretKey,
        stripeWebhookSecret,
        mercadoPagoAccessToken,
        picpayToken,
        asaasAccessToken,
        pagbankEmail,
        pagbankToken,
        pagbankEnvironment,
        ownerDocumentFileUrl,
        bankProofFileUrl,
        companyContractFileUrl,
        whatsapp,
        whatsappEnabled,
        whatsappDisplayName,
        whatsappDefaultMessage,
        receiveOrdersOnWhatsapp,
        receiveStatusNotifications,
        instagram,
        facebook,
        tiktok,
        youtube,
        primaryColor,
        fontFamily,
        seoTitle,
        seoDescription,
        restaurantName,
        restaurantLogo,
        restaurantCoverImage,
        restaurantDescription,
        restaurantAddress,
        restaurantAddressNumber,
        restaurantAddressComplement,
        restaurantAddressDistrict,
        restaurantCity,
        restaurantState,
        restaurantZipCode,
        businessHours,
        isOpenForOrders,
        averageDeliveryTime,
        autoAcceptOrders,
        trackingRequiresLogin,
        soundNotifications,
        maxConcurrentOrders
      }) {
        const settingsExists = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        if (settingsExists) {
          throw new Error("Configura\xE7\xF5es j\xE1 existem para esse restaurante!");
        }
        const normalizedWhatsapp = whatsapp === void 0 ? void 0 : normalizeWhatsappNumber(whatsapp);
        const normalizedRestaurantName = restaurantName === void 0 ? void 0 : String(restaurantName || "").trim();
        const normalizedRestaurantLogo = restaurantLogo === void 0 ? void 0 : normalizeRestaurantImage(restaurantLogo);
        const normalizedRestaurantCoverImage = restaurantCoverImage === void 0 ? void 0 : normalizeRestaurantImage(restaurantCoverImage);
        const normalizedRestaurantDescription = restaurantDescription === void 0 ? void 0 : normalizeOptionalText(restaurantDescription, "Descri\xE7\xE3o do restaurante", 500);
        const establishmentAddress = normalizeEstablishmentAddress({
          address: restaurantAddress,
          number: restaurantAddressNumber,
          complement: restaurantAddressComplement,
          district: restaurantAddressDistrict,
          city: restaurantCity,
          state: restaurantState,
          zipCode: restaurantZipCode
        });
        const addressValidationError = validateEstablishmentAddress(establishmentAddress);
        if (addressValidationError) throw new Error(addressValidationError);
        if (restaurantName !== void 0 && (String(normalizedRestaurantName || "").length < 2 || String(normalizedRestaurantName || "").length > 120)) {
          throw new Error("Nome do restaurante deve ter entre 2 e 120 caracteres.");
        }
        const normalizedLegalDocumentType = String(legalDocumentType || "").trim().toUpperCase();
        const normalizedCompanyDocument = String(companyDocument || "").replace(/\D/g, "");
        const normalizedBankHolderDocument = String(bankHolderDocument || "").replace(/\D/g, "");
        const normalizedOwnerCpf = String(ownerCpf || "").replace(/\D/g, "");
        const normalizedOwnerPhone = String(ownerPhone || "").replace(/\D/g, "");
        const normalizedBusinessHours = normalizeBusinessHours(businessHours);
        const normalizedWhatsappEnabled = normalizeStrictBoolean(
          whatsappEnabled,
          "Integra\xE7\xE3o com WhatsApp",
          false
        );
        if (normalizedWhatsappEnabled && !normalizedWhatsapp) {
          throw new Error("Informe o n\xFAmero comercial antes de ativar o WhatsApp.");
        }
        if (normalizedLegalDocumentType === "CNPJ" && normalizedCompanyDocument.length > 0 && !isValidCnpj(normalizedCompanyDocument)) {
          throw new Error("CNPJ inv\xE1lido para cadastro da empresa.");
        }
        if (normalizedLegalDocumentType === "CPF" && normalizedCompanyDocument.length > 0 && !isValidCpf(normalizedCompanyDocument)) {
          throw new Error("CPF inv\xE1lido para cadastro de aut\xF4nomo.");
        }
        if (normalizedCompanyDocument && normalizedBankHolderDocument && normalizedCompanyDocument !== normalizedBankHolderDocument) {
          throw new Error(
            "A titularidade da conta banc\xE1ria deve ser igual ao documento cadastrado (CPF/CNPJ)."
          );
        }
        if (companyLegalName !== void 0 && String(companyLegalName || "").trim().length < 2) {
          throw new Error("Raz\xE3o social inv\xE1lida.");
        }
        if (ownerPhone !== void 0 && (!normalizedOwnerPhone || !/^\d{10,11}$/.test(normalizedOwnerPhone))) {
          throw new Error("Telefone comercial inv\xE1lido.");
        }
        const normalizedOwnerEmail = ownerEmail === void 0 ? void 0 : String(ownerEmail || "").trim().toLowerCase();
        if (normalizedOwnerEmail !== void 0 && (!normalizedOwnerEmail || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(normalizedOwnerEmail))) {
          throw new Error("E-mail comercial inv\xE1lido.");
        }
        const created = await RestaurantSettingsRepository_default.create({
          restaurantId: Number(restaurantId),
          deliveryFee: normalizeNonNegativeMoney(deliveryFee, "Taxa de entrega"),
          courierFeePerDelivery: normalizeNonNegativeMoney(
            courierFeePerDelivery,
            "Repasse por entrega"
          ),
          minimumOrder: normalizeNonNegativeMoney(minimumOrder, "Pedido m\xEDnimo"),
          freeShippingMinimum: normalizeOptionalNonNegativeMoney(
            freeShippingMinimum,
            "Frete gr\xE1tis acima de"
          ),
          acceptsDelivery: normalizeStrictBoolean(acceptsDelivery, "Delivery", true),
          acceptsPickup: normalizeStrictBoolean(acceptsPickup, "Retirada", true),
          acceptsPix: normalizeStrictBoolean(acceptsPix, "Pagamento por PIX", true),
          acceptsCard: normalizeStrictBoolean(acceptsCard, "Pagamento por cart\xE3o", true),
          tableOrderingEnabled: normalizeStrictBoolean(
            tableOrderingEnabled,
            "Pedidos por QR Code",
            true
          ),
          waiterCallEnabled: normalizeStrictBoolean(waiterCallEnabled, "Chamados ao gar\xE7om", true),
          billRequestEnabled: normalizeStrictBoolean(billRequestEnabled, "Solicita\xE7\xE3o da conta", true),
          pixProvider: String(pixProvider || "MERCADO_PAGO").trim().toUpperCase(),
          pixKey,
          legalDocumentType: normalizedLegalDocumentType || null,
          companyDocument: normalizedCompanyDocument || null,
          companyLegalName: String(companyLegalName || "").trim() || null,
          companyTradeName: String(companyTradeName || "").trim() || null,
          companyAddress: String(companyAddress || "").trim() || null,
          companyCnae: String(companyCnae || "").trim() || null,
          monthlyRevenue: monthlyRevenue === void 0 || monthlyRevenue === null ? null : Number(monthlyRevenue),
          ownerFullName: String(ownerFullName || "").trim() || null,
          ownerCpf: normalizedOwnerCpf || null,
          ownerBirthDate: ownerBirthDate ? new Date(ownerBirthDate) : null,
          ownerEmail: normalizedOwnerEmail || null,
          ownerPhone: normalizedOwnerPhone || null,
          ownerAddress: String(ownerAddress || "").trim() || null,
          bankName: String(bankName || "").trim() || null,
          bankCode: String(bankCode || "").trim() || null,
          bankAccountType: String(bankAccountType || "").trim().toUpperCase() || null,
          bankBranch: String(bankBranch || "").trim() || null,
          bankAccount: String(bankAccount || "").trim() || null,
          bankHolderDocument: normalizedBankHolderDocument || null,
          cardGateway: String(cardGateway || "").trim() || null,
          gatewayMerchantId: String(gatewayMerchantId || "").trim() || null,
          stripeSecretKey: String(stripeSecretKey || "").trim() || null,
          stripeWebhookSecret: String(stripeWebhookSecret || "").trim() || null,
          mercadoPagoAccessToken: String(mercadoPagoAccessToken || "").trim() || null,
          picpayToken: String(picpayToken || "").trim() || null,
          asaasAccessToken: String(asaasAccessToken || "").trim() || null,
          pagbankEmail: String(pagbankEmail || "").trim() || null,
          pagbankToken: String(pagbankToken || "").trim() || null,
          pagbankEnvironment: "production",
          ownerDocumentFileUrl: String(ownerDocumentFileUrl || "").trim() || null,
          bankProofFileUrl: String(bankProofFileUrl || "").trim() || null,
          companyContractFileUrl: String(companyContractFileUrl || "").trim() || null,
          primaryColor: normalizePrimaryColor(primaryColor),
          instagram: normalizeSocialReference(instagram, "Instagram"),
          facebook: normalizeSocialReference(facebook, "Facebook"),
          tiktok: normalizeSocialReference(tiktok, "TikTok"),
          youtube: normalizeSocialReference(youtube, "YouTube"),
          fontFamily: normalizeFontFamily(fontFamily),
          seoTitle: normalizeOptionalText(seoTitle, "T\xEDtulo para buscadores", 70),
          seoDescription: normalizeOptionalText(seoDescription, "Descri\xE7\xE3o para buscadores", 160),
          whatsappEnabled: normalizedWhatsappEnabled,
          whatsappDisplayName: normalizeOptionalText(
            whatsappDisplayName,
            "Nome exibido no WhatsApp",
            80
          ),
          whatsappDefaultMessage: normalizeOptionalText(
            whatsappDefaultMessage,
            "Mensagem inicial do WhatsApp",
            500
          ),
          receiveOrdersOnWhatsapp: normalizeStrictBoolean(
            receiveOrdersOnWhatsapp,
            "Pedidos pelo WhatsApp",
            false
          ),
          receiveStatusNotifications: normalizeStrictBoolean(
            receiveStatusNotifications,
            "Notifica\xE7\xF5es de status pelo WhatsApp",
            false
          ),
          businessHours: normalizedBusinessHours,
          isOpenForOrders: normalizeStrictBoolean(isOpenForOrders, "Recebimento de pedidos", true),
          averageDeliveryTime: averageDeliveryTime === void 0 ? void 0 : String(normalizeIntegerInRange(averageDeliveryTime, "Tempo m\xE9dio de preparo", 1, 240)),
          autoAcceptOrders: normalizeStrictBoolean(
            autoAcceptOrders,
            "Aceite autom\xE1tico de pedidos",
            false
          ),
          trackingRequiresLogin: normalizeStrictBoolean(
            trackingRequiresLogin,
            "Login para rastreamento",
            true
          ),
          soundNotifications: normalizeStrictBoolean(soundNotifications, "Notifica\xE7\xE3o sonora", true),
          maxConcurrentOrders: normalizeIntegerInRange(
            maxConcurrentOrders,
            "Limite de pedidos simult\xE2neos",
            1,
            500,
            20
          )
        });
        const restaurantData = {};
        if (normalizedWhatsapp !== void 0) {
          restaurantData.whatsapp = normalizedWhatsapp;
        }
        if (normalizedRestaurantName !== void 0) {
          restaurantData.name = normalizedRestaurantName;
        }
        if (normalizedRestaurantLogo !== void 0) {
          restaurantData.logo = normalizedRestaurantLogo;
        }
        if (normalizedRestaurantCoverImage !== void 0) {
          restaurantData.coverImage = normalizedRestaurantCoverImage;
        }
        if (normalizedRestaurantDescription !== void 0) {
          restaurantData.description = normalizedRestaurantDescription;
        }
        if (establishmentAddress.address) {
          restaurantData.address = establishmentAddress.address;
          restaurantData.addressNumber = establishmentAddress.number;
          restaurantData.addressComplement = establishmentAddress.complement || null;
          restaurantData.addressDistrict = establishmentAddress.district;
          restaurantData.city = establishmentAddress.city;
          restaurantData.state = establishmentAddress.state;
          restaurantData.zipCode = establishmentAddress.zipCode;
        }
        if (Object.keys(restaurantData).length > 0) {
          await prisma_default.restaurant.update({
            where: {
              id: Number(restaurantId)
            },
            data: restaurantData
          });
        }
        return {
          ...created,
          stripeSecretKey: null,
          stripeWebhookSecret: null,
          mercadoPagoAccessToken: null,
          picpayToken: null,
          asaasAccessToken: null,
          pagbankToken: null,
          stripeSecretKeyConfigured: Boolean(String(created?.stripeSecretKey || "").trim()),
          stripeWebhookSecretConfigured: Boolean(String(created?.stripeWebhookSecret || "").trim()),
          mercadoPagoAccessTokenConfigured: Boolean(
            String(created?.mercadoPagoAccessToken || "").trim()
          ),
          picpayTokenConfigured: Boolean(String(created?.picpayToken || "").trim()),
          asaasAccessTokenConfigured: Boolean(String(created?.asaasAccessToken || "").trim()),
          pagbankTokenConfigured: Boolean(String(created?.pagbankToken || "").trim()),
          whatsapp: normalizedWhatsapp ?? null,
          restaurantName: normalizedRestaurantName ?? null,
          restaurantLogo: normalizedRestaurantLogo ?? null,
          restaurantCoverImage: normalizedRestaurantCoverImage ?? null,
          restaurantDescription: normalizedRestaurantDescription ?? null
        };
      }
    };
    CreateRestaurantSettingsService_default = new CreateRestaurantSettingsService();
  }
});

// src/modules/restaurantSettings/controllers/CreateRestaurantSettingsController.ts
var CreateRestaurantSettingsController, CreateRestaurantSettingsController_default;
var init_CreateRestaurantSettingsController = __esm({
  "src/modules/restaurantSettings/controllers/CreateRestaurantSettingsController.ts"() {
    init_CreateRestaurantSettingsService();
    CreateRestaurantSettingsController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const {
            deliveryFee,
            courierFeePerDelivery,
            minimumOrder,
            freeShippingMinimum,
            acceptsDelivery,
            acceptsPickup,
            acceptsPix,
            acceptsCard,
            tableOrderingEnabled,
            waiterCallEnabled,
            billRequestEnabled,
            pixProvider,
            pixKey,
            legalDocumentType,
            companyDocument,
            companyLegalName,
            companyTradeName,
            companyAddress,
            companyCnae,
            monthlyRevenue,
            ownerFullName,
            ownerCpf,
            ownerBirthDate,
            ownerEmail,
            ownerPhone,
            ownerAddress,
            bankName,
            bankCode,
            bankAccountType,
            bankBranch,
            bankAccount,
            bankHolderDocument,
            cardGateway,
            gatewayMerchantId,
            stripeSecretKey,
            stripeWebhookSecret,
            mercadoPagoAccessToken,
            picpayToken,
            asaasAccessToken,
            pagbankEmail,
            pagbankToken,
            pagbankEnvironment,
            ownerDocumentFileUrl,
            bankProofFileUrl,
            companyContractFileUrl,
            whatsapp,
            whatsappEnabled,
            whatsappDisplayName,
            whatsappDefaultMessage,
            receiveOrdersOnWhatsapp,
            receiveStatusNotifications,
            instagram,
            facebook,
            tiktok,
            youtube,
            primaryColor,
            fontFamily,
            seoTitle,
            seoDescription,
            restaurantName,
            restaurantLogo,
            restaurantCoverImage,
            restaurantDescription,
            restaurantAddress,
            restaurantAddressNumber,
            restaurantAddressComplement,
            restaurantAddressDistrict,
            restaurantCity,
            restaurantState,
            restaurantZipCode,
            businessHours,
            isOpenForOrders,
            averageDeliveryTime,
            autoAcceptOrders,
            trackingRequiresLogin,
            soundNotifications,
            maxConcurrentOrders
          } = req.body;
          const settings = await CreateRestaurantSettingsService_default.execute({
            restaurantId,
            deliveryFee,
            courierFeePerDelivery,
            minimumOrder,
            freeShippingMinimum,
            acceptsDelivery,
            acceptsPickup,
            acceptsPix,
            acceptsCard,
            tableOrderingEnabled,
            waiterCallEnabled,
            billRequestEnabled,
            pixProvider,
            pixKey,
            legalDocumentType,
            companyDocument,
            companyLegalName,
            companyTradeName,
            companyAddress,
            companyCnae,
            monthlyRevenue,
            ownerFullName,
            ownerCpf,
            ownerBirthDate,
            ownerEmail,
            ownerPhone,
            ownerAddress,
            bankName,
            bankCode,
            bankAccountType,
            bankBranch,
            bankAccount,
            bankHolderDocument,
            cardGateway,
            gatewayMerchantId,
            stripeSecretKey,
            stripeWebhookSecret,
            mercadoPagoAccessToken,
            picpayToken,
            asaasAccessToken,
            pagbankEmail,
            pagbankToken,
            pagbankEnvironment,
            ownerDocumentFileUrl,
            bankProofFileUrl,
            companyContractFileUrl,
            whatsapp,
            whatsappEnabled,
            whatsappDisplayName,
            whatsappDefaultMessage,
            receiveOrdersOnWhatsapp,
            receiveStatusNotifications,
            instagram,
            facebook,
            tiktok,
            youtube,
            primaryColor,
            fontFamily,
            seoTitle,
            seoDescription,
            restaurantName,
            restaurantLogo,
            restaurantCoverImage,
            restaurantDescription,
            restaurantAddress,
            restaurantAddressNumber,
            restaurantAddressComplement,
            restaurantAddressDistrict,
            restaurantCity,
            restaurantState,
            restaurantZipCode,
            businessHours,
            isOpenForOrders,
            averageDeliveryTime,
            autoAcceptOrders,
            trackingRequiresLogin,
            soundNotifications,
            maxConcurrentOrders
          });
          return res.status(201).json(settings);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar configuracoes do restaurante"
          });
        }
      }
    };
    CreateRestaurantSettingsController_default = new CreateRestaurantSettingsController();
  }
});

// src/modules/restaurantSettings/services/GetRestaurantSettingsService.ts
var GetRestaurantSettingsService, GetRestaurantSettingsService_default;
var init_GetRestaurantSettingsService = __esm({
  "src/modules/restaurantSettings/services/GetRestaurantSettingsService.ts"() {
    init_RestaurantSettingsRepository();
    GetRestaurantSettingsService = class {
      async execute({ restaurantId }) {
        const normalizedRestaurantId = Number(restaurantId);
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId);
        if (!settings) {
          const restaurant = await RestaurantSettingsRepository_default.findRestaurantById(normalizedRestaurantId);
          if (!restaurant) {
            throw new Error("Restaurante n\xE3o encontrado!");
          }
          const fallback = {
            id: null,
            restaurantId: normalizedRestaurantId,
            deliveryFee: 0,
            minimumOrder: 0,
            freeShippingMinimum: null,
            acceptsDelivery: true,
            acceptsPickup: true,
            acceptsPix: true,
            acceptsCard: true,
            tableOrderingEnabled: true,
            waiterCallEnabled: true,
            billRequestEnabled: true,
            pixProvider: "MERCADO_PAGO",
            pixKey: null,
            legalDocumentType: null,
            companyDocument: null,
            companyLegalName: null,
            companyTradeName: null,
            companyAddress: null,
            companyCnae: null,
            monthlyRevenue: null,
            ownerFullName: null,
            ownerCpf: null,
            ownerBirthDate: null,
            ownerEmail: null,
            ownerPhone: null,
            ownerAddress: null,
            bankName: null,
            bankCode: null,
            bankAccountType: null,
            bankBranch: null,
            bankAccount: null,
            bankHolderDocument: null,
            cardGateway: null,
            gatewayMerchantId: null,
            stripeSecretKey: null,
            stripeWebhookSecret: null,
            mercadoPagoAccessToken: null,
            picpayToken: null,
            asaasAccessToken: null,
            pagbankEmail: null,
            pagbankToken: null,
            pagbankEnvironment: null,
            stripeSecretKeyConfigured: false,
            stripeWebhookSecretConfigured: false,
            mercadoPagoAccessTokenConfigured: false,
            picpayTokenConfigured: false,
            asaasAccessTokenConfigured: false,
            pagbankTokenConfigured: false,
            ownerDocumentFileUrl: null,
            bankProofFileUrl: null,
            companyContractFileUrl: null,
            instagram: null,
            facebook: null,
            tiktok: null,
            youtube: null,
            primaryColor: "#c95d3d",
            fontFamily: "Inter",
            seoTitle: null,
            seoDescription: null,
            whatsapp: String(restaurant.whatsapp || "").trim() || null,
            whatsappEnabled: false,
            whatsappDisplayName: null,
            whatsappDefaultMessage: null,
            receiveOrdersOnWhatsapp: false,
            receiveStatusNotifications: false,
            businessHours: null,
            isOpenForOrders: true,
            averageDeliveryTime: null,
            autoAcceptOrders: false,
            trackingRequiresLogin: true,
            soundNotifications: true,
            maxConcurrentOrders: 20,
            restaurant: {
              name: restaurant.name,
              logo: restaurant.logo,
              coverImage: restaurant.coverImage,
              description: restaurant.description,
              whatsapp: String(restaurant.whatsapp || "").trim() || null,
              address: restaurant.address,
              addressNumber: restaurant.addressNumber,
              addressComplement: restaurant.addressComplement,
              addressDistrict: restaurant.addressDistrict,
              city: restaurant.city,
              state: restaurant.state,
              zipCode: restaurant.zipCode
            }
          };
          return fallback;
        }
        return {
          ...settings,
          stripeSecretKey: null,
          stripeWebhookSecret: null,
          mercadoPagoAccessToken: null,
          picpayToken: null,
          asaasAccessToken: null,
          pagbankToken: null,
          stripeSecretKeyConfigured: Boolean(String(settings?.stripeSecretKey || "").trim()),
          stripeWebhookSecretConfigured: Boolean(String(settings?.stripeWebhookSecret || "").trim()),
          mercadoPagoAccessTokenConfigured: Boolean(
            String(settings?.mercadoPagoAccessToken || "").trim()
          ),
          picpayTokenConfigured: Boolean(String(settings?.picpayToken || "").trim()),
          asaasAccessTokenConfigured: Boolean(String(settings?.asaasAccessToken || "").trim()),
          pagbankTokenConfigured: Boolean(String(settings?.pagbankToken || "").trim()),
          whatsapp: String(settings?.restaurant?.whatsapp || "").trim() || null
        };
      }
    };
    GetRestaurantSettingsService_default = new GetRestaurantSettingsService();
  }
});

// src/modules/restaurantSettings/controllers/GetRestaurantSettingsController.ts
var GetRestaurantSettingsController, GetRestaurantSettingsController_default;
var init_GetRestaurantSettingsController = __esm({
  "src/modules/restaurantSettings/controllers/GetRestaurantSettingsController.ts"() {
    init_GetRestaurantSettingsService();
    GetRestaurantSettingsController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const settings = await GetRestaurantSettingsService_default.execute({
            restaurantId
          });
          return res.status(200).json(settings);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao buscar configuracoes do restaurante"
          });
        }
      }
    };
    GetRestaurantSettingsController_default = new GetRestaurantSettingsController();
  }
});

// src/modules/restaurantSettings/services/UpdateRestaurantSettingsService.ts
var UpdateRestaurantSettingsService, UpdateRestaurantSettingsService_default;
var init_UpdateRestaurantSettingsService = __esm({
  "src/modules/restaurantSettings/services/UpdateRestaurantSettingsService.ts"() {
    init_RestaurantSettingsRepository();
    init_prisma();
    init_normalizeRestaurantImage();
    init_establishmentAddress();
    init_businessHours();
    init_adminSettingsValidation();
    UpdateRestaurantSettingsService = class {
      isValidCpf(value) {
        if (!/^\d{11}$/.test(value) || /^(\d)\1+$/.test(value)) return false;
        const digit = (base, factor) => {
          const sum = base.split("").reduce((total, number, index) => total + Number(number) * (factor - index), 0);
          const result = sum * 10 % 11;
          return result === 10 ? 0 : result;
        };
        return value.endsWith(`${digit(value.slice(0, 9), 10)}${digit(value.slice(0, 10), 11)}`);
      }
      isValidCnpj(value) {
        if (!/^\d{14}$/.test(value) || /^(\d)\1+$/.test(value)) return false;
        const digit = (base, weights) => {
          const sum = base.split("").reduce((total, number, index) => total + Number(number) * weights[index], 0);
          const remainder = sum % 11;
          return remainder < 2 ? 0 : 11 - remainder;
        };
        const first2 = digit(value.slice(0, 12), [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]);
        const second = digit(value.slice(0, 12) + first2, [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]);
        return value.endsWith(`${first2}${second}`);
      }
      getAsaasBaseUrl() {
        return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
      }
      async resolveAsaasWalletIdentifierByToken(token) {
        const normalizedToken = String(token || "").trim();
        if (!normalizedToken) {
          return "";
        }
        const response = await fetch(`${this.getAsaasBaseUrl()}/v3/myAccount`, {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
            access_token: normalizedToken
          }
        });
        if (!response.ok) {
          return "";
        }
        const body = await response.json();
        return String(body?.walletId || body?.id || "").trim();
      }
      async execute({
        restaurantId,
        deliveryFee,
        courierFeePerDelivery,
        minimumOrder,
        freeShippingMinimum,
        acceptsDelivery,
        acceptsPickup,
        acceptsPix,
        acceptsCard,
        tableOrderingEnabled,
        waiterCallEnabled,
        billRequestEnabled,
        pixProvider,
        pixKey,
        legalDocumentType,
        companyDocument,
        companyLegalName,
        companyTradeName,
        companyAddress,
        companyCnae,
        monthlyRevenue,
        ownerFullName,
        ownerCpf,
        ownerBirthDate,
        ownerEmail,
        ownerPhone,
        ownerAddress,
        bankName,
        bankCode,
        bankAccountType,
        bankBranch,
        bankAccount,
        bankHolderDocument,
        cardGateway,
        gatewayMerchantId,
        stripeSecretKey,
        stripeWebhookSecret,
        mercadoPagoAccessToken,
        picpayToken,
        asaasAccessToken,
        pagbankEmail,
        pagbankToken,
        pagbankEnvironment,
        ownerDocumentFileUrl,
        bankProofFileUrl,
        companyContractFileUrl,
        whatsapp,
        whatsappEnabled,
        whatsappDisplayName,
        whatsappDefaultMessage,
        receiveOrdersOnWhatsapp,
        receiveStatusNotifications,
        instagram,
        facebook,
        tiktok,
        youtube,
        primaryColor,
        fontFamily,
        seoTitle,
        seoDescription,
        restaurantName,
        restaurantLogo,
        restaurantCoverImage,
        restaurantDescription,
        restaurantAddress,
        restaurantAddressNumber,
        restaurantAddressComplement,
        restaurantAddressDistrict,
        restaurantCity,
        restaurantState,
        restaurantZipCode,
        businessHours,
        isOpenForOrders,
        averageDeliveryTime,
        autoAcceptOrders,
        trackingRequiresLogin,
        soundNotifications,
        maxConcurrentOrders
      }) {
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        if (!settings) {
          throw new Error("Configura\xE7\xF5es n\xE3o encontradas!");
        }
        const normalizedWhatsapp = whatsapp === void 0 ? void 0 : normalizeWhatsappNumber(whatsapp);
        const normalizedRestaurantName = restaurantName === void 0 ? void 0 : String(restaurantName || "").trim();
        const normalizedRestaurantLogo = restaurantLogo === void 0 ? void 0 : normalizeRestaurantImage(restaurantLogo);
        const normalizedRestaurantCoverImage = restaurantCoverImage === void 0 ? void 0 : normalizeRestaurantImage(restaurantCoverImage);
        const normalizedRestaurantDescription = restaurantDescription === void 0 ? void 0 : normalizeOptionalText(restaurantDescription, "Descri\xE7\xE3o do restaurante", 500);
        const establishmentAddress = normalizeEstablishmentAddress({
          address: restaurantAddress,
          number: restaurantAddressNumber,
          complement: restaurantAddressComplement,
          district: restaurantAddressDistrict,
          city: restaurantCity,
          state: restaurantState,
          zipCode: restaurantZipCode
        });
        const hasAddressPayload = [
          restaurantAddress,
          restaurantAddressNumber,
          restaurantAddressComplement,
          restaurantAddressDistrict,
          restaurantCity,
          restaurantState,
          restaurantZipCode
        ].some((value) => value !== void 0);
        const addressValidationError = hasAddressPayload ? validateEstablishmentAddress(establishmentAddress) : null;
        if (addressValidationError) throw new Error(addressValidationError);
        const normalizedBankName = bankName === void 0 ? void 0 : String(bankName || "").trim() || null;
        const normalizedBankBranch = bankBranch === void 0 ? void 0 : String(bankBranch || "").trim() || null;
        const normalizedBankAccount = bankAccount === void 0 ? void 0 : String(bankAccount || "").trim() || null;
        const normalizedCardGateway = cardGateway === void 0 ? void 0 : String(cardGateway || "").trim() || null;
        const normalizedGatewayMerchantId = gatewayMerchantId === void 0 ? void 0 : String(gatewayMerchantId || "").trim() || null;
        const normalizedStripeSecretKey = stripeSecretKey === void 0 ? void 0 : String(stripeSecretKey || "").trim() || null;
        const normalizedStripeWebhookSecret = stripeWebhookSecret === void 0 ? void 0 : String(stripeWebhookSecret || "").trim() || null;
        const normalizedMercadoPagoAccessToken = mercadoPagoAccessToken === void 0 ? void 0 : String(mercadoPagoAccessToken || "").trim() || null;
        const normalizedPicPayToken = picpayToken === void 0 ? void 0 : String(picpayToken || "").trim() || null;
        const normalizedAsaasAccessToken = asaasAccessToken === void 0 ? void 0 : String(asaasAccessToken || "").trim() || null;
        const normalizedPagBankEmail = pagbankEmail === void 0 ? void 0 : String(pagbankEmail || "").trim() || null;
        const normalizedPagBankToken = pagbankToken === void 0 ? void 0 : String(pagbankToken || "").trim() || null;
        const normalizedPagBankEnvironment = "production";
        const normalizedBusinessHours = normalizeBusinessHours(businessHours);
        const normalizedIsOpenForOrders = isOpenForOrders === void 0 ? void 0 : normalizeStrictBoolean(isOpenForOrders, "Recebimento de pedidos", true);
        const normalizedAverageDeliveryTime = averageDeliveryTime === void 0 ? void 0 : String(normalizeIntegerInRange(averageDeliveryTime, "Tempo m\xE9dio de preparo", 1, 240));
        const normalizedAutoAcceptOrders = autoAcceptOrders === void 0 ? void 0 : normalizeStrictBoolean(autoAcceptOrders, "Aceite autom\xE1tico de pedidos", false);
        const normalizedTrackingRequiresLogin = trackingRequiresLogin === void 0 ? void 0 : normalizeStrictBoolean(trackingRequiresLogin, "Login para rastreamento", true);
        const normalizedSoundNotifications = soundNotifications === void 0 ? void 0 : normalizeStrictBoolean(soundNotifications, "Notifica\xE7\xE3o sonora", true);
        const normalizedMaxConcurrentOrders = maxConcurrentOrders === void 0 ? void 0 : normalizeIntegerInRange(maxConcurrentOrders, "Limite de pedidos simult\xE2neos", 1, 500);
        const normalizedWhatsappEnabled = whatsappEnabled === void 0 ? void 0 : normalizeStrictBoolean(whatsappEnabled, "Integra\xE7\xE3o com WhatsApp", false);
        const resolvedWhatsappNumber = normalizedWhatsapp === void 0 ? String(settings?.restaurant?.whatsapp || "").replace(/\D/g, "") || null : normalizedWhatsapp;
        if (normalizedWhatsappEnabled === true && !resolvedWhatsappNumber) {
          throw new Error("Informe o n\xFAmero comercial antes de ativar o WhatsApp.");
        }
        const normalizedLegalDocumentType = legalDocumentType === void 0 ? void 0 : String(legalDocumentType || "").trim().toUpperCase() || null;
        const normalizedCompanyDocument = companyDocument === void 0 ? void 0 : String(companyDocument || "").replace(/\D/g, "") || null;
        const normalizedOwnerCpf = ownerCpf === void 0 ? void 0 : String(ownerCpf || "").replace(/\D/g, "") || null;
        const normalizedOwnerPhone = ownerPhone === void 0 ? void 0 : String(ownerPhone || "").replace(/\D/g, "") || null;
        const normalizedOwnerEmail = ownerEmail === void 0 ? void 0 : String(ownerEmail || "").trim().toLowerCase() || null;
        const normalizedBankHolderDocument = bankHolderDocument === void 0 ? void 0 : String(bankHolderDocument || "").replace(/\D/g, "") || null;
        const normalizedOwnerBirthDate = ownerBirthDate === void 0 ? void 0 : ownerBirthDate ? new Date(ownerBirthDate) : null;
        const resolvedAsaasToken = normalizedAsaasAccessToken === void 0 ? String(settings.asaasAccessToken || "").trim() : String(normalizedAsaasAccessToken || "").trim();
        const resolvedPixProvider = String(pixProvider || settings.pixProvider || "MERCADO_PAGO").trim().toUpperCase();
        const resolvedCardGateway = normalizedCardGateway === void 0 ? String(settings.cardGateway || "").trim().toUpperCase() : String(normalizedCardGateway || "").trim().toUpperCase();
        let resolvedGatewayMerchantId = normalizedGatewayMerchantId === void 0 ? String(settings.gatewayMerchantId || "").trim() || null : normalizedGatewayMerchantId;
        let gatewayMerchantIdAutoResolved = false;
        let gatewayMerchantIdAutoResolvedSource = null;
        const shouldTryAutoResolveGatewayMerchantId = !resolvedGatewayMerchantId && Boolean(resolvedAsaasToken) && (resolvedPixProvider === "ASAAS" || resolvedCardGateway === "ASAAS");
        if (shouldTryAutoResolveGatewayMerchantId) {
          try {
            const autoWalletId = await this.resolveAsaasWalletIdentifierByToken(resolvedAsaasToken);
            if (autoWalletId) {
              resolvedGatewayMerchantId = autoWalletId;
              gatewayMerchantIdAutoResolved = true;
              gatewayMerchantIdAutoResolvedSource = "asaas_myAccount";
            }
          } catch {
          }
        }
        if (restaurantName !== void 0 && (String(normalizedRestaurantName || "").length < 2 || String(normalizedRestaurantName || "").length > 120)) {
          throw new Error("Nome do restaurante deve ter entre 2 e 120 caracteres.");
        }
        const resolvedDocumentType = normalizedLegalDocumentType || String(settings.legalDocumentType || "");
        const resolvedCompanyDocument = normalizedCompanyDocument || String(settings.companyDocument || "");
        const resolvedBankHolderDocument = normalizedBankHolderDocument || String(settings.bankHolderDocument || "");
        if (resolvedDocumentType === "CNPJ" && resolvedCompanyDocument && !this.isValidCnpj(resolvedCompanyDocument)) {
          throw new Error("CNPJ inv\xE1lido para cadastro da empresa.");
        }
        if (resolvedDocumentType === "CPF" && resolvedCompanyDocument && !this.isValidCpf(resolvedCompanyDocument)) {
          throw new Error("CPF inv\xE1lido para cadastro de aut\xF4nomo.");
        }
        if (resolvedCompanyDocument && resolvedBankHolderDocument && resolvedCompanyDocument !== resolvedBankHolderDocument) {
          throw new Error(
            "A titularidade da conta banc\xE1ria deve ser igual ao documento cadastrado (CPF/CNPJ)."
          );
        }
        if (companyLegalName !== void 0 && String(companyLegalName || "").trim().length < 2) {
          throw new Error("Raz\xE3o social inv\xE1lida.");
        }
        if (normalizedOwnerPhone !== void 0 && (!normalizedOwnerPhone || !/^\d{10,11}$/.test(normalizedOwnerPhone))) {
          throw new Error("Telefone comercial inv\xE1lido.");
        }
        if (normalizedOwnerEmail !== void 0 && (!normalizedOwnerEmail || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(normalizedOwnerEmail))) {
          throw new Error("E-mail comercial inv\xE1lido.");
        }
        const updated = await RestaurantSettingsRepository_default.update(restaurantId, {
          deliveryFee: deliveryFee === void 0 ? void 0 : normalizeNonNegativeMoney(deliveryFee, "Taxa de entrega"),
          courierFeePerDelivery: courierFeePerDelivery === void 0 ? void 0 : normalizeNonNegativeMoney(courierFeePerDelivery, "Repasse por entrega"),
          minimumOrder: minimumOrder === void 0 ? void 0 : normalizeNonNegativeMoney(minimumOrder, "Pedido m\xEDnimo"),
          freeShippingMinimum: freeShippingMinimum === void 0 ? void 0 : normalizeOptionalNonNegativeMoney(freeShippingMinimum, "Frete gr\xE1tis acima de"),
          acceptsDelivery: acceptsDelivery === void 0 ? void 0 : normalizeStrictBoolean(acceptsDelivery, "Delivery", true),
          acceptsPickup: acceptsPickup === void 0 ? void 0 : normalizeStrictBoolean(acceptsPickup, "Retirada", true),
          acceptsPix: acceptsPix === void 0 ? void 0 : normalizeStrictBoolean(acceptsPix, "Pagamento por PIX", true),
          acceptsCard: acceptsCard === void 0 ? void 0 : normalizeStrictBoolean(acceptsCard, "Pagamento por cart\xE3o", true),
          tableOrderingEnabled: tableOrderingEnabled === void 0 ? void 0 : normalizeStrictBoolean(tableOrderingEnabled, "Pedidos por QR Code", true),
          waiterCallEnabled: waiterCallEnabled === void 0 ? void 0 : normalizeStrictBoolean(waiterCallEnabled, "Chamados ao gar\xE7om", true),
          billRequestEnabled: billRequestEnabled === void 0 ? void 0 : normalizeStrictBoolean(billRequestEnabled, "Solicita\xE7\xE3o da conta", true),
          pixProvider: resolvedPixProvider,
          pixKey,
          legalDocumentType: normalizedLegalDocumentType,
          companyDocument: normalizedCompanyDocument,
          companyLegalName: companyLegalName === void 0 ? void 0 : String(companyLegalName || "").trim() || null,
          companyTradeName: companyTradeName === void 0 ? void 0 : String(companyTradeName || "").trim() || null,
          companyAddress: companyAddress === void 0 ? void 0 : String(companyAddress || "").trim() || null,
          companyCnae: companyCnae === void 0 ? void 0 : String(companyCnae || "").trim() || null,
          monthlyRevenue: monthlyRevenue === void 0 ? void 0 : monthlyRevenue === null ? null : Number(monthlyRevenue),
          ownerFullName: ownerFullName === void 0 ? void 0 : String(ownerFullName || "").trim() || null,
          ownerCpf: normalizedOwnerCpf,
          ownerBirthDate: normalizedOwnerBirthDate,
          ownerEmail: normalizedOwnerEmail,
          ownerPhone: normalizedOwnerPhone,
          ownerAddress: ownerAddress === void 0 ? void 0 : String(ownerAddress || "").trim() || null,
          bankName: normalizedBankName,
          bankCode: bankCode === void 0 ? void 0 : String(bankCode || "").trim() || null,
          bankAccountType: bankAccountType === void 0 ? void 0 : String(bankAccountType || "").trim().toUpperCase() || null,
          bankBranch: normalizedBankBranch,
          bankAccount: normalizedBankAccount,
          bankHolderDocument: normalizedBankHolderDocument,
          cardGateway: normalizedCardGateway,
          gatewayMerchantId: resolvedGatewayMerchantId,
          stripeSecretKey: normalizedStripeSecretKey,
          stripeWebhookSecret: normalizedStripeWebhookSecret,
          mercadoPagoAccessToken: normalizedMercadoPagoAccessToken,
          picpayToken: normalizedPicPayToken,
          asaasAccessToken: normalizedAsaasAccessToken,
          pagbankEmail: normalizedPagBankEmail,
          pagbankToken: normalizedPagBankToken,
          pagbankEnvironment: normalizedPagBankEnvironment,
          ownerDocumentFileUrl: ownerDocumentFileUrl === void 0 ? void 0 : String(ownerDocumentFileUrl || "").trim() || null,
          bankProofFileUrl: bankProofFileUrl === void 0 ? void 0 : String(bankProofFileUrl || "").trim() || null,
          companyContractFileUrl: companyContractFileUrl === void 0 ? void 0 : String(companyContractFileUrl || "").trim() || null,
          primaryColor: primaryColor === void 0 ? void 0 : normalizePrimaryColor(primaryColor),
          instagram: instagram === void 0 ? void 0 : normalizeSocialReference(instagram, "Instagram"),
          facebook: facebook === void 0 ? void 0 : normalizeSocialReference(facebook, "Facebook"),
          tiktok: tiktok === void 0 ? void 0 : normalizeSocialReference(tiktok, "TikTok"),
          youtube: youtube === void 0 ? void 0 : normalizeSocialReference(youtube, "YouTube"),
          fontFamily: fontFamily === void 0 ? void 0 : normalizeFontFamily(fontFamily),
          seoTitle: seoTitle === void 0 ? void 0 : normalizeOptionalText(seoTitle, "T\xEDtulo para buscadores", 70),
          seoDescription: seoDescription === void 0 ? void 0 : normalizeOptionalText(seoDescription, "Descri\xE7\xE3o para buscadores", 160),
          whatsappEnabled: normalizedWhatsappEnabled,
          whatsappDisplayName: whatsappDisplayName === void 0 ? void 0 : normalizeOptionalText(whatsappDisplayName, "Nome exibido no WhatsApp", 80),
          whatsappDefaultMessage: whatsappDefaultMessage === void 0 ? void 0 : normalizeOptionalText(whatsappDefaultMessage, "Mensagem inicial do WhatsApp", 500),
          receiveOrdersOnWhatsapp: receiveOrdersOnWhatsapp === void 0 ? void 0 : normalizeStrictBoolean(receiveOrdersOnWhatsapp, "Pedidos pelo WhatsApp", false),
          receiveStatusNotifications: receiveStatusNotifications === void 0 ? void 0 : normalizeStrictBoolean(
            receiveStatusNotifications,
            "Notifica\xE7\xF5es de status pelo WhatsApp",
            false
          ),
          businessHours: normalizedBusinessHours,
          isOpenForOrders: normalizedIsOpenForOrders,
          averageDeliveryTime: normalizedAverageDeliveryTime,
          autoAcceptOrders: normalizedAutoAcceptOrders,
          trackingRequiresLogin: normalizedTrackingRequiresLogin,
          soundNotifications: normalizedSoundNotifications,
          maxConcurrentOrders: normalizedMaxConcurrentOrders
        });
        const restaurantData = {};
        if (normalizedWhatsapp !== void 0) {
          restaurantData.whatsapp = normalizedWhatsapp;
        }
        if (normalizedRestaurantName !== void 0) {
          restaurantData.name = normalizedRestaurantName;
        }
        if (normalizedRestaurantLogo !== void 0) {
          restaurantData.logo = normalizedRestaurantLogo;
        }
        if (normalizedRestaurantCoverImage !== void 0) {
          restaurantData.coverImage = normalizedRestaurantCoverImage;
        }
        if (normalizedRestaurantDescription !== void 0) {
          restaurantData.description = normalizedRestaurantDescription;
        }
        if (hasAddressPayload && establishmentAddress.address) {
          restaurantData.address = establishmentAddress.address;
          restaurantData.addressNumber = establishmentAddress.number;
          restaurantData.addressComplement = establishmentAddress.complement || null;
          restaurantData.addressDistrict = establishmentAddress.district;
          restaurantData.city = establishmentAddress.city;
          restaurantData.state = establishmentAddress.state;
          restaurantData.zipCode = establishmentAddress.zipCode;
        }
        if (Object.keys(restaurantData).length > 0) {
          await prisma_default.restaurant.update({
            where: {
              id: Number(restaurantId)
            },
            data: restaurantData
          });
        }
        return {
          ...updated,
          stripeSecretKey: null,
          stripeWebhookSecret: null,
          mercadoPagoAccessToken: null,
          picpayToken: null,
          asaasAccessToken: null,
          pagbankToken: null,
          stripeSecretKeyConfigured: Boolean(String(updated?.stripeSecretKey || "").trim()),
          stripeWebhookSecretConfigured: Boolean(String(updated?.stripeWebhookSecret || "").trim()),
          mercadoPagoAccessTokenConfigured: Boolean(
            String(updated?.mercadoPagoAccessToken || "").trim()
          ),
          picpayTokenConfigured: Boolean(String(updated?.picpayToken || "").trim()),
          asaasAccessTokenConfigured: Boolean(String(updated?.asaasAccessToken || "").trim()),
          pagbankTokenConfigured: Boolean(String(updated?.pagbankToken || "").trim()),
          whatsapp: whatsapp !== void 0 ? normalizedWhatsapp : String(settings?.restaurant?.whatsapp || "").trim() || null,
          restaurantName: restaurantName !== void 0 ? normalizedRestaurantName : String(settings?.restaurant?.name || "").trim() || null,
          restaurantLogo: restaurantLogo !== void 0 ? normalizedRestaurantLogo : String(settings?.restaurant?.logo || "").trim() || null,
          restaurantCoverImage: restaurantCoverImage !== void 0 ? normalizedRestaurantCoverImage : String(settings?.restaurant?.coverImage || "").trim() || null,
          restaurantDescription: restaurantDescription !== void 0 ? normalizedRestaurantDescription : String(settings?.restaurant?.description || "").trim() || null,
          gatewayMerchantIdConfigured: Boolean(String(updated?.gatewayMerchantId || "").trim()),
          gatewayMerchantIdAutoResolved,
          gatewayMerchantIdAutoResolvedSource
        };
      }
    };
    UpdateRestaurantSettingsService_default = new UpdateRestaurantSettingsService();
  }
});

// src/modules/restaurantSettings/controllers/UpdateRestaurantSettingsController.ts
var UpdateRestaurantSettingsController, UpdateRestaurantSettingsController_default;
var init_UpdateRestaurantSettingsController = __esm({
  "src/modules/restaurantSettings/controllers/UpdateRestaurantSettingsController.ts"() {
    init_UpdateRestaurantSettingsService();
    UpdateRestaurantSettingsController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const {
            deliveryFee,
            courierFeePerDelivery,
            minimumOrder,
            freeShippingMinimum,
            acceptsDelivery,
            acceptsPickup,
            acceptsPix,
            acceptsCard,
            tableOrderingEnabled,
            waiterCallEnabled,
            billRequestEnabled,
            pixProvider,
            pixKey,
            legalDocumentType,
            companyDocument,
            companyLegalName,
            companyTradeName,
            companyAddress,
            companyCnae,
            monthlyRevenue,
            ownerFullName,
            ownerCpf,
            ownerBirthDate,
            ownerEmail,
            ownerPhone,
            ownerAddress,
            bankName,
            bankCode,
            bankAccountType,
            bankBranch,
            bankAccount,
            bankHolderDocument,
            cardGateway,
            gatewayMerchantId,
            stripeSecretKey,
            stripeWebhookSecret,
            mercadoPagoAccessToken,
            picpayToken,
            asaasAccessToken,
            pagbankEmail,
            pagbankToken,
            pagbankEnvironment,
            ownerDocumentFileUrl,
            bankProofFileUrl,
            companyContractFileUrl,
            whatsapp,
            whatsappEnabled,
            whatsappDisplayName,
            whatsappDefaultMessage,
            receiveOrdersOnWhatsapp,
            receiveStatusNotifications,
            instagram,
            facebook,
            tiktok,
            youtube,
            primaryColor,
            fontFamily,
            seoTitle,
            seoDescription,
            restaurantName,
            restaurantLogo,
            restaurantCoverImage,
            restaurantDescription,
            restaurantAddress,
            restaurantAddressNumber,
            restaurantAddressComplement,
            restaurantAddressDistrict,
            restaurantCity,
            restaurantState,
            restaurantZipCode,
            businessHours,
            isOpenForOrders,
            averageDeliveryTime,
            autoAcceptOrders,
            trackingRequiresLogin,
            soundNotifications,
            maxConcurrentOrders
          } = req.body;
          const settings = await UpdateRestaurantSettingsService_default.execute({
            restaurantId,
            deliveryFee,
            courierFeePerDelivery,
            minimumOrder,
            freeShippingMinimum,
            acceptsDelivery,
            acceptsPickup,
            acceptsPix,
            acceptsCard,
            tableOrderingEnabled,
            waiterCallEnabled,
            billRequestEnabled,
            pixProvider,
            pixKey,
            legalDocumentType,
            companyDocument,
            companyLegalName,
            companyTradeName,
            companyAddress,
            companyCnae,
            monthlyRevenue,
            ownerFullName,
            ownerCpf,
            ownerBirthDate,
            ownerEmail,
            ownerPhone,
            ownerAddress,
            bankName,
            bankCode,
            bankAccountType,
            bankBranch,
            bankAccount,
            bankHolderDocument,
            cardGateway,
            gatewayMerchantId,
            stripeSecretKey,
            stripeWebhookSecret,
            mercadoPagoAccessToken,
            picpayToken,
            asaasAccessToken,
            pagbankEmail,
            pagbankToken,
            pagbankEnvironment,
            ownerDocumentFileUrl,
            bankProofFileUrl,
            companyContractFileUrl,
            whatsapp,
            whatsappEnabled,
            whatsappDisplayName,
            whatsappDefaultMessage,
            receiveOrdersOnWhatsapp,
            receiveStatusNotifications,
            instagram,
            facebook,
            tiktok,
            youtube,
            primaryColor,
            fontFamily,
            seoTitle,
            seoDescription,
            restaurantName,
            restaurantLogo,
            restaurantCoverImage,
            restaurantDescription,
            restaurantAddress,
            restaurantAddressNumber,
            restaurantAddressComplement,
            restaurantAddressDistrict,
            restaurantCity,
            restaurantState,
            restaurantZipCode,
            businessHours,
            isOpenForOrders,
            averageDeliveryTime,
            autoAcceptOrders,
            trackingRequiresLogin,
            soundNotifications,
            maxConcurrentOrders
          });
          return res.status(200).json(settings);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar configuracoes do restaurante"
          });
        }
      }
    };
    UpdateRestaurantSettingsController_default = new UpdateRestaurantSettingsController();
  }
});

// src/modules/restaurantSettings/services/GetPublicRestaurantSettingsService.ts
var GetPublicRestaurantSettingsService, GetPublicRestaurantSettingsService_default;
var init_GetPublicRestaurantSettingsService = __esm({
  "src/modules/restaurantSettings/services/GetPublicRestaurantSettingsService.ts"() {
    init_RestaurantSettingsRepository();
    init_RestaurantRepository();
    GetPublicRestaurantSettingsService = class {
      async execute({ restaurantId, slug, useDefault }) {
        let normalizedRestaurantId = Number(restaurantId);
        if ((!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) && slug) {
          const restaurant = await RestaurantRepository_default.findBySlug(String(slug).trim());
          normalizedRestaurantId = restaurant?.active === false ? 0 : Number(restaurant?.id || 0);
        }
        if (useDefault && (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0)) {
          const restaurant = await RestaurantSettingsRepository_default.findDefaultActiveRestaurant();
          normalizedRestaurantId = Number(restaurant?.id || 0);
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido.");
        }
        const settings = await RestaurantSettingsRepository_default.findPublicByRestaurantId(normalizedRestaurantId);
        if (!settings) {
          const restaurant = await RestaurantSettingsRepository_default.findRestaurantById(normalizedRestaurantId);
          if (restaurant?.active === false) {
            throw new Error("Restaurante n\xE3o encontrado ou indispon\xEDvel.");
          }
          const fallback = {
            restaurantId: normalizedRestaurantId,
            primaryColor: "#c95d3d",
            deliveryFee: 0,
            minimumOrder: 0,
            freeShippingMinimum: null,
            acceptsDelivery: true,
            acceptsPickup: true,
            acceptsPix: true,
            acceptsCard: true,
            tableOrderingEnabled: true,
            waiterCallEnabled: true,
            billRequestEnabled: true,
            pixProvider: "MERCADO_PAGO",
            pixKey: null,
            instagram: null,
            facebook: null,
            tiktok: null,
            youtube: null,
            fontFamily: "Inter",
            seoTitle: null,
            seoDescription: null,
            whatsapp: String(restaurant?.whatsapp || "").replace(/\D/g, "") || null,
            whatsappEnabled: false,
            whatsappDisplayName: null,
            whatsappDefaultMessage: null,
            receiveOrdersOnWhatsapp: false,
            receiveStatusNotifications: false,
            companyLegalName: null,
            ownerEmail: null,
            ownerPhone: null,
            businessHours: null,
            isOpenForOrders: true,
            averageDeliveryTime: null,
            autoAcceptOrders: false,
            trackingRequiresLogin: true,
            soundNotifications: true,
            maxConcurrentOrders: 20,
            restaurant: {
              name: restaurant?.name || null,
              slug: restaurant?.slug || null,
              logo: restaurant?.logo || null,
              coverImage: restaurant?.coverImage || null,
              description: restaurant?.description || null,
              address: restaurant?.address || null,
              addressNumber: restaurant?.addressNumber || null,
              addressComplement: restaurant?.addressComplement || null,
              addressDistrict: restaurant?.addressDistrict || null,
              city: restaurant?.city || null,
              state: restaurant?.state || null,
              zipCode: restaurant?.zipCode || null,
              banners: restaurant?.banners || []
            }
          };
          return fallback;
        }
        if (settings.restaurant?.active === false) {
          throw new Error("Restaurante n\xE3o encontrado ou indispon\xEDvel.");
        }
        return {
          ...settings,
          whatsapp: String(settings.restaurant?.whatsapp || "").replace(/\D/g, "") || null
        };
      }
    };
    GetPublicRestaurantSettingsService_default = new GetPublicRestaurantSettingsService();
  }
});

// src/modules/restaurantSettings/controllers/GetPublicRestaurantSettingsController.ts
var GetPublicRestaurantSettingsController, GetPublicRestaurantSettingsController_default;
var init_GetPublicRestaurantSettingsController = __esm({
  "src/modules/restaurantSettings/controllers/GetPublicRestaurantSettingsController.ts"() {
    init_GetPublicRestaurantSettingsService();
    GetPublicRestaurantSettingsController = class {
      async handle(req, res) {
        try {
          const restaurantId = Array.isArray(req.params.restaurantId) ? req.params.restaurantId[0] : req.params.restaurantId;
          const slug = Array.isArray(req.params.slug) ? req.params.slug[0] : req.params.slug;
          const useDefault = req.path.endsWith("/default");
          const settings = await GetPublicRestaurantSettingsService_default.execute({
            restaurantId,
            slug,
            useDefault
          });
          return res.status(200).json(settings);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao buscar configuracoes publicas do restaurante"
          });
        }
      }
    };
    GetPublicRestaurantSettingsController_default = new GetPublicRestaurantSettingsController();
  }
});

// src/modules/restaurantSettings/services/OnboardRestaurantAsaasService.ts
var OnboardRestaurantAsaasService, OnboardRestaurantAsaasService_default;
var init_OnboardRestaurantAsaasService = __esm({
  "src/modules/restaurantSettings/services/OnboardRestaurantAsaasService.ts"() {
    init_RestaurantSettingsRepository();
    init_adminSettingsValidation();
    OnboardRestaurantAsaasService = class {
      normalizeDocument(value) {
        return String(value || "").replace(/\D/g, "");
      }
      resolveDocumentType(value) {
        if (value.length === 14) {
          return "CNPJ";
        }
        if (value.length === 11) {
          return "CPF";
        }
        return null;
      }
      normalizeMobilePhone(value) {
        const digits = String(value || "").replace(/\D/g, "");
        const withoutBrazilCountryCode = digits.length >= 12 && digits.startsWith("55") ? digits.slice(2) : digits;
        return /^\d{10,11}$/.test(withoutBrazilCountryCode) ? withoutBrazilCountryCode : "";
      }
      normalizePostalCode(value) {
        const digits = String(value || "").replace(/\D/g, "");
        return /^\d{8}$/.test(digits) ? digits : "";
      }
      getAsaasBaseUrl() {
        return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
      }
      getAsaasApiKey() {
        return String(process.env.ASAAS_API_KEY || "").trim();
      }
      extractWalletIdentifier(payload) {
        return String(payload?.walletId || payload?.id || "").trim();
      }
      extractAsaasToken(payload) {
        return String(payload?.accessToken || payload?.apiKey || "").trim();
      }
      extractProviderError(payload) {
        if (!Array.isArray(payload?.errors) || payload.errors.length === 0) {
          return "Falha ao criar conta no Asaas.";
        }
        const firstError = String(payload.errors[0]?.description || "").trim();
        return firstError || "Falha ao criar conta no Asaas.";
      }
      async execute({
        restaurantId,
        cnpj,
        cpf,
        restaurantName,
        pixKey,
        email,
        mobilePhone,
        incomeValue,
        address,
        addressNumber,
        province,
        postalCode
      }) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante invalido para onboarding Asaas.");
        }
        const existingSettings = await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId);
        const restaurant = existingSettings?.restaurant || await RestaurantSettingsRepository_default.findRestaurantById(normalizedRestaurantId);
        if (!restaurant) {
          throw new Error("Restaurante nao encontrado para onboarding Asaas.");
        }
        const normalizedCnpj = this.normalizeDocument(
          cnpj || existingSettings?.companyDocument || restaurant.cnpj || ""
        );
        const normalizedCpf = this.normalizeDocument(cpf || "");
        if (normalizedCnpj && normalizedCpf && normalizedCnpj !== normalizedCpf) {
          throw new Error("Informe apenas um documento valido: CPF ou CNPJ.");
        }
        const normalizedDocument = normalizedCnpj || normalizedCpf;
        const legalDocumentType = this.resolveDocumentType(normalizedDocument);
        const normalizedRestaurantName = String(
          restaurantName || existingSettings?.companyTradeName || existingSettings?.companyLegalName || restaurant.name || ""
        ).trim();
        const normalizedPixKey = String(pixKey || "").trim();
        const normalizedEmail = String(email || existingSettings?.ownerEmail || restaurant.email || "").trim().toLowerCase();
        const normalizedMobilePhone = this.normalizeMobilePhone(
          mobilePhone || existingSettings?.ownerPhone || restaurant.phone || restaurant.whatsapp
        );
        const normalizedAddress = String(address || restaurant.address || "").trim();
        const normalizedAddressNumber = String(addressNumber || restaurant.addressNumber || "").trim();
        const normalizedProvince = String(province || restaurant.addressDistrict || "").trim();
        const normalizedPostalCode = this.normalizePostalCode(postalCode || restaurant.zipCode);
        const incomeCandidate = incomeValue === void 0 || incomeValue === null || incomeValue === "" ? existingSettings?.monthlyRevenue : incomeValue;
        const normalizedIncomeValue = Number(incomeCandidate);
        if (!legalDocumentType) {
          throw new Error("Documento invalido. Informe CPF (11) ou CNPJ (14) digitos.");
        }
        if (legalDocumentType === "CPF" && !isValidCpf(normalizedDocument) || legalDocumentType === "CNPJ" && !isValidCnpj(normalizedDocument)) {
          throw new Error(`${legalDocumentType} invalido.`);
        }
        if (normalizedRestaurantName.length < 2) {
          throw new Error("Nome do restaurante invalido.");
        }
        if (!normalizedPixKey) {
          throw new Error("Chave PIX obrigatoria para onboarding Asaas.");
        }
        if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(normalizedEmail)) {
          throw new Error("E-mail do responsavel invalido. Complete os dados gerais do restaurante.");
        }
        if (!normalizedMobilePhone) {
          throw new Error("Celular do responsavel invalido. Complete os dados gerais do restaurante.");
        }
        if (!Number.isFinite(normalizedIncomeValue) || normalizedIncomeValue <= 0) {
          throw new Error("Informe uma renda/faturamento mensal valido para criar a conta Asaas.");
        }
        if (!normalizedAddress || !normalizedAddressNumber || !normalizedProvince) {
          throw new Error(
            "Endereco, numero e bairro sao obrigatorios. Complete os dados gerais do restaurante."
          );
        }
        if (!normalizedPostalCode) {
          throw new Error("CEP invalido. Complete os dados gerais do restaurante.");
        }
        const asaasApiKey = this.getAsaasApiKey();
        if (!asaasApiKey) {
          throw new Error("ASAAS_API_KEY nao configurada no backend.");
        }
        const asaasBaseUrl = this.getAsaasBaseUrl();
        const response = await fetch(`${asaasBaseUrl}/v3/accounts`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            access_token: asaasApiKey
          },
          body: JSON.stringify({
            cpfCnpj: normalizedDocument,
            name: normalizedRestaurantName,
            email: normalizedEmail,
            mobilePhone: normalizedMobilePhone,
            incomeValue: Math.round(normalizedIncomeValue * 100) / 100,
            address: normalizedAddress,
            addressNumber: normalizedAddressNumber,
            province: normalizedProvince,
            postalCode: normalizedPostalCode
          })
        });
        const responseBody = await response.json();
        if (!response.ok) {
          throw new Error(this.extractProviderError(responseBody));
        }
        const walletIdentifier = this.extractWalletIdentifier(responseBody);
        if (!walletIdentifier) {
          throw new Error("Asaas nao retornou identificador da conta/carteira da subconta.");
        }
        const asaasSubaccountToken = this.extractAsaasToken(responseBody);
        if (existingSettings) {
          await RestaurantSettingsRepository_default.update(normalizedRestaurantId, {
            legalDocumentType,
            companyDocument: normalizedDocument,
            companyTradeName: normalizedRestaurantName,
            pixProvider: "ASAAS",
            pixKey: normalizedPixKey,
            monthlyRevenue: Math.round(normalizedIncomeValue * 100) / 100,
            gatewayMerchantId: walletIdentifier,
            ...asaasSubaccountToken ? {
              asaasAccessToken: asaasSubaccountToken
            } : {}
          });
        } else {
          await RestaurantSettingsRepository_default.create({
            restaurantId: normalizedRestaurantId,
            deliveryFee: 0,
            minimumOrder: 0,
            pixProvider: "ASAAS",
            pixKey: normalizedPixKey,
            monthlyRevenue: Math.round(normalizedIncomeValue * 100) / 100,
            ownerEmail: normalizedEmail,
            ownerPhone: normalizedMobilePhone,
            legalDocumentType,
            companyDocument: normalizedDocument,
            companyTradeName: normalizedRestaurantName,
            gatewayMerchantId: walletIdentifier,
            asaasAccessToken: asaasSubaccountToken || null
          });
        }
        return {
          restaurantId: normalizedRestaurantId,
          walletId: walletIdentifier,
          pixKey: normalizedPixKey,
          asaasSubaccountTokenConfigured: Boolean(asaasSubaccountToken)
        };
      }
    };
    OnboardRestaurantAsaasService_default = new OnboardRestaurantAsaasService();
  }
});

// src/modules/restaurantSettings/controllers/OnboardRestaurantAsaasController.ts
var OnboardRestaurantAsaasController, OnboardRestaurantAsaasController_default;
var init_OnboardRestaurantAsaasController = __esm({
  "src/modules/restaurantSettings/controllers/OnboardRestaurantAsaasController.ts"() {
    init_OnboardRestaurantAsaasService();
    OnboardRestaurantAsaasController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user?.restaurantId;
          const {
            cnpj,
            cpf,
            restaurantName,
            pixKey,
            email,
            mobilePhone,
            incomeValue,
            address,
            addressNumber,
            province,
            postalCode
          } = req.body;
          const result = await OnboardRestaurantAsaasService_default.execute({
            restaurantId,
            cnpj,
            cpf,
            restaurantName,
            pixKey,
            email,
            mobilePhone,
            incomeValue,
            address,
            addressNumber,
            province,
            postalCode
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar subconta Asaas do restaurante."
          });
        }
      }
    };
    OnboardRestaurantAsaasController_default = new OnboardRestaurantAsaasController();
  }
});

// src/modules/restaurantSettings/services/GetAsaasWalletBalanceService.ts
var GetAsaasWalletBalanceService, GetAsaasWalletBalanceService_default;
var init_GetAsaasWalletBalanceService = __esm({
  "src/modules/restaurantSettings/services/GetAsaasWalletBalanceService.ts"() {
    init_RestaurantSettingsRepository();
    GetAsaasWalletBalanceService = class {
      getAsaasBaseUrl() {
        return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
      }
      extractProviderError(payload) {
        if (!Array.isArray(payload?.errors) || payload.errors.length === 0) {
          return "Falha ao consultar saldo no Asaas.";
        }
        const firstError = String(payload.errors[0]?.description || "").trim();
        return firstError || "Falha ao consultar saldo no Asaas.";
      }
      async execute({ restaurantId }) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante invalido para consultar carteira Asaas.");
        }
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId);
        const asaasToken = String(settings?.asaasAccessToken || "").trim();
        if (!asaasToken) {
          throw new Error(
            "Conta Asaas ainda nao vinculada. Finalize o onboarding para consultar saldo."
          );
        }
        const asaasBaseUrl = this.getAsaasBaseUrl();
        const response = await fetch(`${asaasBaseUrl}/v3/finance/balance`, {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
            access_token: asaasToken
          }
        });
        const responseBody = await response.json();
        if (!response.ok) {
          throw new Error(this.extractProviderError(responseBody));
        }
        const balance = Number(responseBody?.balance || 0);
        const blockedBalance = Number(responseBody?.blockedBalance || 0);
        const pendingBalance = Number(responseBody?.pendingBalance || 0);
        return {
          balance: Number.isFinite(balance) ? balance : 0,
          blockedBalance: Number.isFinite(blockedBalance) ? blockedBalance : 0,
          pendingBalance: Number.isFinite(pendingBalance) ? pendingBalance : 0
        };
      }
    };
    GetAsaasWalletBalanceService_default = new GetAsaasWalletBalanceService();
  }
});

// src/modules/restaurantSettings/controllers/GetAsaasWalletBalanceController.ts
var GetAsaasWalletBalanceController, GetAsaasWalletBalanceController_default;
var init_GetAsaasWalletBalanceController = __esm({
  "src/modules/restaurantSettings/controllers/GetAsaasWalletBalanceController.ts"() {
    init_GetAsaasWalletBalanceService();
    GetAsaasWalletBalanceController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user?.restaurantId;
          const result = await GetAsaasWalletBalanceService_default.execute({
            restaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao consultar saldo da carteira Asaas."
          });
        }
      }
    };
    GetAsaasWalletBalanceController_default = new GetAsaasWalletBalanceController();
  }
});

// src/modules/restaurantSettings/services/WithdrawAsaasWalletService.ts
var WithdrawAsaasWalletService, WithdrawAsaasWalletService_default;
var init_WithdrawAsaasWalletService = __esm({
  "src/modules/restaurantSettings/services/WithdrawAsaasWalletService.ts"() {
    init_RestaurantSettingsRepository();
    WithdrawAsaasWalletService = class {
      getAsaasBaseUrl() {
        return String(process.env.ASAAS_API_BASE_URL || "https://api.asaas.com").trim().replace(/\/+$/, "");
      }
      extractProviderError(payload) {
        if (!Array.isArray(payload?.errors) || payload.errors.length === 0) {
          return "Falha ao solicitar saque no Asaas.";
        }
        const firstError = String(payload.errors[0]?.description || "").trim();
        return firstError || "Falha ao solicitar saque no Asaas.";
      }
      async execute({ restaurantId, value, pixKey, description }) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante invalido para saque Asaas.");
        }
        const normalizedValue = Number(value);
        if (!Number.isFinite(normalizedValue) || normalizedValue <= 0) {
          throw new Error("Valor de saque invalido.");
        }
        const settings = await RestaurantSettingsRepository_default.findByRestaurantId(normalizedRestaurantId);
        const asaasToken = String(settings?.asaasAccessToken || "").trim();
        if (!asaasToken) {
          throw new Error("Conta Asaas ainda nao vinculada. Finalize o onboarding para sacar.");
        }
        const targetPixKey = String(pixKey || settings?.pixKey || "").trim();
        if (!targetPixKey) {
          throw new Error("Chave PIX obrigatoria para saque.");
        }
        const transferDescription = String(description || "Saque carteira Asaas").trim().slice(0, 150);
        const asaasBaseUrl = this.getAsaasBaseUrl();
        const response = await fetch(`${asaasBaseUrl}/v3/transfers`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            access_token: asaasToken
          },
          body: JSON.stringify({
            value: Number(normalizedValue.toFixed(2)),
            operationType: "PIX",
            pixAddressKey: targetPixKey,
            description: transferDescription
          })
        });
        const responseBody = await response.json();
        if (!response.ok) {
          throw new Error(this.extractProviderError(responseBody));
        }
        return {
          transferId: String(responseBody?.id || ""),
          status: String(responseBody?.status || "PENDING"),
          value: Number(responseBody?.value || normalizedValue),
          operationType: String(responseBody?.operationType || "PIX"),
          dateCreated: String(responseBody?.dateCreated || ""),
          pixKey: targetPixKey
        };
      }
    };
    WithdrawAsaasWalletService_default = new WithdrawAsaasWalletService();
  }
});

// src/modules/restaurantSettings/controllers/WithdrawAsaasWalletController.ts
var WithdrawAsaasWalletController, WithdrawAsaasWalletController_default;
var init_WithdrawAsaasWalletController = __esm({
  "src/modules/restaurantSettings/controllers/WithdrawAsaasWalletController.ts"() {
    init_WithdrawAsaasWalletService();
    WithdrawAsaasWalletController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user?.restaurantId;
          const { value, pixKey, description } = req.body;
          const result = await WithdrawAsaasWalletService_default.execute({
            restaurantId,
            value: Number(value),
            pixKey,
            description
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao solicitar saque da carteira Asaas."
          });
        }
      }
    };
    WithdrawAsaasWalletController_default = new WithdrawAsaasWalletController();
  }
});

// src/modules/restaurantSettings/services/StartMercadoPagoOAuthService.ts
import jwt5 from "jsonwebtoken";
var StartMercadoPagoOAuthService, StartMercadoPagoOAuthService_default;
var init_StartMercadoPagoOAuthService = __esm({
  "src/modules/restaurantSettings/services/StartMercadoPagoOAuthService.ts"() {
    StartMercadoPagoOAuthService = class {
      getBackendBaseUrl() {
        return String(process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 3e3}`).trim().replace(/\/+$/, "");
      }
      getRedirectUri() {
        return String(process.env.MP_OAUTH_REDIRECT_URI || "").trim();
      }
      getAuthBaseUrl() {
        return String(process.env.MP_OAUTH_AUTH_URL || "https://auth.mercadopago.com.br/authorization").trim().replace(/\/+$/, "");
      }
      getClientId() {
        return String(
          process.env.MP_OAUTH_CLIENT_ID || process.env.MP_CLIENT_ID || process.env.MERCADO_PAGO_CLIENT_ID || ""
        ).trim();
      }
      getJwtSecret() {
        return String(process.env.JWT_SECRET || "").trim();
      }
      async execute({ restaurantId, userId }) {
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedUserId = Number(userId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante invalido para conectar Mercado Pago.");
        }
        if (!Number.isInteger(normalizedUserId) || normalizedUserId <= 0) {
          throw new Error("Usuario invalido para conectar Mercado Pago.");
        }
        const clientId = this.getClientId();
        if (!clientId) {
          throw new Error(
            "Client ID OAuth do Mercado Pago nao configurado. Defina MP_OAUTH_CLIENT_ID (ou MP_CLIENT_ID / MERCADO_PAGO_CLIENT_ID) no backend."
          );
        }
        const jwtSecret = this.getJwtSecret();
        if (jwtSecret.length < 32) {
          throw new Error("JWT_SECRET invalido para assinar estado OAuth.");
        }
        const backendBaseUrl = this.getBackendBaseUrl();
        const redirectUri = this.getRedirectUri() || `${backendBaseUrl}/settings/mercado-pago/oauth/callback`;
        const state = jwt5.sign(
          {
            restaurantId: normalizedRestaurantId,
            userId: normalizedUserId
          },
          jwtSecret,
          {
            expiresIn: "10m"
          }
        );
        const query = new URLSearchParams({
          client_id: clientId,
          response_type: "code",
          platform_id: "mp",
          state,
          redirect_uri: redirectUri
        });
        return {
          authorizationUrl: `${this.getAuthBaseUrl()}?${query.toString()}`
        };
      }
    };
    StartMercadoPagoOAuthService_default = new StartMercadoPagoOAuthService();
  }
});

// src/modules/restaurantSettings/controllers/StartMercadoPagoOAuthController.ts
var StartMercadoPagoOAuthController, StartMercadoPagoOAuthController_default;
var init_StartMercadoPagoOAuthController = __esm({
  "src/modules/restaurantSettings/controllers/StartMercadoPagoOAuthController.ts"() {
    init_StartMercadoPagoOAuthService();
    StartMercadoPagoOAuthController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user?.restaurantId;
          const userId = req.user?.id;
          const result = await StartMercadoPagoOAuthService_default.execute({
            restaurantId,
            userId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao iniciar conexao com Mercado Pago."
          });
        }
      }
    };
    StartMercadoPagoOAuthController_default = new StartMercadoPagoOAuthController();
  }
});

// src/modules/restaurantSettings/services/CompleteMercadoPagoOAuthService.ts
import jwt6 from "jsonwebtoken";
var CompleteMercadoPagoOAuthService, CompleteMercadoPagoOAuthService_default;
var init_CompleteMercadoPagoOAuthService = __esm({
  "src/modules/restaurantSettings/services/CompleteMercadoPagoOAuthService.ts"() {
    init_RestaurantSettingsRepository();
    CompleteMercadoPagoOAuthService = class {
      getApiBaseUrl() {
        return String(process.env.MP_OAUTH_API_BASE_URL || "https://api.mercadopago.com").trim().replace(/\/+$/, "");
      }
      getBackendBaseUrl() {
        return String(process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 3e3}`).trim().replace(/\/+$/, "");
      }
      getRedirectUri() {
        return String(process.env.MP_OAUTH_REDIRECT_URI || "").trim();
      }
      getClientId() {
        return String(
          process.env.MP_OAUTH_CLIENT_ID || process.env.MP_CLIENT_ID || process.env.MERCADO_PAGO_CLIENT_ID || ""
        ).trim();
      }
      getClientSecret() {
        return String(
          process.env.MP_OAUTH_CLIENT_SECRET || process.env.MP_CLIENT_SECRET || process.env.MERCADO_PAGO_CLIENT_SECRET || ""
        ).trim();
      }
      getJwtSecret() {
        return String(process.env.JWT_SECRET || "").trim();
      }
      decodeState(rawState) {
        const jwtSecret = this.getJwtSecret();
        if (jwtSecret.length < 32) {
          throw new Error("JWT_SECRET invalido para validar estado OAuth.");
        }
        const decoded = jwt6.verify(rawState, jwtSecret);
        const restaurantId = Number(decoded?.restaurantId || 0);
        if (!Number.isInteger(restaurantId) || restaurantId <= 0) {
          throw new Error("Estado OAuth invalido para restaurante.");
        }
        return {
          restaurantId,
          userId: Number(decoded?.userId || 0)
        };
      }
      extractProviderError(response) {
        const apiError = String(response?.error || "").trim();
        const apiMessage = String(response?.message || "").trim();
        if (apiError || apiMessage) {
          return [apiError, apiMessage].filter(Boolean).join(": ");
        }
        return "Mercado Pago nao concluiu a autorizacao.";
      }
      async execute({
        code,
        state,
        providerError,
        providerErrorDescription
      }) {
        if (providerError) {
          const details = String(providerErrorDescription || "").trim();
          const baseMessage = `Mercado Pago recusou autorizacao (${providerError}).`;
          throw new Error(details ? `${baseMessage} ${details}` : baseMessage);
        }
        const normalizedCode = String(code || "").trim();
        if (!normalizedCode) {
          throw new Error("Codigo OAuth do Mercado Pago nao recebido.");
        }
        const normalizedState = String(state || "").trim();
        if (!normalizedState) {
          throw new Error("State OAuth do Mercado Pago nao recebido.");
        }
        const { restaurantId } = this.decodeState(normalizedState);
        const clientId = this.getClientId();
        const clientSecret = this.getClientSecret();
        if (!clientId || !clientSecret) {
          throw new Error(
            "Credenciais OAuth do Mercado Pago nao configuradas. Defina MP_OAUTH_CLIENT_ID e MP_OAUTH_CLIENT_SECRET (ou aliases MP_CLIENT_ID/MP_CLIENT_SECRET)."
          );
        }
        const redirectUri = this.getRedirectUri() || `${this.getBackendBaseUrl()}/settings/mercado-pago/oauth/callback`;
        const tokenResponse = await fetch(`${this.getApiBaseUrl()}/oauth/token`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Accept: "application/json"
          },
          body: JSON.stringify({
            client_id: clientId,
            client_secret: clientSecret,
            grant_type: "authorization_code",
            code: normalizedCode,
            redirect_uri: redirectUri
          })
        });
        const tokenBody = await tokenResponse.json();
        if (!tokenResponse.ok) {
          throw new Error(this.extractProviderError(tokenBody));
        }
        const accessToken = String(tokenBody?.access_token || "").trim();
        if (!accessToken) {
          throw new Error("Mercado Pago nao retornou access_token valido.");
        }
        const existingSettings = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        if (existingSettings) {
          await RestaurantSettingsRepository_default.update(restaurantId, {
            pixProvider: "MERCADO_PAGO",
            cardGateway: "MERCADO_PAGO",
            mercadoPagoAccessToken: accessToken
          });
        } else {
          await RestaurantSettingsRepository_default.create({
            restaurantId,
            deliveryFee: 0,
            minimumOrder: 0,
            pixProvider: "MERCADO_PAGO",
            cardGateway: "MERCADO_PAGO",
            mercadoPagoAccessToken: accessToken
          });
        }
        return {
          restaurantId,
          connected: true
        };
      }
    };
    CompleteMercadoPagoOAuthService_default = new CompleteMercadoPagoOAuthService();
  }
});

// src/modules/restaurantSettings/controllers/MercadoPagoOAuthCallbackController.ts
var MercadoPagoOAuthCallbackController, MercadoPagoOAuthCallbackController_default;
var init_MercadoPagoOAuthCallbackController = __esm({
  "src/modules/restaurantSettings/controllers/MercadoPagoOAuthCallbackController.ts"() {
    init_CompleteMercadoPagoOAuthService();
    MercadoPagoOAuthCallbackController = class {
      getFrontendBaseUrl() {
        return String(process.env.FRONTEND_URL || "http://localhost:5173").trim().replace(/\/+$/, "");
      }
      buildAdminRedirect(status, message) {
        const query = new URLSearchParams({
          mp_oauth: status
        });
        const normalizedMessage = String(message || "").trim();
        if (normalizedMessage) {
          query.set("message", normalizedMessage);
        }
        return `${this.getFrontendBaseUrl()}/admin?${query.toString()}`;
      }
      async handle(req, res) {
        try {
          const code = String(req.query.code || "").trim();
          const state = String(req.query.state || "").trim();
          const providerError = String(req.query.error || "").trim();
          const providerErrorDescription = String(req.query.error_description || "").trim();
          await CompleteMercadoPagoOAuthService_default.execute({
            code,
            state,
            providerError,
            providerErrorDescription
          });
          return res.redirect(this.buildAdminRedirect("success"));
        } catch (error2) {
          const message = error2 instanceof Error ? error2.message : "Erro ao concluir conexao com Mercado Pago.";
          return res.redirect(this.buildAdminRedirect("error", message));
        }
      }
    };
    MercadoPagoOAuthCallbackController_default = new MercadoPagoOAuthCallbackController();
  }
});

// src/modules/restaurantSettings/services/StartPagBankOAuthService.ts
import jwt7 from "jsonwebtoken";
var StartPagBankOAuthService, StartPagBankOAuthService_default;
var init_StartPagBankOAuthService = __esm({
  "src/modules/restaurantSettings/services/StartPagBankOAuthService.ts"() {
    StartPagBankOAuthService = class {
      async execute({ restaurantId, userId }) {
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedUserId = Number(userId);
        const clientId = String(process.env.PAGBANK_CONNECT_CLIENT_ID || "").trim();
        const jwtSecret = String(process.env.JWT_SECRET || "").trim();
        if (!normalizedRestaurantId || !normalizedUserId) {
          throw new Error("Restaurante ou administrador inv\xE1lido para conectar PagBank.");
        }
        if (!clientId) {
          throw new Error("PAGBANK_CONNECT_CLIENT_ID n\xE3o configurado no backend.");
        }
        if (jwtSecret.length < 32) {
          throw new Error("JWT_SECRET inv\xE1lido para iniciar conex\xE3o PagBank.");
        }
        const backendUrl = String(
          process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 3e3}`
        ).trim().replace(/\/+$/, "");
        const redirectUri = String(
          process.env.PAGBANK_CONNECT_REDIRECT_URI || `${backendUrl}/settings/pagbank/oauth/callback`
        ).trim();
        const state = jwt7.sign(
          { restaurantId: normalizedRestaurantId, userId: normalizedUserId },
          jwtSecret,
          { expiresIn: "10m" }
        );
        const authBaseUrl = String(
          process.env.PAGBANK_CONNECT_AUTH_URL || "https://connect.pagbank.com.br/oauth2/authorize"
        ).trim();
        const query = new URLSearchParams({
          response_type: "code",
          client_id: clientId,
          redirect_uri: redirectUri,
          scope: "payments.read payments.create payments.refund checkout.create checkout.view",
          state
        });
        return { authorizationUrl: `${authBaseUrl}?${query.toString()}` };
      }
    };
    StartPagBankOAuthService_default = new StartPagBankOAuthService();
  }
});

// src/modules/restaurantSettings/controllers/StartPagBankOAuthController.ts
var StartPagBankOAuthController, StartPagBankOAuthController_default;
var init_StartPagBankOAuthController = __esm({
  "src/modules/restaurantSettings/controllers/StartPagBankOAuthController.ts"() {
    init_StartPagBankOAuthService();
    StartPagBankOAuthController = class {
      async handle(req, res) {
        try {
          const result = await StartPagBankOAuthService_default.execute({
            restaurantId: req.user?.restaurantId,
            userId: req.user?.id
          });
          return res.json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao conectar PagBank."
          });
        }
      }
    };
    StartPagBankOAuthController_default = new StartPagBankOAuthController();
  }
});

// src/modules/restaurantSettings/services/CompletePagBankOAuthService.ts
import jwt8 from "jsonwebtoken";
var CompletePagBankOAuthService, CompletePagBankOAuthService_default;
var init_CompletePagBankOAuthService = __esm({
  "src/modules/restaurantSettings/services/CompletePagBankOAuthService.ts"() {
    init_RestaurantSettingsRepository();
    CompletePagBankOAuthService = class {
      async execute({ code, state }) {
        const normalizedCode = String(code || "").trim();
        const normalizedState = String(state || "").trim();
        const jwtSecret = String(process.env.JWT_SECRET || "").trim();
        if (!normalizedCode || !normalizedState) {
          throw new Error("C\xF3digo de autoriza\xE7\xE3o PagBank n\xE3o recebido.");
        }
        const decoded = jwt8.verify(normalizedState, jwtSecret);
        const restaurantId = Number(decoded.restaurantId || 0);
        if (!restaurantId) throw new Error("Estado OAuth PagBank inv\xE1lido.");
        const clientId = String(process.env.PAGBANK_CONNECT_CLIENT_ID || "").trim();
        const clientSecret = String(process.env.PAGBANK_CONNECT_CLIENT_SECRET || "").trim();
        const platformToken = String(process.env.PAGBANK_CONNECT_PLATFORM_TOKEN || "").trim();
        if (!clientId || !clientSecret || !platformToken) {
          throw new Error("Credenciais da aplica\xE7\xE3o PagBank Connect n\xE3o configuradas.");
        }
        const backendUrl = String(
          process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 3e3}`
        ).trim().replace(/\/+$/, "");
        const redirectUri = String(
          process.env.PAGBANK_CONNECT_REDIRECT_URI || `${backendUrl}/settings/pagbank/oauth/callback`
        ).trim();
        const apiBaseUrl = String(process.env.PAGBANK_CONNECT_API_URL || "https://api.pagseguro.com").trim().replace(/\/+$/, "");
        const response = await fetch(`${apiBaseUrl}/oauth2/token`, {
          method: "POST",
          headers: {
            Authorization: `Bearer ${platformToken}`,
            X_CLIENT_ID: clientId,
            X_CLIENT_SECRET: clientSecret,
            "Content-Type": "application/json",
            Accept: "application/json"
          },
          body: JSON.stringify({
            grant_type: "authorization_code",
            code: normalizedCode,
            redirect_uri: redirectUri
          })
        });
        const body = await response.json();
        const accessToken = String(body.access_token || "").trim();
        if (!response.ok || !accessToken) {
          throw new Error(String(body.error_description || body.error || "PagBank recusou a conex\xE3o."));
        }
        const refreshToken = String(body.refresh_token || "").trim() || null;
        const expiresIn = Number(body.expires_in || 0);
        const expiresAt = expiresIn > 0 ? new Date(Date.now() + Math.max(expiresIn - 300, 60) * 1e3) : null;
        const existing = await RestaurantSettingsRepository_default.findByRestaurantId(restaurantId);
        const data = {
          pixProvider: "PAGBANK",
          cardGateway: "PAGBANK",
          pagbankToken: accessToken,
          pagbankRefreshToken: refreshToken,
          pagbankTokenExpiresAt: expiresAt,
          pagbankEnvironment: "production"
        };
        if (existing) await RestaurantSettingsRepository_default.update(restaurantId, data);
        else
          await RestaurantSettingsRepository_default.create({
            restaurantId,
            deliveryFee: 0,
            minimumOrder: 0,
            ...data
          });
        return { restaurantId, connected: true };
      }
    };
    CompletePagBankOAuthService_default = new CompletePagBankOAuthService();
  }
});

// src/modules/restaurantSettings/controllers/PagBankOAuthCallbackController.ts
var PagBankOAuthCallbackController, PagBankOAuthCallbackController_default;
var init_PagBankOAuthCallbackController = __esm({
  "src/modules/restaurantSettings/controllers/PagBankOAuthCallbackController.ts"() {
    init_CompletePagBankOAuthService();
    PagBankOAuthCallbackController = class {
      async handle(req, res) {
        const frontendUrl = String(process.env.FRONTEND_URL || "http://localhost:5173").trim().replace(/\/+$/, "");
        try {
          if (req.query.error) {
            throw new Error(String(req.query.error_description || req.query.error));
          }
          await CompletePagBankOAuthService_default.execute({
            code: String(req.query.code || ""),
            state: String(req.query.state || "")
          });
          return res.redirect(`${frontendUrl}/admin?pagbank_oauth=success`);
        } catch (error2) {
          const query = new URLSearchParams({
            pagbank_oauth: "error",
            message: error2 instanceof Error ? error2.message : "Erro ao conectar PagBank."
          });
          return res.redirect(`${frontendUrl}/admin?${query.toString()}`);
        }
      }
    };
    PagBankOAuthCallbackController_default = new PagBankOAuthCallbackController();
  }
});

// src/modules/restaurantSettings/routes/RestaurantSettingsRoutes.ts
import { Router as Router9 } from "express";
var router9, RestaurantSettingsRoutes_default;
var init_RestaurantSettingsRoutes = __esm({
  "src/modules/restaurantSettings/routes/RestaurantSettingsRoutes.ts"() {
    init_authMiddleware();
    init_CreateRestaurantSettingsController();
    init_GetRestaurantSettingsController();
    init_UpdateRestaurantSettingsController();
    init_GetPublicRestaurantSettingsController();
    init_OnboardRestaurantAsaasController();
    init_GetAsaasWalletBalanceController();
    init_WithdrawAsaasWalletController();
    init_StartMercadoPagoOAuthController();
    init_MercadoPagoOAuthCallbackController();
    init_StartPagBankOAuthController();
    init_PagBankOAuthCallbackController();
    init_adminMiddleware();
    init_publicRestaurantBillingMiddleware();
    router9 = Router9();
    router9.get(
      "/public/default",
      publicRestaurantBillingMiddleware,
      (req, res) => GetPublicRestaurantSettingsController_default.handle(req, res)
    );
    router9.get(
      "/public/slug/:slug",
      publicRestaurantBillingMiddleware,
      (req, res) => GetPublicRestaurantSettingsController_default.handle(req, res)
    );
    router9.get(
      "/public/:restaurantId",
      publicRestaurantBillingMiddleware,
      (req, res) => GetPublicRestaurantSettingsController_default.handle(req, res)
    );
    router9.post(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => CreateRestaurantSettingsController_default.handle(req, res)
    );
    router9.get(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => GetRestaurantSettingsController_default.handle(req, res)
    );
    router9.post(
      "/mercado-pago/oauth/start",
      authMiddleware,
      adminMiddleware,
      (req, res) => StartMercadoPagoOAuthController_default.handle(req, res)
    );
    router9.get(
      "/mercado-pago/oauth/callback",
      (req, res) => MercadoPagoOAuthCallbackController_default.handle(req, res)
    );
    router9.post(
      "/pagbank/oauth/start",
      authMiddleware,
      adminMiddleware,
      (req, res) => StartPagBankOAuthController_default.handle(req, res)
    );
    router9.get(
      "/pagbank/oauth/callback",
      (req, res) => PagBankOAuthCallbackController_default.handle(req, res)
    );
    router9.post(
      "/asaas/onboard",
      authMiddleware,
      adminMiddleware,
      (req, res) => OnboardRestaurantAsaasController_default.handle(req, res)
    );
    router9.get(
      "/asaas/wallet/balance",
      authMiddleware,
      adminMiddleware,
      (req, res) => GetAsaasWalletBalanceController_default.handle(req, res)
    );
    router9.post(
      "/asaas/wallet/withdraw",
      authMiddleware,
      adminMiddleware,
      (req, res) => WithdrawAsaasWalletController_default.handle(req, res)
    );
    router9.put(
      "/:id",
      authMiddleware,
      adminMiddleware,
      (req, res) => UpdateRestaurantSettingsController_default.handle(req, res)
    );
    RestaurantSettingsRoutes_default = router9;
  }
});

// src/modules/banner/repositories/BannerRepository.ts
var BannerRepository, BannerRepository_default;
var init_BannerRepository = __esm({
  "src/modules/banner/repositories/BannerRepository.ts"() {
    init_prisma();
    BannerRepository = class {
      async create(data) {
        return prisma_default.banner.create({
          data
        });
      }
      async findAllByRestaurant(restaurantId) {
        return prisma_default.banner.findMany({
          where: {
            restaurantId: Number(restaurantId)
          },
          orderBy: {
            id: "desc"
          }
        });
      }
      async findById(id, restaurantId) {
        return prisma_default.banner.findFirst({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          }
        });
      }
      async update(id, restaurantId, data) {
        const result = await prisma_default.banner.updateMany({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          },
          data
        });
        if (result.count === 0) {
          return null;
        }
        return this.findById(id, restaurantId);
      }
      async delete(id, restaurantId) {
        return prisma_default.banner.deleteMany({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          }
        });
      }
    };
    BannerRepository_default = new BannerRepository();
  }
});

// src/modules/banner/services/CreateBannerService.ts
var CreateBannerService, CreateBannerService_default;
var init_CreateBannerService = __esm({
  "src/modules/banner/services/CreateBannerService.ts"() {
    init_BannerRepository();
    init_normalizeRestaurantImage();
    CreateBannerService = class {
      async execute({ title, image, restaurantId }) {
        const normalizedTitle = String(title || "").trim();
        const normalizedImage = normalizeRestaurantImage(image);
        if (!normalizedTitle || !normalizedImage) {
          throw new Error("T\xEDtulo e imagem s\xE3o obrigat\xF3rios");
        }
        if (normalizedTitle.length > 80) throw new Error("O t\xEDtulo pode ter no m\xE1ximo 80 caracteres.");
        return await BannerRepository_default.create({
          title: normalizedTitle,
          image: normalizedImage,
          restaurantId: Number(restaurantId)
        });
      }
    };
    CreateBannerService_default = new CreateBannerService();
  }
});

// src/modules/banner/controllers/CreateBannerController.ts
var CreateBannerController, CreateBannerController_default;
var init_CreateBannerController = __esm({
  "src/modules/banner/controllers/CreateBannerController.ts"() {
    init_CreateBannerService();
    CreateBannerController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const { title, image } = req.body;
          const banner = await CreateBannerService_default.execute({
            title,
            image,
            restaurantId
          });
          return res.status(201).json(banner);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar banner"
          });
        }
      }
    };
    CreateBannerController_default = new CreateBannerController();
  }
});

// src/modules/banner/services/ListBannerService.ts
var ListBannerService, ListBannerService_default;
var init_ListBannerService = __esm({
  "src/modules/banner/services/ListBannerService.ts"() {
    init_BannerRepository();
    ListBannerService = class {
      async execute({ restaurantId }) {
        return await BannerRepository_default.findAllByRestaurant(Number(restaurantId));
      }
    };
    ListBannerService_default = new ListBannerService();
  }
});

// src/modules/banner/controllers/ListBannerController.ts
var ListBannerController, ListBannerController_default;
var init_ListBannerController = __esm({
  "src/modules/banner/controllers/ListBannerController.ts"() {
    init_ListBannerService();
    ListBannerController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const banners = await ListBannerService_default.execute({
            restaurantId
          });
          return res.status(200).json(banners);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao listar banners"
          });
        }
      }
    };
    ListBannerController_default = new ListBannerController();
  }
});

// src/modules/banner/services/UpdateBannerService.ts
var UpdateBannerService, UpdateBannerService_default;
var init_UpdateBannerService = __esm({
  "src/modules/banner/services/UpdateBannerService.ts"() {
    init_BannerRepository();
    init_normalizeRestaurantImage();
    UpdateBannerService = class {
      async execute({ id, restaurantId, title, image }) {
        const banner = await BannerRepository_default.findById(id, restaurantId);
        if (!banner) {
          throw new Error("Banner n\xE3o encontrado");
        }
        const normalizedTitle = title === void 0 ? void 0 : String(title || "").trim();
        if (normalizedTitle !== void 0 && !normalizedTitle) {
          throw new Error("O t\xEDtulo do banner \xE9 obrigat\xF3rio.");
        }
        if (normalizedTitle && normalizedTitle.length > 80) {
          throw new Error("O t\xEDtulo pode ter no m\xE1ximo 80 caracteres.");
        }
        const normalizedImage = image === void 0 ? void 0 : normalizeRestaurantImage(image);
        if (image !== void 0 && !normalizedImage) {
          throw new Error("A imagem do banner \xE9 obrigat\xF3ria.");
        }
        return await BannerRepository_default.update(id, restaurantId, {
          title: normalizedTitle,
          image: normalizedImage
        });
      }
    };
    UpdateBannerService_default = new UpdateBannerService();
  }
});

// src/modules/banner/controllers/UpdateBannerController.ts
var UpdateBannerController, UpdateBannerController_default;
var init_UpdateBannerController = __esm({
  "src/modules/banner/controllers/UpdateBannerController.ts"() {
    init_UpdateBannerService();
    UpdateBannerController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const { title, image } = req.body;
          const banner = await UpdateBannerService_default.execute({
            id,
            restaurantId,
            title,
            image
          });
          return res.status(200).json(banner);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar banner"
          });
        }
      }
    };
    UpdateBannerController_default = new UpdateBannerController();
  }
});

// src/modules/banner/services/DeleteBannerService.ts
var DeleteBannerService, DeleteBannerService_default;
var init_DeleteBannerService = __esm({
  "src/modules/banner/services/DeleteBannerService.ts"() {
    init_BannerRepository();
    DeleteBannerService = class {
      async execute({ id, restaurantId }) {
        const banner = await BannerRepository_default.findById(id, restaurantId);
        if (!banner) {
          throw new Error("Banner n\xE3o encontrado");
        }
        await BannerRepository_default.delete(id, restaurantId);
        return { message: "Banner removido com sucesso" };
      }
    };
    DeleteBannerService_default = new DeleteBannerService();
  }
});

// src/modules/banner/controllers/DeleteBannerController.ts
var DeleteBannerController, DeleteBannerController_default;
var init_DeleteBannerController = __esm({
  "src/modules/banner/controllers/DeleteBannerController.ts"() {
    init_DeleteBannerService();
    DeleteBannerController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const result = await DeleteBannerService_default.execute({
            id,
            restaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao remover banner"
          });
        }
      }
    };
    DeleteBannerController_default = new DeleteBannerController();
  }
});

// src/modules/banner/routes/BannerRoutes.ts
import { Router as Router10 } from "express";
var router10, BannerRoutes_default;
var init_BannerRoutes = __esm({
  "src/modules/banner/routes/BannerRoutes.ts"() {
    init_authMiddleware();
    init_CreateBannerController();
    init_ListBannerController();
    init_UpdateBannerController();
    init_DeleteBannerController();
    init_adminMiddleware();
    init_billingMiddleware();
    router10 = Router10();
    router10.post(
      "/",
      authMiddleware,
      adminMiddleware,
      billingMiddleware,
      (req, res) => CreateBannerController_default.handle(req, res)
    );
    router10.get(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => ListBannerController_default.handle(req, res)
    );
    router10.put(
      "/:id",
      authMiddleware,
      adminMiddleware,
      (req, res) => UpdateBannerController_default.handle(req, res)
    );
    router10.delete(
      "/:id",
      authMiddleware,
      adminMiddleware,
      (req, res) => DeleteBannerController_default.handle(req, res)
    );
    BannerRoutes_default = router10;
  }
});

// src/modules/coupon/repositories/CouponRepository.ts
var CouponRepository, CouponRepository_default;
var init_CouponRepository = __esm({
  "src/modules/coupon/repositories/CouponRepository.ts"() {
    init_prisma();
    CouponRepository = class {
      async create(data) {
        return prisma_default.coupon.create({
          data
        });
      }
      async findAllByRestaurant(restaurantId) {
        return prisma_default.coupon.findMany({
          where: {
            restaurantId: Number(restaurantId)
          },
          orderBy: {
            id: "desc"
          }
        });
      }
      async findById(id, restaurantId) {
        return prisma_default.coupon.findFirst({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          }
        });
      }
      async findByCode(code, restaurantId) {
        return prisma_default.coupon.findFirst({
          where: {
            code: {
              equals: code,
              mode: "insensitive"
            },
            restaurantId: Number(restaurantId)
          }
        });
      }
      async findActiveById(id, restaurantId, now = /* @__PURE__ */ new Date()) {
        return prisma_default.coupon.findFirst({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId),
            active: true,
            OR: [{ expiration: null }, { expiration: { gt: now } }]
          }
        });
      }
      async findActiveLoyaltyByRestaurant(restaurantId, now = /* @__PURE__ */ new Date()) {
        return prisma_default.coupon.findMany({
          where: {
            restaurantId: Number(restaurantId),
            active: true,
            OR: [{ expiration: null }, { expiration: { gt: now } }]
          },
          orderBy: [{ loyaltyPurchasesRequired: "asc" }, { id: "desc" }]
        });
      }
      async countCompletedPurchases(userId, restaurantId, completedAfter) {
        return prisma_default.order.count({
          where: {
            userId: Number(userId),
            restaurantId: Number(restaurantId),
            paid: true,
            status: "ENTREGUE",
            ...completedAfter ? {
              OR: [
                { deliveredAt: { gt: completedAfter } },
                { deliveredAt: null, updatedAt: { gt: completedAfter } }
              ]
            } : {}
          }
        });
      }
      async findRedemptions(userId, restaurantId, couponIds) {
        if (couponIds.length === 0) return [];
        return prisma_default.couponRedemption.findMany({
          where: {
            userId: Number(userId),
            restaurantId: Number(restaurantId),
            couponId: { in: couponIds }
          },
          include: { order: { select: { id: true } } },
          orderBy: [{ couponId: "asc" }, { cycle: "asc" }]
        });
      }
      async findAllRedemptions(userId, restaurantId) {
        return prisma_default.couponRedemption.findMany({
          where: {
            userId: Number(userId),
            restaurantId: Number(restaurantId)
          },
          include: {
            coupon: true,
            order: { select: { id: true } }
          },
          orderBy: [{ claimedAt: "desc" }, { id: "desc" }]
        });
      }
      async expireClaimedRedemptions({
        restaurantId,
        userId,
        couponIds,
        redemptionId,
        now = /* @__PURE__ */ new Date()
      } = {}) {
        return prisma_default.couponRedemption.updateMany({
          where: {
            status: "CLAIMED",
            expiresAt: { lte: now },
            ...restaurantId ? { restaurantId: Number(restaurantId) } : {},
            ...userId ? { userId: Number(userId) } : {},
            ...couponIds ? { couponId: { in: couponIds } } : {},
            ...redemptionId ? { id: Number(redemptionId) } : {}
          },
          data: {
            status: "EXPIRED",
            reservedAt: null
          }
        });
      }
      async createRedemption(data) {
        return prisma_default.couponRedemption.create({
          data,
          include: { order: { select: { id: true } } }
        });
      }
      async hasRedemptions(id, restaurantId) {
        const redemption = await prisma_default.couponRedemption.findFirst({
          where: {
            couponId: Number(id),
            restaurantId: Number(restaurantId)
          },
          select: { id: true }
        });
        return redemption !== null;
      }
      async update(id, restaurantId, data) {
        const result = await prisma_default.coupon.updateMany({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          },
          data
        });
        if (result.count === 0) {
          return null;
        }
        return this.findById(id, restaurantId);
      }
      async delete(id, restaurantId) {
        return prisma_default.coupon.deleteMany({
          where: {
            id: Number(id),
            restaurantId: Number(restaurantId)
          }
        });
      }
    };
    CouponRepository_default = new CouponRepository();
  }
});

// src/validators/CouponValidator.ts
import { z as z13 } from "zod";
function validatePercentage(payload, context) {
  if (payload.discountType === "PERCENTAGE" && Number(payload.discount) >= 100) {
    context.addIssue({
      code: z13.ZodIssueCode.custom,
      path: ["discount"],
      message: "O desconto percentual deve ser menor que 100%."
    });
  }
}
function couponValidationMessage(error2) {
  return error2.issues[0]?.message || "Confira os dados do cupom e tente novamente.";
}
var nullableMoneySchema, nullableDateSchema, booleanSchema, couponCodeSchema, couponFields, createCouponSchema, updateCouponSchema, couponIdSchema, loyaltyRestaurantQuerySchema;
var init_CouponValidator = __esm({
  "src/validators/CouponValidator.ts"() {
    nullableMoneySchema = z13.preprocess(
      (value) => value === "" || value === null || value === void 0 ? null : value,
      z13.union([z13.coerce.number().positive("O limite de desconto deve ser maior que zero."), z13.null()])
    );
    nullableDateSchema = z13.preprocess(
      (value) => value === "" || value === null || value === void 0 ? null : value,
      z13.union([z13.null(), z13.coerce.date()])
    );
    booleanSchema = z13.preprocess((value) => {
      if (value === "false" || value === 0 || value === "0") return false;
      if (value === "true" || value === 1 || value === "1") return true;
      return value;
    }, z13.boolean());
    couponCodeSchema = z13.string({ required_error: "Informe o c\xF3digo do cupom." }).trim().min(3, "O c\xF3digo deve ter pelo menos 3 caracteres.").max(40, "O c\xF3digo deve ter no m\xE1ximo 40 caracteres.").transform((value) => value.toUpperCase()).refine(
      (value) => /^[A-Z0-9][A-Z0-9_-]*$/.test(value),
      "Use apenas letras, n\xFAmeros, h\xEDfen ou sublinhado no c\xF3digo."
    );
    couponFields = {
      code: couponCodeSchema,
      title: z13.string().trim().max(80, "O t\xEDtulo deve ter no m\xE1ximo 80 caracteres.").optional(),
      description: z13.string().trim().max(300, "A descri\xE7\xE3o deve ter no m\xE1ximo 300 caracteres.").optional(),
      discountType: z13.enum(["FIXED", "PERCENTAGE"]),
      discount: z13.coerce.number().positive("Informe um desconto maior que zero."),
      minimumSubtotal: z13.coerce.number().min(0, "O pedido m\xEDnimo n\xE3o pode ser negativo."),
      maxDiscount: nullableMoneySchema,
      loyaltyPurchasesRequired: z13.coerce.number().int("A quantidade de compras deve ser um n\xFAmero inteiro.").min(1, "Exija pelo menos uma compra para liberar o cupom.").max(1e3, "A quantidade m\xE1xima \xE9 de 1000 compras."),
      perCustomerLimit: z13.coerce.number().int("O limite por cliente deve ser um n\xFAmero inteiro.").min(1, "Permita pelo menos um cupom guardado por cliente.").max(100, "O limite m\xE1ximo \xE9 de 100 cupons guardados por cliente."),
      redemptionValidityDays: z13.coerce.number().int("A validade da recompensa deve ser informada em dias inteiros.").min(1, "A recompensa deve ficar v\xE1lida por pelo menos um dia.").max(365, "A validade m\xE1xima da recompensa \xE9 de 365 dias."),
      active: booleanSchema,
      expiration: nullableDateSchema
    };
    createCouponSchema = z13.object({
      ...couponFields,
      title: couponFields.title.default(""),
      description: couponFields.description.default(""),
      discountType: couponFields.discountType.default("FIXED"),
      minimumSubtotal: couponFields.minimumSubtotal.default(0),
      maxDiscount: couponFields.maxDiscount.default(null),
      loyaltyPurchasesRequired: couponFields.loyaltyPurchasesRequired.default(1),
      perCustomerLimit: couponFields.perCustomerLimit.default(1),
      redemptionValidityDays: couponFields.redemptionValidityDays.default(30),
      active: couponFields.active.default(true),
      expiration: couponFields.expiration.default(null)
    }).superRefine(validatePercentage);
    updateCouponSchema = z13.object({
      code: couponFields.code.optional(),
      title: couponFields.title,
      description: couponFields.description,
      discountType: couponFields.discountType.optional(),
      discount: couponFields.discount.optional(),
      minimumSubtotal: couponFields.minimumSubtotal.optional(),
      maxDiscount: couponFields.maxDiscount.optional(),
      loyaltyPurchasesRequired: couponFields.loyaltyPurchasesRequired.optional(),
      perCustomerLimit: couponFields.perCustomerLimit.optional(),
      redemptionValidityDays: couponFields.redemptionValidityDays.optional(),
      active: couponFields.active.optional(),
      expiration: couponFields.expiration.optional()
    }).refine((payload) => Object.keys(payload).length > 0, {
      message: "Informe ao menos um campo para atualizar o cupom."
    }).superRefine(validatePercentage);
    couponIdSchema = z13.coerce.number().int("Cupom inv\xE1lido.").positive("Cupom inv\xE1lido.");
    loyaltyRestaurantQuerySchema = z13.object({
      restaurantId: z13.coerce.number({ required_error: "Informe o restaurante." }).int("Restaurante inv\xE1lido.").positive("Restaurante inv\xE1lido.")
    });
  }
});

// src/modules/coupon/utils/couponPresenter.ts
function nullableNumber(value) {
  if (value === null || value === void 0 || value === "") return null;
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}
function nullableIsoDate(value) {
  if (!value) return null;
  const date = value instanceof Date ? value : new Date(value);
  return Number.isNaN(date.getTime()) ? null : date.toISOString();
}
function presentCoupon(coupon) {
  const code = String(coupon.code || "").trim().toUpperCase();
  return {
    id: coupon.id,
    restaurantId: coupon.restaurantId,
    code,
    title: coupon.title?.trim() || code,
    description: coupon.description?.trim() || "",
    discountType: coupon.discountType === "PERCENTAGE" ? "PERCENTAGE" : "FIXED",
    discount: Number(coupon.discount || 0),
    minimumSubtotal: Number(coupon.minimumSubtotal || 0),
    maxDiscount: nullableNumber(coupon.maxDiscount),
    loyaltyPurchasesRequired: Number(coupon.loyaltyPurchasesRequired || 1),
    perCustomerLimit: Number(coupon.perCustomerLimit || 1),
    redemptionValidityDays: Number(coupon.redemptionValidityDays || 30),
    active: coupon.active,
    expiration: nullableIsoDate(coupon.expiration),
    ...coupon.createdAt ? { createdAt: nullableIsoDate(coupon.createdAt) } : {},
    ...coupon.updatedAt ? { updatedAt: nullableIsoDate(coupon.updatedAt) } : {}
  };
}
var init_couponPresenter = __esm({
  "src/modules/coupon/utils/couponPresenter.ts"() {
  }
});

// src/modules/coupon/services/CreateCouponService.ts
var CreateCouponService, CreateCouponService_default;
var init_CreateCouponService = __esm({
  "src/modules/coupon/services/CreateCouponService.ts"() {
    init_CouponRepository();
    init_CouponValidator();
    init_couponPresenter();
    CreateCouponService = class {
      async execute(payload) {
        const { restaurantId, ...couponPayload } = payload;
        const parsed = createCouponSchema.parse(couponPayload);
        const exists = await CouponRepository_default.findByCode(parsed.code, restaurantId);
        if (exists) {
          throw new Error("J\xE1 existe um cupom com este c\xF3digo neste restaurante.");
        }
        let coupon;
        try {
          coupon = await CouponRepository_default.create({
            code: parsed.code,
            title: parsed.title || null,
            description: parsed.description || null,
            discountType: parsed.discountType,
            discount: parsed.discount,
            minimumSubtotal: parsed.minimumSubtotal,
            maxDiscount: parsed.maxDiscount,
            loyaltyPurchasesRequired: parsed.loyaltyPurchasesRequired,
            perCustomerLimit: parsed.perCustomerLimit,
            redemptionValidityDays: parsed.redemptionValidityDays,
            active: parsed.active,
            expiration: parsed.expiration,
            restaurantId
          });
        } catch (error2) {
          if (error2?.code === "P2002") {
            throw new Error("J\xE1 existe um cupom com este c\xF3digo neste restaurante.");
          }
          throw error2;
        }
        return presentCoupon(coupon);
      }
    };
    CreateCouponService_default = new CreateCouponService();
  }
});

// src/modules/coupon/controllers/CouponControllerHelpers.ts
import { ZodError as ZodError2 } from "zod";
function couponControllerError(res, error2, fallback) {
  if (error2 instanceof ZodError2) {
    return res.status(422).json({
      error: couponValidationMessage(error2),
      fields: error2.flatten().fieldErrors
    });
  }
  const message = error2 instanceof Error ? error2.message : fallback;
  if (/não encontrad|indisponível|expirado/i.test(message)) {
    return res.status(404).json({ error: message });
  }
  if (/já existe|já foi resgatado|limite de resgates|possui resgates|não pode ser excluído/i.test(
    message
  )) {
    return res.status(409).json({ error: message });
  }
  if (/não pode ultrapassar|maior que zero|número inteiro|pelo menos/i.test(message)) {
    return res.status(422).json({ error: message });
  }
  if (/faltam? \d+ compras? concluídas?/i.test(message)) {
    return res.status(400).json({ error: message });
  }
  console.error("[coupons] unexpected error", error2);
  return res.status(500).json({ error: fallback });
}
var init_CouponControllerHelpers = __esm({
  "src/modules/coupon/controllers/CouponControllerHelpers.ts"() {
    init_CouponValidator();
  }
});

// src/modules/coupon/controllers/CreateCouponController.ts
var CreateCouponController, CreateCouponController_default;
var init_CreateCouponController = __esm({
  "src/modules/coupon/controllers/CreateCouponController.ts"() {
    init_CreateCouponService();
    init_CouponControllerHelpers();
    CreateCouponController = class {
      async handle(req, res) {
        try {
          const restaurantId = Number(req.user?.restaurantId || 0);
          if (!restaurantId) {
            return res.status(403).json({ error: "Restaurante n\xE3o identificado." });
          }
          const coupon = await CreateCouponService_default.execute({
            ...req.body,
            restaurantId
          });
          return res.status(201).json(coupon);
        } catch (error2) {
          return couponControllerError(res, error2, "N\xE3o foi poss\xEDvel criar o cupom.");
        }
      }
    };
    CreateCouponController_default = new CreateCouponController();
  }
});

// src/modules/coupon/services/ListCouponService.ts
var ListCouponService, ListCouponService_default;
var init_ListCouponService = __esm({
  "src/modules/coupon/services/ListCouponService.ts"() {
    init_CouponRepository();
    init_couponPresenter();
    ListCouponService = class {
      async execute({ restaurantId }) {
        const coupons = await CouponRepository_default.findAllByRestaurant(restaurantId);
        return coupons.map(presentCoupon);
      }
    };
    ListCouponService_default = new ListCouponService();
  }
});

// src/modules/coupon/controllers/ListCouponController.ts
var ListCouponController, ListCouponController_default;
var init_ListCouponController = __esm({
  "src/modules/coupon/controllers/ListCouponController.ts"() {
    init_ListCouponService();
    init_CouponControllerHelpers();
    ListCouponController = class {
      async handle(req, res) {
        try {
          const restaurantId = Number(req.user?.restaurantId || 0);
          if (!restaurantId) {
            return res.status(403).json({ error: "Restaurante n\xE3o identificado." });
          }
          const coupons = await ListCouponService_default.execute({
            restaurantId
          });
          return res.status(200).json(coupons);
        } catch (error2) {
          return couponControllerError(res, error2, "N\xE3o foi poss\xEDvel listar os cupons.");
        }
      }
    };
    ListCouponController_default = new ListCouponController();
  }
});

// src/modules/coupon/services/UpdateCouponService.ts
var UpdateCouponService, UpdateCouponService_default;
var init_UpdateCouponService = __esm({
  "src/modules/coupon/services/UpdateCouponService.ts"() {
    init_CouponRepository();
    init_CouponValidator();
    init_couponPresenter();
    UpdateCouponService = class {
      async execute({ id, restaurantId, ...payload }) {
        const normalizedId = Array.isArray(id) ? id[0] : id;
        const coupon = await CouponRepository_default.findById(normalizedId, restaurantId);
        if (!coupon) {
          throw new Error("Cupom n\xE3o encontrado");
        }
        const parsed = updateCouponSchema.parse(payload);
        const effectiveDiscountType = parsed.discountType || coupon.discountType || "FIXED";
        const effectiveDiscount = parsed.discount ?? Number(coupon.discount);
        if (effectiveDiscountType === "PERCENTAGE" && effectiveDiscount >= 100) {
          throw new Error("O desconto percentual deve ser menor que 100%.");
        }
        if (parsed.code && parsed.code !== coupon.code.toUpperCase()) {
          const duplicate = await CouponRepository_default.findByCode(parsed.code, restaurantId);
          if (duplicate && duplicate.id !== coupon.id) {
            throw new Error("J\xE1 existe um cupom com este c\xF3digo neste restaurante.");
          }
        }
        const updated = await CouponRepository_default.update(normalizedId, restaurantId, {
          ...parsed,
          ...parsed.title !== void 0 ? { title: parsed.title || null } : {},
          ...parsed.description !== void 0 ? { description: parsed.description || null } : {}
        });
        if (!updated) {
          throw new Error("Cupom n\xE3o encontrado");
        }
        return presentCoupon(updated);
      }
    };
    UpdateCouponService_default = new UpdateCouponService();
  }
});

// src/modules/coupon/controllers/UpdateCouponController.ts
var UpdateCouponController, UpdateCouponController_default;
var init_UpdateCouponController = __esm({
  "src/modules/coupon/controllers/UpdateCouponController.ts"() {
    init_UpdateCouponService();
    init_CouponValidator();
    init_CouponControllerHelpers();
    UpdateCouponController = class {
      async handle(req, res) {
        try {
          const restaurantId = Number(req.user?.restaurantId || 0);
          const id = couponIdSchema.parse(
            Array.isArray(req.params.id) ? req.params.id[0] : req.params.id
          );
          if (!restaurantId) {
            return res.status(403).json({ error: "Restaurante n\xE3o identificado." });
          }
          const coupon = await UpdateCouponService_default.execute({
            ...req.body,
            id,
            restaurantId
          });
          return res.status(200).json(coupon);
        } catch (error2) {
          return couponControllerError(res, error2, "N\xE3o foi poss\xEDvel atualizar o cupom.");
        }
      }
    };
    UpdateCouponController_default = new UpdateCouponController();
  }
});

// src/modules/coupon/services/DeleteCouponService.ts
var redemptionHistoryError, DeleteCouponService, DeleteCouponService_default;
var init_DeleteCouponService = __esm({
  "src/modules/coupon/services/DeleteCouponService.ts"() {
    init_CouponRepository();
    redemptionHistoryError = () => new Error(
      "Este cupom j\xE1 possui resgates e n\xE3o pode ser exclu\xEDdo. Pause ou arquive a campanha para preservar o hist\xF3rico dos clientes."
    );
    DeleteCouponService = class {
      async execute({ id, restaurantId }) {
        const normalizedId = Array.isArray(id) ? id[0] : id;
        const coupon = await CouponRepository_default.findById(normalizedId, restaurantId);
        if (!coupon) {
          throw new Error("Cupom n\xE3o encontrado");
        }
        if (await CouponRepository_default.hasRedemptions(normalizedId, restaurantId)) {
          throw redemptionHistoryError();
        }
        try {
          await CouponRepository_default.delete(normalizedId, restaurantId);
        } catch (error2) {
          if (await CouponRepository_default.hasRedemptions(normalizedId, restaurantId)) {
            throw redemptionHistoryError();
          }
          throw error2;
        }
        return { message: "Cupom removido com sucesso" };
      }
    };
    DeleteCouponService_default = new DeleteCouponService();
  }
});

// src/modules/coupon/controllers/DeleteCouponController.ts
var DeleteCouponController, DeleteCouponController_default;
var init_DeleteCouponController = __esm({
  "src/modules/coupon/controllers/DeleteCouponController.ts"() {
    init_DeleteCouponService();
    init_CouponValidator();
    init_CouponControllerHelpers();
    DeleteCouponController = class {
      async handle(req, res) {
        try {
          const restaurantId = Number(req.user?.restaurantId || 0);
          const id = couponIdSchema.parse(
            Array.isArray(req.params.id) ? req.params.id[0] : req.params.id
          );
          if (!restaurantId) {
            return res.status(403).json({ error: "Restaurante n\xE3o identificado." });
          }
          const result = await DeleteCouponService_default.execute({
            id,
            restaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return couponControllerError(res, error2, "N\xE3o foi poss\xEDvel remover o cupom.");
        }
      }
    };
    DeleteCouponController_default = new DeleteCouponController();
  }
});

// src/modules/coupon/utils/loyaltyProgress.ts
function calculateLoyaltyProgress({
  purchasesCompleted,
  purchasesRequired,
  perCustomerLimit,
  redemptions,
  now = /* @__PURE__ */ new Date()
}) {
  const completed = Math.max(0, Math.trunc(purchasesCompleted));
  const required = Math.max(1, Math.trunc(purchasesRequired));
  const limit = Math.max(1, Math.trunc(perCustomerLimit));
  const cycles = redemptions.map((redemption) => Math.trunc(Number(redemption.cycle))).filter((cycle) => cycle > 0);
  const earnedCycles = Math.floor(completed / required);
  const activeRedemptions = redemptions.filter((redemption) => {
    if (redemption.status === "RESERVED") return true;
    if (redemption.status !== "CLAIMED") return false;
    if (!redemption.expiresAt) return true;
    const expiresAt = new Date(redemption.expiresAt);
    return Number.isNaN(expiresAt.getTime()) || expiresAt > now;
  }).length;
  const limitReached = activeRedemptions >= limit;
  const nextCycle = (cycles.length > 0 ? Math.max(...cycles) : 0) + 1;
  const redeemableCycle = !limitReached && earnedCycles >= 1 ? nextCycle : null;
  const progressPercent = Math.min(100, Math.round(completed / required * 100));
  return {
    earnedCycles,
    nextCycle,
    redeemableCycle,
    canRedeem: redeemableCycle !== null && !limitReached,
    limitReached,
    activeRedemptions,
    walletLimit: limit,
    remaining: Math.max(0, required - completed),
    progressPercent
  };
}
var init_loyaltyProgress = __esm({
  "src/modules/coupon/utils/loyaltyProgress.ts"() {
  }
});

// src/modules/coupon/services/ListLoyaltyCouponsService.ts
function isoDate(value) {
  if (!value) return null;
  const date = value instanceof Date ? value : new Date(String(value));
  return Number.isNaN(date.getTime()) ? null : date.toISOString();
}
function presentRedemption(redemption, coupon, now) {
  const expiresAt = redemption.expiresAt ? new Date(redemption.expiresAt) : null;
  const expired = redemption.status === "EXPIRED" || redemption.status === "CLAIMED" && expiresAt !== null && !Number.isNaN(expiresAt.getTime()) && expiresAt <= now;
  return {
    id: redemption.id,
    restaurantId: redemption.restaurantId ?? coupon.restaurantId,
    couponId: redemption.couponId ?? coupon.id,
    status: redemption.status,
    cycle: redemption.cycle,
    orderId: redemption.order?.id ?? null,
    claimedAt: isoDate(redemption.claimedAt),
    reservedAt: isoDate(redemption.reservedAt),
    usedAt: isoDate(redemption.usedAt),
    expiresAt: isoDate(expiresAt),
    expired,
    createdAt: isoDate(redemption.createdAt),
    updatedAt: isoDate(redemption.updatedAt),
    coupon
  };
}
function getLatestLoyaltyCycleStartedAt(redemptionRecords) {
  return redemptionRecords.reduce((latest, redemption) => {
    const value = redemption?.claimedAt || redemption?.createdAt;
    if (!value) return latest;
    const candidate = value instanceof Date ? value : new Date(String(value));
    if (Number.isNaN(candidate.getTime())) return latest;
    return !latest || candidate > latest ? candidate : latest;
  }, null);
}
function buildLoyaltyReward(couponRecord, purchasesCompleted, redemptionRecords, now = /* @__PURE__ */ new Date()) {
  const coupon = presentCoupon(couponRecord);
  const redemptions = redemptionRecords.filter((redemption) => Number(redemption.couponId) === Number(coupon.id)).map((redemption) => presentRedemption(redemption, coupon, now));
  const progress = calculateLoyaltyProgress({
    purchasesCompleted,
    purchasesRequired: coupon.loyaltyPurchasesRequired,
    perCustomerLimit: coupon.perCustomerLimit,
    redemptions,
    now
  });
  return {
    coupon,
    purchasesCompleted,
    purchasesRequired: coupon.loyaltyPurchasesRequired,
    remaining: progress.remaining,
    progressPercent: progress.progressPercent,
    canRedeem: progress.canRedeem,
    nextCycle: progress.nextCycle,
    redeemableCycle: progress.redeemableCycle,
    limitReached: progress.limitReached,
    activeRedemptions: progress.activeRedemptions,
    walletLimit: progress.walletLimit,
    redemptions
  };
}
var ListLoyaltyCouponsService, ListLoyaltyCouponsService_default;
var init_ListLoyaltyCouponsService = __esm({
  "src/modules/coupon/services/ListLoyaltyCouponsService.ts"() {
    init_CouponRepository();
    init_couponPresenter();
    init_loyaltyProgress();
    ListLoyaltyCouponsService = class {
      async execute({ restaurantId, userId, now = /* @__PURE__ */ new Date() }) {
        const [coupons, purchasesCompleted] = await Promise.all([
          CouponRepository_default.findActiveLoyaltyByRestaurant(restaurantId, now),
          CouponRepository_default.countCompletedPurchases(userId, restaurantId)
        ]);
        await CouponRepository_default.expireClaimedRedemptions({
          restaurantId,
          userId,
          now
        });
        const redemptions = await CouponRepository_default.findAllRedemptions(userId, restaurantId);
        const rewards = await Promise.all(
          coupons.map(async (coupon) => {
            const couponRedemptions = redemptions.filter(
              (redemption) => Number(redemption.couponId) === Number(coupon.id)
            );
            const cycleStartedAt = getLatestLoyaltyCycleStartedAt(couponRedemptions);
            const purchasesInCurrentCycle = cycleStartedAt ? await CouponRepository_default.countCompletedPurchases(userId, restaurantId, cycleStartedAt) : purchasesCompleted;
            return buildLoyaltyReward(coupon, purchasesInCurrentCycle, couponRedemptions, now);
          })
        );
        return {
          restaurantId,
          purchasesCompleted,
          rewards,
          redemptions: redemptions.map(
            (redemption) => presentRedemption(redemption, presentCoupon(redemption.coupon), now)
          )
        };
      }
    };
    ListLoyaltyCouponsService_default = new ListLoyaltyCouponsService();
  }
});

// src/modules/coupon/controllers/ListLoyaltyCouponsController.ts
var ListLoyaltyCouponsController, ListLoyaltyCouponsController_default;
var init_ListLoyaltyCouponsController = __esm({
  "src/modules/coupon/controllers/ListLoyaltyCouponsController.ts"() {
    init_CouponValidator();
    init_ListLoyaltyCouponsService();
    init_CouponControllerHelpers();
    ListLoyaltyCouponsController = class {
      async handle(req, res) {
        try {
          const { restaurantId } = loyaltyRestaurantQuerySchema.parse(req.query);
          const userId = Number(req.user?.id || 0);
          const result = await ListLoyaltyCouponsService_default.execute({ restaurantId, userId });
          return res.status(200).json(result);
        } catch (error2) {
          return couponControllerError(
            res,
            error2,
            "N\xE3o foi poss\xEDvel carregar seu progresso de fidelidade."
          );
        }
      }
    };
    ListLoyaltyCouponsController_default = new ListLoyaltyCouponsController();
  }
});

// src/modules/coupon/services/RedeemLoyaltyCouponService.ts
function calculateRedemptionExpiresAt(coupon, claimedAt) {
  const validityDays = Math.min(
    365,
    Math.max(1, Math.trunc(Number(coupon.redemptionValidityDays || 30)))
  );
  const validityDeadline = new Date(claimedAt.getTime() + validityDays * 24 * 60 * 60 * 1e3);
  if (!coupon.expiration) return validityDeadline;
  const campaignDeadline = new Date(coupon.expiration);
  if (Number.isNaN(campaignDeadline.getTime())) return validityDeadline;
  return campaignDeadline < validityDeadline ? campaignDeadline : validityDeadline;
}
var RedeemLoyaltyCouponService, RedeemLoyaltyCouponService_default;
var init_RedeemLoyaltyCouponService = __esm({
  "src/modules/coupon/services/RedeemLoyaltyCouponService.ts"() {
    init_CouponRepository();
    init_ListLoyaltyCouponsService();
    RedeemLoyaltyCouponService = class {
      async execute({ couponId, restaurantId, userId, now = /* @__PURE__ */ new Date() }) {
        const coupon = await CouponRepository_default.findActiveById(couponId, restaurantId, now);
        if (!coupon) {
          throw new Error("Cupom indispon\xEDvel ou expirado.");
        }
        await CouponRepository_default.expireClaimedRedemptions({
          restaurantId: coupon.restaurantId,
          userId,
          couponIds: [coupon.id],
          now
        });
        const redemptions = await CouponRepository_default.findRedemptions(
          userId,
          coupon.restaurantId,
          [coupon.id]
        );
        const cycleStartedAt = getLatestLoyaltyCycleStartedAt(redemptions);
        const purchasesCompleted = await CouponRepository_default.countCompletedPurchases(
          userId,
          coupon.restaurantId,
          cycleStartedAt
        );
        const reward = buildLoyaltyReward(coupon, purchasesCompleted, redemptions, now);
        if (reward.limitReached) {
          throw new Error(
            "Voc\xEA j\xE1 atingiu o limite de resgates simult\xE2neos deste benef\xEDcio. Use o cupom guardado antes de resgatar outro."
          );
        }
        if (!reward.canRedeem || reward.redeemableCycle === null) {
          const remaining = Math.max(1, reward.remaining);
          throw new Error(
            `${remaining === 1 ? "Falta" : "Faltam"} ${remaining} ${remaining === 1 ? "compra conclu\xEDda" : "compras conclu\xEDdas"} para liberar este cupom.`
          );
        }
        try {
          const expiresAt = calculateRedemptionExpiresAt(coupon, now);
          const redemption = await CouponRepository_default.createRedemption({
            restaurantId: coupon.restaurantId,
            couponId: coupon.id,
            userId,
            cycle: reward.redeemableCycle,
            status: "CLAIMED",
            claimedAt: now,
            expiresAt
          });
          const updatedReward = buildLoyaltyReward(coupon, 0, [
            ...redemptions,
            redemption
          ], now);
          return {
            message: `Cupom ${coupon.code.toUpperCase()} resgatado. Use o c\xF3digo no seu pr\xF3ximo pedido.`,
            redemption: updatedReward.redemptions.find((item) => item.id === redemption.id),
            reward: updatedReward
          };
        } catch (error2) {
          if (error2?.code === "P2002") {
            throw new Error("Este ciclo de fidelidade j\xE1 foi resgatado.");
          }
          throw error2;
        }
      }
    };
    RedeemLoyaltyCouponService_default = new RedeemLoyaltyCouponService();
  }
});

// src/modules/coupon/controllers/RedeemLoyaltyCouponController.ts
var RedeemLoyaltyCouponController, RedeemLoyaltyCouponController_default;
var init_RedeemLoyaltyCouponController = __esm({
  "src/modules/coupon/controllers/RedeemLoyaltyCouponController.ts"() {
    init_CouponValidator();
    init_RedeemLoyaltyCouponService();
    init_CouponControllerHelpers();
    RedeemLoyaltyCouponController = class {
      async handle(req, res) {
        try {
          const couponId = couponIdSchema.parse(
            Array.isArray(req.params.id) ? req.params.id[0] : req.params.id
          );
          const { restaurantId } = loyaltyRestaurantQuerySchema.parse(req.body);
          const userId = Number(req.user?.id || 0);
          const result = await RedeemLoyaltyCouponService_default.execute({
            couponId,
            restaurantId,
            userId
          });
          return res.status(201).json(result);
        } catch (error2) {
          return couponControllerError(res, error2, "N\xE3o foi poss\xEDvel resgatar este cupom.");
        }
      }
    };
    RedeemLoyaltyCouponController_default = new RedeemLoyaltyCouponController();
  }
});

// src/modules/coupon/routes/CouponRoutes.ts
import { Router as Router11 } from "express";
function clientMiddleware(req, res, next) {
  if (req.user?.role !== "CLIENTE" || !req.user.id) {
    return res.status(403).json({ error: "O programa de fidelidade \xE9 exclusivo para clientes." });
  }
  return next();
}
var router11, CouponRoutes_default;
var init_CouponRoutes = __esm({
  "src/modules/coupon/routes/CouponRoutes.ts"() {
    init_authMiddleware();
    init_CreateCouponController();
    init_ListCouponController();
    init_UpdateCouponController();
    init_DeleteCouponController();
    init_ListLoyaltyCouponsController();
    init_RedeemLoyaltyCouponController();
    init_adminMiddleware();
    init_billingMiddleware();
    router11 = Router11();
    router11.get(
      "/loyalty",
      authMiddleware,
      clientMiddleware,
      (req, res) => ListLoyaltyCouponsController_default.handle(req, res)
    );
    router11.post(
      "/:id/redeem",
      authMiddleware,
      clientMiddleware,
      (req, res) => RedeemLoyaltyCouponController_default.handle(req, res)
    );
    router11.post(
      "/",
      authMiddleware,
      adminMiddleware,
      billingMiddleware,
      (req, res) => CreateCouponController_default.handle(req, res)
    );
    router11.get(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => ListCouponController_default.handle(req, res)
    );
    router11.put(
      "/:id",
      authMiddleware,
      adminMiddleware,
      (req, res) => UpdateCouponController_default.handle(req, res)
    );
    router11.delete(
      "/:id",
      authMiddleware,
      adminMiddleware,
      (req, res) => DeleteCouponController_default.handle(req, res)
    );
    CouponRoutes_default = router11;
  }
});

// src/modules/subscription/services/CreateSubscriptionService.ts
var CreateSubscriptionService, CreateSubscriptionService_default;
var init_CreateSubscriptionService = __esm({
  "src/modules/subscription/services/CreateSubscriptionService.ts"() {
    init_SubscriptionRepository();
    init_planConfig();
    CreateSubscriptionService = class {
      async execute({ restaurantId, plan, status, trialEndsAt }) {
        if (!isAvailablePlan(plan)) {
          throw new Error("Plano indispon\xEDvel para contrata\xE7\xE3o.");
        }
        const exists = await SubscriptionRepository_default.findByRestaurantId(restaurantId);
        if (exists) {
          throw new Error("Assinatura j\xE1 existe para esse restaurante!");
        }
        return await SubscriptionRepository_default.create({
          restaurantId: Number(restaurantId),
          plan,
          status,
          trialEndsAt
        });
      }
    };
    CreateSubscriptionService_default = new CreateSubscriptionService();
  }
});

// src/modules/subscription/controllers/CreateSubscriptionController.ts
var CreateSubscriptionController, CreateSubscriptionController_default;
var init_CreateSubscriptionController = __esm({
  "src/modules/subscription/controllers/CreateSubscriptionController.ts"() {
    init_CreateSubscriptionService();
    CreateSubscriptionController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const { plan, status, trialEndsAt } = req.body;
          const subscription = await CreateSubscriptionService_default.execute({
            restaurantId,
            plan,
            status,
            trialEndsAt
          });
          return res.status(201).json(subscription);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao criar assinatura"
          });
        }
      }
    };
    CreateSubscriptionController_default = new CreateSubscriptionController();
  }
});

// src/modules/subscription/services/PlanChangePolicy.ts
function evaluatePlanChangeEligibility({
  invoices,
  consumedInvoiceId,
  hasScheduledPlan = false,
  now = /* @__PURE__ */ new Date()
}) {
  if (hasScheduledPlan) {
    return {
      allowed: false,
      invoiceId: consumedInvoiceId || null,
      reason: "A escolha deste ciclo j\xE1 foi registrada e a troca est\xE1 agendada."
    };
  }
  const overdueOpenInvoice = invoices.find((invoice) => {
    const status = String(invoice.status || "").toUpperCase();
    return status === "ATRASADO" || status === "VENCIDO" || status === "PENDENTE" && normalizeDate(invoice.dueDate) < now;
  });
  if (overdueOpenInvoice) {
    return {
      allowed: false,
      invoiceId: null,
      reason: "Pague a fatura vencida para liberar a escolha do pr\xF3ximo plano."
    };
  }
  const paidOverdueInvoice = [...invoices].filter((invoice) => {
    if (String(invoice.status || "").toUpperCase() !== "PAGO" || !invoice.paidAt) {
      return false;
    }
    return normalizeDate(invoice.paidAt) > normalizeDate(invoice.dueDate);
  }).sort((left, right) => Number(right.id) - Number(left.id))[0];
  if (!paidOverdueInvoice) {
    return {
      allowed: false,
      invoiceId: null,
      reason: "A escolha ser\xE1 liberada ap\xF3s o pagamento de uma fatura vencida."
    };
  }
  if (Number(consumedInvoiceId) === paidOverdueInvoice.id) {
    return {
      allowed: false,
      invoiceId: paidOverdueInvoice.id,
      reason: "A escolha referente \xE0 \xFAltima fatura paga j\xE1 foi registrada."
    };
  }
  return {
    allowed: true,
    invoiceId: paidOverdueInvoice.id,
    reason: "Fatura vencida paga. Escolha manter o plano atual ou trocar no pr\xF3ximo ciclo."
  };
}
var normalizeDate;
var init_PlanChangePolicy = __esm({
  "src/modules/subscription/services/PlanChangePolicy.ts"() {
    normalizeDate = (value) => new Date(value);
  }
});

// src/modules/subscription/services/GetSubscriptionService.ts
var GetSubscriptionService, GetSubscriptionService_default;
var init_GetSubscriptionService = __esm({
  "src/modules/subscription/services/GetSubscriptionService.ts"() {
    init_SubscriptionRepository();
    init_BillingRepository();
    init_PlanChangePolicy();
    GetSubscriptionService = class {
      async execute({ restaurantId }) {
        const subscription = await SubscriptionRepository_default.findByRestaurantId(restaurantId);
        if (!subscription) {
          throw new Error("Assinatura n\xE3o encontrada!");
        }
        const invoices = await BillingRepository_default.findInvoicesByRestaurantId(Number(restaurantId));
        const planChangeEligibility = evaluatePlanChangeEligibility({
          invoices,
          consumedInvoiceId: subscription.planChangeInvoiceId,
          hasScheduledPlan: Boolean(subscription.scheduledPlan)
        });
        return {
          ...subscription,
          planChangeEligibility
        };
      }
    };
    GetSubscriptionService_default = new GetSubscriptionService();
  }
});

// src/modules/subscription/controllers/GetSubscriptionController.ts
var GetSubscriptionController, GetSubscriptionController_default;
var init_GetSubscriptionController = __esm({
  "src/modules/subscription/controllers/GetSubscriptionController.ts"() {
    init_GetSubscriptionService();
    GetSubscriptionController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const subscription = await GetSubscriptionService_default.execute({
            restaurantId
          });
          return res.status(200).json(subscription);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao buscar assinatura"
          });
        }
      }
    };
    GetSubscriptionController_default = new GetSubscriptionController();
  }
});

// src/modules/subscription/services/UpdateSubscriptionService.ts
var UpdateSubscriptionService, UpdateSubscriptionService_default;
var init_UpdateSubscriptionService = __esm({
  "src/modules/subscription/services/UpdateSubscriptionService.ts"() {
    init_SubscriptionRepository();
    init_planConfig();
    UpdateSubscriptionService = class {
      async execute({ restaurantId, plan, status, trialEndsAt }) {
        if (plan && !isAvailablePlan(plan)) {
          throw new Error("Plano indispon\xEDvel para contrata\xE7\xE3o.");
        }
        const subscription = await SubscriptionRepository_default.findByRestaurantId(restaurantId);
        if (!subscription) {
          throw new Error("Assinatura n\xE3o encontrada!");
        }
        return await SubscriptionRepository_default.update(restaurantId, {
          plan,
          status,
          trialEndsAt
        });
      }
    };
    UpdateSubscriptionService_default = new UpdateSubscriptionService();
  }
});

// src/modules/subscription/controllers/UpdateSubscriptionController.ts
var UpdateSubscriptionController, UpdateSubscriptionController_default;
var init_UpdateSubscriptionController = __esm({
  "src/modules/subscription/controllers/UpdateSubscriptionController.ts"() {
    init_UpdateSubscriptionService();
    UpdateSubscriptionController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const { plan, status, trialEndsAt } = req.body;
          const subscription = await UpdateSubscriptionService_default.execute({
            restaurantId,
            plan,
            status,
            trialEndsAt
          });
          return res.status(200).json(subscription);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao atualizar assinatura"
          });
        }
      }
    };
    UpdateSubscriptionController_default = new UpdateSubscriptionController();
  }
});

// src/modules/subscription/services/RequestPlanChangeService.ts
import { PlanType as PlanType3 } from "@prisma/client";
function getNextMonthPeriod(fromDate) {
  const next = new Date(fromDate);
  next.setDate(1);
  next.setMonth(next.getMonth() + 1);
  return {
    month: next.getMonth() + 1,
    year: next.getFullYear()
  };
}
var RequestPlanChangeService, RequestPlanChangeService_default;
var init_RequestPlanChangeService = __esm({
  "src/modules/subscription/services/RequestPlanChangeService.ts"() {
    init_SubscriptionRepository();
    init_BillingRepository();
    init_planConfig();
    init_PlanChangePolicy();
    RequestPlanChangeService = class {
      async execute({ restaurantId, plan }) {
        if (!Object.values(PlanType3).includes(plan) || !isAvailablePlan(plan)) {
          throw new Error("Escolha um plano dispon\xEDvel: B\xE1sico ou Premium.");
        }
        const subscription = await SubscriptionRepository_default.findByRestaurantId(restaurantId);
        if (!subscription) {
          throw new Error("Assinatura n\xE3o encontrada.");
        }
        const invoices = await BillingRepository_default.findInvoicesByRestaurantId(Number(restaurantId));
        const eligibility = evaluatePlanChangeEligibility({
          invoices,
          consumedInvoiceId: subscription.planChangeInvoiceId,
          hasScheduledPlan: Boolean(subscription.scheduledPlan)
        });
        if (!eligibility.allowed || !eligibility.invoiceId) {
          throw new Error(eligibility.reason);
        }
        if (subscription.plan === plan) {
          const updated2 = await SubscriptionRepository_default.update(restaurantId, {
            planChangeInvoiceId: eligibility.invoiceId,
            planChangeLockedUntil: null,
            scheduledPlan: null,
            scheduledPlanEffectiveMonth: null,
            scheduledPlanEffectiveYear: null
          });
          return {
            ...updated2,
            message: "Plano atual mantido para o pr\xF3ximo ciclo de faturamento."
          };
        }
        const nextPeriod = getNextMonthPeriod(/* @__PURE__ */ new Date());
        const updated = await SubscriptionRepository_default.update(restaurantId, {
          planChangeInvoiceId: eligibility.invoiceId,
          scheduledPlan: plan,
          scheduledPlanEffectiveMonth: nextPeriod.month,
          scheduledPlanEffectiveYear: nextPeriod.year,
          planChangeLockedUntil: null
        });
        return {
          ...updated,
          message: "Troca de plano agendada para o pr\xF3ximo ciclo de faturamento."
        };
      }
    };
    RequestPlanChangeService_default = new RequestPlanChangeService();
  }
});

// src/modules/subscription/controllers/RequestPlanChangeController.ts
import { PlanType as PlanType4 } from "@prisma/client";
var RequestPlanChangeController, RequestPlanChangeController_default;
var init_RequestPlanChangeController = __esm({
  "src/modules/subscription/controllers/RequestPlanChangeController.ts"() {
    init_RequestPlanChangeService();
    RequestPlanChangeController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          const { plan } = req.body;
          if (!restaurantId) {
            return res.status(400).json({
              error: "Restaurant ID not found in user context"
            });
          }
          if (!plan || !Object.values(PlanType4).includes(plan)) {
            return res.status(400).json({
              error: "Plano invalido."
            });
          }
          const subscription = await RequestPlanChangeService_default.execute({
            restaurantId,
            plan
          });
          return res.status(200).json(subscription);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao solicitar troca de plano"
          });
        }
      }
    };
    RequestPlanChangeController_default = new RequestPlanChangeController();
  }
});

// src/modules/subscription/routes/SubscriptionRoutes.ts
import { Router as Router12 } from "express";
var router12, SubscriptionRoutes_default;
var init_SubscriptionRoutes = __esm({
  "src/modules/subscription/routes/SubscriptionRoutes.ts"() {
    init_authMiddleware();
    init_CreateSubscriptionController();
    init_GetSubscriptionController();
    init_UpdateSubscriptionController();
    init_RequestPlanChangeController();
    init_adminMiddleware();
    router12 = Router12();
    router12.post(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => CreateSubscriptionController_default.handle(req, res)
    );
    router12.get(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => GetSubscriptionController_default.handle(req, res)
    );
    router12.put(
      "/",
      authMiddleware,
      adminMiddleware,
      (req, res) => UpdateSubscriptionController_default.handle(req, res)
    );
    router12.post(
      "/change-plan",
      authMiddleware,
      adminMiddleware,
      (req, res) => RequestPlanChangeController_default.handle(req, res)
    );
    SubscriptionRoutes_default = router12;
  }
});

// src/modules/aiSupport/services/ListSupportChatMessagesService.ts
var ListSupportChatMessagesService, ListSupportChatMessagesService_default;
var init_ListSupportChatMessagesService = __esm({
  "src/modules/aiSupport/services/ListSupportChatMessagesService.ts"() {
    init_prisma();
    ListSupportChatMessagesService = class {
      async execute(input) {
        const normalizedRole = String(input.requesterRole || "").toUpperCase();
        const isAdmin = normalizedRole === "ADMIN";
        const isSuperAdmin = normalizedRole === "SUPER_ADMIN";
        if (!isAdmin && !isSuperAdmin) {
          throw new Error("Sem permiss\xE3o para acessar o chat de suporte.");
        }
        const requesterRestaurantId = Number(input.requesterRestaurantId || 0);
        const queryRestaurantId = Number(input.queryRestaurantId || 0);
        const restaurantId = isSuperAdmin ? queryRestaurantId : requesterRestaurantId;
        const beforeId = Number(input.queryBeforeId || 0);
        const requestedLimit = Number(input.queryLimit || 0);
        const limit = Number.isInteger(requestedLimit) ? Math.min(Math.max(requestedLimit, 1), 100) : 40;
        if (!Number.isInteger(restaurantId) || restaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para carregar hist\xF3rico.");
        }
        const messages = await prisma_default.$queryRaw`
      SELECT
        "id",
        "message",
        "senderRole",
        "senderUserId",
        "senderLabel",
        "issueStatus",
        "issueResponse",
        "issueRespondedAt",
        "issueClosedAt",
        "restaurantId",
        "sentAt"
      FROM "SupportChatMessage"
      WHERE
        "restaurantId" = ${restaurantId}
        AND (${beforeId} <= 0 OR "id" < ${beforeId})
      ORDER BY "id" DESC
      LIMIT ${limit + 1}
    `;
        const hasMore = messages.length > limit;
        const slicedMessages = hasMore ? messages.slice(0, limit) : messages;
        const oldestMessage = slicedMessages[slicedMessages.length - 1] || null;
        return {
          restaurantId,
          hasMore,
          nextBeforeId: oldestMessage ? String(oldestMessage.id) : null,
          messages: slicedMessages.slice().reverse().map((item) => ({
            id: String(item.id),
            message: item.message,
            senderRole: item.senderRole,
            senderUserId: Number(item.senderUserId || 0) || 0,
            senderLabel: item.senderLabel,
            issueStatus: item.issueStatus,
            issueResponse: item.issueResponse,
            issueRespondedAt: item.issueRespondedAt?.toISOString?.() || null,
            issueClosedAt: item.issueClosedAt?.toISOString?.() || null,
            restaurantId: item.restaurantId,
            sentAt: item.sentAt?.toISOString?.() || null
          }))
        };
      }
    };
    ListSupportChatMessagesService_default = new ListSupportChatMessagesService();
  }
});

// src/modules/aiSupport/controllers/ListSupportChatMessagesController.ts
var ListSupportChatMessagesController, ListSupportChatMessagesController_default;
var init_ListSupportChatMessagesController = __esm({
  "src/modules/aiSupport/controllers/ListSupportChatMessagesController.ts"() {
    init_ListSupportChatMessagesService();
    ListSupportChatMessagesController = class {
      async handle(req, res) {
        try {
          const { role, restaurantId } = req.user;
          const rawRestaurantId = Array.isArray(req.query.restaurantId) && req.query.restaurantId.length > 0 ? req.query.restaurantId[0] : req.query.restaurantId || null;
          const queryRestaurantId = typeof rawRestaurantId === "string" || typeof rawRestaurantId === "number" ? rawRestaurantId : null;
          const rawBeforeId = Array.isArray(req.query.beforeId) && req.query.beforeId.length > 0 ? req.query.beforeId[0] : req.query.beforeId || null;
          const queryBeforeId = typeof rawBeforeId === "string" || typeof rawBeforeId === "number" ? rawBeforeId : null;
          const rawLimit = Array.isArray(req.query.limit) && req.query.limit.length > 0 ? req.query.limit[0] : req.query.limit || null;
          const queryLimit = typeof rawLimit === "string" || typeof rawLimit === "number" ? rawLimit : null;
          const result = await ListSupportChatMessagesService_default.execute({
            requesterRole: role,
            requesterRestaurantId: restaurantId,
            queryRestaurantId,
            queryBeforeId,
            queryLimit
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao carregar hist\xF3rico do chat de suporte."
          });
        }
      }
    };
    ListSupportChatMessagesController_default = new ListSupportChatMessagesController();
  }
});

// src/modules/aiSupport/controllers/GetAllSupportTicketsController.ts
function formatElapsed(date) {
  const diffMs = Date.now() - new Date(date).getTime();
  const diffMin = Math.floor(diffMs / 6e4);
  if (diffMin < 60) return `${diffMin}min`;
  const diffH = Math.floor(diffMin / 60);
  if (diffH < 24) return `${diffH}h ${diffMin % 60}min`;
  return `${Math.floor(diffH / 24)}d`;
}
var GetAllSupportTicketsController, GetAllSupportTicketsController_default;
var init_GetAllSupportTicketsController = __esm({
  "src/modules/aiSupport/controllers/GetAllSupportTicketsController.ts"() {
    init_prisma();
    GetAllSupportTicketsController = class {
      async handle(_req, res) {
        try {
          const grouped = await prisma_default.supportChatMessage.groupBy({
            by: ["restaurantId"],
            _max: { sentAt: true },
            _count: { id: true },
            orderBy: { _max: { sentAt: "desc" } },
            take: 100
          });
          if (grouped.length === 0) return res.status(200).json([]);
          const restaurantIds = grouped.map((g) => g.restaurantId);
          const [restaurants, firstMessages] = await Promise.all([
            prisma_default.restaurant.findMany({
              where: { id: { in: restaurantIds } },
              select: { id: true, name: true }
            }),
            prisma_default.supportChatMessage.findMany({
              where: {
                restaurantId: { in: restaurantIds },
                senderRole: { not: "SUPER_ADMIN" }
              },
              orderBy: { sentAt: "asc" },
              select: { restaurantId: true, message: true },
              distinct: ["restaurantId"]
            })
          ]);
          const restaurantMap = new Map(restaurants.map((r) => [r.id, r.name]));
          const subjectMap = new Map(firstMessages.map((m) => [m.restaurantId, m.message]));
          const tickets = grouped.map((g) => ({
            id: `#SUP-${String(g.restaurantId).padStart(4, "0")}`,
            restaurant: restaurantMap.get(g.restaurantId) ?? "Desconhecido",
            subject: (subjectMap.get(g.restaurantId) ?? "Sem mensagem").slice(0, 60),
            priority: "MEDIUM",
            status: "OPEN",
            responsible: "Suporte",
            elapsed: g._max.sentAt ? formatElapsed(g._max.sentAt) : "\u2014"
          }));
          return res.status(200).json(tickets);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao listar tickets"
          });
        }
      }
    };
    GetAllSupportTicketsController_default = new GetAllSupportTicketsController();
  }
});

// src/modules/aiSupport/controllers/UpdateSupportIssueController.ts
var validStatuses, UpdateSupportIssueController, UpdateSupportIssueController_default;
var init_UpdateSupportIssueController = __esm({
  "src/modules/aiSupport/controllers/UpdateSupportIssueController.ts"() {
    init_prisma();
    init_server();
    validStatuses = /* @__PURE__ */ new Set(["OPEN", "IN_PROGRESS", "CLOSED"]);
    UpdateSupportIssueController = class {
      async handle(req, res) {
        try {
          if (String(req.user.role).toUpperCase() !== "ADMIN") {
            return res.status(403).json({ error: "Somente administradores podem atender relatos." });
          }
          const id = Number(req.params.id);
          const status = String(req.body?.status || "").toUpperCase();
          const response = typeof req.body?.response === "string" ? req.body.response.replace(/\s+/g, " ").trim() : "";
          if (!Number.isInteger(id) || !validStatuses.has(status)) {
            return res.status(400).json({ error: "Relato ou status inv\xE1lido." });
          }
          if (response && (response.length < 3 || response.length > 1200)) {
            return res.status(400).json({ error: "A resposta deve ter entre 3 e 1200 caracteres." });
          }
          const responder = response ? await prisma_default.user.findFirst({
            where: { id: Number(req.user.id), restaurantId: Number(req.user.restaurantId), role: "ADMIN" },
            select: { name: true }
          }) : null;
          if (response && !responder) {
            return res.status(403).json({ error: "Administrador n\xE3o encontrado para registrar a resposta." });
          }
          const result = await prisma_default.$queryRaw`
        UPDATE "SupportChatMessage"
        SET
          "issueStatus" = ${status},
          "issueResponse" = COALESCE(${response || null}, "issueResponse"),
          "issueResponderName" = COALESCE(${responder?.name || null}, "issueResponderName"),
          "issueRespondedAt" = CASE WHEN ${Boolean(response)} THEN ${/* @__PURE__ */ new Date()} ELSE "issueRespondedAt" END,
          "issueClosedAt" = ${status === "CLOSED" ? /* @__PURE__ */ new Date() : null}
        WHERE "id" = ${id} AND "restaurantId" = ${Number(req.user.restaurantId)} AND "issueStatus" IS NOT NULL
        RETURNING "id", "senderUserId"
      `;
          if (!result[0]) return res.status(404).json({ error: "Relato n\xE3o encontrado." });
          const payload = {
            id: String(result[0].id),
            status,
            response: response || null,
            responderName: responder?.name || null
          };
          io.to(`restaurant:${Number(req.user.restaurantId)}:admin`).emit(
            "support:issue-updated",
            payload
          );
          if (result[0].senderUserId && (status === "CLOSED" || response)) {
            io.to(`user:${result[0].senderUserId}`).emit("support:issue-updated", payload);
          }
          return res.status(200).json(payload);
        } catch (error2) {
          console.error("Erro ao atualizar relato de suporte:", error2);
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel atualizar o relato." });
        }
      }
    };
    UpdateSupportIssueController_default = new UpdateSupportIssueController();
  }
});

// src/modules/aiSupport/controllers/DeleteSupportIssueController.ts
var DeleteSupportIssueController, DeleteSupportIssueController_default;
var init_DeleteSupportIssueController = __esm({
  "src/modules/aiSupport/controllers/DeleteSupportIssueController.ts"() {
    init_prisma();
    init_server();
    DeleteSupportIssueController = class {
      async handle(req, res) {
        try {
          if (String(req.user.role).toUpperCase() !== "ADMIN") {
            return res.status(403).json({ error: "Somente administradores podem excluir relatos." });
          }
          const id = Number(req.params.id);
          const restaurantId = Number(req.user.restaurantId);
          if (!Number.isInteger(id) || id <= 0 || !Number.isInteger(restaurantId) || restaurantId <= 0) {
            return res.status(400).json({ error: "Relato inv\xE1lido." });
          }
          const deleted = await prisma_default.$queryRaw`
        DELETE FROM "SupportChatMessage"
        WHERE
          "id" = ${id}
          AND "restaurantId" = ${restaurantId}
          AND "issueStatus" = 'CLOSED'
        RETURNING "id"
      `;
          if (!deleted[0]) {
            return res.status(409).json({
              error: "Encerre o relato antes de exclu\xED-lo, ou confirme se ele pertence ao seu restaurante."
            });
          }
          io.to(`restaurant:${restaurantId}:admin`).emit("support:issue-deleted", { id: String(id) });
          return res.status(200).json({ id: String(id) });
        } catch (error2) {
          console.error("Erro ao excluir relato de suporte:", error2);
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel excluir o relato." });
        }
      }
    };
    DeleteSupportIssueController_default = new DeleteSupportIssueController();
  }
});

// src/modules/aiSupport/controllers/ListMySupportIssueUpdatesController.ts
var ListMySupportIssueUpdatesController, ListMySupportIssueUpdatesController_default;
var init_ListMySupportIssueUpdatesController = __esm({
  "src/modules/aiSupport/controllers/ListMySupportIssueUpdatesController.ts"() {
    init_prisma();
    ListMySupportIssueUpdatesController = class {
      async handle(req, res) {
        try {
          const role = String(req.user.role || "").toUpperCase();
          if (role !== "FUNCIONARIO" && role !== "MOTOQUEIRO") {
            return res.status(403).json({ error: "Apenas funcion\xE1rios podem consultar seus relatos." });
          }
          const userId = Number(req.user.id);
          const restaurantId = Number(req.user.restaurantId);
          if (!Number.isInteger(userId) || userId <= 0 || !Number.isInteger(restaurantId) || restaurantId <= 0) {
            return res.status(401).json({ error: "Sess\xE3o inv\xE1lida para consultar relatos." });
          }
          const updates = await prisma_default.$queryRaw`
        SELECT "id", "issueStatus", "issueResponse", "issueResponderName", "issueRespondedAt", "issueClosedAt"
        FROM "SupportChatMessage"
        WHERE
          "restaurantId" = ${restaurantId}
          AND "senderUserId" = ${userId}
          AND "issueStatus" IS NOT NULL
          AND ("issueResponse" IS NOT NULL OR "issueStatus" = 'CLOSED')
        ORDER BY "id" DESC
        LIMIT 50
      `;
          return res.status(200).json({
            updates: updates.map((item) => ({
              id: String(item.id),
              status: item.issueStatus,
              response: item.issueResponse,
              responderName: item.issueResponderName,
              respondedAt: item.issueRespondedAt?.toISOString?.() || null,
              closedAt: item.issueClosedAt?.toISOString?.() || null
            }))
          });
        } catch (error2) {
          console.error("Erro ao listar atualiza\xE7\xF5es de relatos do funcion\xE1rio:", error2);
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel carregar as atualiza\xE7\xF5es dos relatos." });
        }
      }
    };
    ListMySupportIssueUpdatesController_default = new ListMySupportIssueUpdatesController();
  }
});

// src/modules/aiSupport/routes/AiSupportRoutes.ts
import { Router as Router13 } from "express";
var router13, AiSupportRoutes_default;
var init_AiSupportRoutes = __esm({
  "src/modules/aiSupport/routes/AiSupportRoutes.ts"() {
    init_authMiddleware();
    init_superAdminMiddleware();
    init_ListSupportChatMessagesController();
    init_GetAllSupportTicketsController();
    init_UpdateSupportIssueController();
    init_DeleteSupportIssueController();
    init_ListMySupportIssueUpdatesController();
    router13 = Router13();
    router13.get("/messages", authMiddleware, (req, res) => {
      ListSupportChatMessagesController_default.handle(req, res);
    });
    router13.get("/my-issue-updates", authMiddleware, (req, res) => {
      ListMySupportIssueUpdatesController_default.handle(req, res);
    });
    router13.get("/tickets/all", authMiddleware, superAdminMiddleware, (req, res) => {
      GetAllSupportTicketsController_default.handle(req, res);
    });
    router13.patch(
      "/messages/:id/issue",
      authMiddleware,
      (req, res) => UpdateSupportIssueController_default.handle(req, res)
    );
    router13.delete(
      "/messages/:id/issue",
      authMiddleware,
      (req, res) => DeleteSupportIssueController_default.handle(req, res)
    );
    AiSupportRoutes_default = router13;
  }
});

// src/modules/menuImport/services/ImportIfoodMenuScraperService.ts
import axios from "axios";
import * as cheerio from "cheerio";
import { z as z14 } from "zod";
function normalizeText(value) {
  return String(value || "").replace(/\s+/g, " ").trim();
}
function normalizeComparableName(value) {
  return normalizeText(value).toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "");
}
function buildUserAgent() {
  return [
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
    "AppleWebKit/537.36 (KHTML, like Gecko)",
    "Chrome/126.0.0.0 Safari/537.36"
  ].join(" ");
}
function parsePrice(raw) {
  if (typeof raw === "number") {
    return Number.isFinite(raw) ? raw : null;
  }
  const text = normalizeText(raw);
  if (!text) {
    return null;
  }
  const normalized = text.replace(/[^\d,.-]/g, "").replace(/\.(?=\d{3}(\D|$))/g, "").replace(/,(?=\d{1,2}$)/g, ".").replace(/,/g, "");
  const value = Number(normalized);
  return Number.isFinite(value) ? value : null;
}
function extractStructuredData($) {
  const collected = [];
  $("script[type='application/ld+json']").each((_, element) => {
    const raw = normalizeText($(element).text());
    if (!raw) {
      return;
    }
    try {
      collected.push(JSON.parse(raw));
    } catch {
    }
  });
  return collected;
}
function collectTextCandidates($, selectors) {
  const values = /* @__PURE__ */ new Set();
  for (const selector of selectors) {
    $(selector).each((_, element) => {
      const text = normalizeText($(element).text());
      if (text) {
        values.add(text);
      }
    });
  }
  return Array.from(values);
}
function looksLikePrice(value) {
  return /\d+[,.]\d{1,2}|\bR\$\b/i.test(String(value || ""));
}
function extractRestaurantName($) {
  const candidates = [
    $("h1").first().text(),
    $("meta[property='og:title']").attr("content"),
    $("title").text()
  ].map((item) => normalizeText(item)).filter(Boolean);
  return candidates[0] || null;
}
function extractMenuFromCheerio($) {
  const items = [];
  const restaurantName = extractRestaurantName($);
  const structuredData = extractStructuredData($);
  const categoryHints = collectTextCandidates($, [
    "h2",
    "h3",
    "[data-testid*='category']",
    "[class*='category']"
  ]);
  const productHints = collectTextCandidates($, [
    "h4",
    "h5",
    "[data-testid*='product']",
    "[class*='product']",
    "button",
    "article",
    "li"
  ]);
  const priceHints = collectTextCandidates($, [
    "[class*='price']",
    "[data-testid*='price']",
    "span",
    "div"
  ]).filter(looksLikePrice);
  const itemNodes = $("article, [data-testid*='product'], [class*='product']");
  itemNodes.each((_, node) => {
    const root = $(node);
    const productName = normalizeText(
      root.find("h3, h4, h5, [class*='name'], [data-testid*='name']").first().text() || root.text()
    );
    const description = normalizeText(
      root.find("p, [class*='description'], [data-testid*='description']").first().text()
    );
    const priceText = normalizeText(
      root.find("[class*='price'], [data-testid*='price'], span").filter((_2, el) => looksLikePrice($(el).text())).first().text()
    );
    const price = parsePrice(priceText);
    if (!productName || !price) {
      return;
    }
    const categoryName = normalizeText(
      root.closest("section, [class*='category'], [data-testid*='category']").find("h2, h3, [class*='category']").first().text()
    );
    items.push({
      categoryName: categoryName || "Card\xE1pio iFood",
      productName,
      description: description || null,
      price
    });
  });
  if (!items.length && structuredData.length) {
    for (const entry of structuredData) {
      if (!entry || typeof entry !== "object") {
        continue;
      }
      const typedEntry = entry;
      const graph = Array.isArray(typedEntry.graph) ? typedEntry.graph : Array.isArray(typedEntry["@graph"]) ? typedEntry["@graph"] : [];
      for (const graphItem of graph) {
        if (!graphItem || typeof graphItem !== "object") {
          continue;
        }
        const name = normalizeText(graphItem.name);
        const description = normalizeText(
          graphItem.description
        );
        const priceValue = parsePrice(
          graphItem.price
        );
        if (name && priceValue) {
          items.push({
            categoryName: "Card\xE1pio iFood",
            productName: name,
            description: description || null,
            price: priceValue
          });
        }
      }
    }
  }
  if (!items.length) {
    const fallbackCategory = categoryHints[0] || "Card\xE1pio iFood";
    const candidateNames = productHints.filter((candidate) => !looksLikePrice(candidate));
    for (let index = 0; index < candidateNames.length; index += 1) {
      const productName = candidateNames[index];
      const pairedPrice = priceHints[index] || priceHints[0] || null;
      const price = parsePrice(pairedPrice);
      if (!productName || !price) {
        continue;
      }
      items.push({
        categoryName: fallbackCategory,
        productName,
        description: null,
        price
      });
    }
  }
  const dedupedItems = Array.from(
    new Map(
      items.map((item) => [
        `${normalizeComparableName(item.categoryName)}::${normalizeComparableName(item.productName)}`,
        item
      ])
    ).values()
  );
  return {
    restaurantName,
    items: dedupedItems
  };
}
var scrapeInputSchema, ImportIfoodMenuScraperService, ImportIfoodMenuScraperService_default;
var init_ImportIfoodMenuScraperService = __esm({
  "src/modules/menuImport/services/ImportIfoodMenuScraperService.ts"() {
    init_prisma();
    init_CategoryRepository();
    init_ProductRepository();
    scrapeInputSchema = z14.object({
      url: z14.string().trim().url("Informe uma URL v\xE1lida do iFood."),
      restaurantId: z14.union([z14.number(), z14.string()])
    });
    ImportIfoodMenuScraperService = class {
      async execute(input) {
        const parsedInput = scrapeInputSchema.parse(input);
        const restaurantId = Number(parsedInput.restaurantId);
        if (!Number.isInteger(restaurantId) || restaurantId <= 0) {
          throw new Error("restauranteId inv\xE1lido.");
        }
        const response = await axios.get(parsedInput.url, {
          headers: {
            "User-Agent": buildUserAgent(),
            Accept: "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8",
            "Accept-Language": "pt-BR,pt;q=0.9,en;q=0.8",
            Referer: "https://www.ifood.com.br/",
            "Cache-Control": "no-cache",
            Pragma: "no-cache"
          },
          timeout: 15e3,
          maxRedirects: 5
        });
        const html = String(response.data || "");
        if (!html.trim()) {
          throw new Error("N\xE3o foi poss\xEDvel carregar o HTML da p\xE1gina informada.");
        }
        const $ = cheerio.load(html);
        const parsedMenu = extractMenuFromCheerio($);
        if (!parsedMenu.items.length) {
          throw new Error(
            "N\xE3o foi poss\xEDvel identificar categorias e produtos na p\xE1gina p\xFAblica do iFood."
          );
        }
        const summary = await prisma_default.$transaction(async (db) => {
          const createdCategories = [];
          const createdProducts = [];
          for (const item of parsedMenu.items) {
            const categoryName = normalizeText(item.categoryName);
            const productName = normalizeText(item.productName);
            let category = await CategoryRepository_default.findByName(categoryName, restaurantId, db);
            if (!category) {
              category = await CategoryRepository_default.create(
                {
                  name: categoryName,
                  description: null,
                  image: null,
                  active: true
                },
                restaurantId,
                db
              );
              createdCategories.push({
                id: Number(category.id),
                name: category.name
              });
            }
            const existingProduct = await ProductRepository_default.findByName(productName, restaurantId, db);
            if (existingProduct) {
              continue;
            }
            const createdProduct = await ProductRepository_default.create(
              {
                name: productName,
                description: item.description || void 0,
                image: null,
                price: item.price,
                categoryId: Number(category.id),
                featured: false,
                active: true,
                stock: void 0,
                preparationTime: void 0
              },
              restaurantId,
              db
            );
            createdProducts.push({
              id: Number(createdProduct.id),
              name: createdProduct.name
            });
          }
          return {
            restaurantName: parsedMenu.restaurantName || null,
            sourceUrl: parsedInput.url,
            categoriesCreated: createdCategories.length,
            productsCreated: createdProducts.length,
            createdCategories,
            createdProducts
          };
        });
        return summary;
      }
    };
    ImportIfoodMenuScraperService_default = new ImportIfoodMenuScraperService();
  }
});

// src/modules/menuImport/controllers/ImportIfoodMenuController.ts
var ImportIfoodMenuController, ImportIfoodMenuController_default;
var init_ImportIfoodMenuController = __esm({
  "src/modules/menuImport/controllers/ImportIfoodMenuController.ts"() {
    init_ImportIfoodMenuScraperService();
    ImportIfoodMenuController = class {
      async handle(req, res) {
        try {
          const { url, restaurantId: bodyRestaurantId } = req.body;
          const resolvedRestaurantId = Number(req.user?.restaurantId || bodyRestaurantId || 0);
          const summary = await ImportIfoodMenuScraperService_default.execute({
            url,
            restaurantId: resolvedRestaurantId
          });
          return res.status(201).json(summary);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao importar cardapio do iFood"
          });
        }
      }
    };
    ImportIfoodMenuController_default = new ImportIfoodMenuController();
  }
});

// src/modules/menuImport/services/ImportMenuFromImageService.ts
import OpenAI from "openai";
import { z as z15 } from "zod";
function normalizeText2(value) {
  return String(value || "").replace(/\s+/g, " ").trim();
}
function parsePrice2(value) {
  if (typeof value === "number") {
    return Number.isFinite(value) ? value : null;
  }
  const text = normalizeText2(value);
  if (!text) {
    return null;
  }
  const normalized = text.replace(/[^\d,.-]/g, "").replace(/\.(?=\d{3}(\D|$))/g, "").replace(/,(?=\d{1,2}$)/g, ".").replace(/,/g, "");
  const parsed = Number(normalized);
  return Number.isFinite(parsed) ? parsed : null;
}
function stripCodeFences(value) {
  return value.replace(/^```(?:json)?/i, "").replace(/```$/i, "").trim();
}
function buildPrompt() {
  return [
    "Voc\xEA \xE9 um extrator de card\xE1pios a partir de imagem.",
    "Leia a imagem e retorne SOMENTE um JSON v\xE1lido, sem markdown, sem coment\xE1rios e sem texto extra.",
    "Estrutura obrigat\xF3ria:",
    "{",
    '  "restaurantName": string | null,',
    '  "categories": [',
    "    {",
    '      "name": string,',
    '      "items": [',
    "        {",
    '          "name": string,',
    '          "description": string | null,',
    '          "price": number,',
    '          "imageUrl": string | null',
    "        }",
    "      ]",
    "    }",
    "  ]",
    "}",
    "Regras:",
    "- price deve ser number em BRL, sem simbolo de moeda.",
    "- Se a descricao nao existir, use null.",
    "- imageUrl deve ser URL publica valida da foto do item quando existir na imagem/origem; se nao existir, use null.",
    "- Se houver varios grupos na imagem, organize em categorias coerentes.",
    "- Nao invente itens que nao estejam visiveis.",
    "- Se nao conseguir ler a imagem, retorne categories como array vazio."
  ].join("\n");
}
function isValidHttpUrl(value) {
  const text = normalizeText2(value);
  if (!text) {
    return false;
  }
  try {
    const parsed = new URL(text);
    return parsed.protocol === "http:" || parsed.protocol === "https:";
  } catch {
    return false;
  }
}
function createOpenAiClient() {
  const apiKey = String(process.env.OPENAI_API_KEY || "").trim();
  if (!apiKey) {
    throw new Error(
      "OPENAI_API_KEY nao configurada. Adicione a chave no ambiente antes de usar o importador por imagem."
    );
  }
  return new OpenAI({ apiKey });
}
function parseImportedMenuContent(rawContent) {
  const cleanedContent = stripCodeFences(normalizeText2(rawContent));
  let parsed;
  try {
    parsed = JSON.parse(cleanedContent);
  } catch {
    throw new Error("A OpenAI retornou um JSON invalido.");
  }
  const validated = importedMenuResponseSchema.safeParse(parsed);
  if (!validated.success) {
    throw new Error("A resposta da OpenAI nao seguiu a estrutura esperada.");
  }
  return {
    restaurantName: validated.data.restaurantName || null,
    categories: validated.data.categories.map((category) => ({
      name: normalizeText2(category.name),
      items: category.items.map((item) => {
        const price = parsePrice2(item.price);
        if (price === null) {
          throw new Error(
            `Preco invalido retornado pela OpenAI no item "${normalizeText2(item.name)}".`
          );
        }
        return {
          name: normalizeText2(item.name),
          description: normalizeText2(item.description) || null,
          price,
          imageUrl: isValidHttpUrl(item.imageUrl) ? normalizeText2(item.imageUrl) : null
        };
      })
    }))
  };
}
var importedMenuItemSchema, importedMenuCategorySchema, importedMenuResponseSchema, importInputSchema, ImportMenuFromImageService, ImportMenuFromImageService_default;
var init_ImportMenuFromImageService = __esm({
  "src/modules/menuImport/services/ImportMenuFromImageService.ts"() {
    init_prisma();
    init_CategoryRepository();
    init_ProductRepository();
    importedMenuItemSchema = z15.object({
      name: z15.string().trim().min(1, "Nome do item invalido."),
      description: z15.string().trim().nullable().optional(),
      price: z15.union([z15.number(), z15.string()]),
      imageUrl: z15.string().trim().url().nullable().optional()
    });
    importedMenuCategorySchema = z15.object({
      name: z15.string().trim().min(1, "Nome da categoria invalido."),
      items: z15.array(importedMenuItemSchema).min(1, "Categoria sem itens.")
    });
    importedMenuResponseSchema = z15.object({
      restaurantName: z15.string().trim().nullable().optional(),
      categories: z15.array(importedMenuCategorySchema).min(1, "Cardapio vazio.")
    });
    importInputSchema = z15.object({
      imageUrl: z15.string().trim().url("Informe uma URL valida da imagem."),
      restaurantId: z15.union([z15.number(), z15.string()])
    });
    ImportMenuFromImageService = class {
      async execute(input) {
        const parsedInput = importInputSchema.parse(input);
        const restaurantId = Number(parsedInput.restaurantId);
        if (!Number.isInteger(restaurantId) || restaurantId <= 0) {
          throw new Error("restauranteId invalido.");
        }
        const openai = createOpenAiClient();
        const completion = await openai.chat.completions.create({
          model: "gpt-4o",
          temperature: 0.1,
          response_format: { type: "json_object" },
          messages: [
            {
              role: "system",
              content: buildPrompt()
            },
            {
              role: "user",
              content: [
                {
                  type: "text",
                  text: "Extraia o cardapio desta imagem e retorne somente o JSON estruturado."
                },
                {
                  type: "image_url",
                  image_url: {
                    url: parsedInput.imageUrl
                  }
                }
              ]
            }
          ]
        });
        const rawContent = completion.choices[0]?.message?.content;
        if (!rawContent) {
          throw new Error("A OpenAI nao retornou conteudo para ser processado.");
        }
        const menu = parseImportedMenuContent(rawContent);
        if (!menu.categories.length) {
          throw new Error("Nenhuma categoria foi identificada na imagem enviada.");
        }
        const summary = await prisma_default.$transaction(async (db) => {
          const createdCategories = [];
          const createdProducts = [];
          for (const categoryInput of menu.categories) {
            const categoryName = normalizeText2(categoryInput.name);
            if (!categoryName) {
              continue;
            }
            let category = await CategoryRepository_default.findByName(categoryName, restaurantId, db);
            if (!category) {
              category = await CategoryRepository_default.create(
                {
                  name: categoryName,
                  description: null,
                  image: null,
                  active: true
                },
                restaurantId,
                db
              );
              createdCategories.push({
                id: Number(category.id),
                name: category.name
              });
            }
            for (const itemInput of categoryInput.items) {
              const productName = normalizeText2(itemInput.name);
              const description = normalizeText2(itemInput.description) || null;
              const price = parsePrice2(itemInput.price);
              const imageUrl = isValidHttpUrl(itemInput.imageUrl) ? normalizeText2(itemInput.imageUrl) : null;
              if (!productName || price === null) {
                continue;
              }
              const existingProduct = await ProductRepository_default.findByName(productName, restaurantId, db);
              if (existingProduct) {
                continue;
              }
              const createdProduct = await ProductRepository_default.create(
                {
                  name: productName,
                  description,
                  image: imageUrl,
                  price,
                  categoryId: Number(category.id),
                  featured: false,
                  active: true,
                  stock: void 0,
                  preparationTime: void 0
                },
                restaurantId,
                db
              );
              createdProducts.push({
                id: Number(createdProduct.id),
                name: createdProduct.name
              });
            }
          }
          return {
            restaurantName: menu.restaurantName || null,
            sourceImageUrl: parsedInput.imageUrl,
            categoriesCreated: createdCategories.length,
            productsCreated: createdProducts.length,
            createdCategories,
            createdProducts
          };
        });
        return summary;
      }
    };
    ImportMenuFromImageService_default = new ImportMenuFromImageService();
  }
});

// src/modules/menuImport/controllers/ImportMenuFromImageController.ts
var ImportMenuFromImageController, ImportMenuFromImageController_default;
var init_ImportMenuFromImageController = __esm({
  "src/modules/menuImport/controllers/ImportMenuFromImageController.ts"() {
    init_ImportMenuFromImageService();
    ImportMenuFromImageController = class {
      async handle(req, res) {
        try {
          const { imageUrl, restaurantId: bodyRestaurantId } = req.body;
          const resolvedRestaurantId = Number(req.user?.restaurantId || bodyRestaurantId || 0);
          const summary = await ImportMenuFromImageService_default.execute({
            imageUrl,
            restaurantId: resolvedRestaurantId
          });
          return res.status(201).json(summary);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao importar cardapio a partir da imagem"
          });
        }
      }
    };
    ImportMenuFromImageController_default = new ImportMenuFromImageController();
  }
});

// src/modules/menuImport/routes/MenuImportRoutes.ts
import { Router as Router14 } from "express";
var router14, MenuImportRoutes_default;
var init_MenuImportRoutes = __esm({
  "src/modules/menuImport/routes/MenuImportRoutes.ts"() {
    init_authMiddleware();
    init_adminMiddleware();
    init_ImportIfoodMenuController();
    init_ImportMenuFromImageController();
    router14 = Router14();
    router14.post("/ifood", authMiddleware, adminMiddleware, (req, res) => {
      ImportIfoodMenuController_default.handle(req, res);
    });
    router14.post("/image", authMiddleware, adminMiddleware, (req, res) => {
      ImportMenuFromImageController_default.handle(req, res);
    });
    MenuImportRoutes_default = router14;
  }
});

// src/modules/audit/controllers/GetAuditLogsController.ts
var GetAuditLogsController, GetAuditLogsController_default;
var init_GetAuditLogsController = __esm({
  "src/modules/audit/controllers/GetAuditLogsController.ts"() {
    init_prisma();
    GetAuditLogsController = class {
      async handle(_req, res) {
        try {
          const logs = await prisma_default.auditLog.findMany({
            orderBy: { createdAt: "desc" },
            take: 200
          });
          const result = logs.map((log2) => ({
            id: `AUD-${String(log2.id).padStart(4, "0")}`,
            date: log2.createdAt.toLocaleString("pt-BR"),
            user: log2.userName ?? "\u2014",
            role: log2.userRole ?? "\u2014",
            restaurant: log2.restaurantName ?? "\u2014",
            action: log2.action,
            resource: log2.resource ?? "\u2014",
            ip: log2.ipAddress ?? "\u2014",
            result: log2.result
          }));
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao listar logs de auditoria"
          });
        }
      }
    };
    GetAuditLogsController_default = new GetAuditLogsController();
  }
});

// src/modules/audit/routes/AuditRoutes.ts
import { Router as Router15 } from "express";
var router15, AuditRoutes_default;
var init_AuditRoutes = __esm({
  "src/modules/audit/routes/AuditRoutes.ts"() {
    init_authMiddleware();
    init_superAdminMiddleware();
    init_GetAuditLogsController();
    router15 = Router15();
    router15.get(
      "/",
      authMiddleware,
      superAdminMiddleware,
      (req, res) => GetAuditLogsController_default.handle(req, res)
    );
    AuditRoutes_default = router15;
  }
});

// src/modules/favorites/routes/FavoriteRoutes.ts
import { Router as Router16 } from "express";
function clientContext(req, res) {
  if (req.user.role !== "CLIENTE" || !req.user.id) {
    res.status(403).json({ error: "Favoritos s\xE3o exclusivos para clientes." });
    return null;
  }
  return { userId: Number(req.user.id) };
}
var router16, FavoriteRoutes_default;
var init_FavoriteRoutes = __esm({
  "src/modules/favorites/routes/FavoriteRoutes.ts"() {
    init_prisma();
    init_authMiddleware();
    router16 = Router16();
    router16.get("/", authMiddleware, async (req, res) => {
      const context = clientContext(req, res);
      if (!context) return;
      const restaurantId = Number(req.query.restaurantId);
      const favorites = await prisma_default.productFavorite.findMany({
        where: {
          userId: context.userId,
          ...Number.isInteger(restaurantId) && restaurantId > 0 ? { restaurantId } : {}
        },
        include: { product: { include: { category: true } } },
        orderBy: { createdAt: "desc" }
      });
      res.json({ favorites: favorites.map((item) => item.product) });
    });
    router16.post("/:productId", authMiddleware, async (req, res) => {
      const context = clientContext(req, res);
      if (!context) return;
      const productId = Number(req.params.productId);
      if (!Number.isInteger(productId) || productId <= 0) {
        res.status(400).json({ error: "Produto inv\xE1lido." });
        return;
      }
      const product = await prisma_default.product.findFirst({
        where: { id: productId }
      });
      if (!product) {
        res.status(404).json({ error: "Produto n\xE3o encontrado." });
        return;
      }
      await prisma_default.productFavorite.upsert({
        where: { userId_productId: { userId: context.userId, productId } },
        update: { restaurantId: product.restaurantId },
        create: {
          userId: context.userId,
          productId,
          restaurantId: product.restaurantId
        }
      });
      res.status(201).json({ favorite: true, product });
    });
    router16.delete("/:productId", authMiddleware, async (req, res) => {
      const context = clientContext(req, res);
      if (!context) return;
      await prisma_default.productFavorite.deleteMany({
        where: { userId: context.userId, productId: Number(req.params.productId) }
      });
      res.json({ favorite: false });
    });
    FavoriteRoutes_default = router16;
  }
});

// src/modules/imageEnhancement/services/EnhanceRestaurantImageService.ts
import OpenAI2, { toFile } from "openai";
var DATA_URL_PATTERN, EnhanceRestaurantImageService, EnhanceRestaurantImageService_default;
var init_EnhanceRestaurantImageService = __esm({
  "src/modules/imageEnhancement/services/EnhanceRestaurantImageService.ts"() {
    DATA_URL_PATTERN = /^data:(image\/(?:jpeg|png|webp));base64,([A-Za-z0-9+/=\r\n]+)$/;
    EnhanceRestaurantImageService = class {
      async execute(imageDataUrl) {
        const apiKey = String(process.env.OPENAI_API_KEY || "").trim();
        if (!apiKey) throw new Error("OPENAI_API_KEY n\xE3o configurada no servidor.");
        const match = String(imageDataUrl || "").match(DATA_URL_PATTERN);
        if (!match) throw new Error("Envie uma imagem JPG, PNG ou WebP v\xE1lida.");
        const input = Buffer.from(match[2], "base64");
        if (!input.length || input.length > 5 * 1024 * 1024) {
          throw new Error("A imagem deve ter no m\xE1ximo 5 MB.");
        }
        const client = new OpenAI2({ apiKey });
        const editRequest = {
          model: "gpt-image-2",
          image: await toFile(input, "restaurant-cover.webp", { type: match[1] }),
          prompt: "Create a polished high-definition square login hero from this restaurant brand image. Faithfully restore the complete original logo, lettering, colors and identity with crisp clean edges. Place the entire logo centered and clearly visible, occupying at most 55 percent of the canvas, with generous space around it. Build a tasteful, softly lit pizza restaurant background that complements the logo. Remove blur, pixelation and compression artifacts. Do not crop the logo, do not enlarge it to fill the canvas, do not alter its wording, and do not add new text, brands or watermarks.",
          size: "1024x1024",
          quality: "high"
        };
        const result = await client.images.edit(editRequest);
        const base64 = result.data?.[0]?.b64_json;
        if (!base64) throw new Error("A IA n\xE3o retornou a imagem melhorada.");
        return { imageDataUrl: `data:image/png;base64,${base64}` };
      }
    };
    EnhanceRestaurantImageService_default = new EnhanceRestaurantImageService();
  }
});

// src/modules/imageEnhancement/controllers/EnhanceRestaurantImageController.ts
var EnhanceRestaurantImageController, EnhanceRestaurantImageController_default;
var init_EnhanceRestaurantImageController = __esm({
  "src/modules/imageEnhancement/controllers/EnhanceRestaurantImageController.ts"() {
    init_EnhanceRestaurantImageService();
    EnhanceRestaurantImageController = class {
      async handle(req, res) {
        try {
          return res.json(await EnhanceRestaurantImageService_default.execute(req.body?.imageDataUrl));
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel melhorar a imagem."
          });
        }
      }
    };
    EnhanceRestaurantImageController_default = new EnhanceRestaurantImageController();
  }
});

// src/modules/imageEnhancement/routes/ImageEnhancementRoutes.ts
import { Router as Router17 } from "express";
var router17, ImageEnhancementRoutes_default;
var init_ImageEnhancementRoutes = __esm({
  "src/modules/imageEnhancement/routes/ImageEnhancementRoutes.ts"() {
    init_authMiddleware();
    init_adminMiddleware();
    init_EnhanceRestaurantImageController();
    router17 = Router17();
    router17.post(
      "/restaurant",
      authMiddleware,
      adminMiddleware,
      (req, res) => EnhanceRestaurantImageController_default.handle(req, res)
    );
    ImageEnhancementRoutes_default = router17;
  }
});

// src/modules/customerAddresses/routes/CustomerAddressRoutes.ts
import { Router as Router18 } from "express";
import { z as z16 } from "zod";
function customerId(req, res) {
  if (req.user.role !== "CLIENTE" || !req.user.id) {
    res.status(403).json({ error: "Endere\xE7os s\xE3o exclusivos para clientes." });
    return null;
  }
  return Number(req.user.id);
}
var router18, addressSchema, CustomerAddressRoutes_default;
var init_CustomerAddressRoutes = __esm({
  "src/modules/customerAddresses/routes/CustomerAddressRoutes.ts"() {
    init_prisma();
    init_authMiddleware();
    router18 = Router18();
    router18.use(authMiddleware);
    addressSchema = z16.object({
      label: z16.string().trim().min(1).max(40),
      address: z16.string().trim().min(3).max(160),
      number: z16.string().trim().regex(/^\d+[A-Za-z]?$/).max(10),
      district: z16.string().trim().min(2).max(100),
      city: z16.string().trim().min(2).max(100),
      state: z16.string().trim().length(2).transform((value) => value.toUpperCase()),
      zipCode: z16.string().transform((value) => value.replace(/\D/g, "")).refine((value) => value.length === 8),
      complement: z16.string().trim().max(160).optional().default(""),
      isDefault: z16.boolean().optional().default(false)
    });
    router18.get("/", async (req, res) => {
      const userId = customerId(req, res);
      if (!userId) return;
      const addresses = await prisma_default.userAddress.findMany({
        where: { userId },
        orderBy: [{ isDefault: "desc" }, { createdAt: "desc" }]
      });
      res.json({ addresses });
    });
    router18.post("/", async (req, res) => {
      const userId = customerId(req, res);
      if (!userId) return;
      const parsed = addressSchema.safeParse(req.body);
      if (!parsed.success) {
        res.status(400).json({ error: "Preencha todos os dados do endere\xE7o corretamente." });
        return;
      }
      const count = await prisma_default.userAddress.count({ where: { userId } });
      const makeDefault = parsed.data.isDefault || count === 0;
      const address = await prisma_default.$transaction(async (tx) => {
        if (makeDefault)
          await tx.userAddress.updateMany({ where: { userId }, data: { isDefault: false } });
        return tx.userAddress.create({
          data: {
            userId,
            label: parsed.data.label,
            address: parsed.data.address,
            number: parsed.data.number,
            district: parsed.data.district,
            city: parsed.data.city,
            state: parsed.data.state,
            zipCode: parsed.data.zipCode,
            complement: parsed.data.complement || null,
            isDefault: makeDefault
          }
        });
      });
      res.status(201).json({ address });
    });
    router18.put("/:id/default", async (req, res) => {
      const userId = customerId(req, res);
      if (!userId) return;
      const id = Number(req.params.id);
      const exists = await prisma_default.userAddress.findFirst({ where: { id, userId } });
      if (!exists) {
        res.status(404).json({ error: "Endere\xE7o n\xE3o encontrado." });
        return;
      }
      const address = await prisma_default.$transaction(async (tx) => {
        await tx.userAddress.updateMany({ where: { userId }, data: { isDefault: false } });
        return tx.userAddress.update({ where: { id }, data: { isDefault: true } });
      });
      res.json({ address });
    });
    CustomerAddressRoutes_default = router18;
  }
});

// src/modules/orders/controllers/AsaasOrderWebhookController.ts
var TERMINAL_UNPAID_EVENTS, AsaasOrderWebhookController, AsaasOrderWebhookController_default;
var init_AsaasOrderWebhookController = __esm({
  "src/modules/orders/controllers/AsaasOrderWebhookController.ts"() {
    init_prisma();
    init_server();
    init_OrderRepository();
    init_couponRedemptionLifecycle();
    init_waiterOrderRealtime();
    init_FailPendingOrderPaymentService();
    init_ReconcileLateCancelledPaymentService();
    TERMINAL_UNPAID_EVENTS = /* @__PURE__ */ new Set(["PAYMENT_CANCELED", "PAYMENT_DELETED", "PAYMENT_REFUNDED"]);
    AsaasOrderWebhookController = class {
      async handle(req, res) {
        try {
          const tokenFromHeader = String(req.header("asaas-access-token") || "").trim();
          const expectedToken = String(process.env.ASAAS_WEBHOOK_TOKEN || "").trim();
          if (!expectedToken || tokenFromHeader !== expectedToken) {
            return res.status(401).json({ error: "Token de webhook invalido." });
          }
          const payload = req.body;
          const event = String(payload?.event || "").trim().toUpperCase();
          const isPaymentReceived = event === "PAYMENT_RECEIVED";
          const isTerminalUnpaidEvent = TERMINAL_UNPAID_EVENTS.has(event);
          if (!isPaymentReceived && !isTerminalUnpaidEvent) {
            return res.status(200).json({ received: true, ignored: true });
          }
          const payment = payload?.payment;
          const externalReference = String(payment?.externalReference || "").trim();
          const asaasPaymentId = String(payment?.id || "").trim();
          const paymentValue = Number(payment?.value);
          const walletId = String(payment?.walletId || "").trim();
          const hasRequiredPaymentFields = Boolean(asaasPaymentId) && Boolean(externalReference) && (isTerminalUnpaidEvent || Number.isFinite(paymentValue) && paymentValue >= 0 && Boolean(walletId));
          if (!hasRequiredPaymentFields) {
            return res.status(200).json({ received: true, ignored: true });
          }
          const pixReference = /^orderpix:(\d+):(\d+)$/i.exec(externalReference);
          const referencedRestaurantId = pixReference ? Number(pixReference[1]) : null;
          const orderId = Number(pixReference?.[2] || externalReference);
          if (!Number.isInteger(orderId) || orderId <= 0) {
            return res.status(200).json({ received: true, ignored: true });
          }
          const order = await prisma_default.order.findUnique({
            where: {
              id: orderId
            },
            select: {
              id: true,
              restaurantId: true,
              userId: true,
              paid: true,
              status: true,
              paymentMethod: true,
              pixPaymentId: true,
              total: true
            }
          });
          if (!order) {
            return res.status(200).json({ received: true, ignored: true });
          }
          if (referencedRestaurantId && referencedRestaurantId !== order.restaurantId) {
            return res.status(200).json({ received: true, ignored: true });
          }
          if (isTerminalUnpaidEvent) {
            await FailPendingOrderPaymentService_default.execute({
              orderId: order.id,
              restaurantId: order.restaurantId
            });
            return res.status(200).json({ received: true, processed: true });
          }
          if (Math.abs(paymentValue - Number(order.total)) > 9e-3) {
            return res.status(200).json({ received: true, ignored: true });
          }
          const normalizedPaymentMethod = String(order.paymentMethod || "").trim().toUpperCase();
          const isSupportedAutomaticMethod = normalizedPaymentMethod === "PIX" || normalizedPaymentMethod === "CARTAO";
          if (!isSupportedAutomaticMethod) {
            return res.status(200).json({ received: true, ignored: true });
          }
          if (String(order.status) === "CANCELADO" && order.paid !== true) {
            await ReconcileLateCancelledPaymentService_default.execute({
              orderId: order.id,
              restaurantId: order.restaurantId,
              paymentMethod: normalizedPaymentMethod,
              paymentReference: normalizedPaymentMethod === "PIX" ? `asaas:${asaasPaymentId}` : `asaas_pay:${asaasPaymentId}`
            });
            return res.status(200).json({ received: true, processed: true, refunded: true });
          }
          if (walletId) {
            try {
              await prisma_default.restaurantSettings.updateMany({
                where: {
                  restaurantId: order.restaurantId,
                  OR: [{ gatewayMerchantId: null }, { gatewayMerchantId: "" }]
                },
                data: {
                  gatewayMerchantId: walletId
                }
              });
            } catch (settingsUpdateError) {
              console.warn(
                "[ASAAS_WEBHOOK_GATEWAY_ID_BACKFILL_ERROR]",
                settingsUpdateError instanceof Error ? settingsUpdateError.message : String(settingsUpdateError)
              );
            }
          }
          if (!order.paid) {
            if (normalizedPaymentMethod === "PIX" && asaasPaymentId && !String(order.pixPaymentId || "").trim()) {
              await prisma_default.order.update({
                where: {
                  id: order.id
                },
                data: {
                  pixPaymentId: `asaas:${asaasPaymentId}`
                }
              });
            }
            const updatedOrder = await prisma_default.$transaction(async (tx) => {
              const confirmedOrder = await OrderRepository_default.confirmPayment(
                order.id,
                order.restaurantId,
                tx
              );
              await markCouponRedemptionUsedForOrder(order.id, order.restaurantId, tx);
              return confirmedOrder;
            });
            io.to(`restaurant:${updatedOrder.restaurantId}`).emit("order:payment-confirmed", {
              orderId: updatedOrder.id,
              paid: true,
              paymentMethod: updatedOrder.paymentMethod
            });
            emitTableSessionOrderEvent(io, "order:payment-confirmed", updatedOrder);
            io.to(`restaurant:${updatedOrder.restaurantId}`).emit("new-order", updatedOrder);
            emitWaiterTableOrderEvent(io, "new-order", updatedOrder);
            emitTableSessionOrderEvent(io, "new-order", updatedOrder);
            io.to(`restaurant:${updatedOrder.restaurantId}`).emit("order:status-changed", updatedOrder);
            emitTableSessionOrderEvent(io, "order:status-changed", updatedOrder);
            io.to(`restaurant:${updatedOrder.restaurantId}:kitchen`).emit("kitchen:order-paid", {
              orderId: updatedOrder.id,
              restaurantId: updatedOrder.restaurantId,
              paid: true
            });
            if (updatedOrder.userId) {
              io.to(`user:${updatedOrder.userId}`).emit("payment-confirmed", {
                orderId: updatedOrder.id,
                paid: true,
                paymentMethod: updatedOrder.paymentMethod,
                status: updatedOrder.status
              });
            }
          }
          return res.status(200).json({ received: true, processed: true });
        } catch (error2) {
          console.error(
            "[ASAAS_WEBHOOK_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(500).json({ received: true, processed: false });
        }
      }
    };
    AsaasOrderWebhookController_default = new AsaasOrderWebhookController();
  }
});

// src/modules/restaurantSettings/controllers/AsaasWithdrawValidationWebhookController.ts
var AsaasWithdrawValidationWebhookController, AsaasWithdrawValidationWebhookController_default;
var init_AsaasWithdrawValidationWebhookController = __esm({
  "src/modules/restaurantSettings/controllers/AsaasWithdrawValidationWebhookController.ts"() {
    AsaasWithdrawValidationWebhookController = class {
      async handle(req, res) {
        try {
          const tokenFromHeader = String(req.header("asaas-access-token") || "").trim();
          const expectedToken = String(
            process.env.ASAAS_WITHDRAW_WEBHOOK_TOKEN || process.env.ASAAS_WEBHOOK_TOKEN || ""
          ).trim();
          if (!expectedToken || tokenFromHeader !== expectedToken) {
            return res.status(401).json({
              status: "REFUSED",
              refuseReason: "Token de webhook invalido."
            });
          }
          const payload = req.body;
          const normalizedType = String(payload?.type || "").trim().toUpperCase();
          if (!normalizedType) {
            return res.status(200).json({
              status: "REFUSED",
              refuseReason: "Payload sem tipo de operacao."
            });
          }
          return res.status(200).json({ status: "APPROVED" });
        } catch (error2) {
          console.error(
            "[ASAAS_WITHDRAW_VALIDATION_WEBHOOK_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(200).json({
            status: "REFUSED",
            refuseReason: "Falha interna na validacao de saque."
          });
        }
      }
    };
    AsaasWithdrawValidationWebhookController_default = new AsaasWithdrawValidationWebhookController();
  }
});

// src/validators/IngredientValidator.ts
import { z as z17 } from "zod";
var createIngredientSchema, updateIngredientSchema;
var init_IngredientValidator = __esm({
  "src/validators/IngredientValidator.ts"() {
    createIngredientSchema = z17.object({
      name: z17.string().trim().min(1, "Nome do ingrediente \xE9 obrigat\xF3rio.").max(80),
      category: z17.string({
        invalid_type_error: "A categoria do ingrediente deve ser um texto.",
        required_error: "Categoria do ingrediente \xE9 obrigat\xF3ria."
      }).trim().min(1, "Categoria do ingrediente \xE9 obrigat\xF3ria.").max(60, "A categoria deve ter no m\xE1ximo 60 caracteres."),
      price: z17.number({
        invalid_type_error: "O valor adicional deve ser um n\xFAmero.",
        required_error: "O valor adicional \xE9 obrigat\xF3rio."
      }).min(0, "O valor adicional n\xE3o pode ser negativo.").max(99999, "O valor adicional informado \xE9 muito alto."),
      active: z17.boolean().optional()
    });
    updateIngredientSchema = createIngredientSchema.partial().refine(
      (data) => Object.keys(data).length > 0,
      "Informe ao menos um campo para atualizar."
    );
  }
});

// src/modules/ingredients/repositories/IngredientRepository.ts
var IngredientRepository, IngredientRepository_default;
var init_IngredientRepository = __esm({
  "src/modules/ingredients/repositories/IngredientRepository.ts"() {
    init_prisma();
    IngredientRepository = class {
      async findAll(restaurantId, db = prisma_default) {
        return db.ingredient.findMany({
          where: { restaurantId },
          orderBy: [{ category: "asc" }, { active: "desc" }, { name: "asc" }]
        });
      }
      async findById(id, restaurantId, db = prisma_default) {
        return db.ingredient.findFirst({ where: { id, restaurantId } });
      }
      async findByName(name, restaurantId, db = prisma_default) {
        return db.ingredient.findFirst({
          where: {
            restaurantId,
            name: { equals: name.trim(), mode: "insensitive" }
          }
        });
      }
      async create(data, restaurantId, db = prisma_default) {
        return db.ingredient.create({ data: { ...data, restaurantId } });
      }
      async update(id, data, restaurantId, db = prisma_default) {
        await db.ingredient.updateMany({ where: { id, restaurantId }, data });
        return this.findById(id, restaurantId, db);
      }
      async delete(id, restaurantId, db = prisma_default) {
        const usedByProduct = await db.productOption.findFirst({
          where: {
            ingredientId: id,
            group: { restaurantId }
          },
          select: { id: true }
        });
        if (usedByProduct) {
          throw new Error(
            "Este ingrediente est\xE1 vinculado a um produto. Remova-o dos grupos ou desative-o."
          );
        }
        return db.ingredient.deleteMany({ where: { id, restaurantId } });
      }
    };
    IngredientRepository_default = new IngredientRepository();
  }
});

// src/modules/ingredients/services/IngredientServices.ts
function assertRestaurantId(restaurantId) {
  if (!Number.isInteger(Number(restaurantId)) || Number(restaurantId) <= 0) {
    throw new Error("Restaurante n\xE3o encontrado.");
  }
  return Number(restaurantId);
}
var ListIngredientsService, CreateIngredientService, UpdateIngredientService, DeleteIngredientService, listIngredientsService, createIngredientService, updateIngredientService, deleteIngredientService;
var init_IngredientServices = __esm({
  "src/modules/ingredients/services/IngredientServices.ts"() {
    init_IngredientValidator();
    init_IngredientRepository();
    ListIngredientsService = class {
      async execute(restaurantId) {
        const tenantId = assertRestaurantId(restaurantId);
        const ingredients = await IngredientRepository_default.findAll(tenantId);
        const categories = [...new Set(ingredients.map((ingredient) => ingredient.category))].sort(
          (left, right) => left.localeCompare(right, "pt-BR")
        );
        return { ingredients, count: ingredients.length, categories };
      }
    };
    CreateIngredientService = class {
      async execute(input, restaurantId) {
        const tenantId = assertRestaurantId(restaurantId);
        const data = createIngredientSchema.parse(input);
        const duplicate = await IngredientRepository_default.findByName(data.name, tenantId);
        if (duplicate) {
          throw new Error("J\xE1 existe um ingrediente com este nome neste restaurante.");
        }
        return IngredientRepository_default.create(
          {
            name: data.name,
            category: data.category,
            price: data.price,
            active: data.active ?? true
          },
          tenantId
        );
      }
    };
    UpdateIngredientService = class {
      async execute(id, input, restaurantId) {
        const tenantId = assertRestaurantId(restaurantId);
        const ingredientId = Number(id);
        const data = updateIngredientSchema.parse(input);
        const existing = await IngredientRepository_default.findById(ingredientId, tenantId);
        if (!existing) {
          throw new Error("Ingrediente n\xE3o encontrado neste restaurante.");
        }
        if (data.name) {
          const duplicate = await IngredientRepository_default.findByName(data.name, tenantId);
          if (duplicate && duplicate.id !== ingredientId) {
            throw new Error("J\xE1 existe um ingrediente com este nome neste restaurante.");
          }
        }
        return IngredientRepository_default.update(ingredientId, data, tenantId);
      }
    };
    DeleteIngredientService = class {
      async execute(id, restaurantId) {
        const tenantId = assertRestaurantId(restaurantId);
        const ingredientId = Number(id);
        const existing = await IngredientRepository_default.findById(ingredientId, tenantId);
        if (!existing) {
          throw new Error("Ingrediente n\xE3o encontrado neste restaurante.");
        }
        await IngredientRepository_default.delete(ingredientId, tenantId);
        return { message: "Ingrediente exclu\xEDdo com sucesso." };
      }
    };
    listIngredientsService = new ListIngredientsService();
    createIngredientService = new CreateIngredientService();
    updateIngredientService = new UpdateIngredientService();
    deleteIngredientService = new DeleteIngredientService();
  }
});

// src/modules/ingredients/controllers/IngredientControllers.ts
function errorMessage(error2, fallback) {
  return error2 instanceof Error ? error2.message : fallback;
}
async function listIngredients(req, res) {
  try {
    return res.json(await listIngredientsService.execute(Number(req.user?.restaurantId)));
  } catch (error2) {
    return res.status(400).json({ error: errorMessage(error2, "Erro ao listar ingredientes.") });
  }
}
async function createIngredient(req, res) {
  try {
    const ingredient = await createIngredientService.execute(
      req.body,
      Number(req.user?.restaurantId)
    );
    return res.status(201).json(ingredient);
  } catch (error2) {
    return res.status(400).json({ error: errorMessage(error2, "Erro ao criar ingrediente.") });
  }
}
async function updateIngredient(req, res) {
  try {
    const ingredient = await updateIngredientService.execute(
      Number(req.params.id),
      req.body,
      Number(req.user?.restaurantId)
    );
    return res.json(ingredient);
  } catch (error2) {
    return res.status(400).json({ error: errorMessage(error2, "Erro ao atualizar ingrediente.") });
  }
}
async function deleteIngredient(req, res) {
  try {
    return res.json(
      await deleteIngredientService.execute(
        Number(req.params.id),
        Number(req.user?.restaurantId)
      )
    );
  } catch (error2) {
    return res.status(400).json({ error: errorMessage(error2, "Erro ao excluir ingrediente.") });
  }
}
var init_IngredientControllers = __esm({
  "src/modules/ingredients/controllers/IngredientControllers.ts"() {
    init_IngredientServices();
  }
});

// src/modules/ingredients/routes/ingredientRoutes.ts
import { Router as Router19 } from "express";
var router19, ingredientRoutes_default;
var init_ingredientRoutes = __esm({
  "src/modules/ingredients/routes/ingredientRoutes.ts"() {
    init_authMiddleware();
    init_adminMiddleware();
    init_IngredientControllers();
    router19 = Router19();
    router19.use(authMiddleware, adminMiddleware);
    router19.get("/", listIngredients);
    router19.post("/", createIngredient);
    router19.put("/:id", updateIngredient);
    router19.delete("/:id", deleteIngredient);
    ingredientRoutes_default = router19;
  }
});

// src/modules/waiterCalls/services/CreateTableServiceCallService.ts
import { PlanType as PlanType5, Prisma as Prisma8, SubscriptionStatus as SubscriptionStatus2, TableServiceCallType as TableServiceCallType2 } from "@prisma/client";
function isUniqueConflict3(error2) {
  return Boolean(error2 && typeof error2 === "object" && "code" in error2 && error2.code === "P2002");
}
async function emitCreated(call) {
  try {
    await tableServiceCallEvents.created(call);
  } catch (error2) {
    console.error(
      "[WAITER_CALL_REALTIME_ERROR]",
      error2 instanceof Error ? error2.message : String(error2)
    );
  }
}
var CreateTableServiceCallService, CreateTableServiceCallService_default;
var init_CreateTableServiceCallService = __esm({
  "src/modules/waiterCalls/services/CreateTableServiceCallService.ts"() {
    init_prisma();
    init_TableServiceCallRepository();
    init_tableServiceCallEvents();
    init_TableAccountSettingsRepository();
    init_tablePaymentLedger();
    CreateTableServiceCallService = class {
      normalizeSessionCallType(type) {
        const normalized = String(type || "").trim().toUpperCase();
        if (normalized !== TableServiceCallType2.WAITER && normalized !== TableServiceCallType2.BILL) {
          throw new Error("Tipo de chamado inv\xE1lido. Use WAITER ou BILL.");
        }
        return normalized;
      }
      async createIdempotently({
        restaurantId,
        tableId,
        tableSessionId,
        type,
        db
      }) {
        const existing = await TableServiceCallRepository_default.findActiveByTableAndType(
          restaurantId,
          tableId,
          type,
          db
        );
        if (existing) {
          return { call: existing, duplicate: true };
        }
        try {
          const call = await TableServiceCallRepository_default.create({
            restaurantId,
            tableId,
            tableSessionId,
            type
          }, db);
          return { call, duplicate: false };
        } catch (error2) {
          if (isUniqueConflict3(error2)) {
            const concurrent = await TableServiceCallRepository_default.findActiveByTableAndType(
              restaurantId,
              tableId,
              type,
              db
            );
            if (concurrent) {
              return { call: concurrent, duplicate: true };
            }
          }
          throw error2;
        }
      }
      async execute({ sessionId, tableId, restaurantId, type }) {
        const normalizedSessionId = Number(sessionId);
        const normalizedTableId = Number(tableId);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedSessionId) || normalizedSessionId <= 0 || !Number.isInteger(normalizedTableId) || normalizedTableId <= 0 || !Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Sess\xE3o de mesa inv\xE1lida para criar o chamado.");
        }
        const normalizedType = this.normalizeSessionCallType(type);
        const result = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, normalizedRestaurantId, normalizedSessionId);
            const context = await TableServiceCallRepository_default.findOpenSessionContext(
              normalizedSessionId,
              normalizedTableId,
              normalizedRestaurantId,
              tx
            );
            if (!context) {
              throw new Error("A sess\xE3o desta mesa n\xE3o est\xE1 mais ativa. Escaneie o QR novamente.");
            }
            const subscription = context.table.restaurant.subscription;
            const subscriptionIsActive = subscription?.status === SubscriptionStatus2.ATIVA || subscription?.status === SubscriptionStatus2.TESTE;
            if (!subscriptionIsActive || subscription?.plan !== PlanType5.PREMIUM) {
              throw new Error(
                "O atendimento pelo card\xE1pio de mesa n\xE3o est\xE1 dispon\xEDvel neste restaurante."
              );
            }
            const settings = context.table.restaurant.settings;
            if (settings?.tableOrderingEnabled === false) {
              throw new Error("O atendimento pelo card\xE1pio de mesa est\xE1 desativado neste restaurante.");
            }
            if (normalizedType === TableServiceCallType2.WAITER && settings?.waiterCallEnabled === false) {
              throw new Error("Chamados ao gar\xE7om est\xE3o desativados neste restaurante.");
            }
            if (normalizedType === TableServiceCallType2.BILL && settings?.billRequestEnabled === false) {
              throw new Error("Solicita\xE7\xF5es de conta est\xE3o desativadas neste restaurante.");
            }
            const callResult = await this.createIdempotently({
              restaurantId: normalizedRestaurantId,
              tableId: normalizedTableId,
              tableSessionId: normalizedSessionId,
              type: normalizedType,
              db: tx
            });
            if (normalizedType === TableServiceCallType2.BILL) {
              const accountSettings = await TableAccountSettingsRepository_default.findByRestaurantId(
                normalizedRestaurantId,
                tx
              );
              if (accountSettings.blockNewOrdersOnClosingRequest) {
                await TableServiceCallRepository_default.requestSessionClosing(
                  normalizedSessionId,
                  normalizedRestaurantId,
                  tx
                );
              }
            }
            return callResult;
          },
          { isolationLevel: Prisma8.TransactionIsolationLevel.Serializable }
        );
        if (!result.duplicate) {
          await emitCreated(result.call);
        }
        return result;
      }
    };
    CreateTableServiceCallService_default = new CreateTableServiceCallService();
  }
});

// src/modules/waiterCalls/controllers/CreateTableServiceCallController.ts
var CreateTableServiceCallController, CreateTableServiceCallController_default;
var init_CreateTableServiceCallController = __esm({
  "src/modules/waiterCalls/controllers/CreateTableServiceCallController.ts"() {
    init_CreateTableServiceCallService();
    CreateTableServiceCallController = class {
      async handle(req, res) {
        try {
          const result = await CreateTableServiceCallService_default.execute({
            sessionId: req.tableSession.id,
            tableId: req.tableSession.tableId,
            restaurantId: req.tableSession.restaurantId,
            type: req.body?.type
          });
          return res.status(result.duplicate ? 200 : 201).json({
            ...result.call,
            duplicate: result.duplicate
          });
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel enviar o chamado."
          });
        }
      }
    };
    CreateTableServiceCallController_default = new CreateTableServiceCallController();
  }
});

// src/modules/waiterCalls/services/ListTableServiceCallsService.ts
import { TableServiceCallStatus as TableServiceCallStatus2, TableServiceCallType as TableServiceCallType3 } from "@prisma/client";
var ListTableServiceCallsService, ListTableServiceCallsService_default;
var init_ListTableServiceCallsService = __esm({
  "src/modules/waiterCalls/services/ListTableServiceCallsService.ts"() {
    init_TableServiceCallRepository();
    ListTableServiceCallsService = class {
      async execute({ restaurantId, status, type, tableNumber }) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante n\xE3o identificado para listar chamados.");
        }
        const normalizedStatus = String(status || "").trim().toUpperCase();
        const normalizedType = String(type || "").trim().toUpperCase();
        const normalizedTableNumber = String(tableNumber || "").trim();
        if (normalizedStatus && !Object.values(TableServiceCallStatus2).includes(normalizedStatus)) {
          throw new Error("Status de chamado inv\xE1lido.");
        }
        if (normalizedType && !Object.values(TableServiceCallType3).includes(normalizedType)) {
          throw new Error("Tipo de chamado inv\xE1lido.");
        }
        let parsedTableNumber;
        if (normalizedTableNumber) {
          parsedTableNumber = Number(normalizedTableNumber);
          if (!Number.isInteger(parsedTableNumber) || parsedTableNumber <= 0) {
            throw new Error("N\xFAmero da mesa inv\xE1lido.");
          }
        }
        const startOfToday = /* @__PURE__ */ new Date();
        startOfToday.setHours(0, 0, 0, 0);
        return TableServiceCallRepository_default.listByRestaurant(normalizedRestaurantId, {
          status: normalizedStatus ? normalizedStatus : void 0,
          type: normalizedType ? normalizedType : void 0,
          tableNumber: parsedTableNumber,
          resolvedSince: startOfToday,
          take: 200
        });
      }
    };
    ListTableServiceCallsService_default = new ListTableServiceCallsService();
  }
});

// src/modules/waiterCalls/controllers/ListTableServiceCallsController.ts
function first(value) {
  return Array.isArray(value) ? value[0] : value;
}
var ListTableServiceCallsController, ListTableServiceCallsController_default;
var init_ListTableServiceCallsController = __esm({
  "src/modules/waiterCalls/controllers/ListTableServiceCallsController.ts"() {
    init_ListTableServiceCallsService();
    ListTableServiceCallsController = class {
      async handle(req, res) {
        try {
          const calls = await ListTableServiceCallsService_default.execute({
            restaurantId: req.user.restaurantId,
            status: String(first(req.query.status) || ""),
            type: String(first(req.query.type) || ""),
            tableNumber: String(first(req.query.tableNumber) || "")
          });
          return res.status(200).json(calls);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel listar os chamados."
          });
        }
      }
    };
    ListTableServiceCallsController_default = new ListTableServiceCallsController();
  }
});

// src/modules/waiterCalls/services/UpdateTableServiceCallStatusService.ts
import { TableServiceCallStatus as TableServiceCallStatus3, UserRole as UserRole30 } from "@prisma/client";
var UpdateTableServiceCallStatusService, UpdateTableServiceCallStatusService_default;
var init_UpdateTableServiceCallStatusService = __esm({
  "src/modules/waiterCalls/services/UpdateTableServiceCallStatusService.ts"() {
    init_TableServiceCallRepository();
    init_tableServiceCallEvents();
    UpdateTableServiceCallStatusService = class {
      async execute({ id, restaurantId, actorUserId, actorRole, status }) {
        const normalizedId = Number(id);
        const normalizedRestaurantId = Number(restaurantId);
        const normalizedActorId = Number(actorUserId);
        const normalizedStatus = String(status || "").trim().toUpperCase();
        if (!Number.isInteger(normalizedId) || normalizedId <= 0) {
          throw new Error("Chamado inv\xE1lido.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante n\xE3o identificado para atualizar o chamado.");
        }
        if (!Number.isInteger(normalizedActorId) || normalizedActorId <= 0) {
          throw new Error("Funcion\xE1rio n\xE3o identificado para atualizar o chamado.");
        }
        if (normalizedStatus !== TableServiceCallStatus3.IN_PROGRESS && normalizedStatus !== TableServiceCallStatus3.RESOLVED) {
          throw new Error("Use IN_PROGRESS para assumir ou RESOLVED para concluir o chamado.");
        }
        const current = await TableServiceCallRepository_default.findByIdForRestaurant(
          normalizedId,
          normalizedRestaurantId
        );
        if (!current) {
          throw new Error("Chamado n\xE3o encontrado neste restaurante.");
        }
        if (current.status === normalizedStatus) {
          return current;
        }
        if (normalizedStatus === TableServiceCallStatus3.IN_PROGRESS) {
          if (current.status !== TableServiceCallStatus3.WAITING) {
            throw new Error("Somente chamados aguardando podem ser assumidos.");
          }
          const changed = await TableServiceCallRepository_default.assignIfWaiting(
            normalizedId,
            normalizedRestaurantId,
            normalizedActorId
          );
          if (changed !== 1) {
            throw new Error("Este chamado foi assumido por outra pessoa. Atualize a tela.");
          }
        } else {
          if (current.status !== TableServiceCallStatus3.IN_PROGRESS) {
            throw new Error("Assuma o chamado antes de conclu\xED-lo.");
          }
          const isAdmin = String(actorRole || "").toUpperCase() === UserRole30.ADMIN;
          if (!isAdmin && current.assignedToId !== normalizedActorId) {
            throw new Error("Somente o gar\xE7om que assumiu o chamado pode conclu\xED-lo.");
          }
          const changed = await TableServiceCallRepository_default.resolveIfInProgress(
            normalizedId,
            normalizedRestaurantId,
            normalizedActorId
          );
          if (changed !== 1) {
            throw new Error("Este chamado j\xE1 foi atualizado. Atualize a tela.");
          }
        }
        const updated = await TableServiceCallRepository_default.findByIdForRestaurant(
          normalizedId,
          normalizedRestaurantId
        );
        if (!updated) {
          throw new Error("Chamado n\xE3o encontrado ap\xF3s a atualiza\xE7\xE3o.");
        }
        try {
          await tableServiceCallEvents.updated(
            updated
          );
        } catch (error2) {
          console.error(
            "[WAITER_CALL_REALTIME_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
        }
        return updated;
      }
    };
    UpdateTableServiceCallStatusService_default = new UpdateTableServiceCallStatusService();
  }
});

// src/modules/waiterCalls/controllers/UpdateTableServiceCallStatusController.ts
var UpdateTableServiceCallStatusController, UpdateTableServiceCallStatusController_default;
var init_UpdateTableServiceCallStatusController = __esm({
  "src/modules/waiterCalls/controllers/UpdateTableServiceCallStatusController.ts"() {
    init_UpdateTableServiceCallStatusService();
    UpdateTableServiceCallStatusController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const call = await UpdateTableServiceCallStatusService_default.execute({
            id,
            restaurantId: req.user.restaurantId,
            actorUserId: req.user.id,
            actorRole: req.user.role,
            status: req.body?.status
          });
          return res.status(200).json(call);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel atualizar o chamado."
          });
        }
      }
    };
    UpdateTableServiceCallStatusController_default = new UpdateTableServiceCallStatusController();
  }
});

// src/modules/waiterCalls/services/DeleteTableServiceCallService.ts
var DeleteTableServiceCallService, DeleteTableServiceCallService_default;
var init_DeleteTableServiceCallService = __esm({
  "src/modules/waiterCalls/services/DeleteTableServiceCallService.ts"() {
    init_TableServiceCallRepository();
    DeleteTableServiceCallService = class {
      async execute({ id, restaurantId }) {
        const normalizedId = Number(id);
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedId) || normalizedId <= 0) {
          throw new Error("Chamado inv\xE1lido.");
        }
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante n\xE3o identificado para excluir o chamado.");
        }
        const current = await TableServiceCallRepository_default.findByIdForRestaurant(
          normalizedId,
          normalizedRestaurantId
        );
        if (!current) throw new Error("Chamado n\xE3o encontrado neste restaurante.");
        if (current.status !== "RESOLVED") {
          throw new Error("Somente chamados conclu\xEDdos podem ser exclu\xEDdos.");
        }
        const deleted = await TableServiceCallRepository_default.deleteResolved(
          normalizedId,
          normalizedRestaurantId
        );
        if (deleted !== 1) throw new Error("O chamado j\xE1 foi exclu\xEDdo. Atualize a tela.");
        return { id: normalizedId };
      }
    };
    DeleteTableServiceCallService_default = new DeleteTableServiceCallService();
  }
});

// src/modules/waiterCalls/controllers/DeleteTableServiceCallController.ts
var DeleteTableServiceCallController, DeleteTableServiceCallController_default;
var init_DeleteTableServiceCallController = __esm({
  "src/modules/waiterCalls/controllers/DeleteTableServiceCallController.ts"() {
    init_DeleteTableServiceCallService();
    DeleteTableServiceCallController = class {
      async handle(req, res) {
        try {
          const id = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
          const result = await DeleteTableServiceCallService_default.execute({
            id,
            restaurantId: req.user.restaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "N\xE3o foi poss\xEDvel excluir o chamado."
          });
        }
      }
    };
    DeleteTableServiceCallController_default = new DeleteTableServiceCallController();
  }
});

// src/modules/waiterCalls/routes/TableServiceCallRoutes.ts
import { Router as Router20 } from "express";
var router20, TableServiceCallRoutes_default;
var init_TableServiceCallRoutes = __esm({
  "src/modules/waiterCalls/routes/TableServiceCallRoutes.ts"() {
    init_authMiddleware();
    init_sessionMiddleware();
    init_waiterMiddleware();
    init_tableSessionRateLimitMiddleware();
    init_CreateTableServiceCallController();
    init_ListTableServiceCallsController();
    init_UpdateTableServiceCallStatusController();
    init_DeleteTableServiceCallController();
    router20 = Router20();
    router20.post(
      "/",
      tableServiceCallRateLimitMiddleware,
      sessionMiddleware,
      (req, res) => CreateTableServiceCallController_default.handle(req, res)
    );
    router20.get(
      "/",
      authMiddleware,
      waiterMiddleware,
      (req, res) => ListTableServiceCallsController_default.handle(req, res)
    );
    router20.patch(
      "/:id/status",
      authMiddleware,
      waiterMiddleware,
      (req, res) => UpdateTableServiceCallStatusController_default.handle(req, res)
    );
    router20.delete(
      "/:id",
      authMiddleware,
      waiterMiddleware,
      (req, res) => DeleteTableServiceCallController_default.handle(req, res)
    );
    TableServiceCallRoutes_default = router20;
  }
});

// src/modules/tableAccount/repositories/TablePaymentRepository.ts
import { TableParticipantStatus as TableParticipantStatus3, TableSessionStatus as TableSessionStatus9 } from "@prisma/client";
var tablePaymentIntentDtoSelect, tablePaymentIntentAdminSelect, TablePaymentRepository, TablePaymentRepository_default;
var init_TablePaymentRepository = __esm({
  "src/modules/tableAccount/repositories/TablePaymentRepository.ts"() {
    init_prisma();
    tablePaymentIntentDtoSelect = {
      id: true,
      publicId: true,
      restaurantId: true,
      tableSessionId: true,
      payerParticipantId: true,
      selectionMode: true,
      method: true,
      status: true,
      splitCount: true,
      idempotencyKeyHash: true,
      requestFingerprint: true,
      subtotalCents: true,
      serviceFeeCents: true,
      totalCents: true,
      provider: true,
      providerExternalId: true,
      providerCheckoutUrl: true,
      expiresAt: true,
      processingAt: true,
      paidAt: true,
      failedAt: true,
      canceledAt: true,
      refundedAt: true,
      failureCode: true,
      manualConfirmedById: true,
      manualConfirmedAt: true,
      createdAt: true,
      updatedAt: true,
      payerParticipant: {
        select: {
          publicId: true
        }
      },
      tableSession: {
        select: {
          publicId: true
        }
      },
      allocations: {
        select: {
          amountCents: true,
          tableBillItem: {
            select: {
              publicId: true
            }
          }
        },
        orderBy: { id: "asc" }
      }
    };
    tablePaymentIntentAdminSelect = {
      ...tablePaymentIntentDtoSelect,
      manualConfirmedBy: {
        select: {
          name: true
        }
      },
      events: {
        select: {
          type: true,
          fromStatus: true,
          toStatus: true,
          provider: true,
          providerEventId: true,
          amountCents: true,
          metadata: true,
          occurredAt: true,
          actorUser: {
            select: {
              name: true
            }
          }
        },
        orderBy: [{ occurredAt: "asc" }, { id: "asc" }]
      }
    };
    TablePaymentRepository = class {
      async findSessionParticipantForPayment(tableSessionId, restaurantId, participantId, now, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            id: tableSessionId,
            restaurantId,
            status: { in: [TableSessionStatus9.OPEN, TableSessionStatus9.CLOSING_REQUESTED] },
            OR: [{ expiresAt: null }, { expiresAt: { gt: now } }],
            participants: {
              some: {
                id: participantId,
                restaurantId,
                status: TableParticipantStatus3.ACTIVE,
                revokedAt: null
              }
            }
          },
          select: {
            id: true,
            publicId: true,
            restaurantId: true,
            status: true
          }
        });
      }
      async findByIdempotencyHash(restaurantId, tableSessionId, idempotencyKeyHash, db = prisma_default) {
        return db.tablePaymentIntent.findUnique({
          where: {
            restaurantId_tableSessionId_idempotencyKeyHash: {
              restaurantId,
              tableSessionId,
              idempotencyKeyHash
            }
          },
          select: tablePaymentIntentDtoSelect
        });
      }
      async findOwnedByPublicId(publicId, restaurantId, tableSessionId, participantId, db = prisma_default) {
        return db.tablePaymentIntent.findFirst({
          where: {
            publicId,
            restaurantId,
            tableSessionId,
            payerParticipantId: participantId
          },
          select: tablePaymentIntentDtoSelect
        });
      }
      async findForStaffByPublicId(publicId, restaurantId, db = prisma_default) {
        return db.tablePaymentIntent.findFirst({
          where: { publicId, restaurantId },
          select: tablePaymentIntentDtoSelect
        });
      }
    };
    TablePaymentRepository_default = new TablePaymentRepository();
  }
});

// src/modules/tableAccount/repositories/TableAccountRepository.ts
import { TableParticipantStatus as TableParticipantStatus4, TableSessionStatus as TableSessionStatus10 } from "@prisma/client";
var activeSessionStatuses, tableAccountSnapshotSelect, TableAccountRepository, TableAccountRepository_default;
var init_TableAccountRepository = __esm({
  "src/modules/tableAccount/repositories/TableAccountRepository.ts"() {
    init_prisma();
    init_TablePaymentRepository();
    activeSessionStatuses = [
      TableSessionStatus10.OPEN,
      TableSessionStatus10.CLOSING_REQUESTED
    ];
    tableAccountSnapshotSelect = {
      id: true,
      publicId: true,
      restaurantId: true,
      status: true,
      table: { select: { number: true } },
      participants: {
        select: {
          publicId: true,
          displayName: true,
          status: true,
          joinedAt: true,
          leftAt: true
        },
        orderBy: [{ joinedAt: "asc" }, { id: "asc" }]
      },
      billItems: {
        select: {
          publicId: true,
          productName: true,
          unitIndex: true,
          unitPriceCents: true,
          financialStatus: true,
          canceledAt: true,
          order: {
            select: {
              publicId: true,
              status: true,
              paid: true,
              refundStatus: true
            }
          },
          participant: {
            select: {
              publicId: true,
              displayName: true
            }
          },
          paymentAllocations: {
            select: {
              amountCents: true,
              paymentIntent: {
                select: {
                  status: true,
                  expiresAt: true
                }
              }
            }
          }
        },
        orderBy: [{ createdAt: "asc" }, { orderItemId: "asc" }, { unitIndex: "asc" }]
      },
      paymentIntents: {
        select: tablePaymentIntentAdminSelect,
        orderBy: [{ createdAt: "desc" }, { id: "desc" }]
      }
    };
    TableAccountRepository = class {
      async findSessionContextByPublicId(publicId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            publicId,
            status: { in: [...activeSessionStatuses] }
          },
          select: {
            id: true,
            publicId: true,
            tableId: true,
            restaurantId: true,
            status: true,
            expiresAt: true
          }
        });
      }
      async findSnapshotData(tableSessionId, restaurantId, currentParticipantId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            id: tableSessionId,
            restaurantId,
            status: { in: [...activeSessionStatuses] },
            participants: {
              some: {
                id: currentParticipantId,
                restaurantId,
                status: TableParticipantStatus4.ACTIVE,
                revokedAt: null
              }
            }
          },
          select: tableAccountSnapshotSelect
        });
      }
      async findAdminSnapshotData(sessionPublicId, restaurantId, db = prisma_default) {
        return db.tableSession.findFirst({
          where: {
            publicId: sessionPublicId,
            restaurantId
          },
          select: tableAccountSnapshotSelect
        });
      }
    };
    TableAccountRepository_default = new TableAccountRepository();
  }
});

// src/middlewares/tableAccountSessionMiddleware.ts
import { z as z18 } from "zod";
async function tableAccountSessionMiddleware(req, res, next) {
  try {
    const parsedPublicId = sessionPublicIdSchema.safeParse(req.params.sessionPublicId);
    if (!parsedPublicId.success) {
      return res.status(404).json({
        error: "Conta da mesa n\xE3o encontrada.",
        code: "TABLE_ACCOUNT_NOT_FOUND"
      });
    }
    const session = await TableAccountRepository_default.findSessionContextByPublicId(parsedPublicId.data);
    if (!session) {
      return res.status(404).json({
        error: "Conta da mesa n\xE3o encontrada ou j\xE1 encerrada.",
        code: "TABLE_ACCOUNT_NOT_FOUND"
      });
    }
    if (session.expiresAt && session.expiresAt.getTime() <= Date.now()) {
      return res.status(403).json({
        error: "A sess\xE3o desta mesa expirou. Pe\xE7a ao gar\xE7om para abrir um novo atendimento.",
        code: "TABLE_ACCOUNT_EXPIRED"
      });
    }
    req.tableSession = {
      id: session.id,
      publicId: session.publicId,
      tableId: session.tableId,
      restaurantId: session.restaurantId,
      status: session.status
    };
    return next();
  } catch (error2) {
    return res.status(500).json({
      error: "N\xE3o foi poss\xEDvel validar a conta desta mesa.",
      ...process.env.NODE_ENV === "development" && error2 instanceof Error ? { detail: error2.message } : {}
    });
  }
}
var sessionPublicIdSchema;
var init_tableAccountSessionMiddleware = __esm({
  "src/middlewares/tableAccountSessionMiddleware.ts"() {
    init_TableAccountRepository();
    sessionPublicIdSchema = z18.string().uuid();
  }
});

// src/modules/tableAccount/services/GetCurrentTableAccountService.ts
import { OrderRefundStatus as OrderRefundStatus7, OrderStatus as OrderStatus20 } from "@prisma/client";
function toSafeMoneyCents(value, fieldName) {
  return assertMoneyCents(Number(value), fieldName);
}
function resolveFinancialStatus(item) {
  if (item.order.refundStatus === OrderRefundStatus7.SUCCEEDED) {
    return "REFUNDED";
  }
  if (item.order.paid) {
    return "PAID";
  }
  return item.financialStatus;
}
function buildTableAccountBaseSnapshot(data, now = /* @__PURE__ */ new Date()) {
  const paymentIntents = data.paymentIntents || [];
  const normalizedItems = data.billItems.map((item) => {
    const unitPriceCents = toSafeMoneyCents(item.unitPriceCents, `valor do item ${item.publicId}`);
    const orderStatus = operationalStatusMap[item.order.status];
    const financialStatus = resolveFinancialStatus(item);
    const canceled = Boolean(item.canceledAt) || orderStatus === "CANCELED";
    const paymentAllocations = item.paymentAllocations || [];
    const ledger = calculateTableBillItemLedger({
      unitPriceCents,
      projectedStatus: financialStatus,
      canceled,
      allocations: paymentAllocations.map((allocation) => ({
        amountCents: toSafeMoneyCents(allocation.amountCents, `aloca\xE7\xE3o do item ${item.publicId}`),
        intentStatus: allocation.paymentIntent.status,
        expiresAt: allocation.paymentIntent.expiresAt
      }))
    });
    return {
      canceled,
      hasAllocations: paymentAllocations.length > 0,
      unitPriceCents,
      financialStatus: ledger.projectedStatus,
      ledger,
      dto: {
        publicId: item.publicId,
        orderPublicId: item.order.publicId,
        productName: item.productName,
        unitIndex: item.unitIndex,
        unitPriceCents,
        paidCents: ledger.paidCents,
        reservedCents: ledger.reservedCents,
        processingCents: ledger.processingCents,
        availableCents: ledger.availableCents,
        financialStatus: ledger.projectedStatus,
        orderStatus,
        orderedByParticipantPublicId: item.participant.publicId,
        orderedByDisplayName: item.participant.displayName?.trim() || "Cliente da mesa"
      }
    };
  });
  const consumedCents = sumMoneyCents(
    normalizedItems.filter((item) => !item.canceled && item.financialStatus !== "REFUNDED").map((item) => item.unitPriceCents)
  );
  const legacyGrossPaidCents = sumMoneyCents(
    normalizedItems.filter((item) => !item.hasAllocations && ["PAID", "REFUNDED"].includes(item.financialStatus)).map((item) => item.unitPriceCents)
  );
  const legacyRefundedCents = sumMoneyCents(
    normalizedItems.filter((item) => !item.hasAllocations && item.financialStatus === "REFUNDED").map((item) => item.unitPriceCents)
  );
  const grossPaidCents = sumMoneyCents([
    legacyGrossPaidCents,
    ...paymentIntents.filter((payment) => ["PAID", "REFUNDED"].includes(payment.status)).map((payment) => toSafeMoneyCents(payment.totalCents, `pagamento ${payment.publicId}`))
  ]);
  const refundedCents = sumMoneyCents([
    legacyRefundedCents,
    ...paymentIntents.filter((payment) => payment.status === "REFUNDED").map((payment) => toSafeMoneyCents(payment.totalCents, `estorno ${payment.publicId}`))
  ]);
  const serviceFeeCents = sumMoneyCents(
    paymentIntents.filter((payment) => payment.status === "PAID").map((payment) => toSafeMoneyCents(payment.serviceFeeCents, `taxa ${payment.publicId}`))
  );
  const reservedCents = sumMoneyCents([
    ...normalizedItems.filter((item) => !item.canceled).map((item) => item.ledger.reservedCents),
    ...paymentIntents.filter((payment) => payment.status === "RESERVED" && payment.expiresAt > now).map(
      (payment) => toSafeMoneyCents(payment.serviceFeeCents, `taxa reservada ${payment.publicId}`)
    )
  ]);
  const processingCents = sumMoneyCents([
    ...normalizedItems.filter((item) => !item.canceled).map((item) => item.ledger.processingCents),
    ...paymentIntents.filter((payment) => payment.status === "PROCESSING" && payment.expiresAt > now).map(
      (payment) => toSafeMoneyCents(payment.serviceFeeCents, `taxa em processamento ${payment.publicId}`)
    )
  ]);
  const balance = calculateTableAccountBalance({
    consumedCents,
    serviceFeeCents,
    grossPaidCents,
    refundedCents
  });
  return {
    contractVersion: TABLE_ACCOUNT_CONTRACT_VERSION,
    summary: {
      sessionPublicId: data.publicId,
      tableNumber: data.table.number,
      status: data.status,
      consumedCents,
      serviceFeeCents,
      grossPaidCents,
      refundedCents,
      netPaidCents: balance.netPaidCents,
      reservedCents,
      processingCents,
      remainingCents: balance.remainingCents,
      overpaidCents: balance.overpaidCents,
      participantsCount: data.participants.filter((participant) => participant.status === "ACTIVE").length
    },
    participants: data.participants.map((participant) => ({
      publicId: participant.publicId,
      displayName: participant.displayName,
      status: participant.status,
      joinedAt: participant.joinedAt.toISOString(),
      leftAt: participant.leftAt?.toISOString() || null
    })),
    items: normalizedItems.map((item) => item.dto)
  };
}
var operationalStatusMap, TableAccountAccessError, GetCurrentTableAccountService, GetCurrentTableAccountService_default;
var init_GetCurrentTableAccountService = __esm({
  "src/modules/tableAccount/services/GetCurrentTableAccountService.ts"() {
    init_tableAccountContracts();
    init_tableAccountRules();
    init_TableAccountRepository();
    init_TableAccountSettingsRepository();
    init_tablePaymentAllocation();
    operationalStatusMap = {
      [OrderStatus20.PENDENTE]: "PENDING",
      [OrderStatus20.PREPARANDO]: "PREPARING",
      [OrderStatus20.PRONTO]: "READY",
      [OrderStatus20.SAIU_PARA_ENTREGA]: "READY",
      [OrderStatus20.ENTREGUE]: "DELIVERED",
      [OrderStatus20.CANCELADO]: "CANCELED"
    };
    TableAccountAccessError = class extends Error {
      constructor(message = "Conta da mesa n\xE3o encontrada.") {
        super(message);
        this.name = "TableAccountAccessError";
      }
    };
    GetCurrentTableAccountService = class {
      async execute(input) {
        const tableSessionId = Number(input.tableSessionId);
        const restaurantId = Number(input.restaurantId);
        const participantId = Number(input.participantId);
        if (!Number.isSafeInteger(tableSessionId) || tableSessionId <= 0 || !Number.isSafeInteger(restaurantId) || restaurantId <= 0 || !Number.isSafeInteger(participantId) || participantId <= 0) {
          throw new TableAccountAccessError();
        }
        const [data, settings] = await Promise.all([
          TableAccountRepository_default.findSnapshotData(tableSessionId, restaurantId, participantId),
          TableAccountSettingsRepository_default.findByRestaurantId(restaurantId)
        ]);
        if (!data) {
          throw new TableAccountAccessError();
        }
        const now = /* @__PURE__ */ new Date();
        const paymentIntents = data.paymentIntents || [];
        return {
          ...buildTableAccountBaseSnapshot(data, now),
          currentParticipantPublicId: input.participantPublicId,
          capabilities: {
            enabled: settings.enabled,
            allowCash: settings.allowCash,
            allowCardMachine: settings.allowCardMachine,
            allowOnlinePayment: settings.allowOnlinePayment,
            allowSplit: settings.allowSplit,
            serviceFeeMode: settings.serviceFeeMode,
            serviceFeeBasisPoints: settings.serviceFeeBasisPoints,
            reservationTimeoutMinutes: settings.reservationTimeoutMinutes
          },
          payments: paymentIntents.map((payment) => ({
            publicId: payment.publicId,
            payerParticipantPublicId: payment.payerParticipant.publicId,
            selectionMode: payment.selectionMode,
            status: ["RESERVED", "PROCESSING"].includes(payment.status) && payment.expiresAt <= now ? "EXPIRED" : payment.status,
            totalCents: toSafeMoneyCents(payment.totalCents, `pagamento ${payment.publicId}`),
            createdAt: payment.createdAt.toISOString()
          }))
        };
      }
    };
    GetCurrentTableAccountService_default = new GetCurrentTableAccountService();
  }
});

// src/modules/tableAccount/controllers/GetCurrentTableAccountController.ts
var GetCurrentTableAccountController, GetCurrentTableAccountController_default;
var init_GetCurrentTableAccountController = __esm({
  "src/modules/tableAccount/controllers/GetCurrentTableAccountController.ts"() {
    init_GetCurrentTableAccountService();
    GetCurrentTableAccountController = class {
      async handle(req, res) {
        try {
          const account = await GetCurrentTableAccountService_default.execute({
            tableSessionId: req.tableSession.id,
            restaurantId: req.tableSession.restaurantId,
            participantId: req.tableParticipant.id,
            participantPublicId: req.tableParticipant.publicId
          });
          res.setHeader("Cache-Control", "no-store");
          return res.status(200).json(account);
        } catch (error2) {
          const expected = error2 instanceof TableAccountAccessError;
          return res.status(expected ? 404 : 500).json({
            error: expected ? error2.message : "N\xE3o foi poss\xEDvel carregar a conta desta mesa."
          });
        }
      }
    };
    GetCurrentTableAccountController_default = new GetCurrentTableAccountController();
  }
});

// src/modules/tableAccount/domain/tablePaymentPlan.ts
function buildTablePaymentPlan(input) {
  const eligibleItems = input.items.filter(
    (item) => !item.canceled && item.projectedStatus !== "REFUNDED" && item.availableCents > 0
  );
  const activeBlockers = input.items.filter(
    (item) => !item.canceled && (item.reservedCents > 0 || item.processingCents > 0)
  );
  let selected;
  if (input.payment.selectionMode === "SELECTED_ITEMS") {
    const requestedIds = new Set(input.payment.billItemPublicIds || []);
    selected = eligibleItems.filter((item) => requestedIds.has(item.publicId));
    const fullyAvailable = selected.every(
      (item) => item.availableCents === item.unitPriceCents - item.paidCents
    );
    if (selected.length !== requestedIds.size || !fullyAvailable) {
      throw new TablePaymentPlanError(
        "Um ou mais itens j\xE1 foram pagos, reservados ou n\xE3o pertencem a esta mesa.",
        "BILL_ITEM_UNAVAILABLE"
      );
    }
  } else if (input.payment.selectionMode === "MY_ITEMS") {
    if (activeBlockers.some((item) => item.participantId === input.participantId)) {
      throw new TablePaymentPlanError(
        "Seus itens j\xE1 possuem um pagamento em andamento. Aguarde ou atualize a conta.",
        "PARTICIPANT_BALANCE_RESERVED"
      );
    }
    selected = eligibleItems.filter((item) => item.participantId === input.participantId);
  } else {
    if (activeBlockers.length > 0) {
      throw new TablePaymentPlanError(
        "J\xE1 existe outro pagamento em andamento nesta mesa. Aguarde ou atualize a conta.",
        "TABLE_BALANCE_RESERVED"
      );
    }
    selected = eligibleItems;
  }
  const selectedAvailableCents = sumAvailableTableBillItemCents(selected);
  if (selectedAvailableCents <= 0) {
    throw new TablePaymentPlanError(
      "N\xE3o h\xE1 saldo dispon\xEDvel para esta forma de pagamento.",
      "NO_AVAILABLE_BALANCE"
    );
  }
  const subtotalCents = input.payment.selectionMode === "EQUAL_SPLIT" ? splitCentsEqually(selectedAvailableCents, Number(input.payment.splitCount))[0] : selectedAvailableCents;
  return {
    subtotalCents,
    allocations: allocateCentsAcrossTableBillItems(selected, subtotalCents),
    selectedItemPublicIds: selected.map((item) => item.publicId)
  };
}
var TablePaymentPlanError;
var init_tablePaymentPlan = __esm({
  "src/modules/tableAccount/domain/tablePaymentPlan.ts"() {
    init_tableAccountRules();
    init_tablePaymentAllocation();
    TablePaymentPlanError = class extends Error {
      constructor(message, code) {
        super(message);
        this.code = code;
        this.name = "TablePaymentPlanError";
      }
      code;
    };
  }
});

// src/modules/tableAccount/providers/FakePaymentProvider.ts
import { timingSafeEqual } from "crypto";
function safeSecretMatches(received, expected) {
  const receivedBuffer = Buffer.from(received);
  const expectedBuffer = Buffer.from(expected);
  return receivedBuffer.length === expectedBuffer.length && timingSafeEqual(receivedBuffer, expectedBuffer);
}
var WEBHOOK_STATUSES, FakePaymentProvider, FakePaymentProvider_default;
var init_FakePaymentProvider = __esm({
  "src/modules/tableAccount/providers/FakePaymentProvider.ts"() {
    init_tableAccountRules();
    WEBHOOK_STATUSES = /* @__PURE__ */ new Set([
      "PENDING",
      "PAID",
      "FAILED",
      "EXPIRED",
      "CANCELED",
      "REFUNDED"
    ]);
    FakePaymentProvider = class {
      constructor(options = {}) {
        this.options = options;
      }
      options;
      code = "FAKE_TABLE";
      payments = /* @__PURE__ */ new Map();
      assertEnabled() {
        const enabled = this.options.enabled ?? (process.env.NODE_ENV === "test" || String(process.env.NODE_ENV) !== "production");
        if (!enabled || process.env.NODE_ENV === "production") {
          throw new Error("O provedor de pagamento simulado est\xE1 desativado neste ambiente.");
        }
      }
      async createPayment(input) {
        this.assertEnabled();
        const amountCents = assertMoneyCents(input.amountCents, "valor do pagamento");
        if (amountCents === 0) {
          throw new Error("O pagamento deve possuir valor maior que zero.");
        }
        const externalId = `fake-table:${input.intentPublicId}`;
        const existing = this.payments.get(externalId);
        if (existing) {
          if (existing.amountCents !== amountCents) {
            throw new Error("A chave do pagamento simulado j\xE1 foi usada com outro valor.");
          }
          return { ...existing };
        }
        const payment = {
          externalId,
          status: "PENDING",
          amountCents,
          checkoutUrl: `/pagamentos/mesa/simulado/${encodeURIComponent(externalId)}`,
          expiresAt: new Date(input.expiresAt)
        };
        this.payments.set(externalId, payment);
        return { ...payment };
      }
      async getPayment(externalId) {
        this.assertEnabled();
        const payment = this.payments.get(externalId);
        if (!payment) {
          throw new Error("Pagamento simulado n\xE3o encontrado.");
        }
        return { ...payment };
      }
      async cancelPayment(input) {
        return this.changeStatus(input.externalId, "CANCELED");
      }
      async refundPayment(input) {
        return this.changeStatus(input.externalId, "REFUNDED");
      }
      async changeStatus(externalId, status) {
        this.assertEnabled();
        const current = await this.getPayment(externalId);
        const updated = { ...current, status };
        this.payments.set(externalId, updated);
        return { ...updated };
      }
      async validateWebhook(input) {
        this.assertEnabled();
        const expectedSecret = String(
          this.options.webhookSecret || process.env.FAKE_TABLE_PAYMENT_WEBHOOK_SECRET || ""
        ).trim();
        const receivedSecret = String(input.headers["x-fake-table-payment-secret"] || "").trim();
        if (!expectedSecret || !safeSecretMatches(receivedSecret, expectedSecret)) {
          throw new Error("Assinatura do webhook simulado inv\xE1lida.");
        }
        const body = input.body || {};
        const eventId = String(body.eventId || "").trim();
        const externalId = String(body.externalId || "").trim();
        const status = String(body.status || "").trim().toUpperCase();
        const amountCents = Number(body.amountCents);
        const occurredAt = new Date(String(body.occurredAt || ""));
        if (!eventId || !externalId || !WEBHOOK_STATUSES.has(status) || !Number.isSafeInteger(amountCents) || amountCents < 0 || !Number.isFinite(occurredAt.getTime())) {
          throw new Error("Evento do webhook simulado inv\xE1lido.");
        }
        const payment = await this.getPayment(externalId);
        if (payment.amountCents !== amountCents) {
          throw new Error("O valor do evento n\xE3o corresponde ao pagamento simulado.");
        }
        this.payments.set(externalId, { ...payment, status });
        return { eventId, externalId, status, amountCents, occurredAt };
      }
    };
    FakePaymentProvider_default = new FakePaymentProvider();
  }
});

// src/modules/tableAccount/services/tablePaymentSupport.ts
import { createHash } from "crypto";
function sha256(value) {
  return createHash("sha256").update(value).digest("hex");
}
function buildTablePaymentRequestFingerprint(participantId, input) {
  return sha256(
    JSON.stringify({
      participantId,
      selectionMode: input.selectionMode,
      method: input.method,
      billItemPublicIds: [...input.billItemPublicIds || []].sort(),
      splitCount: input.splitCount || null,
      includeOptionalServiceFee: input.includeOptionalServiceFee
    })
  );
}
function serializeTablePaymentIntent(record, sessionPublicId) {
  return {
    publicId: record.publicId,
    sessionPublicId,
    payerParticipantPublicId: record.payerParticipant.publicId,
    selectionMode: record.selectionMode,
    method: record.method,
    status: record.status,
    billItemPublicIds: record.allocations.map((allocation) => allocation.tableBillItem.publicId),
    subtotalCents: bigintToMoneyCents(
      record.subtotalCents,
      `subtotal do pagamento ${record.publicId}`
    ),
    serviceFeeCents: bigintToMoneyCents(
      record.serviceFeeCents,
      `taxa do pagamento ${record.publicId}`
    ),
    totalCents: bigintToMoneyCents(record.totalCents, `total do pagamento ${record.publicId}`),
    provider: record.provider,
    externalId: record.providerExternalId,
    checkoutUrl: record.providerCheckoutUrl,
    expiresAt: record.expiresAt.toISOString(),
    createdAt: record.createdAt.toISOString(),
    updatedAt: record.updatedAt.toISOString()
  };
}
function readAuditReason(metadata) {
  if (!metadata || typeof metadata !== "object" || Array.isArray(metadata)) {
    return null;
  }
  const reason = metadata.reason;
  return typeof reason === "string" ? reason : null;
}
function serializeTablePaymentIntentForAdmin(record, sessionPublicId) {
  return {
    ...serializeTablePaymentIntent(record, sessionPublicId),
    manualConfirmedAt: record.manualConfirmedAt?.toISOString() || null,
    manualConfirmedByName: record.manualConfirmedBy?.name || null,
    events: record.events.map((event) => ({
      type: event.type,
      fromStatus: event.fromStatus,
      toStatus: event.toStatus,
      provider: event.provider,
      providerEventId: event.providerEventId,
      amountCents: event.amountCents === null ? null : bigintToMoneyCents(event.amountCents, `evento do pagamento ${record.publicId}`),
      actorName: event.actorUser?.name || null,
      reason: readAuditReason(event.metadata),
      occurredAt: event.occurredAt.toISOString()
    }))
  };
}
var TablePaymentError;
var init_tablePaymentSupport = __esm({
  "src/modules/tableAccount/services/tablePaymentSupport.ts"() {
    init_tablePaymentLedger();
    TablePaymentError = class extends Error {
      constructor(message, statusCode = 400, code = "TABLE_PAYMENT_ERROR") {
        super(message);
        this.statusCode = statusCode;
        this.code = code;
        this.name = "TablePaymentError";
      }
      statusCode;
      code;
    };
  }
});

// src/modules/tableAccount/services/CreateTablePaymentIntentService.ts
import {
  Prisma as Prisma11,
  TablePaymentEventType as TablePaymentEventType3,
  TablePaymentIntentStatus as TablePaymentIntentStatus3,
  TablePaymentMethod
} from "@prisma/client";
var CreateTablePaymentIntentService, CreateTablePaymentIntentService_default;
var init_CreateTablePaymentIntentService = __esm({
  "src/modules/tableAccount/services/CreateTablePaymentIntentService.ts"() {
    init_prisma();
    init_tableAccountSchemas();
    init_tableAccountRules();
    init_tablePaymentPlan();
    init_FakePaymentProvider();
    init_TablePaymentRepository();
    init_TableAccountSettingsRepository();
    init_tablePaymentLedger();
    init_tablePaymentSupport();
    CreateTablePaymentIntentService = class {
      constructor(provider = FakePaymentProvider_default, now = () => /* @__PURE__ */ new Date()) {
        this.provider = provider;
        this.now = now;
      }
      provider;
      now;
      async execute(context, rawInput) {
        const input = createTablePaymentIntentInputSchema.parse(rawInput);
        const now = this.now();
        const idempotencyKeyHash = sha256(input.idempotencyKey);
        const requestFingerprint = buildTablePaymentRequestFingerprint(
          context.participantId,
          input
        );
        const reservation = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, context.restaurantId, context.tableSessionId);
            await expireTablePaymentReservations(
              tx,
              context.restaurantId,
              context.tableSessionId,
              now
            );
            const session = await TablePaymentRepository_default.findSessionParticipantForPayment(
              context.tableSessionId,
              context.restaurantId,
              context.participantId,
              now,
              tx
            );
            if (!session || session.publicId !== context.sessionPublicId) {
              throw new TablePaymentError(
                "Sua participa\xE7\xE3o nesta mesa n\xE3o est\xE1 mais ativa.",
                403,
                "TABLE_PARTICIPANT_INACTIVE"
              );
            }
            const settings = await TableAccountSettingsRepository_default.findByRestaurantId(
              context.restaurantId,
              tx
            );
            if (!settings.enabled) {
              throw new TablePaymentError(
                "A conta e o pagamento por mesa n\xE3o est\xE3o habilitados neste restaurante.",
                409,
                "TABLE_ACCOUNT_DISABLED"
              );
            }
            const onlineMethod2 = input.method === TablePaymentMethod.PIX || input.method === TablePaymentMethod.CARD;
            if (onlineMethod2 && !settings.allowOnlinePayment) {
              throw new TablePaymentError(
                "O pagamento online da conta da mesa est\xE1 desativado.",
                409,
                "ONLINE_TABLE_PAYMENT_DISABLED"
              );
            }
            if (input.method === TablePaymentMethod.CASH && !settings.allowCash) {
              throw new TablePaymentError(
                "O pagamento em dinheiro n\xE3o est\xE1 dispon\xEDvel para esta mesa.",
                409,
                "CASH_TABLE_PAYMENT_DISABLED"
              );
            }
            if (input.method === TablePaymentMethod.CARD_MACHINE && !settings.allowCardMachine) {
              throw new TablePaymentError(
                "O pagamento na maquininha n\xE3o est\xE1 dispon\xEDvel para esta mesa.",
                409,
                "CARD_MACHINE_TABLE_PAYMENT_DISABLED"
              );
            }
            if (input.selectionMode === "EQUAL_SPLIT" && !settings.allowSplit) {
              throw new TablePaymentError(
                "A divis\xE3o igual da conta est\xE1 desativada neste restaurante.",
                409,
                "TABLE_SPLIT_DISABLED"
              );
            }
            const existing = await TablePaymentRepository_default.findByIdempotencyHash(
              context.restaurantId,
              context.tableSessionId,
              idempotencyKeyHash,
              tx
            );
            if (existing) {
              if (existing.payerParticipantId !== context.participantId || existing.requestFingerprint !== requestFingerprint) {
                throw new TablePaymentError(
                  "Esta chave de idempot\xEAncia j\xE1 foi usada em outro pagamento.",
                  409,
                  "IDEMPOTENCY_KEY_REUSED"
                );
              }
              return { intent: existing, reused: true };
            }
            const ledgerItems = await loadTablePaymentLedgerItems(
              tx,
              context.restaurantId,
              context.tableSessionId,
              now
            );
            let plan;
            try {
              plan = buildTablePaymentPlan({
                payment: input,
                participantId: context.participantId,
                items: ledgerItems
              });
            } catch (error2) {
              if (error2 instanceof TablePaymentPlanError) {
                throw new TablePaymentError(error2.message, 409, error2.code);
              }
              throw error2;
            }
            const subtotalCents = plan.subtotalCents;
            const serviceFeeCents = shouldIncludeServiceFee(
              settings.serviceFeeMode,
              input.includeOptionalServiceFee
            ) ? calculateServiceFeeCents(subtotalCents, settings.serviceFeeBasisPoints) : 0;
            const totalCents = sumMoneyCents([subtotalCents, serviceFeeCents]);
            const allocationSeeds = plan.allocations;
            const expiresAt = new Date(now.getTime() + settings.reservationTimeoutMinutes * 6e4);
            const created = await tx.tablePaymentIntent.create({
              data: {
                restaurantId: context.restaurantId,
                tableSessionId: context.tableSessionId,
                payerParticipantId: context.participantId,
                selectionMode: input.selectionMode,
                method: input.method,
                status: TablePaymentIntentStatus3.RESERVED,
                splitCount: input.splitCount || null,
                idempotencyKeyHash,
                requestFingerprint,
                subtotalCents: BigInt(subtotalCents),
                serviceFeeCents: BigInt(serviceFeeCents),
                totalCents: BigInt(totalCents),
                expiresAt
              },
              select: {
                id: true,
                publicId: true
              }
            });
            await tx.tablePaymentAllocation.createMany({
              data: allocationSeeds.map((allocation) => ({
                restaurantId: context.restaurantId,
                tableSessionId: context.tableSessionId,
                paymentIntentId: created.id,
                tableBillItemId: allocation.tableBillItemId,
                amountCents: BigInt(allocation.amountCents)
              }))
            });
            await tx.tablePaymentEvent.create({
              data: {
                restaurantId: context.restaurantId,
                tableSessionId: context.tableSessionId,
                paymentIntentId: created.id,
                deduplicationKey: `table-payment:${created.publicId}:created`,
                type: TablePaymentEventType3.CREATED,
                fromStatus: null,
                toStatus: TablePaymentIntentStatus3.RESERVED,
                amountCents: BigInt(totalCents),
                occurredAt: now
              }
            });
            await projectTableSessionFinancialState(
              tx,
              context.restaurantId,
              context.tableSessionId,
              now
            );
            const intent = await tx.tablePaymentIntent.findUniqueOrThrow({
              where: { id: created.id },
              select: tablePaymentIntentDtoSelect
            });
            return { intent, reused: false };
          },
          { isolationLevel: Prisma11.TransactionIsolationLevel.Serializable }
        );
        const onlineMethod = reservation.intent.method === TablePaymentMethod.PIX || reservation.intent.method === TablePaymentMethod.CARD;
        if (!onlineMethod || reservation.intent.status !== TablePaymentIntentStatus3.RESERVED || reservation.intent.providerExternalId) {
          return {
            payment: serializeTablePaymentIntent(reservation.intent, context.sessionPublicId),
            idempotentReplay: reservation.reused
          };
        }
        return this.createProviderPayment(context, reservation.intent, reservation.reused);
      }
      async createProviderPayment(context, intent, reused) {
        try {
          const payment = await this.provider.createPayment({
            intentPublicId: intent.publicId,
            amountCents: Number(intent.totalCents),
            method: intent.method,
            idempotencyKeyHash: intent.idempotencyKeyHash,
            expiresAt: intent.expiresAt
          });
          const updated = await prisma_default.$transaction(
            async (tx) => {
              await lockTablePaymentSession(tx, context.restaurantId, context.tableSessionId);
              const changed = await tx.tablePaymentIntent.updateMany({
                where: {
                  id: intent.id,
                  restaurantId: context.restaurantId,
                  tableSessionId: context.tableSessionId,
                  status: TablePaymentIntentStatus3.RESERVED
                },
                data: {
                  status: TablePaymentIntentStatus3.PROCESSING,
                  provider: this.provider.code,
                  providerExternalId: payment.externalId,
                  providerCheckoutUrl: payment.checkoutUrl,
                  processingAt: this.now()
                }
              });
              if (changed.count === 1) {
                await tx.tablePaymentEvent.create({
                  data: {
                    restaurantId: context.restaurantId,
                    tableSessionId: context.tableSessionId,
                    paymentIntentId: intent.id,
                    deduplicationKey: `table-payment:${intent.publicId}:processing`,
                    type: TablePaymentEventType3.PROCESSING,
                    fromStatus: TablePaymentIntentStatus3.RESERVED,
                    toStatus: TablePaymentIntentStatus3.PROCESSING,
                    provider: this.provider.code,
                    amountCents: intent.totalCents,
                    occurredAt: this.now()
                  }
                });
                await projectTableSessionFinancialState(
                  tx,
                  context.restaurantId,
                  context.tableSessionId,
                  this.now()
                );
              }
              return tx.tablePaymentIntent.findUniqueOrThrow({
                where: { id: intent.id },
                select: tablePaymentIntentDtoSelect
              });
            },
            { isolationLevel: Prisma11.TransactionIsolationLevel.Serializable }
          );
          return {
            payment: serializeTablePaymentIntent(updated, context.sessionPublicId),
            idempotentReplay: reused
          };
        } catch (error2) {
          await this.failProviderCreation(context, intent, error2);
          throw new TablePaymentError(
            "N\xE3o foi poss\xEDvel iniciar o pagamento online. A reserva foi liberada.",
            502,
            "PAYMENT_PROVIDER_UNAVAILABLE"
          );
        }
      }
      async failProviderCreation(context, intent, error2) {
        await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, context.restaurantId, context.tableSessionId);
            const changed = await tx.tablePaymentIntent.updateMany({
              where: {
                id: intent.id,
                restaurantId: context.restaurantId,
                tableSessionId: context.tableSessionId,
                status: TablePaymentIntentStatus3.RESERVED
              },
              data: {
                status: TablePaymentIntentStatus3.FAILED,
                failedAt: this.now(),
                failureCode: "PROVIDER_CREATE_FAILED"
              }
            });
            if (changed.count === 1) {
              await tx.tablePaymentEvent.create({
                data: {
                  restaurantId: context.restaurantId,
                  tableSessionId: context.tableSessionId,
                  paymentIntentId: intent.id,
                  deduplicationKey: `table-payment:${intent.publicId}:provider-create-failed`,
                  type: TablePaymentEventType3.FAILED,
                  fromStatus: TablePaymentIntentStatus3.RESERVED,
                  toStatus: TablePaymentIntentStatus3.FAILED,
                  provider: this.provider.code,
                  amountCents: intent.totalCents,
                  occurredAt: this.now(),
                  metadata: {
                    code: error2 instanceof Error ? error2.name : "UNKNOWN_ERROR"
                  }
                }
              });
              await projectTableSessionFinancialState(
                tx,
                context.restaurantId,
                context.tableSessionId,
                this.now()
              );
            }
          },
          { isolationLevel: Prisma11.TransactionIsolationLevel.Serializable }
        );
      }
    };
    CreateTablePaymentIntentService_default = new CreateTablePaymentIntentService();
  }
});

// src/modules/tableAccount/controllers/CreateTablePaymentIntentController.ts
import { ZodError as ZodError3 } from "zod";
var CreateTablePaymentIntentController, CreateTablePaymentIntentController_default;
var init_CreateTablePaymentIntentController = __esm({
  "src/modules/tableAccount/controllers/CreateTablePaymentIntentController.ts"() {
    init_CreateTablePaymentIntentService();
    init_tablePaymentSupport();
    CreateTablePaymentIntentController = class {
      async handle(req, res) {
        try {
          const result = await CreateTablePaymentIntentService_default.execute(
            {
              tableSessionId: Number(req.tableSession?.id),
              sessionPublicId: String(req.tableSession?.publicId || ""),
              restaurantId: Number(req.tableSession?.restaurantId),
              participantId: Number(req.tableParticipant?.id)
            },
            {
              ...req.body || {},
              idempotencyKey: String(req.header("idempotency-key") || "").trim() || req.body?.idempotencyKey
            }
          );
          res.setHeader("Cache-Control", "no-store");
          return res.status(result.idempotentReplay ? 200 : 201).json(result);
        } catch (error2) {
          if (error2 instanceof ZodError3) {
            return res.status(400).json({
              error: error2.issues[0]?.message || "Dados do pagamento inv\xE1lidos.",
              code: "INVALID_TABLE_PAYMENT_INPUT"
            });
          }
          if (error2 instanceof TablePaymentError) {
            return res.status(error2.statusCode).json({ error: error2.message, code: error2.code });
          }
          console.error(
            "[CREATE_TABLE_PAYMENT_INTENT_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(500).json({
            error: "N\xE3o foi poss\xEDvel iniciar o pagamento da mesa.",
            code: "TABLE_PAYMENT_INTERNAL_ERROR"
          });
        }
      }
    };
    CreateTablePaymentIntentController_default = new CreateTablePaymentIntentController();
  }
});

// src/modules/tableAccount/services/CancelTablePaymentIntentService.ts
import { Prisma as Prisma12, TablePaymentEventType as TablePaymentEventType4, TablePaymentIntentStatus as TablePaymentIntentStatus4 } from "@prisma/client";
var CancelTablePaymentIntentService, CancelTablePaymentIntentService_default;
var init_CancelTablePaymentIntentService = __esm({
  "src/modules/tableAccount/services/CancelTablePaymentIntentService.ts"() {
    init_prisma();
    init_FakePaymentProvider();
    init_TablePaymentRepository();
    init_tablePaymentLedger();
    init_tablePaymentSupport();
    CancelTablePaymentIntentService = class {
      constructor(provider = FakePaymentProvider_default, now = () => /* @__PURE__ */ new Date()) {
        this.provider = provider;
        this.now = now;
      }
      provider;
      now;
      async execute(input) {
        const now = this.now();
        const result = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, input.restaurantId, input.tableSessionId);
            await expireTablePaymentReservations(tx, input.restaurantId, input.tableSessionId, now);
            const intent = await TablePaymentRepository_default.findOwnedByPublicId(
              input.publicId,
              input.restaurantId,
              input.tableSessionId,
              input.participantId,
              tx
            );
            if (!intent) {
              throw new TablePaymentError(
                "Pagamento n\xE3o encontrado nesta participa\xE7\xE3o.",
                404,
                "TABLE_PAYMENT_NOT_FOUND"
              );
            }
            if (intent.status === TablePaymentIntentStatus4.CANCELED) {
              return { intent, previousStatus: intent.status, changed: false };
            }
            if (intent.status !== TablePaymentIntentStatus4.RESERVED && intent.status !== TablePaymentIntentStatus4.PROCESSING) {
              throw new TablePaymentError(
                "Este pagamento n\xE3o pode mais ser cancelado pelo cliente.",
                409,
                "TABLE_PAYMENT_NOT_CANCELABLE"
              );
            }
            const changed = await tx.tablePaymentIntent.updateMany({
              where: {
                id: intent.id,
                restaurantId: input.restaurantId,
                tableSessionId: input.tableSessionId,
                payerParticipantId: input.participantId,
                status: intent.status
              },
              data: {
                status: TablePaymentIntentStatus4.CANCELED,
                canceledAt: now,
                failureCode: "CANCELED_BY_PAYER"
              }
            });
            if (changed.count !== 1) {
              throw new TablePaymentError(
                "O pagamento foi atualizado por outra opera\xE7\xE3o. Atualize a conta.",
                409,
                "TABLE_PAYMENT_CONFLICT"
              );
            }
            await tx.tablePaymentEvent.create({
              data: {
                restaurantId: input.restaurantId,
                tableSessionId: input.tableSessionId,
                paymentIntentId: intent.id,
                deduplicationKey: `table-payment:${intent.publicId}:canceled-by-payer`,
                type: TablePaymentEventType4.CANCELED,
                fromStatus: intent.status,
                toStatus: TablePaymentIntentStatus4.CANCELED,
                provider: intent.provider,
                amountCents: intent.totalCents,
                occurredAt: now
              }
            });
            await projectTableSessionFinancialState(tx, input.restaurantId, input.tableSessionId, now);
            const updated = await tx.tablePaymentIntent.findUniqueOrThrow({
              where: { id: intent.id },
              select: tablePaymentIntentDtoSelect
            });
            return { intent: updated, previousStatus: intent.status, changed: true };
          },
          { isolationLevel: Prisma12.TransactionIsolationLevel.Serializable }
        );
        let providerCancellationPending = false;
        if (result.changed && result.previousStatus === TablePaymentIntentStatus4.PROCESSING && result.intent.provider === this.provider.code && result.intent.providerExternalId) {
          try {
            await this.provider.cancelPayment({
              externalId: result.intent.providerExternalId,
              idempotencyKey: `cancel:${result.intent.publicId}`
            });
          } catch (error2) {
            providerCancellationPending = true;
            console.error(
              "[CANCEL_TABLE_PAYMENT_PROVIDER_ERROR]",
              error2 instanceof Error ? error2.name : "UNKNOWN_ERROR"
            );
          }
        }
        return {
          payment: serializeTablePaymentIntent(result.intent, input.sessionPublicId),
          providerCancellationPending
        };
      }
    };
    CancelTablePaymentIntentService_default = new CancelTablePaymentIntentService();
  }
});

// src/modules/tableAccount/controllers/CancelTablePaymentIntentController.ts
var CancelTablePaymentIntentController, CancelTablePaymentIntentController_default;
var init_CancelTablePaymentIntentController = __esm({
  "src/modules/tableAccount/controllers/CancelTablePaymentIntentController.ts"() {
    init_CancelTablePaymentIntentService();
    init_tablePaymentSupport();
    CancelTablePaymentIntentController = class {
      async handle(req, res) {
        try {
          const result = await CancelTablePaymentIntentService_default.execute({
            publicId: String(req.params.publicId || "").trim(),
            tableSessionId: Number(req.tableSession?.id),
            sessionPublicId: String(req.tableSession?.publicId || ""),
            restaurantId: Number(req.tableSession?.restaurantId),
            participantId: Number(req.tableParticipant?.id)
          });
          res.setHeader("Cache-Control", "no-store");
          return res.json(result);
        } catch (error2) {
          if (error2 instanceof TablePaymentError) {
            return res.status(error2.statusCode).json({ error: error2.message, code: error2.code });
          }
          console.error(
            "[CANCEL_TABLE_PAYMENT_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel cancelar este pagamento." });
        }
      }
    };
    CancelTablePaymentIntentController_default = new CancelTablePaymentIntentController();
  }
});

// src/modules/tableAccount/services/ConfirmManualTablePaymentService.ts
import {
  Prisma as Prisma13,
  TablePaymentEventType as TablePaymentEventType5,
  TablePaymentIntentStatus as TablePaymentIntentStatus5,
  TablePaymentMethod as TablePaymentMethod2
} from "@prisma/client";
var ConfirmManualTablePaymentService, ConfirmManualTablePaymentService_default;
var init_ConfirmManualTablePaymentService = __esm({
  "src/modules/tableAccount/services/ConfirmManualTablePaymentService.ts"() {
    init_prisma();
    init_tableAccountRules();
    init_TablePaymentRepository();
    init_tablePaymentLedger();
    init_tablePaymentSupport();
    ConfirmManualTablePaymentService = class {
      constructor(now = () => /* @__PURE__ */ new Date()) {
        this.now = now;
      }
      now;
      async execute(input) {
        const restaurantId = Number(input.actor.restaurantId || 0);
        if (!canConfirmManualTablePayment(input.actor, restaurantId)) {
          throw new TablePaymentError(
            "Somente o administrador ou um gar\xE7om deste restaurante pode confirmar o pagamento.",
            403,
            "MANUAL_PAYMENT_FORBIDDEN"
          );
        }
        const initial = await TablePaymentRepository_default.findForStaffByPublicId(
          input.publicId,
          restaurantId
        );
        if (!initial) {
          throw new TablePaymentError(
            "Pagamento presencial n\xE3o encontrado neste restaurante.",
            404,
            "TABLE_PAYMENT_NOT_FOUND"
          );
        }
        const now = this.now();
        const updated = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, restaurantId, initial.tableSessionId);
            await expireTablePaymentReservations(tx, restaurantId, initial.tableSessionId, now);
            const intent = await TablePaymentRepository_default.findForStaffByPublicId(
              input.publicId,
              restaurantId,
              tx
            );
            if (!intent) {
              throw new TablePaymentError(
                "Pagamento presencial n\xE3o encontrado neste restaurante.",
                404,
                "TABLE_PAYMENT_NOT_FOUND"
              );
            }
            const manualMethod = intent.method === TablePaymentMethod2.CASH || intent.method === TablePaymentMethod2.CARD_MACHINE;
            if (!manualMethod) {
              throw new TablePaymentError(
                "Somente pagamentos em dinheiro ou maquininha podem ser confirmados manualmente.",
                409,
                "NOT_A_MANUAL_PAYMENT"
              );
            }
            if (intent.status === TablePaymentIntentStatus5.PAID && intent.manualConfirmedById === input.actor.id) {
              return intent;
            }
            if (intent.status !== TablePaymentIntentStatus5.RESERVED && intent.status !== TablePaymentIntentStatus5.PROCESSING) {
              throw new TablePaymentError(
                "Este pagamento n\xE3o est\xE1 mais aguardando confirma\xE7\xE3o.",
                409,
                "MANUAL_PAYMENT_NOT_PENDING"
              );
            }
            const changed = await tx.tablePaymentIntent.updateMany({
              where: {
                id: intent.id,
                restaurantId,
                tableSessionId: intent.tableSessionId,
                status: intent.status
              },
              data: {
                status: TablePaymentIntentStatus5.PAID,
                paidAt: now,
                manualConfirmedAt: now,
                manualConfirmedById: input.actor.id
              }
            });
            if (changed.count !== 1) {
              throw new TablePaymentError(
                "O pagamento foi atualizado por outra opera\xE7\xE3o. Atualize a conta.",
                409,
                "TABLE_PAYMENT_CONFLICT"
              );
            }
            await tx.tablePaymentEvent.create({
              data: {
                restaurantId,
                tableSessionId: intent.tableSessionId,
                paymentIntentId: intent.id,
                deduplicationKey: `table-payment:${intent.publicId}:manual-confirmed`,
                type: TablePaymentEventType5.MANUAL_CONFIRMED,
                fromStatus: intent.status,
                toStatus: TablePaymentIntentStatus5.PAID,
                amountCents: intent.totalCents,
                actorUserId: input.actor.id,
                occurredAt: now
              }
            });
            await projectTableSessionFinancialState(tx, restaurantId, intent.tableSessionId, now);
            return tx.tablePaymentIntent.findUniqueOrThrow({
              where: { id: intent.id },
              select: tablePaymentIntentDtoSelect
            });
          },
          { isolationLevel: Prisma13.TransactionIsolationLevel.Serializable }
        );
        return {
          payment: serializeTablePaymentIntent(updated, initial.tableSession.publicId)
        };
      }
    };
    ConfirmManualTablePaymentService_default = new ConfirmManualTablePaymentService();
  }
});

// src/modules/tableAccount/controllers/ConfirmManualTablePaymentController.ts
var ConfirmManualTablePaymentController, ConfirmManualTablePaymentController_default;
var init_ConfirmManualTablePaymentController = __esm({
  "src/modules/tableAccount/controllers/ConfirmManualTablePaymentController.ts"() {
    init_ConfirmManualTablePaymentService();
    init_tablePaymentSupport();
    ConfirmManualTablePaymentController = class {
      async handle(req, res) {
        try {
          const result = await ConfirmManualTablePaymentService_default.execute({
            publicId: String(req.params.publicId || "").trim(),
            actor: {
              id: Number(req.user?.id),
              role: String(req.user?.role || ""),
              subRole: req.user?.subRole || null,
              restaurantId: Number(req.user?.restaurantId || 0)
            }
          });
          res.setHeader("Cache-Control", "no-store");
          return res.json(result);
        } catch (error2) {
          if (error2 instanceof TablePaymentError) {
            return res.status(error2.statusCode).json({ error: error2.message, code: error2.code });
          }
          console.error(
            "[CONFIRM_MANUAL_TABLE_PAYMENT_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel confirmar este pagamento." });
        }
      }
    };
    ConfirmManualTablePaymentController_default = new ConfirmManualTablePaymentController();
  }
});

// src/modules/tableAccount/domain/tablePaymentProviderTransition.ts
function resolveTablePaymentProviderTransition(currentStatus, providerStatus) {
  const latePayment = providerStatus === "PAID" && latePaymentStatuses.includes(currentStatus);
  if (latePayment) {
    return {
      nextStatus: null,
      eventType: "PROVIDER_WEBHOOK",
      latePayment: true
    };
  }
  if (providerStatus === "PAID" && (currentStatus === "RESERVED" || currentStatus === "PROCESSING")) {
    return { nextStatus: "PAID", eventType: "PAID", latePayment: false };
  }
  if (providerStatus === "REFUNDED" && currentStatus === "PAID") {
    return {
      nextStatus: "REFUNDED",
      eventType: "REFUNDED",
      latePayment: false
    };
  }
  const failureStatus = providerStatus === "FAILED" ? "FAILED" : providerStatus === "EXPIRED" ? "EXPIRED" : providerStatus === "CANCELED" ? "CANCELED" : null;
  if (failureStatus && (currentStatus === "RESERVED" || currentStatus === "PROCESSING")) {
    return {
      nextStatus: failureStatus,
      eventType: failureStatus,
      latePayment: false
    };
  }
  return {
    nextStatus: null,
    eventType: "PROVIDER_WEBHOOK",
    latePayment: false
  };
}
var latePaymentStatuses;
var init_tablePaymentProviderTransition = __esm({
  "src/modules/tableAccount/domain/tablePaymentProviderTransition.ts"() {
    latePaymentStatuses = ["FAILED", "EXPIRED", "CANCELED"];
  }
});

// src/modules/tableAccount/services/ProcessTablePaymentWebhookService.ts
import {
  Prisma as Prisma14,
  TablePaymentEventType as TablePaymentEventType6,
  TablePaymentIntentStatus as TablePaymentIntentStatus6
} from "@prisma/client";
var ProcessTablePaymentWebhookService, ProcessTablePaymentWebhookService_default;
var init_ProcessTablePaymentWebhookService = __esm({
  "src/modules/tableAccount/services/ProcessTablePaymentWebhookService.ts"() {
    init_prisma();
    init_FakePaymentProvider();
    init_TablePaymentRepository();
    init_tablePaymentLedger();
    init_tablePaymentSupport();
    init_tablePaymentProviderTransition();
    ProcessTablePaymentWebhookService = class {
      constructor(provider = FakePaymentProvider_default) {
        this.provider = provider;
      }
      provider;
      async execute(input) {
        const event = await this.provider.validateWebhook(input);
        const initial = await prisma_default.tablePaymentIntent.findUnique({
          where: {
            provider_providerExternalId: {
              provider: this.provider.code,
              providerExternalId: event.externalId
            }
          },
          select: tablePaymentIntentDtoSelect
        });
        if (!initial) {
          return { received: true, ignored: true };
        }
        if (Number(initial.totalCents) !== event.amountCents) {
          throw new TablePaymentError(
            "O valor do evento n\xE3o corresponde ao pagamento.",
            409,
            "PROVIDER_AMOUNT_MISMATCH"
          );
        }
        const deduplicationKey = `table-payment-webhook:${sha256(
          `${this.provider.code}:${event.eventId}`
        )}`;
        const outcome = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, initial.restaurantId, initial.tableSessionId);
            const duplicate = await tx.tablePaymentEvent.findUnique({
              where: { deduplicationKey },
              select: { id: true }
            });
            const current = await tx.tablePaymentIntent.findUniqueOrThrow({
              where: { id: initial.id },
              select: tablePaymentIntentDtoSelect
            });
            const transition = resolveTablePaymentProviderTransition(current.status, event.status);
            const latePayment = transition.latePayment;
            if (duplicate) {
              return { duplicate: true, latePayment, current };
            }
            const nextStatus = transition.nextStatus;
            const eventType = transition.eventType;
            if (nextStatus) {
              const timestampData = nextStatus === TablePaymentIntentStatus6.PAID ? { paidAt: event.occurredAt } : nextStatus === TablePaymentIntentStatus6.REFUNDED ? { refundedAt: event.occurredAt } : nextStatus === TablePaymentIntentStatus6.CANCELED ? { canceledAt: event.occurredAt } : { failedAt: event.occurredAt, failureCode: `PROVIDER_${event.status}` };
              const changed = await tx.tablePaymentIntent.updateMany({
                where: {
                  id: current.id,
                  restaurantId: current.restaurantId,
                  tableSessionId: current.tableSessionId,
                  status: current.status
                },
                data: { status: nextStatus, ...timestampData }
              });
              if (changed.count !== 1) {
                throw new TablePaymentError(
                  "O pagamento foi atualizado por outra opera\xE7\xE3o.",
                  409,
                  "TABLE_PAYMENT_CONFLICT"
                );
              }
            }
            await tx.tablePaymentEvent.create({
              data: {
                restaurantId: current.restaurantId,
                tableSessionId: current.tableSessionId,
                paymentIntentId: current.id,
                deduplicationKey,
                type: eventType,
                fromStatus: current.status,
                toStatus: nextStatus || current.status,
                provider: this.provider.code,
                providerEventId: event.eventId,
                amountCents: BigInt(event.amountCents),
                occurredAt: event.occurredAt,
                metadata: latePayment ? { latePayment: true, refundRequired: true } : void 0
              }
            });
            if (nextStatus) {
              await projectTableSessionFinancialState(
                tx,
                current.restaurantId,
                current.tableSessionId,
                event.occurredAt
              );
            }
            const updated = await tx.tablePaymentIntent.findUniqueOrThrow({
              where: { id: current.id },
              select: tablePaymentIntentDtoSelect
            });
            return { duplicate: false, latePayment, current: updated };
          },
          { isolationLevel: Prisma14.TransactionIsolationLevel.Serializable }
        );
        if (outcome.latePayment && outcome.current.providerExternalId) {
          await this.provider.refundPayment({
            externalId: outcome.current.providerExternalId,
            idempotencyKey: `late-refund:${outcome.current.publicId}`
          });
          await prisma_default.tablePaymentEvent.upsert({
            where: {
              deduplicationKey: `table-payment:${outcome.current.publicId}:late-refunded`
            },
            create: {
              restaurantId: outcome.current.restaurantId,
              tableSessionId: outcome.current.tableSessionId,
              paymentIntentId: outcome.current.id,
              deduplicationKey: `table-payment:${outcome.current.publicId}:late-refunded`,
              type: TablePaymentEventType6.REFUNDED,
              fromStatus: outcome.current.status,
              toStatus: outcome.current.status,
              provider: this.provider.code,
              providerEventId: event.eventId,
              amountCents: outcome.current.totalCents,
              occurredAt: /* @__PURE__ */ new Date(),
              metadata: { automaticLateRefund: true }
            },
            update: {}
          });
        }
        return {
          received: true,
          processed: true,
          duplicate: outcome.duplicate,
          latePaymentRefunded: outcome.latePayment
        };
      }
    };
    ProcessTablePaymentWebhookService_default = new ProcessTablePaymentWebhookService();
  }
});

// src/modules/tableAccount/controllers/FakeTablePaymentWebhookController.ts
var FakeTablePaymentWebhookController, FakeTablePaymentWebhookController_default;
var init_FakeTablePaymentWebhookController = __esm({
  "src/modules/tableAccount/controllers/FakeTablePaymentWebhookController.ts"() {
    init_ProcessTablePaymentWebhookService();
    init_tablePaymentSupport();
    FakeTablePaymentWebhookController = class {
      async handle(req, res) {
        try {
          const result = await ProcessTablePaymentWebhookService_default.execute({
            headers: req.headers,
            body: req.body
          });
          return res.status(200).json(result);
        } catch (error2) {
          if (error2 instanceof TablePaymentError) {
            return res.status(error2.statusCode).json({ error: error2.message, code: error2.code });
          }
          const message = error2 instanceof Error ? error2.message : String(error2);
          if (/assinatura|desativado/i.test(message)) {
            return res.status(401).json({ error: "Webhook de pagamento n\xE3o autorizado." });
          }
          console.error(
            "[FAKE_TABLE_PAYMENT_WEBHOOK_ERROR]",
            error2 instanceof Error ? error2.name : "UNKNOWN_ERROR"
          );
          return res.status(500).json({ received: false });
        }
      }
    };
    FakeTablePaymentWebhookController_default = new FakeTablePaymentWebhookController();
  }
});

// src/modules/tableAccount/services/RefundTablePaymentService.ts
import { Prisma as Prisma15, TablePaymentEventType as TablePaymentEventType7, TablePaymentIntentStatus as TablePaymentIntentStatus7 } from "@prisma/client";
var RefundTablePaymentService, RefundTablePaymentService_default;
var init_RefundTablePaymentService = __esm({
  "src/modules/tableAccount/services/RefundTablePaymentService.ts"() {
    init_prisma();
    init_tableAccountSchemas();
    init_tableAccountRules();
    init_FakePaymentProvider();
    init_TablePaymentRepository();
    init_tablePaymentLedger();
    init_tablePaymentSupport();
    RefundTablePaymentService = class {
      constructor(provider = FakePaymentProvider_default, now = () => /* @__PURE__ */ new Date()) {
        this.provider = provider;
        this.now = now;
      }
      provider;
      now;
      async execute(input, rawPayload) {
        const payload = refundTablePaymentInputSchema.parse(rawPayload);
        const restaurantId = Number(input.actor.restaurantId || 0);
        if (!canRefundTablePayment(input.actor, restaurantId)) {
          throw new TablePaymentError(
            "Somente o administrador deste restaurante pode estornar pagamentos da mesa.",
            403,
            "TABLE_PAYMENT_REFUND_FORBIDDEN"
          );
        }
        const initial = await TablePaymentRepository_default.findForStaffByPublicId(
          input.publicId,
          restaurantId
        );
        if (!initial) {
          throw new TablePaymentError(
            "Pagamento n\xE3o encontrado neste restaurante.",
            404,
            "TABLE_PAYMENT_NOT_FOUND"
          );
        }
        if (initial.status === TablePaymentIntentStatus7.REFUNDED) {
          return {
            payment: serializeTablePaymentIntent(initial, initial.tableSession.publicId),
            idempotentReplay: true
          };
        }
        if (initial.status !== TablePaymentIntentStatus7.PAID) {
          throw new TablePaymentError(
            "Somente pagamentos confirmados podem ser estornados.",
            409,
            "TABLE_PAYMENT_NOT_REFUNDABLE"
          );
        }
        if (initial.provider) {
          if (initial.provider !== this.provider.code || !initial.providerExternalId) {
            throw new TablePaymentError(
              "O provedor deste pagamento ainda n\xE3o possui integra\xE7\xE3o de estorno.",
              409,
              "TABLE_PAYMENT_PROVIDER_UNAVAILABLE"
            );
          }
          try {
            const providerPayment = await this.provider.refundPayment({
              externalId: initial.providerExternalId,
              idempotencyKey: `admin-refund:${initial.publicId}`
            });
            if (providerPayment.status !== "REFUNDED") {
              throw new Error("O provedor n\xE3o confirmou o estorno.");
            }
          } catch (error2) {
            console.error(
              "[REFUND_TABLE_PAYMENT_PROVIDER_ERROR]",
              error2 instanceof Error ? error2.name : "UNKNOWN_ERROR"
            );
            throw new TablePaymentError(
              "O provedor n\xE3o confirmou o estorno. Tente novamente sem alterar o pagamento.",
              502,
              "TABLE_PAYMENT_PROVIDER_REFUND_FAILED"
            );
          }
        }
        const now = this.now();
        const result = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, restaurantId, initial.tableSessionId);
            const intent = await TablePaymentRepository_default.findForStaffByPublicId(
              input.publicId,
              restaurantId,
              tx
            );
            if (!intent) {
              throw new TablePaymentError(
                "Pagamento n\xE3o encontrado neste restaurante.",
                404,
                "TABLE_PAYMENT_NOT_FOUND"
              );
            }
            if (intent.status === TablePaymentIntentStatus7.REFUNDED) {
              return { intent, idempotentReplay: true };
            }
            if (intent.status !== TablePaymentIntentStatus7.PAID) {
              throw new TablePaymentError(
                "O pagamento foi atualizado por outra opera\xE7\xE3o e n\xE3o pode ser estornado.",
                409,
                "TABLE_PAYMENT_CONFLICT"
              );
            }
            const changed = await tx.tablePaymentIntent.updateMany({
              where: {
                id: intent.id,
                restaurantId,
                tableSessionId: intent.tableSessionId,
                status: TablePaymentIntentStatus7.PAID
              },
              data: {
                status: TablePaymentIntentStatus7.REFUNDED,
                refundedAt: now
              }
            });
            if (changed.count !== 1) {
              throw new TablePaymentError(
                "O pagamento foi atualizado por outra opera\xE7\xE3o. Atualize a conta.",
                409,
                "TABLE_PAYMENT_CONFLICT"
              );
            }
            await tx.tablePaymentEvent.create({
              data: {
                restaurantId,
                tableSessionId: intent.tableSessionId,
                paymentIntentId: intent.id,
                deduplicationKey: `table-payment:${intent.publicId}:admin-refunded`,
                type: TablePaymentEventType7.REFUNDED,
                fromStatus: TablePaymentIntentStatus7.PAID,
                toStatus: TablePaymentIntentStatus7.REFUNDED,
                provider: intent.provider,
                amountCents: intent.totalCents,
                actorUserId: input.actor.id,
                occurredAt: now,
                metadata: { reason: payload.reason }
              }
            });
            await projectTableSessionFinancialState(tx, restaurantId, intent.tableSessionId, now);
            const updated = await tx.tablePaymentIntent.findUniqueOrThrow({
              where: { id: intent.id },
              select: tablePaymentIntentDtoSelect
            });
            return { intent: updated, idempotentReplay: false };
          },
          { isolationLevel: Prisma15.TransactionIsolationLevel.Serializable }
        );
        return {
          payment: serializeTablePaymentIntent(result.intent, initial.tableSession.publicId),
          idempotentReplay: result.idempotentReplay
        };
      }
    };
    RefundTablePaymentService_default = new RefundTablePaymentService();
  }
});

// src/modules/tableAccount/controllers/RefundTablePaymentController.ts
import { ZodError as ZodError4 } from "zod";
var RefundTablePaymentController, RefundTablePaymentController_default;
var init_RefundTablePaymentController = __esm({
  "src/modules/tableAccount/controllers/RefundTablePaymentController.ts"() {
    init_RefundTablePaymentService();
    init_tablePaymentSupport();
    RefundTablePaymentController = class {
      async handle(req, res) {
        try {
          const result = await RefundTablePaymentService_default.execute(
            {
              publicId: String(req.params.publicId || "").trim(),
              actor: {
                id: Number(req.user?.id),
                role: String(req.user?.role || ""),
                subRole: req.user?.subRole || null,
                restaurantId: Number(req.user?.restaurantId || 0)
              }
            },
            req.body
          );
          res.setHeader("Cache-Control", "no-store");
          return res.json(result);
        } catch (error2) {
          if (error2 instanceof ZodError4) {
            return res.status(400).json({
              error: error2.issues[0]?.message || "Dados do estorno inv\xE1lidos.",
              code: "INVALID_TABLE_PAYMENT_REFUND_INPUT"
            });
          }
          if (error2 instanceof TablePaymentError) {
            return res.status(error2.statusCode).json({ error: error2.message, code: error2.code });
          }
          console.error(
            "[REFUND_TABLE_PAYMENT_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel estornar este pagamento." });
        }
      }
    };
    RefundTablePaymentController_default = new RefundTablePaymentController();
  }
});

// src/modules/tableAccount/services/GetTableAccountAdminSnapshotService.ts
import { Prisma as Prisma16 } from "@prisma/client";
var GetTableAccountAdminSnapshotService, GetTableAccountAdminSnapshotService_default;
var init_GetTableAccountAdminSnapshotService = __esm({
  "src/modules/tableAccount/services/GetTableAccountAdminSnapshotService.ts"() {
    init_prisma();
    init_tableAccountRules();
    init_TableAccountRepository();
    init_tablePaymentSupport();
    init_tablePaymentLedger();
    init_GetCurrentTableAccountService();
    GetTableAccountAdminSnapshotService = class {
      constructor(now = () => /* @__PURE__ */ new Date()) {
        this.now = now;
      }
      now;
      async execute(input) {
        const restaurantId = Number(input.actor.restaurantId || 0);
        if (!canViewTableAccountFinancialHistory(input.actor, restaurantId)) {
          throw new TablePaymentError(
            "Somente o administrador ou um gar\xE7om deste restaurante pode consultar este hist\xF3rico.",
            403,
            "TABLE_ACCOUNT_HISTORY_FORBIDDEN"
          );
        }
        const sessionPublicId = String(input.sessionPublicId || "").trim();
        const initial = await TableAccountRepository_default.findAdminSnapshotData(
          sessionPublicId,
          restaurantId
        );
        if (!initial) {
          throw new TablePaymentError(
            "Conta da mesa n\xE3o encontrada neste restaurante.",
            404,
            "TABLE_ACCOUNT_NOT_FOUND"
          );
        }
        const now = this.now();
        const data = await prisma_default.$transaction(
          async (tx) => {
            await lockTablePaymentSession(tx, restaurantId, initial.id);
            await expireTablePaymentReservations(tx, restaurantId, initial.id, now);
            const refreshed = await TableAccountRepository_default.findAdminSnapshotData(
              sessionPublicId,
              restaurantId,
              tx
            );
            if (!refreshed) {
              throw new TablePaymentError(
                "Conta da mesa n\xE3o encontrada neste restaurante.",
                404,
                "TABLE_ACCOUNT_NOT_FOUND"
              );
            }
            return refreshed;
          },
          { isolationLevel: Prisma16.TransactionIsolationLevel.Serializable }
        );
        return {
          ...buildTableAccountBaseSnapshot(data, now),
          paymentIntents: data.paymentIntents.map(
            (payment) => serializeTablePaymentIntentForAdmin(payment, data.publicId)
          )
        };
      }
    };
    GetTableAccountAdminSnapshotService_default = new GetTableAccountAdminSnapshotService();
  }
});

// src/modules/tableAccount/controllers/GetTableAccountAdminSnapshotController.ts
var GetTableAccountAdminSnapshotController, GetTableAccountAdminSnapshotController_default;
var init_GetTableAccountAdminSnapshotController = __esm({
  "src/modules/tableAccount/controllers/GetTableAccountAdminSnapshotController.ts"() {
    init_GetTableAccountAdminSnapshotService();
    init_tablePaymentSupport();
    GetTableAccountAdminSnapshotController = class {
      async handle(req, res) {
        try {
          const result = await GetTableAccountAdminSnapshotService_default.execute({
            sessionPublicId: String(req.params.sessionPublicId || "").trim(),
            actor: {
              id: Number(req.user?.id),
              role: String(req.user?.role || ""),
              subRole: req.user?.subRole || null,
              restaurantId: Number(req.user?.restaurantId || 0)
            }
          });
          res.setHeader("Cache-Control", "no-store");
          return res.json(result);
        } catch (error2) {
          if (error2 instanceof TablePaymentError) {
            return res.status(error2.statusCode).json({ error: error2.message, code: error2.code });
          }
          console.error(
            "[GET_TABLE_ACCOUNT_ADMIN_SNAPSHOT_ERROR]",
            error2 instanceof Error ? error2.message : String(error2)
          );
          return res.status(500).json({ error: "N\xE3o foi poss\xEDvel carregar o hist\xF3rico da mesa." });
        }
      }
    };
    GetTableAccountAdminSnapshotController_default = new GetTableAccountAdminSnapshotController();
  }
});

// src/modules/tableAccount/services/GetTableAccountSettingsService.ts
var GetTableAccountSettingsService, GetTableAccountSettingsService_default;
var init_GetTableAccountSettingsService = __esm({
  "src/modules/tableAccount/services/GetTableAccountSettingsService.ts"() {
    init_TableAccountSettingsRepository();
    GetTableAccountSettingsService = class {
      async execute(restaurantId) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para consultar as configura\xE7\xF5es da conta da mesa.");
        }
        return TableAccountSettingsRepository_default.findByRestaurantId(normalizedRestaurantId);
      }
    };
    GetTableAccountSettingsService_default = new GetTableAccountSettingsService();
  }
});

// src/modules/tableAccount/controllers/GetTableAccountSettingsController.ts
var GetTableAccountSettingsController, GetTableAccountSettingsController_default;
var init_GetTableAccountSettingsController = __esm({
  "src/modules/tableAccount/controllers/GetTableAccountSettingsController.ts"() {
    init_GetTableAccountSettingsService();
    GetTableAccountSettingsController = class {
      async handle(req, res) {
        try {
          const settings = await GetTableAccountSettingsService_default.execute(req.user.restaurantId);
          return res.status(200).json(settings);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao consultar configura\xE7\xF5es da mesa."
          });
        }
      }
    };
    GetTableAccountSettingsController_default = new GetTableAccountSettingsController();
  }
});

// src/modules/tableAccount/services/UpdateTableAccountSettingsService.ts
var UpdateTableAccountSettingsService, UpdateTableAccountSettingsService_default;
var init_UpdateTableAccountSettingsService = __esm({
  "src/modules/tableAccount/services/UpdateTableAccountSettingsService.ts"() {
    init_tableAccountSchemas();
    init_TableAccountSettingsRepository();
    UpdateTableAccountSettingsService = class {
      async execute(restaurantId, rawInput) {
        const normalizedRestaurantId = Number(restaurantId);
        if (!Number.isInteger(normalizedRestaurantId) || normalizedRestaurantId <= 0) {
          throw new Error("Restaurante inv\xE1lido para alterar as configura\xE7\xF5es da conta da mesa.");
        }
        const patch = tableAccountSettingsPatchSchema.parse(rawInput);
        const current = await TableAccountSettingsRepository_default.findByRestaurantId(
          normalizedRestaurantId
        );
        const merged = tableAccountSettingsSchema.parse({
          ...current,
          ...patch
        });
        return TableAccountSettingsRepository_default.upsert(normalizedRestaurantId, merged);
      }
    };
    UpdateTableAccountSettingsService_default = new UpdateTableAccountSettingsService();
  }
});

// src/modules/tableAccount/controllers/UpdateTableAccountSettingsController.ts
var UpdateTableAccountSettingsController, UpdateTableAccountSettingsController_default;
var init_UpdateTableAccountSettingsController = __esm({
  "src/modules/tableAccount/controllers/UpdateTableAccountSettingsController.ts"() {
    init_UpdateTableAccountSettingsService();
    UpdateTableAccountSettingsController = class {
      async handle(req, res) {
        try {
          const settings = await UpdateTableAccountSettingsService_default.execute(
            req.user.restaurantId,
            req.body
          );
          return res.status(200).json(settings);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao salvar configura\xE7\xF5es da mesa."
          });
        }
      }
    };
    UpdateTableAccountSettingsController_default = new UpdateTableAccountSettingsController();
  }
});

// src/modules/tableAccount/services/ListTableAccountAdminSessionsService.ts
var ListTableAccountAdminSessionsService, ListTableAccountAdminSessionsService_default;
var init_ListTableAccountAdminSessionsService = __esm({
  "src/modules/tableAccount/services/ListTableAccountAdminSessionsService.ts"() {
    init_tableAccountRules();
    init_TableAccountRepository();
    init_TableSessionRepository();
    init_GetCurrentTableAccountService();
    init_tablePaymentSupport();
    ListTableAccountAdminSessionsService = class {
      async execute(actor) {
        const restaurantId = Number(actor.restaurantId || 0);
        if (!canViewTableAccountFinancialHistory(actor, restaurantId)) {
          throw new TablePaymentError(
            "Somente o administrador ou um gar\xE7om deste restaurante pode consultar as contas.",
            403,
            "TABLE_ACCOUNT_HISTORY_FORBIDDEN"
          );
        }
        const sessions = await TableSessionRepository_default.listOpenByRestaurant(restaurantId);
        const now = /* @__PURE__ */ new Date();
        const snapshots = await Promise.all(
          sessions.map(async (session) => {
            const data = await TableAccountRepository_default.findAdminSnapshotData(
              session.publicId,
              restaurantId
            );
            if (!data) return null;
            const account = buildTableAccountBaseSnapshot(data, now);
            return {
              tableSessionId: session.id,
              sessionPublicId: session.publicId,
              tableId: session.tableId,
              tableNumber: session.table.number,
              openedAt: session.openedAt.toISOString(),
              status: session.status,
              openedByName: session.openedBy.name,
              summary: account.summary,
              participants: account.participants,
              itemsCount: account.items.length,
              paymentCounts: {
                reserved: data.paymentIntents.filter((payment) => payment.status === "RESERVED").length,
                processing: data.paymentIntents.filter((payment) => payment.status === "PROCESSING").length,
                online: data.paymentIntents.filter((payment) => Boolean(payment.provider)).length,
                inPerson: data.paymentIntents.filter(
                  (payment) => ["CASH", "CARD_MACHINE"].includes(payment.method)
                ).length
              }
            };
          })
        );
        return { sessions: snapshots.filter((session) => session !== null) };
      }
    };
    ListTableAccountAdminSessionsService_default = new ListTableAccountAdminSessionsService();
  }
});

// src/modules/tableAccount/controllers/ListTableAccountAdminSessionsController.ts
var ListTableAccountAdminSessionsController, ListTableAccountAdminSessionsController_default;
var init_ListTableAccountAdminSessionsController = __esm({
  "src/modules/tableAccount/controllers/ListTableAccountAdminSessionsController.ts"() {
    init_ListTableAccountAdminSessionsService();
    init_tablePaymentSupport();
    ListTableAccountAdminSessionsController = class {
      async handle(req, res) {
        try {
          const result = await ListTableAccountAdminSessionsService_default.execute({
            id: Number(req.user?.id),
            role: String(req.user?.role || ""),
            subRole: req.user?.subRole || null,
            restaurantId: Number(req.user?.restaurantId || 0)
          });
          return res.status(200).json(result);
        } catch (error2) {
          const status = error2 instanceof TablePaymentError ? error2.statusCode : 400;
          return res.status(status).json({
            error: error2 instanceof Error ? error2.message : "Erro ao consultar contas de mesa."
          });
        }
      }
    };
    ListTableAccountAdminSessionsController_default = new ListTableAccountAdminSessionsController();
  }
});

// src/modules/tableAccount/routes/TableAccountRoutes.ts
import { Router as Router21 } from "express";
var router21, TableAccountRoutes_default;
var init_TableAccountRoutes = __esm({
  "src/modules/tableAccount/routes/TableAccountRoutes.ts"() {
    init_optionalAuthMiddleware();
    init_tableAccountSessionMiddleware();
    init_tableParticipantMiddleware();
    init_GetCurrentTableAccountController();
    init_CreateTablePaymentIntentController();
    init_CancelTablePaymentIntentController();
    init_accountActionRateLimitMiddleware();
    init_ConfirmManualTablePaymentController();
    init_authMiddleware();
    init_waiterMiddleware();
    init_FakeTablePaymentWebhookController();
    init_RefundTablePaymentController();
    init_adminMiddleware();
    init_GetTableAccountAdminSnapshotController();
    init_GetTableAccountSettingsController();
    init_UpdateTableAccountSettingsController();
    init_ListTableAccountAdminSessionsController();
    router21 = Router21();
    router21.get(
      "/settings",
      authMiddleware,
      adminMiddleware,
      (req, res) => GetTableAccountSettingsController_default.handle(req, res)
    );
    router21.patch(
      "/settings",
      authMiddleware,
      adminMiddleware,
      (req, res) => UpdateTableAccountSettingsController_default.handle(req, res)
    );
    router21.get(
      "/admin/sessions",
      authMiddleware,
      waiterMiddleware,
      (req, res) => ListTableAccountAdminSessionsController_default.handle(req, res)
    );
    router21.post("/webhooks/fake", (req, res) => FakeTablePaymentWebhookController_default.handle(req, res));
    router21.get(
      "/sessions/:sessionPublicId",
      optionalAuthMiddleware,
      tableAccountSessionMiddleware,
      tableParticipantMiddleware,
      (req, res) => GetCurrentTableAccountController_default.handle(req, res)
    );
    router21.get(
      "/sessions/:sessionPublicId/admin",
      authMiddleware,
      waiterMiddleware,
      (req, res) => GetTableAccountAdminSnapshotController_default.handle(req, res)
    );
    router21.post(
      "/sessions/:sessionPublicId/payments",
      optionalAuthMiddleware,
      tableAccountSessionMiddleware,
      tableParticipantMiddleware,
      tablePaymentActionRateLimitMiddleware,
      (req, res) => CreateTablePaymentIntentController_default.handle(req, res)
    );
    router21.patch(
      "/sessions/:sessionPublicId/payments/:publicId/cancel",
      optionalAuthMiddleware,
      tableAccountSessionMiddleware,
      tableParticipantMiddleware,
      tablePaymentActionRateLimitMiddleware,
      (req, res) => CancelTablePaymentIntentController_default.handle(req, res)
    );
    router21.post(
      "/payments/:publicId/confirm-manual",
      authMiddleware,
      waiterMiddleware,
      (req, res) => ConfirmManualTablePaymentController_default.handle(req, res)
    );
    router21.post(
      "/payments/:publicId/refund",
      authMiddleware,
      adminMiddleware,
      (req, res) => RefundTablePaymentController_default.handle(req, res)
    );
    TableAccountRoutes_default = router21;
  }
});

// src/routes/index.ts
import { Router as Router22 } from "express";
var router22, routes_default;
var init_routes = __esm({
  "src/routes/index.ts"() {
    init_authRoutes();
    init_productsRoutes();
    init_orderRoutes();
    init_restaurantRoutes();
    init_CategoryRoutes();
    init_EmployeeRoutes();
    init_SessionsTablesRoutes();
    init_TablesRoutes();
    init_authMiddleware();
    init_RestaurantSettingsRoutes();
    init_BannerRoutes();
    init_CouponRoutes();
    init_SubscriptionRoutes();
    init_AiSupportRoutes();
    init_MenuImportRoutes();
    init_AuditRoutes();
    init_FavoriteRoutes();
    init_ImageEnhancementRoutes();
    init_CustomerAddressRoutes();
    init_AsaasOrderWebhookController();
    init_AsaasWithdrawValidationWebhookController();
    init_ingredientRoutes();
    init_TableServiceCallRoutes();
    init_TableAccountRoutes();
    router22 = Router22();
    router22.post("/api/webhooks/asaas", (req, res) => {
      AsaasOrderWebhookController_default.handle(req, res);
    });
    router22.post("/api/webhooks/asaas/withdraw-validation", (req, res) => {
      AsaasWithdrawValidationWebhookController_default.handle(req, res);
    });
    router22.use("/auth", authRoutes_default);
    router22.use("/restaurants", restaurantRoutes_default);
    router22.use("/categories", CategoryRoutes_default);
    router22.use("/products", productsRoutes_default);
    router22.use("/ingredients", ingredientRoutes_default);
    router22.use("/orders", orderRoutes_default);
    router22.use("/employees", EmployeeRoutes_default);
    router22.use("/table-sessions", SessionsTablesRoutes_default);
    router22.use("/table-accounts", TableAccountRoutes_default);
    router22.use("/waiter-calls", TableServiceCallRoutes_default);
    router22.use("/tables", TablesRoutes_default);
    router22.use("/settings", RestaurantSettingsRoutes_default);
    router22.use("/banners", BannerRoutes_default);
    router22.use("/coupons", CouponRoutes_default);
    router22.use("/subscription", SubscriptionRoutes_default);
    router22.use("/ai-support", AiSupportRoutes_default);
    router22.use("/menu-import", MenuImportRoutes_default);
    router22.use("/audit-logs", AuditRoutes_default);
    router22.use("/favorites", FavoriteRoutes_default);
    router22.use("/image-enhancement", ImageEnhancementRoutes_default);
    router22.use("/customer-addresses", CustomerAddressRoutes_default);
    router22.get("/profile", authMiddleware, (req, res) => {
      return res.json({
        message: "Rota protegida!",
        user: req.user
      });
    });
    routes_default = router22;
  }
});

// src/modules/billing/config/platformMercadoPago.ts
function getPlatformMercadoPagoAccessToken() {
  return String(process.env.PLATFORM_MP_ACCESS_TOKEN || process.env.MP_ACCESS_TOKEN || "").trim();
}
function requirePlatformMercadoPagoAccessToken() {
  const accessToken = getPlatformMercadoPagoAccessToken();
  if (!accessToken) {
    throw new Error(
      "Mercado Pago da plataforma n\xE3o configurado. Defina PLATFORM_MP_ACCESS_TOKEN no backend."
    );
  }
  return accessToken;
}
var init_platformMercadoPago = __esm({
  "src/modules/billing/config/platformMercadoPago.ts"() {
  }
});

// src/modules/billing/services/MercadoPagoClient.ts
import { MercadoPagoConfig as MercadoPagoConfig3, Payment as Payment3 } from "mercadopago";
function createPlatformClient() {
  return new MercadoPagoConfig3({
    accessToken: requirePlatformMercadoPagoAccessToken()
  });
}
function getPlatformPaymentClient() {
  return new Payment3(createPlatformClient());
}
var init_MercadoPagoClient = __esm({
  "src/modules/billing/services/MercadoPagoClient.ts"() {
    init_platformMercadoPago();
  }
});

// src/modules/billing/utils/billingLogger.ts
function formatMeta(meta) {
  if (!meta) {
    return "";
  }
  return ` ${JSON.stringify(meta)}`;
}
function log(level, message, meta) {
  const line = `[billing] ${level} ${message}${formatMeta(meta)}`;
  if (level === "ERROR") {
    console.error(line);
    return;
  }
  if (level === "WARN") {
    console.warn(line);
    return;
  }
  console.log(line);
}
function info(message, meta) {
  log("INFO", message, meta);
}
function warn(message, meta) {
  log("WARN", message, meta);
}
function error(message, meta) {
  log("ERROR", message, meta);
}
function debug(message, meta) {
  if (!DEBUG_ENABLED) {
    return;
  }
  log("DEBUG", message, meta);
}
var DEBUG_ENABLED;
var init_billingLogger = __esm({
  "src/modules/billing/utils/billingLogger.ts"() {
    DEBUG_ENABLED = process.env.BILLING_DEBUG === "true";
  }
});

// src/modules/billing/services/ProcessPaymentService.ts
var ProcessPaymentService, ProcessPaymentService_default;
var init_ProcessPaymentService = __esm({
  "src/modules/billing/services/ProcessPaymentService.ts"() {
    init_BillingRepository();
    init_prisma();
    init_billingRules();
    init_billingLogger();
    ProcessPaymentService = class {
      async execute({ invoiceId }) {
        const normalizedInvoiceId = Number(invoiceId);
        if (!Number.isInteger(normalizedInvoiceId) || normalizedInvoiceId <= 0) {
          throw new Error("Fatura inv\xE1lida.");
        }
        const result = await prisma_default.$transaction(async (tx) => {
          const existingInvoice = await BillingRepository_default.findInvoiceById(normalizedInvoiceId, tx);
          if (!existingInvoice) {
            throw new Error("Fatura n\xE3o encontrada.");
          }
          const invoice = existingInvoice.status === "PAGO" ? existingInvoice : await BillingRepository_default.updateInvoice(
            normalizedInvoiceId,
            {
              status: "PAGO",
              paidAt: /* @__PURE__ */ new Date()
            },
            tx
          );
          const subscription = await BillingRepository_default.findSubscriptionByRestaurantId(
            invoice.restaurantId,
            tx
          );
          const openInvoices = await tx.invoice.findMany({
            where: {
              restaurantId: invoice.restaurantId,
              status: {
                in: ["PENDENTE", "ATRASADO"]
              }
            }
          });
          const remainsBlocked = hasBlockingInvoices(openInvoices, /* @__PURE__ */ new Date());
          if (subscription) {
            await BillingRepository_default.updateSubscription(
              subscription.id,
              { status: remainsBlocked ? "EXPIRADA" : "ATIVA" },
              tx
            );
          }
          if (remainsBlocked) {
            await BillingRepository_default.deactivateRestaurant(invoice.restaurantId, tx);
          } else {
            await BillingRepository_default.activateRestaurant(invoice.restaurantId, tx);
          }
          return { invoice, remainsBlocked };
        });
        info(
          result.remainsBlocked ? "payment processed but restaurant remains blocked" : "payment processed and restaurant activated",
          {
            invoiceId: normalizedInvoiceId,
            restaurantId: result.invoice.restaurantId
          }
        );
        return result.invoice;
      }
    };
    ProcessPaymentService_default = new ProcessPaymentService();
  }
});

// src/modules/billing/utils/webhookUtils.ts
function normalizePaymentStatus(status) {
  return String(status || "").trim().toLowerCase();
}
function isApprovedPaymentStatus(status) {
  return APPROVED_STATUSES2.has(normalizePaymentStatus(status));
}
function readFirstDefined(...values) {
  for (const value of values) {
    if (value === void 0 || value === null || value === "") {
      continue;
    }
    return value;
  }
  return null;
}
function extractInvoiceId(payload = {}, paymentDetails = {}) {
  const payloadData = payload;
  const detailsData = paymentDetails;
  const candidates = [
    readFirstDefined(
      payloadData.data?.external_reference,
      payloadData.external_reference,
      payloadData.externalReference,
      payloadData.metadata?.invoice_id,
      payloadData.metadata?.invoiceId,
      payloadData.invoice_id,
      payloadData.invoiceId,
      detailsData.external_reference,
      detailsData.body?.external_reference,
      detailsData.metadata?.invoice_id,
      detailsData.metadata?.invoiceId,
      detailsData.invoice_id,
      detailsData.invoiceId
    ),
    payloadData.resource?.external_reference,
    detailsData.resource?.external_reference
  ];
  for (const candidate of candidates) {
    if (candidate === null || candidate === void 0 || candidate === "") {
      continue;
    }
    const numericCandidate = Number(String(candidate).trim());
    if (!Number.isNaN(numericCandidate)) {
      return numericCandidate;
    }
    const match = String(candidate).match(/(\d+)/);
    if (match) {
      return Number(match[1]);
    }
  }
  return null;
}
var APPROVED_STATUSES2;
var init_webhookUtils = __esm({
  "src/modules/billing/utils/webhookUtils.ts"() {
    APPROVED_STATUSES2 = /* @__PURE__ */ new Set(["approved", "paid", "authorized", "settled"]);
  }
});

// src/modules/billing/controllers/MercadoPagoWebhookController.ts
var MercadoPagoWebhookController, MercadoPagoWebhookController_default;
var init_MercadoPagoWebhookController = __esm({
  "src/modules/billing/controllers/MercadoPagoWebhookController.ts"() {
    init_MercadoPagoClient();
    init_ProcessPaymentService();
    init_webhookUtils();
    init_billingLogger();
    MercadoPagoWebhookController = class {
      async handle(req, res) {
        try {
          const paymentId = req.body?.data?.id || req.body?.id || req.query?.id;
          debug("MP webhook received", { paymentId });
          if (!paymentId) {
            debug("webhook ignored: missing paymentId");
            return res.sendStatus(200);
          }
          const paymentApi = getPlatformPaymentClient();
          const payment = await paymentApi.get({ id: paymentId });
          const paymentDetails = typeof payment === "object" && payment !== null ? payment.body ?? payment : {};
          const paymentDetailsRecord = typeof paymentDetails === "object" && paymentDetails !== null ? paymentDetails : {};
          const payloadRecord = typeof req.body === "object" && req.body !== null ? req.body : {};
          const payloadData = typeof payloadRecord["data"] === "object" && payloadRecord["data"] !== null ? payloadRecord["data"] : {};
          const status = paymentDetailsRecord["status"] || payloadRecord["status"] || payloadData["status"];
          debug("MP payment status", { status });
          if (!isApprovedPaymentStatus(status)) {
            debug("webhook ignored: payment not approved");
            return res.sendStatus(200);
          }
          const invoiceId = extractInvoiceId(payloadRecord, paymentDetailsRecord);
          debug("webhook extracted invoice", { invoiceId });
          if (!invoiceId) {
            debug("webhook ignored: missing invoiceId");
            return res.sendStatus(200);
          }
          await ProcessPaymentService_default.execute({ invoiceId });
          info("webhook processed", { invoiceId });
          return res.sendStatus(200);
        } catch (err) {
          error("webhook processing failed", {
            message: err instanceof Error ? err.message : String(err)
          });
          return res.sendStatus(500);
        }
      }
    };
    MercadoPagoWebhookController_default = new MercadoPagoWebhookController();
  }
});

// src/modules/billing/controllers/BillingWebhookController.ts
import crypto11 from "crypto";
var BillingWebhookController, BillingWebhookController_default;
var init_BillingWebhookController = __esm({
  "src/modules/billing/controllers/BillingWebhookController.ts"() {
    init_BillingRepository();
    init_ProcessPaymentService();
    init_webhookUtils();
    init_billingLogger();
    BillingWebhookController = class {
      async handle(req, res) {
        try {
          const isEnabled = process.env.NODE_ENV !== "production" && String(process.env.ENABLE_TEST_PAYMENT_WEBHOOK || "false").toLowerCase() === "true";
          const configuredSecret = String(process.env.TEST_PAYMENT_WEBHOOK_SECRET || "").trim();
          const receivedSecret = String(req.headers["x-test-webhook-secret"] || "").trim();
          if (!isEnabled || !configuredSecret || !receivedSecret) {
            return res.sendStatus(404);
          }
          const configuredBuffer = Buffer.from(configuredSecret);
          const receivedBuffer = Buffer.from(receivedSecret);
          const secretMatches = configuredBuffer.length === receivedBuffer.length && crypto11.timingSafeEqual(configuredBuffer, receivedBuffer);
          if (!secretMatches) {
            return res.sendStatus(404);
          }
          const payment = req.body;
          const paymentId = payment.data?.id || payment.id || payment.data?.payment_id;
          debug("test webhook received", { paymentId });
          if (!paymentId) {
            debug("test webhook ignored: missing paymentId");
            return res.sendStatus(200);
          }
          const paymentStatus = payment.status || payment.data?.status || payment.action;
          if (!isApprovedPaymentStatus(paymentStatus)) {
            debug("test webhook ignored: payment not approved", {
              paymentStatus
            });
            return res.sendStatus(200);
          }
          const invoiceId = extractInvoiceId(payment);
          if (!invoiceId) {
            debug("test webhook ignored: missing invoiceId");
            return res.sendStatus(200);
          }
          const invoice = await BillingRepository_default.findInvoiceById(invoiceId);
          if (!invoice) {
            debug("test webhook ignored: invoice not found");
            return res.sendStatus(200);
          }
          if (invoice.status === "PAGO") {
            debug("test webhook ignored: invoice already paid");
            return res.sendStatus(200);
          }
          await ProcessPaymentService_default.execute({ invoiceId });
          info("test webhook processed", { invoiceId });
          return res.sendStatus(200);
        } catch (error2) {
          error("test webhook failed", {
            message: error2 instanceof Error ? error2.message : String(error2)
          });
          return res.sendStatus(200);
        }
      }
    };
    BillingWebhookController_default = new BillingWebhookController();
  }
});

// src/modules/billing/utils/billingTimeline.ts
function getBillingStartDate(restaurantCreatedAt, adminCreatedAt) {
  if (!adminCreatedAt) return new Date(restaurantCreatedAt);
  return new Date(Math.max(restaurantCreatedAt.getTime(), adminCreatedAt.getTime()));
}
function getCompletedSubscriptionMonths(startedAt, referenceDate = /* @__PURE__ */ new Date()) {
  if (referenceDate <= startedAt) return 0;
  let months = (referenceDate.getFullYear() - startedAt.getFullYear()) * 12 + referenceDate.getMonth() - startedAt.getMonth();
  if (referenceDate.getDate() < startedAt.getDate()) months -= 1;
  return Math.max(0, months);
}
var init_billingTimeline = __esm({
  "src/modules/billing/utils/billingTimeline.ts"() {
  }
});

// src/modules/billing/utils/billingPaymentWindow.ts
function getPixAvailableAt(dueDate) {
  const availableAt = new Date(dueDate);
  const configuredDays = Number(process.env.BILLING_PIX_OPEN_DAYS_BEFORE_DUE || 5);
  const daysBeforeDue = Number.isFinite(configuredDays) && configuredDays >= 0 ? Math.floor(configuredDays) : 5;
  availableAt.setDate(availableAt.getDate() - daysBeforeDue);
  return availableAt;
}
function isInvoicePixAvailable(invoice, now = /* @__PURE__ */ new Date()) {
  const status = String(invoice.status || "").toUpperCase();
  if (!["PENDENTE", "ATRASADO", "VENCIDO"].includes(status)) {
    return false;
  }
  return now >= getPixAvailableAt(invoice.dueDate);
}
var init_billingPaymentWindow = __esm({
  "src/modules/billing/utils/billingPaymentWindow.ts"() {
  }
});

// src/modules/billing/controllers/GetInvoicesController.ts
var GetInvoicesController, GetInvoicesController_default;
var init_GetInvoicesController = __esm({
  "src/modules/billing/controllers/GetInvoicesController.ts"() {
    init_BillingRepository();
    init_billingRules();
    init_billingTimeline();
    init_billingPaymentWindow();
    GetInvoicesController = class {
      async handle(req, res) {
        try {
          const restaurantId = req.user.restaurantId;
          if (!restaurantId) {
            return res.status(400).json({
              error: "Restaurant ID not found in user context"
            });
          }
          const invoices = await BillingRepository_default.findInvoicesByRestaurantId(restaurantId);
          const subscription = await BillingRepository_default.findSubscriptionByRestaurantId(
            Number(restaurantId)
          );
          const subscriptionStatus = String(subscription?.status || "").toUpperCase();
          const isPlanActive = subscriptionStatus === "ATIVA" || subscriptionStatus === "TESTE";
          const admin = subscription?.restaurant.users[0] || null;
          const restaurantCreatedAt = subscription?.restaurant.createdAt || null;
          const billingStartedAt = restaurantCreatedAt ? getBillingStartDate(restaurantCreatedAt, admin?.createdAt) : subscription?.createdAt || null;
          const payableInvoice = invoices.find(
            (invoice) => ["PENDENTE", "ATRASADO"].includes(invoice.status)
          );
          const billing = {
            plan: String(subscription?.plan || "BASICO").toUpperCase(),
            subscriptionStatus,
            isPlanActive,
            restaurantCreatedAt,
            adminCreatedAt: admin?.createdAt || null,
            adminName: admin?.name || null,
            billingStartedAt,
            completedMonths: billingStartedAt ? getCompletedSubscriptionMonths(billingStartedAt) : 0,
            currentCycle: billingStartedAt ? getCompletedSubscriptionMonths(billingStartedAt) + 1 : 1,
            currentInvoiceId: payableInvoice?.id || null,
            dueDate: payableInvoice?.dueDate || null,
            graceLimitDate: payableInvoice?.dueDate ? getGraceLimitDate(payableInvoice.dueDate) : null,
            pixAvailableAt: payableInvoice?.dueDate ? getPixAvailableAt(payableInvoice.dueDate) : null,
            pixAvailable: payableInvoice ? isInvoicePixAvailable(payableInvoice) : false
          };
          return res.status(200).json({
            invoices,
            billing
          });
        } catch (error2) {
          console.error("Error fetching invoices:", error2);
          return res.status(500).json({
            error: error2 instanceof Error ? error2.message : "Failed to fetch invoices"
          });
        }
      }
    };
    GetInvoicesController_default = new GetInvoicesController();
  }
});

// src/modules/billing/controllers/GetAllInvoicesController.ts
var STATUS_MAP, GetAllInvoicesController, GetAllInvoicesController_default;
var init_GetAllInvoicesController = __esm({
  "src/modules/billing/controllers/GetAllInvoicesController.ts"() {
    init_prisma();
    STATUS_MAP = {
      PAGO: "PAID",
      PENDENTE: "PENDING",
      ATRASADO: "OVERDUE",
      CANCELADO: "REFUNDED"
    };
    GetAllInvoicesController = class {
      async handle(_req, res) {
        try {
          const invoices = await prisma_default.invoice.findMany({
            include: {
              restaurant: { select: { name: true } }
            },
            orderBy: { createdAt: "desc" },
            take: 200
          });
          const result = invoices.map((inv) => ({
            id: `#FAT-${String(inv.id).padStart(4, "0")}`,
            restaurant: inv.restaurant.name,
            dueDate: inv.dueDate.toLocaleDateString("pt-BR"),
            value: Number(inv.total),
            method: "Sistema",
            status: STATUS_MAP[inv.status] ?? "PENDING"
          }));
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            message: error2 instanceof Error ? error2.message : "Erro ao listar faturas"
          });
        }
      }
    };
    GetAllInvoicesController_default = new GetAllInvoicesController();
  }
});

// src/modules/billing/controllers/GetPlansController.ts
var GetPlansController, GetPlansController_default;
var init_GetPlansController = __esm({
  "src/modules/billing/controllers/GetPlansController.ts"() {
    init_planConfig();
    GetPlansController = class {
      handle(_req, res) {
        const plans = Object.entries(PLAN_CONFIG).filter(([, config]) => config.availableForSale).map(([key, config]) => ({
          plan: key,
          name: config.name,
          monthlyFee: config.monthlyFee,
          trialDays: config.trialDays,
          features: config.features
        }));
        return res.status(200).json(plans);
      }
    };
    GetPlansController_default = new GetPlansController();
  }
});

// src/modules/billing/services/MercadoPagoService.ts
var MercadoPagoService, MercadoPagoService_default;
var init_MercadoPagoService = __esm({
  "src/modules/billing/services/MercadoPagoService.ts"() {
    init_MercadoPagoClient();
    init_billingLogger();
    MercadoPagoService = class {
      async createPayment({
        invoiceId,
        title,
        description,
        amount,
        payerEmail
      }) {
        const isProduction2 = process.env.NODE_ENV === "production";
        const port2 = process.env.PORT || 3e3;
        const backendBaseUrl = String(process.env.BACKEND_URL || "").trim();
        const fallbackUrl = backendBaseUrl ? `${backendBaseUrl}/billing/webhook/mercadopago` : `http://localhost:${port2}/billing/webhook/mercadopago`;
        const notificationUrl = String(process.env.MP_NOTIFICATION_URL || fallbackUrl).trim();
        if (isProduction2 && (!notificationUrl || notificationUrl.includes("localhost"))) {
          throw new Error(
            "Webhook Mercado Pago inv\xE1lido para produ\xE7\xE3o. Configure MP_NOTIFICATION_URL com uma URL p\xFAblica HTTPS."
          );
        }
        const expiresAt = new Date(Date.now() + 30 * 60 * 1e3).toISOString();
        debug("creating Mercado Pago Pix", { invoiceId, amount: Number(amount) });
        try {
          const payment = getPlatformPaymentClient();
          const response = await payment.create({
            body: {
              transaction_amount: Number(amount),
              payment_method_id: "pix",
              description: `${title} - ${description}`,
              external_reference: String(invoiceId),
              notification_url: notificationUrl,
              date_of_expiration: expiresAt,
              payer: { email: payerEmail }
            },
            requestOptions: {
              idempotencyKey: `invoice-pix-${invoiceId}-${Date.now()}`
            }
          });
          const transaction = response.point_of_interaction?.transaction_data;
          if (!response.id || !transaction?.qr_code || !transaction.qr_code_base64) {
            throw new Error("Mercado Pago n\xE3o retornou os dados do Pix.");
          }
          return {
            id: String(response.id),
            status: response.status || null,
            qrCode: transaction.qr_code,
            qrCodeBase64: transaction.qr_code_base64,
            ticketUrl: transaction.ticket_url || null,
            expiresAt: response.date_of_expiration || expiresAt
          };
        } catch (error2) {
          error("failed to create Mercado Pago Pix", {
            invoiceId,
            message: error2 instanceof Error ? error2.message : String(error2)
          });
          throw error2;
        }
      }
    };
    MercadoPagoService_default = new MercadoPagoService();
  }
});

// src/modules/billing/services/RegenerateInvoicePaymentLinkService.ts
var RegenerateInvoicePaymentLinkService, RegenerateInvoicePaymentLinkService_default;
var init_RegenerateInvoicePaymentLinkService = __esm({
  "src/modules/billing/services/RegenerateInvoicePaymentLinkService.ts"() {
    init_BillingRepository();
    init_MercadoPagoService();
    init_billingPaymentWindow();
    RegenerateInvoicePaymentLinkService = class {
      async execute({ invoiceId, restaurantId }) {
        const invoice = await BillingRepository_default.findInvoiceByIdAndRestaurantId(invoiceId, restaurantId);
        if (!invoice) {
          throw new Error("Fatura n\xE3o encontrada para este restaurante.");
        }
        if (!["PENDENTE", "ATRASADO"].includes(invoice.status)) {
          throw new Error("Esta mensalidade n\xE3o est\xE1 dispon\xEDvel para pagamento.");
        }
        if (!isInvoicePixAvailable(invoice)) {
          throw new Error(
            `O Pix desta mensalidade estar\xE1 dispon\xEDvel em ${getPixAvailableAt(invoice.dueDate).toLocaleDateString("pt-BR")}.`
          );
        }
        const payment = await MercadoPagoService_default.createPayment({
          invoiceId: invoice.id,
          title: `Mensalidade restaurante ${invoice.restaurantId}`,
          description: `Fatura ${invoice.month}/${invoice.year}`,
          amount: invoice.total,
          payerEmail: invoice.restaurant.email
        });
        const updatedInvoice = await BillingRepository_default.updateInvoice(invoice.id, {
          paymentLink: payment.ticketUrl,
          paymentExternalId: payment.id,
          pixQrCode: payment.qrCode,
          pixQrCodeBase64: payment.qrCodeBase64,
          pixExpiresAt: payment.expiresAt ? new Date(payment.expiresAt) : null
        });
        return {
          invoice: updatedInvoice,
          paymentLink: payment.ticketUrl,
          pixQrCode: payment.qrCode,
          pixQrCodeBase64: payment.qrCodeBase64,
          pixExpiresAt: payment.expiresAt
        };
      }
    };
    RegenerateInvoicePaymentLinkService_default = new RegenerateInvoicePaymentLinkService();
  }
});

// src/modules/billing/controllers/RegenerateInvoicePaymentLinkController.ts
var RegenerateInvoicePaymentLinkController, RegenerateInvoicePaymentLinkController_default;
var init_RegenerateInvoicePaymentLinkController = __esm({
  "src/modules/billing/controllers/RegenerateInvoicePaymentLinkController.ts"() {
    init_RegenerateInvoicePaymentLinkService();
    RegenerateInvoicePaymentLinkController = class {
      async handle(req, res) {
        try {
          const invoiceId = Number(req.params.id);
          const restaurantId = Number(req.user.restaurantId);
          if (!invoiceId) {
            return res.status(400).json({ error: "Invoice ID inv\xE1lido." });
          }
          const result = await RegenerateInvoicePaymentLinkService_default.execute({
            invoiceId,
            restaurantId
          });
          return res.status(200).json(result);
        } catch (error2) {
          return res.status(400).json({
            error: error2 instanceof Error ? error2.message : "Erro ao regenerar link de pagamento."
          });
        }
      }
    };
    RegenerateInvoicePaymentLinkController_default = new RegenerateInvoicePaymentLinkController();
  }
});

// src/modules/billing/routes/BillingRoutes.ts
import { Router as Router23 } from "express";
var router23, BillingRoutes_default;
var init_BillingRoutes = __esm({
  "src/modules/billing/routes/BillingRoutes.ts"() {
    init_MercadoPagoWebhookController();
    init_BillingWebhookController();
    init_GetInvoicesController();
    init_GetAllInvoicesController();
    init_GetPlansController();
    init_RegenerateInvoicePaymentLinkController();
    init_authMiddleware();
    init_adminMiddleware();
    init_superAdminMiddleware();
    router23 = Router23();
    router23.post("/webhook/mercadopago", MercadoPagoWebhookController_default.handle);
    router23.post("/webhook/mercadopago/test", BillingWebhookController_default.handle);
    router23.get(
      "/plans",
      authMiddleware,
      adminMiddleware,
      (req, res) => GetPlansController_default.handle(req, res)
    );
    router23.get(
      "/invoices",
      authMiddleware,
      adminMiddleware,
      (req, res) => GetInvoicesController_default.handle(req, res)
    );
    router23.get(
      "/invoices/all",
      authMiddleware,
      superAdminMiddleware,
      (req, res) => GetAllInvoicesController_default.handle(req, res)
    );
    router23.post(
      "/invoices/:id/regenerate-link",
      authMiddleware,
      adminMiddleware,
      (req, res) => RegenerateInvoicePaymentLinkController_default.handle(req, res)
    );
    BillingRoutes_default = router23;
  }
});

// src/middlewares/security/requestIdMiddleware.ts
import crypto12 from "crypto";
function requestIdMiddleware(req, res, next) {
  const rawRequestId = req.headers["x-request-id"];
  const requestId = Array.isArray(rawRequestId) ? rawRequestId[0] : rawRequestId || crypto12.randomUUID();
  req.requestId = requestId;
  res.setHeader("X-Request-Id", requestId);
  next();
}
var init_requestIdMiddleware = __esm({
  "src/middlewares/security/requestIdMiddleware.ts"() {
  }
});

// src/middlewares/security/notFoundMiddleware.ts
function notFoundMiddleware(req, res) {
  return res.status(404).json({
    error: "Rota nao encontrada",
    requestId: req.requestId
  });
}
var init_notFoundMiddleware = __esm({
  "src/middlewares/security/notFoundMiddleware.ts"() {
  }
});

// src/services/alertNotifier.ts
import nodemailer3 from "nodemailer";
function resolveProvider2() {
  if (configuredProvider2 !== "generic") {
    return configuredProvider2;
  }
  if (smtpHost && alertEmailTo) {
    return "email";
  }
  if (alertWebhookUrl.includes("discord.com/api/webhooks")) {
    return "discord";
  }
  if (alertWebhookUrl.includes("hooks.slack.com/services/")) {
    return "slack";
  }
  if (alertWebhookUrl.includes("chat.googleapis.com/")) {
    return "google_chat";
  }
  return "generic";
}
function canSendEmail() {
  return Boolean(
    smtpHost && smtpPort > 0 && smtpUser && smtpPass && alertEmailTo && alertEmailFrom
  );
}
function getTransporter() {
  if (cachedTransporter) {
    return cachedTransporter;
  }
  cachedTransporter = nodemailer3.createTransport({
    host: smtpHost,
    port: smtpPort,
    secure: smtpSecure,
    auth: {
      user: smtpUser,
      pass: smtpPass
    }
  });
  return cachedTransporter;
}
function formatDetails(details) {
  if (typeof details === "string") {
    return details;
  }
  return JSON.stringify(details, null, 2);
}
async function sendEmailAlert(title, details) {
  if (!canSendEmail()) {
    return false;
  }
  try {
    await getTransporter().sendMail({
      from: alertEmailFrom,
      to: alertEmailTo,
      subject: `[ALERTA] ${title}`,
      text: `${title}

${formatDetails(details)}`
    });
    return true;
  } catch (emailError) {
    console.error(
      "[ALERT_EMAIL_ERROR]",
      emailError instanceof Error ? emailError.message : String(emailError)
    );
    return false;
  }
}
function buildPayload(title, details) {
  const provider = resolveProvider2();
  const message = `${title}
${formatDetails(details)}`;
  if (provider === "discord") {
    return {
      content: message
    };
  }
  if (provider === "slack") {
    return {
      text: `*${title}*
${details}`
    };
  }
  if (provider === "google_chat") {
    return {
      text: message
    };
  }
  return {
    text: message
  };
}
async function notifyCriticalError(title, details) {
  const provider = resolveProvider2();
  if (provider === "email") {
    const sentByEmail = await sendEmailAlert(title, details);
    if (sentByEmail) {
      return;
    }
  }
  if (!alertWebhookUrl) {
    return;
  }
  try {
    await fetch(alertWebhookUrl, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify(buildPayload(title, details))
    });
  } catch (notificationError) {
    console.error(
      "[ALERT_WEBHOOK_ERROR]",
      notificationError instanceof Error ? notificationError.message : String(notificationError)
    );
  }
}
var alertWebhookUrl, configuredProvider2, alertEmailTo, alertEmailFrom, smtpHost, smtpPort, smtpSecure, smtpUser, smtpPass, cachedTransporter;
var init_alertNotifier = __esm({
  "src/services/alertNotifier.ts"() {
    alertWebhookUrl = process.env.ALERT_WEBHOOK_URL || "";
    configuredProvider2 = (process.env.ALERT_PROVIDER || "generic").trim().toLowerCase();
    alertEmailTo = process.env.ALERT_EMAIL_TO || "";
    alertEmailFrom = process.env.ALERT_EMAIL_FROM || "";
    smtpHost = process.env.SMTP_HOST || "";
    smtpPort = Number(process.env.SMTP_PORT || 587);
    smtpSecure = process.env.SMTP_SECURE === "true";
    smtpUser = process.env.SMTP_USER || "";
    smtpPass = process.env.SMTP_PASS || "";
    cachedTransporter = null;
  }
});

// src/middlewares/security/errorHandlerMiddleware.ts
import * as Sentry from "@sentry/node";
function sanitizedHeaders(headers) {
  return Object.fromEntries(
    Object.entries(headers).map(([key, value]) => [
      key,
      SENSITIVE_HEADERS.has(key.toLowerCase()) ? "[Filtered]" : value
    ])
  );
}
var INTERNAL_SERVER_ERROR_MESSAGE, SENSITIVE_HEADERS, errorHandlerMiddleware;
var init_errorHandlerMiddleware = __esm({
  "src/middlewares/security/errorHandlerMiddleware.ts"() {
    init_alertNotifier();
    INTERNAL_SERVER_ERROR_MESSAGE = "Erro interno do servidor";
    SENSITIVE_HEADERS = /* @__PURE__ */ new Set([
      "authorization",
      "cookie",
      "set-cookie",
      "x-api-key",
      "x-session-token"
    ]);
    errorHandlerMiddleware = (err, req, res, _next) => {
      const errObj = typeof err === "object" && err !== null ? err : {};
      const statusCode = Number(errObj.status || errObj.statusCode || 500);
      const safeStatusCode = statusCode >= 400 && statusCode < 600 ? statusCode : 500;
      if (safeStatusCode >= 500) {
        console.error("[API_ERROR]", {
          requestId: req.requestId,
          method: req.method,
          path: req.originalUrl,
          message: errObj.message,
          stack: errObj.stack
        });
        notifyCriticalError(
          "[CRITICAL_API_ERROR]",
          `requestId=${req.requestId} method=${req.method} path=${req.originalUrl} message=${errObj.message || "unknown"}`
        );
      }
      Sentry.withScope((scope) => {
        scope.setTag("request_id", req.requestId || "unknown");
        scope.setTag("method", req.method || "unknown");
        scope.setTag("path", req.originalUrl || "unknown");
        scope.setContext("request", {
          headers: sanitizedHeaders(req.headers),
          query: req.query,
          params: req.params
        });
        Sentry.captureException(err);
      });
      const message = safeStatusCode >= 500 ? INTERNAL_SERVER_ERROR_MESSAGE : errObj.message || "Erro na requisicao";
      return res.status(safeStatusCode).json({
        error: message,
        requestId: req.requestId
      });
    };
  }
});

// src/middlewares/security/httpAccessProtection.ts
import cors from "cors";
import rateLimit5 from "express-rate-limit";
function resolveGlobalRateLimitMax(isProduction2, configuredMax) {
  return isProduction2 ? configuredMax : Math.max(configuredMax, 5e3);
}
function applyCorsAndGlobalRateLimit(app2) {
  const isProduction2 = process.env.NODE_ENV === "production";
  const allowedOrigins = [process.env.CORS_ORIGINS || "", process.env.FRONTEND_URL || ""].flatMap((value) => value.split(",")).map((origin) => normalizeOrigin(origin)).filter(Boolean);
  const configuredMax = Number(process.env.RATE_LIMIT_MAX_REQUESTS || 300);
  app2.use(
    cors({
      origin: (origin, callback) => {
        if (!origin) {
          callback(null, true);
          return;
        }
        const normalizedOrigin = normalizeOrigin(origin);
        if (!isProduction2 || allowedOrigins.includes(normalizedOrigin)) {
          callback(null, true);
          return;
        }
        callback(new Error("Not allowed by CORS"));
      },
      credentials: true
    })
  );
  app2.use(
    rateLimit5({
      windowMs: Number(process.env.RATE_LIMIT_WINDOW_MS || 15 * 60 * 1e3),
      max: resolveGlobalRateLimitMax(isProduction2, configuredMax),
      standardHeaders: true,
      legacyHeaders: false,
      message: {
        error: "Muitas requisicoes. Tente novamente em instantes."
      }
    })
  );
}
var normalizeOrigin;
var init_httpAccessProtection = __esm({
  "src/middlewares/security/httpAccessProtection.ts"() {
    normalizeOrigin = (value) => value.trim().replace(/\/+$/, "");
  }
});

// src/health/readiness.ts
function readinessTimeoutMs() {
  const parsed = Number(process.env.READINESS_TIMEOUT_MS || 3e3);
  return Number.isInteger(parsed) && parsed > 0 ? parsed : 3e3;
}
async function probeDatabaseReadiness(probe = () => prisma_default.$queryRaw`SELECT 1`, timeoutMs = readinessTimeoutMs()) {
  let timer2;
  try {
    await Promise.race([
      probe(),
      new Promise((_resolve, reject) => {
        timer2 = setTimeout(() => reject(new Error("database readiness timeout")), timeoutMs);
        timer2.unref?.();
      })
    ]);
    return { ready: true };
  } catch {
    return { ready: false };
  } finally {
    if (timer2) clearTimeout(timer2);
  }
}
var init_readiness = __esm({
  "src/health/readiness.ts"() {
    init_prisma();
  }
});

// src/app.ts
import express from "express";
import helmet from "helmet";
import rateLimit6 from "express-rate-limit";
var app, authRateLimit, app_default;
var init_app = __esm({
  "src/app.ts"() {
    init_routes();
    init_BillingRoutes();
    init_requestIdMiddleware();
    init_notFoundMiddleware();
    init_errorHandlerMiddleware();
    init_httpAccessProtection();
    init_readiness();
    app = express();
    authRateLimit = rateLimit6({
      windowMs: Number(process.env.AUTH_RATE_LIMIT_WINDOW_MS || 15 * 60 * 1e3),
      max: Number(process.env.AUTH_RATE_LIMIT_MAX_REQUESTS || 50),
      standardHeaders: true,
      legacyHeaders: false,
      message: {
        error: "Muitas tentativas de autenticacao. Aguarde alguns minutos."
      }
    });
    app.set("trust proxy", 1);
    app.use(requestIdMiddleware);
    app.use(
      helmet({
        contentSecurityPolicy: false,
        crossOriginEmbedderPolicy: false
      })
    );
    app.get("/health", (_req, res) => {
      return res.status(200).json({
        status: "ok",
        service: "pizza-ia-backend",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      });
    });
    app.get("/ready", async (_req, res) => {
      const database = await probeDatabaseReadiness();
      return res.status(database.ready ? 200 : 503).json({
        status: database.ready ? "ready" : "unavailable",
        database: database.ready ? "ok" : "unavailable",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      });
    });
    applyCorsAndGlobalRateLimit(app);
    app.use("/orders/webhook/stripe", express.raw({ type: "application/json" }));
    app.use(express.json({ limit: process.env.MAX_JSON_BODY_SIZE || "1mb" }));
    app.use(express.urlencoded({ extended: true }));
    app.use("/auth", authRateLimit);
    app.use("/billing", BillingRoutes_default);
    app.use(routes_default);
    app.use(notFoundMiddleware);
    app.use(errorHandlerMiddleware);
    app_default = app;
  }
});

// src/config/sentry.ts
import * as Sentry2 from "@sentry/node";
var dsn, environment;
var init_sentry = __esm({
  "src/config/sentry.ts"() {
    dsn = process.env.SENTRY_DSN || "";
    environment = process.env.NODE_ENV || "development";
    if (dsn) {
      Sentry2.init({
        dsn,
        environment,
        tracesSampleRate: Number(process.env.SENTRY_TRACES_SAMPLE_RATE || 0)
      });
    }
  }
});

// src/socket/socketAuth.ts
import jwt9 from "jsonwebtoken";
import { TableSessionStatus as TableSessionStatus11, UserRole as UserRole31 } from "@prisma/client";
async function socketAuth(socket, next) {
  try {
    const token = socket.handshake.auth?.token;
    const sessionToken = socket.handshake.auth?.sessionToken;
    const tableToken = socket.handshake.auth?.tableToken;
    if (token) {
      const decoded = jwt9.verify(token, process.env.JWT_SECRET);
      const normalizedRole = String(decoded.role || "").toUpperCase();
      if (normalizedRole === UserRole31.MOTOQUEIRO || normalizedRole === UserRole31.ADMIN) {
        const accountId = Number(decoded.id || 0);
        const restaurantId = Number(decoded.restaurantId || 0);
        const isCourier = normalizedRole === UserRole31.MOTOQUEIRO;
        if (!Number.isInteger(accountId) || accountId <= 0 || !Number.isInteger(restaurantId) || restaurantId <= 0) {
          return next(new Error(`Conta de ${isCourier ? "motoqueiro" : "administrador"} inv\xE1lida`));
        }
        const activeAccount = await prisma_default.user.findFirst({
          where: {
            id: accountId,
            restaurantId,
            role: isCourier ? UserRole31.MOTOQUEIRO : UserRole31.ADMIN,
            active: true
          },
          select: {
            id: true,
            role: true,
            restaurantId: true
          }
        });
        if (!activeAccount) {
          return next(
            new Error(
              `Conta de ${isCourier ? "motoqueiro" : "administrador"} inativa ou fora do restaurante`
            )
          );
        }
        decoded.id = activeAccount.id;
        decoded.role = activeAccount.role;
        decoded.restaurantId = activeAccount.restaurantId;
      }
      socket.user = decoded;
      socket.authType = "user";
      return next();
    }
    if (sessionToken) {
      const session = await TableSessionRepository_default.findBySessionToken(sessionToken);
      if (!session || session.status !== TableSessionStatus11.OPEN || session.expiresAt && session.expiresAt.getTime() <= Date.now()) {
        return next(new Error("Sess\xE3o da mesa inv\xE1lida"));
      }
      socket.authType = "table-session";
      socket.tableSession = {
        id: session.id,
        tableId: session.tableId,
        tableNumber: session?.table?.number ?? null,
        restaurantId: session?.table?.restaurantId ?? null
      };
      return next();
    }
    if (tableToken) {
      const table = await ResolvePublicTableService_default.execute({
        tableNumber: socket.handshake.auth?.tableNumber,
        tableToken,
        restaurantId: socket.handshake.auth?.restaurantId,
        restaurantSlug: socket.handshake.auth?.restaurantSlug
      });
      socket.authType = "table-waiting";
      socket.waitingTable = {
        id: table.id,
        number: table.number,
        restaurantId: table.restaurantId
      };
      return next();
    }
    return next(new Error("Token n\xE3o enviado"));
  } catch (_error) {
    return next(new Error("Token inv\xE1lido"));
  }
}
var init_socketAuth = __esm({
  "src/socket/socketAuth.ts"() {
    init_TableSessionRepository();
    init_ResolvePublicTableService();
    init_prisma();
  }
});

// src/socket/supportChatPolicy.ts
function normalizeSupportChatRole(role) {
  return String(role || "").trim().toUpperCase();
}
function canSendSupportChat(role) {
  return allowedRoles.has(normalizeSupportChatRole(role));
}
function isOperationalSupportReporter(role) {
  return operationalRoles.has(normalizeSupportChatRole(role));
}
function getSupportMessageSender(role) {
  const normalizedRole = normalizeSupportChatRole(role);
  if (normalizedRole === "MOTOQUEIRO") {
    return { senderRole: normalizedRole, senderLabel: "Motoqueiro" };
  }
  if (normalizedRole === "FUNCIONARIO") {
    return { senderRole: normalizedRole, senderLabel: "Funcion\xE1rio" };
  }
  if (normalizedRole === "SUPER_ADMIN") {
    return { senderRole: normalizedRole, senderLabel: "Super Admin" };
  }
  return { senderRole: "ADMIN", senderLabel: "Admin" };
}
var operationalRoles, allowedRoles;
var init_supportChatPolicy = __esm({
  "src/socket/supportChatPolicy.ts"() {
    operationalRoles = /* @__PURE__ */ new Set(["FUNCIONARIO", "MOTOQUEIRO"]);
    allowedRoles = /* @__PURE__ */ new Set([
      "ADMIN",
      "SUPER_ADMIN",
      "FUNCIONARIO",
      "MOTOQUEIRO"
    ]);
  }
});

// src/socket/employeeIssuePayload.ts
function normalizeText3(value) {
  return String(value || "").replace(/\s+/g, " ").trim();
}
function validateEmployeeIssuePayload(rawPayload) {
  if (rawPayload?.type !== "employee-issue") return { isEmployeeIssue: false };
  const reporterName = normalizeText3(rawPayload.reporterName);
  const reporterRole = normalizeText3(rawPayload.reporterRole);
  const subject = normalizeText3(rawPayload.subject);
  const description = normalizeText3(rawPayload.description);
  if (reporterName.length < 3 || reporterName.length > 100) {
    return { isEmployeeIssue: true, ok: false, error: "Informe seu nome para enviar o relato." };
  }
  const reporterRoleLabel = reporterRole === "kitchen" ? "Cozinheiro" : reporterRole === "waiter" ? "Gar\xE7om" : reporterRole === "courier" ? "Motoqueiro" : null;
  if (!reporterRoleLabel) {
    return { isEmployeeIssue: true, ok: false, error: "Fun\xE7\xE3o do funcion\xE1rio inv\xE1lida." };
  }
  if (subject.length < 3 || subject.length > 100) {
    return { isEmployeeIssue: true, ok: false, error: "Informe um assunto v\xE1lido para o relato." };
  }
  if (description.length < 5 || description.length > 900) {
    return {
      isEmployeeIssue: true,
      ok: false,
      error: "Explique o problema com pelo menos 5 caracteres (m\xE1x. 900)."
    };
  }
  return {
    isEmployeeIssue: true,
    ok: true,
    reporterName,
    message: `Relato de problema
Remetente: ${reporterName} (${reporterRoleLabel})
Assunto: ${subject}
Descri\xE7\xE3o: ${description}`
  };
}
var init_employeeIssuePayload = __esm({
  "src/socket/employeeIssuePayload.ts"() {
  }
});

// src/socket/socketHandler.ts
import { OrderStatus as OrderStatus21, OrderType as OrderType10, UserRole as UserRole32 } from "@prisma/client";
function socketHandler(socket) {
  console.log("\u{1F50C} conectado:", socket.id);
  if (socket.authType === "table-session" && socket.tableSession) {
    const { id: id2, tableId, restaurantId: restaurantId2 } = socket.tableSession;
    socket.join(`table:${tableId}`);
    socket.join(`table-session:${id2}`);
    socket.on("disconnect", () => {
      console.log("\u274C desconectado:", socket.id);
    });
    return;
  }
  if (socket.authType === "table-waiting" && socket.waitingTable) {
    socket.join(`table-waiting:${socket.waitingTable.id}`);
    socket.on("disconnect", () => {
      console.log("\u274C desconectado:", socket.id);
    });
    return;
  }
  const user = socket.user;
  if (!user) {
    socket.disconnect(true);
    return;
  }
  const { id, role, subRole, restaurantId } = user;
  const lastLocationStoredAtByOrder = /* @__PURE__ */ new Map();
  let accountValidationTimer = null;
  socket.join(`user:${id}`);
  if (role === "FUNCIONARIO" && String(subRole || "").toUpperCase() === "COZINHA") {
    socket.join(`restaurant:${restaurantId}`);
    socket.join(`restaurant:${restaurantId}:kitchen`);
  }
  if (role === "FUNCIONARIO" && String(subRole || "").toUpperCase() === "GARCOM") {
    socket.join(`restaurant:${restaurantId}:waiter`);
  }
  if (role === "MOTOQUEIRO") {
    socket.join(`restaurant:${restaurantId}`);
    socket.join(`restaurant:${restaurantId}:courier`);
  }
  if (role === "ADMIN") {
    socket.join(`restaurant:${restaurantId}`);
    socket.join("admin");
    socket.join(`restaurant:${restaurantId}:admin`);
    accountValidationTimer = setInterval(() => {
      void prisma_default.user.findFirst({
        where: {
          id: Number(id || 0),
          restaurantId: Number(restaurantId || 0),
          role: UserRole32.ADMIN,
          active: true
        },
        select: { id: true }
      }).then((activeAdmin) => {
        if (!activeAdmin) socket.disconnect(true);
      }).catch((error2) => {
        console.warn("[SOCKET_ACCOUNT_REVALIDATION_FAILED]", {
          userId: Number(id || 0),
          restaurantId: Number(restaurantId || 0),
          error: error2 instanceof Error ? error2.message : String(error2)
        });
      });
    }, 6e4);
    accountValidationTimer.unref();
  }
  if (role === "SUPER_ADMIN") {
    socket.join("admin");
    socket.join("super_admin");
  }
  socket.on("delivery:location:update", async (rawPayload, ack) => {
    const reply = typeof ack === "function" ? ack : (_result) => {
    };
    let locationOrderId = 0;
    try {
      if (String(role || "").toUpperCase() !== "MOTOQUEIRO") {
        reply({
          ok: false,
          error: "Somente motoqueiros podem enviar localiza\xE7\xE3o."
        });
        return;
      }
      const receivedAt = Date.now();
      const validation = validateDeliveryLocationPayload(rawPayload, receivedAt);
      if ("error" in validation) {
        reply({ ok: false, error: validation.error });
        return;
      }
      const location = validation.value;
      locationOrderId = location.orderId;
      const lastStoredAt = lastLocationStoredAtByOrder.get(location.orderId) || 0;
      if (receivedAt - lastStoredAt < 3e3) {
        reply({ ok: true, throttled: true });
        return;
      }
      const order = await prisma_default.order.findUnique({
        where: { id: location.orderId },
        select: {
          id: true,
          userId: true,
          restaurantId: true,
          type: true,
          status: true,
          assignedCourierId: true,
          assignedCourier: {
            select: {
              id: true,
              restaurantId: true,
              role: true,
              active: true
            }
          }
        }
      });
      if (!order) {
        reply({ ok: false, error: "Pedido n\xE3o encontrado." });
        return;
      }
      if (Number(order.restaurantId || 0) !== Number(restaurantId || 0)) {
        reply({ ok: false, error: "Pedido n\xE3o pertence ao seu restaurante." });
        return;
      }
      if (String(order.type || "").toUpperCase() !== "DELIVERY") {
        reply({ ok: false, error: "Rastreio dispon\xEDvel apenas para delivery." });
        return;
      }
      if (String(order.status || "").toUpperCase() !== "SAIU_PARA_ENTREGA") {
        reply({ ok: false, error: "Rastreio dispon\xEDvel apenas em entrega." });
        return;
      }
      const assignedCourier = order.assignedCourier;
      const isAuthenticatedCourier = Number(order.assignedCourierId || 0) === Number(id || 0) && Number(assignedCourier?.id || 0) === Number(id || 0) && Number(assignedCourier?.restaurantId || 0) === Number(restaurantId || 0) && String(assignedCourier?.role || "").toUpperCase() === "MOTOQUEIRO" && assignedCourier?.active === true;
      if (!isAuthenticatedCourier) {
        reply({ ok: false, error: "Esta entrega n\xE3o est\xE1 atribu\xEDda \xE0 sua conta ativa." });
        return;
      }
      const lastAuthorizedUpdateAt = lastLocationStoredAtByOrder.get(location.orderId) || 0;
      if (receivedAt - lastAuthorizedUpdateAt < 3e3) {
        reply({ ok: true, throttled: true });
        return;
      }
      lastLocationStoredAtByOrder.set(location.orderId, receivedAt);
      const savedLocation = await prisma_default.$transaction(async (tx) => {
        const lockedOrders = await tx.$queryRaw`
          SELECT o."id"
          FROM "Order" AS o
          INNER JOIN "User" AS courier ON courier."id" = o."assignedCourierId"
          WHERE o."id" = ${order.id}
            AND o."restaurantId" = ${Number(restaurantId)}
            AND o."type" = CAST(${OrderType10.DELIVERY} AS "OrderType")
            AND o."status" = CAST(${OrderStatus21.SAIU_PARA_ENTREGA} AS "OrderStatus")
            AND o."assignedCourierId" = ${Number(id)}
            AND courier."restaurantId" = ${Number(restaurantId)}
            AND courier."role" = CAST(${UserRole32.MOTOQUEIRO} AS "UserRole")
            AND courier."active" = TRUE
          FOR UPDATE OF o, courier
        `;
        if (lockedOrders.length !== 1) return null;
        return tx.deliveryLocation.create({
          data: {
            orderId: order.id,
            courierId: Number(id),
            latitude: location.latitude,
            longitude: location.longitude,
            heading: location.heading,
            speed: location.speed,
            accuracy: location.accuracy,
            recordedAt: location.recordedAt
          },
          select: { recordedAt: true }
        });
      });
      if (!savedLocation) {
        lastLocationStoredAtByOrder.delete(location.orderId);
        reply({
          ok: false,
          error: "A entrega foi encerrada ou n\xE3o est\xE1 mais atribu\xEDda \xE0 sua conta."
        });
        return;
      }
      const payload = {
        orderId: order.id,
        restaurantId: order.restaurantId,
        latitude: location.latitude,
        longitude: location.longitude,
        heading: location.heading,
        speed: location.speed,
        accuracy: location.accuracy,
        sentAt: location.sentAt,
        recordedAt: savedLocation.recordedAt.toISOString(),
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      socket.to(`user:${order.userId}`).emit("order:delivery-location", payload);
      socket.to(`restaurant:${order.restaurantId}:admin`).emit("order:delivery-location", payload);
      reply({ ok: true });
    } catch (error2) {
      if (locationOrderId > 0) {
        lastLocationStoredAtByOrder.delete(locationOrderId);
      }
      console.error("[DELIVERY_LOCATION_UPDATE_FAILED]", {
        orderId: locationOrderId || null,
        courierId: Number(id || 0),
        restaurantId: Number(restaurantId || 0),
        error: error2 instanceof Error ? error2.message : String(error2)
      });
      reply({
        ok: false,
        error: "N\xE3o foi poss\xEDvel atualizar a localiza\xE7\xE3o agora. Tente novamente."
      });
    }
  });
  socket.on("support:chat-send", async (rawPayload, ack) => {
    const reply = typeof ack === "function" ? ack : (_result) => {
    };
    const normalizedRole = normalizeSupportChatRole(role);
    const isOperationalRole = isOperationalSupportReporter(normalizedRole);
    if (!canSendSupportChat(normalizedRole)) {
      reply({ ok: false, error: "Sem permiss\xE3o para usar este chat." });
      return;
    }
    const employeeIssue = validateEmployeeIssuePayload(rawPayload || {});
    let employeeIssueMessage = null;
    let employeeIssueReporterName = null;
    if (employeeIssue.isEmployeeIssue) {
      if (!isOperationalRole) {
        reply({
          ok: false,
          error: "Esse tipo de relato est\xE1 dispon\xEDvel apenas para funcion\xE1rios."
        });
        return;
      }
      if ("error" in employeeIssue) {
        reply({ ok: false, error: employeeIssue.error });
        return;
      }
      employeeIssueMessage = employeeIssue.message;
      employeeIssueReporterName = employeeIssue.reporterName;
    }
    const normalizedMessage = employeeIssueMessage ? employeeIssueMessage : String(rawPayload?.message || "").replace(/\s+/g, " ").trim();
    if (normalizedMessage.length < 2) {
      reply({ ok: false, error: "Digite uma mensagem v\xE1lida." });
      return;
    }
    if (normalizedMessage.length > 1200) {
      reply({ ok: false, error: "Mensagem muito longa (m\xE1x. 1200)." });
      return;
    }
    let targetRestaurantId = Number(restaurantId || 0);
    if (normalizedRole === "SUPER_ADMIN") {
      targetRestaurantId = Number(rawPayload?.restaurantId || 0);
      if (!Number.isInteger(targetRestaurantId) || targetRestaurantId <= 0) {
        reply({
          ok: false,
          error: "Informe o restaurante para falar com o admin."
        });
        return;
      }
    }
    if (!Number.isInteger(targetRestaurantId) || targetRestaurantId <= 0) {
      reply({ ok: false, error: "Restaurante inv\xE1lido para este chat." });
      return;
    }
    if (isOperationalRole) {
      const activeEmployee = await prisma_default.user.findFirst({
        where: {
          id: Number(id || 0),
          restaurantId: targetRestaurantId,
          role: normalizedRole,
          active: true
        },
        select: { id: true }
      });
      if (!activeEmployee) {
        reply({
          ok: false,
          error: "Seu acesso n\xE3o est\xE1 vinculado a este restaurante. Entre novamente ou fale com o administrador."
        });
        return;
      }
    }
    if (!isOperationalRole) {
      const subscription = await prisma_default.subscription.findUnique({
        where: {
          restaurantId: targetRestaurantId
        },
        select: {
          plan: true
        }
      });
      const plan = String(subscription?.plan || "").toUpperCase();
      const supportChatEnabledPlan = plan === "BASICO" || plan === "PREMIUM";
      if (!supportChatEnabledPlan) {
        reply({
          ok: false,
          error: "Chat com Super Admin dispon\xEDvel nos planos ativos do sistema."
        });
        return;
      }
    }
    let savedMessage;
    const { senderRole: senderRoleValue, senderLabel: senderLabelValue } = getSupportMessageSender(normalizedRole);
    const senderLabel = employeeIssueReporterName ? `${senderLabelValue} \xB7 ${employeeIssueReporterName}` : senderLabelValue;
    try {
      const insertedRows = await prisma_default.$queryRaw`
        INSERT INTO "SupportChatMessage" (
          "restaurantId",
          "senderUserId",
          "senderRole",
          "senderLabel",
          "message",
          "issueStatus"
        )
        VALUES (
          ${targetRestaurantId},
          ${Number(id || 0) || null},
          CAST(${senderRoleValue} AS "SupportChatSenderRole"),
          ${senderLabel},
          ${normalizedMessage},
          ${employeeIssueReporterName ? "OPEN" : null}
        )
        RETURNING
          "id",
          "message",
          "senderRole",
          "senderUserId",
          "senderLabel",
          "restaurantId",
          "sentAt"
      `;
      savedMessage = insertedRows[0] || null;
      if (!savedMessage) {
        reply({ ok: false, error: "N\xE3o foi poss\xEDvel salvar a mensagem." });
        return;
      }
    } catch (error2) {
      console.error("Erro ao salvar relato no chat de suporte:", {
        error: error2,
        restaurantId: targetRestaurantId,
        senderRole: senderRoleValue,
        isEmployeeIssue: Boolean(employeeIssueReporterName)
      });
      reply({
        ok: false,
        error: "N\xE3o foi poss\xEDvel registrar o relato agora. Tente novamente em instantes."
      });
      return;
    }
    const payload = {
      id: String(savedMessage.id),
      message: savedMessage.message,
      senderRole: savedMessage.senderRole,
      senderUserId: Number(savedMessage.senderUserId || 0) || 0,
      senderLabel: savedMessage.senderLabel,
      issueStatus: employeeIssueReporterName ? "OPEN" : null,
      restaurantId: savedMessage.restaurantId,
      sentAt: savedMessage.sentAt?.toISOString?.() || (/* @__PURE__ */ new Date()).toISOString()
    };
    socket.to(`user:${id}`).emit("support:chat-message", payload);
    socket.emit("support:chat-message", payload);
    if (isOperationalRole) {
      socket.to(`restaurant:${targetRestaurantId}:admin`).emit("support:chat-message", payload);
      reply({ ok: true });
      return;
    }
    if (normalizedRole === "ADMIN") {
      socket.to("super_admin").emit("support:chat-message", payload);
      reply({ ok: true });
      return;
    }
    socket.to(`restaurant:${targetRestaurantId}:admin`).emit("support:chat-message", payload);
    socket.to("super_admin").emit("support:chat-message", payload);
    reply({ ok: true });
  });
  socket.on("disconnect", () => {
    if (accountValidationTimer) clearInterval(accountValidationTimer);
    console.log("\u274C desconectado:", socket.id);
  });
}
var init_socketHandler = __esm({
  "src/socket/socketHandler.ts"() {
    init_prisma();
    init_supportChatPolicy();
    init_employeeIssuePayload();
    init_deliveryLocationPayload();
  }
});

// src/modules/billing/services/InvoiceService.ts
var InvoiceService, InvoiceService_default;
var init_InvoiceService = __esm({
  "src/modules/billing/services/InvoiceService.ts"() {
    init_BillingRepository();
    init_planConfig();
    init_dateUtils();
    InvoiceService = class {
      async execute({ restaurantId, month, year, startDate, endDate }) {
        const subscription = await BillingRepository_default.findSubscriptionByRestaurantId(restaurantId);
        if (!subscription) {
          throw new Error("Assinatura n\xE3o encontrada.");
        }
        let activePlan = subscription.plan;
        const shouldApplyScheduledPlan = subscription.scheduledPlan && subscription.scheduledPlanEffectiveMonth === month && subscription.scheduledPlanEffectiveYear === year;
        if (shouldApplyScheduledPlan) {
          const updatedSubscription = await BillingRepository_default.updateSubscription(subscription.id, {
            plan: subscription.scheduledPlan,
            scheduledPlan: null,
            scheduledPlanEffectiveMonth: null,
            scheduledPlanEffectiveYear: null
          });
          activePlan = updatedSubscription.plan;
        }
        const plan = PLAN_CONFIG[activePlan];
        if (!plan) {
          throw new Error("Plano inv\xE1lido.");
        }
        const invoiceExists = await BillingRepository_default.findInvoiceByMonth(restaurantId, month, year);
        if (invoiceExists) {
          return invoiceExists;
        }
        const total = plan.monthlyFee;
        const trialEndsAtDate = subscription.trialEndsAt ? new Date(subscription.trialEndsAt) : null;
        const dueDate = subscription.status === "TESTE" && trialEndsAtDate && !Number.isNaN(trialEndsAtDate.getTime()) ? trialEndsAtDate : addDays(/* @__PURE__ */ new Date(), 30);
        return BillingRepository_default.createInvoice({
          restaurantId,
          month,
          year,
          monthlyFee: plan.monthlyFee,
          systemFees: 0,
          total,
          dueDate,
          status: "PENDENTE"
        });
      }
    };
    InvoiceService_default = new InvoiceService();
  }
});

// src/modules/billing/services/TrialService.ts
var TrialService, TrialService_default;
var init_TrialService = __esm({
  "src/modules/billing/services/TrialService.ts"() {
    init_BillingRepository();
    init_InvoiceService();
    TrialService = class {
      async execute() {
        const subscriptions = await BillingRepository_default.findExpiredTrials();
        for (const subscription of subscriptions) {
          const today = /* @__PURE__ */ new Date();
          const month = today.getMonth() + 1;
          const year = today.getFullYear();
          const startDate = new Date(year, month - 1, 1);
          const endDate = new Date(year, month, 0);
          await InvoiceService_default.execute({
            restaurantId: subscription.restaurantId,
            month,
            year,
            startDate,
            endDate
          });
          await BillingRepository_default.updateSubscription(subscription.id, {
            status: "ATIVA"
          });
        }
      }
    };
    TrialService_default = new TrialService();
  }
});

// src/modules/billing/jobs/BillingJob.ts
var BillingJob, BillingJob_default;
var init_BillingJob = __esm({
  "src/modules/billing/jobs/BillingJob.ts"() {
    init_prisma();
    init_TrialService();
    init_InvoiceService();
    init_BillingRepository();
    init_billingRules();
    init_billingLogger();
    BillingJob = class {
      async execute() {
        info("BillingJob started");
        const now = /* @__PURE__ */ new Date();
        try {
          await TrialService_default.execute();
        } catch (err) {
          error("failed to process trial service", {
            message: err?.message || String(err)
          });
        }
        const activeSubscriptions = await prisma_default.subscription.findMany({
          where: { status: "ATIVA" }
        });
        debug("active subscriptions to process", {
          count: activeSubscriptions.length
        });
        const month = now.getMonth() + 1;
        const year = now.getFullYear();
        const startDate = new Date(year, month - 1, 1);
        const endDate = new Date(year, month, 0);
        for (const sub of activeSubscriptions) {
          try {
            await InvoiceService_default.execute({
              restaurantId: sub.restaurantId,
              month,
              year,
              startDate,
              endDate
            });
          } catch (err) {
            error("failed to process restaurant billing", {
              restaurantId: sub.restaurantId,
              message: err?.message || String(err)
            });
          }
        }
        try {
          const pendingInvoices = await BillingRepository_default.findPendingInvoices();
          for (const invoice of pendingInvoices) {
            const shouldBlock = isInvoiceBlocking(invoice, now);
            if (!shouldBlock) {
              continue;
            }
            warn("applying block for overdue invoice", {
              invoiceId: invoice.id,
              dueDate: invoice.dueDate
            });
            await BillingRepository_default.updateInvoice(invoice.id, {
              status: "ATRASADO"
            });
            const subscription = await BillingRepository_default.findSubscriptionByRestaurantId(
              invoice.restaurantId
            );
            if (subscription) {
              await BillingRepository_default.updateSubscription(subscription.id, {
                status: "EXPIRADA"
              });
            }
            await BillingRepository_default.deactivateRestaurant(invoice.restaurantId);
          }
        } catch (err) {
          error("failed to process overdue invoices", {
            message: err?.message || String(err)
          });
        }
        info("BillingJob finished");
      }
    };
    BillingJob_default = new BillingJob();
  }
});

// src/modules/billing/services/ReconcileMercadoPagoInvoicesService.ts
var ReconcileMercadoPagoInvoicesService, ReconcileMercadoPagoInvoicesService_default;
var init_ReconcileMercadoPagoInvoicesService = __esm({
  "src/modules/billing/services/ReconcileMercadoPagoInvoicesService.ts"() {
    init_BillingRepository();
    init_ProcessPaymentService();
    init_webhookUtils();
    init_billingLogger();
    init_platformMercadoPago();
    ReconcileMercadoPagoInvoicesService = class {
      isEnabled() {
        const enabled = String(process.env.MP_AUTO_RECONCILE_ENABLED || "true").toLowerCase();
        return enabled !== "false";
      }
      getAccessToken() {
        return getPlatformMercadoPagoAccessToken();
      }
      getApiBaseUrl() {
        return String(process.env.MP_API_BASE_URL || "https://api.mercadopago.com").trim();
      }
      getMaxInvoices() {
        const parsed = Number(process.env.MP_AUTO_RECONCILE_MAX_INVOICES || 50);
        if (!Number.isFinite(parsed) || parsed <= 0) {
          return 50;
        }
        return Math.floor(parsed);
      }
      async fetchLatestPaymentStatus(invoiceId, accessToken) {
        const searchUrl = new URL(`${this.getApiBaseUrl()}/v1/payments/search`);
        searchUrl.searchParams.set("external_reference", String(invoiceId));
        searchUrl.searchParams.set("sort", "date_created");
        searchUrl.searchParams.set("criteria", "desc");
        searchUrl.searchParams.set("limit", "1");
        const response = await fetch(searchUrl.toString(), {
          method: "GET",
          headers: {
            Authorization: `Bearer ${accessToken}`,
            "Content-Type": "application/json"
          }
        });
        const payload = await response.json().catch(() => ({}));
        if (!response.ok) {
          throw new Error(`MP search failed [${response.status}] for invoice ${invoiceId}`);
        }
        return {
          paymentId: payload?.results?.[0]?.id || null,
          status: String(payload?.results?.[0]?.status || "").trim()
        };
      }
      async execute() {
        if (!this.isEnabled()) {
          debug("MP auto reconciliation disabled");
          return;
        }
        const accessToken = this.getAccessToken();
        if (!accessToken) {
          warn("MP auto reconciliation skipped: missing PLATFORM_MP_ACCESS_TOKEN");
          return;
        }
        const pendingInvoices = await BillingRepository_default.findPendingInvoices();
        const invoicesToProcess = pendingInvoices.filter((invoice) => String(invoice.paymentLink || "").trim()).slice(0, this.getMaxInvoices());
        if (!invoicesToProcess.length) {
          debug("MP auto reconciliation: no pending invoices");
          return;
        }
        info("MP auto reconciliation started", {
          pendingCount: pendingInvoices.length,
          processingCount: invoicesToProcess.length
        });
        for (const invoice of invoicesToProcess) {
          try {
            const payment = await this.fetchLatestPaymentStatus(invoice.id, accessToken);
            if (!payment.paymentId) {
              debug("MP auto reconciliation: payment not found", {
                invoiceId: invoice.id
              });
              continue;
            }
            if (!isApprovedPaymentStatus(payment.status)) {
              debug("MP auto reconciliation: payment not approved", {
                invoiceId: invoice.id,
                paymentId: payment.paymentId,
                status: payment.status
              });
              continue;
            }
            await ProcessPaymentService_default.execute({ invoiceId: invoice.id });
            info("MP auto reconciliation: invoice paid", {
              invoiceId: invoice.id,
              paymentId: payment.paymentId,
              status: payment.status
            });
          } catch (err) {
            error("MP auto reconciliation failed for invoice", {
              invoiceId: invoice.id,
              message: err instanceof Error ? err.message : String(err)
            });
          }
        }
        info("MP auto reconciliation finished");
      }
    };
    ReconcileMercadoPagoInvoicesService_default = new ReconcileMercadoPagoInvoicesService();
  }
});

// src/modules/orders/jobs/DeliveryLocationCleanupJob.ts
var DeliveryLocationCleanupJob, DeliveryLocationCleanupJob_default;
var init_DeliveryLocationCleanupJob = __esm({
  "src/modules/orders/jobs/DeliveryLocationCleanupJob.ts"() {
    init_prisma();
    DeliveryLocationCleanupJob = class {
      async execute() {
        const configuredDays = Number(process.env.DELIVERY_LOCATION_RETENTION_DAYS || 30);
        const retentionDays = Number.isFinite(configuredDays) ? Math.min(Math.max(Math.floor(configuredDays), 1), 365) : 30;
        const cutoff = new Date(Date.now() - retentionDays * 864e5);
        return prisma_default.deliveryLocation.deleteMany({
          where: { recordedAt: { lt: cutoff } }
        });
      }
    };
    DeliveryLocationCleanupJob_default = new DeliveryLocationCleanupJob();
  }
});

// src/modules/coupon/jobs/LoyaltyRedemptionExpirationJob.ts
var LoyaltyRedemptionExpirationJob, LoyaltyRedemptionExpirationJob_default;
var init_LoyaltyRedemptionExpirationJob = __esm({
  "src/modules/coupon/jobs/LoyaltyRedemptionExpirationJob.ts"() {
    init_CouponRepository();
    LoyaltyRedemptionExpirationJob = class {
      async execute(now = /* @__PURE__ */ new Date()) {
        return CouponRepository_default.expireClaimedRedemptions({ now });
      }
    };
    LoyaltyRedemptionExpirationJob_default = new LoyaltyRedemptionExpirationJob();
  }
});

// src/modules/billing/jobs/scheduler.ts
import cron from "node-cron";
function schedule(...args) {
  const task = cron.schedule(...args);
  scheduledTasks.push(task);
  return task;
}
function startJobs() {
  if (scheduledTasks.length > 0) return;
  schedule(
    "0 0 * * *",
    async () => {
      info("scheduler triggered BillingJob");
      try {
        await BillingJob_default.execute();
      } catch (err) {
        error("BillingJob execution failed", {
          message: err?.message || String(err)
        });
      }
    },
    {
      timezone: "America/Sao_Paulo"
    }
  );
  schedule(
    process.env.BILLING_MP_RECONCILE_CRON || "*/5 * * * *",
    async () => {
      info("scheduler triggered MP auto reconciliation");
      try {
        await ReconcileMercadoPagoInvoicesService_default.execute();
      } catch (err) {
        error("MP auto reconciliation execution failed", {
          message: err?.message || String(err)
        });
      }
    },
    {
      timezone: "America/Sao_Paulo"
    }
  );
  schedule(
    process.env.LOYALTY_REDEMPTION_EXPIRATION_CRON || "*/5 * * * *",
    async () => {
      try {
        const result = await LoyaltyRedemptionExpirationJob_default.execute();
        if (result.count > 0) {
          info("expired loyalty rewards updated", { count: result.count });
        }
      } catch (err) {
        error("loyalty reward expiration failed", {
          message: err instanceof Error ? err.message : String(err)
        });
      }
    },
    { timezone: "America/Sao_Paulo" }
  );
  schedule(
    "30 3 * * *",
    async () => {
      try {
        const result = await DeliveryLocationCleanupJob_default.execute();
        info("old delivery locations removed", { count: result.count });
      } catch (err) {
        error("delivery location cleanup failed", {
          message: err instanceof Error ? err.message : String(err)
        });
      }
    },
    { timezone: "America/Sao_Paulo" }
  );
}
function stopJobs() {
  while (scheduledTasks.length) {
    const task = scheduledTasks.pop();
    task?.stop();
    task?.destroy();
  }
}
var scheduledTasks;
var init_scheduler = __esm({
  "src/modules/billing/jobs/scheduler.ts"() {
    init_BillingJob();
    init_ReconcileMercadoPagoInvoicesService();
    init_billingLogger();
    init_DeliveryLocationCleanupJob();
    init_LoyaltyRedemptionExpirationJob();
    scheduledTasks = [];
  }
});

// src/modules/tableAccount/jobs/TablePaymentReservationExpirationJob.ts
import { Prisma as Prisma17, TablePaymentIntentStatus as TablePaymentIntentStatus8 } from "@prisma/client";
var TablePaymentReservationExpirationJob, TablePaymentReservationExpirationJob_default;
var init_TablePaymentReservationExpirationJob = __esm({
  "src/modules/tableAccount/jobs/TablePaymentReservationExpirationJob.ts"() {
    init_prisma();
    init_tablePaymentLedger();
    TablePaymentReservationExpirationJob = class {
      async execute(now = /* @__PURE__ */ new Date()) {
        const candidates = await prisma_default.tablePaymentIntent.findMany({
          where: {
            status: {
              in: [TablePaymentIntentStatus8.RESERVED, TablePaymentIntentStatus8.PROCESSING]
            },
            expiresAt: { lte: now }
          },
          select: {
            restaurantId: true,
            tableSessionId: true
          },
          distinct: ["restaurantId", "tableSessionId"],
          take: 200
        });
        let expiredCount = 0;
        for (const candidate of candidates) {
          try {
            expiredCount += await prisma_default.$transaction(
              async (tx) => {
                await lockTablePaymentSession(tx, candidate.restaurantId, candidate.tableSessionId);
                return expireTablePaymentReservations(
                  tx,
                  candidate.restaurantId,
                  candidate.tableSessionId,
                  now
                );
              },
              { isolationLevel: Prisma17.TransactionIsolationLevel.Serializable }
            );
          } catch (error2) {
            console.error("[TABLE_PAYMENT_EXPIRATION_ERROR]", {
              restaurantId: candidate.restaurantId,
              tableSessionId: candidate.tableSessionId,
              error: error2 instanceof Error ? error2.name : "UNKNOWN_ERROR"
            });
          }
        }
        return { sessionsChecked: candidates.length, expiredCount };
      }
    };
    TablePaymentReservationExpirationJob_default = new TablePaymentReservationExpirationJob();
  }
});

// src/modules/tableAccount/jobs/tablePaymentScheduler.ts
function startTablePaymentJobs() {
  if (timer) {
    return;
  }
  const configuredInterval = Number(process.env.TABLE_PAYMENT_EXPIRATION_INTERVAL_MS || 6e4);
  const intervalMs = Number.isSafeInteger(configuredInterval) && configuredInterval >= 1e4 ? configuredInterval : 6e4;
  timer = setInterval(() => {
    TablePaymentReservationExpirationJob_default.execute().catch((error2) => {
      console.error(
        "[TABLE_PAYMENT_EXPIRATION_JOB_ERROR]",
        error2 instanceof Error ? error2.name : "UNKNOWN_ERROR"
      );
    });
  }, intervalMs);
  timer.unref();
}
function stopTablePaymentJobs() {
  if (timer) {
    clearInterval(timer);
    timer = null;
  }
}
var timer;
var init_tablePaymentScheduler = __esm({
  "src/modules/tableAccount/jobs/tablePaymentScheduler.ts"() {
    init_TablePaymentReservationExpirationJob();
    timer = null;
  }
});

// src/server.ts
var server_exports = {};
__export(server_exports, {
  io: () => io
});
import "dotenv/config";
import http from "http";
import { Server } from "socket.io";
async function shutdown(signal, exitCode = 0) {
  if (shuttingDown) return;
  shuttingDown = true;
  console.info("[SHUTDOWN_STARTED]", { signal });
  stopJobs();
  stopTablePaymentJobs();
  const configuredTimeout = Number(process.env.SHUTDOWN_TIMEOUT_MS || 1e4);
  const shutdownTimeoutMs = Number.isInteger(configuredTimeout) && configuredTimeout > 0 ? configuredTimeout : 1e4;
  const forceExitTimer = setTimeout(() => {
    console.error("[SHUTDOWN_TIMEOUT]", { signal });
    process.exit(1);
  }, shutdownTimeoutMs);
  forceExitTimer.unref();
  try {
    io.disconnectSockets(true);
    await new Promise((resolve) => io.close(() => resolve()));
    server.closeIdleConnections?.();
    await prisma_default.$disconnect();
    await Sentry2.flush(2e3);
    clearTimeout(forceExitTimer);
    console.info("[SHUTDOWN_COMPLETED]", { signal });
    process.exit(exitCode);
  } catch (error2) {
    clearTimeout(forceExitTimer);
    console.error("[SHUTDOWN_FAILED]", error2);
    Sentry2.captureException(error2);
    process.exit(1);
  }
}
var server, port, isProduction, normalizeOrigin2, socketAllowedOrigins, io, shuttingDown;
var init_server = __esm({
  "src/server.ts"() {
    init_validateEnvOnStartup();
    init_app();
    init_sentry();
    init_socketAuth();
    init_socketHandler();
    init_scheduler();
    init_BillingJob();
    init_ReconcileMercadoPagoInvoicesService();
    init_alertNotifier();
    init_prisma();
    init_tablePaymentScheduler();
    server = http.createServer(app_default);
    port = Number(process.env.PORT) || 3e3;
    isProduction = process.env.NODE_ENV === "production";
    normalizeOrigin2 = (value) => value.trim().replace(/\/+$/, "");
    socketAllowedOrigins = [
      process.env.SOCKET_CORS_ORIGINS || "",
      process.env.CORS_ORIGINS || "",
      process.env.FRONTEND_URL || ""
    ].flatMap((value) => value.split(",")).map((origin) => normalizeOrigin2(origin)).filter(Boolean);
    io = new Server(server, {
      cors: {
        origin: isProduction ? socketAllowedOrigins : "*",
        methods: ["GET", "POST"],
        credentials: true
      },
      transports: ["polling", "websocket"]
    });
    io.use(socketAuth);
    io.on("connection", socketHandler);
    server.listen(port, "0.0.0.0", () => {
      console.log(`Servidor rodando na porta ${port}`);
      startJobs();
      startTablePaymentJobs();
      BillingJob_default.execute().catch((error2) => {
        console.error(error2);
        Sentry2.captureException(error2);
      });
      ReconcileMercadoPagoInvoicesService_default.execute().catch((error2) => {
        console.error(error2);
        Sentry2.captureException(error2);
      });
    });
    shuttingDown = false;
    process.once("SIGTERM", () => void shutdown("SIGTERM"));
    process.once("SIGINT", () => void shutdown("SIGINT"));
    process.on("unhandledRejection", (reason) => {
      console.error("[UNHANDLED_REJECTION]", reason);
      Sentry2.captureException(reason instanceof Error ? reason : new Error(String(reason)));
      notifyCriticalError("[UNHANDLED_REJECTION]", String(reason));
    });
    process.on("uncaughtException", (error2) => {
      console.error("[UNCAUGHT_EXCEPTION]", error2);
      Sentry2.captureException(error2);
      notifyCriticalError("[UNCAUGHT_EXCEPTION]", error2?.message || "unknown");
      void shutdown("SIGTERM", 1);
    });
  }
});
init_server();
export {
  io
};
